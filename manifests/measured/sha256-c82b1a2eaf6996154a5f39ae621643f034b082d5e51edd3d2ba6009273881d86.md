# measured compose @ c82b1a2e... (GPU-node measured app_compose — gpu04, the second CVM behind `glm-5-3-flash`: byte-identical to 341313ae except the host label)

## verdict: QUALIFIED PASS at the manifest layer — this document is the 341313ae harness with one line changed (INSTANCE_LABEL=gpu30 to gpu04, a Slack-notification string that reaches no telemetry attribute and no content path), so every base finding carries over at the same line numbers: the management-only collector is fail-closed twice over, both control-plane images resolve to audited builds, vm.swappiness=0 is measured in, and the off-box log destination count is one; but the measured boot path replays a persisted compose-manager image override from a docker volume with no boot-time signature check, the root-equivalent ptrace-capable compose-manager holds write access to that file, the collector mounts the entire guest root filesystem, the compose-fetch tag-age floor is an operator env defaulting to 0, and the plaintext stack this harness launched at 18:25Z today is log-pinned, not measured — for gpu04 this measurement replaces the 0.5.5 gateway-on harness 9385918d and inherits the a6eafc5f guest OS whose page reports an armed core-dump path and no LSM

| | |
|---|---|
| **compose_hash** | `c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86` |
| **role** | GPU-node measured `app_compose` — management harness only, `INSTANCE_LABEL=gpu04` (`:135`). Per the orchestrator's 2026-09-02 fleet capture this hash is live on **1 CVM**: the second of two CVMs load-balanced behind `glm-5-3-flash.completions.near.ai` (instance `7bb9af0ac5b0…`, roughly half the hostname's traffic), serving `z-ai/glm-5.3-flash`; the other CVM measures [`341313ae…`](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md) (gpu30), this page's base. Fleet placement is from that capture, not re-derived here (Not traced). The physical label `gpu04` previously carried [`9385918d…`](sha256-9385918de0a73b861ae833d99fb5be6f7e1c8a50487a835df4f277497c206825.md); host lineage is diffed below |
| **identity** | hardware-measured: the orchestrator fetched the live attestation report from `glm-5-3-flash.completions.near.ai` on 2026-09-02 with a fresh nonce, landed on instance `7bb9af0ac5b0…`, and checked `sha256(info.tcb_info.app_compose) == info.compose_hash` before saving; re-verified locally two ways — `shasum -a 256` over the captured 18,212-byte JSON document returns exactly `c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86`, the `compose_hash` this page is keyed on (the base capture likewise returns `341313ae…`); and in the orchestrator's saved attestation report for this instance, `sha256(tcb_info.app_compose)` recomputed here equals the report's own `compose_hash`, with the captured document byte-equal to that `app_compose` string. I did not perform the live fetch or the TDX quote-signature verification myself (Not traced) |
| **dstack** | `manifest_version: 2`, `name: dstack-nvidia-0.5.11`, `runner: docker-compose`, `kms_enabled: true`, `gateway_enabled: false`, `local_key_provider_enabled: false`, `key_provider_id: ""`, **`public_logs: false`**, `public_sysinfo: true`, `no_instance_id: false`, `secure_time: false` — every outer field byte-identical to the base. The guest OS under this harness is `os_image_hash` `a6eafc5f…`, now published: [`os/a6eafc5f`](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) (QUALIFIED PASS at measured configuration) |
| **review** | reviewed 2026-09-02 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review; base = [`341313ae…`](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md) (gpu30, the other CVM behind the same hostname). Both documents were parsed as JSON and their `docker_compose_file` (**374 lines** in both) and `pre_launch_script` (**34 lines** in both) extracted programmatically and byte-diffed: the pre-launch scripts are identical (`diff` exit 0); the compose files differ in **exactly one hunk, one line** (`:135`); all thirteen outer fields, including the 22-entry `allowed_envs` in identical order, are equal. Line citations `:NN` are this document's embedded compose's own line numbers; `pre_launch_script:NN` the script's; outer dstack fields are cited by name. Because the only differing line is `:135`, every citation on the base page re-locates at the **identical** line number in this document — and each line cited below was re-read from this document's extraction, not inferred from the diff. The plaintext-handling stack is **not in this measurement**: on this CVM it is the runtime-fetched inner recipe `prod/GLM-5.3-Flash-SGL-TP4.yaml` @ `d76413cd…`, which per the orchestrator's reading of the signed action log this CVM brought up at `compose_up` 2026-09-02T18:25:37Z, four seconds after `compose_manager_started` at 18:25:33Z — a CVM created today. That recipe is under concurrent review (its page has no verdict at the time of writing) and was **not read** for this page; under the repo's fail-closed rule no claim is made about it here, including whether it joins the network this document defines (Not traced).

---

# Deployment Manifests & Configuration — Privacy Audit (harness leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-09-02).
> Method: [/notes/method.md](/notes/method.md) §4 (manifest-only checks) · Base: [341313ae](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md) (gpu30, same hostname) · Host lineage: [9385918d](sha256-9385918de0a73b861ae833d99fb5be6f7e1c8a50487a835df4f277497c206825.md) (gpu04's previous measured compose) · Guest OS: [a6eafc5f](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) · Same-regeneration siblings: [d4c89033](sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) (gpu13), [5c027cc8](sha256-5c027cc86ab48fa98550f5be9be0621adff811fa231d955653396f6e13f85bd2.md) (gpu26)

## Check 1 — What changed from the audited base, enumerated

Re-derived by byte diff of the extracted files, not taken from the orchestrator's summary. Relative to [`341313ae`](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md):

**Outer document:** nothing. `name: dstack-nvidia-0.5.11`, `gateway_enabled: false`, `public_logs: false`, `kms_enabled: true`, `public_sysinfo: true`, `no_instance_id: false`, `secure_time: false`, `local_key_provider_enabled: false`, `key_provider_id: ""`, `runner: docker-compose`, `manifest_version: 2`, and the 22-entry `allowed_envs` (byte-identical order) — all equal.

**`pre_launch_script`:** nothing. 34 lines, `diff` exit 0. The sysctls (`pre_launch_script:4-7`), the `OVERRIDES` path (`:15`), the dead `if [ "false" = "true" ]` re-pin branch naming `6e035c8f…` (`:24-28`) and the live `COMPOSE_MANAGER_IMAGE` loader (`:29-34`) are the base's bytes.

**Compose (one hunk, 374 → 374 lines):**
1. **`INSTANCE_LABEL=gpu30` → `gpu04`** (`:135`). A compose-manager environment entry. Its consumers in this document, grep-verified: the string `INSTANCE_LABEL` occurs at exactly two lines — the comment at `:132` ("INSTANCE_LABEL identifies this host in the message ("Deployed on gpuXX")", `:132-134`) and the assignment at `:135`. It is **not** referenced by the collector's `resource` processor (`:293-309`, whose attributes come from `DD_HOSTNAME`, `CVM_NAME`, `CVM_HOST`, `ENV` at `:296`, `:299`, `:302`, `:305`), not by any receiver, exporter, or label, and not by the pre-launch script. Finding (INFO) below.

**Carried unchanged — each re-verified against this document at the identical line number, not assumed:** otelcol image digest `85ac41c2…` (`:5`), `user: "0:0"` (`:8`), `mem_limit: 768m` (`:9`), `MONITORING_INGEST_TOKEN` (`:11`), mounts `/var/run/docker.sock:ro` (`:18`), `/:/hostfs:ro` (`:22`), `/var/lib/docker/containers:ro` (`:23`), storage volume (`:24`), the collector's own `logging:` block (`:33`); certbot/certbot-init at `certbot/dns-cloudflare@742dbd2e…` (`:41`, `:64`) with the `certs` volume (`:45`) and the Cloudflare token config (`:213`), ECDSA P-256 (`:79`), cert names `completions.near.ai` + `*.completions.near.ai` + `$HOSTNAME.hosts.near.ai` (`:80-83`); compose-manager's fallback-image default `6e035c8f…` (`:95`), deploy API `8080:8080` (`:98-99`), the nsenter/AppArmor comment (`:100-116`, "dstack-OS does NOT ship AppArmor" at `:113`), `pid: host` (`:117`), `cap_add: [SYS_ADMIN, SYS_PTRACE]` (`:118-120`), rw `docker.sock` + `dstack.sock` + decrypted env ro (`:122-124`), `work` volume (`:125`), `GITHUB_REPO=https://github.com/nearai/cvm-compose-files` (`:127`), `BEARER_TOKEN` (`:128`), `MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`), `ENV_FILES=/app/.env` (`:131`), `SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL:-}` (`:136`); the launcher at digest `91fdff3c…` (`:148`), `network_mode: host` (`:156`), rw `docker.sock` (`:158`), decrypted env ro (`:162`), `work` (`:163`), `COMPOSE_MANAGER_IMAGE_REPO=nearaidev/compose-manager` (`:165`), `CM_MIN_TAG_AGE_HOURS` passthrough (`:169`), `LAUNCHER_CHANNEL=${LAUNCHER_CHANNEL:-latest}` (`:170`), `LAUNCHER_COMPOSE_PROJECT=dstack` (`:171`), `LAUNCHER_ENV_FILE=/app/work/.env.launcher` (`:172`), `LAUNCHER_BASE_ENV_FILE=/dstack-env` (`:173`), health URL `http://127.0.0.1:8080/version` (`:174`), the cosign identity regexp (`:179`), registry credentials (`:180-181`), `LAUNCHER_STATE_FILE` (`:182`); the four `com.datadoghq.ad.logs` labels (`:54`, `:85`, `:138`, `:186`) and their log-driver exports (`:60`, `:91`, `:144`, `:192`); the shared `dstack_default` network with its comment (`:195-200`); volumes `otelcol-management-storage` (`:203`) and `work` (`:205`); the collector config: `filelog/docker_containers` (`:222`), glob (`:223`), `start_at: end` (`:224`), `on_error: send_quiet` (`:235`, `:250`), the unconditional `add` of `service.name: cvm-docker` (`:251-253`), `hostmetrics/cvm` with `root_path` (`:255`, `:257`), `filesystem` scraper (`:265`), `docker_stats/cvm` (`:270`), `prometheus/self` (`:279`), `transform/docker_metadata` (`:311`, statements `:316-318`), `filter/management_logs` (`:321`, `error_mode: ignore` `:322`, expression `:325`), the literal endpoint `https://telemetry.infra.near.ai` (`:341`) with `Authorization: Bearer $${env:MONITORING_INGEST_TOKEN}` (`:343`), the FIFO comment (`:344`) and `sending_queue` (`:345-348`: `num_consumers: 1`, `queue_size: 128`, `storage: file_storage`), `extensions:` (`:350`) with directory `/var/lib/otelcol-management/storage` (`:352`); pipeline `logs/cvm` (`:358`) processors (`:360`) and exporter (`:361`); collector self-telemetry `logs: level: info` (`:373-374`). Grep-verified absent, as on the base: `datadog/agent`, `4317`, `dogstatsd`, `journald`, `DD_SITE`, `privileged`, `${ENCRYPTION_KEY}`; `DD_API_KEY` occurs only in `allowed_envs`.

### INFO (deployed: ON) — INSTANCE_LABEL=gpu04 is a host-identity string for the Slack deploy notification; it is metadata that reaches no telemetry attribute and no content path (egress)

The one changed line. `INSTANCE_LABEL` is consumed only by compose-manager (`:135`), whose in-document comment says it "identifies this host in the message ("Deployed on gpuXX")" (`:132-133`) sent to the operator-supplied `SLACK_WEBHOOK_URL` (`:136`, empty = off, `:134`). It does not appear in the collector's `resource` attributes (`:293-309`), which are built from `DD_HOSTNAME`/`CVM_NAME`/`CVM_HOST`/`ENV` instead; it is not in `allowed_envs` (it is a literal, changing it changes the measurement — which is exactly why this document has a different hash from the base). Its privacy content is "which physical host deployed" — a label, never a message. The operator-chosen egress destination it rides on is the base page's carried `SLACK_WEBHOOK_URL` control primitive ([base, Check 2](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#check-2--allowed_envs-the-operators-measurement-free-knob-set)), unchanged. For a client, the practical consequence is the positive one: the two CVMs behind `glm-5-3-flash.completions.near.ai` run **byte-identical measured harnesses** apart from this label — there is no measured-layer asymmetry between them of the kind the earlier gpu30 (`66007399`, Datadog) vs gpu04 (`9385918d`, no Datadog) pair had. Whether the two CVMs run the same inner recipe revision is action-log state, not measurement (Not traced).

### INFO (deployed: ON) — no Datadog agent in the measurement; one off-box log destination by measurement, management records only (telemetry)

Carried; for gpu04 this is **continuity**, not a resolution — the label's previous harness `9385918d` already had no agent. Grep-verified in this document: the only `datadog` strings are the `com.datadoghq.ad.logs` labels (`:54`, `:85`, `:138`, `:186`; exported by the log driver at `:60`, `:91`, `:144`, `:192`) and the transform that parses them (`:317-318`); no `4317`, `dogstatsd`, `journald`, `DD_SITE`. Receivers are `filelog/docker_containers` (`:222`), `hostmetrics/cvm` (`:255`), `docker_stats/cvm` (`:270`), `prometheus/self` (`:279`) — no network ingestion surface at the measured layer. Full trace: [base, Check 1 INFO](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#check-1--what-changed-from-the-audited-base-enumerated).

## Check 2 — `allowed_envs` (the operator's measurement-free knob set)

**Result: no content switch, no log-level switch, no telemetry-destination switch. 22 entries, byte-identical to the base and to `9385918d`.** Verbatim:

```
BEARER_TOKEN, CM_MIN_TAG_AGE_HOURS, SLACK_WEBHOOK_URL, AWS_REGION,
AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_S3_BUCKET, CERTBOT_EMAIL,
CLOUDFLARE_API_TOKEN, DOCKER_REGISTRY_USER, DOCKER_REGISTRY_TOKEN,
LAUNCHER_CHANNEL, DD_API_KEY, HUGGING_FACE_HUB_TOKEN, PROXY_TOKEN, ENV,
HOST_IP, DD_HOSTNAME, HOSTNAME, MONITORING_INGEST_TOKEN, CVM_NAME, CVM_HOST
```

Consumers re-located in this document at the base's line numbers: `CM_MIN_TAG_AGE_HOURS` (`:130`, `:169`), `BEARER_TOKEN` (`:128`) for the `8080:8080` deploy API (`:98-99`), `LAUNCHER_CHANNEL` (`:170`) against the literal cosign regexp (`:179`), `SLACK_WEBHOOK_URL` (`:136`), `MONITORING_INGEST_TOKEN` (`:11`, `:343`) selecting the account for the literal endpoint (`:341`), `DD_HOSTNAME`/`CVM_NAME`/`CVM_HOST`/`ENV` as resource attributes (`:296`, `:299`, `:302`, `:305`), `HOSTNAME` in the cert request (`:83`), `CLOUDFLARE_API_TOKEN` (`:213`), `DOCKER_REGISTRY_*` (`:180-181`); `DD_API_KEY` consumed by nothing (vestigial, drift-ready); `HUGGING_FACE_HUB_TOKEN`, `PROXY_TOKEN`, `AWS_*`, `HOST_IP` pass through `ENV_FILES=/app/.env` (`:131`) / `LAUNCHER_BASE_ENV_FILE=/dstack-env` (`:173`) to the inner stack. No env here can raise the collector's log level (`:373-374`), alter the filter (`:325`), or add an exporter. `COMPOSE_MANAGER_IMAGE` is not in the list; its only measured setter is `pre_launch_script:29-34`. Full trace: [base, Check 2](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#check-2--allowed_envs-the-operators-measurement-free-knob-set).

### MEDIUM (deployed: ON, carried) — the compose-fetch tag-age floor is an operator env defaulting to zero (supply chain)

`MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`; launcher `:169`) — identical to the base and to `9385918d`. On this CVM the channel was exercised at creation: `compose_up` of `d76413cd…` four seconds after `compose_manager_started` (orchestrator's action-log reading). Each such deploy is disclosed in the signed action log, the compensating control (C1). Full trace: [base, Check 2 MEDIUM](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#medium-deployed-on-carried--the-compose-fetch-tag-age-floor-is-an-operator-env-defaulting-to-zero-supply-chain).

## Check 3 — The otelcol-management log pipeline

**Result: fail-CLOSED by two independent mechanisms, re-verified at this document's lines** — the unconditional `add` of `service.name: cvm-docker` (`:251-253`) and the three-way inequality in `filter/management_logs` (`:325`), between the CVM-wide `filelog` glob (`:223`) and the literal endpoint (`:341`) via the `logs/cvm` processor chain (`:360`). Pipeline bytes identical to the base.

### INFO (deployed: ON) — num_consumers: 1 serializes export dispatch; it changes neither what is collected, filtered, nor where it goes (telemetry)

`:346` under the FIFO comment (`:344`), block `:345-348`. Carried; new relative to `9385918d` for this host. Full trace: [base, Check 3 INFO](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on-new--num_consumers-1-serializes-export-dispatch-it-changes-neither-what-is-collected-filtered-nor-where-it-goes-telemetry).

### MEDIUM (deployed: OFF, one condition wide) — every engine's stdout is ingested by the measured collector and excluded by filter expression, not by scoping (telemetry)

Glob `:223`, filter `:325`, default `:251-253`; no redaction stage on `:360`; admission by spoofable label (`:316-317`). Identical to the base and to `9385918d`. Full trace: [base, Check 3 MEDIUM](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#medium-deployed-off-one-condition-wide--every-engines-stdout-is-ingested-by-the-measured-collector-and-excluded-by-filter-expression-not-by-scoping-telemetry).

## Check 4 — Privileges and mounts

### HIGH (deployed: ON, architectural) — compose-manager is root-equivalent and can read the engine's process memory (structural)

`pid: host` (`:117`), `cap_add: [SYS_ADMIN, SYS_PTRACE]` (`:118-120`), rw `docker.sock` (`:122`), `dstack.sock` (`:123`), decrypted env ro (`:124`), rw `work` (`:125`); comment `:100-116`. Identical to the base and to `9385918d`. What is new **for this host** is the substrate under it: the [`a6eafc5f` OS page](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) reports the 0.5.11 kernel built with `CONFIG_SECURITY` unset (no AppArmor, no Yama `ptrace_scope`) — cited as published, not re-derived; a property outside this document, so a residual, not a change in this finding's severity. Full trace: [base, Check 4 HIGH](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#high-deployed-on-architectural--compose-manager-is-root-equivalent-and-can-read-the-engines-process-memory-structural).

### HIGH (deployed: ON, architectural) — the launcher holds the docker socket on the host network (structural)

`network_mode: host` (`:156`), rw `docker.sock` (`:158`), decrypted env ro (`:162`), rw `work` (`:163`), image `91fdff3c…` (`:148`). Identical to the base and to `9385918d`. Full trace: [base, Check 4 HIGH (launcher)](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#high-deployed-on-architectural--the-launcher-holds-the-docker-socket-on-the-host-network-structural).

### MEDIUM (deployed: ON, capability unused at this config) — the collector mounts the entire guest root filesystem and the docker socket (disk)

`/:/hostfs:ro` (`:22`), `/var/run/docker.sock:ro` (`:18`), `/var/lib/docker/containers:ro` (`:23`), `user: "0:0"` (`:8`); scrapers `:255-277`, `filesystem` exclusions at `:265-268`. Identical to the base. Unlike on gpu30, this is **not new for gpu04** — `9385918d` already mounted the guest root; the base page's "new for this host" qualifier does not apply here. Full trace: [base, Check 4 MEDIUM](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#medium-deployed-on-capability-unused-at-this-config-new-for-this-host--the-collector-mounts-the-entire-guest-root-filesystem-and-the-docker-socket-disk).

### INFO (deployed: ON) — certbot holds the wildcard TLS key and the Cloudflare token in-enclave (disk)

`certs` (`:45`, `:207-208`), token config (`:211-213`), names `:80-83`, ECDSA P-256 (`:79`); no `privileged: true` anywhere (grep-verified). Identical to the base. Full trace: [base, Check 4 INFO](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on--certbot-holds-the-wildcard-tls-key-and-the-cloudflare-token-in-enclave-disk).

## Check 5 — Image pinning, identity, and the measured boot-path override restore **[priority item]**

### HIGH (deployed: ON, widened vs 9385918d) — the measured boot path replays a persisted compose-manager image override from a docker volume with no boot-time signature check, and the compose-manager it replaces can write that file (supply chain)

`pre_launch_script:9-34`, byte-identical to the base: `OVERRIDES=/var/lib/docker/volumes/dstack_work/_data/.env.launcher` (`:15`), parsed as data (`:30-32`), exporting an **unvalidated** `COMPOSE_MANAGER_IMAGE` (`:31`) that interpolates into the default-not-a-pin at `:95`; the file is rw to compose-manager (`work:/app/work`, `:125`) and the launcher (`:163`); the cosign gate (`:179`) is enforced at swap time, never at boot. For gpu04 this is the widening **directly against its own previous measurement**: `9385918d` had a two-line pre-launch script and a boot that reverted to the measured fallback (the comment at `:9-10` describes exactly that prior behaviour). The OS page reports (cited, not re-derived) that the script runs as root via `source` inside `app-compose.service` with the decrypted env loaded, that docker's data-root is the default `/var/lib/docker` the script assumes (`:13-14`), and that the `dstack_work` volume sits on the KMS-keyed LUKS2 data disk — settling three of the base page's Not-traced items in the direction the document assumed, without changing the finding: the writer set and the missing boot-time check are properties of this document. Full trace: [base, Check 5 HIGH](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#high-deployed-on-widened-vs-base--the-measured-boot-path-replays-a-persisted-compose-manager-image-override-from-a-docker-volume-with-no-boot-time-signature-check-and-the-compose-manager-it-replaces-can-write-that-file-supply-chain).

### INFO (deployed: ON) — both control-plane images resolve to audited builds, and the dead re-pin literal is consistent with the fallback (supply chain)

Fallback `compose-manager@6e035c8f…` (`:95`; [review](/images/docker.io/nearaidev/compose-manager/sha256-6e035c8fb99c1d31f41760d25af178683a658cca4afd73cd0d366fbb5bf884be.md)), launcher `91fdff3c…` (`:148`; [review](/images/docker.io/nearaidev/compose-manager-launcher/sha256-91fdff3cfa3543d72656b2368c7d8a0a83d95a0f1087378c897aa1537acdba56.md)), dead branch `if [ "false" = "true" ]` (`pre_launch_script:24`) naming the same `6e035c8f…` (`:26-27`); `certbot/dns-cloudflare@742dbd2e…` (`:41`, `:64`) digest-pinned, no source page. Both control-plane digests are the **same** ones `9385918d` carried — no image change for gpu04 in this regeneration. Full trace: [base, Check 5 INFO](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on--both-control-plane-images-resolve-to-audited-builds-and-the-dead-re-pin-literal-is-consistent-with-the-fallback-supply-chain).

## Check 6 — Network topology and egress

### MEDIUM (deployed: ON) — one flat shared network spans management and the plaintext services that attach to it; no egress restriction (egress)

`dstack_default` (`:195-200`), no `internal: true`; published `8080:8080` (`:98-99`); launcher on host network (`:156`); literal destinations `:341`, `:127`, `:165`, `:43`, `:72-83`, `:211-213`, plus operator-set `SLACK_WEBHOOK_URL` (`:136`). Identical to the base and to `9385918d`. Whether `prod/GLM-5.3-Flash-SGL-TP4.yaml` @ `d76413cd…` joins this network was not read (Not traced). Full trace: [base, Check 6 MEDIUM](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#medium-deployed-on--one-flat-shared-network-spans-management-and-the-plaintext-services-that-attach-to-it-no-egress-restriction-egress).

## Check 7 — dstack-level settings: `gateway_enabled: false`, the 0.5.11 guest, and the sysctls

### INFO (deployed: ON, guest-OS answered) — gateway_enabled: false turns off the dstack gateway for this CVM; the a6eafc5f OS page now reports what that removes on the guest side (egress)

The flag is `false` (outer field, identical to the base); TLS terminates in-enclave (`:79-83`). The base page listed four questions (a)-(d) for the then-unpublished OS page. That page exists now and — cited as published, not re-derived here — reports: the flag disables gateway registration, WireGuard tunnel and the `DSTACK_WG` iptables chain as one unit (a surface reduction, since the chain only filtered the port the tunnel itself opened); the guest's outbound path is unchanged; `8080` reachability from outside is a VMM port-mapping property it too leaves Not traced; and the 0.5.11 rootfs still ships no AppArmor (and no LSM at all). Severity stays INFO: this document shows a flag change and no plaintext path, and nothing here is a mitigation claim. Full trace: [base, Check 7 INFO (gateway)](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on-guest-os-dependent--gateway_enabled-false-turns-off-the-dstack-gateway-for-this-cvm-what-that-removes-on-the-guest-side-is-a-question-for-the-unreviewed-0511-os-page-egress).

### INFO (deployed: ON, resolves 9385918d MEDIUM) — vm.swappiness=0 is measured in; KV-cache pages are kept out of swap by a measured sysctl (disk)

`echo 0 > /proc/sys/vm/swappiness` (`pre_launch_script:5`); `dirty_ratio=5` / `dirty_background_ratio=1` (`:6-7`) privacy-neutral. For gpu04 this resolves the MEDIUM its own `9385918d` page carried for the absence of this control. The OS page reports the script runs as root (so the write succeeds) and that swap, if any, sits on the encrypted data disk — cited, not re-derived. Full trace: [base, Check 7 INFO (swappiness)](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on-resolves-base-medium--vmswappiness0-is-measured-in-kv-cache-pages-are-kept-out-of-swap-by-a-measured-sysctl-disk).

### INFO (deployed: ON) — the k256 app key is exported into the compose env namespace; no consumer exists in this document (log)

`export ENCRYPTION_KEY=$(cat /dstack/.host-shared/.appkeys.json | jq -r .k256_key)` (`pre_launch_script:2`); `${ENCRYPTION_KEY}` referenced nowhere in the compose (grep-verified); the script's two `echo` lines (`:27`, `:33`) print a path and an image reference, never the key. The OS page reports that this stdout goes to the journal **and** the host-readable serial console (`StandardOutput=journal+console`) — so the `[pre_launch] Loaded compose-manager image override from …` line (`:33`) is host-visible metadata, and the trap the OS page names (`set -x` or `env` in a future measured script would print secrets to the host) applies to this script's successors. Cited, not re-derived. Full trace: [base, Check 7 INFO (k256)](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on--the-k256-app-key-is-exported-into-the-compose-env-namespace-no-consumer-exists-in-this-document-log).

## Host lineage — gpu04 from `9385918d` (0.5.5, gateway on, near.ai OS build) to this harness

This hash replaces [`9385918d…`](sha256-9385918de0a73b861ae833d99fb5be6f7e1c8a50487a835df4f277497c206825.md) on the physical label `gpu04` (`INSTANCE_LABEL=gpu04` in both). Model history on the label, from the earlier page and the orchestrator's saved attestation reports (read here, not fetched): `9385918d`'s 2026-08-03 review recorded gpu04 serving `glm-5-2`; the orchestrator's 2026-09-01 report for `gemma-4-31b.completions.near.ai` shows instance `ab611bbb0d52…` with `INSTANCE_LABEL=gpu04`, `compose_hash` `9385918d…` and `os_image_hash` `9b69bb16…` — the same harness hash, repurposed through the log-pinned recipe channel that hash does not see (the Qwen3.6 hostnames in that capture attest to gpu02 on `d98a2568…`, not to this label); today it is one of the two `glm-5-3-flash` CVMs, instance `7bb9af0ac5b0…`, created 2026-09-02T18:25:33Z. Placements are the orchestrator's captures, not re-derived live (Not traced). `9385918d` was already the no-Datadog five-service template — so unlike gpu30's move, gpu04's regeneration removes **no** telemetry sink. What changes for this host, each checked against this document:

**Substrate and dstack flags (outer document):**
- **`dstack-nvidia-0.5.5` → `0.5.11`**, and with it the guest OS from near.ai's build [`9b69bb16`](/os/sha256-9b69bb1698bacbb6985409a2c272bcb892e09cdcea63d5399c6768b67d3ff677.md) to the **upstream** Dstack-TEE build [`a6eafc5f`](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) (both pages published; both QUALIFIED PASS). The new OS page surfaces two things the old one did not — an **armed core-dump path** (a crashing engine writes its memory as a core file into its container layer on the in-enclave LUKS2 disk; MEDIUM, ARMED there) and **no LSM of any kind** (INFO there). Both are guest-OS findings and residuals for this page: this document neither creates nor mitigates them. Whether the inner recipe sets `ulimits: core` for the engine is the recipe page's question (Not traced).
- **`gateway_enabled: true` → `false`.** Per the OS page: tunnel, registration and UDP filter removed together — fewer host-facing services on gpu04, no control lost.

**Measured boot path (`pre_launch_script`, 2 → 34 lines):**
- **[HIGH, new for gpu04]** the persisted compose-manager override is now replayed on every boot (`:29-34`); under `9385918d` a reboot reverted to the measured fallback. Check 5.
- **[resolves 9385918d MEDIUM]** `vm.swappiness=0` (`:5`). Check 7.

**Compose (372 → 374 lines):** `num_consumers: 1` (`:346`) is the only functional change; INFO. Everything else — collector image, mounts (including the guest-root mount `9385918d` already had), certbot, compose-manager privileges, launcher, control-plane digests `6e035c8f…`/`91fdff3c…`, `dstack_default`, `8080:8080`, the 22-entry `allowed_envs`, `MIN_TAG_AGE_HOURS` default 0, `SLACK_WEBHOOK_URL` — carried at the line numbers cited above.

## Verdict

**QUALIFIED PASS.** Within hardware-measured config + `allowed_envs`, the invariant holds exactly as on the base — this document *is* the base with a different host label: the 22-entry env list contains no content, log-level, or telemetry switch; the one measured off-box log path is management-only, to one literal near.ai endpoint, through a filter that is fail-closed by construction and by OTTL semantics; the changed line is a Slack-notification identity string that touches no telemetry attribute and no content path; and the two CVMs behind `glm-5-3-flash.completions.near.ai` present clients with byte-identical measured harnesses. The qualifications carry over unchanged: (C2, **widened for gpu04 against its own previous measurement**) the measured boot path installs whatever compose-manager reference `.env.launcher` holds, without a boot-time signature check, from a volume the root-equivalent, ptrace-capable compose-manager can write; (C1) the plaintext stack is log-pinned, not measured, with the tag-age floor at 0 — this CVM deployed `d76413cd…` four seconds after its compose-manager started; (C3) engine stdout still leaves the CVM through the recipe's collector with no redaction anywhere; (C4) the flat network this document defines keeps any engine admin surface reachable; and the substrate this harness now runs on carries, per its own page, an armed core-dump path and no LSM — residuals this document does not touch.

## Not traced

- **Live attestation and TDX quote verification** — the fresh-nonce fetch, the landing on instance `7bb9af0ac5b0…`, and the `sha256(app_compose) == compose_hash` check were the orchestrator's; this review re-hashed the captured bytes of both documents only.
- **Fleet placement** — "1 CVM, the second behind `glm-5-3-flash`, serving `z-ai/glm-5.3-flash`", the label's earlier `gemma-4-31b` tenure on `9385918d` / `9b69bb16`, and the 18:25:33Z / 18:25:37Z action-log timestamps are from the orchestrator's saved captures (the hash/label/instance fields were read from those files; the fetches themselves were not repeated); not re-derived live.
- **The inner recipe `prod/GLM-5.3-Flash-SGL-TP4.yaml` @ `d76413cd…`** — under concurrent review, **not read** here; no claim about its engine flags, its collector, its core `ulimits`, whether it joins `dstack_default`, or whether both `glm-5-3-flash` CVMs run the same revision.
- **The 0.5.11 guest OS (`a6eafc5f…`)** — its page now exists and is cited above for what `gateway_enabled: false` disables, the AppArmor/LSM posture, the pre-launch script's privilege, stdout and data-root assumptions (`pre_launch_script:13-14`), the encrypted placement of `dstack_work`, and swap placement. None of that was re-derived for this page; the items are kept here because they remain outside this document. Whether `8080` is reachable from outside the CVM is left Not traced by that page as well (a VMM property).
- **The currently-running compose-manager digest** — runtime state (`.env.launcher` + action log), invisible to this layer by design (Check 5).
- **Launcher behaviour on a non-verified running image** — whether a launcher poll would swap back a compose-manager that booted from a tampered `.env.launcher`, or re-seed it (`pre_launch_script:22-23`); a launcher source question for the `91fdff3c` page.
- **The contents of `/dstack/.host-shared/.decrypted-env`** — `allowed_envs` constrains names on the measured channel; values, and anything spliced via `/compose/up`, are not visible here.
- **`certbot/dns-cloudflare@742dbd2e…`** — digest-pinned, no source page; configuration-audited only.
- **Collector binary semantics** — the fail-closed and `num_consumers` analyses rely on documented OTTL / filterprocessor / exporterhelper contracts for the deployed collector line, not on the `85ac41c2…` binary.
