# measured compose @ 341313ae... (GPU-node measured app_compose — gpu30, `glm-5-3-flash`: the regenerated no-Datadog harness on dstack-nvidia-0.5.11, dstack gateway off)

## verdict: QUALIFIED PASS at the manifest layer — gpu30 leaves the Datadog side of the node lottery (the measured agent that shipped every container's stdout to us3.datadoghq.com is gone, along with its open OTLP and dogstatsd ingestion surfaces), the management filter is fail-closed twice over, both control-plane images resolve to audited builds and vm.swappiness=0 is now measured in; but the measured boot path now replays a persisted compose-manager image override from a docker volume with no boot-time signature check — a widening of the base's default-not-a-pin channel that the document's own comment says previously reverted on reboot — the root-equivalent ptrace-capable compose-manager holds write access to the very file that decides which compose-manager boots next, the collector now mounts the entire guest root filesystem, the compose-fetch tag-age floor remains an operator env defaulting to 0, and gateway_enabled: false turns off a guest-side control whose consequences only the unreviewed 0.5.11 guest-OS page can settle

| | |
|---|---|
| **compose_hash** | `341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14` |
| **role** | GPU-node measured `app_compose` — management harness only, `INSTANCE_LABEL=gpu30` (`:135`). Per the orchestrator's 2026-09-01 fleet capture this hash is live on **1 host**: `glm-5-3-flash` (`glm-5-3-flash.completions.near.ai`, serving `z-ai/glm-5.3-flash`). Fleet placement is from that capture, not re-derived here (Not traced). This is the **same physical label** the retired [`66007399…`](sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md) harness carried (gpu30, then `qwen35-122b` + `dsv4-flash`); host lineage is diffed below |
| **identity** | hardware-measured: the orchestrator fetched the live attestation report from `glm-5-3-flash.completions.near.ai` on 2026-09-01 with a fresh nonce and checked `sha256(info.tcb_info.app_compose) == info.compose_hash` before saving; re-verified locally — `shasum -a 256` over the captured 18,212-byte JSON document returns exactly `341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14`, the `compose_hash` this page is keyed on. I did not perform the live fetch or the TDX quote-signature verification myself (Not traced) |
| **dstack** | `manifest_version: 2`, **`name: dstack-nvidia-0.5.11`** (base: `0.5.5`), `runner: docker-compose`, `kms_enabled: true`, **`gateway_enabled: false`** (base and every published measured page: `true`), `local_key_provider_enabled: false`, `key_provider_id: ""`, **`public_logs: false`**, `public_sysinfo: true`, `no_instance_id: false`, `secure_time: false`. The guest OS under this harness is `os_image_hash` `a6eafc5f…`, which has **no page** in [`os/`](/os/) (under separate review) |
| **review** | reviewed 2026-09-01 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review; base = [`9385918d…`](sha256-9385918de0a73b861ae833d99fb5be6f7e1c8a50487a835df4f277497c206825.md) (gpu04, the same five-service no-Datadog template: `otelcol-management`, `certbot` + `certbot-init`, `compose-manager` + `compose-manager-launcher`). Both documents were parsed as JSON and their `docker_compose_file` (base 372 lines, this document **374**) and `pre_launch_script` (base 2 lines, this document **34**) extracted programmatically and byte-diffed. Line citations `:NN` are this document's embedded compose's own line numbers; `pre_launch_script:NN` the script's; outer dstack fields are cited by name. Every load-bearing citation from the base page re-locates at the **identical** line number up to `:343`; after that the exporter reshaping shifts citations by +2 (Check 1). The plaintext-handling stack is **not in this measurement**: on this host it is the runtime-fetched inner recipe `prod/GLM-5.3-Flash-SGL-TP4.yaml`, pinned by the compose-manager action log and under separate review — per the orchestrator, this host redeployed that recipe **three times on 2026-09-01**, which is exactly the class of change this hash does not see (C1). That recipe was **not read** for this page; under the repo's fail-closed rule no claim is made about it here, including whether it joins the network this document defines (Not traced).

---

# Deployment Manifests & Configuration — Privacy Audit (harness leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-09-01).
> Method: [/notes/method.md](/notes/method.md) §4 (manifest-only checks) · Base: [9385918d](sha256-9385918de0a73b861ae833d99fb5be6f7e1c8a50487a835df4f277497c206825.md) (gpu04) · Host lineage: [66007399](sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md) (gpu30's previous measured compose) · Same-regeneration siblings: [d4c89033](sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) (gpu13), [5c027cc8](sha256-5c027cc86ab48fa98550f5be9be0621adff811fa231d955653396f6e13f85bd2.md) (gpu26)

## Check 1 — What changed from the audited base, enumerated

Re-derived by byte diff of the extracted files, not taken from the orchestrator's summary. Relative to [`9385918d`](sha256-9385918de0a73b861ae833d99fb5be6f7e1c8a50487a835df4f277497c206825.md):

**Outer document (two fields):**
1. **`name: dstack-nvidia-0.5.5` → `dstack-nvidia-0.5.11`.** A label naming the dstack release; the measured guest is identified by `os_image_hash` (`a6eafc5f…`, no page yet), not by this string. What 0.5.11 changes inside the guest is the OS page's question (Not traced).
2. **`gateway_enabled: true` → `false`.** The first `false` in the published corpus. Check 7.

`allowed_envs` (22, byte-identical order), `kms_enabled`, `public_logs: false`, `public_sysinfo`, `no_instance_id`, `secure_time`, `local_key_provider_enabled`, `key_provider_id: ""`, `runner`, `manifest_version` — all identical.

**Compose (two hunks, 372 → 374 lines):**
3. **`INSTANCE_LABEL=gpu04` → `gpu30`** (`:135`). Deploy-notification identity only (`:132-134`).
4. **The exporter's `sending_queue` gains a FIFO comment and `num_consumers: 1`** (`:344`, `:346`; the block is now `:345-348`: `num_consumers: 1`, `queue_size: 128`, `storage: file_storage`). The base's `queue_size` and `storage` lines carry over unchanged. Downstream citations shift +2: `extensions: file_storage` `:350-353` (directory `:352`), pipeline `logs/cvm` processors `:358` → **`:360`**, collector self-telemetry `logs: level: info` `:371-372` → **`:373-374`**. Finding (INFO), Check 3.

**`pre_launch_script` (+32 lines; the base's two lines carry over verbatim at `:1-2`):**
5. **Kernel sysctls** (`pre_launch_script:4-7`): `vm.swappiness=0` (`:5`, comment "prevent KV cache swap-out"), `vm.dirty_ratio=5` (`:6`), `vm.dirty_background_ratio=1` (`:7`). Check 7.
6. **The compose-manager image-override restore** (`pre_launch_script:9-34`): `OVERRIDES="/var/lib/docker/volumes/dstack_work/_data/.env.launcher"` (`:15`); a **dead** forced re-pin branch `if [ "false" = "true" ]` (`:24-28`) naming `nearaidev/compose-manager@sha256:6e035c8f…` (`:26-27`); and a live loader (`:29-34`) that reads the file line by line as `k=v` data and `export`s only the `COMPOSE_MANAGER_IMAGE` key (`:30-32`). Check 5 — the priority item.

**Carried unchanged — each re-verified against this document at the identical line number, not assumed:** otelcol image digest `85ac41c2…` (`:5`), `user: "0:0"` (`:8`), `mem_limit: 768m` (`:9`), mounts `/var/run/docker.sock:ro` (`:18`), `/:/hostfs:ro` (`:22`), `/var/lib/docker/containers:ro` (`:23`), storage volume (`:24`), the `local` log driver on the collector itself (`:33-37`); certbot/certbot-init with the Cloudflare credentials config and the `certs` volume (`:40-91`, `:207-208`, `:211-213`), cert names `*.completions.near.ai` + `completions.near.ai` + `$HOSTNAME.hosts.near.ai` (`:80-83`); compose-manager's fallback-image default (`:95`), deploy API `8080:8080` (`:98-99`), the nsenter/AppArmor comment (`:100-116`), `pid: host` (`:117`), `cap_add: [SYS_ADMIN, SYS_PTRACE]` (`:118-120`), rw `docker.sock` + `dstack.sock` + decrypted env ro (`:122-124`), `work` volume (`:125`), `GITHUB_REPO=https://github.com/nearai/cvm-compose-files` (`:127`), `BEARER_TOKEN` (`:128`), `MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`), `ENV_FILES=/app/.env` (`:131`), `SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL:-}` (`:136`); the launcher at digest `91fdff3c…` (`:148`), `network_mode: host` (`:156`), rw `docker.sock` (`:158`), decrypted env ro (`:162`), `work` (`:163`), `COMPOSE_MANAGER_IMAGE_REPO=nearaidev/compose-manager` (`:165`), `CM_MIN_TAG_AGE_HOURS` passthrough (`:169`), `LAUNCHER_CHANNEL=${LAUNCHER_CHANNEL:-latest}` (`:170`), `LAUNCHER_COMPOSE_PROJECT=dstack` (`:171`), `LAUNCHER_ENV_FILE=/app/work/.env.launcher` (`:172`), `LAUNCHER_BASE_ENV_FILE=/dstack-env` (`:173`), health URL (`:174`), the cosign identity regexp (`:179`), registry credentials (`:180-181`), `LAUNCHER_STATE_FILE` (`:182`); the shared `dstack_default` network with its comment (`:195-200`); the collector config: filelog glob (`:223`), `start_at: end` (`:224`), the operator chain (`:227-253` — `json_parser` `on_error: send_quiet` `:235`, container-id regex `on_error: send_quiet` `:250`, the unconditional `add` of `service.name: cvm-docker` `:251-253`), `hostmetrics/cvm` with `root_path` (`:255-268`), `docker_stats/cvm` on the socket (`:270-277`), `prometheus/self` (`:279-285`), the `resource` attributes (`:293-309`), `transform/docker_metadata` (`:311-319`), `filter/management_logs` (`:321-325`), `groupbyattrs` (`:327-332`), `batch` (`:334-337`), the literal endpoint `https://telemetry.infra.near.ai` (`:341`) with `Authorization: Bearer $${env:MONITORING_INGEST_TOKEN}` (`:343`); pipelines `logs/cvm` (`:358-361`), `metrics/cvm_system` (`:362-365`), `metrics/self` (`:366-369`).

### INFO (deployed: ON) — gpu30 leaves the Datadog side of the node lottery; the measured off-box log destination count drops from two to one (telemetry)

Stated against this host's own previous measurement, since the gpu04 base already had no agent. [`66007399`](sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md) measured a `datadog/agent` on gpu30 with `DD_LOGS_CONFIG_CONTAINER_COLLECT_ALL=true`, container-log and journald mounts, `DD_PROCESS_AGENT_ENABLED=true`, an OTLP-logs receiver on `0.0.0.0:4317` and non-local dogstatsd, shipping every container's stdout — engines included — to `us3.datadoghq.com`. **None of that exists in this document** — grep-verified: the only `datadog` strings are the `com.datadoghq.ad.logs` *labels* on the four management containers (`:54`, `:85`, `:138`, `:186`; exported by the log driver at `:60`, `:91`, `:144`, `:192`) and the OTel transform that parses them (`:317-318`). No `4317`, no `dogstatsd`, no `journald`, no `DD_SITE` anywhere. The receivers in this document are `filelog/docker_containers` (`:222`), `hostmetrics/cvm` (`:255`), `docker_stats/cvm` (`:270`) and `prometheus/self` (`:279`) — **no network ingestion surface at the measured layer.** Engine stdout still leaves the CVM via the inner recipe's own collector (the fleet pattern, assessed on recipe pages; this host's recipe was not read here). Correct statement for this node: **one off-box log destination by measurement, near.ai's own gateway, management records only.**

## Check 2 — `allowed_envs` (the operator's measurement-free knob set)

**Result: no content switch, no log-level switch, no telemetry-destination switch. 22 entries, byte-identical to the base and to this host's previous measurement.** Verbatim:

```
BEARER_TOKEN, CM_MIN_TAG_AGE_HOURS, SLACK_WEBHOOK_URL, AWS_REGION,
AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_S3_BUCKET, CERTBOT_EMAIL,
CLOUDFLARE_API_TOKEN, DOCKER_REGISTRY_USER, DOCKER_REGISTRY_TOKEN,
LAUNCHER_CHANNEL, DD_API_KEY, HUGGING_FACE_HUB_TOKEN, PROXY_TOKEN, ENV,
HOST_IP, DD_HOSTNAME, HOSTNAME, MONITORING_INGEST_TOKEN, CVM_NAME, CVM_HOST
```

Consumers in this document, re-located:

- **`CM_MIN_TAG_AGE_HOURS`** — `MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`) and the launcher copy (`:169`). Finding below.
- **`BEARER_TOKEN`** (`:128`) — authenticates compose-manager's deploy API, published `8080:8080` (`:98-99`); its `POST /compose/up` env overlay is the channel by which env not in this list reaches the plaintext stack (established on the compose-manager source pages, not re-derived here). **`LAUNCHER_CHANNEL`** (`:170`, default `latest`) — chooses which cosign-signed compose-manager build the node self-updates to, measurement-free; the cosign identity regexp is a literal (`:179`) and `LAUNCHER_SKIP_VERIFY` is not in the list. Both carried from the base, unchanged.
- **`SLACK_WEBHOOK_URL`** (`:136`, empty = off) — an operator-suppliable arbitrary outbound URL receiving deploy notifications tagged `gpu30` (`:135`). Metadata, not content; egress chosen purely by env. Carried MEDIUM-class control primitive, unchanged.
- **`DD_API_KEY`** — **consumed by nothing in this document** (grep-verified: its only occurrence is the `allowed_envs` list). On this host's previous measurement it was *live* — it selected the Datadog account for the collect-all agent. Now vestigial; drift-ready if a future measurement re-adds an agent.
- **`MONITORING_INGEST_TOKEN`** — the bearer for the OTLP export (`:11`, `:343`); selects the telemetry *account*, never the destination, which is the literal `https://telemetry.infra.near.ai` (`:341`).
- **`DD_HOSTNAME`, `CVM_NAME`, `CVM_HOST`, `ENV`** — resource attributes only (`:296`, `:299`, `:302`, `:305`; passed at `:12-15`). `HOSTNAME` lands in certbot-init's cert request as `-d "$HOSTNAME.hosts.near.ai"` (`:83`). `CERTBOT_EMAIL` (`:77`), `CLOUDFLARE_API_TOKEN` (`:213`), `DOCKER_REGISTRY_*` (`:180-181`) are service credentials. `HUGGING_FACE_HUB_TOKEN`, `PROXY_TOKEN`, `AWS_*`, `HOST_IP` are consumed by nothing in this compose — they flow through the decrypted env file (`ENV_FILES=/app/.env`, `:131`; launcher `LAUNCHER_BASE_ENV_FILE=/dstack-env`, `:173`) to the inner stack. Credentials, not switches.

No env named here can raise the collector's own log level (`telemetry: logs: level: info`, a literal at `:373-374`), alter the filter expression (`:325`), or add an exporter. `COMPOSE_MANAGER_IMAGE` is *not* in `allowed_envs`; its only measured setter is the `pre_launch_script` loader (Check 5).

### MEDIUM (deployed: ON, carried) — the compose-fetch tag-age floor is an operator env defaulting to zero (supply chain)

`MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`; launcher `:169`). Unset, the floor between a `cvm-compose-files` tag appearing and this node deploying it is **zero**, and the operator can keep it zero without a measurement change. Same on the gpu04 base and on this host's previous measurement — carried, not a regression. The orchestrator's observation that this host redeployed its recipe three times on the capture day is consistent with this posture; each such deploy is disclosed in the signed action log, which is the compensating control (C1).

## Check 3 — The otelcol-management log pipeline: fail-closed direction, and the new `num_consumers`

**Result: fail-CLOSED by two independent mechanisms, re-verified at this document's lines; the only pipeline change is export concurrency.**

The chain: the measured collector's `filelog/docker_containers` receiver tails `/var/lib/docker/containers/*/*-json.log` (`:223`, from `start_at: end`, `:224`) — **every container in the CVM that uses the `json-file` driver, the inner recipe's engines and proxies included.** What stands between that and `https://telemetry.infra.near.ai` (`:341`) is the `logs/cvm` processor chain (`:360`): `memory_limiter, resource, transform/docker_metadata, filter/management_logs, groupbyattrs/docker_service, batch`.

1. **Receiver operators** (`:227-253`): `json_parser` (`on_error: send_quiet`, `:235`), `move` log to body (`:236-239`), stream (`:240-243`), timestamp removal (`:244-246`), container-id regex (`on_error: send_quiet`, `:250`), and **last, an unconditional `add`** of `attributes["service.name"] = cvm-docker` (`:251-253`) — no `if` clause. Every record leaves the receiver with a non-nil `service.name`.
2. **`transform/docker_metadata`** (`:311-319`, `error_mode: ignore`) overwrites `service.name` from the container's exported labels — `com.docker.compose.service` (`:316`), then the `service` field of `com.datadoghq.ad.logs` (`:317`) — each guarded by `attributes["attrs"] != nil`. Management containers resolve to `compose-manager` / `compose-manager-launcher` / `dns-cloudflare`; inner-stack containers to their own names; a statement error leaves `cvm-docker`.
3. **`filter/management_logs`** (`:321-325`) drops any record whose `service.name` differs from all three management names (`:325`). Engine records — named or defaulted — satisfy all three inequalities and are **dropped**. The hypothetical nil case (OTTL map indexing returns nil, `nil != "x"` is true) also drops; the filter's `error_mode: ignore` (`:322`) escape is reachable only via an *erroring* condition, not a missing attribute. Semantics per the collector-contrib OTTL/filterprocessor documentation for the [audited, digest-pinned](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md) `85ac41c2…` (`:5`), not re-derived from the binary (Not traced).
4. **Export:** `otlphttp/gateway` → literal `https://telemetry.infra.near.ai` (`:341`), bearer `:343`, disk-persisted queue on the `otelcol-management-storage` volume (`:345-348`, extension `:350-353`, directory `/var/lib/otelcol-management/storage` `:352`, mounted `:24`, declared `:203`).

### INFO (deployed: ON, new) — `num_consumers: 1` serializes export dispatch; it changes neither what is collected, filtered, nor where it goes (telemetry)

The added line sits inside the exporter's `sending_queue` (`:346`, block `:345-348`) under the comment "Keep source requests FIFO before the central gateway." (`:344`). In otelcol's exporterhelper contract `num_consumers` is the number of workers draining the queue to the endpoint; `1` serializes dispatch. Config-level confirmation that this is dispatch concurrency only: the receivers, operators, transform, filter, pipeline membership, endpoint and auth header are diff-identical to the base — the diff has no other compose hunk beyond `INSTANCE_LABEL`. Second-order effects: the queue sits **after** the filter (`:360-361`), so at this configuration it only ever holds management records; those drain single-file, so under backpressure a record resides longer in the disk-persisted queue (`queue_size: 128`, `:347`; `storage: file_storage`, `:348`) before export, and the 128-batch queue fills sooner — an availability trade, not a new sink. Same treatment as the [gpu26 sibling](sha256-5c027cc86ab48fa98550f5be9be0621adff811fa231d955653396f6e13f85bd2.md) and the recipe-side [`ae05e203`](/manifests/nearai/cvm-compose-files/prod/GLM-5.1-SGL-AWQ-TP4/sha256-ae05e203532562170e62e7ec7f9c7e271b1e5bfc3cdc9bd658d5dcc6c7814eb1.md) page.

### MEDIUM (deployed: OFF, one condition wide) — every engine's stdout is ingested by the measured collector and excluded by filter expression, not by scoping (telemetry)

Carried from the base. The isolation between "an engine printed plaintext" and "it left the box via this collector" is one measured OTTL condition (`:325`) plus one measured `add` default (`:251-253`). Both are inside the hash — the strength of the design — but the receiver is deliberately CVM-wide (`:223`), so this is "OFF at the measured config", not "cannot happen". Residuals outside this document: the filter admits by **spoofable label** (any container whose labels declare `service: compose-manager` gets its stdout exported — containers are defined by the log-pinned recipe and by whatever the root-equivalent compose-manager runs, so this collapses into C1/C2); and **no redaction stage** exists for admitted streams (`:360` is route-and-batch only), so whatever the three management services print — compose-manager holds every Check-2 secret — ships verbatim. Engine stdout also persists **on-CVM** through the `json-file` driver regardless of export — in-enclave persistence, sized by the recipe's log options (not read here).

## Check 4 — Privileges and mounts

### HIGH (deployed: ON, architectural) — compose-manager is root-equivalent and can read the engine's process memory (structural)

`pid: host` (`:117`) + `cap_add: [SYS_ADMIN, SYS_PTRACE]` (`:118-120`) + rw `/var/run/docker.sock` (`:122`) + `/var/run/dstack.sock` (`:123`) + the decrypted operator env at `/dstack/.host-shared/.decrypted-env:/app/.env:ro` (`:124`) + rw `work` (`:125`). The manifest's own comment (`:100-116`) documents the intent — `nsenter -t 1 -m -u -i -n -p` into PID 1's namespaces, and that dstack-OS ships no AppArmor to confine it (verified by near.ai on 2026-05-19 against the 0.5.5-era rootfs per `:113-116`; whether the **0.5.11** guest under this harness still ships none is for the `a6eafc5f…` OS page — Not traced). With host PID + `SYS_PTRACE` it can attach to **any process in the CVM, the `glm-5.3-flash` engine included** — the plaintext of every message in flight. Capability, not behavior: the [source review of the fallback build](/images/docker.io/nearaidev/compose-manager/sha256-6e035c8fb99c1d31f41760d25af178683a658cca4afd73cd0d366fbb5bf884be.md) found no code path using it to read process memory. Unchanged from the base and from this host's previous measurement (there at `:147-155`).

### HIGH (deployed: ON, architectural) — the launcher holds the docker socket on the host network (structural)

`network_mode: host` (`:156`, rationale `:151-155`) + rw `/var/run/docker.sock` (`:158`) + the decrypted env ro (`:162`, rationale `:159-161`) + rw `work` (`:163`). Anything driving that socket can start a container with any privilege on this guest. Image digest-pinned and audited (Check 5). Unchanged from the base; on this host's previous measurement the same grant sat at `:186`/`:188`.

### MEDIUM (deployed: ON, capability unused at this config; new for this host) — the collector mounts the entire guest root filesystem and the docker socket (disk)

`/:/hostfs:ro` (`:22`, for `hostmetrics` `root_path` parity, `:16`, `:19-21`, `:257`), `/var/run/docker.sock:ro` (`:18`, for `docker_stats`, `:270-277`), `/var/lib/docker/containers:ro` (`:23`), as `user: "0:0"` (`:8`). The collector process can read **every file in the guest** — the decrypted operator env under `/hostfs/dstack/.host-shared/`, every docker volume including `work` (the launcher override state, Check 5) and `certs` (the TLS private key), and any file an engine ever writes. A read-only bind does not neutralize a unix socket (`:ro` blocks writes to files, not `connect()`), so the socket mount grants the full Docker API. At the measured config nothing reads arbitrary files (`filesystem` scraper collects mount metrics and excludes `/var/lib/docker` paths, `:265-268`) and nothing drives the socket beyond stats queries (`:270-277`). Carried from the gpu04 base — but **new relative to this host's previous measurement**, whose collector mounted `/var/lib/docker/containers` only and which that page recorded as a narrower read surface. That INFO advantage is reversed here.

### INFO (deployed: ON) — certbot holds the wildcard TLS key and the Cloudflare token in-enclave (disk)

`certs` volume (`:45`, `:66`, `:207-208`), Cloudflare token via config (`:47-49`, `:68-71`, `:211-213`), cert names `*.completions.near.ai` / `completions.near.ai` / `$HOSTNAME.hosts.near.ai` (`:80-83`), ECDSA P-256 (`:79`). TLS terminates inside the CVM — which is also why this harness does not need the dstack gateway for ingress (Check 7). No `privileged: true` anywhere in this document; no plaintext-handling service is present at this layer.

## Check 5 — Image pinning, identity, and the measured boot-path override restore **[priority item]**

### HIGH (deployed: ON, widened vs base) — the measured boot path replays a persisted compose-manager image override from a docker volume with no boot-time signature check, and the compose-manager it replaces can write that file (supply chain)

**What the base already found.** `image: ${COMPOSE_MANAGER_IMAGE:-nearaidev/compose-manager@sha256:6e035c8f…}` (`:95`) is a **default, not a pin**; `COMPOSE_MANAGER_IMAGE` is not in `allowed_envs`; the launcher persists each successful swap as `COMPOSE_MANAGER_IMAGE=<digest>` in `LAUNCHER_ENV_FILE=/app/work/.env.launcher` (`:172`) on the shared `work` volume (`:125`, `:163`) and feeds it to its own bundled compose on launcher-driven recreates. The gpu04 base and this host's previous page both rated that HIGH ("image-drift persistence" / "a default, not a pin").

**What this document adds — traced line by line.**

- *What it reads, from where:* `OVERRIDES="/var/lib/docker/volumes/dstack_work/_data/.env.launcher"` (`pre_launch_script:15`) — the guest-side backing directory of the compose named volume `work` (`:205`) under the compose project `dstack` (`LAUNCHER_COMPOSE_PROJECT=dstack`, `:171`; the `dstack_work` name follows the compose `<project>_<volume>` convention and the script's own comment at `:16-17` says this is where "the launcher persists the last image it swapped to"). Same bytes the launcher sees at `/app/work/.env.launcher` (`:172`). The script's comment asserts the path "assumes default Docker data-root; dstack CVMs always use this" (`:13-14`) — a guest-OS property not verified here. Whether that volume sits on an encrypted data disk is likewise a guest-OS property: the document does not say, and this page does not assert it (Not traced).
- *Who writes it:* the launcher (rw `work`, `:163`) after each cosign-verified swap, per the comment (`:16-17`, `:22-23`) and the launcher source page — **and anything else with write access to that volume or to the guest filesystem**: `compose-manager` itself mounts the same volume read-write (`work:/app/work`, `:125`), and both control-plane containers hold the docker socket (`:122`, `:158`), through which any container can be started with the volume mounted. The collector's `:ro` root mount (`:22`) can read it but not write it.
- *What it exports:* only the `COMPOSE_MANAGER_IMAGE` key (`:30-32` — `case "$k" in COMPOSE_MANAGER_IMAGE) export "$k=$v"`). The file is parsed as data, not sourced (`:11-13`; the comment names sourcing "a root-RCE path"). **The value is not validated** — no digest-format check, no repository check, no signature check; any string after `COMPOSE_MANAGER_IMAGE=` is exported and interpolated at `:95`. The script's own comment concedes the file may carry "an unvalidated digest" (`:12-13`).
- *Does it let the measured harness boot a different compose-manager than the measurement pins?* **Yes, by design, every boot** — the comment says so verbatim: "Restore the launcher's last-known-good image override so a CVM reboot doesn't revert compose-manager to the Ansible-baked fallback digest" (`:9-10`). The measured digest at `:95` is the *first-boot* value only.

**Widening, narrowing, or the same channel made explicit?** Two things at once. It is the **same channel** (the launcher-persisted override) **made explicit** in the measurement — a reader of the hash can now see the replay, where before it was launcher behaviour established only from source. And it is a **widening of the measured boot path**: the document's own comment (`:9-10`) states that under the base template a CVM reboot *reverted* compose-manager to the measured fallback, with drift re-established only after the launcher's next cosign-verified poll; now the boot itself installs the persisted reference **before** any launcher verification runs. The trust anchor at boot moves from "the measured digest, then a cosign-gated swap" to "whatever `.env.launcher` says". The cosign gate (`:179`) is enforced by the launcher at *swap* time, not by this script at *boot* time. The sharp consequence: the root-equivalent, ptrace-capable compose-manager holds read-write access (`:125`) to the file that decides which compose-manager boots next — a compromised or maliciously-signed manager build can name its own successor, and the guest will boot it without a signature check. The comment at `:22-23` says the launcher "re-seeds this file from the running container on its next successful poll"; whether a launcher poll would detect a running image that is not a channel-resolved, cosign-verified digest and swap it back — or would re-seed the rogue reference — is launcher source behaviour, not answered here (Not traced; the [`91fdff3c` page](/images/docker.io/nearaidev/compose-manager-launcher/sha256-91fdff3cfa3543d72656b2368c7d8a0a83d95a0f1087378c897aa1537acdba56.md) is the place to look). Compensating controls: the persisting writer is, at the audited launcher build, a post-cosign step; the volume is inside the CVM; and swaps are disclosed in the signed action log. Which digest runs *right now* is runtime state this layer cannot see. Same mechanism as the [gpu13](sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) and [gpu26](sha256-5c027cc86ab48fa98550f5be9be0621adff811fa231d955653396f6e13f85bd2.md) regenerations, here rated against a base that lacked it.

### INFO (deployed: ON) — both control-plane images resolve to audited builds, and the dead re-pin literal is consistent with the fallback (supply chain)

Fallback `compose-manager@sha256:6e035c8f…` (`:95`) — [published review](/images/docker.io/nearaidev/compose-manager/sha256-6e035c8fb99c1d31f41760d25af178683a658cca4afd73cd0d366fbb5bf884be.md) (*COMPROMISABLE by credentialed operator (control-plane; no data-plane role)*). Launcher `sha256:91fdff3c…` (`:148`) — [published review](/images/docker.io/nearaidev/compose-manager-launcher/sha256-91fdff3cfa3543d72656b2368c7d8a0a83d95a0f1087378c897aa1537acdba56.md) (*COMPROMISABLE by credentialed operator — this is the post-attestation mutation engine; no plaintext reach*). Both identical to the gpu04 base; both a step up from this host's previous measurement (`5165400d…` default / `78afb823…` launcher — audited, but older). The forced re-pin branch is **dead**: `if [ "false" = "true" ]` (`pre_launch_script:24`), and its literal (`:26-27`) names the **same** `6e035c8f…` as the compose fallback — the gpu13 page's stale-literal INFO does not apply here. Were a future measurement to flip that literal to `"true"`, the script would *overwrite* the persisted override with the pinned digest on every boot — which would be a **narrowing** of the finding above; at `"false"` it is inert. Third-party sidecars remain configuration-audited only: `otel/opentelemetry-collector-contrib@85ac41c2…` (`:5`, audited page), `certbot/dns-cloudflare@742dbd2e…` (`:41`, `:64` — digest-pinned, no source page, Not traced). No tag-only image in this document.

## Check 6 — Network topology and egress

### MEDIUM (deployed: ON) — one flat shared network spans management and the plaintext services that attach to it; no egress restriction (egress)

This document creates `dstack_default` (`:195-200`) so that "model compose files attach to this shared network" (`:196-198`). No `internal: true`, no egress control at any layer. Every audited recipe in this repo joins it with `external: true`; whether `prod/GLM-5.3-Flash-SGL-TP4.yaml` does was **not read** here (Not traced), but the network's stated purpose is that it does. Anything so attached — engine, E2EE-terminating proxy, nginx — shares this bridge with the `pid: host` + `SYS_PTRACE` + `docker.sock` compose-manager and the root-mounting collector, and, if the engine carries the fleet's known unauthenticated logging switch, keeps that switch reachable from any container on the bridge. Containment is available at this layer and not taken. Carried from the base and from this host's previous measurement (there `:225-230`).

- **Published ports:** compose-manager's deploy API `8080:8080` (`:98-99`), `BEARER_TOKEN`-authenticated (`:128`), on the guest host interface; the launcher is on the host network outright (`:156`), probing `http://127.0.0.1:8080/version` (`:174`). Whether `8080` is reachable from outside the CVM depends on dstack guest port handling (Not traced) — and on this document's `gateway_enabled: false`, see Check 7.
- **Named outbound destinations, all literal except the flagged env:** `https://telemetry.infra.near.ai` (`:341`); `https://github.com/nearai/cvm-compose-files` (`:127`); Docker registries (`:165`, credentials `:180-181`); Let's Encrypt + Cloudflare DNS (`:43`, `:72-83`, `:211-213`); the operator-set `SLACK_WEBHOOK_URL` (`:136`); S3/AWS only via credentials handed to compose-manager (no endpoint in this document). **`us3.datadoghq.com` — present on this host's previous measurement — is gone.**

## Check 7 — dstack-level settings: `gateway_enabled: false`, the 0.5.11 guest, and the sysctls **[priority item]**

Outer flags: `kms_enabled: true`, `local_key_provider_enabled: false`, `key_provider_id: ""`, `no_instance_id: false`, `secure_time: false`, **`public_logs: false`** (the dstack host-facing log endpoint is off — carried), `public_sysinfo: true` (metadata only). Two departures from every published measured page: `name: dstack-nvidia-0.5.11` and `gateway_enabled: false`.

### INFO (deployed: ON, guest-OS dependent) — gateway_enabled: false turns off the dstack gateway for this CVM; what that removes on the guest side is a question for the unreviewed 0.5.11 OS page (egress)

What this document shows: the flag is `false`, and nothing in the harness needs a dstack gateway for ingress — TLS terminates in-enclave on certbot-issued `*.completions.near.ai` certificates (`:79-83`), and the engine-facing ports are published by the inner recipe's nginx (fleet pattern; recipe not read). What the base pages say: they list `gateway_enabled: true` without analysis. What the published guest-OS page for the **0.5.5** guest ([`9b69bb16`](/os/sha256-9b69bb1698bacbb6985409a2c272bcb892e09cdcea63d5399c6768b67d3ff677.md)) describes as the gateway path is dstack-gateway registration over RA-TLS with the gateway app-id validated against the KMS, followed by a WireGuard tunnel **with an iptables chain that drops all UDP not from the registered gateway endpoints**. If `false` disables that registration on the 0.5.11 guest, the consequences cut both ways and this document cannot pick: **fewer host-facing services** (no gateway tunnel, no gateway-routed ingress into the CVM), but possibly also **a removed control** if the UDP drop chain is installed only as part of gateway setup. The `a6eafc5f…` OS page must answer: (a) what `gateway_enabled: false` disables in the 0.5.11 guest agent; (b) whether the WireGuard/iptables posture changes; (c) whether the guest's outbound path or the host-facing `8080` (Check 6) is affected; (d) whether the 0.5.11 rootfs still ships no AppArmor (`:113-116`). None of (a)-(d) is asserted here. Severity is INFO because the document shows a flag change and no plaintext path; it is **not** a mitigation claim.

### INFO (deployed: ON, resolves base MEDIUM) — vm.swappiness=0 is measured in; KV-cache pages are kept out of swap by a measured sysctl (disk)

`echo 0 > /proc/sys/vm/swappiness` (`pre_launch_script:5`, comment "prevent KV cache swap-out"). The gpu04 base and this host's previous page both carried a MEDIUM for the *absence* of this control — the previous gpu30 page noting that `dsv4-flash` clients landed on nodes with asymmetric swap posture. On this host that finding is **resolved**; this page states only what the document shows — the sysctl is set. Whether the 0.5.11 guest has any swap device at all, and whether the pre-launch script runs with the privilege to write `/proc/sys/vm` (it must, or the line fails silently — the script has no `set -e`), are guest-OS properties (Not traced). `dirty_ratio=5` / `dirty_background_ratio=1` (`:6-7`) lower the page-cache writeback thresholds — they change *when* dirty file pages reach disk, not *whether* or *what*; no file content is created by them. Privacy-neutral as far as the document shows.

### INFO (deployed: ON) — the k256 app key is exported into the compose env namespace; no consumer exists in this document (log)

`export ENCRYPTION_KEY=$(cat /dstack/.host-shared/.appkeys.json | jq -r .k256_key)` (`pre_launch_script:2`), carried verbatim from the base. No service in this compose references `${ENCRYPTION_KEY}` (grep-verified). The script's two `echo` lines (`:27`, `:33`) print a path and an image reference, never the key; where pre-launch stdout goes is a dstack/guest-OS property (Not traced). In-CVM key handling; no egress path configured.

## Host lineage — what the harness regeneration resolved on gpu30, and what persists

This hash replaces [`66007399…`](sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md) on the physical label `gpu30` (both `INSTANCE_LABEL=gpu30`: that page's `:165`, this document's `:135`). Per the capture the host was also repurposed — from `qwen35-122b` + `dsv4-flash` to `glm-5-3-flash` — so that page's model-specific cross-node couplings no longer describe this host. Its findings, each checked against this document:

**Resolved by the regeneration:**
- **[HIGH, C3 wide shape] The measured Datadog agent shipping all container stdout to `us3.datadoghq.com`** — gone (Check 1 INFO). Off-box log destinations by measurement: two → one.
- **[MEDIUM] Two open ingestion surfaces feeding Datadog** (OTLP-logs gRPC `0.0.0.0:4317`, non-local dogstatsd) — gone; no network receiver exists in this document.
- **journald collection with the in-manifest `TODO: Add filters`, and `DD_PROCESS_AGENT_ENABLED` process-cmdline shipping** — gone.
- **[INFO] `DD_API_KEY` live** — now inert (Check 2).
- **[MEDIUM] No `vm.swappiness=0`** — resolved (`pre_launch_script:5`, Check 7).
- **Control-plane builds** `compose-manager` default `5165400d…` → `6e035c8f…` (`:95`), launcher `78afb823…` → `91fdff3c…` (`:148`) — both newer audited builds.

**Persisting, re-verified at this document's lines:**
- **[HIGH] compose-manager root-equivalent + ptrace** (`:117-125`); **[HIGH] launcher docker.sock on host network** (`:156-163`); **[HIGH] image default, not a pin** (`:95`) — **now widened** by the measured boot replay (Check 5); **[HIGH, C4] flat `dstack_default`** (`:195-200`); **[MEDIUM] `CM_MIN_TAG_AGE_HOURS` default 0** (`:130`, `:169`); **[HIGH-class control primitives] `BEARER_TOKEN`, `LAUNCHER_CHANNEL`** (`:128`, `:170`); **[MEDIUM] `SLACK_WEBHOOK_URL`** (`:136`); the 22-entry `allowed_envs`, byte-identical; unscrubbed management export to `https://telemetry.infra.near.ai` (`:341`, `:360`); `8080:8080` (`:98-99`).

**New on this host, absent from the previous measurement:**
- **[MEDIUM] the collector's `/:/hostfs:ro` + `docker.sock:ro` mounts** (`:18`, `:22`) with `hostmetrics`/`docker_stats` scrapers (`:255-277`) — that page's "narrower collector" INFO advantage is reversed.
- **[HIGH, widened] the boot-path override replay** (`pre_launch_script:29-34`).
- **`gateway_enabled: false`** and **`dstack-nvidia-0.5.11`** (outer fields) — guest-OS hand-offs.
- **`num_consumers: 1`** (`:346`) — INFO.

## Verdict

**QUALIFIED PASS.** Within hardware-measured config + `allowed_envs`, the invariant holds: the 22-entry env list contains no content, log-level, or telemetry switch; the one measured off-box log path is management-only, to one literal near.ai endpoint, through a filter that is fail-closed by construction and by OTTL semantics; and for this physical host the regeneration is a material improvement — the Datadog collect-all agent, its two open ingestion surfaces, journald and process-cmdline shipping are all gone from the measurement, both control-plane images are audited builds, and `vm.swappiness=0` is now measured in. The qualifications are the fleet's architecture plus two things this document makes worse or leaves open: (C2, **widened**) the measured boot path now installs whatever compose-manager reference `.env.launcher` holds, without a boot-time signature check, from a volume the root-equivalent, ptrace-capable compose-manager itself can write — the base's "default, not a pin" is now "a default overridden by mutable disk state on every boot"; (C1) the plaintext stack is log-pinned, not measured, with the tag-age floor at 0 — and this host exercised that channel three times on the capture day; (C3) engine stdout still leaves the CVM through the recipe's collector with no redaction anywhere; (C4) the flat network this document defines keeps any engine admin surface reachable; the collector's guest-root mount is new for this host; and `gateway_enabled: false` on an unreviewed 0.5.11 guest OS is a flag change whose net effect on host-facing surface — fewer services, or a removed UDP filter — this document cannot decide.

## Not traced

- **Live attestation and TDX quote verification** — the fresh-nonce fetch and `sha256(app_compose) == compose_hash` check were the orchestrator's; this review re-hashed the captured bytes only.
- **Fleet placement** — "live on 1 host, `glm-5-3-flash`, serving `z-ai/glm-5.3-flash`" is from the orchestrator's capture; not re-derived.
- **The inner recipe `prod/GLM-5.3-Flash-SGL-TP4.yaml`** — under separate review, **not read** here; no claim about its engine flags, its collector, or even that it joins `dstack_default`. The "three redeploys on 2026-09-01" is the orchestrator's observation of the action log.
- **The 0.5.11 guest OS (`os_image_hash` `a6eafc5f…`)** — no page. Everything Check 7 lists: what `gateway_enabled: false` disables, the WireGuard/iptables posture without the gateway, whether a swap device exists, whether AppArmor is still absent, whether the Docker data-root is at the path the pre-launch script assumes (`pre_launch_script:13-14`), whether the `dstack_work` volume sits on an encrypted data disk, with what privilege and to what stdout the pre-launch script runs, and whether `8080` is reachable from outside the CVM.
- **The currently-running compose-manager digest** — runtime state (`.env.launcher` + action log), invisible to this layer by design (Check 5).
- **Launcher behaviour on a non-verified running image** — whether a launcher poll would swap back a compose-manager that booted from a tampered `.env.launcher`, or re-seed it (`pre_launch_script:22-23`); a launcher source question for the `91fdff3c` page.
- **The contents of `/dstack/.host-shared/.decrypted-env`** — `allowed_envs` constrains names on the measured channel; values, and anything spliced via `/compose/up`, are not visible here.
- **`certbot/dns-cloudflare@742dbd2e…`** — digest-pinned, no source page; configuration-audited only.
- **Collector binary semantics** — the fail-closed and `num_consumers` analyses rely on documented OTTL / filterprocessor / exporterhelper contracts for the deployed collector line, not on the `85ac41c2…` binary.
