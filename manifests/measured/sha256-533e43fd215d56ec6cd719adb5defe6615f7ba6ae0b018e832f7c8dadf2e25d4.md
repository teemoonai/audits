# measured compose @ 533e43fd... (GPU-node measured app_compose — gpu02, the second CVM behind `glm-5-3-flash` since 2026-09-09: byte-identical to c82b1a2e except the host label)

## verdict: QUALIFIED PASS at the manifest layer — this document is the c82b1a2e harness with one line changed (INSTANCE_LABEL=gpu04 to gpu02, a Slack-notification string that reaches no telemetry attribute and no content path), so every base finding carries over at the same line numbers: the management-only collector is fail-closed twice over, both control-plane images resolve to audited builds, vm.swappiness=0 is measured in, and the off-box log destination count is one; but the measured boot path replays a persisted compose-manager image override from a docker volume with no boot-time signature check, the root-equivalent ptrace-capable compose-manager holds write access to that file, the collector mounts the entire guest root filesystem, the compose-fetch tag-age floor is an operator env defaulting to 0, and the plaintext stack this harness launched is log-pinned, not measured — twenty-four signed actions on 2026-09-09 including a full compose_up of a revision that defines a DeepSeek-V4-Flash engine service between 02:24Z and 04:55Z; for gpu02 this measurement replaces the 0.5.5 gateway-on harness d98a2568, which already carried the override replay, the swappiness sysctl and the guest-root mount, so the host-level change is the upstream 0.5.11 guest OS a6eafc5f (armed core-dump path and no LSM per its page), gateway off, and a serialized export queue; the sibling's different mr_aggregated is accounted for by exactly two RTMR3 events, compose-hash and instance-id

| | |
|---|---|
| **compose_hash** | `533e43fd215d56ec6cd719adb5defe6615f7ba6ae0b018e832f7c8dadf2e25d4` |
| **role** | GPU-node measured `app_compose` — management harness only, `INSTANCE_LABEL=gpu02` (`:135`). Per the orchestrator's 2026-09-10 fetch this hash is live on **1 CVM**: the second of two CVMs load-balanced behind `glm-5-3-flash.completions.near.ai` (instance `fccd496ae28c…`; 15 of 30 parallel samples landed here, 15 on the sibling), serving `z-ai/glm-5.3-flash`; the other CVM measures [`c82b1a2e…`](sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md) (gpu04), this page's base. This CVM's `compose_manager_started` is 2026-09-09T02:14:50Z; it replaced gpu30 ([`341313ae…`](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md)) in the pair. Fleet placement and the sample split are from that capture, not re-derived here (Not traced). The physical label `gpu02` previously carried [`d98a2568…`](sha256-d98a2568546afce9ab53f7eb4d4b2058e5497e1efd05a8ad0934793043240ed7.md); host lineage is diffed below |
| **identity** | hardware-measured: the orchestrator fetched the live attestation report from `glm-5-3-flash.completions.near.ai` on 2026-09-10 with a fresh nonce, landed on instance `fccd496ae28c…`, and checked `sha256(info.tcb_info.app_compose) == info.compose_hash` before saving; re-verified locally two ways — `shasum -a 256` over the captured 18,212-byte JSON document returns exactly `533e43fd215d56ec6cd719adb5defe6615f7ba6ae0b018e832f7c8dadf2e25d4`, the `compose_hash` this page is keyed on (the base capture likewise returns `c82b1a2e…`); and in the orchestrator's saved attestation report for this instance, `sha256(tcb_info.app_compose)` recomputed here equals the report's own `compose_hash`, with the captured document string-equal to that `app_compose` value (18,202 characters; the 10-byte difference is UTF-8 width of the em dashes in comments). The report's `event_log` carries the same value as its `compose-hash` RTMR3 event (Check 8). Report facts read from the JSON: `app_name dstack-nvidia-0.5.11`, `os_image_hash a6eafc5f…`, `app_id 2c0a0c96…` (equal to the sibling's), `mr_aggregated 18d12286…` (sibling `34255d6a…`), `vm_config` 64 vCPU / 2,199,023,255,552 bytes / 8 GPUs / 4 NVSwitches / `hotplug_off: true` (byte-equal to the sibling's `vm_config`). I did not perform the live fetch or the TDX quote-signature verification myself (Not traced) |
| **dstack** | `manifest_version: 2`, `name: dstack-nvidia-0.5.11`, `runner: docker-compose`, `kms_enabled: true`, `gateway_enabled: false`, `local_key_provider_enabled: false`, `key_provider_id: ""`, **`public_logs: false`**, `public_sysinfo: true`, `no_instance_id: false`, `secure_time: false` — every outer field byte-identical to the base. The guest OS under this harness is `os_image_hash` `a6eafc5f…`: [`os/a6eafc5f`](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) (QUALIFIED PASS at measured configuration) |
| **review** | reviewed 2026-09-10 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review; base = [`c82b1a2e…`](sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md) (gpu04, the other CVM behind the same hostname), itself a one-line delta of [`341313ae…`](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md) (gpu30, the original full review, to which the "full trace" links below point). Both documents were parsed as JSON: the same **14** keys (12 scalar/list fields plus the two embedded documents) with every non-embedded value equal, including the 22-entry `allowed_envs` in identical order; the `docker_compose_file` (**374 lines** in both) and `pre_launch_script` (**34 lines** in both) were extracted programmatically and byte-diffed: the pre-launch scripts are identical (`diff` exit 0); the compose files differ in **exactly one hunk, one line** (`:135`). The orchestrator's unfolded copy is byte-equal to my own extraction. Line citations `:NN` are this document's embedded compose's own line numbers; `pre_launch_script:NN` the script's; outer dstack fields are cited by name. Because the only differing line is `:135`, every citation on the base page re-locates at the **identical** line number in this document — and each line cited below was re-read from this document's extraction (both halves read in full), not inferred from the diff. The plaintext-handling stack is **not in this measurement**: on this CVM it is whatever the signed compose-manager action log (24 entries, Check 8) has layered from `prod/GLM-5.3-Flash-SGL-TP4.yaml` and its `-Canary` variant; of the revisions named there, only [`d76413cd`](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d76413cd231a4e871a515e4fa829c6510a374fe335e77fae3ea5a6dccf783909.md) (and its sibling [`d1cc22eb`](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d1cc22eb8a25890e42a090d2c309b5045cdf374e000ed7228d9394f36f7f96d1.md)) has a published page (LEAKS, narrowly); `2878b9c4` and `e5d80ab6` are under concurrent review; `a7549a89`, `5a5b7228`, `a2d3f073` have no page. None of those recipes was read for this page beyond one service-name grep noted in Check 8; under the repo's fail-closed rule no claim is made about them here, including whether they join the network this document defines (Not traced).

---

# Deployment Manifests & Configuration — Privacy Audit (harness leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-09-10).
> Method: [/notes/method.md](/notes/method.md) §4 (manifest-only checks) · Base: [c82b1a2e](sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md) (gpu04, same hostname) · Full review: [341313ae](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md) (gpu30) · Host lineage: [d98a2568](sha256-d98a2568546afce9ab53f7eb4d4b2058e5497e1efd05a8ad0934793043240ed7.md) (gpu02's previous measured compose) · Guest OS: [a6eafc5f](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) · Same-regeneration siblings: [d4c89033](sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) (gpu13), [5c027cc8](sha256-5c027cc86ab48fa98550f5be9be0621adff811fa231d955653396f6e13f85bd2.md) (gpu26)

## Check 1 — What changed from the audited base, enumerated

Re-derived by byte diff of the extracted files, not taken from the orchestrator's summary. Relative to [`c82b1a2e`](sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md):

**Outer document:** nothing. `name: dstack-nvidia-0.5.11`, `gateway_enabled: false`, `public_logs: false`, `kms_enabled: true`, `public_sysinfo: true`, `no_instance_id: false`, `secure_time: false`, `local_key_provider_enabled: false`, `key_provider_id: ""`, `runner: docker-compose`, `manifest_version: 2`, and the 22-entry `allowed_envs` (byte-identical order) — all equal.

**`pre_launch_script`:** nothing. 34 lines, `diff` exit 0. The sysctls (`pre_launch_script:5-7`), the `OVERRIDES` path (`:15`), the dead `if [ "false" = "true" ]` re-pin branch naming `6e035c8f…` (`:24-28`) and the live `COMPOSE_MANAGER_IMAGE` loader (`:29-34`) are the base's bytes.

**Compose (one hunk, 374 → 374 lines):**
1. **`INSTANCE_LABEL=gpu04` → `gpu02`** (`:135`). A compose-manager environment entry, inside that service's `environment:` block (`:126-136`). Its consumers in this document, grep-verified from this extraction: the string `INSTANCE_LABEL` occurs at exactly two lines — the comment at `:132` ("INSTANCE_LABEL identifies this host in the message ("Deployed on gpuXX")", `:132-134`) and the assignment at `:135`; the string `gpu02` occurs only at `:135`. Compose `environment:` entries are per-service, and the collector's own block (`:10-16`) lists `MONITORING_INGEST_TOKEN`, `CVM_NAME`, `CVM_HOST`, `DD_HOSTNAME`, `ENV`, `CVM_HOSTMETRICS_ROOT_PATH` — the label is never handed to the collector process. It is **not** referenced by the collector's `resource` processor (`:293-309`, whose attributes come from `DD_HOSTNAME`, `CVM_NAME`, `CVM_HOST`, `ENV` at `:296`, `:299`, `:302`, `:305`, plus the literal `near-ai` at `:308`), not by the `docker_stats` label map (`:275-277`, compose service/project labels only), not by the `transform` statements (`:316-319`, container labels only), not by any receiver, exporter, or `labels:` entry, and not by the pre-launch script (grep, zero hits). The `HOSTNAME` in the cert request (`:83`) is the `allowed_envs` variable, not this label. Finding (INFO) below.

**Carried unchanged — each re-verified against this document at the identical line number, not assumed:** otelcol image digest `85ac41c2…` (`:5`), `user: "0:0"` (`:8`), `mem_limit: 768m` (`:9`), `MONITORING_INGEST_TOKEN` (`:11`), mounts `/var/run/docker.sock:ro` (`:18`), `/:/hostfs:ro` (`:22`), `/var/lib/docker/containers:ro` (`:23`), storage volume (`:24`), the collector's own `logging:` block (`:33-37`, driver `local`); certbot/certbot-init at `certbot/dns-cloudflare@742dbd2e…` (`:41`, `:64`) with the `certs` volume (`:45`, `:66`) and the Cloudflare token config (`:213`), ECDSA P-256 (`:79`), cert names `completions.near.ai` + `*.completions.near.ai` + `$HOSTNAME.hosts.near.ai` (`:80-83`); compose-manager's fallback-image default `6e035c8f…` (`:95`), deploy API `8080:8080` (`:98-99`), the nsenter/AppArmor comment (`:100-116`, "dstack-OS does NOT ship AppArmor" at `:113`), `pid: host` (`:117`), `cap_add: [SYS_ADMIN, SYS_PTRACE]` (`:118-120`), rw `docker.sock` + `dstack.sock` + decrypted env ro (`:122-124`), `work` volume (`:125`), `GITHUB_REPO=https://github.com/nearai/cvm-compose-files` (`:127`), `BEARER_TOKEN` (`:128`), `MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`), `ENV_FILES=/app/.env` (`:131`), `SLACK_WEBHOOK_URL=${SLACK_WEBHOOK_URL:-}` (`:136`); the launcher at digest `91fdff3c…` (`:148`), `network_mode: host` (`:156`), rw `docker.sock` (`:158`), decrypted env ro (`:162`), `work` (`:163`), `COMPOSE_MANAGER_IMAGE_REPO=nearaidev/compose-manager` (`:165`), `CM_MIN_TAG_AGE_HOURS` passthrough (`:169`), `LAUNCHER_CHANNEL=${LAUNCHER_CHANNEL:-latest}` (`:170`), `LAUNCHER_COMPOSE_PROJECT=dstack` (`:171`), `LAUNCHER_ENV_FILE=/app/work/.env.launcher` (`:172`), `LAUNCHER_BASE_ENV_FILE=/dstack-env` (`:173`), health URL `http://127.0.0.1:8080/version` (`:174`), `LAUNCHER_HEALTH_TIMEOUT=180` (`:178`), the cosign identity regexp (`:179`), registry credentials (`:180-181`), `LAUNCHER_STATE_FILE` (`:182`); the four `com.datadoghq.ad.logs` labels (`:54`, `:85`, `:138`, `:186`) and their log-driver exports (`:60`, `:91`, `:144`, `:192`); the shared `dstack_default` network with its comment (`:195-200`); volumes `otelcol-management-storage` (`:203`), `work` (`:205`) and `certs` (`:207-208`); the collector config: `filelog/docker_containers` (`:222`), glob (`:223`), `start_at: end` (`:224`), `on_error: send_quiet` (`:235`, `:250`), the unconditional `add` of `service.name: cvm-docker` (`:251-253`), `hostmetrics/cvm` with `root_path` (`:255`, `:257`), `filesystem` scraper exclusions (`:265-268`), `docker_stats/cvm` (`:270-277`), `prometheus/self` (`:279`), `transform/docker_metadata` (`:311`, statements `:316-319`), `filter/management_logs` (`:321`, `error_mode: ignore` `:322`, expression `:325`), the literal endpoint `https://telemetry.infra.near.ai` (`:341`) with `Authorization: Bearer $${env:MONITORING_INGEST_TOKEN}` (`:343`), the FIFO comment (`:344`) and `sending_queue` (`:345-348`: `num_consumers: 1`, `queue_size: 128`, `storage: file_storage`), `extensions:` (`:350`) with directory `/var/lib/otelcol-management/storage` (`:352`); pipeline `logs/cvm` (`:358`) receivers (`:359`), processors (`:360`) and exporter (`:361`); the two metrics pipelines (`:362-369`); collector self-telemetry `logs: level: info` (`:373-374`). Grep-verified absent in both the compose and the script, as on the base: `datadog/agent`, `4317`, `dogstatsd`, `journald`, `DD_SITE`, `privileged`, `internal: true`, `${ENCRYPTION_KEY}`; `DD_API_KEY` occurs nowhere in either (only in `allowed_envs`); `datadog` occurs at exactly ten compose lines (`:54`, `:60`, `:85`, `:91`, `:138`, `:144`, `:186`, `:192`, `:317`, `:318`); `COMPOSE_MANAGER_IMAGE` at `:95`, `:165`, `pre_launch_script:26`, `:31`.

### INFO (deployed: ON) — INSTANCE_LABEL=gpu02 is a host-identity string for the Slack deploy notification; it is metadata that reaches no telemetry attribute and no content path (egress)

The one changed line. `INSTANCE_LABEL` is consumed only by compose-manager (`:135`), whose in-document comment says it "identifies this host in the message ("Deployed on gpuXX")" (`:132-133`) sent to the operator-supplied `SLACK_WEBHOOK_URL` (`:136`, empty = off, `:134`). It is not passed to the collector container (`:10-16`), does not appear in the collector's `resource` attributes (`:293-309`), and is not in `allowed_envs` (it is a literal, so changing it changes the measurement — which is exactly why this document has a different hash from the base). Its privacy content is "which physical host deployed" — a label, never a message. The operator-chosen egress destination it rides on is the carried `SLACK_WEBHOOK_URL` control primitive ([full trace, Check 2](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#check-2--allowed_envs-the-operators-measurement-free-knob-set)), unchanged. For a client the consequence is again the positive one: the two CVMs behind `glm-5-3-flash.completions.near.ai` run **byte-identical measured harnesses** apart from this label, as the gpu30/gpu04 pair did before gpu30 left. Whether the two CVMs run the same inner recipe layering is action-log state, not measurement (Check 8, Not traced beyond what the two saved logs show).

### INFO (deployed: ON) — no Datadog agent in the measurement; one off-box log destination by measurement, management records only (telemetry)

Carried; for gpu02 this is **continuity** — the label's previous harness `d98a2568` was the no-Datadog side of its own node lottery (that page's comparison table). Grep-verified in this document: the only `datadog` strings are the `com.datadoghq.ad.logs` labels (`:54`, `:85`, `:138`, `:186`; exported by the log driver at `:60`, `:91`, `:144`, `:192`) and the transform that parses them (`:317-318`); no `4317`, `dogstatsd`, `journald`, `DD_SITE`. Receivers are `filelog/docker_containers` (`:222`), `hostmetrics/cvm` (`:255`), `docker_stats/cvm` (`:270`), `prometheus/self` (`:279`) — no network ingestion surface at the measured layer. Full trace: [341313ae, Check 1 INFO](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#check-1--what-changed-from-the-audited-base-enumerated).

## Check 2 — `allowed_envs` (the operator's measurement-free knob set)

**Result: no content switch, no log-level switch, no telemetry-destination switch. 22 entries, byte-identical to the base; 22 on `d98a2568` per its page, every name that page enumerates present here.** Verbatim from this document:

```
BEARER_TOKEN, CM_MIN_TAG_AGE_HOURS, SLACK_WEBHOOK_URL, AWS_REGION,
AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_S3_BUCKET, CERTBOT_EMAIL,
CLOUDFLARE_API_TOKEN, DOCKER_REGISTRY_USER, DOCKER_REGISTRY_TOKEN,
LAUNCHER_CHANNEL, DD_API_KEY, HUGGING_FACE_HUB_TOKEN, PROXY_TOKEN, ENV,
HOST_IP, DD_HOSTNAME, HOSTNAME, MONITORING_INGEST_TOKEN, CVM_NAME, CVM_HOST
```

Consumers re-located in this document at the base's line numbers: `CM_MIN_TAG_AGE_HOURS` (`:130`, `:169`), `BEARER_TOKEN` (`:128`) for the `8080:8080` deploy API (`:98-99`), `LAUNCHER_CHANNEL` (`:170`) against the literal cosign regexp (`:179`), `SLACK_WEBHOOK_URL` (`:136`), `MONITORING_INGEST_TOKEN` (`:11`, `:343`) selecting the account for the literal endpoint (`:341`), `DD_HOSTNAME`/`CVM_NAME`/`CVM_HOST`/`ENV` as resource attributes (`:296`, `:299`, `:302`, `:305`; passed in at `:12-15`), `HOSTNAME` in the cert request (`:83`), `CERTBOT_EMAIL` (`:77`), `CLOUDFLARE_API_TOKEN` (`:213`), `DOCKER_REGISTRY_*` (`:180-181`); `DD_API_KEY` consumed by nothing (vestigial, drift-ready); `HUGGING_FACE_HUB_TOKEN`, `PROXY_TOKEN`, `AWS_*`, `HOST_IP` pass through `ENV_FILES=/app/.env` (`:131`) / `LAUNCHER_BASE_ENV_FILE=/dstack-env` (`:173`) to the inner stack. No env here can raise the collector's log level (`:373-374`), alter the filter (`:325`), or add an exporter. `COMPOSE_MANAGER_IMAGE` is not in the list; its only measured setter is `pre_launch_script:29-34`. Full trace: [341313ae, Check 2](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#check-2--allowed_envs-the-operators-measurement-free-knob-set).

### MEDIUM (deployed: ON, carried) — the compose-fetch tag-age floor is an operator env defaulting to zero (supply chain)

`MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`; launcher `:169`) — identical to the base and, per its page, to `d98a2568` (`:130`, `:169` there too). On this CVM the channel was exercised at scale on its first day: the signed log shows six distinct `file_sha256` values across seven tags (`v0.0.380`, `.394`, `.402`, `.403`, `.404`, `.408`, `.409`) between 02:24:56Z and 23:53:31Z on 2026-09-09 (Check 8). Each such deploy is disclosed in the signed action log, the compensating control (C1). Full trace: [341313ae, Check 2 MEDIUM](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#medium-deployed-on-carried--the-compose-fetch-tag-age-floor-is-an-operator-env-defaulting-to-zero-supply-chain).

## Check 3 — The otelcol-management log pipeline

**Result: fail-CLOSED by two independent mechanisms, re-verified at this document's lines** — the unconditional `add` of `service.name: cvm-docker` (`:251-253`) and the three-way inequality in `filter/management_logs` (`:325`), between the CVM-wide `filelog` glob (`:223`) and the literal endpoint (`:341`) via the `logs/cvm` processor chain (`:360`). Pipeline bytes identical to the base.

### INFO (deployed: ON) — num_consumers: 1 serializes export dispatch; it changes neither what is collected, filtered, nor where it goes (telemetry)

`:346` under the FIFO comment (`:344`), block `:345-348`. Carried; new relative to `d98a2568` for this host (that page's line citations place its collector two lines shorter, Host lineage). Full trace: [341313ae, Check 3 INFO](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on-new--num_consumers-1-serializes-export-dispatch-it-changes-neither-what-is-collected-filtered-nor-where-it-goes-telemetry).

### MEDIUM (deployed: OFF, one condition wide) — every engine's stdout is ingested by the measured collector and excluded by filter expression, not by scoping (telemetry)

Glob `:223`, filter `:325`, default `:251-253`; no redaction stage on `:360`; admission by spoofable label (`:316-317`). Identical to the base and, per its page, to `d98a2568`. Full trace: [341313ae, Check 3 MEDIUM](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#medium-deployed-off-one-condition-wide--every-engines-stdout-is-ingested-by-the-measured-collector-and-excluded-by-filter-expression-not-by-scoping-telemetry).

## Check 4 — Privileges and mounts

### HIGH (deployed: ON, architectural) — compose-manager is root-equivalent and can read the engine's process memory (structural)

`pid: host` (`:117`), `cap_add: [SYS_ADMIN, SYS_PTRACE]` (`:118-120`), rw `docker.sock` (`:122`), `dstack.sock` (`:123`), decrypted env ro (`:124`), rw `work` (`:125`); comment `:100-116`. Identical to the base and to `d98a2568` (`:117`, `:118-120`, `:122` on that page). The substrate under it is the same as the base's: the [`a6eafc5f` OS page](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) reports the 0.5.11 kernel built with `CONFIG_SECURITY` unset (no AppArmor, no Yama `ptrace_scope`) — cited as published, not re-derived; a property outside this document, so a residual, not a change in this finding's severity. Full trace: [341313ae, Check 4 HIGH](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#high-deployed-on-architectural--compose-manager-is-root-equivalent-and-can-read-the-engines-process-memory-structural).

### HIGH (deployed: ON, architectural) — the launcher holds the docker socket on the host network (structural)

`network_mode: host` (`:156`), rw `docker.sock` (`:158`), decrypted env ro (`:162`), rw `work` (`:163`), image `91fdff3c…` (`:148`). Identical to the base and to `d98a2568` (`:156`, `:158`, `:162`, `:148` there). Full trace: [341313ae, Check 4 HIGH (launcher)](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#high-deployed-on-architectural--the-launcher-holds-the-docker-socket-on-the-host-network-structural).

### MEDIUM (deployed: ON, capability unused at this config) — the collector mounts the entire guest root filesystem and the docker socket (disk)

`/:/hostfs:ro` (`:22`), `/var/run/docker.sock:ro` (`:18`), `/var/lib/docker/containers:ro` (`:23`), `user: "0:0"` (`:8`); scrapers `:255-277`, `filesystem` exclusions at `:265-268`. Identical to the base. **Not new for gpu02** — `d98a2568` already mounted the guest root (`:18-23` on that page; it was the axis on which gpu02 was *worse* than gpu11 in that pair). Full trace: [341313ae, Check 4 MEDIUM](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#medium-deployed-on-capability-unused-at-this-config-new-for-this-host--the-collector-mounts-the-entire-guest-root-filesystem-and-the-docker-socket-disk).

### INFO (deployed: ON) — certbot holds the wildcard TLS key and the Cloudflare token in-enclave (disk)

`certs` (`:45`, `:66`, `:207-208`), token config (`:211-213`), names `:80-83`, ECDSA P-256 (`:79`); no `privileged: true` anywhere (grep-verified). Identical to the base. Full trace: [341313ae, Check 4 INFO](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on--certbot-holds-the-wildcard-tls-key-and-the-cloudflare-token-in-enclave-disk).

## Check 5 — Image pinning, identity, and the measured boot-path override restore **[priority item]**

### HIGH (deployed: ON, carried from d98a2568) — the measured boot path replays a persisted compose-manager image override from a docker volume with no boot-time signature check, and the compose-manager it replaces can write that file (supply chain)

`pre_launch_script:9-34`, byte-identical to the base: `OVERRIDES=/var/lib/docker/volumes/dstack_work/_data/.env.launcher` (`:15`), parsed as data (`:30-32`), exporting an **unvalidated** `COMPOSE_MANAGER_IMAGE` (`:31`) that interpolates into the default-not-a-pin at `:95`; the file is rw to compose-manager (`work:/app/work`, `:125`) and the launcher (`:163`); the cosign gate (`:179`) is enforced at swap time, never at boot. Unlike gpu30 and gpu04, for gpu02 this is **not a widening**: `d98a2568` was the one measured document of its generation whose own pre-launch script already performed this replay (its page's Check 6 HIGH, with the same `OVERRIDES` path, the same dead re-pin branch naming `6e035c8f…`, and the same data-parse loader quoted verbatim there). What the signed log adds for this boot: the `compose_manager_started` entry at 2026-09-09T02:14:50Z records image `nearaidev/compose-manager@sha256:6e035c8f…` — the measured fallback — so on this CVM's first boot the override file either did not exist or named that same digest; the log cannot distinguish the two. The OS page reports (cited, not re-derived) that the script runs as root via `source` inside `app-compose.service` with the decrypted env loaded, that docker's data-root is the default the script assumes (`:13-14`), and that the `dstack_work` volume sits on the KMS-keyed LUKS2 data disk. Full trace: [341313ae, Check 5 HIGH](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#high-deployed-on-widened-vs-base--the-measured-boot-path-replays-a-persisted-compose-manager-image-override-from-a-docker-volume-with-no-boot-time-signature-check-and-the-compose-manager-it-replaces-can-write-that-file-supply-chain).

### INFO (deployed: ON) — both control-plane images resolve to audited builds, and the dead re-pin literal is consistent with the fallback (supply chain)

Fallback `compose-manager@6e035c8f…` (`:95`; [review](/images/docker.io/nearaidev/compose-manager/sha256-6e035c8fb99c1d31f41760d25af178683a658cca4afd73cd0d366fbb5bf884be.md)), launcher `91fdff3c…` (`:148`; [review](/images/docker.io/nearaidev/compose-manager-launcher/sha256-91fdff3cfa3543d72656b2368c7d8a0a83d95a0f1087378c897aa1537acdba56.md)), dead branch `if [ "false" = "true" ]` (`pre_launch_script:24`) naming the same `6e035c8f…` (`:26-27`); `certbot/dns-cloudflare@742dbd2e…` (`:41`, `:64`) digest-pinned, no source page. Both control-plane digests are the **same** ones `d98a2568` carried (`:95`, `:148` in its comparison table) — no image change for gpu02 in this regeneration, and the running compose-manager at boot was that fallback per the action log. Full trace: [341313ae, Check 5 INFO](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on--both-control-plane-images-resolve-to-audited-builds-and-the-dead-re-pin-literal-is-consistent-with-the-fallback-supply-chain).

## Check 6 — Network topology and egress

### MEDIUM (deployed: ON) — one flat shared network spans management and the plaintext services that attach to it; no egress restriction (egress)

`dstack_default` (`:195-200`), no `internal: true` (grep-verified absent); published `8080:8080` (`:98-99`); launcher on host network (`:156`); literal destinations `:341`, `:127`, `:165`, `:43`, `:72-83`, `:211-213`, plus operator-set `SLACK_WEBHOOK_URL` (`:136`). Identical to the base and to `d98a2568` (its C4). Whether any of the log-pinned recipe revisions (Check 8) joins this network was not read (Not traced). Full trace: [341313ae, Check 6 MEDIUM](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#medium-deployed-on--one-flat-shared-network-spans-management-and-the-plaintext-services-that-attach-to-it-no-egress-restriction-egress).

## Check 7 — dstack-level settings: `gateway_enabled: false`, the 0.5.11 guest, and the sysctls

### INFO (deployed: ON, guest-OS answered) — gateway_enabled: false turns off the dstack gateway for this CVM; the a6eafc5f OS page reports what that removes on the guest side (egress)

The flag is `false` (outer field, identical to the base; `true` on `d98a2568`); TLS terminates in-enclave (`:79-83`). The OS page — cited as published, not re-derived here — reports that the flag disables gateway registration, the WireGuard tunnel and its iptables chain as one unit; that the guest's outbound path is unchanged; that `8080` reachability from outside is a VMM property it leaves Not traced; and that the 0.5.11 rootfs ships no LSM. Severity stays INFO: a flag change and no plaintext path. Full trace: [341313ae, Check 7 INFO (gateway)](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on-guest-os-dependent--gateway_enabled-false-turns-off-the-dstack-gateway-for-this-cvm-what-that-removes-on-the-guest-side-is-a-question-for-the-unreviewed-0511-os-page-egress).

### INFO (deployed: ON, carried from d98a2568) — vm.swappiness=0 is measured in; KV-cache pages are kept out of swap by a measured sysctl (disk)

`echo 0 > /proc/sys/vm/swappiness` (`pre_launch_script:5`); `dirty_ratio=5` / `dirty_background_ratio=1` (`:6-7`) privacy-neutral. For gpu02 this is continuity, not a resolution — `d98a2568` already carried it as an INFO-positive. The OS page reports the script runs as root (so the write succeeds) and that swap, if any, sits on the encrypted data disk — cited, not re-derived. Full trace: [341313ae, Check 7 INFO (swappiness)](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on-resolves-base-medium--vmswappiness0-is-measured-in-kv-cache-pages-are-kept-out-of-swap-by-a-measured-sysctl-disk).

### INFO (deployed: ON) — the k256 app key is exported into the compose env namespace; no consumer exists in this document (log)

`export ENCRYPTION_KEY=$(cat /dstack/.host-shared/.appkeys.json | jq -r .k256_key)` (`pre_launch_script:2`); `${ENCRYPTION_KEY}` referenced nowhere in the compose (grep-verified, zero hits); the script's two `echo` lines (`:27`, `:33`) print a path and an image reference, never the key. The OS page reports that this stdout goes to the journal **and** the host-readable serial console — so the `[pre_launch] Loaded compose-manager image override from …` line (`:33`) is host-visible metadata. Cited, not re-derived. Full trace: [341313ae, Check 7 INFO (k256)](sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md#info-deployed-on--the-k256-app-key-is-exported-into-the-compose-env-namespace-no-consumer-exists-in-this-document-log).

## Check 8 — The report outside the compose: `mr_aggregated` and the signed action log **[priority item]**

### INFO (deployed: ON) — the sibling's different mr_aggregated is accounted for by exactly two RTMR3 events, compose-hash and instance-id; no other measurement register differs (structural)

Read from the two saved reports, not from the dstack source. `info.tcb_info` carries `mrtd`, `rtmr0`, `rtmr1`, `rtmr2`, `rtmr3` and an `event_log`: **`mrtd`, `rtmr0`, `rtmr1` and `rtmr2` are byte-identical** between this CVM and the sibling (same `vm_config`, same `os_image_hash`, same OVMF/kernel/initramfs path), and **only `rtmr3` differs** (`e24681ea…` here vs `37cfb6c5…`). The event logs have the same 30 entries in the same order and differ in exactly two, both `imr: 3`: event 22 `compose-hash` (`533e43fd…` here vs `c82b1a2e…`) and event 23 `instance-id` (`fccd496a…` vs `7bb9af0a…`). Every other RTMR3 event is equal, including `app-id` (`2c0a0c96…`), `mr-kms`, `os-image-hash` (`a6eafc5f…`), `key-provider` and `storage-fs` (`zfs`). So the difference in `mr_aggregated` (`18d12286…` vs `34255d6a…`) is fully explained at the register level by this document's one-line change plus the per-instance id, with no other measured input differing. What `mr_aggregated` is as a formula — which registers it hashes, in what order — is defined by dstack, and **no page in this repo defines it** (grep over every published page: no hit); its expected value for this CVM was therefore not recomputed here (Not traced). The `device_id` in both reports is the SHA-256 of the empty string (`e3b0c442…`), i.e. no device id — metadata only, noted for the record.

### INFO (deployed: ON, log-pinned) — the plaintext stack is pinned by the signed action log, not by this measurement; on 2026-09-09 that log records a full compose_up of a revision defining a DeepSeek-V4-Flash engine service between 02:24Z and 04:55Z (structural)

Said as the 24-entry `compose_manager_attestation.actions` list in the saved report shows, and no further. `compose_manager_started` 02:14:50Z on `6e035c8f…`. Project-wide `compose_up` (no `services` key) of `prod/GLM-5.3-Flash-SGL-TP4.yaml` @ `a7549a89…` (`v0.0.403`) at 02:24:56Z, again at 04:31:03Z (after a service-scoped up of the two `model-sg-glm53-fp8-tp4-r{1,2}` engines at 04:30:50Z), then project-wide `compose_up` of the published-and-LEAKS revision `d76413cd…` (`v0.0.380`) at 04:35:26Z; a project-wide `compose_down` of `v0.0.403` at 04:55:14Z, followed by four service-scoped `compose_down` entries naming **`model-sg-dsv4-flash-fp4-tp4`** at 04:56:04Z, 04:56:24Z (tag `v0.0.402` @ `02254afa…`, a revision no `compose_up` in this log names), 04:56:43Z and 04:59:30Z. The `a7549a89` revision's file does define a service of that name (a one-line grep of the orchestrator's copy, `model-sg-dsv4-flash-fp4-tp4:`; that recipe is otherwise unread and has no page), so the project-wide up at 02:24:56Z brought it up on this CVM: **for about two and a half hours a second model's engine ran under this harness**, the co-tenancy configuration `d98a2568`'s C5 named — what it was pointed at, what it logged, and whether it served traffic are that recipe's questions (Not traced). From 05:05:32Z the base is the `-Canary.yaml` revision `5a5b7228…` (`v0.0.394`, project-wide, then `otelcol-contrib` at 05:34:26Z); 20:17:40Z down / 20:18:02Z up of `model-proxy-registrar` + `dcgm-glm53` from `a2d3f073…` (`v0.0.404`); 23:15-23:53Z a stage/down/up cycle of `r1`, `r2` and `glm53-perception-check` from `e5d80ab6…` (`v0.0.408`) and of `proxy-glm53` from `2878b9c4…` (`v0.0.409`), ending 23:53:31Z. The sibling's saved log (43 entries) ends 23:44-23:50Z with the same two revisions on the same services. The layered result is therefore a composite of `5a5b7228`, `a2d3f073`, `e5d80ab6` and `2878b9c4` — none of it measured, and only `d76413cd` (no longer the live base) has a published page. The published verdict on `d76413cd` (LEAKS, narrowly) is the recipe page's, not carried into this harness verdict.

## Host lineage — gpu02 from `d98a2568` (0.5.5, gateway on, override replay already measured) to this harness

This hash replaces [`d98a2568…`](sha256-d98a2568546afce9ab53f7eb4d4b2058e5497e1efd05a8ad0934793043240ed7.md) on the physical label `gpu02` (`INSTANCE_LABEL=gpu02` at `:135` in both, per that page's table). Model history on the label, from published pages and the orchestrator's captures (read here, not fetched): `d98a2568`'s 2026-08-03 review recorded gpu02 serving `deepseek-ai/DeepSeek-V4-Flash` + `zai-org/GLM-5.1-FP8` on one CVM, the measured README later Qwen3.6/3.8 + dsv4; today it is the second `glm-5-3-flash` CVM, instance `fccd496a…`, created 2026-09-09T02:14:50Z, replacing gpu30. No saved capture of gpu02 under `d98a2568` is in the orchestrator's report set available to me, and that page names no `os_image_hash`, so which guest gpu02 ran before is not established here (every 0.5.5 report in the saved set, all other labels, attests `9b69bb16…`) (Not traced). `d98a2568` was already the no-Datadog five-service template, already mounted the guest root, already tuned `vm.swappiness=0`, and was the **only** document of its generation whose measured boot script replayed the override — so gpu02's regeneration removes no sink, resolves no MEDIUM, and widens nothing it had not already disclosed. What changes for this host, each checked against this document:

**Substrate and dstack flags (outer document):**
- **`dstack-nvidia-0.5.5` → `0.5.11`**, and with it the guest OS to the **upstream** Dstack-TEE build [`a6eafc5f`](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) (QUALIFIED PASS). Its page surfaces an **armed core-dump path** (a crashing engine writes its memory as a core file into its container layer on the in-enclave LUKS2 disk; MEDIUM, ARMED there) and **no LSM of any kind** (INFO there). Both are guest-OS findings and residuals for this page: this document neither creates nor mitigates them. Whether any live recipe revision sets `ulimits: core` for the engine is the recipe pages' question (Not traced).
- **`gateway_enabled: true` → `false`.** Per the OS page: tunnel, registration and UDP filter removed together — fewer host-facing services on gpu02, no control lost. All other outer flags equal to those `d98a2568`'s page lists (`kms_enabled: true`, `local_key_provider_enabled: false`, `no_instance_id: false`, `secure_time: false`, `public_logs: false`, `public_sysinfo: true`).

**Measured boot path (`pre_launch_script`, 31 → 34 lines):** the same three parts — `ENCRYPTION_KEY` export (`:2`), the three sysctls (`:5-7`), and the override replay with the `OVERRIDES` path (`:15`), dead re-pin branch (`:24-28`) and data-parse loader (`:29-34`) — match that page's verbatim quotes line for line; the three added lines were not byte-diffed (no `d98a2568` capture here) and, by the quoted content, can only be comment lines. The HIGH in Check 5 is **carried**, not new. Its MEDIUM "path assumed, not discovered" (`:13-14` here) is now answered by the OS page in the direction the script assumed.

**Compose (372 → 374 lines, by that page's citations):** `d98a2568` cites its telemetry level at `:371-372` and its `logs/cvm` processor list at `:358` with the endpoint at `:341`; here they are `:373-374`, `:360` and `:341` — two lines added between the endpoint and the pipelines, consistent with the `num_consumers: 1` line and its FIFO comment (`:344`, `:346`) that `341313ae` found new for its generation; INFO. Everything else — collector image, mounts (`:18-23`), certbot, compose-manager privileges (`:117-125`), launcher, control-plane digests `6e035c8f…` (`:95`) / `91fdff3c…` (`:148`), `dstack_default`, `8080:8080`, the 22-entry `allowed_envs`, `MIN_TAG_AGE_HOURS` default 0 (`:130`, `:169`), `SLACK_WEBHOOK_URL` (`:136`), `INSTANCE_LABEL=gpu02` (`:135`) — at the line numbers that page cites and this document confirms.

## Verdict

**QUALIFIED PASS.** Within hardware-measured config + `allowed_envs`, the invariant holds exactly as on the base — this document *is* the base with a different host label: the 22-entry env list contains no content, log-level, or telemetry switch; the one measured off-box log path is management-only, to one literal near.ai endpoint, through a filter that is fail-closed by construction and by OTTL semantics; the changed line is a Slack-notification identity string that is never handed to the collector and touches no telemetry attribute and no content path; and the two CVMs behind `glm-5-3-flash.completions.near.ai` again present clients with byte-identical measured harnesses, their attestation differing at the register level only in RTMR3's compose-hash and instance-id events. The qualifications carry over unchanged: (C2, **carried for gpu02** — its previous measurement already disclosed it) the measured boot path installs whatever compose-manager reference `.env.launcher` holds, without a boot-time signature check, from a volume the root-equivalent, ptrace-capable compose-manager can write; (C1) the plaintext stack is log-pinned, not measured, with the tag-age floor at 0 — this CVM ran seven tags and six recipe revisions on its first day, including two and a half hours with a second model's engine service defined in the running project; (C3) engine stdout still leaves the CVM through the recipe's collector with no redaction anywhere; (C4) the flat network this document defines keeps any engine admin surface reachable; and the substrate this harness now runs on carries, per its own page, an armed core-dump path and no LSM — residuals this document does not touch.

## Not traced

- **Live attestation and TDX quote verification** — the fresh-nonce fetch, the 30-sample split, the landing on instance `fccd496a…`, and the `sha256(app_compose) == compose_hash` check were the orchestrator's; this review re-hashed the captured bytes of both documents and re-read the saved reports' `tcb_info`, `event_log` and `actions` only. The RTMR3 replay of the event log against the quote was not performed.
- **`mr_aggregated`'s formula** — no page in this repo defines which registers it hashes; the page establishes only that MRTD/RTMR0-2 are equal and RTMR3 differs by exactly two events.
- **Fleet placement** — "1 CVM, the second behind `glm-5-3-flash`, serving `z-ai/glm-5.3-flash`", the gpu30 replacement, and the label's Qwen3.6/3.8 tenure are from the orchestrator's captures and the measured README; not re-derived live. gpu02's guest OS under `d98a2568` is not established.
- **The inner recipes** — `a7549a89`, `5a5b7228`, `a2d3f073`, `e5d80ab6`, `2878b9c4` (no page, or under concurrent review) were not read beyond one service-name grep of `a7549a89`; `d76413cd` (published) was not re-read. No claim about their engine flags, collectors, core `ulimits`, network membership, what the `model-sg-dsv4-flash-fp4-tp4` service was pointed at, or whether both CVMs' layered stacks are identical.
- **The 0.5.11 guest OS (`a6eafc5f…`)** — cited for what `gateway_enabled: false` disables, the LSM posture, the pre-launch script's privilege, stdout and data-root, the encrypted placement of `dstack_work`, swap placement and the core-dump path. None re-derived. `8080` reachability from outside the CVM is left Not traced by that page as well.
- **The currently-running compose-manager digest** — the log's `compose_manager_started` names `6e035c8f…` at boot; whether the launcher has swapped it since is runtime state invisible to this layer (Check 5).
- **Launcher behaviour on a non-verified running image** — a launcher source question for the `91fdff3c` page.
- **The contents of `/dstack/.host-shared/.decrypted-env`** — `allowed_envs` constrains names on the measured channel; values, and anything spliced via `/compose/up`, are not visible here.
- **`certbot/dns-cloudflare@742dbd2e…`** — digest-pinned, no source page; configuration-audited only.
- **Collector binary semantics** — the fail-closed and `num_consumers` analyses rely on documented OTTL / filterprocessor / exporterhelper contracts for the deployed collector line, not on the `85ac41c2…` binary.
