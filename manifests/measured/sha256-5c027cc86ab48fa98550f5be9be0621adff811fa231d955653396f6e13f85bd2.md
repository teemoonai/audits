# measured compose @ 5c027cc8... (GPU-node measured app_compose — gpu26, third node on the flagship `glm-5-2` domain: the regenerated harness, pointed at the staging telemetry gateway)

## verdict: QUALIFIED PASS at the manifest layer — the regenerated no-Datadog harness carries to a third flagship node with the management filter fail-closed twice over and the boot-path re-pin literal finally consistent with the fallback; but the management log stream now exports to the staging ingest host telemetry-stg.infra.near.ai instead of the fleet's production endpoint — splitting this production node's off-box telemetry across two near.ai environments — the compose-fetch tag-age floor remains an operator env defaulting to 0, the root-equivalent ptrace-capable compose-manager and the guest-root-mounting collector carry over unchanged, and the measured boot path still replays the persisted compose-manager image override

| | |
|---|---|
| **compose_hash** | `5c027cc86ab48fa98550f5be9be0621adff811fa231d955653396f6e13f85bd2` |
| **role** | GPU-node measured `app_compose` — management harness only, `INSTANCE_LABEL=gpu26` (`:135`). Per the orchestrator's fleet capture, this hash was fetched from the live attestation of host `glm-5-2` (`glm-5-2.completions.near.ai`, serving `z-ai/glm-5.2`) on 2026-08-21 — a **third** node now behind the flagship domain, joining gpu23 ([`546161bc…`](sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md)) and gpu04 ([`9385918d…`](sha256-9385918de0a73b861ae833d99fb5be6f7e1c8a50487a835df4f277497c206825.md)); fleet placement is from that capture, not re-derived here (Not traced) |
| **identity** | hardware-measured: `sha256(app_compose)` was checked against the TDX quote's `tcb_info.compose_hash` before capture; re-verified locally — `shasum -a 256` of the captured JSON document returns exactly `5c027cc86ab48fa98550f5be9be0621adff811fa231d955653396f6e13f85bd2`, the `compose_hash` this page is keyed on |
| **dstack** | `manifest_version: 2`, `name: dstack-nvidia-0.5.5`, `runner: docker-compose`, `kms_enabled: true`, `gateway_enabled: true`, `local_key_provider_enabled: false`, **`public_logs: false`**, `public_sysinfo: true`, `no_instance_id: false`, `secure_time: false` — all identical to the 2026-08-17 template pages; `key_provider_id: ""` also present in the outer document (not listed on the base pages; whether it is new there is not determinable — the base documents are not on disk) |
| **review** | reviewed 2026-08-21 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review; bases = [`d4c89033…`](sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) (the regenerated five-service template on the small-models cluster, 2026-08-17) and [`546161bc…`](sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md) (the same template on the gpu23 flagship pair, same date). This document was parsed as JSON and its `docker_compose_file` (**374 lines** — the template pages' compose was 373) and `pre_launch_script` (**34 lines**) extracted programmatically, not eyeballed through escapes. Line citations `:NN` are the embedded compose's own line numbers; `pre_launch_script:NN` the script's; `recipe :NN` cites the inner recipe named below; outer dstack fields are cited by name. **Citation re-location result:** every load-bearing citation from both base pages re-locates at the *identical* line number in this document through `:343` (verified individually, not assumed — the full list is in Check 1); from the exporter's queue onward the citations shift by +2 (`:358` → `:360` pipeline processors; `:371-372` → `:373-374` telemetry log level), the queue itself reshaping to `:344-348`; net file length +1. The plaintext-handling stack (two sglang GLM-5.2 replicas at TP4, `vllm-proxy-rs`, nginx, its own collector) is **not in this measurement**: it is the runtime-fetched inner recipe `prod/GLM-5.2-W4AFP8-SGL-2xTP4.yaml` @ file_sha256 `5b8fd687ad6fa58747fcb5731fc28b2d1e91dac121c7843df525f6a93252fa34` (per the orchestrator's action-log capture; hash re-verified locally against the captured file; a *previous* revision `f408de5c…` has a [published page](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-2xTP4/sha256-f408de5c89d7e7f8b4715b1f62409f67cadf52e232cfdce663f94482a134bba2.md), this revision's is under review in the same run). It is read here only for what it proves about this harness: its containers join the network this document defines (`external: true, name: dstack_default`, recipe `:426-427`), write stdout through the `json-file` driver with the label options this document's collector parses (recipe `:65-69`), and run their own collector whose export goes to the **production** gateway (recipe `:677`) — which is what makes this document's staging endpoint a split, not a migration.

---

# Deployment Manifests & Configuration — Privacy Audit (harness leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-08-21).
> Method: [/notes/method.md](/notes/method.md) §4 (manifest-only checks) · Bases: [d4c89033](sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) (template reference), [546161bc](sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md) (same-domain sibling)

**Why this node matters.** The flagship `glm-5-2` domain is now served by at least three nodes with three different measured harnesses — gpu23 (`546161bc`), gpu04 (`9385918d`), and this gpu26 — so which harness a given request's plaintext sits behind is a per-request lottery across one domain name. The engines this harness fronts are two replicas of [`sglang@5027e95b`](/images/docker.io/lmsysorg/sglang/sha256-5027e95bf6ec536856b1b52a91d1f35ff5c564ab83e8a94758a169ff09bb8df3.md) (recipe `:271`, `:316`), whose page's standing verdict is *private at the deployed flags — but not "cannot happen": one unauthenticated runtime logging switch and one unconditional hang-time prompt dump remain live*. How exposed those are is decided by this document: who shares the engines' network, and where stdout goes.

## Check 1 — What changed from the 2026-08-17 template, enumerated

Relative to the [`d4c89033`](sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) / [`546161bc`](sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md) pages' shared 373-line compose:

**Changed:**
1. **The management telemetry endpoint: `https://telemetry.infra.near.ai` → `https://telemetry-stg.infra.near.ai`** (`:341`, same line number, different literal). Every other reviewed harness in the fleet, and this node's own inner recipe (recipe `:677`), export to the production host. Finding, Check 3.
2. **`INSTANCE_LABEL=gpu26`** (`:135`; gpu13 / gpu23 on the bases).
3. **The exporter block is reshaped**: a new FIFO comment (`:344`, "Keep source requests FIFO before the central gateway."), `sending_queue` with `num_consumers: 1`, `queue_size: 128`, `storage: file_storage` at `:345-348` (the bases cited the disk-backed queue at `:344-351` / `:344-346`+`:349-351`); `extensions: file_storage` with `create_directory: true` at `:350-353`. Downstream citations shift +2: pipeline `logs/cvm` processors `:358` → **`:360`**, collector self-telemetry `logs: level: info` `:371-372` → **`:373-374`**. Net compose length 373 → 374. The bases' composes are not on disk, so the intra-exporter reshaping is characterized by this document's content, not a byte diff (Not traced).
4. **`pre_launch_script`: the d4c89033 regeneration, one line shorter (34 vs 35), with the re-pin literal refreshed.** Same structure at the same cited lines — kernel tuning (`vm.swappiness=0`, `pre_launch_script:5`; `dirty_ratio=5` / `dirty_background_ratio=1`, `:6-7`), the parse-as-data comment and loader (`:11-14`, `:29-33`), `OVERRIDES` path (`:15`), dead re-pin branch `if [ "false" = "true" ]` (`:24`) — but the re-pin digest at `:26-27` is now `6e035c8f…`, **matching this document's own fallback** (`:95`), where d4c89033's script cited the superseded `5165400d…`. That page's INFO (stale re-pin literal) is **retired** here. Which single line was dropped is not attributable to any cited line — every base citation re-locates identically; the sibling script is not on disk to byte-diff (Not traced). Versus 546161bc (which kept the old 2-line script): this node **gains** the kernel tuning and the boot-time override replay — so 546161bc's MEDIUM (no `vm.swappiness=0` on a flagship node) does **not** apply here, and its "no boot replay" C2 variant reverts to the d4c89033 form (Check 5).

**Carried unchanged — each re-verified against this document at the identical line number, not assumed:** otelcol image digest `85ac41c2…` (`:5`), `user: "0:0"` (`:8`), `mem_limit: 768m` (`:9`), mounts `/var/run/docker.sock:ro` (`:18`), `/:/hostfs:ro` (`:22`), `/var/lib/docker/containers:ro` (`:23`), the `local` log driver on the collector itself (`:33-37`); certbot/certbot-init with the Cloudflare token config and `certs` volume (`:40-91`, `:207-208`, `:211-213`), cert names `*.completions.near.ai` + `$HOSTNAME.hosts.near.ai` (`:80-83`); compose-manager's fallback-image default (`:95`), deploy API `8080:8080` (`:98-99`), the nsenter/AppArmor comment (`:100-116`), `pid: host` (`:117`), `cap_add: [SYS_ADMIN, SYS_PTRACE]` (`:118-120`), rw docker.sock + dstack.sock + decrypted env (`:122-124`), `work` volume (`:125`), `GITHUB_REPO=https://github.com/nearai/cvm-compose-files` (`:127`), `BEARER_TOKEN` (`:128`), `MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`), `SLACK_WEBHOOK_URL` (`:136`); the launcher at digest `91fdff3c…` (`:148`), `network_mode: host` (`:156`), rw docker.sock (`:158`), decrypted env ro (`:162`), launcher knobs (`:169-182`) including the cosign identity regexp (`:179`); the shared `dstack_default` network (`:195-200`); the collector config: filelog glob (`:223`), `start_at: end` (`:224`), the operator chain (`:227-253` — `json_parser` `on_error: send_quiet` `:235`, container-id regex `on_error: send_quiet` `:250`, the unconditional `add` of `service.name: cvm-docker` `:251-253`), `hostmetrics/cvm` (`:255-268`), `docker_stats/cvm` (`:270-277`), `prometheus/self` (`:279-285`), resource attributes (`:296`, `:299`, `:302`, `:305`), `transform/docker_metadata` (`:311-319`), `filter/management_logs` (`:321-325`), the `MONITORING_INGEST_TOKEN` bearer header (`:343`); all outer dstack flags including `public_logs: false`; the 22-entry `allowed_envs` list, verbatim identical.

### INFO (deployed: ON) — the third flagship node measures no Datadog agent; nothing at the measured layer ships engine stdout off-box (telemetry)

Priority question answered by grep of the whole document, not assumed from the template: **no `datadog/agent` service exists in this measurement.** The `datadog` strings present are: the `com.datadoghq.ad.logs` *labels* on the four management containers (`:54`, `:85`, `:138`, `:186`), the label-export options on their `json-file` config (`:60`, `:91`, `:144`, `:192`), the two transform statements that *consume* those labels (`:317-318`), the `DD_HOSTNAME` pass-through (`:14`, resource attribute at `:296`), and `DD_API_KEY`/`DD_HOSTNAME` in the outer `allowed_envs`. No agent, no OTLP ingest listener, no journald collection. `DD_API_KEY` is consumed by nothing in this document and nothing in the inner recipe (both grep-verified) — vestigial but drift-ready, as on the bases. So on this node the engines' stdout leaves the box **once**, via the inner recipe's own collector (`filter/app_logs` keeps records tagged `model`/`deployment` and drops the management set, recipe `:640-649`; export recipe `:677`, pipeline recipe `:693-696`) — that path is pinned by the recipe's `file_sha256` and assessed on the recipe page, not by this hash. From the measured layer: zero off-box destinations for non-management containers.

## Check 2 — `allowed_envs` (the operator's measurement-free knob set)

**Result: no content switch, no log-level switch, no telemetry-destination switch — identical 22-entry list, same regression as the bases.** Verbatim:

```
BEARER_TOKEN, CM_MIN_TAG_AGE_HOURS, SLACK_WEBHOOK_URL, AWS_REGION,
AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY, AWS_S3_BUCKET, CERTBOT_EMAIL,
CLOUDFLARE_API_TOKEN, DOCKER_REGISTRY_USER, DOCKER_REGISTRY_TOKEN,
LAUNCHER_CHANNEL, DD_API_KEY, HUGGING_FACE_HUB_TOKEN, PROXY_TOKEN, ENV,
HOST_IP, DD_HOSTNAME, HOSTNAME, MONITORING_INGEST_TOKEN, CVM_NAME, CVM_HOST
```

The per-name analysis from the base pages was re-checked against this document's consumers and carries: `CM_MIN_TAG_AGE_HOURS` feeds `:130` and the launcher copy `:169` (finding below); `BEARER_TOKEN` + `LAUNCHER_CHANNEL` are the control-plane levers (`:128`, `:170`, bounded by the cosign regexp `:179`); `SLACK_WEBHOOK_URL` is an operator-set arbitrary outbound URL (`:136`) receiving deploy metadata tagged `INSTANCE_LABEL=gpu26` (`:132-135`) — carried MEDIUM-class egress, metadata only; `MONITORING_INGEST_TOKEN` selects the telemetry *account*, never the destination (`:343` — though what that account now points at is the staging host, Check 3); `HUGGING_FACE_HUB_TOKEN`/`PROXY_TOKEN` flow through the decrypted env (`:131`, `:173`) to the inner stack as credentials, not switches (the recipe's engine logging posture is literal there: `--log-requests-level 0`, recipe `:127`); `DD_HOSTNAME`/`CVM_NAME`/`CVM_HOST`/`ENV` are resource attributes (`:296`, `:299`, `:302`, `:305`); `HOSTNAME` lands in certbot-init's cert request (`:83`); `AWS_*`, `CERTBOT_EMAIL`, `CLOUDFLARE_API_TOKEN`, `DOCKER_REGISTRY_*` are service credentials; `HOST_IP` has no consumer in this document. **No env can flip a content-logging switch in this harness or splice one into the model layer** — the collector's own log level is a literal (`:373-374`), the filter expression and exporter endpoint are inside the measured config content, and `COMPOSE_MANAGER_IMAGE` is not in the list (its only measured setter is the `pre_launch_script` replay, Check 5).

### MEDIUM (deployed: ON, regression carried) — the compose-fetch tag-age floor is an operator env defaulting to zero (supply chain)

`MIN_TAG_AGE_HOURS=${CM_MIN_TAG_AGE_HOURS:-0}` (`:130`; launcher copy `:169`). Unset, the floor between a `cvm-compose-files` tag appearing and being deployable is **zero**, and the operator can keep it zero with no measurement change. Same rating and mechanics as both bases; on this node it governs what can be spliced under a third of the flagship domain's capacity.

## Check 3 — The otelcol-management log pipeline: fail-closed direction, and where it now points

**Filter result: fail-CLOSED, by the same two independent mechanisms as the bases — re-verified at this document's lines, not carried on trust.**

Every container using the `json-file` driver — per the recipe's shared anchor (recipe `:65-69`) that is both engines, the proxy, and nginx — writes `/var/lib/docker/containers/<id>/<id>-json.log`; the collector tails exactly that glob (`:223`, `start_at: end`, `:224`). **The receiver ingests both flagship engines' stdout.** Between it and the exporter stands the `logs/cvm` chain (`:360`): `memory_limiter, resource, transform/docker_metadata, filter/management_logs, groupbyattrs/docker_service, batch`.

1. Receiver operators (`:227-253`): `json_parser` with `on_error: send_quiet` (`:235`); moves; container-id regex with `on_error: send_quiet` (`:250`); then, last and **unconditional**:

   ```
   - type: add
     field: attributes["service.name"]
     value: cvm-docker
   ```

   (`:251-253`). No `if` clause — every record leaves the receiver with `service.name = "cvm-docker"`; the nil case cannot occur in this document.

2. `transform/docker_metadata` (`:311-319`, `error_mode: ignore` `:312`) overwrites `service.name` from exported log labels, each statement nil-guarded (`:316-318`); management containers become `compose-manager` / `compose-manager-launcher` / `dns-cloudflare`; recipe containers become their own names (`sglang`, `vllm-proxy`, `nginx` — recipe labels `:249`, `:293`, `:336`); a failed statement leaves `cvm-docker`.

3. `filter/management_logs` (`:321-325`): the single condition at `:325` drops every record whose `service.name` is not one of the three management names — the filterprocessor drops when a condition is met.

4. The hypothetical nil case: OTTL map indexing returns nil (not an error) for a missing key, so the one genuinely fail-open door — `error_mode: ignore` skipping an *erroring* condition — is not reachable via a missing attribute; and nil compared to a string is "not equal", so the record drops anyway. Semantics per the collector-contrib OTTL/filterprocessor documentation for the deployed line; the image is the digest-pinned, [separately audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md) `85ac41c2…` (`:5`).

Label resolves → non-management name → dropped; label missing → `cvm-docker` → dropped; parse error → `cvm-docker` → dropped; attribute hypothetically absent → nil → dropped. **The fail-closed direction is preserved, line-identical to the bases.**

### MEDIUM (deployed: ON, new) — the management log stream exports to the staging telemetry host, splitting the node's off-box destinations across two environments (telemetry)

`endpoint: https://telemetry-stg.infra.near.ai` (`:341`). Both base pages' verdicts leaned on "one literal near.ai endpoint": the fleet's management collectors, and this very node's inner-recipe collector (recipe `:677`), export to `https://telemetry.infra.near.ai`. This measurement alone points its management stream at a **staging** ingest host. What flows there at deployed config is bounded — management-container log bodies verbatim (no message plaintext handled by any of the three; compose-manager does hold every Check-2 secret in env and its logs narrate deploys) plus CVM host/container metrics with node-identity attributes — and the destination is still a near.ai-operated domain, not a third party, which is what bounds this at MEDIUM. But it is a real weakening of the invariant as stated on the siblings: a staging environment ordinarily carries weaker access control, broader engineering reach, and different retention than the production ingest, and nothing in the attested document explains the deviation (plausibly a harness generated from staging inventory for a node then promoted to flagship serving — which would itself be a config-management miss on the domain's newest node). It also re-prices the carried one-condition-wide finding below: the place engine stdout would land if the filter were ever bypassed is now the lower-assurance environment. Whether `telemetry-stg.infra.near.ai` differs from production in access control or retention is outside this document (Not traced — residual, not mitigation).

### MEDIUM (deployed: OFF, one condition wide) — the flagship engines' stdout is ingested by the measured collector and excluded by filter expression, not by scoping (telemetry)

Carried from both bases, re-verified here: the isolation between "an engine printed plaintext" and "it left the box via this collector" is one measured OTTL condition (`:325`) plus one measured `add` default (`:251-253`). Both are inside the hash — changing either changes `compose_hash` — but the receiver is deliberately CVM-wide, so this is "OFF at the measured config", not "cannot happen". Residuals unchanged: (a) admission is by **spoofable label** — any container whose labels claim `service: compose-manager` gets its stdout exported, and what containers exist is decided by the log-pinned recipe and the root-equivalent compose-manager, folding into the C1/C2 trust chain; (b) no redaction stage for admitted streams (`:360` is route-and-batch only) — whatever the three management services print ships verbatim, now to the staging host; (c) engine stdout persists **on-CVM** regardless: the recipe's `json-file` driver retains up to ~1 GB per container (recipe `:67-68`) — in-enclave persistence, not egress.

## Check 4 — Privileges and mounts

### HIGH (deployed: ON, architectural) — compose-manager is root-equivalent and can read both engine replicas' process memory (structural)

`pid: host` (`:117`) + `cap_add: [SYS_ADMIN, SYS_PTRACE]` (`:118-120`) + rw `/var/run/docker.sock` (`:122`) + `/var/run/dstack.sock` (`:123`) + the decrypted operator env at `/dstack/.host-shared/.decrypted-env:/app/.env:ro` (`:124`). The manifest's own comment (`:100-116`) documents the intent — `nsenter -t 1 -m -u -i -n -p` into PID 1's namespaces, citing `/admin/kernel/algif-blacklist` (PR #14 / CVE-2026-31431) — and that dstack-OS ships no AppArmor to confine it. With host PID + `SYS_PTRACE` it can attach to any process in the CVM — here, two sglang replicas serving a third of the flagship domain's traffic: the plaintext of every message in flight on the node. Capability, not behavior: the [source review of the fallback build](/images/docker.io/nearaidev/compose-manager/sha256-6e035c8fb99c1d31f41760d25af178683a658cca4afd73cd0d366fbb5bf884be.md) found no code path using it to read process memory. Carried unchanged.

### HIGH (deployed: ON, architectural) — the launcher holds the docker socket on the host network (structural)

`network_mode: host` (`:156`) + rw `/var/run/docker.sock` (`:158`) + the decrypted env ro (`:162`): anything driving that socket can start a container with any privilege. The binary holding it is the attested, [reviewed](/images/docker.io/nearaidev/compose-manager-launcher/sha256-91fdff3cfa3543d72656b2368c7d8a0a83d95a0f1087378c897aa1537acdba56.md) `91fdff3c…` (`:148`). Carried unchanged.

### MEDIUM (deployed: ON, capability unused at this config) — the collector mounts the entire guest root filesystem and the docker socket (disk)

`/:/hostfs:ro` (`:22`, for `hostmetrics` `root_path` parity, `:16`, `:19-21`, `:257`) + `/var/run/docker.sock:ro` (`:18`, for `docker_stats`, `:270-277`) + `/var/lib/docker/containers:ro` (`:23`), as `user: "0:0"` (`:8`). The collector process can read every file in the guest — the decrypted operator env under `/hostfs/dstack/.host-shared/`, the `work` volume (launcher override state), the `certs` volume (the `*.completions.near.ai` private key), and any file an engine ever writes (exactly where a runtime-enabled dump-folder would land). `:ro` does not neutralize a unix socket — read-only mounts block writes to files, not `connect()` — so the socket mount grants the full Docker API, root-equivalent. At the measured config nothing reads arbitrary files (the `filesystem` scraper collects mount metrics and excludes `/var/lib/docker` paths, `:265-268`) and nothing drives the socket beyond stats; the image is digest-pinned and [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md). Capability unused, not absent. Carried unchanged.

### INFO (deployed: ON) — certbot holds the wildcard TLS key and the Cloudflare token in-enclave (disk)

`certs` volume (`:45`, `:66`, `:207-208`), Cloudflare token via config (`:211-213`), cert names `*.completions.near.ai` + `$HOSTNAME.hosts.near.ai` (`:80-83`). TLS terminates inside the CVM; key material never leaves by configuration. No `privileged: true` anywhere in this document (grep-verified); no plaintext-handling service exists at this layer. Carried unchanged.

## Check 5 — Image pinning, identity, and the measured-in drift persistence

### HIGH (deployed: ON) — the measured boot path replays a persisted compose-manager image override from mutable disk state (supply chain)

Carried from d4c89033 in the same strongest form (546161bc lacked the mechanism; this node has it). The image reference is a default, not a pin: `image: ${COMPOSE_MANAGER_IMAGE:-nearaidev/compose-manager@sha256:6e035c8f…}` (`:95`), `COMPOSE_MANAGER_IMAGE` is not in `allowed_envs` — but the measured `pre_launch_script` sets it every boot from `/var/lib/docker/volumes/dstack_work/_data/.env.launcher` (`pre_launch_script:15`, `:29-33`), the file the launcher persists after each swap (`LAUNCHER_ENV_FILE=/app/work/.env.launcher`, `:172`, on the shared `work` volume, `:125`, `:163`). The measured digest is only the first-boot value; afterwards the boot path re-installs whatever digest the launcher last persisted, indefinitely. The script parses the file as data, not source (`pre_launch_script:11-14`, `:30-32` — matching only the `COMPOSE_MANAGER_IMAGE` key), and the forced re-pin branch is dead: `if [ "false" = "true" ]` (`pre_launch_script:24`). Compensating controls unchanged: swap targets must be cosign-signed by `https://github.com/nearai/compose-manager/.github/workflows/.*` (`:179`) on the operator-chosen `LAUNCHER_CHANNEL` (`:170`), and swaps are disclosed in the signed action log. Which digest runs right now is runtime state this layer cannot see.

### INFO (deployed: ON) — both control-plane images resolve to audited builds, and the dead re-pin literal is finally consistent with the fallback (supply chain)

The fallback `compose-manager@sha256:6e035c8f…` (`:95`) has a [published review](/images/docker.io/nearaidev/compose-manager/sha256-6e035c8fb99c1d31f41760d25af178683a658cca4afd73cd0d366fbb5bf884be.md) (*COMPROMISABLE by credentialed operator (control-plane; no data-plane role)*); the launcher `sha256:91fdff3c…` (`:148`) has a [published review](/images/docker.io/nearaidev/compose-manager-launcher/sha256-91fdff3cfa3543d72656b2368c7d8a0a83d95a0f1087378c897aa1537acdba56.md) (*COMPROMISABLE by credentialed operator — this is the post-attestation mutation engine; no plaintext reach*). New on this document: the dead re-pin branch's literal (`pre_launch_script:26-27`) now names the **same** `6e035c8f…` as the compose's fallback — d4c89033's INFO finding (the branch cited the superseded `5165400d…`, showing the two pins maintained by hand) is **retired** here; the two literals were brought back into sync. Third-party sidecars remain configuration-audited only: `otel/opentelemetry-collector-contrib@85ac41c2…` (`:5`, audited page exists), `certbot/dns-cloudflare@742dbd2e…` (`:41`, `:64` — digest-pinned, no source page, Not traced). No tag-only image in this document.

## Check 6 — dstack settings and the `pre_launch_script`

Outer flags identical to both bases: `kms_enabled: true`, `gateway_enabled: true`, `local_key_provider_enabled: false`, `no_instance_id: false`, `secure_time: false`, **`public_logs: false`**, `public_sysinfo: true` (plus `key_provider_id: ""`, see the dstack row above). The script is the d4c89033 regeneration (Check 1 item 4): kernel tuning at `pre_launch_script:5-7` — so `vm.swappiness=0` **is** present on this flagship node, retiring the applicability of 546161bc's missing-swappiness MEDIUM for gpu26 — the data-not-source override loader (`:29-33`), and the dead re-pin branch (`:24-28`). Beyond `/proc/sys/vm` writes and the `dstack_work` volume read, the script touches no logs, no mounts, and no docker configuration.

### INFO (deployed: ON) — the k256 app key is exported into the compose env namespace; no consumer exists (log)

`export ENCRYPTION_KEY=$(cat /dstack/.host-shared/.appkeys.json | jq -r .k256_key)` (`pre_launch_script:2`). No service in this document references `${ENCRYPTION_KEY}` (grep-verified). In-CVM, no configured egress. Carried unchanged.

## Check 7 — Network topology and egress

### MEDIUM (deployed: ON) — one flat shared network spans management and the flagship's plaintext services; the engines' unauthenticated logging switch is reachable on it (egress)

This document creates `dstack_default` (`:195-200`) precisely so the inner compose can join it — the in-manifest comment says so (`:196-198`), and the recipe does (`external: true`, recipe `:426-427`). No `internal: true`, no egress restriction at any layer (grep-verified). Consequence: [`sglang@5027e95b`](/images/docker.io/lmsysorg/sglang/sha256-5027e95bf6ec536856b1b52a91d1f35ff5c564ab83e8a94758a169ff09bb8df3.md)'s runtime logging switch is unauthenticated (standing finding on that page), both replicas listen on container port 8000 (recipe `:141`; the proxy targets `http://model-sg-glm52-w4a-tp4-r1:8000` and `-r2:8000`, recipe `:243`), and any container on `dstack_default` reaches them directly, bypassing nginx and the E2EE-terminating proxy. This measured file is what defines that network and what places a ptrace-capable root-equivalent container and a guest-root-mounting collector on it. Segmentation is available at this layer and is not taken. Carried unchanged.

Published ports / host-network usage in the measured layer: compose-manager's `8080:8080` deploy API (`:98-99`, `BEARER_TOKEN`-authenticated, `:128`; external reachability is dstack guest port handling, Not traced); the launcher on the host network (`:156`) for its `http://127.0.0.1:8080/version` probe (`:174`). Engine-facing ports (e.g. nginx `8000:80`, recipe `:190`) come from the recipe, not this document.

Every outbound destination configured in this document:

| destination | service | literal? | carries content? |
|---|---|---|---|
| **`https://telemetry-stg.infra.near.ai`** (`:341`) | otelcol-management | literal — **staging host, deviates from fleet** | management-container log bodies verbatim + CVM metrics; engines dropped fail-closed (Check 3) |
| `https://github.com/nearai/cvm-compose-files` (`:127`) | compose-manager | literal | no — fetches the inner recipe |
| Docker registries (`COMPOSE_MANAGER_IMAGE_REPO`, `:165`; `LAUNCHER_CHANNEL` polling, `:170`; creds `:180-181`) | launcher | literal repo, mutable channel | no |
| Cloudflare DNS API + Let's Encrypt (`:43`, `:72-83`, `:211-213`) | certbot / certbot-init | literal service | no |
| `${SLACK_WEBHOOK_URL}` (`:136`) | compose-manager | **operator-set, arbitrary URL** | deployment metadata only |

No `us3.datadoghq.com`, no third-party log destination anywhere in this measurement.

## Verdict

**QUALIFIED PASS.** Within hardware-measured config + `allowed_envs`, the invariant holds: the 22-entry env list contains no content, log-level, or telemetry switch; the one measured off-box log path is management-only, through a filter that is fail-closed by construction (unconditional `cvm-docker` default, `:251-253`) and by OTTL semantics (nil never equals a management name); no Datadog agent is measured, so nothing at this layer ships engine stdout anywhere; the boot-path re-pin literal is finally consistent with the fallback digest; and the flagship node gains the KV-swap hardening its gpu23 sibling lacked. The qualifications are the fleet's architecture plus one new deviation of this document's own: the management stream's destination changed to the **staging** ingest host `telemetry-stg.infra.near.ai` (`:341`) — still first-party, but a second, presumptively lower-assurance environment now receives a production flagship node's management logs and metrics, unexplained by anything in the measurement, and it is where engine stdout would land were the one-condition-wide filter ever bypassed; the plaintext stack itself is log-pinned, not measured (C1), with the tag-age floor still an operator env defaulting to 0; the enforcement point remains a root-equivalent, ptrace-capable, self-updating compose-manager whose measured digest is a first-boot default with the drift-persistence replay measured into the boot path (C2); engine stdout still leaves the CVM through the inner recipe's collector with no redaction anywhere (C3) — to the production gateway, so this node's telemetry is split across two environments; and this document still defines the flat network on which the engines' unauthenticated logging switch sits (C4).

## Not traced

- **Fleet placement** — "this hash is measured on host `glm-5-2` (gpu26), a third node behind the flagship domain" comes from the orchestrator's 2026-08-21 capture; this review verified the document's hash and content, not the live quote.
- **What `telemetry-stg.infra.near.ai` is** — access control, retention, and whether it shares a backend with the production ingest are outside this document. The finding rests on the destination *string* deviating from the fleet and from this node's own recipe; the environment's actual assurance level is a residual, not assessed.
- **The inner recipe `prod/GLM-5.2-W4AFP8-SGL-2xTP4.yaml` @ `5b8fd687…`** — hash-verified locally and read only for network attachment, logging driver/labels, engine port/flags, and its collector's filter and export path (recipe `:65-69`, `:127`, `:141`, `:243`, `:271`, `:316`, `:426-427`, `:640-649`, `:677`, `:693-696`). Its page is under review in the same run; under this repo's fail-closed rule no further claim is made about it here.
- **The intra-exporter reshaping vs. the 2026-08-17 template** — the base composes are not on disk; the +1-line net difference and the exporter-block changes are characterized from this document's content and the base pages' citations, not a byte diff. Likewise the one line dropped from the 35-line sibling `pre_launch_script`.
- **The currently-running compose-manager digest** — runtime state (persisted override + action log), invisible to this layer by design (Check 5).
- **TDX quote/signature verification** of the attestation this document was extracted from — the quote-verification leg, upstream of this review.
- **The contents of `/dstack/.host-shared/.decrypted-env`** — `allowed_envs` constrains names on the measured channel; values, and anything spliced via compose-manager's `/compose/up` env overlay, are not visible here.
- **Whether compose-manager's published `8080` is reachable from outside the CVM** — dstack guest port handling, carried from the bases.
- **`certbot/dns-cloudflare@742dbd2e…`** — digest-pinned but no source page exists; configuration-audited only.
- **Collector binary behavior** beyond its audited page — the fail-closed analysis relies on documented OTTL/filterprocessor semantics for the deployed collector line, not re-derived from the `85ac41c2…` binary.
- **`key_provider_id: ""`** — present in the outer document; whether the 2026-08-17 documents carried it is not determinable from their pages.
