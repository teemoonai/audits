# cvm-compose-files prod/dsv4-qwen38-glm51.yaml @ 0c1c6841... (GPU02 capacity pack, now five model containers — DeepSeek-V4-Flash TP2 x2 + Qwen3.8-27B x2 + Qwen3.6-27B + Qwen3.6-35B-A3B; GLM-5.1 gone despite the filename; delta over c27ed3b3)

## verdict: QUALIFIED PASS on logging config — delta review against the audited c27ed3b3 revision, the whole 35-hunk diff read against the base bytes: GLM-5.1 (patched in-enclave build, compile volume, EAGLE flags) is removed, the two Qwen3.6 sglang engines and their proxies, dcgm exporters, scrape jobs, nginx listeners and registrar routes are re-added with command lines byte-identical to the ones the c6ef1271 page reviewed yesterday, a second DeepSeek-V4-Flash TP2 replica on GPUs 6-7 is added off the unchanged anchor and pooled by the DeepSeek proxy, and the Datadog OpenMetrics check labels are deleted while the Datadog logs label the collector keys on stays on all 19 services; no content-logging switch anywhere, --log-requests-level 0 on all six engines, OHTTP forced on all four proxies, watchdog labels truthful; but stdout of every plaintext service still ships off-box unredacted, FOUR sglang engines (both Qwen3.8 and both Qwen3.6) are now unguarded multimodal media fetchers on the 300 s watchdog default, both Qwen3.6 engines run --trust-remote-code with no --revision and this file's downloader also pre-fetches them unpinned, the unauthenticated engine admin surface sits behind eight allowlist-free nginx location / blocks and four proxies on the flat network, and the /compose/up env-splice can still arm hidden-state passthrough and web-search egress with no attestation-visible change

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/dsv4-qwen38-glm51.yaml` |
| **file_sha256** | `0c1c6841eeaa837f2083660327b34c70df8c69501189f14508aa4fe052692e40` (re-verified locally: `shasum -a 256` of the captured 1803-line file matches; the captured 1530-line base file re-hashes to `c27ed3b3…` as well, and `diff -u` of the two captured files regenerates the orchestrator's 1051-line diff byte-for-byte apart from the header paths) |
| **role** | log-pinned inner compose for the GPU02 capacity pack, now five model containers on one 8-GPU node: `deepseek-ai/DeepSeek-V4-Flash-0731` (SGLang FP4 TP2, **two replicas**, GPUs 0-1 and 6-7, pooled by one proxy), `Qwen/Qwen3.8-27B-FP8` (SGLang TP1, two replicas, GPUs 2 and 3), `Qwen/Qwen3.6-27B-FP8` (SGLang v0.5.16 TP1, GPU 4), `Qwen/Qwen3.6-35B-A3B-FP8` (SGLang v0.5.16 TP1, GPU 5) — behind **four** `vllm-proxy-rs` instances, nginx (eight listeners), registrar, five dcgm exporters, otelcol-contrib. The filename still says `glm51`; GLM-5.1 is no longer in the file |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. The log pins `compose_up` of this path at `nearai/cvm-compose-files` commit `fd0bdb2cc4461b6f707d37832a0bc885c0a81323` (tag `v0.0.384`, 2026-09-02T18:50Z, services `model-sg-dsv4-flash-fp4-tp2-r2` + `dcgm-dsv4-flash-r2`), two hours after `v0.0.383` (`2b771d2b…`, commit `1009d183`, 16:31-16:41Z, which re-added the two Qwen3.6 services). The orchestrator fetched the file at `fd0bdb2c` and hash-checked it (match). The measured harness on these hosts is `compose_hash` `d98a2568…` ([its page](/manifests/measured/sha256-d98a2568546afce9ab53f7eb4d4b2058e5497e1efd05a8ad0934793043240ed7.md)). **This file is under active revision today** (two compose_up entries within two hours); this page is keyed to this exact hash and says nothing about any later one. The action-log match was not re-fetched live in this review |
| **lineage** | supersedes [`c27ed3b3`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-c27ed3b357f44673e4a84ef89f4625e1009de619d83ffdaae27c1265f676f4bb.md) (reviewed 2026-08-21, QUALIFIED PASS; itself a delta over the full [`46edfb72`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-46edfb72e0d15618429cedde9fb527bc4d4c4c4073eac08099e854b43c44bc96.md) review). Sibling: the two Qwen3.6 services were last reviewed on [`dsv4-qwen36-gemma4 @ c6ef1271`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen36-gemma4/sha256-c6ef1271ffdaed7e936b271d371da944644d63f0c277fe84e29582f334119fec.md) (2026-09-01), which the same four hostnames attested until today |
| **live** | 5 hosts per the orchestrator's 2026-09-02 capture: `dsv4-flash`, `qwen3-30b`, `qwen3-6-27b`, `qwen3-6-35b`, `qwen3-8-27b` (one multi-model node, `INSTANCE_LABEL=gpu02` per its measured page; placement is the orchestrator's capture, not re-derived here) |
| **review** | reviewed 2026-09-02 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review, base = the [`c27ed3b3` page](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-c27ed3b357f44673e4a84ef89f4625e1009de619d83ffdaae27c1265f676f4bb.md). The diff was taken **against the actual base bytes** (both captured files re-hashed here): 1530 -> 1803 lines, +474/-201 across 35 hunks, all 35 read in four chunks and enumerated below. Three upstream commits touch the file between the pins: `c9605490` (2026-08-25, "remove Datadog Agent OpenMetrics checks"), `1009d183` (2026-09-02, "replace gpu02 GLM-5.1 with Qwen3.6 models", PR #219), `fd0bdb2c` (2026-09-02, "add gpu02 DeepSeek-V4-Flash TP2 replica", PR #220). Every carried finding is re-cited at this file's line numbers from the captured bytes, not shifted arithmetically. Per-image behavior is deferred to the published image pages: DeepSeek engine [`nearaidev/sglang@ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) (`:60`), Qwen3.8 engine [`lmsysorg/sglang@febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md) (`:115`, a nightly), Qwen3.6 engines [`lmsysorg/sglang@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (v0.5.16, `:171`, `:207`), proxies [`vllm-proxy-rs@b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md) (`:44`) and [`@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (`:372`, `:413`, `:453`), [dcgm-exporter `ed594cf5`](/images/nvcr.io/nvidia/k8s/dcgm-exporter/sha256-ed594cf53fe6942e84b07b0740cdcbb249fa4b39cb21feeebf93881ae51f0b5e.md) (`:241`), [otelcol-contrib `85ac41c2`](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md) (`:895`). The GLM engine image `aac6b242` and its in-enclave patched build are no longer referenced anywhere in this file.

---

## The complete diff, grouped (base `c27ed3b3` -> this `0c1c6841`, 35 hunks, +474/-201)

**A. GLM-5.1 removal (`1009d183`, hunks 1-3, 15, 20, 21, 23, 26, 28, 30-35 in part).** Header prose rewritten (`:1`, `:5-9`; the GLM paragraph deleted). Deleted wholesale: the `x-glm51-awq-build` `dockerfile_inline` (the sglang#28305 loader patch on `lmsysorg/sglang:dev-cu12@aac6b242`), `x-glm51-awq-cmd` (GLM TP4 + EAGLE + `--watchdog-timeout 1800` + `--revision 8f60817a`), `x-glm51-awq-model-env` (including `TORCHINDUCTOR_CACHE_DIR`/`TRITON_CACHE_DIR`), the `model-sg-glm51-awq-tp4` service (its `image: glm51-sgl-awq-tp4-patched:local`, `build:`, the whole-`/root/.cache` `compile_cache_glm51` volume, GPUs 4-7), `proxy-glm51`, `dcgm-glm51`, the volume declaration `compile_cache_glm51`, the GLM downloader line, the three GLM scrape jobs, the GLM registrar routes and the two GLM nginx blocks. Nothing GLM-related survives (grep `glm`/`GLM`/`aac6b242`/`EAGLE`/`speculative`: only the filename).

**B. Qwen3.6 re-add (`1009d183`, hunks 3-9, 15, 20, 22, 24-25, 27, 29-35).** New `x-sglang-env` anchor (`:159-166`) and two engine anchors `x-qwen3-6-27b-common` (`:168-201`) / `x-qwen3-6-35b-a3b-common` (`:204-237`), each `lmsysorg/sglang:v0.5.16-cu129@984699c2` (`:171`, `:207`); services `model-sg-qwen36-27b-fp8-tp1` (`:677-708`, GPU 4 `:688`) and `model-sg-qwen36-35b-a3b-fp8-tp1` (`:710-741`, GPU 5 `:721`); proxies `proxy-qwen36-27b` (`:411-450`, on `b183677a` `:413`) and `proxy-qwen36-35b-a3b` (`:451-490`, `:453`); dcgm `dcgm-qwen36-27b` (`:834-862`) and `dcgm-qwen36-35b-a3b` (`:864-892`); downloader lines for both models **without `--revision`** (`:262`, `:264`); nginx `depends_on` (`:279-280`), two new host ports `8006`/`8007` (`:289-290`), plaintext listener `8006` (`:1761-1762`) and TLS listeners `8005` (`:1788-1793`, retargeted from GLM) and `8007` (`:1797-1802`); registrar env `TLS_PORT_QWEN36_27B`/`TLS_PORT_QWEN36_35B` (`:317-318`), port loop now `8001 8002 8004 8006` (`:1550`), `routing_port_for`/`model_for`/`domain_for` cases (`:1558-1559`, `:1564-1571`, `:1573-1580`), `register_model` for the two Qwen3.6 domains (`:1708-1709`); six new Prometheus scrape jobs (`:1185`, `:1205`, `:1286`, `:1306`, `:1366`, `:1386`). All new labels/tags are the static launch-parameter taxonomy (`model`, `deployment:qwen36-gpu02`, `gpu_pair`, `ppcie`, `config_variant:qwen36-sglang-v0516`) — no request-derived value.

**C. DeepSeek second replica (`fd0bdb2c`, hunks 1, 8-9, 12, 13, 21, 24).** Header (`:5`, `:9`, `:16-17`); `proxy-dsv4-flash` env `VLLM_BASE_URL` -> `VLLM_BACKEND_URLS=http://model-sg-dsv4-flash-fp4-tp2-r1:8000,http://model-sg-dsv4-flash-fp4-tp2-r2:8000` (`:344`) and its `nearai.otel.instance`/`gpu_pair` labels dropped (proxy now spans both pairs; `:352-369` read); new service `model-sg-dsv4-flash-fp4-tp2-r2` (`:539-585`) off the **unchanged** `x-dsv4-flash-common` anchor (`:56-108`, not in the diff), GPUs 6-7 (`:550`), labels differing from r1 only in `container_name`, `instance:2`, `gpu_pair:6-7` (`:553` vs `:505`, tails compared); new `dcgm-dsv4-flash-r2` (`:773-801`, GPUs 6-7 `:781`); two new scrape jobs (`:1086`, `:1245`); the proxy scrape job's `instance`/`gpu_pair` labels dropped (hunk 27).

**D. Datadog OpenMetrics check removal (`c9605490`, hunks 11, 13-15, 17, 19, 20).** `com.datadoghq.ad.check_names`, `com.datadoghq.ad.init_configs` and `com.datadoghq.ad.instances` deleted from every engine and dcgm service that had them (base: three engines + three dcgm). **Zero** of those three keys remain (grep-verified). `com.datadoghq.ad.logs` is untouched by this commit and present on all 19 services (`:271`, `:300`, `:327`, `:351`, `:392`, `:432`, `:472`, `:505`, `:553`, `:601`, `:646`, `:691`, `:724`, `:754`, `:784`, `:814`, `:845`, `:875`, `:924`); every one of the 19 carries both a `model:` and a `deployment:` tag (grep-counted 19/19/19).

**E. Not changed by any hunk (verified by absence from the diff, then re-read):** `x-logging-conf` (`:27-32`), `x-nvidia` (`:34-41`), `x-vllm-proxy-common` (`:43-54`), the DeepSeek anchor (`:56-108`), the Qwen3.8 anchor (`:110-152`), `x-downloader-common` (`:153-157`), `x-dcgm-common` (`:240-246`), `proxy-qwen38-27b` (`:370-410`), both Qwen3.8 services except the Datadog deletions, `otelcol-contrib` service (`:894-925`), `networks` (`:926-929`), the otelcol receivers/operators/processors/exporters/pipelines (`:943-1050`, `:1415-1529`), the registrar's probe and registration functions, the nginx `log_format`/buffering/body-size directives (`:1716-1743`).

## Delta summary — every base-page finding re-checked here

| base finding (c27ed3b3 page) | status here | evidence at this file |
|---|---|---|
| HIGH ON — all plaintext-service stdout shipped unredacted to `telemetry.infra.near.ai` | **CONFIRMED, wider** — 19 labeled services (was 16), all kept by the filter | `:27-32`, `:911`, `:952`, `:982`, `:990`, `:1451-1460`, `:1494`, `:1510-1513` |
| MEDIUM ON — Qwen3.8 replicas fetch request media with no allowlist | **CONFIRMED and CHANGED (widened)** — now four sglang engines: Qwen3.8 x2 (`fa3` mm attention) + Qwen3.6 x2 (`--mm-feature-transport cpu`) | `:125`, `:181`, `:217`, no `--disable-multimodal`/allowlist anywhere |
| MEDIUM ON — unauthenticated engine admin behind allowlist-free nginx + proxies | **CONFIRMED, wider** — eight `location /` blocks (was six), four proxies (was three) | `:1747`, `:1752`, `:1757`, `:1762`, `:1775`, `:1784`, `:1793`, `:1802`; `:926-929` |
| MEDIUM OFF — `/compose/up` splice arms hidden states + web-search URL | **CONFIRMED** — splice still on the DeepSeek anchor (now two replicas), `WEB_CONTEXT_*`/`FUSION_*` on all four proxies | `:90`; `:342-343`, `:348-349`, `:383-384`, `:389-390`, `:424-425`, `:429-430`, `:464-465`, `:469-470` |
| LOW ARMED — Qwen3.8 on 300 s watchdog default, DeepSeek/GLM 1800 | **CHANGED (widened)** — Qwen3.8 x2 + Qwen3.6 x2 at default; DeepSeek x2 at 1800; GLM gone. Labels still truthful | `:79` only `--watchdog-timeout`; labels `:531`, `:579`, `:1079`, `:1114` only |
| INFO ON — complete `--revision` pinning beside `--trust-remote-code` | **REFUTED for this revision** — DeepSeek and Qwen3.8 pinned, both Qwen3.6 engines unpinned, downloader also unpinned | `:63/:65`, `:118/:120` pinned; `:193`, `:229` with no `--revision`; `:262`, `:264` |
| INFO ON — OHTTP literal on every proxy | **CONFIRMED** on four | `:340`, `:381`, `:422`, `:462` |
| INFO ON — fully-pinned in-enclave GLM build | **GONE** — build, patch, image, compile volume all removed | grep-verified absent |
| INFO ON — audited proxy digests | **CONFIRMED** — `b0c5cd07` (DeepSeek, via anchor `:44`), `b183677a` x3 (`:372`, `:413`, `:453`); the base's "preserve the GLM proxy digest" comment is gone, so no in-file reason for the split remains | |
| INFO ON — privileges/volumes/egress unchanged | **CHANGED (minor)** — `compile_cache_glm51` gone; `kernel_cache` (`/root/.cache/deep_gemm`) now shared by DeepSeek x2 **and** Qwen3.6 x2 | `:91-93`, `:196-198`, `:232-234`, `:931-939` |
| INFO — stale `8192` prose comment | **CONFIRMED** — still `8192-token mixed prefill` at `:13` vs `--chunked-prefill-size 4096` at `:76` | |

---

## Findings

### HIGH (deployed: ON) — every plaintext service's stdout leaves the CVM via the recipe's own otelcol-contrib, unredacted, to near.ai's telemetry gateway; the Datadog OpenMetrics removal did not narrow the shipped set — sink: telemetry / egress

Carried; chain re-verified at this file. Every app service logs via `json-file` (`x-logging-conf`, `:27-32`) with `labels: "com.datadoghq.ad.logs,com.docker.compose.service"` (`:32`), so the Datadog logs label is stamped onto each record. The digest-pinned collector (`:895`) runs as root (`:898`), mounts `/var/lib/docker/containers:ro` (`:911`) and tails `*-json.log` (`:952`) with a file-storage checkpoint (`:954`) on the `otelcol_app_storage` volume (`:912`, `:932`); `regex_parser` operators lift `model:` (`:982`), `served_model:` (`:986`) and `deployment:` (`:990`) out of the label; `filter/app_logs` (`:1451-1460`) drops only records with **neither** attribute (`:1455`) or from five named management services (`:1456-1460` — `compose-manager`, `compose-manager-launcher`, `otelcol-management`, `otelcol-contrib`, `datadog-agent`). All 19 `com.datadoghq.ad.logs` labels in this file carry both `model:` and `deployment:` (counted), so every engine, proxy, dcgm, nginx, registrar and downloader record is kept and exported verbatim by `logs/app` (`:1510-1513`) to `https://telemetry.infra.near.ai` (`:1494`, bearer `:1496`). **No scrub, hash, or truncate processor exists** (`:1512` lists `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch`); "no content leaves" reduces to per-image print discipline, with each engine's `/configure_logging` and watchdog dump as the known switch-on paths (engine pages). Delta specifics: **(a)** commit `c9605490` deleted only the `check_names`/`init_configs`/`instances` OpenMetrics keys — none of the three remains — and left `com.datadoghq.ad.logs` everywhere; since the collector keys on `ad.logs`, the removal changes nothing about which containers ship. The label also still feeds any `datadog-agent` present in the measured harness (a second off-box leg on datadog-bearing nodes; harness page, residual). **(b)** Three new plaintext-handling stdout streams join the export: `model-sg-dsv4-flash-fp4-tp2-r2` (`:553`), `model-sg-qwen36-27b-fp8-tp1` (`:691`), `model-sg-qwen36-35b-a3b-fp8-tp1` (`:724`), plus two proxies (`:432`, `:472`). **(c)** The export is still single-consumer (`num_consumers: 1` `:1498`, `queue_size: 128` `:1499`) over the disk-persisted queue (`:1500`, `:1503-1505`): a content-bearing record, if a print ever fires, sits on the persistent volume awaiting export. The OTLP receivers on `0.0.0.0:4317/4318` (`:947`, `:949`; `expose`d `:907-909`) still feed `metrics/otlp` (`:1518-1521`) and `traces` (`:1526-1529`) off-box with no content filter [MEDIUM, carried]. All Prometheus jobs (`:1051-1410`) scrape `/metrics` with the static config-label taxonomy — the new Qwen3.6/r2 jobs were read value by value: metadata, not content [INFO].

### MEDIUM (deployed: ON) — four sglang engines fetch request-supplied media URLs with no domain allowlist: both Qwen3.8-27B replicas (carried) and both re-added Qwen3.6 engines, and this file adds no containment — sink: egress (request-driven)

Widened from the base. Qwen3.8 anchor (`:110-152`) unchanged: `--mm-attention-backend fa3` (`:125`) keeps multimodal live on the `febfb971` nightly (`:115`), whose page rates the allowlist-free fetch MEDIUM ON. Qwen3.6 anchors: `--mm-feature-transport cpu` (`:181`, `:217`) on `lmsysorg/sglang:v0.5.16` (`:171`, `:207`), whose page verified both models are multimodal at that version with no allowlist mechanism. Both Qwen3.6 command lines are **byte-identical** (modulo indentation) to the ones the `c6ef1271` page reviewed (that file's `:131-153` and `:170-192` diffed against this file's `:173-195` and `:209-231`: identical), so that page's finding transfers unchanged. No `--disable-multimodal`, no media-guard flag, no `--allowed-media-domains` equivalent, no egress allowlist anywhere; the network is the flat external bridge `dstack_default` (`:926-929`, no `internal: true`). Unlike the sibling file, there is no vLLM Gemma here to contrast against — every engine in this file is sglang, and none of the four multimodal ones is contained. DeepSeek (`:56-108`) sets no multimodal flag; whether the `ec518148` build accepts media inputs for this model is an engine question (its page), not asserted here.

### MEDIUM (deployed: ON) — the engines' unauthenticated admin surface is reachable through eight allowlist-free nginx location / blocks and four proxies on the flat external network — sink: log / egress

Carried, widened. Flat `external: true` bridge (`:926-929`); no `--api-key` on any of the six engine commands (grep-verified). nginx (digest-pinned `:274`) now publishes eight host ports — `8001`-`8007` and `8444:443` (`:283-290`) — with four plaintext HTTP listeners (`8001` `:1746`, `8002` `:1751`, `8004` `:1756`, `8006` `:1761`) and four TLS (`443` `:1770`, `8003` `:1779`, `8005` `:1788`, `8007` `:1797`), every one routing with a bare `location /` (`:1747`, `:1752`, `:1757`, `:1762`, `:1775`, `:1784`, `:1793`, `:1802`) — so each engine's unauthenticated `/configure_logging` (CRITICAL on all three engine pages) is one POST away laterally and, absent path filtering at the proxy, from the front door. In-file mitigations hold: upstreams are always proxies, never engines; `proxy_buffering off; proxy_cache off` (`:1739-1740`); `client_max_body_size 100m` / `client_body_buffer_size 1m` (`:1742-1743`, so bodies above 1 MB spool to nginx's container-local temp path — OHTTP ciphertext when OHTTP is used, carried); the access log format (`:1716-1731`) uses no body variable but ships `request_id`/`org_id`/`workspace_id` (`:1718-1720`) as per-request user-attributable metadata on the exported stream [INFO].

### MEDIUM (deployed: OFF, splice-armed) — the /compose/up env-splice can arm hidden-state passthrough on both DeepSeek replicas and hand all four E2EE terminators an external search URL, with no attestation-visible change — sink: response / egress

Carried. `${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}` (`:90`) sits on the shared DeepSeek anchor, so one spliced variable now arms **both** replicas; the `:+` form arms on any non-empty value; deployed OFF (no default). `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (`:348-349`, `:389-390`, `:429-430`, `:469-470`) and `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (`:342-343`, `:383-384`, `:424-425`, `:464-465`) remain operator-suppliable to all four proxies via the non-measured splice; both deployed proxy builds were source-verified fail-closed when unset (proxy pages; digests unchanged at `:44`, `:372`, `:413`, `:453`). `HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}` (`:96`, `:144`, `:162`) is likewise splice-settable but defaults to network-on, which matters for the next finding.

### LOW (deployed: ARMED) — both Qwen3.6 engines run --trust-remote-code with no --revision pin, and this file's downloader pre-fetches them at whatever Hugging Face main resolves to — sink: potential in-enclave code execution

Re-added with the services; **worse than the sibling file in one respect.** Pinning, engine by engine: DeepSeek `--trust-remote-code` (`:63`) with `--revision 7872f01b…` (`:65`) — pinned; Qwen3.8 `:118` with `--revision 017b9c7a…` (`:120`) — pinned; Qwen3.6-27B `--trust-remote-code` (`:193`) with **no `--revision`**; Qwen3.6-35B-A3B `:229`, **no `--revision`**. The model-downloader (`:249-272`) pins DeepSeek (`:256` — the `553034d7` rollback checkpoint carried from base — and `:258`) and Qwen3.8 (`:260`) but fetches `Qwen/Qwen3.6-27B-FP8` (`:262`) and `Qwen/Qwen3.6-35B-A3B-FP8` (`:264`) **unpinned**; on the `c6ef1271` file the downloader did not fetch Qwen3.6 at all. With `HF_HUB_OFFLINE` defaulting to `0` (`:162`) the engines can also re-resolve `main` at load. ARMED not ON: the `984699c2` engine page verified nothing executes at the observed HF `main`; a later push to either repo would run attacker-chosen Python inside a plaintext-handling enclave with no trace in the attestation chain. The fix is in this file: `--revision <sha>` on `:193`/`:229` and `:262`/`:264`, as the other two models already do. This refutes the base page's "complete revision pinning" INFO for this revision.

### LOW (deployed: ARMED) — four sglang engines run the undisableable watchdog hang-dump at the 300 s default (both Qwen3.8 and both Qwen3.6), while both DeepSeek replicas run at 1800 s — sink: log (feeding the telemetry egress)

Widened from the base (was two Qwen3.8 + GLM at 1800). The only `--watchdog-timeout` in the file is DeepSeek's `1800` (`:79`, on the anchor shared by r1 and r2). The Qwen3.8 command (`:117-138`) and both Qwen3.6 commands (`:173-195`, `:209-231`) pass none, so all four run at the engine default — 300 s per the `febfb971` and `984699c2` pages, where the dump (full prompt and output token IDs of the in-flight batch to stdout at ERROR) is HIGH; through the HIGH above it leaves the box. **Label truthfulness re-checked:** `watchdog_timeout:1800` is asserted only for the two DeepSeek replicas (`:531`, `:579`; Prometheus `:1079`, `:1114`) which deploy the flag — truthful; no Qwen3.8 or Qwen3.6 label asserts any watchdog value (`:601`, `:646`, `:691`, `:724` read). The sibling `c6ef1271` page reported the same for the Qwen3.6 pair.

### INFO (deployed: ON) — the DeepSeek second replica: anchor-identical, pooled by VLLM_BACKEND_URLS, per-replica difference is GPUs and static labels only — sink: none new

`model-sg-dsv4-flash-fp4-tp2-r2` (`:539-585`) merges the unchanged `x-dsv4-flash-common` (`:56-108`): same image `ec518148` (`:60`), same command (`--revision` `:65`, `--chunked-prefill-size 4096` `:76`, `--watchdog-timeout 1800` `:79`, `--log-requests-level 0` `:88`, splice `:90`), same volumes (`:91-93`) and env (`:94-105`, including `SGLANG_CHUNKED_PREFILL_ADMISSION_RESERVE=4096` `:102`). It adds only `container_name`, GPUs `6-7` (`:550`), and labels whose tails differ from r1's (`:505`) solely in `instance:2` / `gpu_pair:6-7` (`:553`; otel labels `:554-585`). `dcgm-dsv4-flash-r2` (`:773-801`) mirrors `dcgm-dsv4-flash` on GPUs 6-7 (`:781`) with `SYS_ADMIN` from the digest-pinned anchor (`:241`, `:243-244`). `proxy-dsv4-flash` now names both engines in `VLLM_BACKEND_URLS` (`:344`) — the same key `proxy-qwen38-27b` already used for its two replicas (`:385`) — so backend selection is the proxy's (proxy page `b0c5cd07`); no per-replica env or label distinguishes the two engines' privacy posture. Header prose updated (`:5`, `:9`); the `:13` "8192-token mixed prefill" comment remains stale against `:76`.

### INFO (deployed: ON) — the Qwen3.6 re-add compared line by line with the c6ef1271 review: commands identical, proxies on b183677a with the sibling env, one new shared kernel-cache volume — sink: none new

Engine commands: byte-identical to the `c6ef1271` file's (diffed above). Env: the new `x-sglang-env` (`:159-166`) is the same six variables the Qwen3.8 anchor sets inline (`:142-148`) — `HF_TOKEN`, `HF_HUB_OFFLINE`, `VLLM_LOGGING_LEVEL=INFO` (`:163`, a vLLM knob inert on sglang, carried house pattern), `NVIDIA_DRIVER_CAPABILITIES`, `OPENBLAS_L2_SIZE`, `NCCL_DEBUG=WARN` (`:166`); no `SGLANG_*` debug, dump, or profiler variable. `--log-requests-level 0` on both (`:194`, `:230`); `--enable-metrics` (`:192`, `:228`) is the Prometheus endpoint the scrape jobs read (`:1185`, `:1205`). Volumes (`:196-198`, `:232-234`): `huggingface_cache` (checkpoint bytes) and `kernel_cache:/root/.cache/deep_gemm` — the same named volume DeepSeek mounts (`:93`), so one compiled-kernel cache is now shared across four containers of two engine builds (compile artifacts, not request content; no KV/prompt/request disk tier anywhere, grep-verified for `dump`/`profil`/`DEBUG` beyond `NCCL_DEBUG=WARN`). Proxies `proxy-qwen36-27b` (`:411-450`) / `proxy-qwen36-35b-a3b` (`:451-490`): `b183677a` (`:413`, `:453`), `LOG_FORMAT=json` (`:420`, `:460`), `OHTTP_ENABLED=true` (`:422`, `:462`), single `VLLM_BASE_URL` (`:426`, `:466`), `USE_NV_ATTESTATION_SDK=true` (`:428`, `:468`), the splice-fed `FUSION_*`/`WEB_CONTEXT_*` pairs, and **no** `VLLM_PROXY_MAX_REQUEST_SIZE` (the DeepSeek and Qwen3.8 proxies set `104857600`, `:345`, `:386`) — so the Qwen3.6 proxies run the proxy build's default request cap (proxy page), while nginx caps all routes at 100 MB (`:1742`). Proxy privileges are the carried anchor: `user: root`, `privileged: true`, `/var/run/dstack.sock` (`:45-46`, `:51`). Registrar: health-probes each port with a **synthetic** `"hi"` message (`:1614-1618`) — not user content — and registers the two Qwen3.6 domains (`:1708-1709`) against `https://completions.near.ai` (`:1539`). Proxy build split: the DeepSeek proxy inherits `b0c5cd07` from the anchor (`:44`) while the other three override to `b183677a`; the base file's comment explaining the override ("preserve the exact proxy digest used by the current GLM-5.1 production config") was deleted with GLM, so this file now gives no reason for running two proxy builds. Both are audited (verdicts: `b0c5cd07` core path clean with two client-opt-in egress features; `b183677a` PRIVATE with the same two caveats); the behavioral difference between them is the proxy pages' matter.

### INFO (deployed: ON) — GLM-5.1 removal: the in-enclave patched build, its whole-cache compile volume and EAGLE flags are gone; the filename is now stale — sink: none

Removed in full (group A above). Privacy-relevant consequences: the only `build:` in the file (a `dockerfile_inline` that rewrote engine source at image build time inside the enclave) is gone, so every engine now runs a registry digest; the `compile_cache_glm51` volume that mounted over the whole of `/root/.cache` is gone; the GLM `TORCHINDUCTOR_CACHE_DIR`/`TRITON_CACHE_DIR` env is gone. Remaining volumes (`:931-939`): `otelcol_app_storage`, `huggingface_cache`, `kernel_cache`, `qwen38_kernel_cache` (`/root/.cache/sglang`, `:141`), external `certs`. `otelcol_app_storage` remains the one volume that can hold request-derived bytes (the persisted export queue, HIGH above). The file path `prod/dsv4-qwen38-glm51.yaml` no longer describes its contents; anyone matching the action log by filename to "a GLM node" would be misled.

---

## Verdict

**QUALIFIED PASS at this layer — a capacity reshuffle that adds no new sink class and removes one in-enclave build, but widens three carried residuals.** The diff is exactly: GLM-5.1 out; two Qwen3.6 sglang engines in with command lines identical to those reviewed yesterday on `c6ef1271`; a second DeepSeek replica off the unchanged anchor; Datadog OpenMetrics check labels deleted with the logs label left in place. Nothing in the file logs, dumps, or exports request content at the deployed flags: `--log-requests-level 0` on all six engine commands, OHTTP forced on all four proxies, no debug/profiler env, no scrape target that carries content, all telemetry labels static and truthful. The qualifications, in order of cost: **(1)** all plaintext-service stdout — now 19 labeled streams including three new engine containers — leaves the TEE unredacted via the recipe's own otelcol to `telemetry.infra.near.ai`, queue persisted to disk (HIGH, ON); **(2)** four sglang engines are unguarded multimodal media fetchers on an egress-unrestricted network, all four on the 300 s watchdog hang-dump default (MEDIUM ON + LOW ARMED); **(3)** both Qwen3.6 engines and their downloader lines are `--trust-remote-code` with no revision pin — a regression against this recipe's previous "everything pinned" state, and a step beyond the sibling file, which at least did not pre-fetch them (LOW, ARMED); **(4)** the unauthenticated engine `/configure_logging` sits behind eight bare `location /` listeners and four allowlist-free proxies (MEDIUM, ON); **(5)** the `/compose/up` splice can arm hidden states on both DeepSeek replicas at once and supply search/fusion endpoints to four E2EE terminators (MEDIUM, OFF). Recommendations, all in this file: `--revision` on `:193`, `:229`, `:262`, `:264`; `--watchdog-timeout` on the Qwen3.8 and Qwen3.6 anchors; a path allowlist at nginx or proxy; a scrub/redact processor in `logs/app`; bind the OTLP receivers to localhost; an egress allowlist for the engines; rename the file or the header (`:1`) so the path stops naming a model it does not run; fix `:13`.

## Not traced (load-bearing)

- Whether this `file_sha256` matches the signed action log's current entry live — the orchestrator's `fd0bdb2c` fetch was hash-verified and the captured bytes re-hashed here, but the log was not re-fetched in this review, and this file had two `compose_up` entries within two hours today; a third may exist by the time this is read.
- Whether `/compose/up` env splices (including `DSV4_RETURN_HIDDEN_STATES`, `HF_HUB_OFFLINE`, `WEB_CONTEXT_*`, `FUSION_*`) surface in the signed action log — compose-manager source, carried.
- Engine print discipline, `/configure_logging`, watchdog dump shape, multimodal fetch behavior, and whether the `ec518148` DeepSeek build accepts media inputs — deferred to the engine pages (`ec518148`, `febfb971`, `984699c2`).
- Whether Hugging Face `main` for `Qwen/Qwen3.6-27B-FP8` and `Qwen/Qwen3.6-35B-A3B-FP8` still carries no executable remote code at the moment this node pulled them — the `984699c2` page checked at its review time; this document cannot.
- Proxy backend selection across `VLLM_BACKEND_URLS`, the request-size default when `VLLM_PROXY_MAX_REQUEST_SIZE` is unset (Qwen3.6 proxies), catch-all forwarding, web-search fail-closed behavior, fusion gating, and the behavioral difference between `b0c5cd07` and `b183677a` — proxy pages.
- What `--enable-return-hidden-states` exposes end-to-end if spliced on (inference-proxy + cloud-api source, outside this repo's scope).
- The measured harness (`d98a2568`) for these five hostnames: compose-manager, whether a `datadog-agent` is present (a second off-box stdout leg keyed by the same `com.datadoghq.ad.logs` label), and the measured `allowed_envs` — that page, not re-derived here.
- otelcol exporterhelper drain/retry semantics at `num_consumers: 1`, `queue_size: 128` under a wedged gateway (drop vs indefinite persistence on the volume) — carried, not source-traced.
- The PyPI-resolved `huggingface_hub[hf_xet]` the downloader executes (`:256-264`) — bytes unaudited, carried.
- Whether the shared `kernel_cache` volume (`deep_gemm`) can carry anything request-derived across the DeepSeek and Qwen3.6 engine builds — treated here as compile artifacts on the strength of the mount path only.
- Whether the four hostnames that moved from `c6ef1271` to this file still answer on the GPU02 node exclusively, or whether any `dsv4-qwen36-gemma4` instance remains live elsewhere — fleet placement is the orchestrator's capture.
