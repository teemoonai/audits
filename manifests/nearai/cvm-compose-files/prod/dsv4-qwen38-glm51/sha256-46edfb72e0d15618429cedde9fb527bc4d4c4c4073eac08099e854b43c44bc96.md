# cvm-compose-files prod/dsv4-qwen38-glm51.yaml @ 46edfb72... (three-model inner compose — DeepSeek-V4-Flash-0731 + 2x Qwen3.8-27B + GLM-5.1-AWQ, GPU02 capacity pack)

## verdict: QUALIFIED PASS on logging config — no content-logging switch anywhere, all three engines pin --revision beside --trust-remote-code, OHTTP forced on all three proxies, both watchdog_timeout:1800 labels are truthful, the in-enclave GLM build resolves every input from a pinned digest and its patch is the audited privacy-neutral one; but stdout of every plaintext service still ships off-box unredacted, both Qwen3.8 engines are multimodal media fetchers with no allowlist running the hang-dump watchdog at the 300 s default, the unauthenticated engine admin surface sits behind allowlist-free nginx and proxies on the flat network, and the /compose/up env-splice can arm hidden-state passthrough and web-search egress with no attestation-visible change

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/dsv4-qwen38-glm51.yaml` |
| **file_sha256** | `46edfb72e0d15618429cedde9fb527bc4d4c4c4073eac08099e854b43c44bc96` (re-verified locally: `shasum -a 256` of the captured file matches) |
| **role** | log-pinned inner compose for the GPU02 three-model capacity pack: `deepseek-ai/DeepSeek-V4-Flash-0731` (SGLang FP4 TP2, GPUs 0-1), `Qwen/Qwen3.8-27B-FP8` (SGLang TP1, two replicas, GPUs 2 and 3), `QuantTrio/GLM-5.1-AWQ` served as `zai-org/GLM-5.1-FP8` (SGLang AWQ TP4 + EAGLE, GPUs 4-7, in-enclave patched build) — behind three `vllm-proxy-rs` instances, nginx, registrar, three dcgm exporters, otelcol-contrib. Deployed on 3 fleet hosts: `glm-5`, `glm-5-1`, `qwen3-8-27b` |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. Fetched at compose-manager commit `9b5ec2ec0d60` and hash-checked against the log's `file_sha256` before being written to disk; the action-log match was not re-fetched live in this review |
| **review** | reviewed 2026-08-17 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** the full 1529-line compose, as a **FULL review — there is no prior page for this recipe path**, so every finding is established here (sibling pages are cited only where a shared mechanism was independently re-verified in this file). Covered: three engine definitions (anchors `x-dsv4-flash-common` `:58`, `x-qwen3-8-27b-common` `:112`, and the GLM trio `x-glm51-awq-build` `:164` / `x-glm51-awq-cmd` `:190` / `x-glm51-awq-model-env` `:216`) across four model containers, three proxy configs, the in-enclave GLM build, the model-downloader, nginx routing, the registrar script, three dcgm exporters, and otelcol-contrib end to end. Per-image behavior is deferred to the published image pages, linked per finding: DeepSeek engine [`nearaidev/sglang@ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) (PRIVATE at deployed flags), both Qwen3.8 engines [`lmsysorg/sglang@febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md) (PRIVATE at deployed flags — audited concurrently in this run; identity `sgl-project/sglang` @ `c4271c3f`, a **nightly dev build**), GLM build base [`lmsysorg/sglang:dev-cu12@aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md) (PRIVATE at deployed flags, page covers base **plus** this exact patch), and the two proxy builds [`vllm-proxy-rs@b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md) / [`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (both audited in-repo).

---

## Env reachability

Every logging knob in this file is a literal, not an operator-set env: `--log-requests-level 0` on all three SGLang commands (`:90`, `:140`, `:201` — the minimum), `VLLM_LOGGING_LEVEL=INFO` on DeepSeek and Qwen (`:99`, `:147`), `LOG_FORMAT=json` on all three proxies (`:318`, `:361`, `:404`), `NCCL_DEBUG=WARN` (`:102`, `:150`, `:221`), otelcol `telemetry.logs.level: info` (`:1278`). **`OHTTP_ENABLED=true` is a literal on all three proxies** (`:320`, `:363`, `:406`) — E2EE posture is not operator-degradable. **No `--api-key`/`--admin-api-key` anywhere** (grep-verified; the only `API_KEY` strings are the proxies' `WEB_CONTEXT_SEARCH_API_KEY`) — engines are unauthenticated on the flat network.

Interpolated vars reachable only via compose-manager's non-measured `/compose/up` splice (not the measured `allowed_envs` channel; the measured harness for these nodes is referenced, not re-derived here): `DSV4_RETURN_HIDDEN_STATES` (`:92` — the file's only `${VAR:+flag}` arming form), `HF_HUB_OFFLINE` (defaulted `0`: `:98`, `:146`, `:218`), `HUGGING_FACE_HUB_TOKEN` (`:97`, `:145`, `:217`, `:253`), `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` and `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` on all three proxies (`:322-:323`/`:328-:329`, `:365-:366`/`:371-:372`, `:408-:409`/`:413-:414`), `CLOUD_API_USAGE_TOKEN`, `PROXY_TOKEN`, `MODEL_PROXY_TOKEN` (`:299-:300`), `MONITORING_INGEST_TOKEN` (`:747`). None of them is a log-level or content-logging switch.

---

### HIGH (deployed: ON) — every plaintext service's stdout leaves the CVM via the recipe's own otelcol-contrib, unredacted, to near.ai's telemetry gateway — sink: telemetry / egress

Traced end to end in this file, receiver and filter both — the keep decision is invisible from the filter alone. Every app service logs via the `json-file` driver (`x-logging-conf`, `:29-34`), which stamps `com.datadoghq.ad.logs` and `com.docker.compose.service` onto each container's log labels (`:34`). The recipe runs its **own** `otelcol-contrib` (`:740`, digest-pinned `otel/opentelemetry-collector-contrib@sha256:85ac41c2...`) with `/var/lib/docker/containers:ro` mounted (`:757`), and its `logs/app` pipeline (`:1254-1257`) is:

```
receivers:  [filelog/docker_containers]           # tails /var/lib/docker/containers/*/*-json.log  (:799)
processors: [memory_limiter, resource,
             transform/docker_metadata,           # lifts service.name from the labels             (:1184)
             filter/app_logs,                     # KEEP records with model/deployment attrs       (:1196)
             groupbyattrs/docker_service, batch]
exporters:  [otlphttp/gateway]                    # https://telemetry.infra.near.ai                (:1239)
```

The filelog receiver's `regex_parser` operators lift `model:` (`:827-830`) and `deployment:` (`:835-838`) out of the `com.datadoghq.ad.logs` label into attributes. `filter/app_logs` (`:1196-1205`) then **drops** only records where `attributes["model"] == nil and attributes["deployment"] == nil` (`:1200`) or whose `service.name` is a management service (`compose-manager`, `-launcher`, `otelcol-management`, `otelcol-contrib`, `datadog-agent`, `:1201-1205`), and **keeps everything else**. Every service in this file carries a `model:` + `deployment:` pair in its label — engines (`:452`, `:503`, `:551`, `:610`), proxies (`:331`, `:374`, `:416`), and even nginx/registrar/downloader/dcgm via `model:multi` (`:281`, `:307`, `:255`, `:653`, `:686`, `:720`) — so its stdout is kept and exported verbatim to `https://telemetry.infra.near.ai` with `Authorization: Bearer ${MONITORING_INGEST_TOKEN}` (`:1239-1241`). **Reading `filter/app_logs` alone gives the opposite answer** — the keep/drop polarity is only legible with the receiver that populates `model`/`deployment`. This confirms on this recipe the mechanism verified on three sibling recipes in this run.

**There is no scrub/hash/truncate processor in this pipeline** (`:1160-1235`). So "no content leaves" reduces exactly to "no plaintext service ever prints content" — carried entirely by the per-image source pages. The known ways a print gets switched on or forced are the engines' unauthenticated `/configure_logging` (CRITICAL on all three engine pages) and the watchdog hang-dump (HIGH on the engine pages, armed at **300 s** on both Qwen3.8 replicas — see the LOW below), multiplied here across three engine codebases and three proxies. The exporter's `sending_queue` is disk-persisted (`storage: file_storage`, `:1244`, directory `:1248` on the `otelcol_app_storage` volume, `:758`/`:778`) — any content-bearing record that does get printed is also buffered to a persistent volume while awaiting export. otelcol's own stdout uses the `local` driver (`:764-765`) and its `service.name` is dropped by the filter (`:1204`); no datadog-agent service exists in this file (the filter's `datadog-agent` entry is for the measured layer, where present).

- **[MEDIUM]** otelcol-contrib's OTLP receivers on `0.0.0.0:4317/4318` (`:794`, `:796`; `expose`d `:753-755`) feed the `metrics/otlp` and `traces` pipelines (`:1262-1265`, `:1270-1273`) forwarding **verbatim** to near.ai with no content filter. Any prompt-bearing span injected on the flat network ships unexamined.
- **[INFO]** All metrics paths are Prometheus scrapes of `/metrics` (`:895-1159`) — counters/histograms with routing labels; the filelog `regex_parser` operators (`:823-890`) extract the same static label taxonomy (model, served_model, deployment, ip, port, env, host, instance, gpu_pair, ppcie, config_variant, quantization, eagle, sglang_revision, engine_image). Metadata, not content; no request-derived value anywhere in the label sets (checked service by service).

---

### MEDIUM (deployed: ON) — both Qwen3.8-27B replicas are multimodal engines that fetch request-supplied media URLs with no domain allowlist, and this file adds no containment — sink: egress (request-driven)

The Qwen anchor's command enables multimodal attention explicitly: `--mm-attention-backend fa3` (`:127`), and the file's own header documents "vision" checks in the qualification soak (`:20-21`). The concurrent [engine review of `febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md) rates the media fetch MEDIUM (deployed ON): an image/audio/video URL in a request is fetched by the plaintext engine process with **no domain allowlist mechanism in sglang at all** and redirects followed by default. Two such replicas run here (`:487`, `:535`). The bytes carried outbound are the requesting client's own URL — not cross-tenant prompt exfiltration — but it is a genuine model-external, request-driven egress from the plaintext process (SSRF / confirmation channel / local-read primitive). Unlike the sibling gemma4 recipe, this file has **no vLLM service demonstrating the constrained pattern**, and the network offers no egress allowlist (see INFO below): containment here would require `--disable-multimodal` (at the cost of vision, which the header says was deliberately qualified) or a network-layer control that does not exist in this file. One partial mitigation IS present and pinned: `--revision 017b9c7a...` (`:122`), so the `config.json` that makes the model multimodal cannot drift under the deployment — an improvement over the sibling recipe's unpinned Qwen3.6 pair.

---

### MEDIUM (deployed: ON) — the engines' unauthenticated admin surface is reachable through allowlist-free nginx and proxies on the flat external network — sink: log / egress

Every SGLang engine here exposes an unauthenticated `/configure_logging` (CRITICAL on all three engine pages: [`ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md), [`febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md), [`aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md)): one POST turns on verbatim prompt+output logging and arbitrary-path request dumping, no restart. This file supplies the reachability. The network is the flat `external: true` bridge `dstack_default` (`:772-775`) — any co-resident container can reach engine port 8000 directly, and no `--api-key` is set anywhere (grep-verified). nginx (digest-pinned, `:258`) publishes host ports `8001`, `8002`, `8444:443`, `8003`, `8004`, `8005` (`:265-271`) — three of them plaintext HTTP — and **every one of its 6 server blocks routes with a bare `location /`** (`:1487`, `:1492`, `:1497`, `:1510`, `:1519`, `:1528`) — no path allowlist, so the full HTTP surface of each proxy (and, through the proxies' documented catch-all forwarding — MEDIUM on the [`b0c5cd07` proxy page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md) — each engine) is exposed on every listener. In-file mitigations: upstreams are always proxies, never raw engines (`:1487`, `:1492`, `:1497`, `:1510`, `:1519`, `:1528`); `proxy_buffering off; proxy_cache off` (`:1479-1480` — no bodies to disk); the access log's `json_combined` format uses `$uri`, not `$request`, and no body variable (`:1456-1469`) — though `request_id`/`org_id`/`workspace_id` (`:1458-1460`) are per-request user-attributable metadata that the telemetry pipeline ships off-box **[INFO]**.

---

### MEDIUM (deployed: OFF) — the /compose/up env-splice channel can arm hidden-state passthrough and hand the E2EE terminators an external search URL with no attestation-visible change — sink: response / egress

`${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}` (`:92`) is the file's only env-armed flag, on the DeepSeek command. The `:+` form is a footgun: any non-empty value (`"0"` included) turns the flag ON; only unsetting it is OFF. Deployed OFF (flag absent when the var is unset). Hidden states are prompt-derived tensors in the API response; end-to-end exposure is deferred to inference-proxy/cloud-api source. The var is not a literal and not pinned by this file's hash, so — per the sibling recipes' in-file documentation of the gpu-manager splice, and consistent with everything interpolated here — it is settable through compose-manager's `/compose/up` without any measurement or action-log change.

- **[MEDIUM] `WEB_CONTEXT_SEARCH_URL` + `_API_KEY` on all three plaintext-path proxies** (`:328-:329`, `:371-:372`, `:413-:414`) — an unpinned external URL handed to the E2EE-terminating services. Both deployed proxy builds were source-verified fail-closed when unset and opt-in per request, sending only a model-generated query ([`b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md), [`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md)). Residual: operator-suppliable via the splice channel. Same for `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (`:322-:323`, `:365-:366`, `:408-:409`) — fusion forwards the full plaintext conversation when enabled AND client-opted-in (HIGH, doubly gated, on both proxy pages); deployed state of the env is unknowable from this file.

---

### LOW (deployed: ARMED) — both Qwen3.8 replicas run the undisableable watchdog hang-dump at the 300 s default because no --watchdog-timeout is passed, while DeepSeek and GLM run at 1800 s — sink: log (feeding the telemetry egress)

The Qwen command (`:118-140`) passes no `--watchdog-timeout`; at the deployed engine identity (`sgl-project/sglang` @ `c4271c3f`) the default is **300 s** (`server_args.py:1226`, verified upstream this run for the engine page). The watchdog dump — full prompt and output token IDs of the in-flight batch at ERROR, no flag to disable it — is HIGH on the [engine page](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md); this file leaves it armed at the most sensitive default on exactly the two replicas whose stdout the pipeline above exports. DeepSeek passes `--watchdog-timeout 1800` (`:81`) and GLM passes `--watchdog-timeout 1800` (`:209`) — a 6x longer fuse. A 300 s stall on a busy H200 is a plausible event, which is why this is ARMED rather than OFF.

**Label-vs-command truthfulness — checked service by service, and cleared.** `watchdog_timeout:1800` is asserted exactly twice: on the DeepSeek labels (`:452`, `:453`; `nearai.otel.watchdog_timeout: "1800"` `:479`) and the GLM labels (`:610`, `:611`; `:633`), plus the matching Prometheus copies (`:926`, `:1021`). Both asserting services actually deploy the flag (`:81`, `:209`) — truthful. Neither Qwen replica's label asserts any watchdog value (`:503`, `:551` — grep-verified no other occurrence), so there is no manifest-truthfulness disagreement to report; the sibling recipe's result holds here, but it was checked, not assumed.

---

### INFO (deployed: ON) — the in-enclave GLM build is fully pinned: digest-pinned FROM, a byte-inspected in-place patch, no mutable input, and both the base and the base+patch composition have audited pages

The priority item, cleared. `model-sg-glm51-awq-tp4` (`:583`) runs `image: glm51-sgl-awq-tp4-patched:local` built from `x-glm51-awq-build` (`:590`, `:164-188`). Read in full:

- **Base:** `FROM lmsysorg/sglang:dev-cu12@sha256:aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1` (`:166`) — **digest-pinned**; the tag beside the digest is decorative. The base has its own audited page: [`aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md), verdict PRIVATE at deployed flags — and that page explicitly covers **this build = base + patch** and finds the patch out of the serving/plaintext path.
- **What the build does:** a single `RUN python3 - <<'PYEOF'` heredoc (`:167-188`) that regex-patches one file in place (`/sgl-workspace/sglang/python/sglang/srt/models/deepseek_common/deepseek_weight_loader.py`, `:169`) — the sglang#28305 fused-qkv `cat_dim` fix, adding a shape-conditional dimension swap before a `torch.cat` (`:171-183`). It **fails loudly** on drift: `assert n == 1, "sglang#28305 cat_dim patch anchor not found (loader changed)"` (`:185`). The in-file comment states it is "copied byte-for-byte from the deployed GLM-5.1 recipe" (`:161-163`), whose page exists in-repo ([`GLM-5.1-SGL-AWQ-TP4` @ `eb00b404`](/manifests/nearai/cvm-compose-files/prod/GLM-5.1-SGL-AWQ-TP4/sha256-eb00b404e3218e2e8c8ab8da5845af10ce79929fd232fe8ac3d2f688582817be.md)).
- **No mutable input anywhere in the build:** no `pip install`, no `git clone`, no `curl`/`wget`, no ADD from a URL (grep-verified over `:164-188`). Every byte of the resulting engine is determined by the pinned base digest plus this file's own hash. This is the benign form of the in-enclave-build pattern audit-surface §8 warns about; the warning case (a build-time `pip install` replacing a pinned engine) does not occur.
- **Deployed flags** (`x-glm51-awq-cmd`, `:190-214`): `--log-requests-level 0` (`:201`), `--watchdog-timeout 1800` (`:209`), `--trust-remote-code` (`:214`) **with** `--revision 8f60817aa28023f2607850d1a1e51d21aa34817a` (`:193`), EAGLE speculative decoding (`:205-208` — draft-model state stays in GPU memory; no disk/telemetry sink introduced by this file). **Env** (`x-glm51-awq-model-env`, `:216-224`): no logging var; `TORCHINDUCTOR_CACHE_DIR`/`TRITON_CACHE_DIR` point into the persistent compile cache (`:223-224`), which is config-keyed compile artifacts, not request data.

---

### INFO (deployed: ON) — model pinning is complete and download revisions match serve revisions; one downloaded model is not served by anything in this file

Priority item 2, cleared — this file is the first in its sibling group where **every** `--trust-remote-code` is paired with a `--revision` pin:

| service | model | download (`model-downloader`) | serve command | match |
|---|---|---|---|---|
| DeepSeek (`:58-110`) | `deepseek-ai/DeepSeek-V4-Flash-0731` | `7872f01b...` (`:244`) | `--revision 7872f01b...` (`:67`), `--trust-remote-code` (`:65`) | yes |
| Qwen3.8 x2 (`:112-153`) | `Qwen/Qwen3.8-27B-FP8` | `017b9c7a...` (`:246`) | `--revision 017b9c7a...` (`:122`), `--trust-remote-code` (`:120`) | yes |
| GLM-5.1 (`:583-637`) | `QuantTrio/GLM-5.1-AWQ` | `8f60817a...` (`:248`) | `--revision 8f60817a...` (`:193`), `--trust-remote-code` (`:214`) | yes |
| — | `deepseek-ai/DeepSeek-V4-Flash` | `553034d7...` (`:242`) | **not served** | n/a |

The fourth download (`:242`, base `DeepSeek-V4-Flash` at `553034d7...`) is referenced by **no** serve command: the DeepSeek service's `--model-path` is `-0731` (`:66`) and `deepseek-ai/DeepSeek-V4-Flash` appears elsewhere only as the `--served-model-name` alias (`:91`) and in routing/labels. It is dead weight in the shared `huggingface_cache` (plausibly retained for rollback, consistent with the header's "remaining ... replica" rollout language, `:5`, `:14-15`) — pinned, weights-only, no privacy effect; noted so the asymmetry is on record. `HF_HUB_OFFLINE` defaults to `0` (`:98`, `:146`, `:218`), so an engine cache miss can reach the HF hub — but with every revision pinned, what it fetches is byte-determined. Positive asymmetry vs siblings: `dcgm-exporter` here is **digest**-pinned (`nvcr.io/nvidia/k8s/dcgm-exporter@sha256:ed594cf5...`, `:227`), unlike the mutable tag pin on the sibling recipes — its bytes cannot drift under the attestation.

---

### INFO (deployed: ON) — two different vllm-proxy-rs digests terminate E2EE here; both are audited in-repo, and their config differences are deliberate and content-neutral

Priority item 5, cleared. The anchor `x-vllm-proxy-common` pins `nearaidev/vllm-proxy-rs@sha256:b0c5cd07...` (`:46`); `proxy-dsv4-flash` inherits it (`:309-310`), while `proxy-qwen38-27b` (`:354`) and `proxy-glm51` (`:397`) override to `@sha256:b183677a...` — the GLM override carries an in-file comment: "Preserve the exact proxy digest used by the current GLM-5.1 production config" (`:395-396`).

- **Both digests have pages:** [`b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md) (core request path clean; web-search and fusion are the two client-opt-in plaintext egress features) and [`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (PRIVATE with the same two opt-in caveats, plus a MEDIUM: transport-error logs can carry the full upstream URL incl. caller query string).
- **Config comparison:** all three set `OHTTP_ENABLED=true` (`:320`, `:363`, `:406`), `LOG_FORMAT=json`, `USE_NV_ATTESTATION_SDK=true`, the same `WEB_CONTEXT_SEARCH_*`/`FUSION_*` interpolations, and identical privilege (`user: root`, `privileged: true`, `:47-48`; `ipc: host` via `x-nvidia`, `:36-38`; `dstack.sock` + `certs:ro` mounts, `:52-54`). Differences: the DeepSeek proxy alone sets `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS=20` (`:314`) — the header explains the Qwen proxy "deliberately leaves stream-idle timeout disabled so long-context prefills are not terminated" (`:22-23`); the Qwen proxy load-balances two replicas via `VLLM_BACKEND_URLS` (`:367`) where the others use `VLLM_BASE_URL` (`:324`, `:410`); the GLM proxy omits `VLLM_PROXY_MAX_REQUEST_SIZE` (present at `:325`, `:368`). All differences are availability/routing knobs, none touches logging or content.

---

### INFO (deployed: ON) — privileges, mounts, volumes, network, and the registrar: structural in-box exposure, no new plaintext reader

- **`cap_add: [SYS_NICE]`** on the Qwen anchor only (`:115-116`; priority item 6): grants raising scheduler priority / setting real-time class and CPU affinity for its own processes. It does **not** grant `SYS_PTRACE`-style memory access to other processes, host filesystem access, or any read of another container's plaintext — a performance knob, not a new reader. Neither the DeepSeek nor GLM engine has any `cap_add`; dcgm has `SYS_ADMIN` (`:229-230`) + nvidia runtime as on all sibling recipes — privileged, kept in scope for its privilege, but digest-pinned here (see pinning INFO).
- **`ipc: host` node-wide** (`:38`, inherited via `x-nvidia` by every engine and all three proxies): one shared IPC namespace across three model families' plaintext — cross-model shared-memory visibility, permitted though not configured.
- **Volumes** (`:777-783`; priority item 8) — all named volumes, all persisting across container recreation: `huggingface_cache` (weights, pinned revisions), `kernel_cache` → `/root/.cache/deep_gemm` (`:95`, compile artifacts), `qwen38_kernel_cache` → `/root/.cache/sglang` (`:143`, compile artifacts), `compile_cache_glm51` → the GLM container's whole `/root/.cache` (`:594` — torchinductor + triton per `:223-224`; config-keyed compile artifacts, and the in-file header calls it the "persistent compile cache", `:26-27`), `certs` (external, TLS material, `:783-785`), and `otelcol_app_storage` (`:758`, `:778`) — the one volume that can hold **request-derived** data: it persists the exporter's send-queue, so it contains whatever log records were kept for export (content-free at deployed flags; content-bearing exactly when a print fires — see the HIGH). **No KV/prompt/request disk tier, no responses store, no dump/profiler directory is configured anywhere in this file.**
- **Network**: single flat `external: true` bridge `dstack_default` (`:772-775`) — no `internal: true`, no egress allowlist: **every plaintext-handling container can open arbitrary outbound connections [MEDIUM]**, which is also what makes the Qwen media fetch uncontainable at this layer.
- **Named outbound destinations, exhaustively:** proxies → `https://cloud-api.near.ai` (literal, `:315`, `:358`, `:401`) + `http://compose-manager:8080` (host-gateway, `:317`, `:360`, `:403`) + `${WEB_CONTEXT_SEARCH_URL}` (unset default); registrar → `https://completions.near.ai` only (`:1283`), registering the three model domains (`:1447-1449`); engines → HF hub on cache miss (pinned revisions) and the Qwen media fetch (MEDIUM above); otelcol-contrib → `https://telemetry.infra.near.ai` (`:1239`); downloader → HF hub (pinned) and `ghcr.io` (its `uvx` image is digest-pinned, `:156`, and `uvx --from 'huggingface_hub[hf_xet]'` (`:242-248`) resolves that package from PyPI at run time — mutable-input tooling, but it runs in the throwaway downloader container, touches only the weights cache, and exits; the engines never execute it).
- **Registrar health probe**: `check_chat` POSTs a **synthetic** `{"role":"user","content":"hi"}` with `max_tokens:1` (`:1353-1359`) — not user content; its logs print only status codes and endpoint names (`:1365`, `:1369`, `:1380`, `:1396`, `:1433`, `:1439`).

---

## Verdict

**QUALIFIED PASS at this layer.** The file hands no logging switch to any engine or proxy, pins every model revision beside every `--trust-remote-code` (first file in this sibling group to do so completely), forces OHTTP on all three proxies, digest-pins every image including dcgm and the in-enclave build's base, buffers no plaintext to disk at any hop, and — checked explicitly — both `watchdog_timeout:1800` labels match deployed flags and the in-enclave GLM build resolves no mutable input. The qualifications, in descending order of cost to the end-to-end claim:

1. **Harness-C3, confirmed here end to end** — everything any of these services prints leaves the TEE via the recipe's own otelcol-contrib to `telemetry.infra.near.ai`, unredacted, with the send-queue persisted to a volume; the posture is conditional on per-image print discipline across three engine codebases and two proxy builds (HIGH, deployed ON).
2. **Both Qwen3.8 replicas are multimodal request-driven media fetchers with no allowlist and no in-file containment**, on a network with no egress control (MEDIUM, deployed ON) — and the same two replicas run the undisableable hang-dump watchdog at the 300 s default while their siblings run 1800 s (LOW, ARMED).
3. The unauthenticated engine `/configure_logging` (CRITICAL on the engine pages) is reachable from the flat network through the allowlist-free `location /` nginx/proxies (MEDIUM, deployed ON).
4. The `/compose/up` splice can arm `DSV4_RETURN_HIDDEN_STATES` (`:+` footgun) and supply `WEB_CONTEXT_SEARCH_URL`/`FUSION_*` to the E2EE terminators with no measurement change (MEDIUM, deployed OFF).

Recommend, in-file: pass `--watchdog-timeout 1800` (or an explicit larger value) on the Qwen command to match its siblings; front the engines with a path allowlist; add a redaction/scrub processor to the otelcol `logs/app` pipeline; bind the OTLP receivers to localhost or drop the unused pipelines; add a network egress allowlist (also the only available containment for the sglang media fetch short of `--disable-multimodal`); and drop the unused `DeepSeek-V4-Flash@553034d7` download once rollback is no longer needed.

## Not traced (load-bearing)

- Whether this `file_sha256` matches the signed action log's current entry live (fetched at compose-manager `9b5ec2ec0d60` and hash-checked to disk; not re-fetched against the live log in this review — the fleet can redeploy mid-audit).
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source; the splice mechanism itself is documented on sibling pages and inferred here from the interpolation forms, not re-verified against compose-manager at this commit).
- Engine print discipline and exact `/configure_logging` / watchdog / crash-path behavior at the configured levels — deferred to the engine pages ([`ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md), [`febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md), [`aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md)), which are load-bearing under the telemetry finding. Note the Qwen engine identity is a self-asserted **nightly dev build** (its page's own caveat).
- Proxy catch-all forwarding, web-search fail-closed behavior, and fusion gating — deferred to the two proxy pages ([`b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md), [`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md)).
- What `--enable-return-hidden-states` exposes end-to-end if spliced on (inference-proxy + cloud-api source, outside this repo's scope).
- The measured management harness for the three hosts (`glm-5`, `glm-5-1`, `qwen3-8-27b`): compose-manager, datadog-agent presence per node, and the measured `allowed_envs` — referenced from the measured layer, not re-derived here; whether a datadog agent additionally ships these containers' stdout to a third-party SaaS is a per-node fact of that layer.
- The PyPI-resolved `huggingface_hub[hf_xet]` the downloader executes (`:242-248`) — mutable input in a non-plaintext, exit-after-download container; its bytes were not audited.
- EAGLE speculative-decoding internals (draft tree handling) beyond the flag surface — deferred to the engine/base pages.
