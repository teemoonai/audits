# cvm-compose-files prod/dsv4-qwen38-glm51.yaml @ c27ed3b3... (three-model inner compose, GPU02 capacity pack — dsv4 prefill-chunk tuning + serialized otel export; delta over 46edfb72)

## verdict: QUALIFIED PASS on logging config — delta review against the audited 46edfb72 revision: the whole diff is two config-tuning changes and their metadata, verified against the base bytes — the dsv4 anchor's --chunked-prefill-size drops 8192 to 4096 (a scheduler batching flag naming no log, dump, or debug behavior) with the chunk_size telemetry labels updated to match (labels stay truthful to the deployed command; the in-file prose comment now stale at 8192), and num_consumers: 1 is added to the otelcol otlphttp sending_queue, which serializes export draining only — no receiver, filter, processor, destination, or collection change anywhere in the diff; every base finding carries unchanged and none retires — no content-logging switch anywhere, all three engines pin --revision beside --trust-remote-code, OHTTP forced on all three proxies, watchdog labels truthful; stdout of every plaintext service still ships off-box unredacted, both Qwen3.8 replicas remain unguarded multimodal media fetchers on the 300 s watchdog default, the unauthenticated engine admin surface remains behind allowlist-free nginx and proxies on the flat network, and the /compose/up env-splice can still arm hidden-state passthrough and web-search egress with no attestation-visible change

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/dsv4-qwen38-glm51.yaml` |
| **file_sha256** | `c27ed3b357f44673e4a84ef89f4625e1009de619d83ffdaae27c1265f676f4bb` (re-verified locally: `git show 1a7d6e548b19:prod/dsv4-qwen38-glm51.yaml` from a full clone hashes to this value, and `shasum -a 256` of the captured 1530-line file matches) |
| **role** | log-pinned inner compose for the GPU02 three-model capacity pack: `deepseek-ai/DeepSeek-V4-Flash-0731` (SGLang FP4 TP2, GPUs 0-1), `Qwen/Qwen3.8-27B-FP8` (SGLang TP1, two replicas, GPUs 2 and 3), `QuantTrio/GLM-5.1-AWQ` served as `zai-org/GLM-5.1-FP8` (SGLang AWQ TP4 + EAGLE, GPUs 4-7, in-enclave patched build) — behind three `vllm-proxy-rs` instances, nginx, registrar, three dcgm exporters, otelcol-contrib. Live on 4 fleet hosts per the orchestrator's fleet capture of 2026-08-21: `dsv4-flash`, `glm-5`, `glm-5-1`, `qwen3-8-27b` |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. Pinned by the action log at `nearai/cvm-compose-files` commit `1a7d6e548b1914dc38206d00da37898bde1b0a13` (2026-08-18, merge of PR #173); the orchestrator fetched it at that commit and hash-verified it, and this review independently re-derived the bytes from a full clone and re-hashed both revisions. The action-log match was not re-fetched live in this review |
| **lineage** | supersedes [`46edfb72`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-46edfb72e0d15618429cedde9fb527bc4d4c4c4073eac08099e854b43c44bc96.md) (reviewed 2026-08-17, QUALIFIED PASS on logging config — the full review of this recipe) |
| **review** | reviewed 2026-08-21 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review, base = the [`46edfb72` page](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-46edfb72e0d15618429cedde9fb527bc4d4c4c4073eac08099e854b43c44bc96.md) (the FULL review of this recipe path). The diff was taken **against the actual base bytes**, not the base page: both revisions were extracted from a full clone (`git show 68f0871e0c88:…` → `46edfb72…`, `git show 1a7d6e548b19:…` → `c27ed3b3…`, both hashes re-verified locally) and `git diff 68f0871e0c88 1a7d6e548b19 -- prod/dsv4-qwen38-glm51.yaml` was independently regenerated and matched **byte-for-byte** against the orchestrator's captured diff: 1529 → 1530 lines, 6 insertions / 5 deletions across five hunks, nothing else. The net change is two content commits merged as PR #173 (`ca543aa` "Stop gpu02 DeepSeek-V4-Flash crashes in FlashMLA sparse prefill", `33f0f80` "fix(otel): preserve model metric request order"). The single inserted line sits at `:1243`, so base citations below that point are position-identical and those at or after it shift +1 — but every citation on this page was re-verified against this file's bytes, not shifted arithmetically. Every carried finding below is re-cited at this file's line numbers; every "unchanged" claim is a `diff` fact, not an assumption.

---

### INFO (deployed: ON) — the delta: dsv4 prefill chunk halved with truthful label updates, and otel export serialized; no logging, collection, or destination change — sink: none new

The five hunks, each read in place:

1. **`--chunked-prefill-size 8192` → `4096`** on the dsv4 command anchor (`:78`). A scheduler batching/perf flag: it sits in the tuning cluster between `--enable-mixed-chunk` (`:77`) and `--max-prefill-tokens 16384` (`:79`), and names no log, dump, debug, or persistence behavior — it changes how many prompt tokens the engine schedules per prefill chunk in GPU memory. The commit message frames it as a FlashMLA sparse-prefill crash mitigation — if accurate, mildly privacy-positive, since the engines' stall/crash paths (watchdog dump class) are the known ARMED content writers. The Qwen anchor's own `--chunked-prefill-size 8192` (`:128`) is untouched; `chunked-prefill` appears exactly twice in the file (grep-verified).
2. **Matching label updates, DeepSeek service only**: `chunk_size:4096` in the `com.datadoghq.ad.logs` / `ad.instances` tag strings (`:452`, `:453`), `nearai.otel.chunk_size: "4096"` (`:475`), and the otelcol Prometheus static label (`:922`). Same metadata-truthfulness story the base page told, still true after the change: every asserted `chunk_size` matches its deployed command — DeepSeek `4096` (`:452`, `:453`, `:475`, `:922` ↔ `:78`), both Qwen replicas `8192` (`:526`, `:574`, `:957`, `:989` ↔ `:128`). These are static config labels — metadata, not content. **One drift the diff introduces:** the in-file prose header still describes the DeepSeek configuration as "8192-token mixed prefill" (`:11`) — a stale comment, consumed by no telemetry (the `:18` "8192-token mixed" is the Qwen paragraph and remains correct). Cosmetic; noted for the record.
3. **`num_consumers: 1` added to the otlphttp exporter's `sending_queue`** (`:1243`, in `sending_queue:` `:1242-1245`). This is otelcol exporterhelper queue config: the number of consumers draining the already-filtered send queue to the endpoint. It serializes export (consistent with the PR title "serialize-otel-source-queues" and commit "preserve model metric request order") and changes **export concurrency only** — the endpoint stays the literal `https://telemetry.infra.near.ai` (`:1239`, bearer `:1241`), and no receiver (`:794-801`), operator (`:827-838`), filter (`:1196-1205`), processor chain (`:1257`), or pipeline membership (`:1255-1274`) changes. What is collected and where it goes are both unchanged. Second-order note folded into the carried HIGH below: with a single consumer, records linger longer in the disk-persisted queue under backpressure (`queue_size: 128` `:1244`, `storage: file_storage` `:1245`, directory `:1249` on the `otelcol_app_storage` volume `:758`/`:778`) — a residence-time change on an already-recorded persistence, not a new sink.

**Nothing else changed — a diff fact:** no command flag other than `:78`, no environment block, no engine/proxy/nginx/registrar/downloader definition, the in-enclave GLM build (`:161-188`), networks (`:772-775`), volumes (`:777-785`), and the entire otel receiver/processor/filter config are byte-identical to the audited base. No base finding retires; no new finding opens.

---

## Env reachability (carried, re-verified at this file's lines)

Every logging knob is still a literal: `--log-requests-level 0` on all three SGLang commands (`:90`, `:140`, `:201` — the minimum), `VLLM_LOGGING_LEVEL=INFO` (`:99`, `:147`), `LOG_FORMAT=json` on all three proxies (`:318`, `:361`, `:404`), `NCCL_DEBUG=WARN` (`:102`, `:150`, `:221`), otelcol `telemetry.logs.level: info` (`:1279`). **`OHTTP_ENABLED=true` is a literal on all three proxies** (`:320`, `:363`, `:406`) — E2EE posture is not operator-degradable. **No `--api-key`/`--admin-api-key` anywhere** (grep-verified; the only `API_KEY` strings are the proxies' `WEB_CONTEXT_SEARCH_API_KEY`). Interpolated vars reachable only via the non-measured `/compose/up` splice, unchanged: `DSV4_RETURN_HIDDEN_STATES` (`:92`), `HF_HUB_OFFLINE` defaulted `0` (`:98`, `:146`, `:218`), `HUGGING_FACE_HUB_TOKEN` (`:97`, `:145`, `:217`, `:253`), `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` and `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` on all three proxies (`:328-:329`/`:322-:323`, `:371-:372`/`:365-:366`, `:413-:414`/`:408-:409`), `CLOUD_API_USAGE_TOKEN` (`:316`, `:359`, `:402`), `PROXY_TOKEN`/`MODEL_PROXY_TOKEN` (`:299-:300`), `MONITORING_INGEST_TOKEN` (`:747`). None is a log-level or content-logging switch.

---

### HIGH (deployed: ON) — every plaintext service's stdout leaves the CVM via the recipe's own otelcol-contrib, unredacted, to near.ai's telemetry gateway — sink: telemetry / egress

Carried from the base page; chain re-verified in this file. Every app service logs via the `json-file` driver (`x-logging-conf`, `:29-34`) which exports the `com.datadoghq.ad.logs` label onto each record (`:34`); the recipe's own digest-pinned otelcol-contrib (`:740-741`) mounts `/var/lib/docker/containers:ro` (`:757`) and tails `*-json.log` (`:799`); the `regex_parser` operators lift `model:` (`:827-830`) and `deployment:` (`:835-838`) out of the label; `filter/app_logs` (`:1196-1205`) drops only records with neither attribute (`:1200`) or from five named management services (`:1201-1205`) — and every service in this file carries a `model:` + `deployment:` pair (engines `:452`, `:503`, `:551`, `:610`; proxies `:331`, `:374`, `:416`; nginx/registrar/downloader/dcgm via `model:multi` or per-model tags `:281`, `:307`, `:255`, `:653`, `:686`, `:720`), so all app stdout is kept and exported verbatim by `logs/app` (`:1255-1258`) to `https://telemetry.infra.near.ai` (`:1239`, bearer `:1241`). **No scrub/hash/truncate processor exists in the pipeline** (`:1257`), so "no content leaves" still reduces to per-image print discipline (engine and proxy pages, unchanged builds), with the engines' `/configure_logging` and the watchdog dumps as the known switch-on paths. This revision's only touch on the pipeline is `num_consumers: 1` (`:1243`): export is serialized, and kept records now drain single-file through the disk-persisted queue (`:1244-1245`, `:1249`, volume `:758`/`:778`) — under backpressure a content-bearing record (if a print ever fires) sits on that persistent volume longer awaiting export. The OTLP receivers on `0.0.0.0:4317/4318` (`:794`, `:796`; `expose`d `:753-755`) still feed `metrics/otlp` (`:1263-1266`) and `traces` (`:1271-1274`) verbatim off-box with no content filter [MEDIUM, carried]. All metrics paths remain Prometheus scrapes with the static config-label taxonomy — metadata, not content [INFO, carried].

### MEDIUM (deployed: ON) — both Qwen3.8-27B replicas fetch request-supplied media URLs with no domain allowlist, and this file adds no containment — sink: egress (request-driven)

Carried unchanged — the diff does not touch the Qwen anchor (`:112-153`) or either replica (`:487`, `:535`). `--mm-attention-backend fa3` (`:127`) keeps multimodal live (the header's qualification soak covers vision, `:20-21`); the [engine page `febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md) rates the allowlist-free media fetch MEDIUM (deployed ON). No `--disable-multimodal`, no media-guard flag, no egress allowlist anywhere in this file; the one partial mitigation stays: `--revision 017b9c7a…` (`:122`) pins the config that makes the model multimodal.

### MEDIUM (deployed: ON) — the engines' unauthenticated admin surface is reachable through allowlist-free nginx and proxies on the flat external network — sink: log / egress

Carried unchanged. Flat `external: true` bridge `dstack_default` (`:772-775`); no `--api-key` anywhere (grep-verified); nginx (digest-pinned `:258`) publishes six host ports (`:265-271`, three plaintext HTTP) and all six server blocks route with a bare `location /` (`:1488`, `:1493`, `:1498`, `:1511`, `:1520`, `:1529`) — so the engines' unauthenticated `/configure_logging` (CRITICAL on all three engine pages) remains one POST away laterally and, absent path filtering, from the front door. In-file mitigations hold: upstreams are always proxies; `proxy_buffering off; proxy_cache off` (`:1480-1481`); the access log uses `$uri`, no body variable, though `request_id`/`org_id`/`workspace_id` (`:1459-1461`) remain per-request user-attributable metadata on the shipped stream [INFO].

### MEDIUM (deployed: OFF, splice-armed) — the /compose/up env-splice can arm hidden-state passthrough and hand the E2EE terminators an external search URL with no attestation-visible change — sink: response / egress

Carried unchanged. `${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}` (`:92`) — the `:+` form arms on **any** non-empty value; deployed OFF. `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (`:328-:329`, `:371-:372`, `:413-:414`) and `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (`:322-:323`, `:365-:366`, `:408-:409`) remain operator-suppliable to the E2EE-terminating proxies via the non-measured splice; both deployed proxy builds were source-verified fail-closed when unset (proxy pages, digests unchanged at `:46`, `:354`, `:397`).

### LOW (deployed: ARMED) — both Qwen3.8 replicas run the undisableable watchdog hang-dump at the 300 s default, while DeepSeek and GLM run at 1800 s — sink: log (feeding the telemetry egress)

Carried unchanged. The Qwen command (`:118-140`) still passes no `--watchdog-timeout` (300 s default at the deployed engine identity, per the engine page); DeepSeek passes `--watchdog-timeout 1800` (`:81`) and GLM `1800` (`:209`). **Label truthfulness re-checked at this revision:** `watchdog_timeout:1800` is asserted only by DeepSeek (`:452`, `:453`; `nearai.otel.watchdog_timeout` `:479`; Prometheus `:926`) and GLM (`:610`, `:611`; `:633`; `:1021`), both of which deploy the flag — truthful; neither Qwen replica's labels assert any watchdog value (`:503`, `:551` — checked). The dsv4 prefill-chunk halving does not change the dump's shape or arming, only (per its commit message) the likelihood of the crash class that precedes it.

### INFO (deployed: ON) — headline base properties re-confirmed at this revision: complete revision pinning beside --trust-remote-code, OHTTP literals, the fully-pinned in-enclave GLM build, audited proxy digests, and unchanged privileges/volumes/egress

Priority re-checks, all confirmed against this file's bytes: **(a)** every `--trust-remote-code` still pairs with a `--revision` pin — DeepSeek `:65`/`:67`, Qwen `:120`/`:122`, GLM `:214`/`:193` — with downloads at the same pins (`:244`, `:246`, `:248`; the unserved rollback download `:242` persists, still pinned dead weight); **(b)** `OHTTP_ENABLED=true` literal on all three proxies (`:320`, `:363`, `:406`); **(c)** the in-enclave GLM build is byte-identical: digest-pinned `FROM` (`:166`), the fail-loud sglang#28305 patch (`:161-163` comment, assert `:185`), no mutable input, serving via `image: glm51-sgl-awq-tp4-patched:local` (`:589`, build `:590`, service `:583`, whole-`/root/.cache` compile volume `:594`); **(d)** proxy digests unchanged — anchor `b0c5cd07` (`:46`), Qwen and GLM override `b183677a` (`:354`, `:397` with the preserve-digest comment `:395-396`) — both audited in-repo; **(e)** privileges/mounts unchanged: proxies `user: root`/`privileged: true` (`:47-48`), `ipc: host` node-wide (`:36-38`), Qwen `SYS_NICE` only (`:115-116`), dcgm digest-pinned with `SYS_ADMIN` (`:227`, `:229-230`); volumes unchanged (`:777-785` — `otelcol_app_storage` remains the one request-derived-capable volume); registrar probes with a synthetic `"hi"` (`:1360`) and registers the three model domains (`:1448-1450`) against `https://completions.near.ai` (`:1284`). No KV/prompt/request disk tier, no dump/profiler directory anywhere (grep-verified).

---

## Verdict

**QUALIFIED PASS at this layer — a config-tuning delta that changes nothing about what is collected, logged, or exported.** The diff is exactly the two changes and their metadata: a scheduler batching flag halved on the dsv4 anchor with its telemetry labels truthfully updated (one prose comment now stale at `:11`), and otel export serialized via `num_consumers: 1` — export concurrency only, same endpoint, same filter, same collection. Every base finding carries at unchanged severity and deployed state; none retires; nothing new opens. The qualifications remain the base page's, in the same order of cost: **(1)** all plaintext-service stdout leaves the TEE unredacted via the recipe's own otelcol to `telemetry.infra.near.ai`, send-queue persisted to a volume — now drained single-file (HIGH, ON); **(2)** both Qwen3.8 replicas remain unguarded multimodal media fetchers on an egress-unrestricted network, with the watchdog hang-dump armed at the 300 s default (MEDIUM ON + LOW ARMED); **(3)** the unauthenticated engine `/configure_logging` stays reachable through six bare `location /` listeners and allowlist-free proxies (MEDIUM, ON); **(4)** the `/compose/up` splice can still arm `DSV4_RETURN_HIDDEN_STATES` and supply `WEB_CONTEXT_SEARCH_URL`/`FUSION_*` with no measurement change (MEDIUM, OFF). The base page's recommendations all still apply verbatim (Qwen `--watchdog-timeout`, path allowlist, scrub processor, localhost OTLP, egress allowlist, drop the rollback download); add one: update the `:11` header comment to 4096 so prose and deployed command agree.

## Not traced (load-bearing)

- Whether this `file_sha256` matches the signed action log's current entry live (the orchestrator's fetch at commit `1a7d6e548b19` was hash-verified and independently re-derived from a clone here; the log itself was not re-fetched in this review — the fleet can redeploy mid-audit).
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source; carried from base).
- Engine print discipline, `/configure_logging`, watchdog dump shape, and the FlashMLA sparse-prefill crash behavior the commit message references — deferred to the engine pages ([`ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md), [`febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md), [`aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md)); whether `--chunked-prefill-size 4096` actually stops those crashes is an engine-behavior claim this manifest cannot establish.
- Proxy catch-all forwarding, web-search fail-closed behavior, and fusion gating — deferred to the proxy pages ([`b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md), [`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md)).
- What `--enable-return-hidden-states` exposes end-to-end if spliced on (inference-proxy + cloud-api source, outside this repo's scope).
- The measured management harness for the four hosts now answering on this recipe (`dsv4-flash` newly among them per the 2026-08-21 fleet capture): compose-manager, per-node datadog-agent presence (a second off-box stdout leg where present), and the measured `allowed_envs` — the measured layer's pages, not re-derived here.
- otelcol exporterhelper's exact `num_consumers` drain/retry semantics at the pinned collector digest (config read here names concurrency only; the collector's behavior under a wedged consumer — queue-full drops vs indefinite persistence — was not source-traced).
- The PyPI-resolved `huggingface_hub[hf_xet]` the downloader executes (`:242-248`) — carried from base, bytes unaudited.
- EAGLE speculative-decoding internals beyond the flag surface — deferred to the engine/base pages.
