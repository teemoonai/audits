# cvm-compose-files prod/GLM-5.1-DSV4-Migration.yaml @ 2db74d72... (temporary bridge recipe: one retained GLM-5.1 AWQ TP4 replica on GPUs 0-3 beside two gated DeepSeek-V4-Flash FP4 TP2 replicas on GPUs 4-5 / 6-7 — two model families, two proxies, one CVM)

## verdict: QUALIFIED PASS — full review of a new 1472-line bridge recipe whose every service block is byte-equivalent (commands, env, build, configs) to a block already audited in the GLM-5.1 AWQ TP4 or DSV4-Flash lineages, re-verified here line by line: all three engines run --log-requests-level 0 with pinned --revision on every --trust-remote-code, no dump/crash/trace/hicache flag anywhere, the in-enclave GLM build resolves only a digest-pinned base plus an inline fail-loud patch with no network fetch, OHTTP is a literal true on both E2EE terminators, dcgm is digest-pinned on both arms, and the two new migration helpers are profile-gated (the disk preflight is network-none, read-only, cap-dropped, and prints three byte counts); the qualifications are the fleet's, now doubled across two arms — every plaintext service's stdout leaves the CVM unredacted to near.ai's telemetry gateway through this file's own collector (and, per the measured harness, a second time to Datadog), three engines' unauthenticated /configure_logging sits behind four allowlist-free nginx servers and two catch-all proxies on the flat external network, both proxies carry splice-armed web-search/fusion env, both DSV4 argvs carry the env-shaped hidden-states flag (not in allowed_envs), and the watchdog hang-dump is armed at 1800 s on all three engines; one identity caveat is specific to this file: the signed action log shows the GLM engine container was created from the earlier GLM-5.1-SGL-AWQ-TP4 file at 3c5f8891, not from this file, and this page shows that block is configuration-equivalent

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/GLM-5.1-DSV4-Migration.yaml` |
| **file_sha256** | `2db74d72aea666868a778a4e5c3635ac8940cf0d53095c42d3282f60b115b8d7` (re-verified locally: `shasum -a 256` of the captured 1472-line file matches) |
| **role** | log-pinned inner compose for a **capacity migration**: `zai-org/GLM-5.1-FP8` (served alias; actual checkpoint `QuantTrio/GLM-5.1-AWQ`, `:24`, `:190-191`) as **one** SGLang TP4 replica on GPUs 0-3 behind `proxy-glm51`, plus `deepseek-ai/DeepSeek-V4-Flash` (checkpoint `DeepSeek-V4-Flash-0731`, `:387-388`) as **two** SGLang TP2 replicas on GPUs 4-5 / 6-7 behind `proxy-dsv4-flash`; one nginx with four server blocks, two registrars (the DSV4 one profile-gated), two dcgm exporters, one otelcol-contrib, two one-shot weight downloaders, and a profile-gated read-only disk preflight. The file's own header (`:1-4`): *"Temporary bridge … Drain/remove GLM5.1 r2 BEFORE applying this file. Start ds4f-migration-registrar explicitly only after qualification. Reuses GLM5.1 v0.0.344 and DS4F v0.0.385; do not whole-stack apply."* Live on **3 fleet hosts** that are one CVM: `dsv4-flash`, `glm-5`, `glm-5-1` |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. The three hosts' attestation reports carry byte-identical 142-entry action logs (verified equal); the last entry (index 141, `2026-09-09T23:45:39Z`) is `compose_up` of this file at commit `e14107901d202215ab44eb027eb4792047d5ec83` (tag `v0.0.409`) with `file_sha256 2db74d72…`, scoped to `['proxy-glm51', 'proxy-dsv4-flash']`. `git show e1410790:prod/GLM-5.1-DSV4-Migration.yaml` from a full clone reproduces the hash (verified). Every earlier log entry naming this file was mapped to a repo revision by hash: `e249f519` → `17de8058…` (= file at `5618473e`), `94e6e4fa` and `02254afa` → `84c002e0…` (= file at `53b9608f`), `1755bb7c` → `a74825ee…`, `e1410790` → `2db74d72…`. The CVM's measured harness is `2c650eae…` (read from the report's `event_log` `compose-hash` event) on guest OS `9b69bb16…` (`os-image-hash` event; `vm_config.image = dstack-nvidia-0.5.5`) |
| **lineage** | new file (first commit `26bc1975`, 2026-09-08 00:49Z; then `5618473e` second DSV4 replica, `53b9608f` disk preflight, `1755bb7c` proxy pin, `e1410790` proxy bump — each diff read). GLM arm inherits from [`GLM-5.1-SGL-AWQ-TP4 @ ae05e203`](/manifests/nearai/cvm-compose-files/prod/GLM-5.1-SGL-AWQ-TP4/sha256-ae05e203532562170e62e7ec7f9c7e271b1e5bfc3cdc9bd658d5dcc6c7814eb1.md) / [`3c5f8891`](/manifests/nearai/cvm-compose-files/prod/GLM-5.1-SGL-AWQ-TP4/sha256-3c5f8891d1e47a2762ae3172c0ce88899a11987df5c8531bfa0ce04f10881067.md); DSV4 arm inherits the engine image + command audited on the [`ec518148` engine page](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) and the proxy block shape of [`dsv4-flash-glm51 @ d085dcfc`](/manifests/nearai/cvm-compose-files/prod/dsv4-flash-glm51/sha256-d085dcfc4ee0fbb13b183d52cf3a140cbc075e5e3de44fc039df961a4913ca4c.md). Runs on measured harness [`2c650eae`](/manifests/measured/sha256-2c650eae81601afade86d1aa5ad898473b992081998026bb42ee1524ce99ee38.md), guest OS [`9b69bb16`](/os/sha256-9b69bb1698bacbb6985409a2c272bcb892e09cdcea63d5399c6768b67d3ff677.md) |
| **review** | reviewed 2026-09-10 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** the complete inner compose, all 1472 lines, read in slices: thirteen services (`:7-666`), networks and volumes (`:667-679`), and all four inlined configs — `otelcol_app_config` (`:681-1090`), `registrar_script` (`:1091-1229`), `nginx_conf` (`:1230-1333`), `ds4f_registrar_script` (`:1334-1472`, read in full). Because this is a `docker compose config`-style render (anchors resolved into `&1`/`&2`, decimal `mode:`), every inherited block was compared against the **resolved anchor values** of the audited base files, not their anchor names: the GLM engine's `build:`, `command:`, and `environment:` were diffed whitespace-normalized against `x-awq-build` / `x-awq-cmd` / `x-awq-model-env` of the `3c5f8891` bytes (extracted from the clone at `2390cb8c` and hash-verified) — **identical**; the otelcol receivers, filelog operators, processors, exporters, extensions, and pipelines were diffed against `ae05e203 :452-533` and `:616-730` — identical modulo YAML flow/block style; the registrar script differs from `ae05e203`'s by one echo string; the DSV4 engine command was compared flag-by-flag to the one the `ec518148` page audited. Per-image behaviour (what `/configure_logging` does, what the watchdog prints, what the proxy forwards) is **deferred to the linked image pages** and not re-derived here (method §4). The **`prod/migration-gpu-preflight.yaml`** document that the same action log shows brought up and down on 2026-09-08 (commit `8e1e0a4e`, `file_sha256 62b2bc14…` — hash reproduced from the clone, 162 lines, `network_mode: none`, `read_only`, `cap_drop: [ALL]` + `cap_add: [SYSLOG]`, profile-gated) is a **separate attested document this page does not cover**; its last log action is `compose_down` (entry 138).

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, full review)

> Method: [`/notes/method.md`](/notes/method.md) (§4 Manifest-only checks)

**Why this document is the load-bearing one.** All three engine builds it deploys return PRIVATE at deployed flags on their own pages, each with the same two residuals the flags do not close: an **unauthenticated `/configure_logging`** that turns on full prompt+output logging and arbitrary-path request dumping at runtime (CRITICAL on the [`aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md) base of the GLM build and on the [`ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) DSV4 build), and a **watchdog hang-dump** of every in-flight request's prompt and output token ids to stdout that no flag disables (HIGH ARMED on both). Neither is a finding about this file — but who can reach port 8000 on which engine, and where stdout goes if a switch is flipped, is entirely this file. The E2EE terminator on both arms is [`vllm-proxy-rs @ 98b57ad7`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) (QUALIFIED PASS: opt-in web-search/fusion egress armed, no engine path allowlist).

```text
MANIFEST leg of a defensive privacy audit (authorized). Others audit source; you audit
the attested inner compose that defines the plaintext environment.
MATERIAL: prod/GLM-5.1-DSV4-Migration.yaml, file_sha256 2db74d72... (log-pinned at
e1410790 / v0.0.409, NOT hardware-measured), serving zai-org/GLM-5.1-FP8 AND
deepseek-ai/DeepSeek-V4-Flash on one CVM (hosts dsv4-flash, glm-5, glm-5-1).
CHECKS: (0) the action log - which revision created which container, since near.ai
deploys service-scoped. (1) per-arm engine flags vs the image pages. (2) the in-enclave
build - anything mutable or fetched. (3) telemetry - receivers, scrape targets, log
tailing, exporters, destinations, scrubbing. (4) network - published ports, engine
reachability on dstack_default, nginx allowlists, both registrars end to end. (5) the
two migration helpers. (6) privileges, mounts, shared volumes. (7) both proxies' env:
splice-armed egress, OHTTP, backend pools and affinity. (8) siblings and pinning.
OUTPUT: per-check results with exact evidence at THIS file's lines; findings as
SEVERITY (deployed: state) headings naming the sink; untraced items.
```

## Check 0 — Identity and the action log: which revision created which container

**Deployment nuance, stated up front.** near.ai deploys with **service-scoped** `compose up`: the `file_sha256` a host attests is the last file *touched*, not necessarily the file every container was *created from*. This page reviews the file as the declared configuration — what the attestation surface presents and what this page is keyed to — and reports from the log which revision last touched each service. The last 28 entries of the 142-entry log (identical on all three hosts), read in full:

| # | time (UTC) | action | file @ commit → file_sha256 | services |
|---|---|---|---|---|
| 114 | 08-16 00:45 | up | `GLM-5.1-SGL-AWQ-TP4.yaml` @ `2390cb8c` → `3c5f8891` | *(whole stack)* |
| 115 | 08-20 20:54 | up | same @ `1a7d6e54` → `ae05e203` | `otelcol-contrib` |
| 116-118 | 09-08 01:45-01:55 | down / up / down | same @ `e249f519` → `a5b16f35` | `model-proxy-registrar` down; **`proxy-glm51` up**; `model-sg-glm51-awq-tp4-r2` down |
| 119 | 09-08 02:03 | up | **this file** @ `e249f519` → `17de8058` | `model-proxy-registrar` |
| 120-131 | 09-08 02:23-03:31 | up ×10, stage, down ×2 | this file @ `94e6e4fa` → `84c002e0` | `migration-disk-preflight` (120, 123), `model-downloader-dsv4` (121), `model-sg-dsv4-flash-fp4-tp2-r1` (stage 122, up 124), `-r2` (125), `proxy-dsv4-flash`+`dcgm-dsv4-flash`+`dcgm-glm51`+`otelcol-contrib` (126), otelcol down/up (127-128), registrar down (129), `nginx` (130), `model-proxy-registrar` (131) |
| 132-138 | 09-08 04:53-06:14 | up ×4 / down ×2 | `migration-gpu-preflight.yaml` @ `8e1e0a4e` → `62b2bc14` | `migration-gpu-preflight` (last action: **down**) |
| 134 | 09-08 04:58 | up | this file @ `94e6e4fa` → `84c002e0` | `ds4f-migration-registrar` |
| 139 | 09-08 18:30 | up | this file @ `02254afa` → `84c002e0` | `dcgm-dsv4-flash` |
| 140 | 09-09 20:47 | up | this file @ `1755bb7c` → `a74825ee` | `proxy-dsv4-flash`, `proxy-glm51` |
| 141 | 09-09 23:45 | up | this file @ `e1410790` → **`2db74d72`** | `proxy-glm51`, `proxy-dsv4-flash` |

What the log establishes, and what the diffs then close:

- **Every service except the two proxies and the GLM engine was last created from `84c002e0`** (`53b9608f`), and `git diff 53b9608f e1410790` on this file is **exactly two lines**: `proxy-glm51`'s image `b183677a…` → `98b57ad7…` (`:104`) and `proxy-dsv4-flash`'s image `b0c5cd07…` → `98b57ad7…` (`:316`) — via the intermediate `a74825ee` (`1755bb7c`), which pinned both to [`27265f5a…`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-27265f5a10abcacc3fe80604a00ed2ba32c6e082895115e4b652b40525cb554d.md) for one day (entry 140); each of the three step diffs was read and touches only those two image lines. So for nginx, both dcgm, otelcol, both registrars, both DSV4 engines, the DSV4 downloader, and the preflight, the bytes reviewed here **are** the bytes those containers were created from. The two proxies were recreated at entry 141 from this exact revision.
- **The GLM engine `model-sg-glm51-awq-tp4-r1` and its `model-downloader` were never brought up from this file.** They date from the whole-stack up at entry 114 (`GLM-5.1-SGL-AWQ-TP4.yaml @ 3c5f8891`, [audited](/manifests/nearai/cvm-compose-files/prod/GLM-5.1-SGL-AWQ-TP4/sha256-3c5f8891d1e47a2762ae3172c0ce88899a11987df5c8531bfa0ce04f10881067.md)); nothing in this file `depends_on` that engine (the only `depends_on` edges are nginx → both proxies `:54-56`, and each engine → its downloader `:159-161`, `:423-425`, `:517-519`), so no service-scoped up could have pulled it in as a dependency. The file's own header says as much (`:4`: "Reuses GLM5.1 v0.0.344"). I therefore diffed this file's block (`:154-242`) against the **resolved** `3c5f8891` anchors: `build.dockerfile_inline` (`:164-187` vs `x-awq-build`) **identical**; `command` (`:190-198` vs `x-awq-cmd`) **identical**; `environment` (`:204-211` vs `x-awq-model-env`) **identical**; volumes (`compile_cache_a:/root/.cache`, `huggingface_cache:/root/.cache/huggingface`, `:201-202`) identical; GPUs 0-3 (`:215-220`) identical. The only difference is in **labels**: `3c5f8891` carries `com.datadoghq.ad.check_names` / `ad.init_configs` / `ad.instances` (a Datadog openmetrics autodiscovery scrape of `:8000/metrics`) and this file does not (0 occurrences of `ad.instances` here vs 3 in `3c5f8891`/`ae05e203`, 5 in `d085dcfc`). Consequence: the running GLM engine container likely still carries the Datadog metrics-scrape label; metrics only, no content — INFO below.
- **`proxy-glm51` passed through an unaudited revision** (`GLM-5.1-SGL-AWQ-TP4.yaml @ e249f519` → `a5b16f35…`, entry 117 — no page exists for that hash) but was recreated twice since from this file (entries 140, 141), so its running configuration is `:103-153` of this file.
- The `migration-gpu-preflight.yaml` document (entries 132-138) is a separate attested file; this page does not review it beyond confirming its hash reproduces and its last action is `compose_down`.

## Check 1 — Per-service engine flags, per arm

**GLM arm — `model-sg-glm51-awq-tp4-r1` (`:154-242`), image `glm51-sgl-awq-tp4-patched:local` (`:162`), verbatim command `:190-198`:**

```text
sglang serve --model-path QuantTrio/GLM-5.1-AWQ --revision 8f60817aa28023f2607850d1a1e51d21aa34817a
--served-model-name zai-org/GLM-5.1-FP8 --tp 4 --dist-init-addr 127.0.0.1:29500
--disable-shared-experts-fusion --kv-cache-dtype fp8_e4m3 --reasoning-parser glm45
--tool-call-parser glm47 --log-requests-level 0 --mem-fraction-static 0.85
--max-running-requests 128 --max-queued-requests 16 --speculative-algorithm EAGLE
--speculative-num-steps 3 --speculative-eagle-topk 1 --speculative-num-draft-tokens 4
--watchdog-timeout 1800 --port 8000 --host 0.0.0.0 --enable-cache-report --enable-metrics
--trust-remote-code
```

100% literal (no `${VAR}`); `--revision` pinned and equal to the downloader's pin (`:24`); `--log-requests-level 0` (`:193`); `--watchdog-timeout 1800` (`:196`). Absent, grep-verified across the whole file: `--api-key`, `--admin-api-key`, `--log-requests`, `--crash-dump-folder`, `--enable-trace`, `--enable-hierarchical-cache`, `--disable-radix-cache`, `--debug-tensor-dump-*`, `--soft-watchdog-timeout`. Env (`:204-211`): `HF_TOKEN`, `HF_HUB_OFFLINE` (default 0), `NCCL_DEBUG=WARN`, `SGLANG_ENABLE_SPEC_V2=1`, inductor/triton cache dirs — no logging switch. This is the exact command the `ae05e203`/`3c5f8891` pages audited (diff-verified, Check 0).

**DSV4 arm — `model-sg-dsv4-flash-fp4-tp2-r1` (`:376-469`) and `-r2` (`:470-563`), image `docker.io/nearaidev/sglang@sha256:ec518148…` (`:385`, `:479`), verbatim command `:387-396` (= `:481-490`):**

```text
sglang serve --trust-remote-code --model-path deepseek-ai/DeepSeek-V4-Flash-0731
--revision 7872f01b1d1fe23eabc4c98b48bffcef5a386062 --tp-size 2 --kv-cache-dtype fp8_e4m3
--moe-runner-backend marlin --cuda-graph-max-bs 64 --mem-fraction-static 0.83
--max-running-requests 64 --max-queued-requests 16 --schedule-conservativeness 1.3
--num-continuous-decode-steps 1 --enable-mixed-chunk --chunked-prefill-size 4096
--max-prefill-tokens 16384 --swa-full-tokens-ratio 0.1 --watchdog-timeout 1800
--disable-custom-all-reduce --tool-call-parser deepseekv4 --reasoning-parser deepseek-v4
--json-model-override-args '{"dsv4_reasoning_effort_profile":"official"}'
--port 8000 --host 0.0.0.0 --enable-cache-report --enable-metrics --log-requests-level 0
--served-model-name deepseek-ai/DeepSeek-V4-Flash ${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}
```

The two replicas are identical except name, GPUs (4-5 vs 6-7, `:433-434` / `:527-528`), and `instance`/`gpu_pair` labels (full `diff` of the two blocks: 6 hunks, all naming). Compared flag-by-flag to the command the [`ec518148` page](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) audited (from `prod/qwen35-dsv4-flash.yaml @ d1cdff70`): **one difference — `--chunked-prefill-size 4096` here vs `8192` there.** That is a scheduling batch size (integer token count), privacy-neutral; every privacy-relevant flag and its absence list (`--api-key`, `--admin-api-key`, `--log-requests`, `--crash-dump-folder`, `--enable-trace`, `--enable-hierarchical-cache`, `--soft-watchdog-timeout`, `--debug-tensor-dump-output-folder`) is the same, and the env set (`:403-412`) is the same nine variables that page lists — no speculative decoding, no hicache, radix cache at default (in-GPU-memory, per replica; `SGLANG_ENABLE_UNIFIED_RADIX_TREE=1` at `:410`/`:504`). The `--revision 7872f01b…` pin equals the DSV4 downloader's pin (`:624`). **`--chunked-prefill-size 4096` is a delta vs the audited page's deployed command — stated, not hidden.**

The one **`${VAR}` inside an engine argv** — `${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}` (`:396`, `:490`) — is carried from the `d085dcfc` lineage, where it was rated MEDIUM as a structural precedent. Re-checked here against the harness this CVM actually measures: `DSV4_RETURN_HIDDEN_STATES` is **not** in the 22-entry model-node `allowed_envs` of [`2c650eae`](/manifests/measured/sha256-2c650eae81601afade86d1aa5ad898473b992081998026bb42ee1524ce99ee38.md) (Check 1 there: `BEARER_TOKEN, CM_MIN_TAG_AGE_HOURS, SLACK_WEBHOOK_URL, AWS_*, CERTBOT_EMAIL, CLOUDFLARE_API_TOKEN, DOCKER_REGISTRY_*, LAUNCHER_CHANNEL, DD_API_KEY, HUGGING_FACE_HUB_TOKEN, PROXY_TOKEN, ENV, HOST_IP, DD_HOSTNAME, HOSTNAME, MONITORING_INGEST_TOKEN, CVM_NAME, CVM_HOST`), so only the compose-manager `/compose/up` env overlay can set it. When set, per the engine trace it returns a client its own last-token hidden state over its own channel — no new sink.

### MEDIUM (deployed: OFF, splice-armed) — both DSV4 engine argvs are env-shaped: DSV4_RETURN_HIDDEN_STATES appends a flag to a plaintext process outside allowed_envs — sink: none when on (client-own channel); precedent for argv splicing

`:396` and `:490`. Bounded impact per the `ec518148` trace; the finding at this layer is that nothing in the file constrains the pattern, and the same splice channel that could set it could set anything the `:+` form is later pointed at. Carried severity from the `d085dcfc` page; not upgraded, not downgraded.

### LOW (deployed: ARMED) — the undisableable watchdog hang-dump is armed on all three engines at an explicit 1800 s fuse — sink: log (feeding both telemetry egress paths)

`--watchdog-timeout 1800` at `:196` (GLM), `:392-393` and `:486-487` (DSV4 r1/r2). Per both engine pages no value disables the dump; on a hang, every in-flight request's prompt and output token ids go to stdout, which Check 3 ships off-box. Three engines, one fuse each.

## Check 2 — The in-enclave build block

`model-sg-glm51-awq-tp4-r1` has `build.dockerfile_inline` (`:163-187`), and the image tag `glm51-sgl-awq-tp4-patched:local` (`:162`) is not a registry digest — the bytes that run are produced **inside the CVM at deploy time**. Read in full:

- `FROM lmsysorg/sglang:dev-cu12@sha256:aac6b242…` (`:165`) — **digest-pinned**; the tag is decoration. That base is [audited](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md).
- One `RUN python3 - <<'PYEOF' … PYEOF` (`:166-187`): opens **one** file (`deepseek_weight_loader.py`, `:169`), applies **one** anchored regex substitution with `count=1` (`:171-184`), `assert n == 1` with a fail-loud message (`:185`), writes it back (`:186`), prints a marker (`:187`).
- **No `pip install`, no `curl`, no `git`, no `apt`, no `ADD`/`COPY` from a URL** — grep-verified over `:163-187`. The build has **no mutable input**: its output is a deterministic function of the pinned base digest and the inline text, both of which are covered by this `file_sha256`.

This is the same verdict the `ae05e203` page reached for the same block (INFO there). The small-models recipe's HIGH ARMED rating for in-enclave builds was earned by `pip install vllm[audio]`-style mutable resolution; **that mechanism is absent here**, so the rating does not transfer. What does remain: the built image's bytes are never attested by digest anywhere — the assurance is the attested recipe, not a registry — and `docker compose up` on a service with `build:` rebuilds only when no `:local` image exists; a stale image from an earlier build with the same tag would be used silently. The GLM engine was built at entry 114 (Check 0) from the identical block.

### INFO (deployed: ON) — the in-enclave GLM build is fully pinned: digest-pinned FROM, an inline single-anchor fail-loud patch, no network fetch of any kind — sink: none

## Check 3 — Telemetry pipeline: receivers, scrape targets, exporters, destinations, scrubbing

`otelcol-contrib` (`:283-314`) is `otel/opentelemetry-collector-contrib@sha256:85ac41c2…` (`:284`, [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)), runs as `user: "0:0"` (`:288`), mounts **`/var/lib/docker/containers:ro`** (`:301`) and a durable `otelcol_app_storage` volume (`:302`), exposes (not publishes) `4317`/`4318` (`:297-299`), and loads `otelcol_app_config` (`:681-1090`). The pipeline, end to end at this file's lines — identical to the audited `ae05e203` pipeline (strict normalized diff of receivers/filelog `:682-763` vs `:452-533` and processors→pipelines `:944-1090` vs `:616-730`: zero semantic differences; the only delta is three added scrape jobs):

- **Log source:** `filelog/docker_containers` tails **every** container's json log (`include: /var/lib/docker/containers/*/*-json.log`, `:692-693`), `start_at: end`, persisted checkpoint (`:694-695`), then parses the docker JSON (`:698-708`), moves `log` to `body` (`:705-708`), and regex-extracts `model`, `model_path`, `precision`, `deployment`, `ip`, `port`, `env`, `host`, `instance`, `replica` from each container's `com.datadoghq.ad.logs` label (`:721-760`). Every plaintext-handling service in this file carries that label (`:139`, `:236`, `:367`, `:462`, `:556`) and the json-file log driver with `labels: com.datadoghq.ad.logs,com.docker.compose.service` (`:13-18` via `*1`; `:332-337`, `:415-420`, `:509-514`), which is what stamps the label into the log line's `attrs`.
- **Filter:** `filter/app_logs` (`:989-998`) **drops** records with neither `model` nor `deployment` attribute and records from `compose-manager`, `compose-manager-launcher`, `otelcol-management`, `otelcol-contrib`, `datadog-agent`. It is an **inclusion filter by label, not a content scrub** — engine and proxy lines pass *because* they are labelled. `transform/docker_metadata` (`:966-988`) only sets `service.name`/`log.source`/`container.name`. `groupbyattrs` (`:999-1016`) and `batch` (`:1017-1020`) reshape. **No processor inspects or redacts `body`.**
- **Exporter and destination:** one exporter, `otlphttp/gateway` → **`https://telemetry.infra.near.ai`** (`:1022-1023`), `Authorization: Bearer ${env:MONITORING_INGEST_TOKEN}` (`:1025`), `sending_queue` with `num_consumers: 1`, `queue_size: 128`, persisted to `file_storage` at `/var/lib/otelcol-contrib/storage` (`:1026-1033`) — i.e. **spooled log records, engine stdout included, sit on the `otelcol_app_storage` volume** until delivered. Pipelines (`:1037-1085`): `logs/app` (filelog → gateway), `metrics/app` (`prometheus/apps` → gateway), `metrics/otlp` and `traces` (the `0.0.0.0:4317/4318` OTLP receivers, `:685-690` → gateway, **no filter at all**), `metrics/self`.
- **Scrape targets** (`prometheus/apps`, `:764-935`, 15 s): `model-sg-glm51-awq-tp4-r1:8000` (`:772`), `dcgm-glm51:9400` (`:791`), `proxy-glm51:8000` (`:809`), `model-sg-dsv4-flash-fp4-tp2-r1:8000` (`:827`), `-r2:8000` (`:863`), `dcgm-dsv4-flash:9400` (`:899`), `proxy-dsv4-flash:8000` (`:920`), plus self on `localhost:8888` (`:943`). All `/metrics`. Counters, per the engine and proxy pages (the proxy page re-swept every metric label at `98b57ad7`: content-free).
- **Is there ONE off-box destination or more?** From **this file**: one — `telemetry.infra.near.ai`. No Datadog exporter, no `DD_*` endpoint, no second `otlphttp`. The `com.datadoghq.ad.logs` labels are consumed here by the collector's regex parsers, **but they are also autodiscovery hints for a Datadog agent**, and the measured harness page for this CVM ([`2c650eae`](/manifests/measured/sha256-2c650eae81601afade86d1aa5ad898473b992081998026bb42ee1524ce99ee38.md), Check 4) records that the harness runs a Datadog agent with `DD_LOGS_CONFIG_CONTAINER_COLLECT_ALL=true` over the same `/var/lib/docker/containers:ro`, shipping to `us3.datadoghq.com`, with `DD_PROCESS_AGENT_ENABLED=true` (process cmdlines). Cited, not re-reviewed. So at this CVM the answer is **two** destinations: near.ai's gateway (this file) and Datadog (the harness), with one in-box collector each.

### HIGH (deployed: ON) — every plaintext service's stdout on both arms leaves the CVM unredacted to near.ai's telemetry gateway through this file's own collector, spooled to disk on the way, and a second time through the harness's Datadog agent — sink: telemetry / disk / egress

Evidence: `:301`, `:692-693`, `:989-998` (no scrub), `:1022-1033`, `:1038-1049`; labels on all three engines and both proxies (`:139`, `:236`, `:367`, `:462`, `:556`). What this pipeline ships is decided by what the engines and proxies print: at deployed flags, nothing content-bearing (per their pages) — but the engine CRITICAL and the watchdog HIGH both print to exactly this stream, so the pipeline is the amplifier of every latent finding. The unfiltered OTLP `traces`/`metrics/otlp` pipelines (`:1059-1085`) bound to `0.0.0.0` (`:688`, `:690`) would forward verbatim any span a container on `dstack_default` ever sent; no service in this file emits OTLP, so this is armed-not-active. nginx's access log (`:1232-1245`) is `json_combined` over `$uri` (`:1238`) — no query string, no body — but it does carry `x-request-id`, `x-org-id`, `x-workspace-id` headers (`:1234-1236`) into the same stream: identifiers, not content.

## Check 4 — Network topology, nginx routing, and the two registrars

- **Network:** `default` is `external: true`, `name: dstack_default` (`:667-670`) — the shared flat network with the measured harness's containers. Every engine binds `--host 0.0.0.0 --port 8000` (`:196`, `:395`, `:489`); none publishes a host port, but **any container on `dstack_default` reaches each engine's `:8000` directly**, including the unauthenticated `/configure_logging`.
- **Host-published ports:** nginx `8000:80`, `8444:443`, `8001:8001` (`:60-63`); `dcgm-glm51` `9400:9400` (`:251-252`). `dcgm-dsv4-flash` publishes nothing (`:564-607`, no `ports:`), otelcol only `expose`s (`:297-299`). So the DSV4 arm's dcgm follows the `d085dcfc` good practice and the GLM arm's does not — an in-file asymmetry; metrics only.
- **nginx (`:1230-1333`):** four servers, each a **bare `location /`** with no path allowlist: `:80 default_server` → `proxy-glm51:8000` (`:1248-1253`); `:443` `server_name glm-5-1.completions.near.ai` → `proxy-glm51:8000` (`:1267-1278`); **`:8001`** plain HTTP → `proxy-dsv4-flash:8000` (`:1292-1297`); `:443` `server_name dsv4-flash.completions.near.ai ~^dsv4-flash-i\d+\.completions\.near\.ai$` → `proxy-dsv4-flash:8000` (`:1311-1322`). `client_max_body_size 100m`, `proxy_buffering off`, `proxy_cache off` on all four. Both `:443` servers share one certificate from the external `certs` volume (`:64-65`, `:676-678`); routing between arms on the TLS port is by SNI/Host only.
- **Reachability of the engine CRITICAL, this deployment:** external caller → nginx (`location /`) → proxy (catch-all, no allowlist except a fixed 404 on `/v1/responses`, per the `98b57ad7` page) → engine `:8000`; or any in-box container → engine directly. Three engines, no `--admin-api-key` on any (Check 1).

### MEDIUM (deployed: ON) — three engines' unauthenticated admin surface is reachable through four allowlist-free nginx servers, two catch-all proxies, and directly by any container on the flat external network — sink: log / disk / egress once flipped

- **Registrars.** `model-proxy-registrar` (`:74-102`, `curlimages/curl@sha256:d94d07ba…`) runs `registrar_script` (`:1091-1229`); `ds4f-migration-registrar` (`:631-666`, same image) runs `ds4f_registrar_script` (`:1334-1472`) and is **profile-gated** (`profiles: [register-ds4f]`, `:665-666`) — the log shows it brought up at entry 134. The two scripts were diffed against each other: **identical except** the registered model/domain (`:1141` vs `:1384`), the probe's `model` string (`:1157` vs `:1400`), and log-prefix strings; and `registrar_script` differs from the audited `ae05e203` script by **one echo string** (`1x` vs `2x`). Read in full, what each does with credentials, over what transport, to where:
  - **Registration** (`register_endpoint`, `unregister_endpoint`, `register_model`, `:1108-1130` / `:1351-1373`): `curl -X POST https://completions.near.ai/register/{endpoint,model}` (`PROXY_URL`, `:1094` / `:1337`) with `Authorization: Bearer $MODEL_PROXY_TOKEN` on **argv**, body = `{"endpoint":"HOST_IP:HTTP_PORT","routing_port":TLS_PORT}` / `{"model":…,"domain":…}`. HTTPS, off-box, metadata only. The DSV4 registrar registers `HOST_IP:8001` (`HTTP_PORT=8001`, `:649`) with routing port `TLS_PORT` (default 8444, `:650`); the GLM one registers `HOST_IP:8000` (`:90-91`).
  - **Readiness and health probes** (`:1186-1191` / `:1429-1434`; `check_inference` `:1152-1157` / `:1395-1400`): `GET http://HOST_IP:HTTP_PORT/v1/models` and `POST http://HOST_IP:HTTP_PORT/v1/chat/completions` with `Authorization: Bearer $PROXY_TOKEN` on argv, body **synthetic** — `{"messages":[{"role":"user","content":"hi"}],"max_tokens":1}`. Plain HTTP to the host-published nginx port; the request is the registrar's own, never a user's. All `echo`s print status codes, endpoint names, and counters (`:1109`, `:1117`, `:1142`, `:1163-1170`, `:1178-1185`, `:1193-1200`, `:1214-1224`) — no body.

### INFO (deployed: ON) — both registrars' probes carry MODEL_PROXY_TOKEN and PROXY_TOKEN on curl argv (harness process-agent-visible) and the inference token over plain HTTP to HOST_IP; probe bodies stay synthetic — sink: process cmdline / in-CVM HTTP, not message content

## Check 5 — The migration helpers: disk preflight and the DSV4 downloader

- **`migration-disk-preflight` (`:32-50`)**, added at `53b9608f`: `ghcr.io/astral-sh/uv:…@sha256:4f5d923c…` (`:33`, digest-pinned), **`profiles: [migration-preflight]`** (`:35`) so it is not started by an un-profiled up (log: up at entries 120 and 123, `restart: "no"` `:49`). `entrypoint: [python3, -B, -c]` with an inline three-statement script (`:37-41`): `shutil.disk_usage('/cache')` and `print(json.dumps(...))` of `total_bytes`/`used_bytes`/`free_bytes`. Mounts **only** `huggingface_cache:/cache:ro` (`:43`). Hardening: `network_mode: none` (`:44`), `read_only: true` (`:45`), `user: "65534:65534"` (`:46`), `cap_drop: [ALL]` (`:47`), `security_opt: [no-new-privileges:true]` (`:48`). It can read the shared weights cache and write nothing; it prints three integers, which the pipeline in Check 3 ships (label `:50` via `*1` — it has no `com.datadoghq.ad.logs` label of its own, so `filter/app_logs` at `:993` drops its lines for lack of `model`/`deployment`; the Datadog agent would still collect them). This is the most tightly confined service in the repo's manifest set.
- **`model-downloader-dsv4` (`:608-630`)**: same uv image (`:609`), **not** profile-gated but `restart: "no"` (`:613`) and `depends_on`-gated by both DSV4 engines (`condition: service_completed_successfully`, `:423-425`, `:517-519`). Runs `uvx --from 'huggingface_hub[hf_xet]' hf download deepseek-ai/DeepSeek-V4-Flash-0731 --revision 7872f01b…` (`:624`) with `HF_TOKEN` (`:628`) on the default network — it has full egress (to Hugging Face and to PyPI for the **unpinned** `huggingface_hub[hf_xet]` resolution), writes weights into the shared `huggingface_cache` (`:626`), and exits. It never handles plaintext; its mutable PyPI input is the same residual every recipe in this repo carries for its downloader. The GLM `model-downloader` (`:7-31`) is the same shape for `QuantTrio/GLM-5.1-AWQ @ 8f60817a…` (`:24`).

### INFO (deployed: OFF, profile-gated) — the read-only disk preflight is network-none, read-only, unprivileged, cap-dropped, and prints three byte counts; the DSV4 downloader is a network-attached one-shot with an unpinned PyPI input that never sees plaintext — sink: log (sizes only)

## Check 6 — Privileges, mounts, and shared volumes

- **Proxies** (`proxy-glm51` `:103-153`, `proxy-dsv4-flash` `:315-375`): `user: root` + **`privileged: true`** + `runtime: nvidia` + **`ipc: host`** (`:105-108`, `:317-320`), `/var/run/dstack.sock` (`:117`, `:329`) and `certs:ro` (`:118`, `:330`). The E2EE terminators are the most privileged containers in the file — the structural INFO every sibling page carries.
- **Engines**: `runtime: nvidia`, **`ipc: host`** (`:156`, `:378`, `:472`), `init: true`, `ulimits` memlock/nofile only — **no `core` ulimit**, no `privileged`, no `pid`, no `cap_add`. Three engines and two proxies share the host IPC namespace.
- **dcgm** (`:243-282`, `:564-607`): `cap_add: [SYS_ADMIN]` (`:247-248`, `:567-568`) with `runtime: nvidia`, GPUs 0-3 and 4-7 respectively — split by arm, not `all`; image `nvcr.io/nvidia/k8s/dcgm-exporter@sha256:ed594cf5…` on **both** (`:244`, `:565`, [audited](/images/nvcr.io/nvidia/k8s/dcgm-exporter/sha256-ed594cf53fe6942e84b07b0740cdcbb249fa4b39cb21feeebf93881ae51f0b5e.md): telemetry-not-content, bytes now pinned).
- **otelcol**: root, `/var/lib/docker/containers:ro` (`:301`) — the cross-container log reader (Check 3). **No `docker.sock` and no `pid: host` anywhere in this file** (grep-verified); those belong to the harness.
- **Volumes (`:671-679`)**: `huggingface_cache` is mounted by **five** services across both arms — GLM engine (`:202`), both DSV4 engines (`:398`, `:492`), both downloaders (`:26`, `:626`), preflight (`:43` ro) — weights only. `compile_cache_a:/root/.cache` on the GLM engine (`:201`; inductor/triton kernels, `:209-210`), `kernel_cache:/root/.cache/deep_gemm` on both DSV4 engines (`:399`, `:493`). `compile_cache_b` (`:675`) is declared but **mounted by nothing** — an orphan from the removed GLM r2, harmless. `certs` external (`:676-678`). `otelcol_app_storage` (`:672`, `:302`) holds the export queue — **the only request-derived disk tier in the file** is that spool of stdout records (Check 3). No KV-cache or prompt-cache volume exists; radix caches live in GPU memory.

### INFO (deployed: ON) — privileges, mounts, and volumes: two privileged root proxies and three engines in one host IPC namespace, one SYS_ADMIN dcgm per GPU half, a root log-reading collector, weights shared across arms, no request-derived disk tier except the telemetry spool — sink: disk (spool), otherwise none new

## Check 7 — The two proxies: env reachability, splice-armed egress, backend pools

Both proxies are `nearaidev/vllm-proxy-rs@sha256:98b57ad7…` (`:104`, `:316`) — the same build, [reviewed](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) with this very file as one of its four deployed recipes (its table cites `proxy-dsv4-flash` at `:315`, the idle timeout at `:341`, backends at `:351`, request size at `:352` — all re-verified here at those exact lines). Env, per proxy:

| var | `proxy-glm51` | `proxy-dsv4-flash` | note |
|---|---|---|---|
| `OHTTP_ENABLED` | `true` literal (`:129`) | `true` literal (`:347`) | not env-shaped; cannot be turned off by splice |
| `TOKEN` | `${PROXY_TOKEN}` (`:130`) | `${PROXY_TOKEN}` (`:348`) | in `allowed_envs` |
| `VLLM_BACKEND_URLS` | **1** backend, `model-sg-glm51-awq-tp4-r1:8000` (`:133`) | **2** backends, `-r1:8000,-r2:8000` (`:351`) | vs `ae05e203`'s 2 GLM backends — the only env diff from that audited block |
| `VLLM_BACKEND_CONVERSATION_AFFINITY` | absent | **absent** | per the proxy page: with the flag absent the pool runs plain least-connections — no conversation pinning, no prefix digest kept in memory; each request may land on either DSV4 replica. Privacy effect: none new (both replicas are the same attested image and command; the only cross-request state is each replica's in-GPU radix cache, which the affinity feature would *increase* hits on, not decrease) |
| `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS` | absent | `20` (`:341`) | the only proxy in the fleet that sets it; the proxy page traced the idle-watchdog error path as content-free |
| `VLLM_PROXY_MAX_REQUEST_SIZE` | absent | `104857600` (`:352`) | matches nginx `client_max_body_size 100m` |
| `WEB_CONTEXT_SEARCH_URL` / `_API_KEY` | `${…}` (`:136-137`) | `${…}` (`:355-356`) | **not in `allowed_envs`**; only the `/compose/up` splice can populate them |
| `FUSION_ENABLED` / `FUSION_INTERNAL_BEARER_TOKEN` | `${…}` (`:131-132`) | `${…}` (`:349-350`) | same |
| `CLOUD_API_URL` / `CLOUD_API_USAGE_TOKEN` | literal `https://cloud-api.near.ai` (`:124-125`) | same (`:342-343`) | usage accounting egress, per proxy page content-free |
| `COMPOSE_MANAGER_URL` | `http://compose-manager:8080` (`:126`) via `extra_hosts` (`:113-114`) | same (`:344`) | |
| `USE_NV_ATTESTATION_SDK` / `TLS_CERT_PATH` / `LOG_FORMAT=json` / `MODEL_NAME` | `:128`, `:134-135`, `:127` | `:346`, `:353-354`, `:345` | |

### MEDIUM (deployed: OFF, splice-armed) — unpinned web-search URL/key and fusion toggles on both E2EE-terminating proxies, outside allowed_envs — sink: egress (model-derived query to a third party; full conversation to fusion peers)

The proxy page rates web search CRITICAL and fusion HIGH *when active*; both are empty at deployed state unless spliced, and the splice is not recorded in the signed action log. Two proxies here means two independent splice targets on one CVM.

### INFO (deployed: ON) — two DSV4 replicas share one proxy pool with no conversation affinity: least-connections, no proxy-side prefix digest, no new cross-request path — sink: none

## Check 8 — Sibling comparison and image pinning

- **Pinning:** every registry image is digest-pinned — uv (`:8`, `:33`, `:609`), nginx (`:52`), curl (`:75`, `:632`), vllm-proxy-rs (`:104`, `:316`), nearaidev/sglang (`:385`, `:479`), dcgm ×2 (`:244`, `:565`), otelcol (`:284`), and the build's `FROM` (`:165`). The only non-digest image is the in-enclave build product (`:162`, Check 2). Both checkpoints are revision-pinned with matching download/serve pins (`:24`/`:190-191`; `:624`/`:387-388`).
- **Within the file:** the GLM arm publishes dcgm `9400` to the host, the DSV4 arm does not; the DSV4 proxy sets the stream idle watchdog and max request size, the GLM proxy does not; the DSV4 registrar is profile-gated, the GLM one is not; the DSV4 engines carry the env-shaped flag, the GLM engine is 100% literal. None of these asymmetries is a content path.
- **Across the lineage:** vs `ae05e203` — one GLM backend instead of two, `compile_cache_b` orphaned, the `com.datadoghq.ad.instances` openmetrics labels dropped from every service (0 vs 3), three otelcol scrape jobs added, two nginx servers added; the pipeline, filter, exporter, and receiver bytes are the same. vs `d085dcfc` — the DSV4 engine moved from `lmsysorg/sglang@6bb5fee3` with EAGLE speculative decoding and `--chunked-prefill-size 8192` to the near-built `ec518148` with target-only decode and `4096`; the proxy block shape (idle timeout, request size, hidden-states interpolation) is carried; dcgm remains digest-pinned on both arms (the one thing `d085dcfc` was praised for, kept).

### INFO (deployed: ON) — the attested file is not what created every container: the GLM engine and its downloader run from GLM-5.1-SGL-AWQ-TP4 @ 3c5f8891, whose engine block is configuration-identical to this file's except for Datadog openmetrics labels that this file omits — sink: none (identity; metrics-only label delta)

Check 0 has the evidence. For a reader keyed to this `file_sha256`: the engine you are reading about is the engine that is running, but the attestation surface's pointer to it is the older file's hash, and the running container likely carries a Datadog `/metrics` scrape hint this file does not declare.

### INFO (deployed: ON) — dcgm-exporter is digest-pinned on both arms and split by GPU half; the GLM arm's instance publishes 9400 to the host while the DSV4 arm's does not — sink: none (GPU counters)

## Verdict

**QUALIFIED PASS at this layer.** This is a new file, but not new configuration: every service block resolves to bytes already audited in the GLM-5.1 AWQ TP4 lineage (engine build, command, env, proxy env, registrar, nginx servers, otelcol pipeline) or the DSV4-Flash lineage (engine image + command modulo one scheduling integer, proxy env), and each was re-verified at this file's lines rather than carried on the base pages' word. What the file itself contributes is the *composition*: two model families, three engines, and two E2EE terminators on one CVM, one shared weights cache, one collector, and four nginx front doors — plus two migration helpers of which the disk preflight is the most tightly confined container in this repo's manifest set. Nothing in the file logs, persists, or transmits message content at deployed flags. The qualifications, in descending order of cost:

1. **Every plaintext service's stdout ships off-box unredacted** — to `telemetry.infra.near.ai` via this file's collector (disk-spooled, single consumer) and, per the measured harness, to Datadog (HIGH, deployed ON). Two arms now feed it.
2. **Three unauthenticated `/configure_logging` surfaces** behind four bare `location /` servers, two catch-all proxies, and the flat `dstack_default` network (MEDIUM, deployed ON).
3. **Splice-armed web-search/fusion env on both proxies** and the **env-shaped hidden-states flag on both DSV4 argvs**, none in `allowed_envs` (MEDIUM ×2, deployed OFF).
4. **Watchdog hang-dump armed at 1800 s on all three engines** (LOW, ARMED).
5. **Identity caveat:** the GLM engine runs from `3c5f8891`, not from this hash — shown configuration-equivalent, but the attestation pointer and the running container disagree on which document to read (INFO).

Recommend, in-file: pass `--admin-api-key` to all three engines; add a path allowlist to the four `location /` blocks (the config is in this file and thus under this hash); add a scrub processor or drop the unfiltered `traces`/`metrics/otlp` pipelines; bind OTLP receivers to localhost; either publish dcgm on neither arm or document why one; remove the orphan `compile_cache_b`; and — since the header already forbids whole-stack apply — bring the GLM engine up once from this file so the attested hash and the running engine agree.

## Not traced (load-bearing)

- **Compose dependency semantics of service-scoped `up`.** Whether compose-manager invokes `docker compose up <svc>` with or without `--no-deps` was not verified from its source; the claim that no dependency edge could have recreated the GLM engine rests on the `depends_on` graph in this file (`:54-56`, `:159-161`, `:423-425`, `:517-519`), which has no edge into that engine. If compose-manager also recreates dependents (nginx's `depends_on` the proxies), that still never reaches the engine.
- **The `a5b16f35` revision of `GLM-5.1-SGL-AWQ-TP4.yaml`** (entry 117) has no page; `proxy-glm51` was recreated from this file since, so nothing running depends on it, and it was not diffed.
- **Live action-log match** was read from the three saved attestation reports supplied for this review, not re-fetched at write time; the fleet can redeploy mid-audit.
- **`prod/migration-gpu-preflight.yaml @ 62b2bc14`** — hash reproduced, hardening lines skimmed, last action `compose_down`; not reviewed.
- **Whether the running GLM engine container actually carries the `3c5f8891` labels** (Datadog openmetrics hint) — inferred from the log; not observed on the host.
- **Engine internals** at both builds (`/configure_logging` behaviour, watchdog dump contents, radix-cache and `--enable-cache-report` semantics, `--chunked-prefill-size` effect) — deferred to the [`aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md) and [`ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) pages.
- **Proxy behaviour** — least-connections selection without affinity, the 20 s idle watchdog's error path, web-search/fusion gating, the `/v1/responses` refusal — deferred to the [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md).
- **The harness on this CVM** — Datadog container-collect-all and process-agent, `allowed_envs` — cited from the [`2c650eae` page](/manifests/measured/sha256-2c650eae81601afade86d1aa5ad898473b992081998026bb42ee1524ce99ee38.md) (reviewed 2026-07-15); not re-reviewed. Whether the `HOST_IP` the registrars probe over plain HTTP is a CVM-internal address or the physical host's — decides whether the bearer token in that probe transits the untrusted host network.
- **The `/compose/up` env splice** — inferred from interpolation forms and the compose-manager page; not re-verified at the deployed compose-manager build.
- **PyPI-resolved `huggingface_hub[hf_xet]`** in both downloaders (`:24`, `:624`) — mutable input in exit-after-download containers that never see plaintext; bytes not audited.
- **The in-enclave build's rebuild trigger** — whether compose rebuilt `glm51-sgl-awq-tp4-patched:local` at entry 114 or reused an image from an earlier stack; the block has been byte-identical across every revision audited, so the outcome is the same either way.
