# cvm-compose-files prod/GLM-5.3-Flash-SGL-TP4.yaml @ cdd6948a... (GLM-5.3-Flash, two sglang TP4/EP4 replicas behind one proxy; first revision reviewed; mem-fraction 0.77 variant)

## verdict: LEAKS — narrowly, and the leak is completed by this document: it selects the glm47 tool-call parser (whose deployed image logs a model-emitted tool-argument value at WARNING whenever it is not numeric) and it builds the pipeline that ships every engine stdout line, unscrubbed, to https://telemetry.infra.near.ai. Everything this file configures for the request path is otherwise clean — --log-requests-level 0 literal, zero env interpolation in the engine command, no dump/crash/profile/trace flag, weights and chat template pinned by revision on a local snapshot with HF_HUB_OFFLINE=1, OHTTP_ENABLED=true literal, no KV or prompt disk cache, a fail-closed in-enclave build that touches no logging, disk or network code, one literal telemetry destination — while it declines every containment available to it: no --admin-api-key so the engine's unauthenticated /configure_logging switch stays reachable over the shared dstack_default bridge and two bare location / listeners; --watchdog-timeout 1800 arms the undisableable in-flight prompt dump; no --allowed-media-domains or --disable-multimodal on a multimodal model; no ulimits core so a crashing engine writes its memory into its container layer; two content-egress env switches on the E2EE terminator sit outside allowed_envs but inside the non-measured overlay channel this file demonstrably relies on. Under active revision: six redeploys in 24 h, all mem-fraction and label tuning; this page is keyed to exactly this hash.

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/GLM-5.3-Flash-SGL-TP4.yaml` |
| **file_sha256** | `d1cc22eb8a25890e42a090d2c309b5045cdf374e000ed7228d9394f36f7f96d1` (re-verified locally: `shasum -a 256` over the captured 1031-line file returns exactly this value) |
| **serves** | `z-ai/glm-5.3-flash` at `glm-5-3-flash.completions.near.ai` (registrar `:889`; nginx `server_name` `:1019-1021` also admits `-stg` and `glm-5-3-flash-i<N>` hosts). **Two CVMs on two physical hosts share this hostname** and attest two revisions of this recipe: 24 attestation samples on 2026-09-02 split 13 / 11 between this hash and [`d76413cd`](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d76413cd231a4e871a515e4fa829c6510a374fe335e77fae3ea5a6dccf783909.md) (identical except `--mem-fraction-static 0.80` and the six `config_variant` strings that record it — see coverage) |
| **topology** | two sglang **TP4/EP4** replicas (`model-sg-glm53-fp8-tp4-r1` GPUs 0-3 `:391-427`, `-r2` GPUs 4-7 `:428-464`) behind one `vllm-proxy-rs` (`proxy-glm53`, `:353-390`, `VLLM_BACKEND_URLS` naming both `:367`), behind nginx with **two** listeners (`:8000` plain, `:443` TLS published as `8444`; `:310-312`) — no `:8001` discovery stub in this recipe. Plus `model-downloader` (run-once, `:226-246`), `hf-cleanup` (profile-gated, dormant, `:252-303`), `model-proxy-registrar` (`:324-352`), `dcgm-glm53` (`:466-494`), `otelcol-contrib` (`:495-526`) |
| **engine** | **built in-enclave**: `nearai/glm53-sglang-r12-budget:local` (`:161`) from `x-glm53-build` (`:50-157`) `FROM ghcr.io/phala-network/sglang@sha256:2488ca13…` (`:53`) — [audited](/images/ghcr.io/phala-network/sglang/sha256-2488ca13aee77ffd24ba2181b1b05dcf5e1c76e6477002c7b2f074b44fa49193.md), verdict **LEAKS, narrowly** (that page also read the inline `protocol.py` patch in this file; its engine-behaviour findings are cited below, not re-derived) |
| **runs on** | measured harness [`341313ae…`](/manifests/measured/sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md) (`INSTANCE_LABEL=gpu30`, QUALIFIED PASS — **no Datadog agent measured**, 22-entry `allowed_envs`, defines `dstack_default`) on guest OS [`a6eafc5f…`](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) (QUALIFIED PASS — armed core-dump path, no LSM). Both read for this review |
| **identity** | log-pinned: the signed compose-manager action log on CVM `570768366211…` (gpu30, measured `341313ae…`) pins `compose_up` at compose-manager commit `cdd6948a67f775b40a4d9795982765d2e0bb617c`, tag `gpu30-glm53-tp4-r1-mem077-20260902`, 2026-09-02T18:02:04Z; the orchestrator fetched the file at that commit and hash-checked it (match). **Not hardware-measured.** Six `compose_up` events in the 24 h before capture (from 2026-09-01T22:30Z), all mem-fraction / label tuning; the engine anchor and `FROM` digest were stable across every revision |
| **review** | reviewed 2026-09-02 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** full document review — there is no prior page for this recipe. The whole 1031-line file was read (anchors, seven services, the otel collector config, the registrar script, the nginx config); every `:line` below was located with `grep -n` / `sed -n` against the captured file, not inferred. The sibling revision `d76413cd` was `diff`ed against this file locally: **seven changed lines, no insertions or deletions** — `:170` (`--mem-fraction-static 0.77` here vs `0.80`) and the `config_variant` label/tag string at `:405`, `:417`, `:442`, `:454`, `:652`, `:676` (`…-strict-budget8192-mem077` here vs `…-strict-budget8192`). Both files are 1031 lines, so every citation on this page is valid at the same line in the sibling. Engine source behaviour is deferred to the [engine page](/images/ghcr.io/phala-network/sglang/sha256-2488ca13aee77ffd24ba2181b1b05dcf5e1c76e6477002c7b2f074b44fa49193.md); proxy behaviour to [`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (PRIVATE, two opt-in egress caveats); collector to [`otelcol-contrib@85ac41c2`](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md); dcgm to its [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md). nginx, curl, uv and alpine are ciphertext-side or weights-side and have no page.

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, full review)

> Part of the near.ai inference-path privacy audit (Fable, 2026-09-02).
> Method: [/notes/method.md](/notes/method.md) (§4 Manifest-only checks) · Engine: [2488ca13](/images/ghcr.io/phala-network/sglang/sha256-2488ca13aee77ffd24ba2181b1b05dcf5e1c76e6477002c7b2f074b44fa49193.md) · Harness: [341313ae](/manifests/measured/sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md) · OS: [a6eafc5f](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md)

## Check 1 — Engine anchor `x-sg-glm53-flash-common` and both replicas

The command is the YAML anchor `&sg-glm53-flash-common` (`:158-216`; command `:163-196`), merged by both replicas (`<<: *sg-glm53-flash-common`, `:392` and `:429`). It inherits `x-nvidia` (`:28-36`: `runtime: nvidia` `:29`, **`ipc: host` `:30`**, `ulimits` memlock/nofile only `:31-36`). **Zero `${VAR}` interpolation anywhere in the command** — the only interpolation in the whole anchor is `HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` (`:201`); no operator env, measured or spliced, can alter an engine flag. Verbatim:

```
sglang serve                                                        (:164)
  --model-path /root/.cache/huggingface/hub/models--zai-org--GLM-5.3-Flash/snapshots/84c6a6aa...   (:165)
  --revision 84c6a6aa9497188e15a635ba793b0f95a79b1033                (:166)
  --served-model-name z-ai/glm-5.3-flash                            (:167)
  --tp-size 4  --ep-size 4                                          (:168-169)
  --mem-fraction-static 0.77                                        (:170)   <- the only engine delta vs d76413cd (0.80)
  --max-running-requests 32  --max-queued-requests 32               (:171-172)
  --chunked-prefill-size 4096  --cuda-graph-max-bs-decode 32        (:173-174)
  --dsa-prefill-backend tilelang  --dsa-decode-backend tilelang     (:175-176)
  --kv-cache-dtype bfloat16  --moe-runner-backend deep_gemm         (:177-178)
  --speculative-algorithm EAGLE  (num-steps 5, topk 1, draft 6, adaptive)   (:179-183)
  --reasoning-parser glm45  --enable-strict-thinking                (:184-185)
  --grammar-backend xgrammar  --tool-call-parser glm47              (:186-187)
  --chat-template /root/.cache/huggingface/hub/models--zai-org--GLM-5.3-Flash/snapshots/3f1971b7.../chat_template.jinja   (:188)
  --context-length 1048576                                          (:189)
  --dist-init-addr 127.0.0.1:29510                                  (:190)
  --watchdog-timeout 1800                                           (:191)
  --host 0.0.0.0  --port 8000                                       (:192-193)
  --enable-metrics  --enable-cache-report                           (:194-195)
  --log-requests-level 0                                            (:196)
```

Environment (`:200-212`): `HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` (`:201`), **`HF_HUB_OFFLINE=1`** (`:203`), **`TRANSFORMERS_OFFLINE=1`** (`:204`), `HF_HUB_DISABLE_TELEMETRY=1` (`:205`), `DO_NOT_TRACK=1` (`:206`), `NVIDIA_DRIVER_CAPABILITIES=compute,utility`, `OPENBLAS_L2_SIZE`, `NCCL_DEBUG=WARN` (`:209`), `PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True` (`:210`), `TORCHINDUCTOR_CACHE_DIR=/root/.cache/torchinductor` (`:211`), `TRITON_CACHE_DIR=/root/.cache/triton` (`:212`). Volumes (`:197-199`): **`kernel_cache:/root/.cache`** (`:198`) and `huggingface_cache:/root/.cache/huggingface` (`:199`) — the second mount is nested inside the first. `init: true` (`:160`), `stop_grace_period: 5m` (`:214`), `logging: *logging-conf` (`:215` → json-file, 100m x 10, `:1-6`). No `cap_add`, no `privileged`, no `pid: host`, no `ulimits: core` on the engine.

**Flags and env that touch logging, dump, disk or egress — each classified:**

| item | line | deployed state | what it does here |
|---|---|---|---|
| `--log-requests-level 0` | `:196` | **OFF, safest** | below the code default of 2 at which prompt/output text first appears; the engine page confirms `sglang serve` consumes it on this path. Not a mitigation for the runtime `/configure_logging` override |
| `--log-requests` | absent | OFF | grep-verified over the whole file |
| `--admin-api-key` / `--api-key` | **absent** | **OFF (omission)** | the only `api.key`-shaped string in the file is `WEB_CONTEXT_SEARCH_API_KEY` (`:372`) — see the HIGH below |
| `--watchdog-timeout 1800` | `:191` | **ARMED** | 6x the upstream 300 s default; the engine's hang handler dumps every in-flight request's prompt+output token ids to stdout and no value disables it — HIGH below |
| `--soft-watchdog-timeout` | absent | OFF | no second, earlier dump |
| `--enable-metrics` / `--enable-cache-report` | `:194-195` | ON | counters, histograms and cache-hit token counts on `/metrics`; scraped by the collector (Check 5). Content-free by construction |
| `--crash-dump-folder`, `--export-metrics-to-file`, `--enable-trace`, `--debug-tensor-dump-*`, `--enable-hierarchical-cache` | absent | OFF | grep-verified. `dump_requests_folder` has no CLI flag and is settable only via `/configure_logging` |
| `--allowed-media-domains`, `--disable-multimodal` | **absent** | **exposure ON** | GLM-5.3-Flash is multimodal (engine page verified `config.json`); with no allowlist the engine fetches any client-supplied media URL — MEDIUM below |
| `--trust-remote-code` | absent | OFF | nothing to loosen; `--revision` (`:166`) pins the weights anyway |
| `--model-path` local snapshot + `--revision` + `--chat-template` at a second pinned revision | `:165`, `:166`, `:188` | ON | both snapshot hashes are full SHAs, populated by `model-downloader` (Check 3). The comment at `:202` ("nothing is fetched at engine start") is consistent with `HF_HUB_OFFLINE=1` |
| `HF_HUB_OFFLINE=1`, `TRANSFORMERS_OFFLINE=1`, `HF_HUB_DISABLE_TELEMETRY=1`, `DO_NOT_TRACK=1` | `:203-206` | ON | the engine process makes no Hugging Face call and sends no HF telemetry. Sibling GLM-5.2 recipe left `HF_HUB_OFFLINE` at 0; this is the stricter posture |
| `HF_TOKEN` in the engine env | `:201` | ON | a credential inside the plaintext process with nothing to use it (offline) — LOW below |
| `NCCL_DEBUG=WARN` | `:209` | quiet | NCCL warnings only; no `NCCL_DEBUG_FILE` |
| `TORCHINDUCTOR_CACHE_DIR` / `TRITON_CACHE_DIR` on `kernel_cache` | `:211-212`, `:198` | ON | compiled-kernel caches, content-free — but the mount is the **whole** `/root/.cache`, a persisted named volume (`:535`) — MEDIUM cross-link below |
| `--dist-init-addr 127.0.0.1:29510` | `:190` | loopback | TP rendezvous inside the container's own netns (no `network_mode: host`); both replicas use the same address without clash. Not an egress |
| `--host 0.0.0.0 --port 8000` | `:192-193` | ON | listens on every container interface, i.e. on the shared `dstack_default` bridge. Not host-published (nginx publishes `8000:8000`, `8444:443` `:311-312`; nothing publishes an engine port) |

### HIGH (deployed: OFF, omission) — no --admin-api-key leaves the engine's admin surface ungated; one flag would bearer-gate /configure_logging (sink: log)

`--admin-api-key` is absent from the command (`:163-196`) and from the whole file. Per the [engine page](/images/ghcr.io/phala-network/sglang/sha256-2488ca13aee77ffd24ba2181b1b05dcf5e1c76e6477002c7b2f074b44fa49193.md) (CRITICAL, confirmed at the image's bytes), `/configure_logging` is `ADMIN_OPTIONAL` — permitted when no key is configured — and with neither `--api-key` nor `--admin-api-key` the auth middleware is never installed, so **every request to the engine is unauthenticated**. Passing the flag is the single highest-value change available to this document. Its absence is what makes Check 6's reachability bite.

### HIGH (deployed: ARMED) — watchdog hang dump armed at 1800 s over up to 32 in-flight requests per replica at 1M context (sink: log / telemetry)

`--watchdog-timeout 1800` (`:191`) is an explicit 6x widening of the upstream default. The engine page traces the handler: on a forward-batch stall it logs every in-flight `Req` at ERROR with full `input_ids` and `output_ids` — plaintext after one decode with the public tokenizer — and **no value disables it**. This file bounds the blast radius at `--max-running-requests 32` (`:171`) per replica, two replicas (`:391`, `:428`), at `--context-length 1048576` (`:189`): up to 64 concurrent requests of up to 1M tokens each, node-wide. Smaller than the GLM-5.2 TP8 sibling's 256 but the same undisableable mechanism, and the same exit: stdout → json-file → collector → `https://telemetry.infra.near.ai` (Check 5). Conditional on a hang; the header comment (`:16-17`) documents K-pool workspace exhaustion on this hardware as lived history, i.e. stalls are not hypothetical.

**Label truthfulness (priority item 1):** no label anywhere carries a `watchdog` value (grep-verified) — nothing to falsify there. The labels that do assert engine state: `request_logging:disabled` in both `com.datadoghq.ad.logs` tag lists (`:405`, `:442`), `nearai.otel.request_logging: "disabled"` (`:419`, `:456`) and the same as a prometheus static label (`:654`, `:678`) — **true at the deployed flags** (`--log-requests-level 0`, no `--log-requests`) but a static assertion that `/configure_logging` can falsify at runtime with no attestation-visible change. `config_variant: …-mem077` (`:405`, `:417`, `:442`, `:454`, `:652`, `:676`) matches `:170`. `thinking_budget_policy: "default8192-public-to-native"` (`:418`, `:455`) matches the inline patch's default (Check 2). `precision:fp8-weights-bf16-kv` matches `--kv-cache-dtype bfloat16` (`:177`); the fp8-weights half describes the checkpoint and was not verified here.

### INFO (deployed: ON) — the file's own header comment is stale against the deployed engine flag; the machine-read labels are not (sink: none)

The prose header says `0.80 static memory` (`:13`) while the deployed flag is `--mem-fraction-static 0.77` (`:170`): the mem-fraction retune that produced this revision updated the flag and all six `config_variant` strings but not the comment. No privacy effect — a memory-pool fraction — but a reminder that compose comments are not evidence (this repo has been wrong twice by trusting one; [audit-surface §9](/notes/audit-surface.md)). The other header claims were checked: "Request/response payload logging is off" (`:22`) matches `:196`; "32 running requests, a bounded 32-request queue" matches `:171-172`; "fail-closed adapter patch" (`:19`) matches Check 2. On the sibling `d76413cd` the comment and flag agree.

### LOW (deployed: ON) — HF_TOKEN is injected into the plaintext engine process although the engine is offline; readable through the engine's local-file media primitive (sink: egress, credential not content)

`HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` (`:201`) sits in the engine env beside `HF_HUB_OFFLINE=1` (`:203`), so nothing in the engine can legitimately use it. The engine page's media-fetch finding shows `file://` and absolute paths are read locally by the image loaders, naming `/proc/self/environ` as reachable — which holds this token. It is a credential, not message content, so LOW; the fix is to drop the line from the anchor (the downloader at `:242` is the only service that needs it).

## Check 2 — In-enclave build `x-glm53-build` (`:50-157`)

`image: nearai/glm53-sglang-r12-budget:local` (`:161`) with `build: *glm53-build` (`:162`); `context: .` (`:51`) and a `dockerfile_inline` (`:52-157`). Document-level read of what the Dockerfile does; the engine page did the source read of the patched file and confirmed the pre-hash matches the image's `protocol.py`.

1. **`FROM ghcr.io/phala-network/sglang@sha256:2488ca13…`** (`:53`) — digest-pinned. The only thing resolved from outside the enclave at build time. **No `pip install`, no `git clone`, no `curl`, no `COPY`/`ADD`** — the build context (`:51`) is never read, so the compose-manager working directory cannot influence the image. Grep-verified over `:53-157`.
2. **Pre-patch hash gate** (`:55-58`): `sha256sum -c` of `python/sglang/srt/entrypoints/openai/protocol.py` against `29dbee58…`. A mismatch fails the `RUN` → the build fails → neither replica starts (both have `depends_on` only on the downloader, but a failed `build:` aborts `compose up` for the service). **Fail closed.**
3. **Inline Python** (`:59-114`): reads `protocol.py`, requires three anchor strings to occur **exactly once** each else `raise SystemExit("protocol.py layout changed; refusing to apply budget adapter")` (`:96-101`), then (a) adds `StrictInt` to the pydantic import, (b) adds `thinking_token_budget: Optional[StrictInt] = Field(default=None, ge=0)` after `custom_params`, (c) inserts a `@model_validator(mode="before")` `normalize_thinking_token_budget` that copies the public field into `custom_params["thinking_budget"]` (default 8192; explicit `None` opts out; a native value wins), and writes the file back (`:113`). The validator touches `values.get("custom_params")` and `values.get("thinking_token_budget")` only — **it never reads `messages`**, and the patch contains no `logging`/`logger`/`print`/`open()` (grep-verified over `:59-157`; the only `open`-like call is `path.read_text()`/`write_text()` on the source file itself).
4. **Post-patch gate** (`:115-119`): `py_compile` then `sha256sum -c` of the result against `c3c7a7a3…` — the patched file must be byte-exact. **Fail closed** a second time.
5. **Self-tests** (`:120-157`): seven `ChatCompletionRequest.model_validate` cases over a static message `"Say OK"` (`:126`); `SystemExit` on any mismatch. Test-time only; no network.

### INFO (deployed: ON) — the engine's attested identity is a build recipe, not an image digest; the :local tag is not digest-addressable (supply chain)

What the action log pins is this file, which pins the `FROM` digest and two content hashes of one patched file. The **running** bytes are whatever docker holds under `nearai/glm53-sglang-r12-budget:local` on that CVM at `compose up` — compose does not rebuild an existing tag unless told to (`--build`), and whether compose-manager passes that is outside this document (Not traced). Across the six revisions in 24 h the build anchor was byte-stable (engine page), so today the distinction is academic; it stops being academic the first time the patch changes and a stale `:local` survives. The build itself is clean and twice fail-closed. **Priority item 2: the Dockerfile hash-checks `protocol.py` before and after; the inline Python edits the pydantic request model only; a failed hash check aborts the build.**

## Check 3 — Weights lifecycle: `model-downloader` and `hf-cleanup`

**`model-downloader`** (`:226-246`): `ghcr.io/astral-sh/uv:python3.11-bookworm-slim@sha256:4f5d923c…` (`:227`, digest-pinned), `restart: "no"` (`:229`), runs `hf download zai-org/GLM-5.3-Flash --revision 84c6a6aa…` (`:235`) and `hf download … chat_template.jinja --revision 3f1971b7…` (`:237`) into `huggingface_cache:/root/.cache/huggingface` (`:240`), with `HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` (`:242`; in `allowed_envs`). Both replicas `depends_on` it with `condition: service_completed_successfully` (`:394-396`, `:431-433`). **Egress: Hugging Face, at deploy time only, from a container that never sees a request.** Both revisions are full SHAs and match the engine's `--model-path`/`--revision`/`--chat-template` (`:165-166`, `:188`) — the downloader and the engine agree on identity, and `HF_HUB_OFFLINE=1` means an eviction fails the engine closed rather than triggering a live fetch. Its stdout ("Downloading…", `:234-238`) carries a `model:`/`deployment:` label (`:245`) so it is shipped by the collector — content-free. `uvx --from 'huggingface_hub[hf_xet]'` (`:235`, `:237`) resolves `huggingface_hub` **unpinned** from PyPI at run time inside the weights-side container — a supply-chain note for the weights path, not a plaintext path (the container has no request access).

**`hf-cleanup`** (`:252-303`): `alpine:3.20@sha256:d9e853e8…` (`:253`, digest-pinned), **`profiles: ["maintenance"]`** (`:255`) — **not started by a plain `compose up`**; `restart: "no"` (`:256`). When run it requires `MODEL_NAME` (`:261`, `${MODEL_NAME}` at `:294` — **not** in `allowed_envs`), prints `df`/`du` of the cache, and `rm -rf`s exactly `/root/.cache/huggingface/hub/models--<MODEL_NAME with / -> -->` (`:278-279`, `:288`) on `huggingface_cache` (`:292`) — the **weights** volume only; it does not mount `kernel_cache` or anything the engine writes request-derived data to. Its own logging is a separate json-file 10m x 3 (`:297-302`) with no service labels, so if it ever runs its stdout has neither `model` nor `deployment` attributes and is **dropped** by `filter/app_logs` (`:763`).

### INFO (deployed: OFF, profile-gated) — hf-cleanup can evict the weights volume on operator demand; dormant, weights-side, fail-closed for the engine (disk)

Supply-chain relevance only: a service that deletes from the weights volume, triggered by a compose profile plus an unattested env. If it evicted `models--zai-org--GLM-5.3-Flash` under a running engine the loaded weights stay in GPU memory; a restart would fail the `depends_on` chain until the downloader re-fetches **at the pinned revision** (`:235`). No plaintext path.

## Check 4 — `proxy-glm53` (`:353-390`), the E2EE terminator

Inherits `x-vllm-proxy-common` (`:37-48`): `image: nearaidev/vllm-proxy-rs@sha256:b183677a…` (`:38`, [audited](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md), PRIVATE with two opt-in egress caveats), `user: root` (`:39`), **`privileged: true`** (`:40`), `<<: *nvidia` (`:41` → `runtime: nvidia`, **`ipc: host`**), `extra_hosts: compose-manager:host-gateway` (`:42-43`), mounts `/var/run/dstack.sock` (`:45`, attestation/key ops) and `certs:/etc/letsencrypt:ro` (`:46`). Environment (`:356-372`), line by line:

| var | line | literal? | in `allowed_envs`? | privacy role |
|---|---|---|---|---|
| `NVIDIA_VISIBLE_DEVICES=all` | `:357` | yes | — | GPU visibility for `USE_NV_ATTESTATION_SDK` |
| `CLOUD_API_URL=https://cloud-api.near.ai` | `:358` | yes | — | usage/auth calls; not message bodies |
| `CLOUD_API_USAGE_TOKEN=${…}` | `:359` | no, undefaulted | **no** | credential; arrives via the non-measured overlay |
| `COMPOSE_MANAGER_URL=http://compose-manager:8080` | `:360` | yes | — | control plane, via `extra_hosts` |
| `LOG_FORMAT=json` | `:361` | yes | — | format only; the proxy page finds its logs content-free |
| `MODEL_NAME=z-ai/glm-5.3-flash` | `:362` | yes | — | matches `--served-model-name` (`:167`) and the registrar (`:889`) |
| **`OHTTP_ENABLED=true`** | `:363` | **yes** | — | E2EE posture not operator-degradable via any env channel |
| `TOKEN=${PROXY_TOKEN}` | `:364` | no | yes | inference bearer |
| **`FUSION_ENABLED=${FUSION_ENABLED}`**, **`FUSION_INTERNAL_BEARER_TOKEN=${…}`** | `:365-366` | no, undefaulted | **no** | proxy page MEDIUM-2: Fusion forwards the **full decrypted conversation** to panel/judge endpoints when enabled |
| `VLLM_BACKEND_URLS=http://model-sg-glm53-fp8-tp4-r1:8000,http://model-sg-glm53-fp8-tp4-r2:8000` | `:367` | yes | — | both replicas by service name; least-connections pool per `:9-10` |
| `VLLM_PROXY_MAX_REQUEST_SIZE=104857600` | `:368` | yes | — | 100 MiB, matching nginx `client_max_body_size 100m` (`:1004`) |
| `TLS_CERT_PATH=…/completions.near.ai/fullchain.pem` | `:369` | yes | — | cert only; the key stays in the `certs` volume |
| `USE_NV_ATTESTATION_SDK=true` | `:370` | yes | — | why the proxy has GPU access |
| **`WEB_CONTEXT_SEARCH_URL=${…}`**, **`WEB_CONTEXT_SEARCH_API_KEY=${…}`** | `:371-372` | no, undefaulted | **no** | proxy page INFO-4: model-generated search query egresses to this URL when set |

**Sibling comparison:** nine fleet recipe pages set `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS` and nine set `LONG_TIER_ONLY`; this recipe sets neither (single tier, no long-context alias; stream timeout at the proxy default). `OHTTP_ENABLED=true` is literal here as on every published sibling. No proxy env in this file is a log-level or content switch.

### HIGH (deployed: OFF, splice-armed) — two content-egress switches on the E2EE-terminating proxy are outside allowed_envs but inside the overlay channel this file already depends on (sink: egress)

`FUSION_*` (`:365-366`) and `WEB_CONTEXT_SEARCH_*` (`:371-372`) are interpolated, undefaulted, and absent from the harness's 22-entry `allowed_envs` — the measured dstack env channel cannot set them. The remaining route is compose-manager's non-measured `POST /compose/up` env overlay. **This file shows that channel is live on this node:** `MODEL_PROXY_TOKEN` (`:342`) and `CLOUD_API_USAGE_TOKEN` (`:359`) are also outside `allowed_envs`, and the registrar exits immediately if `MODEL_PROXY_TOKEN` is empty (`:846-849`) — so for the node to be registered at all, the overlay must be delivering at least one secret. Whatever delivers those can deliver `FUSION_ENABLED=true`. Both features are additionally per-request opt-in at the proxy and default off; if empty means fail-closed at the proxy (the source review indicates so) this stays a latent switch rather than a live sink. Not a mitigation this document supplies.

### INFO (deployed: ON) — the proxy runs privileged as root with ipc: host and the dstack socket (structural)

`privileged: true` (`:40`), `user: root` (`:39`), `ipc: host` via `:41`, GPU runtime, `/var/run/dstack.sock` (`:45`). Root-equivalent in the guest and sharing an IPC namespace with both engine replicas (which also carry `ipc: host` via `:159`). Structural exposure inside the box — the E2EE terminator already holds plaintext — not a new reader. The proxy applies no path allowlist (proxy source reviews; catch-all forwarding behind bearer auth), so an accepted bearer reaches any engine route (Check 6).

## Check 5 — Telemetry pipeline (`otelcol-contrib` `:495-526`, config `:540-837`)

**Result: a fixed, entirely unscrubbed path from every app container's stdout to one literal off-box endpoint. This is the pipeline that turns the engine page's two log findings into egress.**

Service: `otel/opentelemetry-collector-contrib@sha256:85ac41c2…` (`:496`, [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)), `user: "0:0"` (`:499`), `mem_limit: 768m` (`:500`), env all defaulted (`MONITORING_INGEST_TOKEN=${…:-}` `:502`; `CVM_NAME`/`CVM_HOST`/`DD_HOSTNAME`/`ENV`/`HOST_IP` `:-unknown` `:503-507`), `expose: 4317, 4318` (`:508-510`, not host-published), mounts **`/var/lib/docker/containers:ro`** (`:512`) and `otelcol_app_storage:/var/lib/otelcol-contrib` (`:513`). Its own logging driver is `local` 20m x 3 (`:519-523`) — not json-file, so it does not tail itself; also excluded by name (`:767`).

Trace both ends:

1. **On-disk retention first.** `x-logging-conf` (`:1-6`): `json-file`, `max-size 100m`, `max-file 10` — up to ~1 GB retained stdout per container on the CVM's encrypted data disk before shipment. Applied to both replicas (`:215`), the proxy (`:48`), nginx (`:320`), registrar (`:348`), downloader (`:243`), dcgm (`:223`).
2. **`filelog/docker_containers`** (`:551-626`) tails `include: [/var/lib/docker/containers/*/*-json.log]` (`:552`), `json_parser` (`:557`), moves the raw line into `body` (`:565-567`), then a chain of `regex_parser` operators lift routing attributes out of the `com.datadoghq.ad.logs` label — `model:` (`:582`), `served_model`, `model_path`, `precision`, `deployment:` (`:598`), `ip`, `port`, `env`, `host`, `instance`, `replica` — ending in an unconditional `add service.name: cvm-docker` (`:624-626`).
3. **`filter/app_logs`** (`:759-768`) **drops** records with `attributes["model"] == nil and attributes["deployment"] == nil` (`:763`) and the five management services by name (`:764-768`: `compose-manager`, `compose-manager-launcher`, `otelcol-management`, `otelcol-contrib`, `datadog-agent`). Both replicas' labels carry `model:` **and** `deployment:` (`:405`, `:442`), as do the proxy (`:373`), nginx (`:322`), registrar (`:350`), downloader (`:245`), dcgm (`:477`). **So the filter keeps exactly the plaintext-side containers.** Reading the filter alone gives the opposite impression; read with the labels it is a keep-list for app stdout.
4. **Processors** on `logs/app` (`:815`): `memory_limiter` (`:724`), `resource` (`:729`, adds `host.name`/`cvm.name`/`host.machine`/`deployment.environment`/`service.namespace`), `transform/docker_metadata` (`:747`, rewrites `service.name`/`source` from labels), `filter/app_logs`, `groupbyattrs/docker_service` (`:770`), `batch` (`:790`). **None redacts, hashes, truncates or pattern-matches a body.**
5. **Export:** `otlphttp/gateway` (`:796`) → `endpoint: https://telemetry.infra.near.ai` (`:797`, literal), `Authorization: Bearer ${env:MONITORING_INGEST_TOKEN}` (`:799`; in `allowed_envs`, selects the account never the destination), disk-backed `sending_queue` `num_consumers: 1` / `queue_size: 128` / `storage: file_storage` (`:801-803`) under `/var/lib/otelcol-contrib/storage` (`:807`) on `otelcol_app_storage` (`:513`, `:533`). Pipelines (`:812-832`): `logs/app` `[filelog] → […] → [otlphttp/gateway]` (`:813-816`); `metrics/app` from `prometheus/apps` (`:817-820`); `metrics/otlp` and `traces` from the open `otlp` receiver (`:821-824`, `:829-832`); `metrics/self` (`:825-828`). Collector self-telemetry `level: info` literal (`:837`).

### HIGH (deployed: ON, systemic) — no scrubbing at any hop; anything either engine replica prints leaves the CVM to telemetry.infra.near.ai (sink: telemetry)

Complete chain: **anything the engine prints → json-file (~1 GB retained, `:1-6`) → filelog (`:552`) → [no scrubbing] (`:815`) → `https://telemetry.infra.near.ai` (`:797`).** The engine page's deployed-ON MEDIUM (glm47 parser logs a model-emitted argument value), its CRITICAL (`/configure_logging` turns on verbatim prompt+output logging) and its HIGH (watchdog dump) all terminate here. The guarantee "no content leaves" is exactly "no plaintext service ever prints content", which the engine page shows is already false in a bounded way.

### MEDIUM (deployed: ON) — the glm47 tool-call parser this file selects logs a model-emitted tool-argument value verbatim at WARNING, and this file ships that line off-box (sink: log / telemetry)

Two choices in this document complete the engine page's LEAKS finding: `--tool-call-parser glm47` (`:187`) selects the parser whose number-fallback `logger.warning(f"Failed to parse '{value}' as number, …")` writes the raw model-generated argument text (engine page, read from the image's layer bytes); and the pipeline above (Check 5, 1-5) ships that WARNING line to near.ai unredacted. Trigger: any tool-using request where the model emits a non-numeric value for a numeric-typed parameter — a routine formatting slip, no attacker, no flag change. Blast radius: one argument fragment per occurrence, which for tool arguments often restates the user's own words. MEDIUM, not HIGH, for the bounded fragment; it is why the verdict class is LEAKS. Controls this file could apply: a body-scrubbing or engine-source drop processor in `logs/app` (`:815`), or a Dockerfile patch to the detector in `x-glm53-build` — the build already demonstrates the mechanism.

### INFO (deployed: ON) — one off-box log destination for this CVM; the Datadog labels are inert because the gpu30 harness measures no agent (telemetry)

The [`341313ae` harness](/manifests/measured/sha256-341313aed4336a5fb592cef58004b3abc82f2ba751df32be69f3169eeba40c14.md) measures no Datadog agent, so the `com.datadoghq.ad.logs` labels this file attaches to every service (`:245`, `:322`, `:350`, `:373`, `:405`, `:442`, `:477`, `:525`) drive only the collector's own regex parsers here; **engine stdout leaves this CVM to exactly one destination, near.ai's gateway.** A property of the host, not of this file — and this recipe's **other** CVM (gpu04, `c82b1a2e…`) has no published harness page yet; see the sibling page's Not traced.

### MEDIUM (deployed: ON) — two open OTLP receivers with no scrubbing on the shared bridge, feeding the traces pipeline (sink: telemetry)

`otlp` grpc `0.0.0.0:4317` (`:547`) and http `0.0.0.0:4318` (`:549`), `expose`d (`:508-510`) not published — but on `dstack_default` (Check 6) any co-resident container can push spans or metrics that go straight to `otlphttp/gateway` via `metrics/otlp` (`:821-824`) and `traces` (`:829-832`) with `[memory_limiter, resource, batch]` only. At this config no service in the file emits OTLP; a span-content risk from a sibling container, not log injection (the `otlp` receiver is not in `logs/app`).

- **[INFO] Metrics are counters only** — `prometheus/apps` (`:628-714`) scrapes `/metrics` on both replicas (`:631-654`, `:655-678`), dcgm (`:679-697`) and the proxy (`:698-714`) with static labels; `--enable-metrics` on sglang emits counters/histograms and, with `--enable-cache-report`, cache-hit token counts.
- **[INFO] nginx access logging is metadata** — `json_combined` (`:978-991`) records `$uri` (`:985`, not `$request`), `request_id`, `org_id`, `workspace_id`, method, status, sizes, timings, `upstream_addr`; `access_log` at `:993`. Per-request, user-attributable; not message content. Shipped via the same pipeline (nginx label `:322`).

## Check 6 — Network topology, nginx, registrar, egress

```
networks:
  default:
    external: true
    name: dstack_default      (:527-530)
```

### CRITICAL (deployed: ON, exposure) — the engine's unauthenticated logging switch is reachable over the shared bridge and through two bare location / listeners, and its output is shipped off-box by this file (sink: log / disk / telemetry)

The mechanism is the engine's ([engine page](/images/ghcr.io/phala-network/sglang/sha256-2488ca13aee77ffd24ba2181b1b05dcf5e1c76e6477002c7b2f074b44fa49193.md) CRITICAL: one `POST /configure_logging` enables verbatim prompt+output logging and arbitrary-path request pickling, applied in-process with no restart and no attestation-visible change). This document supplies no containment and completes the exit:

- **Engine port 8000 is not host-published** — nginx publishes `8000:8000` and `8444:443` (`:311-312`); dcgm publishes nothing (unlike the GLM-5.2 sibling's `9400:9400`). **But `external: true` (`:529`) is the shared `dstack_default` bridge** (defined by the measured harness), and both engines bind `0.0.0.0:8000` (`:192-193`). Any container on that bridge — including the harness's own root-equivalent compose-manager, and any project outside this `file_sha256` — can `curl http://model-sg-glm53-fp8-tp4-r1:8000/configure_logging`, unauthenticated. No `internal: true`, no project-private network, no egress allowlist.
- **Both serving listeners are bare pass-throughs.** `:8000` is `location / { proxy_pass http://proxy-glm53:8000; }` (`:1010`); `:443` likewise (`:1030`), one server block for `glm-5-3-flash.completions.near.ai`, its `-stg` twin and `glm-5-3-flash-i<N>` (`:1019-1021`). **`location /` pass-through count: 2 of 2 listeners; zero allowlisted paths.** This recipe has no `:8001` discovery stub, so — unlike the GLM-5.2 sibling — the allowlist idiom does not appear anywhere in the file.
- **The proxy applies no path allowlist** (proxy source reviews: a catch-all forwards any traversal-safe path to a backend, guarded only by a valid bearer — deployment `TOKEN` or a valid cloud API key). Not re-derived from the `b183677a` tree here.
- **Exit path:** the switched-on log lines go to stdout → Check 5 → off-box; the switched-on pickle can be pointed at `/root/.cache` → `kernel_cache` (persists — below).

**Net:** two routes to the engine's admin surface — (a) any co-resident container on the shared bridge, unauthenticated; (b) any external caller holding an accepted bearer, via `nginx → proxy → engine`. **"OFF at this configuration" — not "cannot happen".** `--admin-api-key` closes both; `internal: true` or a project-private network closes (a); an nginx path allowlist narrows (b).

**Registrar** (`:324-352`, script `:839-975`): `curlimages/curl@sha256:d94d07ba…` (`:325`), env `HOST_IP`, `HTTP_PORT` (default 8000), `TLS_PORT` (default 8444), `MODEL_PROXY_TOKEN`, `PROXY_TOKEN` (`:338-343`). It POSTs to `https://completions.near.ai` (`:842`) `/register/endpoint` (`:858`), `/unregister/endpoint` (`:866`), `/register/model` (`:873`) with `{"model":"z-ai/glm-5.3-flash","domain":"glm-5-3-flash.completions.near.ai"}` (`:889`) — metadata. Its liveness probe is a **synthetic** chat completion: `POST http://${HOST_IP}:${HTTP_PORT}/v1/chat/completions` (`:900`) with body `{"model":"z-ai/glm-5.3-flash","messages":[{"role":"user","content":"hi"}],"max_tokens":1}` (`:903`), bearer `PROXY_TOKEN`; readiness `GET /v1/models` (`:932`). **Probe bodies contain no user content.** Note what the probe demonstrates: the host-published `:8000` listener accepts a **plain-HTTP, non-OHTTP** chat request with a bearer — the degraded path of [method §1](/notes/method.md) is open at nginx and the proxy on this node (nginx and the proxy see that probe's plaintext; a real client on that path would be seen likewise). Healthcheck writes `/tmp/registrar_alive` (`:333`, `:949`) — a timestamp.

Every outbound destination configured in this file:

| destination | service | literal? | carries content? |
|---|---|---|---|
| `https://telemetry.infra.near.ai` (`:797`) | otelcol-contrib | literal | **yes — full app-container log bodies, unscrubbed** |
| `https://cloud-api.near.ai` (`:358`) | proxy | literal | usage/auth; not message bodies |
| `http://compose-manager:8080` (`:360`) via `extra_hosts` (`:42-43`) | proxy | literal | control plane |
| `https://completions.near.ai` (`:842`) | registrar | literal | registration metadata; synthetic probe `"hi"` (`:903`) to the local nginx, not off-box |
| Hugging Face | **model-downloader only** (`:235`, `:237`); engine offline (`:203-204`) | implicit | weights at deploy time |
| PyPI (`uvx --from huggingface_hub[hf_xet]`, `:235`) | model-downloader | implicit, unpinned | tooling at deploy time |
| **any client-supplied media URL** | **engine** (no `--allowed-media-domains`) | request-driven | the requesting client's own URL — MEDIUM below |
| **`${WEB_CONTEXT_SEARCH_URL}`** (`:371`) | **proxy** | not literal, not in `allowed_envs` | **yes if populated** — model-generated search query |
| **Fusion panel/judge endpoints** (`${FUSION_ENABLED}`, `:365`) | **proxy** | not literal, not in `allowed_envs` | **yes if enabled** — full decrypted conversation |

### MEDIUM (deployed: ON) — request-driven media fetch from the plaintext engine with no domain allowlist and multimodal not disabled (sink: egress, request-driven)

This file passes neither `--allowed-media-domains` nor `--disable-multimodal` (`:163-196`, grep-verified). The engine page verified `zai-org/GLM-5.3-Flash@84c6a6aa` is multimodal (`Glm5NextForConditionalGeneration`, `vision_config`) and that with an empty allowlist **no host check runs**: any http(s) URL in a request is fetched by the engine (up to 5 redirects, 64 MiB), and `file://`/absolute paths are read locally. The bytes carried outbound are the requesting client's own URL, so this is not a cross-user plaintext leak — it is a model-external egress and SSRF/local-read primitive from the plaintext process (reaching `127.0.0.1:8000/configure_logging`, other `dstack_default` containers, `/root/.cache`, `/proc/self/environ` with `HF_TOKEN`). One flag (`--allowed-media-domains` with any non-empty list) closes the remote half; `--disable-multimodal` closes all of it at the cost of vision. The sibling Gemma-4 recipe sets both controls in the same file; this one sets neither.

- **[MEDIUM] nginx spills large request bodies to a temp file** — `client_max_body_size 100m` / `client_body_buffer_size 1m` (`:1004-1005`): bodies over 1 MB are written to nginx's client-body temp dir. At 1M-token context that is the normal case. **Under OHTTP the body at nginx is the sealed blob (ciphertext)**; on the plain `:8000` path the registrar itself exercises (`:900`), it would be plaintext. `proxy_buffering off; proxy_cache off` (`:1001-1002`) — no response body buffered to disk; `proxy_read_timeout 3600s` (`:1003`). The image's compiled-in `client_body_temp_path` and the guest's handling of that file are not verified (Not traced).
- **[MEDIUM] `HTTP_PORT` / `TLS_PORT`** (`:340-341`) are interpolated defaults not in `allowed_envs` — routing controls for the registrar's advertised endpoint, not content controls; reachable only through the overlay.

## Check 7 — Privileges, mounts, volumes, image pinning

`volumes:` (`:532-538`): `otelcol_app_storage` (`:533`), `huggingface_cache` (`:534`), **`kernel_cache`** (`:535`) — all named and persistent across container recreation and redeploy — and `certs: external: true` (`:536-538`, shared with the harness's certbot). **No `pid: host`, no `SYS_PTRACE`, no docker socket** in this file (grep-verified; those live in the measured harness). The only `cap_add` is dcgm's `SYS_ADMIN` (`:220-221`).

### MEDIUM (deployed: ARMED) — no ulimits core anywhere in the recipe, so a crashing engine replica writes its process memory as a core file into its container layer (sink: disk)

The [OS page](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) MEDIUM (ARMED) establishes the substrate: `CONFIG_COREDUMP=y`, no core-dump handler, default `core_pattern`, `docker.service`/`containerd.service` at `LimitCORE=infinity`, no `default-ulimits` in `daemon.json` — so **a container inherits an unlimited core limit unless its compose service sets `ulimits: core`**. That is the recipe's to set, and this recipe does not: the only `ulimits` block is `x-nvidia` (`:31-36`) with `memlock` and `nofile`; `core` appears nowhere (grep-verified). A SIGSEGV/SIGABRT in a scheduler or worker process — a CUDA-library fault under the K-pool exhaustion the header (`:16-17`) describes is not exotic — writes prompts, host-side KV copies and everything mapped as `core` into that replica's writable layer on the LUKS2 data disk. Bounds: in-enclave, encrypted at rest, on a container layer that `restart: unless-stopped` (`:213`) recreates only on `compose up` (a crash-restart of the same container **keeps** the layer). ARMED, not ON: it needs a crash. Fix: `ulimits: core: 0` (hard and soft) in `x-nvidia` or the engine anchor — one stanza.

### MEDIUM (deployed: ON, cross-link) — /root/.cache is a persisted named volume, so a runtime-enabled request pickle survives container recreate and redeploy (sink: disk)

`kernel_cache:/root/.cache` (`:198`) mounts the **entire** `.cache` (not just `torchinductor`/`triton`, `:211-212`) as a named volume (`:535`); the HF mount (`:199`) is nested inside it. The engine page shows `dump_requests_folder` — settable only via the unauthenticated `/configure_logging` — accepts an arbitrary path; `/root/.cache/anything` lands on `kernel_cache` and **outlives the container**. Narrowing the mount to the two kernel-cache subdirectories would leave a runtime dump on the ephemeral layer instead. At the deployed flags nothing request-derived is written here (KV is bfloat16 in GPU memory; no hierarchical cache; multimodal caches are in-memory per the engine page). **PASS on configured disk, cross-linked exposure on runtime-enabled disk.**

- **[INFO] `ipc: host` on both replicas and the proxy** (`:30` via `:41`, `:159`) — one shared IPC namespace across the three plaintext-handling containers and the guest.
- **[INFO] `otelcol-contrib` mounts `/var/lib/docker/containers:ro`** (`:512`) as root (`:499`) — cross-container log visibility; that is its function.

**Image pinning.**

Digest-pinned: `vllm-proxy-rs@sha256:b183677a…` (`:38`, audited, signed provenance), `uv@sha256:4f5d923c…` (`:227`), `alpine:3.20@sha256:d9e853e8…` (`:253`), `nginx@sha256:1d13701a…` (`:305`), `curlimages/curl@sha256:d94d07ba…` (`:325`), `otelcol-contrib@sha256:85ac41c2…` (`:496`, audited), and the engine's `FROM ghcr.io/phala-network/sglang@sha256:2488ca13…` (`:53`, audited — identity there is a registry-verified python tree over builder-asserted lower layers with **no signed provenance**).

### MEDIUM (deployed: ON) — dcgm-exporter is tag-pinned in a SYS_ADMIN container over all eight GPUs (supply chain)

`nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:218`) is **tag-only**, with `cap_add: SYS_ADMIN` (`:220-221`), `runtime: nvidia` (`:219`) and `device_ids` 0-7 (`:474`). The registry can serve different bytes under the same tag with no attestation-visible trace. It cannot see request plaintext by configuration (NVML counters; GPU memory is CC-protected) and, unlike the GLM-5.2 sibling, its `:9400` is **not** host-published — scraped only internally (`:683`). It remains the strongest-capability unpinned image in the file. [Tag-addressed page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md).

## Cross-check synthesis

- **[X0 / MEDIUM, deployed ON — the leak]** `--tool-call-parser glm47` (`:187`) + unscrubbed stdout shipping (`:552` → `:815` → `:797`) = a model-emitted argument value reaches `telemetry.infra.near.ai` on a routine formatting slip. Both halves are this document's choices.
- **[X1 / CRITICAL — exposure, not capture]** `/configure_logging` reachable: `external: true` shared bridge (`:527-530`) + two bare `location /` (`:1010`, `:1030`) + no `--admin-api-key` + a pipeline that ships stdout verbatim. Nothing here captures content; everything here declines to contain the mechanism that would.
- **[X2 / HIGH]** Watchdog dump armed (`:191`) over 32 x 2 in-flight requests (`:171`, two replicas) at 1M context (`:189`), same unscrubbed exit.
- **[X3 / HIGH — systemic]** Global app-stdout shipping to one near.ai destination; "no content collected" holds only if no plaintext service prints content — and X0 shows one does.
- **[X4 / HIGH, latent]** `FUSION_*` / `WEB_CONTEXT_SEARCH_*` on the E2EE terminator, outside `allowed_envs` but on the same overlay channel that must already deliver `MODEL_PROXY_TOKEN` and `CLOUD_API_USAGE_TOKEN`.
- **[X5 / MEDIUM x2, disk]** No `ulimits: core` (crash → memory image on the data disk, ARMED) and the whole-`.cache` persisted volume (runtime pickle survives redeploy).
- **[X6 / MEDIUM, egress]** Multimodal engine with no media allowlist.
- **Verified clean at this layer:** engine command literal end to end; `--log-requests-level 0`; no dump/trace/profile flag; weights, template and `FROM` digest-pinned; `HF_HUB_OFFLINE=1`; `OHTTP_ENABLED=true` literal; telemetry destination literal; in-enclave build fail-closed twice and free of logging/disk/network code; downloader run-once and weights-side; `hf-cleanup` profile-gated and weights-side; registrar probes synthetic; nginx access log metadata; no KV/prompt disk cache; no `pid: host`/`SYS_PTRACE`/docker socket.

## Verdict

**LEAKS — narrowly.** The class is inherited from the engine and completed here: this document selects the parser that logs a model-emitted argument value and builds the pipeline that carries that line off-box with no redaction. It is a bounded fragment of tool-call traffic (MEDIUM), which is why "narrowly" — but it is deployed-ON, needs no attacker and no flag change, and a document that configures both ends of it cannot be called a pass.

- **What this file configures for the request path is otherwise clean:** every engine logging flag literal and minimal (`:196`), zero `${VAR}` in the command, no dump/crash/profile/trace flag, weights and chat template pinned by full SHA on a local snapshot with `HF_HUB_OFFLINE=1`, `OHTTP_ENABLED=true` literal, one literal telemetry destination, a twice-fail-closed in-enclave build that never touches `messages`, logging, disk or network, run-once downloader, dormant weights-side cleanup, synthetic registrar probes, no KV or prompt disk cache. On the configured path, and outside the glm47 line, **no plaintext copy is written to any log, file, cache or network destination by anything this file starts.**
- **What this file fails to contain:** the engine's live paths. It leaves `/configure_logging` reachable by two routes and declines `--admin-api-key`; sets the watchdog dump's ceiling to 32 x 2 requests at 1M tokens; guarantees anything printed leaves the CVM unscrubbed; leaves a multimodal engine with no media allowlist; sets no `ulimits: core` on a substrate the OS page shows will core-dump the engine to disk; and mounts the whole `.cache` as a persisted volume. **The invariant this layer delivers is "content capture is not configured", not "content capture cannot be enabled" — and in one bounded case content is already printed.**
- **Under active revision.** Six `compose_up` events in 24 h; two revisions live at once behind one hostname. This page is keyed to `d1cc22eb…` only; the sibling `d76413cd…` differs by one engine memory number and six label strings and carries [its own page](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d76413cd231a4e871a515e4fa829c6510a374fe335e77fae3ea5a6dccf783909.md). A seventh revision needs a seventh page.
- **Recommended, in order:** (1) `--admin-api-key` on the engine anchor; (2) drop `{value}` from the glm47 detector's number-fallback warning via `x-glm53-build` (the patch mechanism is already there) or add a scrub/drop processor for engine-sourced records to `logs/app`; (3) `ulimits: core: 0` in `x-nvidia`; (4) `--allowed-media-domains` (or `--disable-multimodal`); (5) an nginx path allowlist or a project-private engine network; (6) narrow `kernel_cache` to the two kernel-cache subdirectories; (7) remove `HF_TOKEN` from the engine env; (8) digest-pin dcgm-exporter; (9) fix the `:13` comment.

## Not traced

- **Whether the action log on CVM `570768366211…` currently pins this `file_sha256`** — asserted by the orchestrator's 2026-09-02 capture (fetched at `cdd6948a` and hash-checked); the file's own hash was re-verified here, the log was not independently re-fetched. Given six redeploys in 24 h, re-check the log before relying on this page for a later sample.
- **The 13 / 11 host split and which physical host attests which revision** — from the orchestrator's 24-sample capture; not re-sampled here.
- **Whether compose-manager passes `--build` (or `--pull`) on `compose up`**, i.e. whether the `:local` engine image is rebuilt from this file's `x-glm53-build` on every deploy or an existing tag is reused. The build anchor was byte-stable across all six revisions, so the running bytes should equal this recipe's build either way — but the mechanism was not verified.
- **Whether the deployed `vllm-proxy-rs@b183677a` tree applies any path filtering, and whether empty `WEB_CONTEXT_SEARCH_*` / `FUSION_*` means fail-closed** — inherited from the proxy source review (if fail-closed, the splice-armed HIGH downgrades to MEDIUM).
- **How compose-manager sources the env it overlays** (`MODEL_PROXY_TOKEN`, `CLOUD_API_USAGE_TOKEN`, `MODEL_NAME`, `HTTP_PORT`, `TLS_PORT`, `FUSION_*`, `WEB_CONTEXT_SEARCH_*`) — this file shows the channel must be live; what it carries is not visible here.
- **Whether sglang prints any other request content at the configured levels** beyond the glm47 line — engine source leg; load-bearing under X3.
- **`huggingface_hub` version resolved by `uvx` at deploy time** (`:235`, `:237`) — unpinned; weights-side only.
- **Whether nginx actually spills bodies to `client_body_temp` on this image**, where that path is, and what the guest does with the file.
- **The chat template** at `3f1971b7…` (`:188`, `:237`) — a jinja template executed over every prompt in the engine process; pinned by revision, not read here.
- **dcgm-exporter's effective NVML visibility under CC**; the contents of `/var/lib/otelcol-contrib/storage` (queued, unencrypted-at-the-application-layer batches of whatever the pipeline admitted, on a named volume).
