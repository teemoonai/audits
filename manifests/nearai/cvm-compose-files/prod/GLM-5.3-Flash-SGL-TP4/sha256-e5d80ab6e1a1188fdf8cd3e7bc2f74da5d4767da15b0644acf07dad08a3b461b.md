# cvm-compose-files prod/GLM-5.3-Flash-SGL-TP4.yaml @ 676532b9... (GLM-5.3-Flash two-replica sglang TP4 node; sibling revision of 2878b9c4; delta = one proxy image digest; the revision that created both engine replicas on both CVMs)

## verdict: LEAKS — narrowly, identical in class and in every finding to the 2878b9c4 sibling page: this revision differs from it in exactly one line — the proxy image digest at line 47, 27265f5a here versus 98b57ad7 there — and nothing else, so every engine flag, every service, the collector config, the nginx config and the registrar script are byte-identical and every finding carries at the same line numbers: the glm47 parser line shipped unscrubbed to telemetry.infra.near.ai, the CRITICAL exposure of the unauthenticated /configure_logging over the shared bridge and two bare listeners with no --admin-api-key, the armed 1800 s watchdog dump, no media allowlist, no ulimits core, the whole-.cache persisted volume, the splice-armed proxy egress switches, the profile-gated soak relay, and the patchwork of revisions behind the running containers. The one thing this hash says that the running node does not: its proxy digest. Both CVMs' action logs show this revision recreated model-sg-glm53-fp8-tp4-r1, -r2 and glm53-perception-check on 2026-09-09, and that the proxy container was recreated an hour later from 2878b9c4 (98b57ad7) — so the engines that hold plaintext were created by this file and the E2EE terminator was not. Keyed to exactly this hash.

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/GLM-5.3-Flash-SGL-TP4.yaml` |
| **file_sha256** | `e5d80ab6e1a1188fdf8cd3e7bc2f74da5d4767da15b0644acf07dad08a3b461b` (re-verified locally: `shasum -a 256` over the captured 1174-line file returns exactly this value) |
| **serves** | `z-ai/glm-5.3-flash` at `glm-5-3-flash.completions.near.ai` (registrar `:1006`; nginx `server_name` `:1159-1161`) — the same two CVMs (gpu04 `7bb9af0a…`, gpu02 `fccd496a…`) as the [`2878b9c4` sibling](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-2878b9c4c6db512a7538dc4e8981fd5acae72cdaace7794509f48161bcce08e3.md); both revisions appear in both signed action logs, service-scoped |
| **topology** | identical to the sibling: two sglang TP4/EP4 replicas (`:302-338`, `:339-377`) behind one `vllm-proxy-rs` (`:261-301`), behind nginx with two bare listeners (`:1148-1151`, `:1170-1173`); `model-downloader`, `hf-cleanup` (maintenance profile), registrar, `dcgm-glm53`, `otelcol-contrib`, and the two `verification`-profile services `glm53-perception-check` (`:378-418`) and `glm53-soak-relay` (`:420-447`) |
| **engine** | `docker.io/nearaidev/sglang@sha256:a7b7136a…` (`:62`, `:379`) — [audited](/images/docker.io/nearaidev/sglang/sha256-a7b7136abcf5e07522289d96e96fec9b42a1a30f9dfda57e957f142680d2d67b.md), LEAKS narrowly; that page's deployed-command listing was read from this revision's engine anchor, which is byte-identical to the sibling's |
| **proxy** | `nearaidev/vllm-proxy-rs@sha256:27265f5a…` (`:47`) — [audited](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-27265f5a10abcacc3fe80604a00ed2ba32c6e082895115e4b652b40525cb554d.md), QUALIFIED PASS, whose page already states that this is the only attested file naming that digest and that "whether these bytes are the proxy currently executing on glm-5-3-flash is not established". The action logs settle it for the capture: **`proxy-glm53` on both CVMs was last recreated from the sibling `2878b9c4`** (`98b57ad7…`) at 2026-09-09T23:44Z and 23:50Z — after every `compose_up` of this file. This revision's proxy digest was never deployed as a container on either CVM by any log entry (see Check 1) |
| **runs on** | CVM `7bb9af0ac5b0…` (gpu04, harness [`c82b1a2e…`](/manifests/measured/sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md)) and CVM `fccd496ae28c…` (gpu02, harness [`533e43fd…`](/manifests/measured/sha256-533e43fd215d56ec6cd719adb5defe6615f7ba6ae0b018e832f7c8dadf2e25d4.md)), both on guest OS [`a6eafc5f…`](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md); both QUALIFIED PASS, no Datadog agent, the same 22 `allowed_envs` (read from both captured app-compose documents) |
| **identity** | log-pinned, **service-scoped**: the signed compose-manager action logs record `compose_up` at commit `676532b98f9a01da5d43f98299af297b573d242d`, tag `v0.0.408`, file_sha256 `e5d80ab6…` for `model-sg-glm53-fp8-tp4-r1` (gpu04 2026-09-09T22:32:59Z; gpu02 23:18:19Z), `model-sg-glm53-fp8-tp4-r2` (gpu04 22:56:40Z; gpu02 23:35:47Z) and `glm53-perception-check` (gpu04 22:44Z, 23:08Z, 23:47:41Z; gpu02 23:29Z, 23:47Z, 23:53:31Z), each preceded by a `compose_stage` (gpu04 22:30:01Z; gpu02 23:15:08Z) and a `compose_down` of the replica. **No full `compose up` of this file appears in either log.** The orchestrator fetched the file at that commit and hash-checked it (match); re-hashed here (match). **Not hardware-measured** |
| **review** | reviewed 2026-09-10 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review against the [`2878b9c4` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-2878b9c4c6db512a7538dc4e8981fd5acae72cdaace7794509f48161bcce08e3.md) (full delta review of the same recipe against the `d76413cd` base, written in the same session from the same captured files). Both captured files were re-hashed with `shasum -a 256` (each matches its keyed identity) and `diff`ed locally. Result: **one changed line, no insertions, no deletions; both files 1174 lines**:

```
47c47
<   image: nearaidev/vllm-proxy-rs@sha256:27265f5a10abcacc3fe80604a00ed2ba32c6e082895115e4b652b40525cb554d   (this file)
>   image: nearaidev/vllm-proxy-rs@sha256:98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284   (2878b9c4)
```

Because the diff is in-line only, **every `:line` citation on the sibling page is valid at the same line in this file**. Every top-level anchor, service, config, volume and network block was additionally extracted from both files and compared: all identical except `x-vllm-proxy-common` (`:46-57`) at its `image:` line. The load-bearing citations were re-located here by `grep -n` rather than assumed: engine command `:63-98`, `--max-running-requests 32` `:71`, `--tool-call-parser glm47` `:87`, `--context-length 1048576` `:89`, `--watchdog-timeout 1800` `:91`, `--host 0.0.0.0` / `--port 8000` `:92-93`, `--log-requests-level 0` `:96`, `--limit-mm-data-per-request` `:98`, `kernel_cache:/root/.cache` `:100`, `HF_TOKEN` `:103` beside `HF_HUB_OFFLINE=1` `:105`, `ulimits` memlock/nofile only `:40-44`, proxy env `:264-283` with `OHTTP_ENABLED=true` `:271`, `FUSION_*` `:273-274`, `VLLM_BACKEND_CONVERSATION_AFFINITY=1` `:278`, `WEB_CONTEXT_SEARCH_*` `:282-283`, `external: true` / `dstack_default` `:514-515`, soak-relay ports `:433-434` and allowlist `:563`/`:574`, filelog include `:665`, `filter/app_logs` `:873-882`, exporter endpoint `:911`, `logs/app` processors `:929`, bare `location /` `:1148` and `:1170`, dcgm digest `:121` + `SYS_ADMIN` `:124-125`.

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta review)

> Part of the near.ai inference-path privacy audit (Fable, 2026-09-10).
> Method: [/notes/method.md](/notes/method.md) (§4 Manifest-only checks) · Base: [2878b9c4](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-2878b9c4c6db512a7538dc4e8981fd5acae72cdaace7794509f48161bcce08e3.md) · Engine: [a7b7136a](/images/docker.io/nearaidev/sglang/sha256-a7b7136abcf5e07522289d96e96fec9b42a1a30f9dfda57e957f142680d2d67b.md) · Proxy: [27265f5a](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-27265f5a10abcacc3fe80604a00ed2ba32c6e082895115e4b652b40525cb554d.md)

## Check 1 — What changed, and whether any of it has a privacy surface

### INFO (deployed: OFF, superseded) — the delta is one proxy image digest that no container on either CVM was created from; the engines and the perception-check are this file's, the proxy is the sibling's (sink: none)

`:47` names `vllm-proxy-rs@sha256:27265f5a…` where the sibling names `98b57ad7…`. The two proxy pages describe the difference as PR #228 — a fixed-string 404 refusal of `/v1/responses` in the authenticated catch-all, placed before the body is read, with no new log line or sink ([`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md), Delta map); everything else between the two trees is byte-identical, and both are QUALIFIED PASS with the same two client-opt-in egress features and the same missing engine path allowlist. So the line has no privacy surface in either direction: `27265f5a` forwards one more path (`/v1/responses`, an engine route the [engine page](/images/docker.io/nearaidev/sglang/sha256-a7b7136abcf5e07522289d96e96fec9b42a1a30f9dfda57e957f142680d2d67b.md) does not flag) than `98b57ad7` does.

What the action logs add: this file's four `compose_up` entries per CVM name only `model-sg-glm53-fp8-tp4-r1`, `-r2` and `glm53-perception-check`. `proxy-glm53` has no `depends_on` and is not a dependency of any of those three (the replicas depend on `model-downloader` only, `:305-307`, `:342-344`), so none of those ups touched it; it was then recreated from `2878b9c4` on both CVMs. **This revision's only distinguishing byte therefore never became a running container**, and the `27265f5a` page's "running-bytes question" resolves, for the 2026-09-10 capture, to *not running*. Conversely, this is the file that created the two containers that hold plaintext — the engines — on both CVMs (Check 8 of the sibling page). A reader keyed to this hash gets the right engine and the wrong proxy; a reader keyed to the sibling gets the right proxy and an engine that was, in fact, created from a byte-identical anchor. The patchwork finding on the sibling page is the general statement of this.

**Priority-item sweep repeated on this file (not assumed):** `--log-requests-level 0` (`:96`) present; `--log-requests`, `--admin-api-key`, `--api-key`, `--allowed-media-domains`, `--disable-multimodal`, `--crash-dump-folder`, `--export-metrics-to-file`, `--enable-trace`, `--enable-hierarchical-cache`, `--soft-watchdog-timeout`, `--trust-remote-code`, `ulimits: core`, `pid: host`, `SYS_PTRACE`, `docker.sock`, `build:` — all absent (grep-verified over the whole file). The only `${VAR}` in the engine anchor `:59-118` is `HF_TOKEN` (`:103`). `OHTTP_ENABLED=true` literal (`:271`). Exporter endpoint literal (`:911`). Every `image:` digest-pinned.

## Check 2 — Findings carried from the sibling page, re-located at this file's lines

Each heading below is the sibling's finding; the body states only what was re-verified here. Full evidence and reasoning are on the [`2878b9c4` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-2878b9c4c6db512a7538dc4e8981fd5acae72cdaace7794509f48161bcce08e3.md).

### CRITICAL (deployed: ON, exposure) — the engine's unauthenticated logging switch is reachable over the shared bridge and through two bare location / listeners, and its output is shipped off-box by this file (sink: log / disk / telemetry)

Carried. `external: true` / `name: dstack_default` (`:514-515`); both engines bind `0.0.0.0:8000` (`:92-93`); nginx `location / { set $proxy_glm53 …; proxy_pass $proxy_glm53; }` at `:1148-1151` (`:8000`) and `:1170-1173` (`:443`), no allowlisted path on the customer listeners; no `--admin-api-key` in `:63-98`; stdout exit via the pipeline below. Mechanism per the engine page's CRITICAL at `fc91d24`. The proxy in front of these engines is `98b57ad7` (running), not `27265f5a` (this line) — both apply no path allowlist beyond the `/v1/responses` refusal, so route (b) is identical either way.

### HIGH (deployed: OFF, omission) — no --admin-api-key leaves the engine's admin surface ungated; one flag would bearer-gate /configure_logging (sink: log)

Carried; absent from `:63-98` and from the whole file.

### HIGH (deployed: ARMED) — watchdog hang dump armed at 1800 s over up to 32 in-flight requests per replica at 1M context (sink: log / telemetry)

Carried. `--watchdog-timeout 1800` (`:91`), `--max-running-requests 32` (`:71`), two replicas (`:302`, `:339`), `--context-length 1048576` (`:89`). No `--soft-watchdog-timeout`. `request_logging:disabled` (`:316`, `:330`, `:353`, `:367`, `:767`, `:791`) is true at deployed flags and runtime-falsifiable. **These are the engine containers this file created** — the finding attaches to this hash more directly than to the sibling's.

### HIGH (deployed: ON, systemic) — no scrubbing at any hop; anything either engine replica prints leaves the CVM to telemetry.infra.near.ai (sink: telemetry)

Carried. json-file 100m x 10 (`:4-9`) → `filelog` include (`:665`) → `logs/app` processors `[memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch]` (`:929`) — none redacts — → `https://telemetry.infra.near.ai` (`:911`), bearer `${env:MONITORING_INGEST_TOKEN}` (`:913`), disk-backed queue (`:914-917`). `filter/app_logs` (`:873-882`) keeps records carrying `model`/`deployment`, which both replicas' labels do (`:316`, `:353`), as does the perception-check's (`:416`). Note the collector container on both CVMs was created from Canary revisions whose config differs from this block only by a dcgm `scrape_timeout` (sibling page, Check 8).

### MEDIUM (deployed: ON) — the glm47 tool-call parser this file selects logs a model-emitted tool-argument value verbatim at WARNING, and this file ships that line off-box (sink: log / telemetry)

Carried — the reason the class is LEAKS. `--tool-call-parser glm47` (`:87`) plus the pipeline above. Engine-side mechanism per the engine page, at these image bytes (`:62`). Unaffected by the delta.

### HIGH (deployed: OFF, splice-armed) — two content-egress switches on the E2EE-terminating proxy are outside allowed_envs but inside the overlay channel this file already depends on (sink: egress)

Carried. `FUSION_ENABLED` / `FUSION_INTERNAL_BEARER_TOKEN` (`:273-274`), `WEB_CONTEXT_SEARCH_URL` / `_API_KEY` (`:282-283`), undefaulted, absent from both harnesses' 22-entry `allowed_envs`; `MODEL_PROXY_TOKEN` (`:250`) and `CLOUD_API_USAGE_TOKEN` (`:267`) show the overlay is live (registrar exits without the former, `:960-963`). The [`27265f5a` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-27265f5a10abcacc3fe80604a00ed2ba32c6e082895115e4b652b40525cb554d.md) confirms both features are byte-identical to the `98b57ad7` tree and rates the populated case CRITICAL (web search) / HIGH (fusion). The proxy env block `:264-283` is identical in both revisions; only the image line differs.

### INFO (deployed: ON) — VLLM_BACKEND_CONVERSATION_AFFINITY=1 is new and fleet-unique; per the proxy pages it keys on a salted, memory-only conversation digest and emits only enum and backend-index metric labels (sink: none)

Carried (`:278`). The `27265f5a` page is the one that reviewed the affinity change itself (PR #227): same salted digest as the chat cache, confined to memory, three new metric series with enum/index labels. Scraped by `:812-816`.

### MEDIUM (deployed: ON) — request-driven media fetch from the plaintext engine with no domain allowlist and multimodal not disabled; the new 64-image cap bounds count, not source (sink: egress, request-driven)

Carried. Neither `--allowed-media-domains` nor `--disable-multimodal` in `:63-98`; `--limit-mm-data-per-request '{"image": 64}'` (`:98`) is a count; `--disable-fast-image-processor` (`:97`) selects a decoder. Model multimodal per the engine page.

### MEDIUM (deployed: ARMED) — no ulimits core anywhere in the recipe, so a crashing engine replica writes its process memory as a core file into its container layer (sink: disk)

Carried. Only `ulimits` block `:40-44` (memlock, nofile); `core` nowhere. Substrate resolved for both CVMs (`os_image_hash a6eafc5f…` in both reports; OS page: `LimitCORE=infinity`, no `default-ulimits`). `restart: unless-stopped` (`:116`).

### MEDIUM (deployed: ON, cross-link) — /root/.cache is a persisted named volume, so a runtime-enabled request pickle survives container recreate and redeploy (sink: disk)

Carried. `kernel_cache:/root/.cache` (`:100`), named (`:520`); HF mount nested (`:101`). The replicas this file created on 2026-09-09 mounted the same volumes their predecessors (six engine revisions on gpu04 since 2026-09-02; a full `compose down` on gpu02 that did not remove named volumes) had mounted.

### MEDIUM (deployed: ON) — two open OTLP receivers with no scrubbing on the shared bridge, feeding the traces pipeline (sink: telemetry)

Carried. `0.0.0.0:4317` (`:660`), `0.0.0.0:4318` (`:662`), `expose` only (`:493-495`); `metrics/otlp` (`:935-938`), `traces` (`:943-946`).

### MEDIUM (deployed: ON) — the running containers on both CVMs are a patchwork of five revisions across two files; only proxy-glm53 was created from this hash, and the collector that decides where engine stdout goes and the nginx that decides reachability were created from Canary revisions this hash does not name (sink: telemetry / egress, attestation visibility)

Carried with the roles inverted: for **this** hash the statement is "only `model-sg-glm53-fp8-tp4-r1`, `-r2` and `glm53-perception-check` were created from this hash" — the two plaintext-holding engines and the profile-gated test client. The proxy came from the sibling, the collector and nginx from Canary `ee6a8d31`/`5a5b7228`, the registrar and dcgm from `a2d3f073`, gpu04's downloader from `d76413cd` and its soak relay from Canary `1072fbb4`. Every one of those source blocks equals this file's (sibling page, Check 8 table, which applies verbatim here since the two files differ only at `:47`). The heading keeps the sibling's wording so the indexer records one finding, not two.

### LOW (deployed: ARMED, profile-gated) — glm53-soak-relay is a second host-published TLS ingress straight to each engine replica, bypassing OHTTP and the proxy, bearer-gated with a six-path allowlist that excludes /configure_logging; started on gpu04 from a Canary file on 2026-09-05 and never logged down (sink: egress, ingress bypass)

Carried (`:420-447`; ports `:433-434`; config `:526-580`: `error_log /dev/null` `:531`, `access_log off` `:535`, bearer map `:541-544`, 401 `:561`/`:572`, allowlist `:563`/`:574`, `location / { return 404; }` `:567`/`:578`). Not started by any `compose_up` of this file.

### INFO (deployed: OFF, profile-gated) — the perception-check prints up to 400 characters of model output for synthetic fixtures straight to the shipped stdout stream; ran three times per CVM on 2026-09-09, never with user content (sink: log / telemetry)

Carried (`:378-418`; label `:416`) — and **every one of those six runs was from this file** (identity row). Its stdout lines from 2026-09-09 (fixture hashes, statuses, token counts, and on any failed case a 400-character slice of the model's answer to a synthetic image) left both CVMs through the pipeline above.

### INFO (deployed: ON) — the engine's attested identity is now a registry digest with a signed build attestation; the base's ":local tag not digest-addressable" concern is resolved, and the stale-image window it warned about did open on gpu04 in between (supply chain)

Carried (`:62`, no `build:` anywhere). This is the file that first put the registry digest into running engine containers on both CVMs (its commit message: "Pin GLM-5.3 to signed registry image").

### INFO (deployed: ON) — dcgm-exporter is now digest-pinned with an explicit counters-only metrics file; the base's tag-pin MEDIUM is resolved, the SYS_ADMIN privilege over all eight GPUs is not (supply chain)

Carried (`:121-129`, `:582-652`, `:459`). Not started from this file; the running dcgm is `a2d3f073`'s, identical block.

### INFO (deployed: ON) — one off-box log destination on both CVMs; the Datadog labels are inert because neither measured harness includes an agent (telemetry)

Carried (both harness pages; `nearai.otel.*` labels `:286-299`, `:317-336`, `:354-373`, `:463-478` read by nothing in the file).

### INFO (deployed: ON) — the proxy runs privileged as root with ipc: host and the dstack socket (structural)

Carried (`:48-50`, `:54`; engine `ipc: host` via `:60`).

### INFO (deployed: OFF, profile-gated) — hf-cleanup can evict the weights volume on operator demand; dormant, weights-side, fail-closed for the engine (disk)

Carried (`:160-210`; `profiles: ["maintenance"]` `:163`; `rm -rf` `:196` on `huggingface_cache` `:200`). Never started on either CVM.

### LOW (deployed: ON) — HF_TOKEN is injected into the plaintext engine process although the engine is offline; readable through the engine's local-file media primitive (sink: egress, credential not content)

Carried (`:103` beside `:105`) — in the engine containers this file created.

**Not carried:** the sibling's Check 1 INFO on the d76413cd → 2878b9c4 delta (this page's delta is one line; the seven-commit delta is the sibling's to describe).

## Verdict

**LEAKS — narrowly, the sibling's verdict unchanged.** One in-line edit — a proxy image digest that, per both CVMs' action logs, never became a running container — has no privacy surface; every finding of the [`2878b9c4` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-2878b9c4c6db512a7538dc4e8981fd5acae72cdaace7794509f48161bcce08e3.md) was re-located at the identical line in this file and carries at the same severity and deployed state. If anything the engine-side findings attach to this hash more tightly than to the sibling's: this is the revision that created `model-sg-glm53-fp8-tp4-r1` and `-r2` on both CVMs on 2026-09-09, and the sibling created only the proxy in front of them. A consumer keyed to this hash should read the sibling for the proxy and both for the rest — which is the patchwork finding in one sentence. Recommendations as on the sibling page, in the same order.

## Not traced

- **Whether `27265f5a…` ever ran as `proxy-glm53` on either CVM.** The logs show no `compose_up` of this file naming `proxy-glm53` and no full up of this file; unless a dependency edge not visible in the file caused a recreate (none exists in the YAML), the digest at `:47` was never instantiated. The `27265f5a` page's Not-traced item on running bytes is resolved *negatively* for the capture, on the same compose-semantics assumptions the sibling page lists.
- **Whether each `compose_stage` of this file (gpu04 22:30:01Z, gpu02 23:15:08Z) pulled the engine image** or only validated the document — not visible; the subsequent `compose_up` entries are what created the containers.
- Everything in the sibling page's Not traced applies here unchanged (soak-relay liveness on gpu04; compose `--no-deps`/`compose_stage` semantics; the Canary file as a document; dcgm `613ab03c` page and tag mapping; perception-script print sites; proxy path filtering and fail-closed semantics for empty `WEB_CONTEXT_SEARCH_*`/`FUSION_*`; the overlay env source; the `:8001` unregister call; `uvx` resolution; nginx `client_body_temp`; the chat template; the otelcol queue directory).
