# cvm-compose-files prod/GLM-5.3-Flash-SGL-TP4.yaml @ d36548db... (GLM-5.3-Flash two-replica sglang TP4 node; mem-fraction 0.80 sibling revision; delta = one engine memory number and six label strings)

## verdict: LEAKS — narrowly, identical in class and in every finding to the d1cc22eb sibling page: this revision differs from it in exactly seven lines — --mem-fraction-static 0.80 instead of 0.77 at line 170, and the six config_variant label/tag strings that drop the -mem077 suffix — none of which touches logging, dumping, disk, telemetry or egress. The leak (glm47 parser line shipped unscrubbed to telemetry.infra.near.ai), the CRITICAL exposure (unauthenticated /configure_logging reachable over the shared bridge and two bare listeners, no --admin-api-key), the armed watchdog dump, the missing media allowlist, the missing ulimits core and the splice-armed proxy egress switches all carry at the same line numbers. This CVM is a fresh gpu04 instance whose measured harness has no published page yet, so the single-destination telemetry property is not established for this host. Under active revision; keyed to exactly this hash.

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/GLM-5.3-Flash-SGL-TP4.yaml` |
| **file_sha256** | `d76413cd231a4e871a515e4fa829c6510a374fe335e77fae3ea5a6dccf783909` (re-verified locally: `shasum -a 256` over the captured 1031-line file returns exactly this value) |
| **serves** | `z-ai/glm-5.3-flash` at `glm-5-3-flash.completions.near.ai` (`:889`) — the **same hostname** as the [`d1cc22eb` revision](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d1cc22eb8a25890e42a090d2c309b5045cdf374e000ed7228d9394f36f7f96d1.md); 24 attestation samples on 2026-09-02 split 13 (`d1cc22eb`) / 11 (this hash) across the two CVMs behind the load balancer |
| **topology** | identical to the sibling: two sglang TP4/EP4 replicas (`:391-427`, `:428-464`) behind one `vllm-proxy-rs` (`:353-390`), behind nginx with two bare listeners (`:1010`, `:1030`) |
| **engine** | in-enclave build `nearai/glm53-sglang-r12-budget:local` (`:161`) FROM `ghcr.io/phala-network/sglang@sha256:2488ca13…` (`:53`) — [audited](/images/ghcr.io/phala-network/sglang/sha256-2488ca13aee77ffd24ba2181b1b05dcf5e1c76e6477002c7b2f074b44fa49193.md), LEAKS narrowly. Build anchor byte-identical to the sibling |
| **runs on** | CVM `7bb9af0ac5b0…`, `INSTANCE_LABEL=gpu04`, measured harness `c82b1a2e…` — **no published page** (under concurrent review); guest OS not resolved for this CVM. The sibling's harness resolution (gpu30, `341313ae…`, no Datadog agent) **does not transfer** — see Not traced |
| **identity** | log-pinned: the signed compose-manager action log on CVM `7bb9af0ac5b0…` pins `compose_up` at compose-manager commit `d36548db2486f91cc527792ea0c735068eede5de`, tag `v0.0.380`, 2026-09-02T18:25:37Z — four seconds after that CVM's `compose_manager_started` (18:25:33Z): a fresh CVM whose first deploy is this file. Orchestrator fetched at that commit and hash-checked (match). **Not hardware-measured** |
| **review** | reviewed 2026-09-02 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

> **Addendum (2026-09-02, orchestrator).** Written before the gpu04 pages landed, this page says the measured harness `c82b1a2e…` and this CVM's guest OS have no page. Both do now: [`c82b1a2e…`](/manifests/measured/sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md) (QUALIFIED PASS at the manifest layer; byte-identical to the gpu30 harness `341313ae…` except `INSTANCE_LABEL=gpu04`; no Datadog agent, one off-box log destination, the same 22 `allowed_envs`), and the live report from instance `7bb9af0ac5b0…` carries `os_image_hash a6eafc5f…`, the same upstream `dstack-nvidia-0.5.11` guest as gpu30 ([page](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md)). That resolves the Not-traced items (a) and (b) below the same way as on the sibling page; the review text above is left as written.

**coverage:** delta review against the [`d1cc22eb` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d1cc22eb8a25890e42a090d2c309b5045cdf374e000ed7228d9394f36f7f96d1.md) (full review of the same recipe, written in the same session from the same captured pair). Both captured files were re-hashed with `shasum -a 256` (each matches its keyed identity) and `diff`ed locally. Result: **seven changed lines, no insertions, no deletions; both files 1031 lines**:

```
170c170   --mem-fraction-static 0.80          (sibling: 0.77)
405c405   com.datadoghq.ad.logs tag  config_variant:phala-r12-h200-tp4-ep4-eagle-adaptive-5-1-6-strict-budget8192   (sibling: ...-mem077)   replica 1
417c417   nearai.otel.config_variant: "...-strict-budget8192"                                                       replica 1
442c442   com.datadoghq.ad.logs tag  config_variant:...-strict-budget8192                                           replica 2
454c454   nearai.otel.config_variant: "...-strict-budget8192"                                                       replica 2
652c652   prometheus static label    config_variant: "...-strict-budget8192"                                        replica 1 scrape job
676c676   prometheus static label    config_variant: "...-strict-budget8192"                                        replica 2 scrape job
```

Because the diff is in-line only, **every `:line` citation on the sibling page is valid at the same line in this file**; the load-bearing ones were re-located here by `grep -n` rather than assumed (engine command `:163-196`, `--log-requests-level 0` `:196`, `--watchdog-timeout 1800` `:191`, `--tool-call-parser glm47` `:187`, `--max-running-requests 32` `:171`, `--context-length 1048576` `:189`, `kernel_cache:/root/.cache` `:198`, `HF_HUB_OFFLINE=1` `:203`, `ulimits` memlock/nofile only `:31-36`, proxy env `:356-372`, `OHTTP_ENABLED=true` `:363`, `external: true` / `dstack_default` `:527-530`, filelog include `:552`, `filter/app_logs` `:759-768`, exporter endpoint `:797`, `logs/app` `:813-816`, bare `location /` `:1010` and `:1030`, dcgm tag `:218` + `SYS_ADMIN` `:220-221`).

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta review)

> Part of the near.ai inference-path privacy audit (Fable, 2026-09-02).
> Method: [/notes/method.md](/notes/method.md) (§4 Manifest-only checks) · Base: [d1cc22eb](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d1cc22eb8a25890e42a090d2c309b5045cdf374e000ed7228d9394f36f7f96d1.md) · Engine: [2488ca13](/images/ghcr.io/phala-network/sglang/sha256-2488ca13aee77ffd24ba2181b1b05dcf5e1c76e6477002c7b2f074b44fa49193.md)

## Check 1 — What changed, and whether any of it has a privacy surface

### INFO (deployed: ON) — the delta is one GPU memory-pool fraction and six label strings recording it; no logging, dump, disk, telemetry or egress surface (sink: none)

`--mem-fraction-static 0.80` (`:170`) sets the fraction of GPU memory sglang reserves statically for weights plus KV pool; `0.77` on the sibling leaves 3% more headroom for the K-pool workspace the header (`:16-17`) describes exhausting. It changes how much KV cache fits in GPU memory — **KV stays in GPU memory in both revisions** (`--kv-cache-dtype bfloat16` `:177`, no hierarchical cache, no offload flag; grep-verified) — and nothing about what is logged, dumped, written or sent. The six `config_variant` strings (`:405`, `:417`, `:442`, `:454`, `:652`, `:676`) are routing metadata attached to log records and metric series; they change a tag value, not what the pipeline admits or where it goes (the `filter/app_logs` keep set `:759-768` keys on `model`/`deployment`, which are unchanged). The header comment `0.80 static memory` (`:13`) **matches** the deployed flag on this revision — the sibling page's stale-comment INFO does not apply here.

**Priority-item sweep repeated on this file (not assumed):** `--log-requests-level 0` (`:196`) present; `--log-requests`, `--admin-api-key`, `--api-key`, `--allowed-media-domains`, `--disable-multimodal`, `--crash-dump-folder`, `--export-metrics-to-file`, `--enable-trace`, `--trust-remote-code`, `ulimits: core`, `pid: host`, `SYS_PTRACE`, `docker.sock` — all absent (grep-verified over the whole file). Zero `${VAR}` in the engine command. `OHTTP_ENABLED=true` literal (`:363`). Exporter endpoint literal (`:797`).

## Check 2 — Findings carried from the sibling page, re-located at this file's lines

Each heading below is the sibling's finding; the body states only what was re-verified here and anything that differs for this CVM. Full evidence and reasoning are on the [`d1cc22eb` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d1cc22eb8a25890e42a090d2c309b5045cdf374e000ed7228d9394f36f7f96d1.md).

### CRITICAL (deployed: ON, exposure) — the engine's unauthenticated logging switch is reachable over the shared bridge and through two bare location / listeners, and its output is shipped off-box by this file (sink: log / disk / telemetry)

Carried. `external: true` / `name: dstack_default` (`:527-530`); both engines bind `0.0.0.0:8000` (`:192-193`); nginx `location / { proxy_pass http://proxy-glm53:8000; }` at `:1010` (`:8000`) and `:1030` (`:443`), no allowlisted path anywhere; no `--admin-api-key` (`:163-196`); stdout exit via Check 5's pipeline. Mechanism per the engine page's CRITICAL. Identical here.

### HIGH (deployed: OFF, omission) — no --admin-api-key leaves the engine's admin surface ungated; one flag would bearer-gate /configure_logging (sink: log)

Carried; absent from `:163-196` and from the whole file.

### HIGH (deployed: ARMED) — watchdog hang dump armed at 1800 s over up to 32 in-flight requests per replica at 1M context (sink: log / telemetry)

Carried. `--watchdog-timeout 1800` (`:191`), `--max-running-requests 32` (`:171`), two replicas (`:391`, `:428`), `--context-length 1048576` (`:189`). No `--soft-watchdog-timeout`. No label carries a watchdog value; `request_logging:disabled` (`:405`, `:419`, `:442`, `:456`, `:654`, `:678`) is true at deployed flags and runtime-falsifiable. The mem-fraction change does not alter the in-flight ceiling (that is `:171`, unchanged).

### HIGH (deployed: ON, systemic) — no scrubbing at any hop; anything either engine replica prints leaves the CVM to telemetry.infra.near.ai (sink: telemetry)

Carried. json-file 100m x 10 (`:1-6`) → `filelog` include (`:552`) → `logs/app` processors `[memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch]` (`:815`) — none redacts — → `https://telemetry.infra.near.ai` (`:797`), bearer `${env:MONITORING_INGEST_TOKEN}` (`:799`), disk-backed queue (`:801-803`). `filter/app_logs` (`:759-768`) keeps records carrying `model`/`deployment`, which both replicas' labels do (`:405`, `:442`).

### MEDIUM (deployed: ON) — the glm47 tool-call parser this file selects logs a model-emitted tool-argument value verbatim at WARNING, and this file ships that line off-box (sink: log / telemetry)

Carried — the reason the class is LEAKS. `--tool-call-parser glm47` (`:187`) plus the pipeline above. Engine-side mechanism per the engine page (read from the image's layer bytes). Unaffected by the delta.

### HIGH (deployed: OFF, splice-armed) — two content-egress switches on the E2EE-terminating proxy are outside allowed_envs but inside the overlay channel this file already depends on (sink: egress)

Carried. `FUSION_ENABLED` / `FUSION_INTERNAL_BEARER_TOKEN` (`:365-366`), `WEB_CONTEXT_SEARCH_URL` / `_API_KEY` (`:371-372`), undefaulted; `MODEL_PROXY_TOKEN` (`:342`) and `CLOUD_API_USAGE_TOKEN` (`:359`) show the overlay is live (registrar exits without the former, `:846-849`). **Caveat for this CVM:** "outside `allowed_envs`" was established against the gpu30 harness's 22-entry list; this CVM's harness `c82b1a2e…` has no page, so its `allowed_envs` was not read (Not traced).

### MEDIUM (deployed: ON) — request-driven media fetch from the plaintext engine with no domain allowlist and multimodal not disabled (sink: egress, request-driven)

Carried. Neither `--allowed-media-domains` nor `--disable-multimodal` in `:163-196`; model multimodal per the engine page.

### MEDIUM (deployed: ARMED) — no ulimits core anywhere in the recipe, so a crashing engine replica writes its process memory as a core file into its container layer (sink: disk)

Carried at the recipe level: the only `ulimits` block is `x-nvidia` `:31-36` (memlock, nofile). **Substrate caveat:** the ARMED state rests on the [OS page](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) for guest OS `a6eafc5f…`, resolved for the **gpu30** CVM; this CVM's `os_image_hash` was not resolved here. If gpu04 runs the same 0.5.11 guest (likely for a fresh CVM on 2026-09-02, unverified) the finding is identical; the recipe-side omission is identical regardless.

### MEDIUM (deployed: ON, cross-link) — /root/.cache is a persisted named volume, so a runtime-enabled request pickle survives container recreate and redeploy (sink: disk)

Carried. `kernel_cache:/root/.cache` (`:198`), named volume (`:535`); HF mount nested (`:199`). Note: on a fresh CVM (first deploy 18:25:37Z) this volume starts empty — there is nothing carried over from an earlier revision here, unlike the gpu30 CVM which has redeployed six times onto the same volumes.

### MEDIUM (deployed: ON) — two open OTLP receivers with no scrubbing on the shared bridge, feeding the traces pipeline (sink: telemetry)

Carried. `0.0.0.0:4317` (`:547`), `0.0.0.0:4318` (`:549`), `expose` only (`:508-510`); `metrics/otlp` (`:821-824`), `traces` (`:829-832`).

### MEDIUM (deployed: ON) — dcgm-exporter is tag-pinned in a SYS_ADMIN container over all eight GPUs (supply chain)

Carried. `:218`, `:220-221`, `:474`; not host-published.

### INFO (deployed: ON) — the engine's attested identity is a build recipe, not an image digest; the :local tag is not digest-addressable (supply chain)

Carried (`:50-157`, `:161-162`). **For this CVM the concern is weaker:** the CVM started 4 s before this `compose_up`, so no earlier `:local` image can exist in its docker store — the running engine is necessarily this file's build of `FROM …@sha256:2488ca13…` (`:53`), gated by the two `sha256sum -c` checks (`:55-58`, `:115-119`).

### INFO (deployed: ON) — the proxy runs privileged as root with ipc: host and the dstack socket (structural)

Carried (`:39-41`, `:45`; engine `ipc: host` via `:159`).

### INFO (deployed: OFF, profile-gated) — hf-cleanup can evict the weights volume on operator demand; dormant, weights-side, fail-closed for the engine (disk)

Carried (`:252-303`; `profiles: ["maintenance"]` `:255`; `rm -rf` `:288` on `huggingface_cache` `:292`).

### LOW (deployed: ON) — HF_TOKEN is injected into the plaintext engine process although the engine is offline; readable through the engine's local-file media primitive (sink: egress, credential not content)

Carried (`:201` beside `:203`).

**Not carried:** the sibling's INFO on the stale `0.80` header comment — on this revision `:13` and `:170` agree. The sibling's INFO "one off-box log destination; Datadog labels inert" is **not asserted for this CVM** — it depends on the measured harness, which for gpu04 (`c82b1a2e…`) has no page (Not traced).

## Verdict

**LEAKS — narrowly, the sibling's verdict unchanged.** Seven in-line edits — a GPU memory fraction and the label strings that name it — have no privacy surface; every finding of the [`d1cc22eb` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d1cc22eb8a25890e42a090d2c309b5045cdf374e000ed7228d9394f36f7f96d1.md) was re-located at the identical line in this file and carries at the same severity and deployed state. What this file configures is clean apart from the glm47 line it selects and ships; what it fails to contain is the engine's live paths. The one thing this page can say less about than its sibling is the host: a fresh gpu04 CVM whose harness and guest OS are not yet published, so "exactly one telemetry destination" and the core-dump substrate are inherited assumptions here, not resolved facts. Recommendations as on the sibling page, in the same order. **A future revision of this recipe — the seventh in 24 h would be unsurprising — needs its own page.**

## Not traced

- **The measured harness `c82b1a2e…` (gpu04) and this CVM's `os_image_hash`** — no pages exist; not read. Consequences: (a) whether a Datadog agent is measured on this host (a second, third-party destination for engine stdout) is **open**; (b) the `allowed_envs` list assumed for the splice-armed HIGH is gpu30's; (c) the core-dump substrate is inferred from the gpu30 guest OS page.
- **Whether the action log on CVM `7bb9af0ac5b0…` still pins this `file_sha256`** — asserted by the orchestrator's capture (fetched at `d36548db`, hash-checked); the file's own hash was re-verified here, the log was not re-fetched.
- **Why this CVM received `0.80` while gpu30 received `0.77` at nearly the same time** — the tags (`v0.0.380` vs `gpu30-glm53-tp4-r1-mem077-20260902`) suggest a release-tagged deploy versus a host-specific tuning branch; not privacy-relevant, but it means the two CVMs behind one hostname are on different compose-manager commits.
- Everything in the sibling page's Not traced applies here unchanged (proxy path filtering and fail-closed semantics for empty `WEB_CONTEXT_SEARCH_*`/`FUSION_*`; compose-manager `--build` behaviour; other sglang print sites; `uvx` resolution of `huggingface_hub`; nginx `client_body_temp`; the chat template at `3f1971b7…`; dcgm NVML visibility; the otelcol queue directory).
