# cvm-compose-files prod/GLM-5.3-Flash-SGL-TP4.yaml @ e1410790... (GLM-5.3-Flash two-replica sglang TP4 node; registry-digest engine revision; delta = in-enclave build replaced by a signed registry image, two new verification-profile services, digest-pinned dcgm, proxy bump)

## verdict: LEAKS — narrowly, the same class as the d76413cd base and for the same reason: this revision still selects the glm47 tool-call parser (whose deployed image — now docker.io/nearaidev/sglang@a7b7136a, a signed-provenance registry build replacing the in-enclave Phala build — logs a model-emitted tool-argument value at WARNING) and still ships every engine stdout line unscrubbed to https://telemetry.infra.near.ai. Of the 143-line delta, nothing touches a logging, dump, disk or telemetry switch: two new engine flags are an image-processor selection and a 64-image count cap, the proxy gains a memory-only conversation-affinity flag, dcgm becomes digest-pinned with a counters-only metrics file (resolving the base's tag-pin MEDIUM), the registrar gains error handling, nginx gains a DNS resolver, and two operator-profile services appear that a plain compose up never starts — one of which (the soak relay) is a second host-published non-OHTTP ingress to each engine with a bearer gate and a six-path allowlist excluding /configure_logging, and was started on gpu04 from a Canary file on 2026-09-05 with no compose_down since. Every containment the base declined is still declined: no --admin-api-key, the 1800 s watchdog dump, no media domain allowlist, no ulimits core, whole-.cache persisted volume, two bare location / listeners, the splice-armed proxy egress switches (re-verified outside both harnesses' 22 allowed_envs). Novel here: the action logs show the running containers on both CVMs are a patchwork of five revisions across two files — only proxy-glm53 was created from this hash; the collector that decides where engine stdout goes and the nginx that decides reachability were created from Canary revisions this hash does not name. I diffed each of those blocks against this file and they are equivalent today; the attestation alone cannot tell you that. Keyed to exactly this hash.

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/GLM-5.3-Flash-SGL-TP4.yaml` |
| **file_sha256** | `2878b9c4c6db512a7538dc4e8981fd5acae72cdaace7794509f48161bcce08e3` (re-verified locally: `shasum -a 256` over the captured 1174-line file returns exactly this value) |
| **serves** | `z-ai/glm-5.3-flash` at `glm-5-3-flash.completions.near.ai` (registrar `:1006`; nginx `server_name` `:1159-1161` also admits `-stg` and `glm-5-3-flash-i<N>` hosts). Two CVMs on two hosts share the hostname (gpu04, gpu02); both carry this revision **and** the sibling [`e5d80ab6`](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-e5d80ab6e1a1188fdf8cd3e7bc2f74da5d4767da15b0644acf07dad08a3b461b.md) in their signed action logs, service-scoped (see identity and Check 8) |
| **topology** | two sglang **TP4/EP4** replicas (`model-sg-glm53-fp8-tp4-r1` GPUs 0-3 `:302-338`, `-r2` GPUs 4-7 `:339-377`) behind one `vllm-proxy-rs` (`proxy-glm53` `:261-301`, `VLLM_BACKEND_URLS` naming both `:275`), behind nginx (`:212-230`) with two listeners (`8000:8000`, `8444:443` `:219-220`), both bare `location /` (`:1148-1151`, `:1170-1173`). Plus `model-downloader` (run-once `:134-158`), `hf-cleanup` (`maintenance` profile `:160-210`), `model-proxy-registrar` (`:232-259`), `dcgm-glm53` (`:451-478`), `otelcol-contrib` (`:480-510`), and — **new** — `glm53-perception-check` (`:378-418`, `verification` profile) and `glm53-soak-relay` (`:420-447`, `verification` profile, host ports `8008`, `8009` `:433-434`) |
| **engine** | `docker.io/nearaidev/sglang@sha256:a7b7136a…` (`:62`; also the perception-check image `:379`) — [audited](/images/docker.io/nearaidev/sglang/sha256-a7b7136abcf5e07522289d96e96fec9b42a1a30f9dfda57e957f142680d2d67b.md), verdict **LEAKS, narrowly**; identity there is a signed GitHub build attestation to a checksum-pinned source tarball diffed against tree `fc91d24`. The base's in-enclave `x-glm53-build` (Phala `2488ca13…` + inline `protocol.py` patch) is **gone** from this file |
| **proxy** | `nearaidev/vllm-proxy-rs@sha256:98b57ad7…` (`:47`) — [audited](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md), QUALIFIED PASS (two client-opt-in egress features live-or-latent; no engine path allowlist except a `/v1/responses` refusal). The sibling `e5d80ab6` names [`27265f5a…`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-27265f5a10abcacc3fe80604a00ed2ba32c6e082895115e4b652b40525cb554d.md); the proxy container on both CVMs was last recreated from **this** file |
| **runs on** | CVM `7bb9af0ac5b0…` (`INSTANCE_LABEL=gpu04`, measured harness [`c82b1a2e…`](/manifests/measured/sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md), QUALIFIED PASS) and CVM `fccd496ae28c…` (`gpu02`, harness [`533e43fd…`](/manifests/measured/sha256-533e43fd215d56ec6cd719adb5defe6615f7ba6ae0b018e832f7c8dadf2e25d4.md), QUALIFIED PASS — a one-line `INSTANCE_LABEL` delta of gpu04's, verified by `diff` of the two captured harness composes). Both report `os_image_hash a6eafc5f…` ([OS page](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md), `dstack-nvidia-0.5.11`, QUALIFIED PASS). Both harnesses: **no Datadog agent measured**, the same 22-entry `allowed_envs` (read from both captured app-compose documents, not assumed) |
| **identity** | log-pinned, **service-scoped**: the signed compose-manager action logs (43 entries gpu04, 24 entries gpu02; `actions_hash` bound into `report_data`) record `compose_up` at commit `e14107901d202215ab44eb027eb4792047d5ec83`, tag `v0.0.409`, file_sha256 `2878b9c4…`, `services: [proxy-glm53]` — gpu04 2026-09-09T23:44:01Z and 23:50:56Z; gpu02 23:44:55Z and 23:50:57Z. **No full `compose up` of this file appears in either log**: this hash created exactly one container per CVM. The orchestrator fetched the file at that commit and hash-checked it (match); re-hashed here (match). **Not hardware-measured** |
| **review** | reviewed 2026-09-10 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** full delta review against the [`d76413cd` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d76413cd231a4e871a515e4fa829c6510a374fe335e77fae3ea5a6dccf783909.md) (itself a one-number delta of the [`d1cc22eb` full review](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d1cc22eb8a25890e42a090d2c309b5045cdf374e000ed7228d9394f36f7f96d1.md); both read in full for this page). `git diff d3fa2713..e1410790 -- prod/GLM-5.3-Flash-SGL-TP4.yaml` is +275/−132 over 20 hunks and 7 main commits (`1fd14f6` #217, `3e3fa7c` #218, `13e82f9` #227, `c5d1f8c` #228, `46fe7b0` #232, `0eb5c6d` #244, `676532b` #249, `e141079` #250); the file grows 1031 → 1174 lines, so **no base line number survives** — every citation below was re-located by `grep -n` / `sed -n` on the captured file. Rather than reading the patch linearly, every top-level anchor, service, config, volume and network block was extracted from both files and diffed block-by-block (results in Check 1); the 20 hunks were then checked against that list to confirm nothing fell outside it. The sibling `e5d80ab6` was `diff`ed against this file: **one line** (`:47`, proxy digest). Twelve intermediate revisions named by the two action logs (`a648cda7`, `455106d1`, `a2d3f073`, `a7549a89`, `e5d80ab6`, and the Canary file's `30f4456e`, `1072fbb4`, `ee6a8d31`, `710278fc`, `5a5b7228`) were staged and each re-hashed; the blocks that created still-running containers were diffed against this file (Check 8). Engine behaviour is deferred to the [engine page](/images/docker.io/nearaidev/sglang/sha256-a7b7136abcf5e07522289d96e96fec9b42a1a30f9dfda57e957f142680d2d67b.md) (its Results section was read and its findings are carried at this file's lines), proxy behaviour to the [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md), the collector to [`otelcol-contrib@85ac41c2`](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md). The new dcgm digest `613ab03c…` has no page yet (under concurrent review); the audited sibling is [`ed594cf5`](/images/nvcr.io/nvidia/k8s/dcgm-exporter/sha256-ed594cf53fe6942e84b07b0740cdcbb249fa4b39cb21feeebf93881ae51f0b5e.md).

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta review)

> Part of the near.ai inference-path privacy audit (Fable, 2026-09-10).
> Method: [/notes/method.md](/notes/method.md) (§4 Manifest-only checks) · Base: [d76413cd](/manifests/nearai/cvm-compose-files/prod/GLM-5.3-Flash-SGL-TP4/sha256-d76413cd231a4e871a515e4fa829c6510a374fe335e77fae3ea5a6dccf783909.md) · Engine: [a7b7136a](/images/docker.io/nearaidev/sglang/sha256-a7b7136abcf5e07522289d96e96fec9b42a1a30f9dfda57e957f142680d2d67b.md) · Proxy: [98b57ad7](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) · Harness: [c82b1a2e](/manifests/measured/sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md) / [533e43fd](/manifests/measured/sha256-533e43fd215d56ec6cd719adb5defe6615f7ba6ae0b018e832f7c8dadf2e25d4.md) · OS: [a6eafc5f](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md)

## Check 1 — The complete delta d76413cd → 2878b9c4, block by block

Every top-level block of both files, diffed. Base line ranges are the `d76413cd` file's; new ranges are this file's.

| block | base | here | result |
|---|---|---|---|
| header comment | `:1-27` | `:1-35` | rewritten: "canonical … for 8x H200 hosts", engine "published by the repository's GitHub Actions workflow and pinned here by its signed immutable registry digest", source `fc91d24`. Prose only |
| `x-logging-conf` | `:1-6` | `:4-9` | **identical** (json-file, 100m x 10, two labels) |
| `x-nvidia` | `:28-35` | `:37-44` | **identical** (`runtime: nvidia`, `ipc: host`, `ulimits` memlock/nofile only) |
| `x-vllm-proxy-common` | `:37-48` | `:46-57` | one line: proxy digest `b183677a…` → **`98b57ad7…`** (`:47`); `user: root` / `privileged: true` / dstack socket / certs unchanged |
| `x-glm53-build` | `:50-157` | — | **removed** (108 lines: Phala `FROM`, two `sha256sum -c` gates, inline `protocol.py` patch, self-tests) |
| `x-sg-glm53-flash-common` | `:158-215` | `:59-118` | `image:`/`build:` → `docker.io/nearaidev/sglang@sha256:a7b7136a…` (`:62`); command **+2 flags** `--disable-fast-image-processor` (`:97`), `--limit-mm-data-per-request '{"image": 64}'` (`:98`); env **+1** `SGLANG_ENABLE_STRICT_MEM_CHECK_DURING_IDLE=1` (`:115`). **Every other flag byte-identical**, including `--mem-fraction-static 0.80` (`:70`) — the base's only delta from `d1cc22eb` is carried, not reverted |
| `x-dcgm-common` | `:217-223` | `:120-131` | tag `4.5.2-4.8.1-distroless` → **digest `613ab03c…`** (`:121`); **+** `command: ["-f", "/etc/dcgm-exporter/nearai-h200.csv"]` (`:122`); **+** `configs:` mount of `dcgm_h200_metrics` at that path, mode 0444 (`:126-129`). `SYS_ADMIN` (`:124-125`) unchanged |
| `model-downloader` | `:226-251` | `:134-158` | **identical** |
| `hf-cleanup` | `:252-302` | `:160-210` | **identical** |
| `nginx` (service) | `:304-322` | `:212-230` | **identical** |
| `model-proxy-registrar` (service) | `:324-352` | `:232-259` | **identical** |
| `proxy-glm53` | `:353-390` | `:261-301` | **+1 env** `VLLM_BACKEND_CONVERSATION_AFFINITY=1` (`:278`, with a two-line comment `:276-277`). All other env lines identical (Check 4) |
| `model-sg-glm53-fp8-tp4-r1` / `-r2` | `:391-427` / `:428-464` | `:302-338` / `:339-377` | only the `config_variant` string, `phala-r12-…` → `official-upstream-fc91d24-…`, in the `com.datadoghq.ad.logs` tag (`:316`, `:353`) and `nearai.otel.config_variant` (`:328`, `:365`). `request_logging:disabled` / `"disabled"` carried (`:316`, `:330`, `:353`, `:367`) |
| `glm53-perception-check` | — | `:378-418` | **new** (Check 3) |
| `glm53-soak-relay` | — | `:420-447` | **new** (Check 3) |
| `dcgm-glm53` (service) | `:466-493` | `:451-478` | **identical** (device_ids 0-7 `:459`) |
| `otelcol-contrib` (service) | `:495-525` | `:480-510` | **identical** |
| `networks`, `volumes` | `:527-538` | `:512-523` | **identical** (`external: true` / `dstack_default` `:514-515`; `kernel_cache` `:520`; `certs` external `:521-523`) |
| `glm53_soak_nginx_conf` | — | `:526-580` | **new** (Check 3) |
| `dcgm_h200_metrics` | — | `:582-652` | **new** (Check 7) |
| `otelcol_app_config` | `:540-837` | `:654-951` | two `config_variant` static labels (`:765`, `:789`), **+** `scrape_timeout: 10s` on the dcgm job (`:794`). **No receiver, processor, exporter, pipeline or filter changed** (Check 5) |
| `registrar_script` | `:839-975` | `:953-1107` | `curl -sS` → `curl -fsS` on the three registration POSTs (`:972`, `:980`, `:987`); registration failures now return 1 and retry (`:1002-1009`); **new** one-time `unregister_endpoint "${HOST_IP}:8001"` legacy cleanup (`:1016-1020`); `mark_registered` failure counting (`:1080-1092`). Probe body, URLs and token handling unchanged (Check 6) |
| `nginx_conf` | `:976-1031` | `:1108-1174` | **+** `resolver 127.0.0.11 valid=5s ipv6=off` (`:1143`) and `proxy_pass` via a variable (`:1149-1150`, `:1171-1172`) so a recreated proxy container is re-resolved. Log format, `access_log`, buffering, body size, server blocks otherwise identical |

The 20 hunks of the git patch map onto exactly these rows; nothing in the diff falls outside the table.

### INFO (deployed: ON) — the delta replaces the in-enclave build with a signed registry digest, adds two image-handling engine flags, a memory-only proxy affinity flag, a digest-pinned dcgm with a counters-only metrics file, registrar error handling and an nginx resolver; no logging, dump, disk, telemetry or egress switch changes (sink: none)

Each changed line was classified against the six sinks. The two engine flags: `--disable-fast-image-processor` (`:97`) selects the slow HF image processor — the engine page confirms it is remapped, not ignored, and it touches no log path; `--limit-mm-data-per-request '{"image": 64}'` (`:98`) is a per-request **count** cap enforced in the tokenizer manager (engine page), not a source restriction — it does not narrow the media-fetch MEDIUM below. `SGLANG_ENABLE_STRICT_MEM_CHECK_DURING_IDLE=1` (`:115`) is a memory-invariant assertion during idle; the engine page's grep of the tree for log/print/open sites in the fork delta does not implicate it. `VLLM_BACKEND_CONVERSATION_AFFINITY=1` (`:278`) is treated in Check 4. The dcgm command/config (`:122`, `:126-129`) selects which GPU counters are exported (Check 7). The registrar's `-f` and retry logic change exit codes, not bodies. The nginx resolver changes name resolution timing, not routing: both listeners remain bare pass-throughs to `proxy-glm53:8000`.

**Priority-item sweep repeated on this file (not assumed):** `--log-requests-level 0` (`:96`) present; `--log-requests`, `--admin-api-key`, `--api-key`, `--allowed-media-domains`, `--disable-multimodal`, `--crash-dump-folder`, `--export-metrics-to-file`, `--enable-trace`, `--enable-hierarchical-cache`, `--soft-watchdog-timeout`, `--trust-remote-code`, `ulimits: core`, `pid: host`, `SYS_PTRACE`, `docker.sock`, `build:`, `dockerfile` — **all absent** (grep-verified over the whole 1174-line file; the only `ulimits` block is `:40-44`, the only `cap_add` are dcgm's `SYS_ADMIN` `:124-125` and the soak relay's `CHOWN, SETUID, SETGID` `:426`). The only `${VAR}` in the engine anchor `:59-118` is `HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` (`:103`). `OHTTP_ENABLED=true` literal (`:271`). Exporter endpoint literal (`:911`).

## Check 2 — Engine anchor `x-sg-glm53-flash-common` (`:59-118`) and both replicas

Command `:63-98`, merged by both replicas (`<<: *sg-glm53-flash-common` `:303`, `:340`). Inherits `x-nvidia` (`:37-44`: `runtime: nvidia`, **`ipc: host`** `:39`, memlock/nofile only). `init: true` (`:61`), `restart: unless-stopped` (`:116`), `stop_grace_period: 5m` (`:117`), `logging: *logging-conf` (`:118`). Volumes `kernel_cache:/root/.cache` (`:100`) and `huggingface_cache:/root/.cache/huggingface` (`:101`, nested). Env `:102-115`: `HF_TOKEN` (`:103`), **`HF_HUB_OFFLINE=1`** (`:105`), `TRANSFORMERS_OFFLINE=1` (`:106`), `HF_HUB_DISABLE_TELEMETRY=1` (`:107`), `DO_NOT_TRACK=1` (`:108`), `NCCL_DEBUG=WARN` (`:111`), `TORCHINDUCTOR_CACHE_DIR` / `TRITON_CACHE_DIR` on `/root/.cache` (`:113-114`). The load-bearing flags, re-located: `--max-running-requests 32` (`:71`), `--kv-cache-dtype bfloat16` (`:77`), `--tool-call-parser glm47` (`:87`), `--context-length 1048576` (`:89`), `--watchdog-timeout 1800` (`:91`), `--host 0.0.0.0` / `--port 8000` (`:92-93`), `--enable-metrics` / `--enable-cache-report` (`:94-95`), `--log-requests-level 0` (`:96`). Weights and chat template pinned by full SHA on a local snapshot (`:65-66`, `:88`), matching the downloader (`:143`, `:145`). No `cap_add`, `privileged`, `pid: host` or `ulimits: core` on the engine.

### HIGH (deployed: OFF, omission) — no --admin-api-key leaves the engine's admin surface ungated; one flag would bearer-gate /configure_logging (sink: log)

Carried; absent from `:63-98` and from the whole file. The engine page confirms at `fc91d24` that `/configure_logging` is `ADMIN_OPTIONAL`, that with neither key the auth middleware is never installed, and that the runtime switch enables verbatim prompt+output logging and arbitrary-path request pickling in-process. The single highest-value change available to this document, still not taken across seven commits.

### HIGH (deployed: ARMED) — watchdog hang dump armed at 1800 s over up to 32 in-flight requests per replica at 1M context (sink: log / telemetry)

Carried. `--watchdog-timeout 1800` (`:91`), `--max-running-requests 32` (`:71`), two replicas (`:302`, `:339`), `--context-length 1048576` (`:89`); no `--soft-watchdog-timeout`. The engine page re-traces the handler at these bytes (`Req.__repr__` with `input_ids`/`output_ids` at ERROR, undisableable, then SIGQUIT) — the dump leaves via Check 5 before the process dies. The new `SGLANG_ENABLE_STRICT_MEM_CHECK_DURING_IDLE=1` (`:115`) is a memory-invariant assertion during idle, not a watchdog control. Labels: no label carries a watchdog value; `request_logging:disabled` (`:316`, `:330`, `:353`, `:367`, `:767`, `:791`) is true at deployed flags and runtime-falsifiable.

### INFO (deployed: ON) — the engine's attested identity is now a registry digest with a signed build attestation; the base's ":local tag not digest-addressable" concern is resolved, and the stale-image window it warned about did open on gpu04 in between (supply chain)

The base's INFO said the running engine bytes were whatever docker held under `nearai/glm53-sglang-r12-budget:local`, and that the distinction "stops being academic the first time the patch changes and a stale `:local` survives". This file names `docker.io/nearaidev/sglang@sha256:a7b7136a…` (`:62`) — a digest, with no `build:` anywhere (grep-verified), whose [page](/images/docker.io/nearaidev/sglang/sha256-a7b7136abcf5e07522289d96e96fec9b42a1a30f9dfda57e957f142680d2d67b.md) verifies a signed GitHub build attestation to a checksum-pinned tarball diffed clean against tree `fc91d24`. The concern is closed for this revision. It was live in between: gpu04's log shows `model-sg-glm53-fp8-tp4-r1`/`-r2` recreated from five different in-enclave-build revisions between 2026-09-05 and 2026-09-06 (`455106d1`, `a648cda7`, `d76413cd`, Canary `1072fbb4`, `ee6a8d31`, `710278fc`), all sharing the `:local` tag — whether each up rebuilt the image is not visible in the log (Not traced). On 2026-09-09 both replicas on both CVMs were recreated from `e5d80ab6`, whose engine anchor is byte-identical to this file's (Check 8), so the running engines are this digest.

### LOW (deployed: ON) — HF_TOKEN is injected into the plaintext engine process although the engine is offline; readable through the engine's local-file media primitive (sink: egress, credential not content)

Carried. `HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` (`:103`) beside `HF_HUB_OFFLINE=1` (`:105`); the engine page confirms `file://` and absolute paths are read locally by the image loaders at these bytes. Only the downloader (`:150`) needs it.

## Check 3 — New services: `glm53-perception-check` (`:378-418`) and `glm53-soak-relay` (`:420-447`)

Both carry `profiles: ["verification"]` (`:381`, `:423`) — **not started by a plain `compose up`**, only by an explicit operator invocation with that profile or by name. Both have `restart: "no"` (`:387`, `:428`). The action logs show what actually happened: `glm53-perception-check` was brought up by name **three times on each CVM** on 2026-09-09 (gpu04 22:44Z, 23:08Z, 23:47Z; gpu02 23:29Z, 23:47Z, 23:53Z), each from `e5d80ab6` (identical block, Check 8); `glm53-soak-relay` was brought up by name **once, on gpu04 only**, from Canary `1072fbb4` at 2026-09-05T18:26:46Z (identical block and config to this file's), and **no `compose_down` for it appears in the 43-entry log**.

**`glm53-perception-check`** — the engine image (`:379`) run as `runtime: runc` (`:382`), `user: "65534:65534"` (`:383`), `read_only: true` (`:384`), `cap_drop: [ALL]` (`:385`), `no-new-privileges` (`:386`), 512m/1 cpu (`:388-389`), `NVIDIA_VISIBLE_DEVICES=void` (`:391`), no volumes, no ports, no credentials. It runs an inline python program (`:393-413`) that prints a disk-free JSON line (`:397`), then for each replica by service name (`:400`, **bypassing the proxy**) GETs `/health` and `/v1/models` (`:402-406`) and `runpy`s `/sgl-workspace/sglang/test/manual/test_glm5_next_perception.py` with `--base-url`, `--size 512`, `--concurrency 1` (`:407-409`). The engine page read that script: it builds PNG fixtures in-process, posts them as `data:` URLs to `/v1/chat/completions`, and prints one JSON line per case with the fixture hashes, status and token counts — and, **only on failure, the first 400 characters of the model's answer**. Its logging is the shared json-file driver (`:414`) and its label carries `model:` and `deployment:` (`:416`), so its stdout passes `filter/app_logs` (`:877`) and ships to `telemetry.infra.near.ai`. The content is operator-authored synthetic fixtures and the model's answer to them — never a user's message. The perception check does not go through nginx or the proxy, so it neither exercises nor bypasses OHTTP; it is a direct client of the engine's unauthenticated port.

### INFO (deployed: OFF, profile-gated) — the perception-check prints up to 400 characters of model output for synthetic fixtures straight to the shipped stdout stream; ran three times per CVM on 2026-09-09, never with user content (sink: log / telemetry)

As above. Noted because it is the one service in this recipe that deliberately prints model output, and its lines leave the CVM; the input is the operator's, so no user plaintext is involved. A failed qualification run would ship 400-character slices of the model's description of a synthetic image.

**`glm53-soak-relay`** — nginx (`:421`, same digest as the serving nginx) as `runtime: runc` (`:424`), `cap_drop: [ALL]` + `cap_add: [CHOWN, SETUID, SETGID]` (`:425-426`), `no-new-privileges` (`:427`), 256m (`:429`), **host-published `8008:8008`, `8009:8009`** (`:432-434`), `certs:/etc/letsencrypt:ro` (`:435-436`), tmpfs for `/var/cache/nginx` and `/var/run` (`:437-439`), config `glm53_soak_nginx_conf` at `/etc/nginx/nginx.conf` mode 0600 (`:441-444`). Its label carries `model:`/`deployment:` (`:447`) — but nginx here is told to log nothing. The config (`:526-580`), verified line by line:

- `error_log /dev/null` (`:531`); **`access_log off`** (`:535`) — no per-request record at all (unlike the serving nginx's json access log).
- `client_max_body_size 100m` (`:536`); **`proxy_request_buffering off`** (`:549`), `proxy_buffering off` (`:550`), **`proxy_max_temp_file_size 0`** (`:551`) — request and response bodies stream through without a temp file (the serving nginx does buffer request bodies over 1m to disk, Check 6).
- Bearer gate: `map $http_authorization $soak_authorized` (`:541-544`) matching exactly `"Bearer ${PROXY_TOKEN:?…}"` (`:543`) — interpolated at compose time, fail-closed if `PROXY_TOKEN` is unset (`:?`); each server block returns 401 otherwise (`:561`, `:572`). **The same `PROXY_TOKEN` the registrar's liveness probe uses** (`:958`, `:1027`), from `allowed_envs`.
- `proxy_set_header Authorization ""` (`:555`) — the bearer is stripped before the engine (which has no auth anyway).
- Two servers: `listen 8008 ssl` → `model-sg-glm53-fp8-tp4-r1:8000` (`:559`, `:562`); `listen 8009 ssl` → `-r2` (`:570`, `:573`). Each has **one regex location** `^/(health|metrics|v1/models|v1/chat/completions|v1/completions|v1/responses)$` (`:563`, `:574`) with `limit_except GET POST { deny all; }` (`:564`, `:575`), and **`location / { return 404; }`** (`:567`, `:578`). The regex is anchored with `$`, so `/configure_logging`, `/server_info`, `/set_trace_level`, `/v1/chat/completions/anything` and query-string tricks (`$request_uri` is forwarded, but the `location` match is on the path only) do **not** reach the engine through this relay. `/metrics` is reachable — content-free per the engine page.

### LOW (deployed: ARMED, profile-gated) — glm53-soak-relay is a second host-published TLS ingress straight to each engine replica, bypassing OHTTP and the proxy, bearer-gated with a six-path allowlist that excludes /configure_logging; started on gpu04 from a Canary file on 2026-09-05 and never logged down (sink: egress, ingress bypass)

When active, an operator holding `PROXY_TOKEN` can send **plain-HTTPS, non-OHTTP** chat requests to either replica on `<host>:8008`/`:8009` — the relay nginx sees that traffic in plaintext (it terminates TLS) and the request never passes the E2EE terminator. What the relay adds as a *sink* is nothing: it logs nothing, buffers nothing to disk, and forwards only six paths with two methods. What it adds as *exposure* is a second unattested-client route to the engine's port that does not carry the proxy's per-request bearer semantics (any holder of the deployment token, not a cloud API key) — bounded by the allowlist, which was checked to exclude every admin route the engine page names. Deployed state: profile-gated in this file; **on gpu04 the log shows it was started 2026-09-05T18:26:46Z (Canary `1072fbb4`, block identical to `:420-447` / `:526-580`) and no `compose_down` names it since** — with `restart: "no"` a container survives until stopped, but the `docker_restart` event on 2026-09-06T17:26Z would not have restarted a `restart: "no"` container unless docker live-restore is on; whether it is running today is not in the evidence (Not traced). On gpu02 it was never started. LOW rather than MEDIUM because no sink is added and the traffic is the operator's own; ARMED rather than OFF because the ports are host-published the moment the profile is activated, and on one CVM it was.

## Check 4 — `proxy-glm53` (`:261-301`), the E2EE terminator

Inherits `x-vllm-proxy-common` (`:46-57`): `nearaidev/vllm-proxy-rs@sha256:98b57ad7…` (`:47`), `user: root` (`:48`), **`privileged: true`** (`:49`), `<<: *nvidia` (`:50` → `ipc: host`), `extra_hosts: compose-manager:host-gateway` (`:51-52`), `/var/run/dstack.sock` (`:54`), `certs:ro` (`:55`). Environment `:264-283`, every line re-read: `NVIDIA_VISIBLE_DEVICES=all` (`:265`), `CLOUD_API_URL=https://cloud-api.near.ai` (`:266`), `CLOUD_API_USAGE_TOKEN=${…}` (`:267`, undefaulted, not in `allowed_envs`), `COMPOSE_MANAGER_URL=http://compose-manager:8080` (`:268`), `LOG_FORMAT=json` (`:269`), `MODEL_NAME=z-ai/glm-5.3-flash` (`:270`), **`OHTTP_ENABLED=true`** (`:271`), `TOKEN=${PROXY_TOKEN}` (`:272`), **`FUSION_ENABLED=${FUSION_ENABLED}`** / **`FUSION_INTERNAL_BEARER_TOKEN=${…}`** (`:273-274`), `VLLM_BACKEND_URLS=…r1:8000,…r2:8000` (`:275`), **`VLLM_BACKEND_CONVERSATION_AFFINITY=1`** (`:278`, new), `VLLM_PROXY_MAX_REQUEST_SIZE=104857600` (`:279`), `TLS_CERT_PATH` (`:280`), `USE_NV_ATTESTATION_SDK=true` (`:281`), **`WEB_CONTEXT_SEARCH_URL=${…}`** / **`_API_KEY=${…}`** (`:282-283`). Labels `:285-299` (`model:`/`deployment:` in the Datadog tag `:285`; `nearai.otel.*` metadata — inert, the collector uses `static_configs`).

### INFO (deployed: ON) — VLLM_BACKEND_CONVERSATION_AFFINITY=1 is new and fleet-unique; per the proxy pages it keys on a salted, memory-only conversation digest and emits only enum and backend-index metric labels (sink: none)

`:278` is the only place in the fleet this flag is set (the [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) tabulates every proxy service across four recipes). Its purpose (`:276-277`) is to keep append-only conversations on the replica holding their prefix cache. The proxy source review finds the affinity key is the same salted digest already used for chat caching, confined to memory, with three new `backend_affinity_*` metric series whose labels are enums and backend indices — scraped by this file's `inference-proxy-proxy-glm53` job (`:812-816`) and shipped. Nothing content-bearing is written or sent; recorded so the growth in the exported stream is visible. Not re-derived from the Rust tree here.

### HIGH (deployed: OFF, splice-armed) — two content-egress switches on the E2EE-terminating proxy are outside allowed_envs but inside the overlay channel this file already depends on (sink: egress)

Carried, and the base's caveat is now closed: `FUSION_*` (`:273-274`) and `WEB_CONTEXT_SEARCH_*` (`:282-283`) are interpolated, undefaulted, and absent from **both** harnesses' `allowed_envs` — the 22-entry lists were read from the captured gpu04 and gpu02 app-compose documents and are identical (`BEARER_TOKEN`, `CM_MIN_TAG_AGE_HOURS`, `SLACK_WEBHOOK_URL`, AWS x4, `CERTBOT_EMAIL`, `CLOUDFLARE_API_TOKEN`, `DOCKER_REGISTRY_*` x2, `LAUNCHER_CHANNEL`, `DD_API_KEY`, `HUGGING_FACE_HUB_TOKEN`, `PROXY_TOKEN`, `ENV`, `HOST_IP`, `DD_HOSTNAME`, `HOSTNAME`, `MONITORING_INGEST_TOKEN`, `CVM_NAME`, `CVM_HOST`). The overlay channel is demonstrably live: `MODEL_PROXY_TOKEN` (`:250`) and `CLOUD_API_USAGE_TOKEN` (`:267`) are outside the list too, and the registrar exits if the former is empty (`:960-963`). The proxy page rates these branches CRITICAL (web search: a model-written query derived from the decrypted conversation to a third-party API) and HIGH (fusion: the full plaintext conversation to runtime-listed endpoints), both **client-opt-in and gated on these env values**, whose deployed state is not visible in any attested document. This page keeps the base's classification — OFF at what this file declares, armed by a channel it relies on — and notes the proxy page's severities for the populated case.

### INFO (deployed: ON) — the proxy runs privileged as root with ipc: host and the dstack socket (structural)

Carried (`:48-50`, `:54`; engine `ipc: host` via `:60`). The proxy applies no path allowlist except a fixed-string refusal of `/v1/responses` (proxy page MEDIUM), so an accepted bearer still reaches any engine route (Check 6).

## Check 5 — Telemetry pipeline (`otelcol-contrib` `:480-510`, config `:654-951`)

**Result unchanged from the base: a fixed, unscrubbed path from every app container's stdout to one literal off-box endpoint.** Diffed hunk by hunk against `d76413cd:540-837`: the only differences are two `config_variant` label strings (`:765`, `:789`) and `scrape_timeout: 10s` on the dcgm job (`:794`). Verified at this file's lines: `otlp` receivers `0.0.0.0:4317` / `:4318` (`:660`, `:662`); `filelog/docker_containers` `include: [/var/lib/docker/containers/*/*-json.log]` (`:665`), `start_at: end` (`:666`), json parse → `body` move → regex lifts of `model:`, `served_model`, `model_path`, `precision`, `deployment:`, `ip`, `port`, `env`, `host`, `instance`, `replica` from the Datadog label (`:689-736`), `service.name: cvm-docker` (`:737-739`); `prometheus/apps` (`:741`) scraping r1 (`:744-767`), r2 (`:768-791`), dcgm (`:792-811`), proxy (`:812-827`) — **no scrape job for the perception-check or the soak relay**, and neither exposes `/metrics` of its own; `memory_limiter` (`:838`), `resource` (`:843`), `transform/docker_metadata` (`:861`), `filter/app_logs` (`:873-882`; drop rule `attributes["model"] == nil and attributes["deployment"] == nil` `:877`, five management names `:878-882`), `groupbyattrs` (`:884`), `batch` (`:904`); `otlphttp/gateway` → `https://telemetry.infra.near.ai` (`:910-911`), `Bearer ${env:MONITORING_INGEST_TOKEN}` (`:913`), disk-backed queue (`:914-917`) on `/var/lib/otelcol-contrib/storage` (`:920-922`); pipelines `logs/app` (`:927-930`, processors `:929` — none redacts), `metrics/app` (`:931`), `metrics/otlp` (`:935`), `metrics/self` (`:939`), `traces` (`:943`); self-telemetry `level: info` (`:951`). Service: same digest `85ac41c2…` (`:481`), `user: "0:0"` (`:484`), `/var/lib/docker/containers:ro` (`:497`), `expose` only (`:493-495`), own driver `local` (`:504-508`).

**Who passes the keep-filter here** — every service whose label carries `model:` **or** `deployment:`: downloader (`:153`), nginx (`:230`), registrar (`:258`), proxy (`:285`), r1 (`:316`), r2 (`:353`), **perception-check (`:416`)**, **soak relay (`:447`)**, dcgm (`:462`); otelcol's own label has `deployment:` only (`:510`) but it is dropped by name (`:881`) and does not tail itself. `hf-cleanup` has no label (`:203-208`) and is dropped.

### HIGH (deployed: ON, systemic) — no scrubbing at any hop; anything either engine replica prints leaves the CVM to telemetry.infra.near.ai (sink: telemetry)

Carried. Chain: engine stdout → json-file ~1 GB retained (`:4-9`) → filelog (`:665`) → `[memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch]` (`:929`) → `https://telemetry.infra.near.ai` (`:911`). The engine page's deployed-ON MEDIUM (glm47), CRITICAL (`/configure_logging`), HIGH (watchdog) and its LOWs (grammar strings on failure, py-spy stacks on crash) all terminate here.

### MEDIUM (deployed: ON) — the glm47 tool-call parser this file selects logs a model-emitted tool-argument value verbatim at WARNING, and this file ships that line off-box (sink: log / telemetry)

Carried — and re-based on the new engine: the [`a7b7136a` page](/images/docker.io/nearaidev/sglang/sha256-a7b7136abcf5e07522289d96e96fec9b42a1a30f9dfda57e957f142680d2d67b.md) shows `function_call/` is byte-identical between the fork tree `fc91d24` and its upstream merge base, so `glm47_moe_detector.py:347-349` (`logger.warning(f"Failed to parse '{value}' as number …")`) is unchanged across the Phala → registry swap. `--tool-call-parser glm47` (`:87`) plus Check 5. The base's suggested fix via `x-glm53-build` is no longer available in this file (the build anchor is gone); the remaining recipe-side control is a scrub/drop processor in `logs/app` (`:929`), or a one-line upstream-fork commit rebuilt through the attested workflow (engine page).

### MEDIUM (deployed: ON) — two open OTLP receivers with no scrubbing on the shared bridge, feeding the traces pipeline (sink: telemetry)

Carried. `0.0.0.0:4317` / `:4318` (`:660`, `:662`), `expose` only (`:493-495`); `metrics/otlp` (`:935-938`) and `traces` (`:943-946`) with `[memory_limiter, resource, batch]`. No service in this file emits OTLP.

### INFO (deployed: ON) — one off-box log destination on both CVMs; the Datadog labels are inert because neither measured harness includes an agent (telemetry)

The base could assert this for gpu30 only. Both harness pages now exist — [`c82b1a2e`](/manifests/measured/sha256-c82b1a2eaf6996154a5f39ae621643f034b082d5e51edd3d2ba6009273881d86.md) (gpu04) and [`533e43fd`](/manifests/measured/sha256-533e43fd215d56ec6cd719adb5defe6615f7ba6ae0b018e832f7c8dadf2e25d4.md) (gpu02) — each with an INFO "no Datadog agent in the measurement; one off-box log destination by measurement"; the two captured harness composes differ only at `INSTANCE_LABEL` (`diff`-verified). So engine stdout from both CVMs behind this hostname leaves to exactly one destination, near.ai's gateway. The `nearai.otel.*` labels (`:286-299`, `:317-336`, `:354-373`, `:463-478`) are read by nothing in this file.

- **[INFO] Metrics are counters only** — `prometheus/apps` (`:741-827`) scrapes `/metrics` on r1, r2, dcgm and the proxy with static labels; the engine page verifies the sglang label set is content-free and that `--enable-cache-report` (`:95`) adds a token *count*; the dcgm field list is in Check 7.
- **[INFO] nginx access logging is metadata** — `json_combined` (`:1110-1123`) records `$uri` (`:1117`, not `$request`), request/org/workspace ids, method, status, sizes, timings, `upstream_addr`; `access_log` (`:1125`). Shipped via nginx's label (`:230`). The soak relay logs nothing (`:531`, `:535`).

## Check 6 — Network topology, nginx, registrar, egress

```
networks:
  default:
    external: true
    name: dstack_default      (:512-515)
```

### CRITICAL (deployed: ON, exposure) — the engine's unauthenticated logging switch is reachable over the shared bridge and through two bare location / listeners, and its output is shipped off-box by this file (sink: log / disk / telemetry)

Carried, re-verified at this file's lines. Mechanism per the engine page's CRITICAL, confirmed at `fc91d24`. This document supplies no containment:

- **Engine port 8000 is not host-published** — nginx publishes `8000:8000`, `8444:443` (`:219-220`); the soak relay publishes `8008`/`8009` (`:433-434`) but allowlists six paths (Check 3); dcgm publishes nothing. **But `external: true` / `dstack_default` (`:514-515`) is the shared bridge** and both engines bind `0.0.0.0:8000` (`:92-93`): any container on that bridge — the harness's root-equivalent compose-manager, the perception-check itself (which addresses the replicas by name, `:400`), any other project — can `POST http://model-sg-glm53-fp8-tp4-r1:8000/configure_logging`, unauthenticated.
- **Both serving listeners are bare pass-throughs.** `:8000` → `location / { set $proxy_glm53 http://proxy-glm53:8000; proxy_pass $proxy_glm53; }` (`:1147-1151`); `:443` likewise (`:1158`, `:1170-1173`) for `glm-5-3-flash.completions.near.ai`, `-stg` and `glm-5-3-flash-i<N>` (`:1159-1161`). The resolver change (`:1143`) does not add a path filter. **`location /` pass-through count: 2 of 2 serving listeners; zero allowlisted paths** — the allowlist idiom exists in this file only in the soak relay's config, not on the customer path.
- **The proxy applies no path allowlist** beyond a fixed-string `/v1/responses` refusal (proxy page MEDIUM: the authenticated catch-all forwards any traversal-safe path to a backend, guarded by the deployment `TOKEN` or any valid cloud API key).
- **Exit path:** switched-on log lines → Check 5 → off-box; a switched-on pickle folder under `/root/.cache` → `kernel_cache` (Check 7).

**Net:** unchanged — (a) any co-resident container on the shared bridge, unauthenticated; (b) any external caller holding an accepted bearer, via `nginx → proxy → engine`. `--admin-api-key` closes both.

**Registrar** (`:232-259`, script `:953-1107`): `curlimages/curl@sha256:d94d07ba…` (`:233`), env `HOST_IP`, `HTTP_PORT` (default 8000), `TLS_PORT` (default 8444), `MODEL_PROXY_TOKEN`, `PROXY_TOKEN` (`:247-251`). `PROXY_URL="https://completions.near.ai"` (`:956`); POSTs `/register/endpoint` (`:972`), `/unregister/endpoint` (`:980`), `/register/model` (`:987`) with `{"model":"z-ai/glm-5.3-flash","domain":"glm-5-3-flash.completions.near.ai"}` (`:1006`) — metadata. **New:** a one-time `unregister_endpoint "${HOST_IP}:8001"` (`:1016-1020`) that removes the retired co-resident engine's endpoint from the router — a control-plane call carrying an IP:port, nothing else. The liveness probe is unchanged: `POST http://${HOST_IP}:${HTTP_PORT}/v1/chat/completions` (`:1026`) with body `{"model":"z-ai/glm-5.3-flash","messages":[{"role":"user","content":"hi"}],"max_tokens":1}` (`:1029`), bearer `PROXY_TOKEN` (`:1027`); readiness `GET /v1/models` (`:1058`); healthcheck file `/tmp/registrar_alive` (`:1075`). **Probe bodies contain no user content.** As on the base, the probe demonstrates that the host-published `:8000` listener accepts a plain-HTTP, non-OHTTP chat request with a bearer — the degraded path of [method §1](/notes/method.md) is open at nginx and the proxy.

Every outbound destination configured in this file:

| destination | service | literal? | carries content? |
|---|---|---|---|
| `https://telemetry.infra.near.ai` (`:911`) | otelcol-contrib | literal | **yes — full app-container log bodies, unscrubbed** |
| `https://cloud-api.near.ai` (`:266`) | proxy | literal | usage/auth; not message bodies |
| `http://compose-manager:8080` (`:268`) via `extra_hosts` (`:51-52`) | proxy | literal | control plane |
| `https://completions.near.ai` (`:956`) | registrar | literal | registration metadata; the synthetic probe `"hi"` (`:1029`) goes to the local nginx, not off-box |
| Hugging Face | **model-downloader only** (`:143`, `:145`); engine offline (`:105-106`) | implicit | weights at deploy time |
| PyPI (`uvx --from huggingface_hub[hf_xet]`, `:143`) | model-downloader | implicit, unpinned | tooling at deploy time |
| the two replicas by service name (`:399`) | perception-check (profile) | literal | synthetic fixtures; in-enclave only |
| **any client-supplied media URL** | **engine** (no `--allowed-media-domains`) | request-driven | the requesting client's own URL — MEDIUM below |
| **`${WEB_CONTEXT_SEARCH_URL}`** (`:282`) | **proxy** | not literal, not in `allowed_envs` | **yes if populated** — model-generated search query |
| **Fusion endpoints** (`${FUSION_ENABLED}`, `:273`) | **proxy** | not literal, not in `allowed_envs` | **yes if enabled** — full decrypted conversation |

Inbound, host-published: `8000` and `8444` (nginx, `:219-220`); `8008` and `8009` (soak relay, `:433-434`, profile-gated, TLS + bearer).

### MEDIUM (deployed: ON) — request-driven media fetch from the plaintext engine with no domain allowlist and multimodal not disabled; the new 64-image cap bounds count, not source (sink: egress, request-driven)

Carried. Neither `--allowed-media-domains` nor `--disable-multimodal` in `:63-98` (grep-verified). The two new flags are adjacent but do not close it: `--limit-mm-data-per-request '{"image": 64}'` (`:98`) is a per-request count (engine page: enforced in the tokenizer manager, "a count limit, not a source limit"), and `--disable-fast-image-processor` (`:97`) selects which processor decodes the bytes after they are fetched. The engine page re-verifies at `fc91d24` that the allowlist defaults to an empty list, that with an empty list no host check runs, that redirects are re-validated against the same empty list, and that `file://` and absolute paths are read locally (reaching `/proc/self/environ` with `HF_TOKEN`, `/root/.cache`, and `127.0.0.1:8000/configure_logging` from inside the engine's own netns). The proxy's own image-URL allowlist (`VLLM_PROXY_IMAGE_VALIDATION_ALLOWED_DOMAINS`) is also unset in this file (proxy page: empty fleet-wide). One flag closes the remote half.

- **[MEDIUM] nginx spills large request bodies to a temp file** — carried: `client_max_body_size 100m` / `client_body_buffer_size 1m` (`:1136-1137`); `proxy_buffering off` / `proxy_cache off` (`:1133-1134`). Under OHTTP the body at nginx is ciphertext; on the plain `:8000` path the registrar exercises (`:1026`) it would be plaintext. The soak relay, by contrast, sets `proxy_request_buffering off` and `proxy_max_temp_file_size 0` (`:549`, `:551`) — the serving nginx could adopt the same two lines.
- **[MEDIUM] `HTTP_PORT` / `TLS_PORT`** (`:248-249`) — interpolated defaults not in `allowed_envs`; routing controls for the registrar's advertised endpoint, reachable only through the overlay.

## Check 7 — Privileges, mounts, volumes, image pinning

`volumes:` (`:517-523`): `otelcol_app_storage` (`:518`), `huggingface_cache` (`:519`), **`kernel_cache`** (`:520`), `certs: external` (`:521-523`). No `pid: host`, `SYS_PTRACE` or docker socket in this file (grep-verified). `cap_add`: dcgm `SYS_ADMIN` (`:124-125`); soak relay `CHOWN, SETUID, SETGID` after `cap_drop: [ALL]` (`:425-426`, the minimum for nginx to drop to its worker user). The two new services are the most locked-down in the file (`runtime: runc`, `no-new-privileges`, no GPU, no host mounts).

### MEDIUM (deployed: ARMED) — no ulimits core anywhere in the recipe, so a crashing engine replica writes its process memory as a core file into its container layer (sink: disk)

Carried, and now resolved on the substrate for **both** CVMs: both reports carry `os_image_hash a6eafc5f…`, the [OS page](/os/sha256-a6eafc5f007f642d8ea90c7fa8881f1e6715720ccb531941a28218f4f26d7b02.md) establishes `LimitCORE=infinity` on docker/containerd with no `default-ulimits`, so a container inherits an unlimited core limit unless its compose service sets `ulimits: core`. This recipe does not: the only `ulimits` block is `:40-44` (memlock, nofile); `core` appears nowhere. The engine page adds that sglang's own crash path runs py-spy (stack frames only, no `--locals`) and that the CUDA user-triggered coredump needs `CUDA_ENABLE_USER_TRIGGERED_COREDUMP=1`, which is neither in this file nor in `allowed_envs` — so the ARMED path is the kernel's, on a native fault. `restart: unless-stopped` (`:116`) keeps the layer across a crash-restart. Fix: `ulimits: core: 0` in `x-nvidia`.

### MEDIUM (deployed: ON, cross-link) — /root/.cache is a persisted named volume, so a runtime-enabled request pickle survives container recreate and redeploy (sink: disk)

Carried. `kernel_cache:/root/.cache` (`:100`), named (`:520`); HF mount nested (`:101`). The engine page confirms `dump_requests_folder` has no CLI flag, initialises empty, and once set via `/configure_logging` pickles every finished request (full input incl. `image_data` and full output) to that path. At deployed flags nothing request-derived is written here (KV in GPU memory `:77`; no hierarchical cache; multimodal caches in-memory per the engine page). **New context from the logs:** on gpu04 this volume has now been mounted by engines built from six revisions across two files since 2026-09-02 (Check 8), and on gpu02 it survived a full `compose down` on 2026-09-09T04:55Z (named volumes persist through `down` without `-v`). Nothing in any of those revisions writes request data to it at their declared flags, but the volume is the one place a runtime-enabled dump would outlive every redeploy in that history.

### INFO (deployed: ON) — dcgm-exporter is now digest-pinned with an explicit counters-only metrics file; the base's tag-pin MEDIUM is resolved, the SYS_ADMIN privilege over all eight GPUs is not (supply chain)

`nvcr.io/nvidia/k8s/dcgm-exporter@sha256:613ab03c…` (`:121`) replaces the base's tag-only `4.5.2-4.8.1-distroless` — the registry can no longer serve different bytes under this reference without an attestation-visible change, which was the whole of the base's MEDIUM. What remains: `cap_add: SYS_ADMIN` (`:124-125`), `runtime: nvidia` (`:123`), `device_ids` 0-7 (`:459`), not host-published, scraped internally at `dcgm-glm53:9400` (`:797`) with a 10 s timeout (`:794`). **The metrics file** `dcgm_h200_metrics` (`:582-652`, mounted read-only at `/etc/dcgm-exporter/nearai-h200.csv` `:126-129`, selected by `-f` `:122`) was read in full: 50 `DCGM_FI_*` / `DCGM_EXP_*` fields — clocks, temperatures, power, energy, utilisation, framebuffer used/free/reserved, PCIe throughput and replays, XID and clock-event counters, health/P2P status, six throttling-violation counters, ECC/retired-page/row-remap counters, NVLink bandwidth and error counters, three profiling ratios, and four version/brand **labels** (driver, NVML, brand, VBIOS). **Every entry is a gauge, counter or static label; none names a process, a memory region, or anything content-adjacent.** The exporter's page for this digest is under concurrent review; the audited sibling [`ed594cf5`](/images/nvcr.io/nvidia/k8s/dcgm-exporter/sha256-ed594cf53fe6942e84b07b0740cdcbb249fa4b39cb21feeebf93881ae51f0b5e.md) (4.8.1) is "telemetry-not-content at deployed config — GPU-privileged". The header comment (`:584`) says "DCGM 4.6.0 / exporter 4.8.3"; the digest-to-tag mapping was not resolved here (Not traced).

### INFO (deployed: OFF, profile-gated) — hf-cleanup can evict the weights volume on operator demand; dormant, weights-side, fail-closed for the engine (disk)

Carried (`:160-210`; `profiles: ["maintenance"]` `:163`; `rm -rf` `:196` on `huggingface_cache` `:200`; `${MODEL_NAME}` `:202` not in `allowed_envs`; own json-file driver with no service labels `:203-210`, so dropped by `:877`). Never started on either CVM per the logs.

- **[INFO] `ipc: host` on both replicas and the proxy** (`:39` via `:50` and `:60`) — unchanged.
- **[INFO] `otelcol-contrib` mounts `/var/lib/docker/containers:ro`** (`:497`) as root (`:484`) — its function.

**Image pinning.** Every `image:` in the file is digest-pinned (grep over 11 `image:` lines `:47`, `:62`, `:121`, `:135`, `:161`, `:213`, `:233`, `:379`, `:421`, `:481`): proxy `98b57ad7…` (audited), engine `a7b7136a…` (audited, signed provenance), dcgm `613ab03c…` (page pending), `uv` `4f5d923c…`, `alpine` `d9e853e8…`, `nginx` `1d13701a…` (x2), `curl` `d94d07ba…`, `otelcol-contrib` `85ac41c2…` (audited). **No `build:` anywhere** — this is the first revision of this recipe with no in-enclave build and no tag-only image.

## Check 8 — The running containers are a patchwork of revisions and files (novel)

The deploys are service-scoped. Reading each CVM's signed action log forward and taking, per service, the last `compose_up` that names it (or the last full `compose_up` with an empty services list, which starts every non-profile service; a full `compose_down` removes every container), the containers **as of the 2026-09-10 capture** were created from:

| service | gpu04 (`7bb9af0a…`) — last created by | gpu02 (`fccd496a…`) — last created by | block vs this file |
|---|---|---|---|
| `model-downloader` | `d76413cd` full up, 2026-09-05T03:46:44Z (after an explicit `compose_down model-downloader` at 03:46:02Z) | Canary **`5a5b7228`** full up, 2026-09-09T05:05:32Z | **identical** in both source revisions (`:134-158`) |
| `hf-cleanup` | never started | never started | — |
| `nginx` | Canary **`5a5b7228`**, 2026-09-07T22:10:42Z (`proxy-glm53,nginx`) | Canary **`5a5b7228`** full up, 2026-09-09T05:05:32Z | service block **identical**; `nginx_conf` differs by **one blank line** (`5a5b7228:1080`) — same directives, same two bare `location /` |
| `model-proxy-registrar` | **`a2d3f073`**, 2026-09-09T20:13:06Z | **`a2d3f073`**, 2026-09-09T20:18:02Z | service block and `registrar_script` **identical** |
| `proxy-glm53` | **`2878b9c4` (this file)**, 2026-09-09T23:44:01Z and 23:50:56Z | **`2878b9c4` (this file)**, 23:44:55Z and 23:50:57Z | — |
| `model-sg-glm53-fp8-tp4-r1` | **`e5d80ab6`**, 2026-09-09T22:32:59Z | **`e5d80ab6`**, 23:18:19Z | `x-sg-glm53-flash-common`, `x-nvidia`, both replica blocks **identical** (`e5d80ab6` differs from this file only at `:47`) |
| `model-sg-glm53-fp8-tp4-r2` | **`e5d80ab6`**, 22:56:40Z | **`e5d80ab6`**, 23:35:47Z | identical, as above |
| `glm53-perception-check` | **`e5d80ab6`**, 23:47:41Z (also 22:44Z, 23:08Z); `restart: "no"`, exits | **`e5d80ab6`**, 23:53:31Z (also 23:29Z, 23:47Z) | **identical** |
| `glm53-soak-relay` | Canary **`1072fbb4`**, 2026-09-05T18:26:46Z; **no `compose_down` since** | never started | service block and `glm53_soak_nginx_conf` **identical** |
| `dcgm-glm53` | **`a2d3f073`**, 2026-09-09T20:13:06Z (after a `compose_down dcgm-glm53` at `a648cda7`) | **`a2d3f073`**, 20:18:02Z | service block, `x-dcgm-common`, `dcgm_h200_metrics` **identical** |
| `otelcol-contrib` | Canary **`ee6a8d31`**, 2026-09-06T06:50:17Z | Canary **`5a5b7228`**, 2026-09-09T05:34:26Z | service block **identical**; `otelcol_app_config` differs by **one line** — this file adds `scrape_timeout: 10s` on the dcgm job (`:794`); receivers, filter, processors, exporter, endpoint, pipelines identical |

Two compose semantics were assumed and are flagged: (1) `compose up <svc>` also (re)creates that service's `depends_on` targets if their config differs — the only dependency edges here are r1/r2 → `model-downloader` (identical block in every revision involved, so never recreated) and nginx → `proxy-glm53` (the dependency, not the dependent, so a later `up proxy-glm53` does not recreate nginx); (2) a `docker_restart` event (gpu04, 2026-09-06T17:26Z) restarts `unless-stopped` containers with their existing config and does not restart `restart: "no"` ones absent live-restore. `compose_stage` entries (five on gpu04, one on gpu02) are treated as fetch/validate without apply — they name a file but create nothing (semantics not verified; Not traced). Whether compose-manager passes `--no-deps` is not visible (Not traced).

### MEDIUM (deployed: ON) — the running containers on both CVMs are a patchwork of five revisions across two files; only proxy-glm53 was created from this hash, and the collector that decides where engine stdout goes and the nginx that decides reachability were created from Canary revisions this hash does not name (sink: telemetry / egress, attestation visibility)

**What the attestation says:** an attestation sample keyed to `file_sha256 2878b9c4…` describes a document; on both CVMs that document created exactly one container (`proxy-glm53`). The engines were created from `e5d80ab6` (a sibling that names a different proxy digest); the collector — whose `otelcol_app_config` is the entire answer to "where does engine stdout go" — from Canary `ee6a8d31` (gpu04) and `5a5b7228` (gpu02), a file (`prod/GLM-5.3-Flash-SGL-TP4-Canary.yaml`) with **no page in this repo**; the customer-facing nginx from Canary `5a5b7228` on both; the registrar and dcgm from `a2d3f073`; gpu04's downloader from `d76413cd` and its soak relay from Canary `1072fbb4`. **What is true today:** every one of those source blocks was diffed against this file above — the declared config of every running container equals what this file declares, to within a `scrape_timeout` and a blank line. So the findings on this page describe the running node. **Why it is a finding anyway:** that equivalence was established by fetching twelve revisions out of band and diffing them, not by anything in the attestation chain; the app that renders this page keys on one `file_sha256`, and for nine of ten running containers that is the wrong document. The next Canary deploy of `otelcol-contrib` can change the export endpoint, add a receiver, or drop the keep-filter — deciding where plaintext-adjacent stdout goes — while every attestation sample continues to report whichever hash last touched *any* service. Severity MEDIUM (not a sink; an attestation-visibility gap on the two sinks that matter most), deployed ON (it is the state of both CVMs now). A consumer of this repo should treat a manifest verdict as covering the services that hash created, and read the action log for the rest. Control: full-file `compose up` on every change, or a compose-manager that records per-container image digest and config hash alongside the action.

## Cross-check synthesis

- **[X0 / MEDIUM, deployed ON — the leak]** `--tool-call-parser glm47` (`:87`) + unscrubbed stdout shipping (`:665` → `:929` → `:911`) = a model-emitted argument value reaches `telemetry.infra.near.ai` on a routine formatting slip. Unchanged across the engine swap because the parser file is upstream-identical.
- **[X1 / CRITICAL — exposure, not capture]** `/configure_logging` reachable: shared bridge (`:514-515`) + two bare `location /` (`:1148`, `:1170`) + no `--admin-api-key` + verbatim shipping. The soak relay's allowlist (`:563`, `:574`) shows the operator knows how to write one; the customer listeners still have none.
- **[X2 / HIGH]** Watchdog dump armed (`:91`) over 32 x 2 in-flight requests (`:71`) at 1M context (`:89`), same exit.
- **[X3 / HIGH — systemic]** Global app-stdout shipping to one near.ai destination on both CVMs (both harnesses resolved); now also carries the perception-check's synthetic-output lines (`:416`).
- **[X4 / HIGH, latent]** `FUSION_*` / `WEB_CONTEXT_SEARCH_*` on the E2EE terminator, outside both harnesses' 22 `allowed_envs`, on the overlay that must already deliver `MODEL_PROXY_TOKEN` / `CLOUD_API_USAGE_TOKEN`.
- **[X5 / MEDIUM x2, disk]** No `ulimits: core` (both CVMs on the armed 0.5.11 substrate) and the whole-`.cache` persisted volume (now spanning six engine revisions on gpu04).
- **[X6 / MEDIUM, egress]** Multimodal engine with no media allowlist; the new 64-image cap is a count.
- **[X7 / MEDIUM, attestation]** Patchwork: nine of ten running containers per CVM were created by hashes other than the one an attestation sample reports; equivalent today by out-of-band diff.
- **[X8 / LOW, ingress]** Soak relay: host-published non-OHTTP TLS path to each engine, bearer + six-path allowlist, started on gpu04 2026-09-05 and never logged down.
- **Resolved since the base:** the `:local`-tag INFO (engine is a signed registry digest, no `build:`); the dcgm tag-pin MEDIUM (digest `613ab03c…`, counters-only field list); the "gpu04 harness/OS unresolved" caveats (both harness pages and the OS hash are now read for both CVMs).
- **Verified clean at this layer:** engine command literal end to end; `--log-requests-level 0`; no dump/trace/profile flag; weights, template and every image digest-pinned; `HF_HUB_OFFLINE=1`; `OHTTP_ENABLED=true` literal; telemetry destination literal; downloader run-once and weights-side; `hf-cleanup` profile-gated; registrar probes synthetic; nginx access log metadata; soak relay logs nothing and buffers nothing; dcgm field list content-free; no KV/prompt disk cache; no `pid: host`/`SYS_PTRACE`/docker socket.

## Verdict

**LEAKS — narrowly.** The class is the engine's and this document still completes it: it selects the glm47 parser and builds the unredacted stdout pipeline. Seven commits and 143 net lines changed the engine's provenance (for the better — a signed registry digest replaces an in-enclave build, closing one INFO), pinned dcgm by digest with a counters-only field list (closing one MEDIUM), and added two operator-profile services that a normal deploy never starts; none of it touched a logging, dump, disk or egress switch, and none of it took any of the containments the base listed — `--admin-api-key`, a scrub processor, `ulimits: core`, `--allowed-media-domains`, a customer-path allowlist, a narrower `.cache` mount, dropping `HF_TOKEN`. The engine page for the new image re-derives the CRITICAL, HIGH and MEDIUM mechanisms at `fc91d24`; this page re-locates every exposure at this file's lines and finds each one still open.

- **What this page can say that the base could not:** both CVMs behind the hostname are resolved end to end — harness pages (no Datadog agent, one destination, 22 `allowed_envs` read from both), OS hash `a6eafc5f…` on both — so "exactly one telemetry destination" and the armed core-dump substrate are now facts for this node, not inheritances.
- **What this page found that no earlier page could:** service-scoped deploys have made the running node a patchwork. This hash created one container per CVM. The rest were diffed against it and are equivalent today; the attestation cannot show that, and the next Canary touch to `otelcol-contrib` or `nginx` will not change the hash a sample reports.
- **Recommended, in order:** (1) `--admin-api-key` on the engine anchor; (2) a scrub/drop processor for engine-sourced records in `logs/app` (`:929`), or the one-line fork fix rebuilt through the attested workflow; (3) `ulimits: core: 0` in `x-nvidia`; (4) `--allowed-media-domains` (or `--disable-multimodal`); (5) a customer-path allowlist on nginx (`:1148`, `:1170`) modelled on the soak relay's (`:563`); (6) full-file `compose up` on every change, or per-container digest/config-hash in the action log; (7) narrow `kernel_cache` to the two kernel-cache subdirectories; (8) remove `HF_TOKEN` from the engine env; (9) `compose_down glm53-soak-relay` on gpu04 if it is still running, and record profile-service downs in the log; (10) `proxy_request_buffering off` / `proxy_max_temp_file_size 0` on the serving nginx as on the relay.

## Not traced

- **Whether `glm53-soak-relay` is running on gpu04 today.** Started 2026-09-05T18:26:46Z from Canary `1072fbb4`; no `compose_down` names it; the `docker_restart` on 2026-09-06 would not restart a `restart: "no"` container unless live-restore is enabled. The attestation report carries no container listing. If running, host ports 8008/8009 are open behind `PROXY_TOKEN`.
- **Compose semantics assumed in Check 8:** that `compose up <svc>` recreates only that service plus changed `depends_on` targets (no `--no-deps` visible); that `compose_stage` creates nothing; that a full `compose_up` with an empty `services` list starts every non-profile service. The per-service table is exact only if these hold. A per-container `docker inspect` (image digest + config hash) would settle it.
- **Whether each in-enclave `:local` build between 2026-09-05 and 2026-09-06 on gpu04 actually rebuilt** (`--build`) — moot for the running engines (recreated from `e5d80ab6`, a registry digest, on 2026-09-09), relevant to the history of `kernel_cache`.
- **`prod/GLM-5.3-Flash-SGL-TP4-Canary.yaml`** as a document: only the blocks that created still-running containers (`nginx`, `nginx_conf`, `otelcol-contrib`, `otelcol_app_config`, `model-downloader`, `glm53-soak-relay`, `glm53_soak_nginx_conf`) were diffed, from three of its five revisions. The rest of that file has no page.
- **dcgm-exporter `613ab03c…`**: no page yet; the digest-to-tag mapping (`4.6.0-4.8.3-distroless` per the orchestrator, "DCGM 4.6.0 / exporter 4.8.3" per `:584`) was not resolved here; the exporter's effective NVML visibility under CC is inherited from the `ed594cf5` page.
- **The `test_glm5_next_perception.py` script's exact print sites** — read on the engine page, not here; this page relies on its "400 characters on failure" statement.
- **Whether the deployed `98b57ad7` proxy tree applies any further path filtering, and whether empty `WEB_CONTEXT_SEARCH_*` / `FUSION_*` is fail-closed** — inherited from the proxy page (which rates the populated case CRITICAL/HIGH).
- **How compose-manager sources the overlay env** (`MODEL_PROXY_TOKEN`, `CLOUD_API_USAGE_TOKEN`, `MODEL_NAME`, `HTTP_PORT`, `TLS_PORT`, `FUSION_*`, `WEB_CONTEXT_SEARCH_*`) — the channel must be live; its contents are not visible.
- **The `unregister_endpoint "${HOST_IP}:8001"` call (`:1019`)** — a control-plane call whose effect is on the router, not on this node; not traced past the script.
- Everything in the base page's Not traced not listed above (other sglang print sites — now largely covered by the engine page's grep of the fork delta; `uvx` resolution of `huggingface_hub`; nginx `client_body_temp`; the chat template at `3f1971b7…`; the otelcol queue directory contents).
