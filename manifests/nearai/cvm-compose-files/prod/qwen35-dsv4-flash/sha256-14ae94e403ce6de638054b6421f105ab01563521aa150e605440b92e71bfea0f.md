# cvm-compose-files prod/qwen35-dsv4-flash.yaml @ 14ae94e4... (two-model inner compose — Qwen3.5 engine bump to v0.5.16 on the audited 984699c2 image; DeepSeek stack byte-identical)

## verdict: QUALIFIED PASS at the manifest layer — the delta is one change, the Qwen3.5 engine bump from audited v0.5.12 to audited v0.5.16 (984699c2, PRIVATE at deployed flags), and with the dsv4 engine's ec518148 review landed, every plaintext-touching image in this file now has a published review — closing the base page's leading HIGH; the recipe stays clean (no content-logging switch, --log-requests-level 0 on both anchors, OHTTP forced on literally, every checkpoint revision-pinned and pre-fetched at its pin), but the bump imports two engine-side costs the manifest does not compensate: the qwen35 watchdog hang dump is now armed at the new engine's 300 s default because the qwen35 command passes no --watchdog-timeout (dsv4 keeps 1800 s, and its labels match its command — checked), and the multimodal-registered Qwen3.5 architecture makes the engine's unguarded request-driven media fetch reachable with no allowlist flag anywhere in this file; the carried fleet residuals stand — unredacted stdout shipping off-box, an unauthenticated /configure_logging behind six bare nginx locations and allowlist-free proxies, splice-reachable env, one unpinned PyPI resolve, and tag-pinned privileged dcgm

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/qwen35-dsv4-flash.yaml` |
| **file_sha256** | `14ae94e403ce6de638054b6421f105ab01563521aa150e605440b92e71bfea0f` (re-verified locally: `shasum -a 256` of the captured 1270-line file matches) |
| **role** | log-pinned inner compose: two model endpoints — `Qwen/Qwen3.5-122B-A10B` (1x SGLang FP8 TP4, GPUs 0-3, now on upstream **v0.5.16-cu129**) and `deepseek-ai/DeepSeek-V4-Flash` (2x TP2 replicas, GPUs 4-5 / 6-7, 0731 checkpoint on the near-built `ec518148…` engine, unchanged) — behind three `vllm-proxy-rs` instances, nginx, registrar, two dcgm exporters, otelcol-contrib, and a one-shot weight downloader |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. Fetched at the log-pinned commit `ed7dac33e50b` and hash-checked against the log's `file_sha256` before review (matches). Live on **two** fleet attestation surfaces in this run's sampling: `dsv4-flash` and `qwen35-122b`. The `qwen35-122b` surface is the gpu30 host under harness [`66007399…`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md) — the host whose measured layer also runs a Datadog agent with container-collect-all (Check 4) |
| **lineage** | supersedes [`d1cdff70`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-d1cdff7016b9b1fef4ad1662678404eca67c07c4e2ca8993795a0f8dbe15af1c.md) (2026-08-10, the 0731 promotion), itself over [`11da948b`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-11da948ba44faae3f0fd7419197495fe82cbeb221bec5f5f03ea6caf580e9964.md) and the fully-reviewed A/B file [`5be223d3`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-5be223d330932d118eb8ceee6414304af43c98d9a0f43b8195c8d86dfb77d000.md) |
| **review** | reviewed 2026-08-17 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review, base = the [`d1cdff70` page](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-d1cdff7016b9b1fef4ad1662678404eca67c07c4e2ca8993795a0f8dbe15af1c.md). The diff was taken **against the actual base bytes**, not the base page: the base revision was independently re-fetched at its log-pinned commit `5d99a34f12b6…` and hash-verified to `d1cdff70…` before diffing (1266 → 1270 lines, 18 changed lines total, all in one region). Every carried finding below was re-located and re-cited at this file's line numbers; the negative claim that everything outside the qwen35 anchor is byte-identical is a `diff` fact, not an assumption.

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-08-17).
> Method: [/notes/method.md](/notes/method.md) · Base: [d1cdff70](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-d1cdff7016b9b1fef4ad1662678404eca67c07c4e2ca8993795a0f8dbe15af1c.md)

## Check 1 — What changed from the audited base, verified against the base bytes

The whole diff is confined to the `x-sg-qwen35-122b-common` anchor:

1. **Engine image bump** (`:57`): `lmsysorg/sglang:v0.5.12-cu129@sha256:9e02c8e1…` → `lmsysorg/sglang:v0.5.16-cu129@sha256:984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c`. The new image is **audited in this same run** — [`984699c2` page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md), PRIVATE at the deployed flags, identity `sgl-project/sglang` @ `fdebc938` — and that page reads **this recipe's** launch command as one of its three deployed configurations. Engine-internal residuals are deferred to it rather than re-derived here.
2. **Flag rename, same value**: `--mamba-scheduler-strategy extra_buffer` → `--mamba-radix-cache-strategy extra_buffer` (`:69`) — cache-strategy config in GPU memory; no disk tier.
3. **Two new flags**, both config-only at this layer: `--disable-prefill-cuda-graph` (`:70`, scheduler/compile behavior) and `--mm-feature-transport cpu` (`:71`, multimodal feature tensors move via CPU memory — in-process transport, no content sink; but see the media-fetch MEDIUM below for what its presence corroborates).
4. **Env removed**: `SGLANG_ENABLE_SPEC_V2=1` dropped from the qwen35 environment (the EAGLE flags stay, `:65-68`; Spec V2 has been the engine default since 0.5.11 per the in-file comment, so this is dead-config cleanup, not a behavior change at this layer).
5. **Comment changes only**: the dev-box benchmark note is reworded and a v0.5.16 rationale added (`:54-56` — xgrammar 0.2.1 fixes the `qwen3_coder` forced-tool grammar regression, sgl-project/sglang#27336; *"Runtime defaults changed after v0.5.12, so the command below keeps the existing Mamba, prefill, and multimodal behavior explicit"*).

**Everything else is byte-identical to the audited base — a diff fact:** the `json-file` logging anchor (`:1-6`), `ipc: host` (`:13`), proxy anchor (`:20-31`), dcgm anchor (`:99-105`), the entire dsv4 anchor and both replica services, all three proxies, nginx, registrar, downloader, otelcol service and embedded config, networks, and volumes. Residue sweep: zero hits for `9e02c8e1`, `6bb5fee3`, `1e335c48`, `eac0a7c8`, `fairness`, `experiment`, `ab_arm`.

### INFO (deployed: ON) — every plaintext-touching image in this file now has a published review; the base page's leading HIGH closes (posture)

The base verdict led with a HIGH: both DeepSeek replicas ran `nearaidev/sglang@ec518148…` with **no published review at deploy time**. That review has since landed — [`ec518148` page](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md): PRIVATE at the deployed flags, a from-source build of upstream v0.5.16 (`fdebc938`) plus two hash-verified, privacy-neutral patches — and this revision moves the qwen35 side onto the audited [`984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md). With the proxies ([`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md), [`b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md)), [otelcol](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md), and the dcgm [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md) already published, this is the first revision on this lineage in three where **no service runs unreviewed bytes** (dcgm remains reviewed-by-tag only — its bytes can still drift, Check 5). Note both engines now sit on the same upstream line: `fdebc938` (v0.5.16) upstream on qwen35, `fdebc938` + two patches on dsv4.

### MEDIUM (deployed: ARMED) — qwen35 watchdog hang dump is now armed at the new engine's 300 s default; the command passes no --watchdog-timeout (log, then off-box telemetry)

The qwen35 command has never passed `--watchdog-timeout` (base and this file alike), but the engine bytes under it changed, and on the new engine this is a verified, sooner-firing path: the [`984699c2` page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) traces an undisableable watchdog dump that writes the full prompt and output token IDs of every in-flight request to stdout at ERROR on a forward-batch stall, notes `Req.__repr__` now also interpolates `sampling_params`, and confirms the default `watchdog_timeout` is **300 s** at `fdebc938` — with this recipe named as one of the three that does not override it. A 5-minute engine stall, no attacker, is the trigger; Check 4's pipeline ships the dump off-box. The dsv4 side keeps its explicit `--watchdog-timeout 1800` (`:133`) — same dump class ([`ec518148` page](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md), HIGH ARMED), 6x longer fuse. The manifest-layer fix is one line: pass `--watchdog-timeout` on the qwen35 command too, or better, both engines' pages list the upstream controls.

### MEDIUM (deployed: ON) — request-driven media fetch becomes reachable on the Qwen3.5 side: no domain-allowlist flag exists anywhere in this file, and engine egress is unrestricted (egress)

New consequence of the bump, found by the engine review, priced here at the manifest layer: `Qwen3_5MoeForConditionalGeneration` is **registered multimodal** on the new engine, so an image/audio/video URL in a chat request is fetched by the engine process with no domain allowlist and redirects followed by default ([`984699c2` page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md), MEDIUM, deployed ON — which explicitly cites this recipe's `--mm-feature-transport cpu` as corroborating multimodal being live). What this file contributes: **no** `--allowed-media-domains`, `--disable-multimodal`, or any media-guard flag appears anywhere (grep-verified, zero hits for `media`/`multimodal` outside comments), and the network gives engines unrestricted egress (Check 6). The fetch is request-driven — a URL the *client* supplies — so it is not an exfiltration path by itself, but it is engine-originated egress to arbitrary hosts, carrying the URL (which can encode content) plus the enclave's egress IP. The old `9e02c8e1` engine treated the media path as off the text-chat path; that assurance did not survive the bump. The dsv4 side is unaffected (DeepSeek-V4-Flash is text-only per its engine page).

## Check 2 — Env reachability (unchanged topology; re-cited at this file's lines)

Every logging knob is a **literal**: `LOG_FORMAT=json` on all three proxies (`:252`, `:339`, `:378`), `--log-requests-level 0` on both engine anchors (`:84`, `:142` — the minimum level, load-bearing since the field default on both v0.5.16 engines is 2 per their pages), `VLLM_LOGGING_LEVEL=INFO` (`:151`, dsv4 anchor), `NCCL_DEBUG=WARN` (`:94`, `:154`), otelcol `telemetry: logs: level: info` (`:988-989`). **`OHTTP_ENABLED=true` is a literal on all three proxies** (`:254`, `:341`, `:380`) — E2EE posture is not operator-degradable. **No `--api-key` or `--admin-api-key` anywhere** (grep-verified; the only `*API_KEY` strings are the web-search pair) — which is what keeps Check 6's admin-surface finding live.

### MEDIUM (deployed: OFF, splice-armed) — hidden-states return splice on the dsv4 command anchor (egress in API response)

`${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}` (`:144`), inherited by both replicas. Unchanged from base: default OFF, not in the measured `allowed_envs`, reachable only via compose-manager's non-measured `/compose/up` splice; **any** non-empty value (including `"0"`) turns it on. Hidden states are prompt-derived tensors.

### MEDIUM (deployed: OFF, splice-armed) — unpinned web-search URL and key on all three E2EE-terminating proxies (egress)

`WEB_CONTEXT_SEARCH_URL` + `_API_KEY` (`:262-263`, `:349-350`, `:388-389`): an operator-suppliable external URL handed to the processes that decrypt E2EE traffic, riding the splice channel with no measurement change. Held at MEDIUM because both deployed proxy builds were source-verified fail-closed when unset, opt-in per request, sending only a model-generated query (proxy pages, unchanged builds).

Other interpolated vars, none in `allowed_envs` (splice-only): `HF_HUB_OFFLINE` defaulted `0` (`:91`, `:150`), `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (`:256-257`, `:343-344`, `:382-383`), `CLOUD_API_USAGE_TOKEN` (`:250`, `:337`, `:376`), `PROXY_TOKEN`/`MODEL_PROXY_TOKEN` (`:232-233`, consumed as `TOKEN` at `:255`, `:342`, `:381`), `HTTP_PORT`/`TLS_PORT`/`TLS_PORT_2` defaulted (`:229-231`), `MONITORING_INGEST_TOKEN` (`:572`).

## Check 3 — Engine launches, pinning, and the label-vs-command truthfulness check

No `--log-requests`, dump, record, trace-to-disk, profiling, or crash-dump flag on any command (grep-verified). Pinning, service by service:

| service | image | `--revision` | `--trust-remote-code` |
|---|---|---|---|
| model-sg-qwen35-122b-fp8-tp4 (`:283`, anchor `:33-97`) | [`lmsysorg/sglang:v0.5.16-cu129@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (`:57`) — audited | `a099dee7…` (`:61`) | yes (`:83`) — **pinned** |
| model-sg-dsv4-flash-fp4-tp2-r1 (`:407`) / r2 (`:455`) | [`nearaidev/sglang@ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) (`:114`) — audited | `7872f01b…` (`:119`) | yes (`:117`) — **pinned** |

Both replicas inherit the anchor entirely — the only keys they add are `container_name`, `depends_on`, GPU `device_ids` (`:418` `["4","5"]`, `:466` `["6","7"]`), and labels; **no `command:`, `image:`, or `environment:` override exists in either replica block** (verified over `:407-504`). The one-shot downloader (`:165-187`, digest-pinned uv image `:166`) pre-fetches all three checkpoints at the same pins (`:174`, `:176`, `:178`) before any engine starts; `--trust-remote-code` is inert against upstream drift while revisions are pinned.

**Label-vs-command truthfulness — checked, and in this file the labels tell the truth.** This is a live issue class on sibling recipes (labels asserting a watchdog config the command does not deploy), so it was checked here service by service:

- **dsv4 r1/r2**: the `com.datadoghq.ad.logs` and `ad.instances` tag sets assert `watchdog_timeout:1800` (`:423`, `:424`, `:471`, `:472`), as do the `nearai.otel.watchdog_timeout` labels (`:447`, `:495`) and the otelcol Prometheus static labels (`:741`, `:774`) — and the deployed command actually passes `--watchdog-timeout 1800` (`:133`), which both replicas inherit unmodified. **Consistent.** Every other asserted tuning tag was checked against the command the same way: `chunk_size:8192` ↔ `--chunked-prefill-size 8192` (`:130`), `max_prefill_tokens:16384` ↔ `:131`, `continuous_decode_steps:1` ↔ `:128`, `chunked_prefill_admission_reserve:4096` ↔ env `:156`, `max_running_requests:64` ↔ `:125`, `max_queued_requests:16` ↔ `:126`, `schedule_conservativeness:1.3` ↔ `:127`, `cuda_graph:1` ↔ `--cuda-graph-max-bs 64` (`:123`, no disable flag), `model_revision` ↔ `:119`, `engine_image:ec518148762e` ↔ `:114`, `sglang_revision:0.5.16-near-dsv4-0731` ↔ the image's own OCI revision label (engine page). **All consistent.**
- **qwen35**: the label set (`:299`) asserts only `model`/`deployment`/host metadata — it makes **no** watchdog or tuning claim, so nothing to disagree with; the engine runs the 300 s default (the MEDIUM above). The *asymmetry* — dsv4 publishes its full tuning profile to the telemetry backends while qwen35 publishes none, and dsv4 gets a 30-minute watchdog fuse while qwen35 sits on the 5-minute default — is real but carries no content; the fuse half is priced in Check 1's MEDIUM.

New-flag classification (`:69-71`): all config-only at this layer — cache strategy, CUDA-graph scope, and feature-transport medium; none names a log, file, or network sink.

### MEDIUM (deployed: ON) — weight downloader resolves huggingface_hub from PyPI unpinned inside the enclave (disk, supply chain)

Unchanged from base: `uvx --from 'huggingface_hub[hf_xet]' hf download …` three times (`:174`, `:176`, `:178`) installs `huggingface_hub[hf_xet]` from PyPI at whatever version exists on deploy day, running in-enclave with `HF_TOKEN` (`:183`) and write access to the shared `huggingface_cache` volume the engines read weights from (`:181`, `:87`, `:146`). Never sees message plaintext (pre-serving, download-only) — supply-chain/cache-poisoning exposure with no attestation-visible trace on drift.

### MEDIUM (deployed: ARMED) — dsv4 watchdog hang dump onto the shipped stdout stream, 1800 s fuse, dump shape now verified (log)

Carried from base with the deferral resolved: `--watchdog-timeout 1800` (`:133`) on both replicas, and the [`ec518148` page](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) has now confirmed the dump exists on the near build (HIGH, ARMED: full prompt + output token IDs of every in-flight request to stdout, no flag to disable). A 30-minute scheduler hang arms it; Check 4 ships it.

### INFO (deployed: ON) — prompt-derived KV stays in GPU memory; durable volumes are weights and kernels (disk)

`SGLANG_ENABLE_UNIFIED_RADIX_TREE=1` (`:158`) and `--kv-cache-dtype fp8_e4m3` (`:72`, `:121`) keep quantized KV in GPU memory; no disk KV/offload tier is configured on either anchor. Durable volumes are `huggingface_cache`, `kernel_cache`, `otelcol_app_storage`, and `certs` only (`:602-608`); engine mounts are the two caches (`:87-88`, `:146-147`).

## Check 4 — Telemetry pipeline: stdout of every plaintext service leaves the box unredacted

### HIGH (deployed: ON) — engine and proxy stdout ships off-box with no redaction stage; on Datadog-bearing hosts it additionally reaches a third-party US SaaS (telemetry)

Chain re-verified in this file, and the two-sided trap the base pages documented is restated because reading either half alone gives the wrong answer: every app service logs via the `json-file` driver whose anchor **exports the `com.datadoghq.ad.logs` label into each log record's attrs** (`:1-6`). otelcol-contrib (`:565`, digest-pinned `:566`, [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)) tails `/var/lib/docker/containers/*/*-json.log` (`:622`, ro mount `:582`); the filelog receiver's `regex_parser` operators then **lift `model:` and `deployment:` out of that label** into record attributes (`:650-652`, `:662-664`). `filter/app_logs` (`:913-922`) *looks* like a drop-only filter — it drops records where `model` **and** `deployment` are both nil (`:917`) plus five named management services (`:918-922`) — but because every app service's `ad.logs` label carries those tags (downloader `:186`, nginx `:211`, registrar `:240`, proxies `:265`/`:352`/`:391`, qwen35 engine `:299`, dsv4 r1 `:423`, r2 `:471`, dcgm `:519`/`:549`), the receiver+filter pair **keeps exactly the plaintext-adjacent services** and ships them to the literal `https://telemetry.infra.near.ai` (`:950`, bearer `:952`). The filter alone reads as an allowlist of nothing; the receiver alone reads as metadata plumbing; only together do they say "all app stdout leaves." No processor in the pipeline redacts content: `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch` (`:967`) — limits, metadata, grouping, batching. (otelcol's own stdout is label-tagged `deployment:` at `:595` but excluded by name at `:921`, and uses the `local` driver `:590-593`.)

The second leg is measured-layer, host-dependent: the `qwen35-122b` surface (gpu30) runs under harness [`66007399`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md), whose measured Datadog agent ships every container's stdout to `us3.datadoghq.com` (harness page, Check 3) — so there, engine stdout leaves **twice**, one copy to a third-party US SaaS. What can land on that stream at the deployed flags is bounded by the engines' print discipline — now verified on **both** engine pages (the qualification the base page carried is gone), with the watchdog dumps of Checks 1 and 3 as the known ARMED writers.

### MEDIUM (deployed: ON) — open OTLP receivers forward spans and metrics verbatim off-box (telemetry)

OTLP on `0.0.0.0:4317/4318` (`:617`, `:619`; `expose`d not host-published, `:578-580`) feeds `metrics/otlp` (`:973-976`) and `traces` (`:981-984`) pipelines exporting verbatim to `:950`. Any container on the flat network can push prompt-bearing spans and they ship unexamined; nothing in this file does so today.

**INFO:** all metrics paths are pull-based Prometheus scrapes — nine jobs (`:697`, `:715`, `:748`, `:781`, `:798`, `:815`, `:832`, `:850`, `:872`); the dsv4 jobs' static labels carry the tuning-profile constants verified in Check 3 (`:719-747`, `:752-780`) — config metadata, no request-derived dimension. Both engine pages verified `--enable-metrics`/`--enable-cache-report` (`:81-82`, `:140-141`) emit counters/latencies with config-identifier labels only.

## Check 5 — Privileges, mounts, co-tenancy (byte-identical to base; re-cited)

### INFO (deployed: ON) — all three proxies run privileged as root with the attestation socket and TLS key (structural)

`x-vllm-proxy-common` (`:20-31`): `user: root` (`:22`), `privileged: true` (`:23`), `ipc: host` via `x-nvidia` (`:13`), `dstack.sock` (`:28`), `certs:ro` (`:29`). Root-equivalent E2EE terminators — structural exposure inside the box, not a new reader.

### MEDIUM (deployed: ON) — shared IPC namespace across both models' engines and proxies (cross-model memory exposure)

`ipc: host` (`:13`) inherited by every engine and proxy: one IPC namespace spanning the Qwen3.5 and DeepSeek stacks — cross-model shared-memory visibility (different customers' plaintext) permitted though not configured. Both sides are now audited engines, which narrows the practical worry without removing the structural one.

### MEDIUM (deployed: ON) — dcgm-exporter runs mutable-tag bytes with SYS_ADMIN and GPU access (telemetry, privileged drift)

Two instances of tag-only `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:100`) with `SYS_ADMIN` (`:102-103`) + nvidia runtime, GPU-scoped per model (`:513`, `:543`). Bytes can drift under the attestation; the [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md) covers tag + config, never bytes.

No `pid: host`, no `SYS_PTRACE`, no docker-socket mount anywhere in this file (grep-verified; the only `privileged:` is the proxy anchor `:23`, and otelcol mounts container **logs** read-only `:582`). Those capabilities live in the measured harness.

## Check 6 — Network topology, nginx, egress (byte-identical to base; re-cited)

### MEDIUM (deployed: ON) — flat external network, no egress restriction on plaintext services (egress)

`dstack_default`, `external: true` (`:597-600`); no `internal: true`, no egress allowlist. Sanctioned engine egress is HF only (`HF_HUB_OFFLINE` defaulted `0`) — plus, new this revision, the request-driven media fetch on the qwen35 side (Check 1 MEDIUM), which is exactly the kind of engine-originated egress an unrestricted network fails to contain.

### MEDIUM (deployed: ON) — unauthenticated engine admin surface reachable through bare nginx locations and allowlist-free proxies; both engines' /configure_logging is a one-POST content-logging switch (log-enable path)

nginx (digest-pinned `:189`, conf `:1178-1270`) publishes plaintext HTTP host ports 8000-8002, TLS 8444→443, and the second TLS listener on host 8003 (`:197-201`; listeners `:1210`, `:1216`, `:1222`, `:1231`, `:1246`, `:1264`). All six server blocks route with a bare `location /` (`:1211`, `:1217`, `:1223`, `:1242`, `:1251`, `:1269`) — no path allowlist — and the proxies forward any path (their source pages). Both engine pages confirm the deployed builds carry an **unauthenticated `/configure_logging`** (CRITICAL on each: one POST enables verbatim prompt+output logging and arbitrary-path request dumping, no restart, middleware never installed), and no `--api-key` exists anywhere in this file (Check 2) — so that switch is reachable laterally on the flat network and, absent proxy path-filtering, from the front door. Runtime reconfiguration leaves no measurement trace. Mitigations in-file: upstreams are always the proxies, never raw engines; `proxy_buffering off; proxy_cache off` (`:1202-1203`) — no request bodies buffered to disk at the nginx hop.

### INFO (deployed: ON) — nginx access log ships per-request user-attributable metadata off-box (log)

`json_combined` with `$uri` not `$request` (`:1179-1194` — no query strings or bodies), but `request_id`/`org_id`/`workspace_id` (`:1181-1183`) make it per-request, user-attributable metadata on Check 4's shipped stream.

**Named outbound destinations, exhaustively:** proxies → `https://cloud-api.near.ai` (literal `:249`, `:336`, `:375`), `http://compose-manager:8080` (`:251`, `:338`, `:377`; host-gateway `:25-26`), `${WEB_CONTEXT_SEARCH_URL}` (unset default — Check 2); registrar → `https://completions.near.ai` (`:994`), registering both model domains (`:1172-1173`), health-probing backends with a **synthetic** `"hi"` message (`:1063` — not user content), TLS self-check via SNI resolve (`:1097-1099`), r2's routing port mapped to the ACL'd 8003 (`:1010-1018`); engines → HF hub (+ request-driven media fetch on qwen35, Check 1); downloader → HF hub + PyPI (Check 3); otelcol → `https://telemetry.infra.near.ai` (`:950`). The measured layer adds Datadog and compose-manager paths (harness pages).

## Check 7 — Sibling comparison within this file

The two `vllm-proxy-rs` digests, resolved: the qwen35 proxy runs the anchor's [`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (`:21`); both dsv4 proxies override with [`b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md) (`:331`, `:370`) — **both audited in-repo**, both with clean core request paths and the same two client-opt-in egress caveats (web search, fusion). The configuration asymmetry is exactly one env var: the dsv4 proxies add `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS=20` (`:335`, `:374` — the SSE idle watchdog the newer build ships, per the in-file comment `:318-328`, build = inference-proxy#213 / main `48d7e34`); otherwise the three env blocks are identical mod `MODEL_NAME`/`VLLM_BASE_URL`, verified by direct diff — same `OHTTP_ENABLED=true`, same `LOG_FORMAT=json`, same token/webhook wiring, same privilege (all inherit `:20-31`). The two dsv4 proxy blocks are identical mod name/port/backend. This is a version skew (older audited build on qwen35), not a privacy-control gap. Remaining sibling asymmetries — dsv4's tuning-profile labels and 1800 s watchdog vs qwen35's bare labels and 300 s default — are covered in Checks 1 and 3; pin asymmetries: none (both engines revision-pinned with pinned `--trust-remote-code`, unlike the unpinned siblings elsewhere in the fleet). No `build:` block exists anywhere in this file (grep-verified) — nothing is resolved from a mutable source at engine build time; the one mutable in-enclave resolve is the downloader's PyPI fetch (Check 3).

## Verdict

**QUALIFIED PASS at this layer — cleanest revision on this lineage so far, with the qualifications now living in the audited engines' residuals rather than in unreviewed bytes.** The recipe stays clean on delta review: no logging switch handed to any engine or proxy, `--log-requests-level 0` kept on both anchors (load-bearing — the new engines' field default is 2), OHTTP forced on literally, every checkpoint revision-pinned and pre-fetched at those pins, every telemetry destination literal, labels verified truthful against the deployed commands, no plaintext buffered to disk at any hop. Qualifications, in descending order of cost: **(1)** whatever any plaintext service prints leaves the TEE unredacted — twice on the Datadog-bearing gpu30 host — and two verified ARMED writers exist: the qwen35 watchdog dump now on a 5-minute default fuse this file declines to lengthen, and the dsv4 dump on its 30-minute one (Checks 1, 3, 4); **(2)** both engines' unauthenticated `/configure_logging` is one POST from full content logging, behind six bare `location /` blocks and allowlist-free proxies, with no API key anywhere in this file (Check 6); **(3)** the Qwen3.5 side now has an unguarded request-driven media fetch and this file supplies neither an allowlist flag nor any egress restriction (Checks 1, 6); **(4)** the `/compose/up` splice can set `DSV4_RETURN_HIDDEN_STATES` and `WEB_CONTEXT_SEARCH_URL` with no measurement change (Check 2); **(5)** the downloader's unpinned PyPI resolve and dcgm's mutable tag persist (Checks 3, 5). Recommendations: add `--watchdog-timeout` to the qwen35 command (or adopt the engine pages' upstream controls), set `--allowed-media-domains` (or disable multimodal serving) on the qwen35 anchor, and the standing ones — path-allowlist nginx/proxies, pin the downloader.

## Not traced

- Engine internals of both images — print discipline, `/configure_logging` mechanics, watchdog dump shape, patch contents, media-fetch code paths — **relied on** via the published [`984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) and [`ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) pages, not re-derived here.
- The signed action log's compose_up timestamp for this revision, and which harness answers for the `dsv4-flash` surface in this round (the `qwen35-122b` surface is gpu30/[`66007399`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md); the Datadog second-leg claim is asserted only for that host). The fetch-commit `ed7dac33e50b` and `file_sha256` binding were taken from the orchestrator's capture and re-hashed locally (matches); the log itself was not re-fetched in this review.
- Whether `--mamba-radix-cache-strategy`, `--disable-prefill-cuda-graph`, and `--mm-feature-transport` are all consumed as spelled by `sglang serve` at `fdebc938` (flag-consumption is engine-side; the engine page read this command without flagging a parse gap, but no explicit parse trace was made for the two new flags).
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source pages).
- Proxy internals (path forwarding, web-search fail-closed, stream-idle watchdog behavior) — proxy source pages, relied on above.
- PyPI state on deploy day; dcgm-exporter bytes under its mutable tag; guest swap posture (guest-OS leg, [`os/`](/os/)).
- Live fleet routing and traffic split across the surfaces this file answers on (the in-file comment `:318-328` describes the July topology; drift tooling, not this page, tracks convergence).
