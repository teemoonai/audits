# cvm-compose-files prod/qwen35-dsv4-flash.yaml @ d1cdff70... (two-model inner compose — DeepSeek checkpoint bump to 0731 on a near-built engine image, canary promoted to both replicas)

## verdict: QUALIFIED PASS at the manifest layer, with one qualification that outranks the rest: both DeepSeek replicas now run a near-built engine image (nearaidev/sglang@ec518148) that had NO published source review at deploy time — the base page's posture leaned on both replicas running an audited image, and that support is gone until the in-flight engine review lands; the recipe itself stays clean on delta review against 11da948b (no content-logging switch, --log-requests-level 0 kept, OHTTP forced on, every checkpoint revision-pinned and pre-fetched at its pin, all new flags and labels config-metadata only), and the carried residuals are the fleet's — splice-reachable env, an engine admin surface behind allowlist-free proxies, unredacted stdout shipping (twice on the one live host, including a third-party US SaaS), and one unpinned PyPI resolve in the weight downloader

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/qwen35-dsv4-flash.yaml` |
| **file_sha256** | `d1cdff7016b9b1fef4ad1662678404eca67c07c4e2ca8993795a0f8dbe15af1c` (re-verified locally: `shasum -a 256` of the captured 1266-line file matches) |
| **role** | log-pinned inner compose: two model endpoints — `Qwen/Qwen3.5-122B-A10B` (1x SGLang FP8 TP4, GPUs 0-3, unchanged) and `deepseek-ai/DeepSeek-V4-Flash` (2x TP2 replicas, GPUs 4-5 / 6-7, now serving the **0731 checkpoint on a near-built engine image**, `--served-model-name` unchanged `:139`) — behind three `vllm-proxy-rs` instances, nginx, registrar, two dcgm exporters, otelcol-contrib, and a one-shot weight downloader |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. Orchestrator fetched the file at the log-pinned commit `5d99a34f12b6072052c38ddaa2784cbeaf9ecc37` (compose_up 2026-08-10T12:47:23Z); hash re-verified against this review's capture. Live on **one** host at capture time: `qwen35-122b` under harness [`66007399…`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md) — the Datadog-side host. The `dsv4-flash` host was observed **still on [`11da948b`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-11da948ba44faae3f0fd7419197495fe82cbeb221bec5f5f03ea6caf580e9964.md)** in the same sampling round; this page does not claim fleet-wide rollout |
| **lineage** | all today, 2026-08-10, per the signed action log: the audited [`11da948b`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-11da948ba44faae3f0fd7419197495fe82cbeb221bec5f5f03ea6caf580e9964.md) revision was live from 09:28Z; at 12:47Z **this** revision replaced it on `qwen35-122b`. Between 07:56Z and 09:28Z a transient single-replica canary (`6cccfec702b2…`) ran the same 0731 checkpoint on r1 only under a **different** unaudited image (`eac0a7c825c1…` — base page, Check 7). This revision is the full promotion of that canary onto both replicas — under a **new** image build, not the canary's (Check 7 below) |
| **review** | reviewed 2026-08-10 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review, base = the [`11da948b` page](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-11da948ba44faae3f0fd7419197495fe82cbeb221bec5f5f03ea6caf580e9964.md) (reviewed earlier today; itself a delta over the fully-reviewed `5be223d3` A/B file). The orchestrator's diff (base 1220 lines → this 1266) was **independently re-verified against the captured file**: every changed hunk was located and read at this file's line numbers, every load-bearing base finding was re-located and re-cited, and the whole file was swept to confirm the negative claim that proxies, nginx, registrar, otelcol pipeline, volumes, and network are unchanged.

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-08-10).
> Method: [/notes/method.md](/notes/method.md) · Base: [11da948b](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-11da948ba44faae3f0fd7419197495fe82cbeb221bec5f5f03ea6caf580e9964.md) · Harness: [qwen35-122b @ 66007399](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md)

## Check 1 — What changed from the audited base, verified hunk-by-hunk

Relative to `11da948b`, the changes are confined to the dsv4 anchor, the downloader, and telemetry labels:

1. **Engine image swap** (`:110`): the dsv4 anchor drops the audited `lmsysorg/sglang@sha256:6bb5fee3…` for `docker.io/nearaidev/sglang@sha256:ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615` — a near-built image, per its own labels at revision `0.5.16-near-dsv4-0731` (`:419`). Both replicas inherit it (`<<: *dsv4-flash-common`, `:404`, `:452`; neither declares an `image:` or `environment:` override — grep-verified, the only `image:` lines in the file are `:21`, `:54`, `:96`, `:110`, `:162`, `:185`, `:210`, `:327`, `:366`, `:562`). See the HIGH below.
2. **Checkpoint bump, still pinned**: `--model-path deepseek-ai/DeepSeek-V4-Flash-0731` (`:114`) at `--revision 7872f01b1d1fe23eabc4c98b48bffcef5a386062` (`:115`); `--served-model-name deepseek-ai/DeepSeek-V4-Flash` unchanged (`:139`), so the public model name is stable across the checkpoint change.
3. **EAGLE spec-decode removed**: the four `--speculative-*` flags are gone from the dsv4 command; the only `speculative` hits left in the file are the Qwen3.5 anchor's own unchanged EAGLE flags (`:62-65`). `SGLANG_ENABLE_SPEC_V2=1` remains in the dsv4 env (`:151`) — vestigial with no spec algorithm configured; behavior on the near build is engine-side.
4. **New scheduler/memory flags** (`:116-133`) and one new env (`:152`) — assessed one-by-one in Check 3; all config-only at this layer.
5. **Downloader adds a third prefetch** (`:174`): `DeepSeek-V4-Flash-0731` at the same pinned `7872f01b…` — all three prefetches pinned (`:170`, `:172`, `:174`); the superseded `V4-Flash` prefetch (`:172`) is retained.
6. **Telemetry label expansion**: the dsv4 replicas' Datadog `ad.logs`/`ad.instances` labels (`:419-420`, `:467-468`), `nearai.otel.*` labels (`:421-449`, `:473-497`), and the otelcol Prometheus static labels (`:715-743`, `:748-776`) gain ~13 tags each — `model_revision`, `config_variant`, `decode_mode`, `chunk_size`, `max_prefill_tokens`, `continuous_decode_steps`, `chunked_prefill_admission_reserve`, `watchdog_timeout`, `cuda_graph`, `max_running_requests`, `max_queued_requests`, `schedule_conservativeness`, `sglang_revision`, `engine_image` (e.g. `engine_image: "ec518148762e"` at `:449`, `:497`, `:743`, `:776`). Every added value was read: all are **deploy-time config constants** (flag values, revisions, an image digest prefix) — no request-derived or content field. See the INFO below.
7. **Comment changes only**: the gpu26-migration header becomes a gpu30-layout note (`:8-9`), and the dsv4 anchor's long A/B/EAGLE/incident comment block collapses to four lines (`:106-109`). Note what this deletes: the base file's own documentation of fleet streaming freezes — evidence the watchdog ARMED finding previously cited in-file — is gone, while `--watchdog-timeout 1800` stays (`:129`).

**Everything else is unchanged from the base — verified against the whole file, not assumed:** the `json-file` logging anchor with the Datadog label export (`:1-6`), `ipc: host` (`:13`), the proxy anchor (`:20-31`), the whole Qwen3.5 stack (anchor `:33-93`, service `:280-324`), nginx (`:185-208`, conf `:1174-1266`), registrar (`:209-238`, script `:988-1172`), all three proxy env blocks, dcgm (`:95-101`, `:501-560`), otelcol service and pipeline (`:561-591`, `:606-985`), networks (`:593-596`) and volumes (`:598-604`). Residue sweep: zero case-insensitive hits for `fairness`/`experiment`, zero hits for `6bb5fee3`, `1e335c48`, or `eac0a7c8`.

### HIGH (deployed: ON) — both DeepSeek replicas run engine bytes with no published review; the engines' print discipline that gates the stdout-shipping leak is unverified (log, telemetry)

At this file's deploy time (12:47Z), `nearaidev/sglang@sha256:ec518148…` (`:110`) had **no published source review** — unlike every other plaintext-touching image in this file, whose audited pages the base verdict leaned on. The manifest still hands it `--log-requests-level 0` (`:138`) and no dump/trace flag, but Check 4's pipeline ships whatever these processes print off-box unredacted — twice on the live host — so the end-to-end guarantee is exactly as strong as the engine's print discipline, and that is now an unreviewed near-patched build (`sglang_revision: 0.5.16-near-dsv4-0731`, `:419`) rather than an audited upstream one. Every engine-behavior claim on this page (watchdog dump shape, `/configure_logging` existence and auth, flag semantics of the near-patch symbols) is **deferred** to the concurrent Fable review of that image, which will publish at [/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) — in flight at this page's writing, not yet landed; under this repo's fail-closed rule the app shows no audit link for the engine until it does.

### INFO (deployed: ON) — label expansion is config metadata only; the log-collection switch topology is unchanged (telemetry)

All ~13 added tags per surface (Check 1 item 6) are deploy-time constants; none is request-derived. The collection switch itself is untouched: `com.datadoghq.ad.logs` still marks **every** app service for pickup (`:182` downloader, `:207` nginx, `:236` registrar, `:261`/`:348`/`:387` proxies, `:295` qwen35 engine, `:419` r1, `:467` r2, `:515`/`:545` dcgm, `:591` otelcol), the `json-file` driver anchor still exports the label (`:6`), and otelcol's ten filelog label-extraction parsers are unchanged (`:646-683` — regex over the `ad.logs` label value, metadata not content). What the expansion does change: the operator's Datadog and otel backends now receive the full engine tuning profile and image identity as metadata on every log line and metric — config disclosure to the telemetry backend, not a content path.

## Check 2 — Env reachability (cross-referenced against the measured `allowed_envs`)

The live host's harness carries the 22-entry `allowed_envs` (verbatim on the [harness page](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md), Check 1) with **no logging, debug, or telemetry-content switch**. Every logging knob in this file is a literal: `LOG_FORMAT=json` on all three proxies (`:248`, `:335`, `:374`), `--log-requests-level 0` on both engine command anchors (`:79`, `:138` — the minimum level; both dsv4 replicas inherit `:138` unmodified), `VLLM_LOGGING_LEVEL=INFO` on the dsv4 anchor (`:147`), `NCCL_DEBUG=WARN` (`:89`, `:150`), otelcol `telemetry: logs: level: info` (`:984-985`). **`OHTTP_ENABLED=true` is a literal on all three proxies** (`:250`, `:337`, `:376`) — E2EE posture is not operator-degradable. **No `--api-key` or `--admin-api-key` appears anywhere in the file** (grep-verified) — the engines are unauthenticated on the flat network, which is what makes Check 6's reachability finding bite.

The new env `SGLANG_CHUNKED_PREFILL_ADMISSION_RESERVE=4096` (`:152`) is a **literal**, not an interpolation — not operator-settable through any channel without changing the file (and hence the `file_sha256`). Its name declares a scheduler admission quantity; it is a near-patch symbol that does not exist in upstream sglang, so what it actually gates is an engine-side question deferred to the `ec518148` page. At the manifest layer it touches no log, disk, or egress config.

Interpolated vars **not** in `allowed_envs` — unreachable from the measured env channel, reachable only via compose-manager's non-measured `/compose/up` splice (harness page, Check 1, `BEARER_TOKEN`): `HF_HUB_OFFLINE` (defaulted `0`, `:86`, `:146`), `DSV4_RETURN_HIDDEN_STATES` (`:140`), `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (`:258-259`, `:345-346`, `:384-385`), `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (`:252-253`, `:339-340`, `:378-379`), `CLOUD_API_USAGE_TOKEN` (`:246`, `:333`, `:372`), `MODEL_PROXY_TOKEN`/`PROXY_TOKEN` (`:228-229`), `HTTP_PORT`/`TLS_PORT`/`TLS_PORT_2` (defaulted, `:225-227`), `MONITORING_INGEST_TOKEN` (`:568`).

### MEDIUM (deployed: OFF, splice-armed) — hidden-states return splice on the dsv4 command anchor (egress in API response)

`${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}` (`:140`) — both replicas inherit it. Default OFF (var unset → flag absent); the var is not in `allowed_envs`, so only the `/compose/up` splice channel can enable it — no measurement change. The `:+` operator means **any** non-empty value, `"0"` included, turns it ON. Hidden states are prompt-derived tensors returned in the API response. The base page cited the audited engine's handling; on this build the flag's exact behavior is deferred to the `ec518148` page — the splice reachability is manifest-layer fact either way.

### MEDIUM (deployed: OFF, splice-armed) — unpinned web-search URL and key on all three E2EE-terminating proxies (egress)

`WEB_CONTEXT_SEARCH_URL` + `_API_KEY` (`:258-259`, `:345-346`, `:384-385`) hand an operator-suppliable external URL to the services that decrypt E2EE traffic. Downgraded from HIGH because both deployed proxy builds — unchanged by this delta — were source-verified fail-closed when unset, opt-in per request, sending only a model-generated query: [`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (Qwen3.5 proxy, `:21`) and [`vllm-proxy-rs@b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md) (both dsv4 proxies, `:327`, `:366`). Residual: the URL rides the splice channel with no measurement change.

## Check 3 — Engine launches: logging flags PASS at the manifest layer; every checkpoint revision-pinned

No `--log-requests`, dump, record, trace-to-disk, or profiling flag appears on any command (grep-verified). The new dsv4 flags, one by one:

| new flag / env (line) | class | manifest-layer effect |
|---|---|---|
| `--kv-cache-dtype fp8_e4m3` (`:117`) | config | KV quantization **in GPU memory** — aligns dsv4 with the qwen35 anchor's existing setting (`:67`); no disk tier introduced |
| `--num-continuous-decode-steps 1` (`:124`) | config | scheduler cadence; no content path |
| `--max-prefill-tokens 16384` (`:127`) | config | prefill budget; no content path |
| `--swa-full-tokens-ratio 0.1` (`:128`) | config | sliding-window-attention ratio; no content path |
| `--json-model-override-args '{"dsv4_reasoning_effort_profile":"official"}'` (`:133`) | config | a model-config dict passed on the command line — config metadata, visible in process args and the shipped labels; contains no content and enables no logging. The `dsv4_reasoning_effort_profile` key is a near-patch symbol — semantics deferred engine-side |
| `SGLANG_CHUNKED_PREFILL_ADMISSION_RESERVE=4096` (`:152`) | config (literal env) | Check 2 — near-patch symbol, no manifest-layer sink |

Pinning, service by service:

| service (lines) | image | `--revision` | `--trust-remote-code` |
|---|---|---|---|
| model-sg-qwen35-122b-fp8-tp4 (`:280-324`, anchor `:33-93`) | [`lmsysorg/sglang:v0.5.12-cu129@9e02c8e1`](/images/docker.io/lmsysorg/sglang/sha256-9e02c8e1fe2790a1c445bd5f6814305fe43639a4adb01c8ad1e8e21e750bf581.md) (`:54`) — audited, unchanged | `a099dee7…` (`:58`) | yes (`:78`) — pinned |
| model-sg-dsv4-flash-fp4-tp2-r1 (`:403-450`, anchor `:103-158`) | `nearaidev/sglang@ec518148` (`:110`) — **unaudited, review in flight** | `7872f01b…` (`:115`) | yes (`:113`) — pinned |
| model-sg-dsv4-flash-fp4-tp2-r2 (`:451-498`) | same — inherits anchor image and command entirely (Check 1) | same (`:115`) | yes — pinned |

**Still true, worth naming:** every model is revision-pinned, and the one-shot `model-downloader` (`:161-183`) pre-fetches all three checkpoints at the **same** pinned revisions (`:170`, `:172`, `:174`) before any engine starts (`depends_on: service_completed_successfully` — `:282-284`, `:406-408`, `:454-456`). `--trust-remote-code` is present on all three commands but inert against upstream drift while the revision is pinned; `HF_HUB_OFFLINE` is defaulted `0` (`:86`, `:146`), so pinning is what carries the weight.

### MEDIUM (deployed: ON) — weight downloader resolves huggingface_hub from PyPI unpinned inside the enclave (disk, supply chain)

The uv image is digest-pinned (`:162`), but `uvx --from 'huggingface_hub[hf_xet]' hf download …` — now **three** invocations (`:170`, `:172`, `:174`) — installs `huggingface_hub[hf_xet]` **from PyPI at whatever version exists on deploy day**. That code runs inside the enclave with `HF_TOKEN` (`:179`) and write access to the shared `huggingface_cache` volume the engines read weights from (`:177`, `:82`, `:142`). It never sees message plaintext (runs before serving, downloads only) — cache-poisoning/supply-chain exposure, not a content leak. No attestation-visible trace on drift. Unchanged from the base except the third invocation.

### MEDIUM (deployed: ARMED) — watchdog hang dump onto the shipped stdout stream, now with unverified dump shape (log)

`--watchdog-timeout 1800` (`:129`) survives the EAGLE removal on both dsv4 replicas. On the audited base image this connected to a traced engine-page HIGH: a 30-minute scheduler hang dumps the prompt and output token IDs of every in-flight request to stdout, undisableable — and Check 4's pipelines ship stdout off-box twice on the live host. On this build the dump's existence and shape are **deferred** to the `ec518148` page — the near build derives from the same sglang line, so the prudent assumption is the dump persists until shown otherwise. Note the evidence drift: the base file's own comment documenting real fleet streaming freezes (hang-adjacent conditions) was deleted in this revision (Check 1 item 7); the incident history remains true of the fleet, it is just no longer stated in-file.

### INFO (deployed: ON) — prompt-derived KV stays in GPU memory; durable volumes are weights and kernels (disk)

`SGLANG_ENABLE_UNIFIED_RADIX_TREE=1` (`:154`) keeps prompt-derived KV in GPU memory; `--kv-cache-dtype fp8_e4m3` (`:117`) quantizes it there — no KV/prompt disk tier is configured. Durable volumes are `huggingface_cache` and `kernel_cache` only (`:598-604`; engine mounts `:82-83`, `:142-143`). The live `qwen35-122b` harness does not pin `vm.swappiness` (harness page, Check 6) — the guest-OS swap posture residual from the base page applies to this host.

## Check 4 — Telemetry pipeline: stdout of every plaintext service leaves the box unredacted — twice on the live host

### HIGH (deployed: ON) — engine and proxy stdout ships off-box with no redaction; on the live qwen35-122b host it additionally reaches a third-party US SaaS (telemetry)

Chain, re-verified in this file: every app service logs via the `json-file` driver (anchor `:1-6`) → **otelcol-contrib** (`:561-591`, digest-pinned `:562`, [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)) tails `/var/lib/docker/containers/*/*-json.log` (`:618`, mount `:578`), its `filter/app_logs` (`:909-918`) drops **only** management/telemetry containers (compose-manager, launcher, otelcol-management, otelcol-contrib, datadog-agent, plus unlabeled records), and the app containers' logs — all three engines, all three proxies, nginx, registrar, dcgm, downloader — ship to the literal `https://telemetry.infra.near.ai` (`:946`). **No processor in the pipeline redacts or scrubs content**: `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch` (`:963`) — routing, metadata, and batching only.

The second leg, on the one host running this file: the `qwen35-122b` harness ([`66007399`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md)) runs a **measured Datadog agent** with `DD_LOGS_CONFIG_CONTAINER_COLLECT_ALL=true` shipping every container's stdout to `us3.datadoghq.com` (harness page, Check 3). So on the only host where this revision is live, stdout leaves **twice**, one copy to a third-party US SaaS — and the processes printing to that stream now include two engines running unreviewed bytes (Check 1 HIGH). The guarantee "no content leaves" is exactly equivalent to "no plaintext service ever prints content", which this delta makes temporarily unverifiable on the dsv4 side.

### MEDIUM (deployed: ON) — open OTLP receivers forward spans and metrics verbatim off-box (telemetry)

otelcol's OTLP receivers on `0.0.0.0:4317/4318` (`:610-615`, `expose`d `:574-576`, not host-published) feed `metrics/otlp` and `traces` pipelines (`:969-972`, `:977-980`) that forward **verbatim** to near.ai (`:946`). Any service on the flat network that ever emits prompt-bearing spans gets them shipped unexamined. The measured Datadog agent on this host adds a second open OTLP-logs receiver (harness page, Check 3).

**INFO:** all metrics paths are Prometheus scrapes of `/metrics` — nine scrape jobs (`:693`, `:711`, `:744`, `:777`, `:794`, `:811`, `:828`, `:846`, `:868`), whose dsv4 static-label blocks now carry the expanded config metadata (`:715-743`, `:748-776` — Check 1 INFO); counters and histograms with no content dimension.

## Check 5 — Privileges, mounts, co-tenancy (all unchanged from base; re-cited)

### INFO (deployed: ON) — all three proxies run privileged as root with the attestation socket and TLS key (structural)

`x-vllm-proxy-common` (`:20-31`): `user: root` (`:22`), `privileged: true` (`:23`), plus `ipc: host` via the `x-nvidia` anchor (`:11-18`). Each mounts `dstack.sock` (`:28`) and `certs:ro` (`:29`). A privileged root container is root-equivalent in the guest; these are the E2EE-terminating processes, so this is structural exposure inside the box, not a new reader.

### MEDIUM (deployed: ON) — shared IPC namespace across both models' engines and proxies (cross-model memory exposure)

`ipc: host` (`:13`) is inherited by every engine and proxy: one shared IPC namespace across the Qwen3.5 and DeepSeek stacks — cross-model shared-memory visibility (different customers' plaintext), permitted though not configured. Sharpened by this delta: the namespace is now shared between an audited engine (qwen35) and two unaudited ones.

### MEDIUM (deployed: ON) — dcgm-exporter runs mutable-tag bytes with SYS_ADMIN and GPU access (telemetry, privileged drift)

Two instances of tag-only `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:96`) with `SYS_ADMIN` (`:98-99`) + nvidia runtime (`:501-560`) — bytes can drift under the attestation; see the [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md) (covers tag and config, never bytes).

No service in this file has `pid: host`, `SYS_PTRACE`, or a docker-socket mount (grep-verified; otelcol mounts container **logs** read-only, `:578`) — those capabilities live in the measured harness (compose-manager: harness page, Check 2). **Media fetch: not configured** — both models are text checkpoints; no `--allowed-media-domains`, no media/multimodal flag appears anywhere (grep-verified). Absent at this config, not a control.

## Check 6 — Network topology, nginx, egress (all unchanged from base; re-cited)

### MEDIUM (deployed: ON) — flat external network, no egress restriction on plaintext services (egress)

Joins the measured layer's shared bridge as `external: true` (`dstack_default`, `:593-596`). No `internal: true`, no egress allowlist — every engine can open arbitrary outbound connections; sanctioned engine egress is HF only (`HF_HUB_OFFLINE` defaulted `0`). The engines that could exercise this are now unreviewed bytes (Check 1 HIGH) — the flat network is what an exfiltrating engine would use, which is why the engine review is load-bearing.

### MEDIUM (deployed: ON) — unauthenticated engine admin surface reachable through bare nginx locations and allowlist-free proxies (log-enable path)

nginx (digest-pinned `:185`, conf `:1174-1266`) publishes **plaintext HTTP host ports 8000-8002** plus TLS on 8444→443 and the second TLS listener on host 8003 (`:193-197`; listeners `:1206`, `:1212`, `:1218`, `:1227`, `:1242`, `:1260`) — the per-replica endpoint split survives (in-file comment `:314-325`; r1 via 8444, r2 via 8003, distinct backends `:1168-1169`, port map `:1009-1013`, 8003 ACL rationale `:1250-1258`). All **six** server blocks route with a bare `location /` (`:1207`, `:1213`, `:1219`, `:1238`, `:1247`, `:1265`) — no path allowlist; and the `b0c5cd07` dsv4 proxies forward any path (their source page). On the audited base image the exposed surface included the unauthenticated `/configure_logging` (CRITICAL on its engine page); whether the near build carries the same surface is deferred to the `ec518148` page — no `--api-key` exists anywhere in this file (Check 2), so **whatever** admin surface the engine has is unauthenticated and reachable both laterally and through the front door. Mitigations in-file: upstreams are always the proxies, never raw engines; `proxy_buffering off; proxy_cache off` (`:1198-1199` — no request bodies buffered to disk).

### INFO (deployed: ON) — nginx access log ships per-request user-attributable metadata off-box (log)

The access log is `json_combined` with `$uri` not `$request` (`:1175-1190`) — no query strings or bodies — but `request_id`/`org_id`/`workspace_id` (`:1177-1179`) make it per-request, user-attributable metadata that Check 4's pipeline ships off-box.

**Named outbound destinations, exhaustively:** proxies → `https://cloud-api.near.ai` (literal, `:245`, `:332`, `:371`) + `http://compose-manager:8080` (`:247`, `:334`, `:373`; host-gateway `:25-26`) + `${WEB_CONTEXT_SEARCH_URL}` (unset default — Check 2); registrar → `https://completions.near.ai` (literal, `:990`), registering both model domains (`:1168-1169`) and health-probing backends with a **synthetic** `"hi"` message (`:1058` — not user content) plus a TLS-route self-check via SNI resolve (`:1093-1095`); engines + downloader → HF hub; downloader → PyPI (Check 3); otelcol → `https://telemetry.infra.near.ai` (`:946`, bearer `:948`/`:568`). The measured layer adds Datadog and the compose-manager fetch/notify paths (harness page, Checks 3-4).

## Check 7 — Lineage: how this revision reached production

### INFO (deployed: ON, posture) — the promoted engine bytes never ran the canary they were promoted from (lineage)

Per the signed action log, all today: the 0731 checkpoint was canaried on r1 alone for ~90 minutes (07:56Z-09:28Z) under image `eac0a7c825c1…`, rolled back to the audited `11da948b`, and then at 12:47Z promoted to **both** replicas by this file — under a **different, newer** image build (`ec518148…`). Whatever assurance the canary produced attached to bytes that are not the bytes now serving; both the canary image and the promoted image were unaudited at their deploy times. This is the second observation on this lineage (base page, Check 7) that the log-pinned layer routes production traffic through unreviewed engine bytes with no measurement change — the compensating control remains the signed action log that makes the windows visible, and the in-flight `ec518148` review that can close this one retroactively.

## Verdict

**QUALIFIED PASS at this layer — with the engine-image qualification now leading.** The recipe itself stays clean on delta review: no logging switch handed to any engine or proxy, `--log-requests-level 0` kept on both anchors, OHTTP forced on literally, every checkpoint revision-pinned and pre-fetched at those pins, every telemetry destination literal, all new flags/env/labels config-metadata only, no plaintext buffered to disk at any hop (Checks 1-4). The qualifications, in descending order of cost to the end-to-end claim: **(1)** both DeepSeek replicas run engine bytes with no published review — the print discipline, watchdog dump shape, and admin surface that every conditional guarantee on this page hangs from are unverified until the [`ec518148` page](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) lands (Check 1); **(2)** whatever any plaintext service prints leaves the TEE unredacted, twice on the live host including a third-party US SaaS (Check 4); **(3)** the engines' admin surface — whatever it is on this build — is unauthenticated behind six bare `location /` blocks and allowlist-free proxies (Check 6); **(4)** the `/compose/up` splice channel can set `DSV4_RETURN_HIDDEN_STATES` and `WEB_CONTEXT_SEARCH_URL` with no measurement change (Check 2); **(5)** the weight downloader resolves `huggingface_hub` unpinned from PyPI inside the enclave, now three times (Check 3). Recommendations, beyond the base page's standing ones (path allowlist; pin the downloader): publish the engine review **before** promotion rather than concurrently with it, and promote the bytes that ran the canary, not a successor build.

## Not traced

- **The engine bytes** (`nearaidev/sglang@ec518148…`): print discipline at the configured level, the `/configure_logging` surface, the watchdog dump, and the near-patch symbols (`SGLANG_CHUNKED_PREFILL_ADMISSION_RESERVE`, `dsv4_reasoning_effort_profile`, `SGLANG_DSV4_FP4_EXPERTS`) — all deferred to the in-flight image review; this page's conditional guarantees are open until it lands.
- Whether this `file_sha256` matches the signed action log's current entry on the live host — asserted by the orchestrator's capture at commit `5d99a34f…` and re-hashed locally (matches), but the log itself was not re-fetched in this review.
- Whether the `dsv4-flash` host has since converged on this revision (observed still on `11da948b` in the sampling round) — the fleet drift check, not this page, tracks that.
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source pages).
- Proxy internals (no-allowlist forwarding, web-search fail-closed behavior) — proxy source pages, relied on above.
- PyPI state on deploy day for the downloader; dcgm-exporter bytes under its mutable tag; whether the guest has a swap device (guest-OS leg, [`os/`](/os/)) — sharpened here by the live harness not pinning `vm.swappiness`.
- Today's live fleet routing (backend counts and traffic split across the two hosts and gpu26): the in-file comment (`:314-325`) describes the July topology; not re-traced into cloud-api's current backend registry.
