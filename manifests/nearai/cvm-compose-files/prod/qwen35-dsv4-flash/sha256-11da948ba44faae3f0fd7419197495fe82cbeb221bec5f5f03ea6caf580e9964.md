# cvm-compose-files prod/qwen35-dsv4-flash.yaml @ 11da948b... (two-model inner compose, post-A/B — Qwen3.5-122B + DeepSeek-V4-Flash, single engine image, now live on two hosts)

## verdict: QUALIFIED PASS — delta review against the audited A/B revision 5be223d3: the change is removals-only (the DeepSeek prefill-fairness A/B ends; r2 drops its image override and env block and inherits the audited anchor image, with zero experiment residue anywhere in the file); no content-logging switch exists, OHTTP is forced on for all three proxies, every model is revision-pinned; the carried residuals are the fleet's — splice-reachable env, an unauthenticated engine admin surface behind allowlist-free proxies, unredacted stdout shipping (off-box twice on the qwen35-122b host, once on dsv4-flash), and one unpinned PyPI resolve in the weight downloader

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/qwen35-dsv4-flash.yaml` |
| **file_sha256** | `11da948ba44faae3f0fd7419197495fe82cbeb221bec5f5f03ea6caf580e9964` (re-verified locally: `shasum -a 256` of the captured 1220-line file matches) |
| **role** | log-pinned inner compose: two model endpoints — `Qwen/Qwen3.5-122B-A10B` (1x SGLang FP8 TP4, GPUs 0-3) and `deepseek-ai/DeepSeek-V4-Flash` (2x SGLang FP4 TP2 replicas, GPUs 4-5 / 6-7, **both now on the same audited engine image**) — behind three `vllm-proxy-rs` instances, nginx, registrar, two dcgm exporters, otelcol-contrib, and a one-shot weight downloader |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log of both live hosts — **not** hardware-measured. Live on **two** differently-measured hosts (fleet attestation sampled 2026-08-10): `dsv4-flash` under harness [`d98a2568…`](/manifests/measured/sha256-d98a2568546afce9ab53f7eb4d4b2058e5497e1efd05a8ad0934793043240ed7.md) and `qwen35-122b` under harness [`66007399…`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md); each harness page's findings apply on top, per host (Check 4) |
| **lineage** | this exact content **first** ran 2026-06-24→2026-07-27 (compose_up 2026-07-27T09:27Z, commit `575d8f8665…`), was superseded 2026-07-28 by the audited A/B revision [`5be223d3…`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-5be223d330932d118eb8ceee6414304af43c98d9a0f43b8195c8d86dfb77d000.md), and returned 2026-08-10T09:28Z at commit `f53931c48395e3ddce9a26fbba814e2e83e052b3` (content byte-identical, hash-verified). See the INFO finding in Check 7 for the transient unaudited canary that ran between 07:56Z and 09:28Z today |
| **review** | reviewed 2026-08-10 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review, base = the [`5be223d3` page](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-5be223d330932d118eb8ceee6414304af43c98d9a0f43b8195c8d86dfb77d000.md) (which reviewed the full 1318-line A/B file). The orchestrator's characterization of the diff (removals-only, ~101 lines) was **independently re-verified against the captured file**: every load-bearing base finding was re-located and re-cited at this file's line numbers, and the whole file was swept for experiment residue and for anything the base review's file did not contain (this content is *older* than the base's — the base of the base). This file predates the A/B: it is the pre-experiment production shape the A/B was built on, now restored.

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-08-10).
> Method: [/notes/method.md](/notes/method.md) · Base: [5be223d3](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-5be223d330932d118eb8ceee6414304af43c98d9a0f43b8195c8d86dfb77d000.md) · Harnesses: [dsv4-flash @ d98a2568](/manifests/measured/sha256-d98a2568546afce9ab53f7eb4d4b2058e5497e1efd05a8ad0934793043240ed7.md), [qwen35-122b @ 66007399](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md)

## Check 1 — What changed from the audited base (the A/B teardown), verified line-by-line

Relative to `5be223d3`, this file removes — and removes only:

1. **r2's image override and environment block** (`model-sg-dsv4-flash-fp4-tp2-r2`, `:450-483`): the `nearaidev/sglang@sha256:1e335c48…` override and the eleven-entry env block (which was the anchor's ten entries plus `SGLANG_EAGLE_PREFILL_FAIRNESS=1`) are gone. r2 now declares **only** `container_name` (`:452`), `depends_on` (`:453-455`), GPU `device_ids ["6","7"]` (`:460`), and labels (`:464-483`).
2. **All experiment metadata**: the `experiment_id/arm/eagle/…` tag sets in the Datadog `ad.logs`/`ad.instances` labels (r2's are now the plain per-service sets, `:466-467`), the seven experiment regex parsers in otelcol's filelog operators, and the experiment static labels in the Prometheus scrape configs.
3. **The A/B comment block** in the dsv4 command anchor.

**Residue sweep — clean.** Case-insensitive grep over the whole file for `fairness`, `experiment`, `1e335c48`, and `nearaidev/sglang`: zero hits. `SGLANG_EAGLE_PREFILL_FAIRNESS` does not appear anywhere. The only `image:` lines in the file are the anchor proxies/engines and sidecars (`:23`, `:56`, `:98`, `:113`, `:178`, `:199`, `:224`, `:341`, `:380`, `:546`) — **no `nearaidev/sglang` reference remains**.

**r2 inheritance is complete.** Via `<<: *dsv4-flash-common` (`:451`), r2 inherits from the anchor (`:105-174`) the image `lmsysorg/sglang@sha256:6bb5fee3…` (`:113` — [audited page](/images/docker.io/lmsysorg/sglang/sha256-6bb5fee34b6c4537c09a4775e2292ac40350d5ad1218fcc835b2692142f443b1.md)), the full command (`:129-157`) including `--log-requests-level 0` (`:155`) and `--revision 553034d7…` (`:133`), the ten-entry environment block (`:161-171`), volumes (`:158-160`), and logging config (`:174`). **Both dsv4 replicas now run the same audited engine image with byte-identical flags and env** — the only differences between r1 (`:417-449`) and r2 (`:450-483`) are container name, GPU ids, and per-instance label values (port/instance tags).

### INFO (deployed: OFF) — A/B teardown narrowed telemetry metadata; no log-collection widening (telemetry)

The label shortening is metadata reduction only. The load-bearing collection switch — the `com.datadoghq.ad.logs` label that marks a container's stdout for Datadog log pickup — is **still present on every app service**, engines included (`:309` qwen35, `:433` r1, `:466` r2, `:275`/`:362`/`:401` proxies, `:221` nginx, `:250` registrar, `:196` downloader, `:499`/`:529` dcgm, `:575` otelcol), and the `json-file` driver anchor still exports it (`:6`). So the systemic stdout-shipping HIGH below is unchanged by the teardown — neither widened nor closed. The otelcol filelog parsers now extract ten metadata fields (`parse_from` regex parsers at `:631-:667`: model, model_path, precision, deployment, env, host, ip, port, instance, replica) — label extraction, not content selection.

## Check 2 — Env reachability (cross-referenced against both measured `allowed_envs`)

Both harnesses carry the **byte-identical 22-entry `allowed_envs`** (verbatim on each measured page, Check 1) with **no logging, debug, or telemetry-content switch**. Every logging knob in this file is a literal: `LOG_FORMAT=json` on all three proxies (`:262`, `:349`, `:388`), `--log-requests-level 0` on both engine command anchors (`:81`, `:155` — the minimum level; both dsv4 replicas inherit `:155` unmodified), `VLLM_LOGGING_LEVEL=INFO` on the dsv4 anchor (`:164` — one occurrence now, inherited by both replicas; the base's second copy died with r2's env block), `NCCL_DEBUG=WARN` (`:91`, `:167`), otelcol `telemetry: logs: level: info` (`:935-939`). **`OHTTP_ENABLED=true` is a literal on all three proxies** (`:264`, `:351`, `:390`) — E2EE posture is not operator-degradable. **No `--api-key` or `--admin-api-key` appears anywhere in the file** (grep-verified) — the engines are unauthenticated on the flat network, which is what makes Check 6's reachability finding bite.

Interpolated vars **not** in `allowed_envs` — unreachable from the measured env channel, reachable only via compose-manager's non-measured `/compose/up` splice (both harness pages, Check 1, `BEARER_TOKEN`): `HF_HUB_OFFLINE` (defaulted `0`, `:88`, `:163`), `DSV4_RETURN_HIDDEN_STATES` (`:157`), `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (`:272-273`, `:359-360`, `:398-399`), `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (`:266-267`, `:353-354`, `:392-393`), `CLOUD_API_USAGE_TOKEN` (`:260`, `:347`, `:386`), `MODEL_PROXY_TOKEN`/`PROXY_TOKEN` (`:242-243`), `HTTP_PORT`/`TLS_PORT`/`TLS_PORT_2` (defaulted, `:239-241`).

### MEDIUM (deployed: OFF, splice-armed) — hidden-states return splice on the dsv4 command anchor (egress in API response)

`${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}` (`:157`) — both replicas inherit it. Default OFF (var unset → flag absent); the var is not in `allowed_envs`, so only the `/compose/up` splice channel can enable it — no measurement change. The `:+` operator means **any** non-empty value, `"0"` included, turns it ON. Hidden states are prompt-derived tensors returned in the API response; engine-side trace: [`sglang@6bb5fee3`](/images/docker.io/lmsysorg/sglang/sha256-6bb5fee34b6c4537c09a4775e2292ac40350d5ad1218fcc835b2692142f443b1.md).

### MEDIUM (deployed: OFF, splice-armed) — unpinned web-search URL and key on all three E2EE-terminating proxies (egress)

`WEB_CONTEXT_SEARCH_URL` + `_API_KEY` (`:272-273`, `:359-360`, `:398-399`) hand an operator-suppliable external URL to the services that decrypt E2EE traffic. Downgraded from HIGH because both deployed proxy builds were source-verified fail-closed when unset, opt-in per request, sending only a model-generated query: [`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (Qwen3.5 proxy, `:23`) and [`vllm-proxy-rs@b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md) (both dsv4 proxies, `:341`, `:380`). Residual: the URL rides the splice channel with no measurement change.

## Check 3 — Engine launches: logging flags PASS; every model revision-pinned

No `--log-requests`, dump, record, trace-to-disk, or profiling flag appears on any command (grep-verified). Pinning, service by service:

| service (lines) | image | `--revision` | `--trust-remote-code` |
|---|---|---|---|
| model-sg-qwen35-122b-fp8-tp4 (`:293-326`, anchor `:35-95`) | [`lmsysorg/sglang:v0.5.12-cu129@9e02c8e1`](/images/docker.io/lmsysorg/sglang/sha256-9e02c8e1fe2790a1c445bd5f6814305fe43639a4adb01c8ad1e8e21e750bf581.md) (`:56`) | `a099dee7…` (`:60`) | yes (`:80`) — pinned |
| model-sg-dsv4-flash-fp4-tp2-r1 (`:417-449`, anchor `:105-174`) | [`lmsysorg/sglang@6bb5fee3`](/images/docker.io/lmsysorg/sglang/sha256-6bb5fee34b6c4537c09a4775e2292ac40350d5ad1218fcc835b2692142f443b1.md) (`:113`) | `553034d7…` (`:133`) | yes (`:131`) — pinned |
| model-sg-dsv4-flash-fp4-tp2-r2 (`:450-483`) | same — inherits anchor image and command entirely (Check 1) | same (`:133`) | yes — pinned |

**PASS, worth naming:** every model on this node is revision-pinned, and the one-shot `model-downloader` (`:177-197`) pre-fetches both checkpoints at the **same** pinned revisions (`:186`, `:188`) before any engine starts (`depends_on: service_completed_successfully` — `:296-298`, `:420-422`, `:453-455`). `--trust-remote-code` is present on all three commands but inert against upstream drift while the revision is pinned; `HF_HUB_OFFLINE` is defaulted `0` (`:88`, `:163`), so pinning is what carries the weight.

### MEDIUM (deployed: ON) — weight downloader resolves huggingface_hub from PyPI unpinned inside the enclave (disk, supply chain)

The uv image is digest-pinned (`:178`), but `uvx --from 'huggingface_hub[hf_xet]' hf download …` (`:186`, `:188`) installs `huggingface_hub[hf_xet]` **from PyPI at whatever version exists on deploy day**. That code runs inside the enclave with `HF_TOKEN` (`:193`) and write access to the shared `huggingface_cache` volume the engines read weights from (`:191`, `:84`, `:159`). It never sees message plaintext (runs before serving, downloads only) — cache-poisoning/supply-chain exposure, not a content leak. No attestation-visible trace on drift.

### MEDIUM (deployed: ARMED) — watchdog hang dump of in-flight prompt and output token IDs lands on the shipped stdout stream (log)

`--watchdog-timeout 1800` (`:143`) on both dsv4 replicas connects directly to the engine pages' **HIGH**: on any 30-minute scheduler hang, this build dumps the prompt and output token IDs of every in-flight request to stdout, undisableable — and Check 4's pipelines ship stdout off-box (twice on the qwen35-122b host, including to Datadog). Configured-ON exposure conditional on a hang, not steady-state; the anchor's own comment (`:123-128`) documents that hang-adjacent streaming freezes have actually occurred on this fleet.

### INFO (deployed: ON) — prompt-derived KV stays in GPU memory; durable volumes are weights and kernels; swap posture differs per host (disk)

`SGLANG_ENABLE_UNIFIED_RADIX_TREE=1` (`:170`) keeps prompt-derived KV in GPU memory; no KV/prompt disk tier is configured. Durable volumes are `huggingface_cache` and `kernel_cache` only (`:582-588`). Per-host asymmetry: the `dsv4-flash` harness pins `vm.swappiness=0` in its measured `pre_launch_script`; the `qwen35-122b` harness does not tune the kernel at all (both harness pages, Check 6) — same recipe, different KV-swap discipline, chosen by which backend routing picks.

## Check 4 — Telemetry pipeline: stdout of every plaintext service leaves the box unredacted — per host, once or twice

### HIGH (deployed: ON, per-host) — engine and proxy stdout ships off-box with no redaction; on the qwen35-122b host it additionally reaches a third-party US SaaS (telemetry)

Chain, re-verified in this file: every app service logs via the `json-file` driver (anchor `:1-6`) → **otelcol-contrib** (`:545-575`, digest-pinned `:546`, [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)) tails `/var/lib/docker/containers/*/*-json.log` (`:601-602`, mount `:562`), its `filter/app_logs` (`:863-872`) drops **only** management/telemetry containers (compose-manager, launcher, otelcol-management, otelcol-contrib, datadog-agent, plus unlabeled records), and the app containers' logs — all three engines, all three proxies, nginx, registrar, dcgm, downloader — ship to the literal `https://telemetry.infra.near.ai` (`:899-900`). **No processor in the pipeline redacts or scrubs content**: `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch` (`:917`) — routing, metadata, and batching only.

The second leg is per-host, and this is a **change from the base page**, which saw the file on the gpu30-class host only:

- **`qwen35-122b` host** (harness [`66007399`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md)): a **measured Datadog agent** with `DD_LOGS_CONFIG_CONTAINER_COLLECT_ALL=true` ships every container's stdout to `us3.datadoghq.com` (harness page, Check 3). Stdout leaves **twice**, one copy to a third-party US SaaS. The base page's "double stdout shipping" HIGH applies unchanged here.
- **`dsv4-flash` host** (harness [`d98a2568`](/manifests/measured/sha256-d98a2568546afce9ab53f7eb4d4b2058e5497e1efd05a8ad0934793043240ed7.md)): **no Datadog agent** — the only off-box log path is this file's otelcol to near.ai. Stdout leaves **once**, to the operator. That harness instead mounts the entire guest root filesystem read-only into its management collector (`/:/hostfs:ro` — its Check 2 MEDIUM), and its measured `pre_launch_script` replays a persisted compose-manager image override at boot (its Check 6) — control-plane findings that sit on top of this recipe on that host.

Either way the guarantee "no content leaves" is exactly equivalent to "no plaintext service ever prints content" — carried by the per-image source pages, with the engines' unauthenticated `/configure_logging` (CRITICAL on the engine pages) and the watchdog dump (ARMED above) as the two known ways prints get switched on or forced.

### MEDIUM (deployed: ON) — open OTLP receivers forward spans and metrics verbatim off-box (telemetry)

otelcol's OTLP receivers on `0.0.0.0:4317/4318` (`:595-599`, `expose`d `:559-560`, not host-published) feed `metrics/otlp` and `traces` pipelines (`:923-926`, `:931-934`) that forward **verbatim** to near.ai. Any service on the flat network that ever emits prompt-bearing spans gets them shipped unexamined. On the qwen35-122b host the measured Datadog agent adds a second open OTLP-logs receiver (harness page, Check 3).

**INFO:** all metrics paths are Prometheus scrapes of `/metrics` — nine scrape jobs (`:677`, `:695`, `:713`, `:731`, `:748`, `:765`, `:782`, `:800`, `:822`), now carrying only routing labels (model, deployment, env, host, ip, port, instance — the experiment label sets are gone, Check 1); counters and histograms with no content dimension.

## Check 5 — Privileges, mounts, co-tenancy

### INFO (deployed: ON) — all three proxies run privileged as root with the attestation socket and TLS key (structural)

`x-vllm-proxy-common` (`:22-33`): `user: root` (`:24`), `privileged: true` (`:25`), plus `ipc: host` via the `x-nvidia` anchor (`:13-20`). Each mounts `dstack.sock` (`:30`) and `certs:ro` (`:31`). A privileged root container is root-equivalent in the guest; these are the E2EE-terminating processes, so this is structural exposure inside the box, not a new reader.

### MEDIUM (deployed: ON) — shared IPC namespace across both models' engines and proxies (cross-model memory exposure)

`ipc: host` (`:15`) is inherited by every engine and proxy: one shared IPC namespace across the Qwen3.5 and DeepSeek stacks — cross-model shared-memory visibility (different customers' plaintext), permitted though not configured.

### MEDIUM (deployed: ON) — dcgm-exporter runs mutable-tag bytes with SYS_ADMIN and GPU access (telemetry, privileged drift)

Two instances of tag-only `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:98`) with `SYS_ADMIN` (`:101`) + nvidia runtime — bytes can drift under the attestation; see the [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md) (covers tag and config, never bytes).

No service in this file has `pid: host`, `SYS_PTRACE`, or a docker-socket mount (grep-verified; otelcol mounts container **logs** read-only, `:562`) — those capabilities live in the measured harnesses (compose-manager, both hosts: harness pages, Check 2). **Media fetch: not configured** — both models are text checkpoints; no `--allowed-media-domains`, no media/multimodal flag appears anywhere (grep-verified). Absent at this config, not a control.

## Check 6 — Network topology, nginx, egress

### MEDIUM (deployed: ON) — flat external network, no egress restriction on plaintext services (egress)

Joins the measured layer's shared bridge as `external: true` (`dstack_default`, `:577-580`). No `internal: true`, no egress allowlist — every engine can open arbitrary outbound connections; sanctioned engine egress is HF only (`HF_HUB_OFFLINE` defaulted `0`).

### MEDIUM (deployed: ON) — unauthenticated engine admin surface reachable through bare nginx locations and allowlist-free proxies (log-enable path)

nginx (digest-pinned `:199`, config `:1126-1220`) publishes **plaintext HTTP host ports 8000-8002** plus TLS on 8444→443 and the **second TLS listener on host 8003** (`:206-211`) — 8003 still exists post-A/B because the per-replica endpoint split survives the experiment: each dsv4 replica keeps its own proxy and TLS routing port (in-file comment `:328-338`; r1 via 8444, r2 via 8003, registered as distinct backends `:1122-1123`, port map `:966`). All **six** server blocks route with a bare `location /` (`:1161`, `:1167`, `:1173`, `:1192`, `:1201`, `:1219`) — no path allowlist; and the `b0c5cd07` dsv4 proxies forward any path (their source page). So the engines' unauthenticated `/configure_logging` (no `--api-key` anywhere, Check 2) is reachable both laterally on the flat network and through the front door by any valid credential holder. Mitigations in-file: upstreams are always the proxies, never raw engines; `proxy_buffering off; proxy_cache off` (`:1152-1153` — no request bodies buffered to disk).

### INFO (deployed: ON) — nginx access log ships per-request user-attributable metadata off-box (log)

The access log is `json_combined` with `$uri` not `$request` (`:1129-1144`) — no query strings or bodies — but `request_id`/`org_id`/`workspace_id` (`:1131-1133`) make it per-request, user-attributable metadata that Check 4's pipeline ships off-box.

**Named outbound destinations, exhaustively:** proxies → `https://cloud-api.near.ai` (literal, `:259`, `:346`, `:385`) + `http://compose-manager:8080` (`:261`, `:348`, `:387`; host-gateway `:27-28`) + `${WEB_CONTEXT_SEARCH_URL}` (unset default — Check 2); registrar → `https://completions.near.ai` (literal, `:944`), registering both model domains (`:1122-1123`) and health-probing backends with a **synthetic** `"hi"` message (`:1012` — not user content) plus a TLS-route self-check via SNI resolve (`:1043-1053`); engines + downloader → HF hub; downloader → PyPI (Check 3); otelcol → `https://telemetry.infra.near.ai` (`:900`). The measured layer adds, per host: Datadog (`qwen35-122b` only) and the compose-manager fetch/notify paths (harness pages, Check 4).

## Check 7 — Lineage: what ran between the audited revisions

### INFO (deployed: OFF, historical) — transient canary ran r1 on an unaudited engine image for ~90 minutes today (lineage)

Per the signed action log: between 07:56Z and 09:28Z on 2026-08-10, a transient revision `6cccfec702b2…` (commit `c8ad95a9c8b7…`) ran a DeepSeek-V4-Flash-0731 checkpoint on r1 under an **unaudited** image `nearaidev/sglang@sha256:eac0a7c825c1…`, then was rolled back to this file. That revision is not live and is not the target of this review; no page exists for that image or that file_sha256, so under this repo's fail-closed rule the app shows no audit link for the window. Recorded because config churn through unaudited images is itself a posture observation: the log-pinned layer can (and did) route production traffic through unreviewed engine bytes with no measurement change — the compensating control is exactly the signed action log that made this window visible.

## Verdict

**QUALIFIED PASS at this layer.** The delta from the audited base is a clean experiment teardown: r2 returns to the audited anchor image with zero residue (Check 1), and everything load-bearing carries over unchanged — no logging switch handed to any engine or proxy, OHTTP forced on literally, every model revision-pinned and pre-fetched at those pins, every telemetry destination literal, no plaintext buffered to disk at any hop. The qualifications, in descending order of cost to the end-to-end claim: **(1)** the engines' unauthenticated `/configure_logging` (CRITICAL on the engine pages) is reachable from the flat network and through the front door — a path allowlist would close it and none exists (Check 6); **(2)** whatever any plaintext service prints leaves the TEE unredacted — twice on the `qwen35-122b` host including a third-party US SaaS, once on `dsv4-flash` — so the posture is conditional on per-image print discipline, and on no 30-minute hang given the ARMED watchdog dump (Checks 3, 4); **(3)** the `/compose/up` splice channel can set `DSV4_RETURN_HIDDEN_STATES` and `WEB_CONTEXT_SEARCH_URL` with no measurement change (Check 2); **(4)** the weight downloader resolves `huggingface_hub` unpinned from PyPI inside the enclave (Check 3). Same recommendations as the base page, still open: front the engines with a path allowlist; pin the downloader; and align the two hosts' harness posture (today a user's requests land on a host with either third-party log shipping *or* no swappiness pinning, by routing lottery).

## Not traced

- Whether this `file_sha256` matches the signed action log's current entries on both hosts — asserted by the orchestrator's capture and re-hashed locally (matches), but the log itself was not re-fetched in this review.
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source pages).
- Engine print discipline at the configured levels, and the exact `/configure_logging` and watchdog behavior — engine source pages ([`6bb5fee3`](/images/docker.io/lmsysorg/sglang/sha256-6bb5fee34b6c4537c09a4775e2292ac40350d5ad1218fcc835b2692142f443b1.md), [`9e02c8e1`](/images/docker.io/lmsysorg/sglang/sha256-9e02c8e1fe2790a1c445bd5f6814305fe43639a4adb01c8ad1e8e21e750bf581.md)) — load-bearing under the stdout-shipping HIGH.
- Proxy internals (no-allowlist forwarding, web-search fail-closed behavior) — proxy source pages, relied on above.
- The transient canary window (Check 7): what `nearaidev/sglang@eac0a7c825c1…` would have logged is unknown — that image has no page and its bytes were not reviewed.
- PyPI state on deploy day for the downloader; dcgm-exporter bytes under its mutable tag; whether either guest has a swap device (guest-OS leg, [`os/`](/os/)).
- Today's live fleet routing (how many `dsv4-flash` backends exist across the two hosts and gpu26, and the traffic split): the in-file comment (`:328-338`) describes the July topology; the two-host deployment observed today was not traced into cloud-api's current backend registry.
