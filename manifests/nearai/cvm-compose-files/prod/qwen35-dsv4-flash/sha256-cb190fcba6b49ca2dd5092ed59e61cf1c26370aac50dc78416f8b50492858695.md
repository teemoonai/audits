# cvm-compose-files prod/qwen35-dsv4-flash.yaml @ cb190fcb... (two-model inner compose — DS4F consolidates to one TP4 replica, sparse prefill on, on an unreviewed nightly engine; Qwen3.5 stack byte-identical)

## verdict: QUALIFIED PASS at the manifest layer — the recipe stays clean on delta review (no content-logging switch anywhere in the diff, --log-requests-level 0 kept on both anchors, OHTTP forced on literally, every checkpoint revision-pinned and pre-fetched at its pin, the three new engine env vars are literal compute/memory config naming no log or dump sink, the new ab_arm/sparse_prefill/compress_state_dtype telemetry tags are static deployment metadata, and the retired r2/8002/8003 topology leaves zero dangling references — the front door shrinks from six listeners to four), but the consolidation re-opens the exact gap the base revision had just closed: the single TP4 DeepSeek replica now runs its plaintext on an unreviewed nightly engine build (5bc4bc0dfd36, labeled nightly-c0b6474-near-v1) whose commissioned review was unpublished at review time, so every dsv4-side engine assurance — print discipline, watchdog dump shape, /configure_logging — is an inherited expectation, not a verified fact; the carried residuals stand — unredacted stdout shipping off-box, an unauthenticated /configure_logging behind four bare nginx locations and allowlist-free proxies with no API key anywhere, the qwen35 watchdog dump still on its 300 s default fuse, the unguarded qwen35 media fetch, splice-reachable env, one unpinned PyPI resolve, and tag-pinned privileged dcgm

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/qwen35-dsv4-flash.yaml` |
| **file_sha256** | `cb190fcba6b49ca2dd5092ed59e61cf1c26370aac50dc78416f8b50492858695` (re-verified locally: `shasum -a 256` of the captured 1084-line file matches, and `git show bde3ddd87955:prod/qwen35-dsv4-flash.yaml` reproduces the identical bytes) |
| **role** | log-pinned inner compose: two model endpoints — `Qwen/Qwen3.5-122B-A10B` (1x SGLang FP8 TP4, GPUs 0-3, upstream v0.5.16-cu129, unchanged) and `deepseek-ai/DeepSeek-V4-Flash` (now **one** TP4 replica spanning GPUs 4-7, 0731 checkpoint, on the near-built **nightly** `5bc4bc0dfd36…`) — behind **two** `vllm-proxy-rs` instances, nginx, registrar, two dcgm exporters, otelcol-contrib, and a one-shot weight downloader |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. Fetched at the log-pinned commit `bde3ddd87955` ("DS4F: disable fused TopK v2 (#184)", 2026-08-21) and hash-checked against the log's `file_sha256` before review (matches). Live on the `qwen35-122b` host (`qwen35-122b.completions.near.ai`) per the orchestrator's fleet capture of 2026-08-21; the in-file layout comment names gpu30 (`:8-9`) |
| **lineage** | supersedes [`14ae94e4`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-14ae94e403ce6de638054b6421f105ab01563521aa150e605440b92e71bfea0f.md) (2026-08-17, the v0.5.16 qwen35 bump — the first revision on this lineage with no unreviewed bytes; this revision gives that property back up), itself over [`d1cdff70`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-d1cdff7016b9b1fef4ad1662678404eca67c07c4e2ca8993795a0f8dbe15af1c.md) and [`11da948b`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-11da948ba44faae3f0fd7419197495fe82cbeb221bec5f5f03ea6caf580e9964.md) |
| **review** | reviewed 2026-08-21 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review, base = the [`14ae94e4` page](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-14ae94e403ce6de638054b6421f105ab01563521aa150e605440b92e71bfea0f.md) (QUALIFIED PASS at the manifest layer). The diff was taken **against the actual base bytes**, not the base page: both revisions were re-fetched from the pinned commits (`ed7dac33e50b` → `14ae94e403ce…`, `bde3ddd87955` → `cb190fcba6b4…`, both hashes re-verified locally) and the 523-line diff (1270 → 1084 lines) was independently regenerated with `git diff` and matched hunk-for-hunk against the orchestrator's capture. Every carried finding below was re-located and re-cited at this file's line numbers; every negative claim ("everything else is byte-identical") is a `diff` fact, not an assumption.

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-08-21).
> Method: [/notes/method.md](/notes/method.md) · Base: [14ae94e4](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-14ae94e403ce6de638054b6421f105ab01563521aa150e605440b92e71bfea0f.md)

## Check 1 — What changed from the audited base, verified against the base bytes

The whole diff is confined to the DeepSeek arm and its plumbing; the Qwen3.5 stack is untouched:

1. **Engine image swap** (`:117`): `nearaidev/sglang@sha256:ec518148…` (audited, PRIVATE at deployed flags) → `nearaidev/sglang@sha256:5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b` — a **nightly** build, self-described by its own labels as `sglang_revision: nightly-c0b6474-near-v1` (`:380`, `:412`, `:663`) and by the anchor comment as carrying the upstream overlap-scheduler WAR-fence alignment from sgl-project/sglang#33587, with DSPARK disabled and gpu04 as the matched sparse-off treatment (`:110-116`).
2. **Topology consolidation, TP2 → TP4**: `--tp-size 4` (`:123`); the r1/r2 replica pair becomes the single service `model-sg-dsv4-flash-fp4-tp4` (`:364`) on GPUs 4-7 (`:375`). The command is otherwise byte-identical to the base anchor (diff fact — the only command change in the whole file is `--tp-size 2` → `4`).
3. **Three new env literals** on the dsv4 anchor, all config-only at this layer: `SGLANG_DSV4_COMPRESS_STATE_DTYPE=bf16` (`:162` — compressed-KV dtype in GPU memory; the comment `:161` prices it as +10% KV tokens), `SGLANG_OPT_USE_TOPK_V2=0` (`:165` — crash containment for sgl-project/sglang#34718 per `:163-164`), `SGLANG_OPT_FLASHMLA_SPARSE_PREFILL=1` (`:168` — the A/B treatment, a prefill kernel choice). **None names a logging, dump, debug, or path-like sink**; all three are literals, not operator interpolations.
4. **Second dsv4 proxy and its routing retired**: service `proxy-dsv4-flash-2` deleted; nginx loses ports `8002:8002` and `8003:8003` (published ports now exactly 8000/8001/8444 → 443, `:204-207`) and its `depends_on` entry; the `TLS_PORT_2` interpolation is gone from the registrar env; the registrar's port loops shrink to `8000 8001` (`:875`, `:908`, `:985`), `routing_port_for` collapses to the single `TLS_PORT` (`:880-882`), and the entire `check_tls_route` r2 path-verification function is deleted; nginx drops the `:8002` plaintext block and the `:8003` TLS listener (four server blocks remain: `:1048`, `:1054`, `:1063`, `:1078`).
5. **Telemetry label growth** (the A/B arm published to the backends): `ab_arm:tp4-target-only-sparse-on-gpu30` and `sparse_prefill:on` added to the dsv4 proxy labels (`:346`, `:361-362`), the dsv4 engine labels (`:380-381`, otel `:400`, `:402`), both dcgm-dsv4 label sets (`:460-461`, otel `:476-477`), and the Prometheus static labels (`:651`, `:653`, `:699-700`, `:736-737`); `compress_state_dtype:bf16` added on the engine (`:380-381`, `:406`, `:657`); `config_variant` renamed to `dsv4-0731-tp4-target-only-sparse-on` (`:398`, `:649`); `sglang_revision`/`engine_image` updated to the nightly identity (`:412-413`, `:663-664`). All are **fixed strings written in this file** — no request-derived value exists in any of them (Check 4).
6. **otelcol exporter**: `num_consumers: 1` added to the otlphttp sending queue (`:823`) — export concurrency only, no content effect.
7. **Comment changes**: the layout comment (`:8-9`), the dsv4 anchor rationale (`:110-116`), and deletion of the July per-replica-endpoints topology note that documented the old three-backend split.

**Everything else is byte-identical to the audited base — a diff fact:** the `json-file` logging anchor (`:1-6`), `ipc: host` (`:13`), proxy anchor (`:20-31`), the **entire qwen35 anchor** (`:33-97`), dcgm anchor (`:99-105`), downloader, nginx service (minus ports/depends_on), registrar core, qwen35 proxy and engine services, both dcgm services (minus dsv4 labels), the otelcol service block and its parser/filter/exporter config (minus the label/job edits and `:823`), networks (`:511-514`), volumes (`:516-521`), and the nginx conf core (minus the two removed blocks). Residue sweep: **zero** hits for `ec518148`, `fp4-tp2`, `proxy-dsv4-flash-2`, `8002`, `8003`, `TLS_PORT_2` (grep-verified — the topology shrink left no dangling reference anywhere: probe loops, routing function, nginx blocks, ports, and Prometheus jobs all reduced consistently).

### HIGH (deployed: ON) — DeepSeek plaintext moves onto an unreviewed nightly engine build; the commissioned review is unpublished at review time (posture)

The base lineage's original leading HIGH — plaintext handed to engine bytes with no published review — had been closed one revision ago; this revision re-opens it. The dsv4 arm's only engine, the process that reads every DeepSeek user's prompt, is now `nearaidev/sglang@5bc4bc0dfd36…` (`:117`), a nightly (`nightly-c0b6474-near-v1` per its own labels `:412`) rather than a release: its source review is commissioned in this same audit run and its page will be [`5bc4bc0d`](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md), but **no such page exists in this repo at review time** (checked). Until it lands, the engine-side properties this manifest leans on — stdout print discipline under `--log-requests-level 0` (`:145`), the watchdog dump shape behind `--watchdog-timeout 1800` (`:136`), the presence and default state of `/configure_logging`, and the content of the near.ai patches — are expectations inherited from the audited `ec518148`/upstream lineage, not verified facts about these bytes. The qwen35 side is unaffected (audited [`984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md), `:57`, byte-identical anchor).

### INFO (deployed: ON) — topology shrink retires r2, ports 8002/8003, and the second proxy with zero dangling references (structural)

One fewer E2EE-terminating privileged proxy, two fewer published host ports, two fewer TLS/plaintext listeners, one fewer engine process: the plaintext-touching surface of this file is strictly smaller than the base. Verified clean: no leftover reference to the removed service, ports, or `TLS_PORT_2` anywhere (residue sweep above), and the registrar now registers exactly two backends (8000/8001) with all TLS routing on the single 8444 path (`:880-882`).

### MEDIUM (deployed: ARMED) — qwen35 watchdog hang dump still armed at the engine's 300 s default; the command still passes no --watchdog-timeout (log, then off-box telemetry)

Carried unchanged — the qwen35 anchor is byte-identical, including the absence of any `--watchdog-timeout` (grep: the only occurrence in the file is the dsv4 `:136`). The [`984699c2` page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) traces the undisableable watchdog dump (full prompt + output token IDs of every in-flight request to stdout at ERROR) with a 300 s default at this build, and reads this recipe's command as one that does not override it. A 5-minute engine stall, no attacker, is the trigger; Check 4's pipeline ships the dump off-box. The manifest-layer fix remains one line.

### MEDIUM (deployed: ON) — qwen35 request-driven media fetch remains unguarded: no allowlist flag anywhere in this file, and engine egress is unrestricted (egress)

Carried unchanged: `Qwen3_5MoeForConditionalGeneration` is registered multimodal on the audited engine (engine page, which cites this recipe's `--mm-feature-transport cpu` `:71` as corroboration), and this revision still supplies **no** `--allowed-media-domains`, `--disable-multimodal`, or any media-guard flag (grep-verified, zero hits outside comments), on a network with no egress restriction (Check 6). Request-driven (client-supplied URL), so not exfiltration by itself — but engine-originated egress to arbitrary hosts carrying the URL and the enclave's egress IP. The dsv4 side remains text-only per its lineage; re-confirmation on the nightly is deferred to its page.

## Check 2 — Env reachability (topology smaller; re-cited at this file's lines)

Every logging knob is a **literal**: `LOG_FORMAT=json` on both proxies (`:257`, `:333`), `--log-requests-level 0` on both engine anchors (`:84`, `:145` — the minimum level, load-bearing since the field default on this engine line is 2 per the engine pages), `VLLM_LOGGING_LEVEL=INFO` (`:154`, dsv4 anchor), `NCCL_DEBUG=WARN` (`:94`, `:157`), otelcol `telemetry: logs: level: info` (`:858-859`). **`OHTTP_ENABLED=true` is a literal on both proxies** (`:259`, `:335`) — E2EE posture is not operator-degradable. **No `--api-key` or `--admin-api-key` anywhere** (grep-verified; the only `*API_KEY` strings are the web-search pair) — which keeps Check 6's admin-surface finding live. The operator-interpolable surface **shrank** this revision: `TLS_PORT_2` is gone, and the three new engine env vars are literals, not interpolations. No new volume, mount, or port appears anywhere in the diff — the durable-volume set is unchanged (`:516-521`) and the published-port set shrank (`:204-207`).

### MEDIUM (deployed: OFF, splice-armed) — hidden-states return splice on the dsv4 command anchor (egress in API response)

`${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}` (`:147`), inherited by the single TP4 replica. Unchanged from base: default OFF, not in the measured `allowed_envs`, reachable only via compose-manager's non-measured `/compose/up` splice; **any** non-empty value (including `"0"`) turns it on. Hidden states are prompt-derived tensors.

### MEDIUM (deployed: OFF, splice-armed) — unpinned web-search URL and key on both E2EE-terminating proxies (egress)

`WEB_CONTEXT_SEARCH_URL` + `_API_KEY` (`:267-268`, `:343-344`): an operator-suppliable external URL handed to the processes that decrypt E2EE traffic, riding the splice channel with no measurement change. Held at MEDIUM because both deployed proxy builds were source-verified fail-closed when unset, opt-in per request, sending only a model-generated query (proxy pages, builds unchanged this revision). One fewer instance than base (the third proxy is retired).

Other interpolated vars, none in `allowed_envs` (splice-only): `HF_HUB_OFFLINE` defaulted `0` (`:91`, `:153`), `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (`:261-262`, `:337-338`), `CLOUD_API_USAGE_TOKEN` (`:255`, `:331`), `PROXY_TOKEN`/`MODEL_PROXY_TOKEN` (`:237-238`, consumed as `TOKEN` at `:260`, `:336`), `HTTP_PORT`/`TLS_PORT` defaulted (`:235-236`), `MONITORING_INGEST_TOKEN` (`:486`).

## Check 3 — Engine launches, pinning, and the label-vs-command truthfulness check

No `--log-requests`, dump, record, trace-to-disk, profiling, or crash-dump flag on any command (grep-verified). Pinning, service by service:

| service | image | `--revision` | `--trust-remote-code` |
|---|---|---|---|
| model-sg-qwen35-122b-fp8-tp4 (`:290`, anchor `:33-97`) | [`lmsysorg/sglang:v0.5.16-cu129@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (`:57`) — audited | `a099dee7…` (`:61`) | yes (`:83`) — **pinned** |
| model-sg-dsv4-flash-fp4-tp4 (`:364`, anchor `:107-171`) | `nearaidev/sglang@5bc4bc0d` (`:117`) — **unreviewed nightly** (Check 1 HIGH) | `7872f01b…` (`:122`) | yes (`:120`) — **pinned** |

The single dsv4 service inherits the anchor entirely — its only keys are `container_name` (`:366`), `depends_on` (`:367-369`), GPU `device_ids ["4","5","6","7"]` (`:375`), and labels; **no `command:`, `image:`, or `environment:` override exists in the service block** (verified over `:364-413`). The one-shot downloader (`:174-196`, digest-pinned uv image `:175`) pre-fetches all three checkpoints at the same pins (`:183`, `:185`, `:187`) before any engine starts; `--trust-remote-code` is inert against upstream drift while revisions are pinned.

**Label-vs-command truthfulness — checked; the labels tell the truth, including all the new ones.** Every asserted tuning tag on the dsv4 label sets (`:380`, `:381`, otel `:398-413`, Prometheus `:645-664`) was checked against the deployed command/env: `watchdog_timeout:1800` ↔ `--watchdog-timeout 1800` (`:136`); `sparse_prefill:on` ↔ `SGLANG_OPT_FLASHMLA_SPARSE_PREFILL=1` (`:168`); `compress_state_dtype:bf16` ↔ env `:162`; `chunk_size:8192` ↔ `:133`; `max_prefill_tokens:16384` ↔ `:134`; `continuous_decode_steps:1` ↔ `:131`; `chunked_prefill_admission_reserve:4096` ↔ env `:159`; `max_running_requests:64` ↔ `:128`; `max_queued_requests:16` ↔ `:129`; `schedule_conservativeness:1.3` ↔ `:130`; `cuda_graph:1` ↔ `--cuda-graph-max-bs 64` (`:126`, no disable flag); `decode_mode:target_only` ↔ no speculative flag on the dsv4 command (diff fact); `model_revision` ↔ `:122`; `engine_image:5bc4bc0dfd36` ↔ `:117`. **All consistent.** Two caveats: `sglang_revision:nightly-c0b6474-near-v1` (`:412`) asserts a source identity this manifest cannot prove — only the forthcoming engine page can bind the digest to that commit; and the published tuning profile is **incomplete**, not false — `SGLANG_OPT_USE_TOPK_V2=0` (`:165`) appears in no label, so the telemetry backend sees the sparse-prefill arm but not the TopK containment. The `ab_arm` tag's matched-pair claim (gpu04 sparse-off, comment `:116`) is cross-host and untraceable here (Not traced). The qwen35 label set (`:304`) still asserts only model/deployment/host metadata — the base's asymmetry (dsv4 publishes its full profile and gets a 30-minute fuse; qwen35 publishes none and sits on the 5-minute default) carries unchanged.

### MEDIUM (deployed: ON) — weight downloader resolves huggingface_hub from PyPI unpinned inside the enclave (disk, supply chain)

Unchanged from base: `uvx --from 'huggingface_hub[hf_xet]' hf download …` three times (`:183`, `:185`, `:187`) installs `huggingface_hub[hf_xet]` from PyPI at whatever version exists on deploy day, running in-enclave with `HF_TOKEN` (`:192`) and write access to the shared `huggingface_cache` volume the engines read weights from (`:190`, `:87`, `:149`). Never sees message plaintext (pre-serving, download-only) — supply-chain/cache-poisoning exposure with no attestation-visible trace on drift.

### MEDIUM (deployed: ARMED) — dsv4 watchdog hang dump onto the shipped stdout stream, 1800 s fuse; dump shape on the nightly is inherited, not verified (log)

`--watchdog-timeout 1800` (`:136`) on the single replica. On the previous audited engine this was a verified dump path (full prompt + output token IDs of every in-flight request to stdout, no flag to disable — [`ec518148` page](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md), HIGH ARMED); the nightly descends from the same upstream line, so the dump is expected present, but its shape on `5bc4bc0d` bytes awaits that image's page (Check 1 HIGH). A 30-minute scheduler hang arms it; Check 4 ships it.

### INFO (deployed: ON) — prompt-derived KV stays in GPU memory; the new compress-state dtype is a GPU-memory layout choice; durable volumes unchanged (disk)

`SGLANG_ENABLE_UNIFIED_RADIX_TREE=1` (`:166`) and `--kv-cache-dtype fp8_e4m3` (`:72`, `:124`) keep quantized KV in GPU memory, and the new `SGLANG_DSV4_COMPRESS_STATE_DTYPE=bf16` (`:162`) selects the dtype of that same in-GPU compressed state — no disk KV/offload tier is configured on either anchor. Durable volumes are `huggingface_cache`, `kernel_cache`, `otelcol_app_storage`, and `certs` only (`:516-521`); engine mounts are the two caches (`:87-88`, `:148-150`).

## Check 4 — Telemetry pipeline: stdout of every plaintext service leaves the box unredacted

### HIGH (deployed: ON) — engine and proxy stdout ships off-box with no redaction stage; on Datadog-bearing hosts it additionally reaches a third-party US SaaS (telemetry)

Chain re-verified in this file, restating the two-sided trap because reading either half alone gives the wrong answer: every app service logs via the `json-file` driver whose anchor **exports the `com.datadoghq.ad.logs` label into each log record's attrs** (`:1-6`). otelcol-contrib (`:479`, digest-pinned `:480`, [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)) tails `/var/lib/docker/containers/*/*-json.log` (`:536`, ro mount `:496`); the filelog receiver's `regex_parser` operators then **lift `model:` and `deployment:` out of that label** into record attributes (`:564-567`, `:576-579`). `filter/app_logs` (`:782-791`) drops records only where `model` **and** `deployment` are both nil (`:786`) plus five named management services (`:787-791`) — and because every app service's `ad.logs` label carries those tags (downloader `:195`, nginx `:217`, registrar `:245`, proxies `:270`/`:346`, qwen35 engine `:304`, dsv4 engine `:380`, dcgm `:431`/`:461`), the receiver+filter pair **keeps exactly the plaintext-adjacent services** and ships them to the literal `https://telemetry.infra.near.ai` (`:819`, bearer `:821`). No processor in the pipeline redacts content: `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch` (`:837`) — limits, metadata, grouping, batching. (otelcol's own stdout uses the `local` driver `:504-507`, is label-tagged `deployment:` at `:509`, and is excluded by name at `:790`.) The second leg is measured-layer, host-dependent: this file answers on the `qwen35-122b` surface, which the base round placed on gpu30 under harness [`66007399`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md), whose measured Datadog agent ships every container's stdout to `us3.datadoghq.com` — there, engine stdout leaves **twice**, one copy to a third-party US SaaS (harness binding for this deploy round not re-fetched — Not traced). What lands on that stream at deployed flags is bounded by the engines' print discipline — verified on the qwen35 side, **inherited-not-verified on the dsv4 nightly** (Check 1 HIGH), with the watchdog dumps of Checks 1 and 3 as the known ARMED writers.

### MEDIUM (deployed: ON) — open OTLP receivers forward spans and metrics verbatim off-box (telemetry)

OTLP on `0.0.0.0:4317/4318` (`:531`, `:533`; `expose`d not host-published, `:492-494`) feeds the `metrics/otlp` (`:843-846`) and `traces` (`:851-854`) pipelines exporting verbatim to `:819`. Any container on the flat network can push prompt-bearing spans and they ship unexamined; nothing in this file does so today.

### INFO (deployed: ON) — the new A/B labels are static deployment metadata; the label pipeline still ships logs, never request content (telemetry)

Direct answer to the label-growth question: `ab_arm`, `sparse_prefill`, `compress_state_dtype`, and the renamed `config_variant` are **fixed strings authored in this file** (Check 1 item 5) — no mechanism exists by which a request-derived value could enter them. The filelog regex parsers extract only `model`/`model_path`/`precision`/`deployment`/`ip`/`port`/`env`/`host`/`instance`/`replica` (`:560-603` — none of the new tags becomes a first-class attribute; the full label string rides inside each record's attrs regardless). What the labels *tag* is the same stdout stream as before — the pipeline's content exposure is the stdout HIGH above, unchanged by the tags. All metrics paths remain pull-based Prometheus scrapes — seven jobs (`:611`, `:629`, `:665`, `:682`, `:701`, `:718`, `:741`) — whose static labels carry the config constants verified in Check 3; no request-derived dimension. Both engine anchors emit counters/latencies via `--enable-metrics`/`--enable-cache-report` (`:81-82`, `:143-144`); the dsv4 claim inherits from the engine lineage pending the nightly's page.

## Check 5 — Privileges, mounts, co-tenancy (byte-identical anchors; re-cited)

### INFO (deployed: ON) — both proxies run privileged as root with the attestation socket and TLS key (structural)

`x-vllm-proxy-common` (`:20-31`): `user: root` (`:22`), `privileged: true` (`:23`), `ipc: host` via `x-nvidia` (`:13`), `dstack.sock` (`:28`), `certs:ro` (`:29`). Root-equivalent E2EE terminators — structural exposure inside the box, not a new reader. One fewer instance than base.

### MEDIUM (deployed: ON) — shared IPC namespace across both models' engines and proxies (cross-model memory exposure)

`ipc: host` (`:13`) inherited by every engine and proxy: one IPC namespace spanning the Qwen3.5 and DeepSeek stacks — cross-model shared-memory visibility (different customers' plaintext) permitted though not configured. This revision **widens the audited-bytes caveat**: one side of the shared namespace is now the unreviewed nightly (Check 1 HIGH).

### MEDIUM (deployed: ON) — dcgm-exporter runs mutable-tag bytes with SYS_ADMIN and GPU access (telemetry, privileged drift)

Two instances of tag-only `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:100`) with `SYS_ADMIN` (`:102-103`) + nvidia runtime, GPU-scoped per model (`:425`, `:455`). Bytes can drift under the attestation; the [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md) covers tag + config, never bytes.

No `pid: host`, no `SYS_PTRACE`, no docker-socket mount anywhere in this file (grep-verified; the only `privileged:` is the proxy anchor `:23`, the only socket is `dstack.sock` `:28`, and otelcol mounts container **logs** read-only `:496`). Those capabilities live in the measured harness.

## Check 6 — Network topology, nginx, egress (shrunk; re-cited)

### MEDIUM (deployed: ON) — flat external network, no egress restriction on plaintext services (egress)

`dstack_default`, `external: true` (`:511-514`); no `internal: true`, no egress allowlist. Sanctioned engine egress is HF only (`HF_HUB_OFFLINE` defaulted `0`, `:91`, `:153`) — plus the request-driven media fetch on the qwen35 side (Check 1 MEDIUM), which is exactly the kind of engine-originated egress an unrestricted network fails to contain.

### MEDIUM (deployed: ON) — unauthenticated engine admin surface reachable through four bare nginx locations and allowlist-free proxies; /configure_logging is a one-POST content-logging switch (log-enable path)

nginx (digest-pinned `:198`, conf `:1015-1084`) publishes plaintext HTTP host ports 8000-8001 and TLS 8444→443 (`:204-207`; listeners `:1048`, `:1054`, `:1063`, `:1078`). All four server blocks route with a bare `location /` (`:1049`, `:1055`, `:1074`, `:1083`) — no path allowlist — and the proxies forward any path (their source pages). The qwen35 engine's build is confirmed to carry an **unauthenticated `/configure_logging`** (one POST enables verbatim prompt+output logging and arbitrary-path request dumping, no restart — engine page CRITICAL), no `--api-key` exists anywhere in this file (Check 2), and the dsv4 nightly is expected to carry the same upstream surface pending its page — so that switch is reachable laterally on the flat network and, absent proxy path-filtering, from the front door. Runtime reconfiguration leaves no measurement trace. Improvement this revision: two fewer routed listeners (six → four). Mitigations in-file: upstreams are always the proxies, never raw engines; `proxy_buffering off; proxy_cache off` (`:1040-1041`) — no request bodies buffered to disk at the nginx hop.

### INFO (deployed: ON) — nginx access log ships per-request user-attributable metadata off-box (log)

`json_combined` with `$uri` not `$request` (`:1017-1030` — no query strings or bodies), but `request_id`/`org_id`/`workspace_id` (`:1019-1021`) make it per-request, user-attributable metadata on Check 4's shipped stream (`access_log` `:1032`).

**Named outbound destinations, exhaustively:** proxies → `https://cloud-api.near.ai` (literal `:254`, `:330`), `http://compose-manager:8080` (`:256`, `:332`; host-gateway `:25-26`), `${WEB_CONTEXT_SEARCH_URL}` (unset default — Check 2); registrar → `https://completions.near.ai` (`:864`), registering both model domains (`:1010-1011`), health-probing backends with a **synthetic** `"hi"` message (`:925` — not user content), plain HTTP probes on 8000/8001 only (`:956-957`); engines → HF hub (+ request-driven media fetch on qwen35, Check 1); downloader → HF hub + PyPI (Check 3); otelcol → `https://telemetry.infra.near.ai` (`:819`). The measured layer adds Datadog and compose-manager paths (harness pages). The r2 TLS self-check destination is gone with the r2 path.

## Check 7 — Sibling comparison within this file

The two `vllm-proxy-rs` digests, resolved: the qwen35 proxy runs the anchor's [`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (`:21`); the dsv4 proxy overrides with [`b0c5cd07`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b0c5cd0786b19dfa46144107be15e340b7a338b1c341d02ffa994c9f1ea77dfd.md) (`:325`) — **both audited in-repo**, both with clean core request paths and the same two client-opt-in egress caveats (web search, fusion). The configuration asymmetry is still exactly one env var: `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS=20` on the dsv4 proxy only (`:329` — the explanatory comment was deleted with the topology note; the behavior is documented on the proxy's page); otherwise the two env blocks are identical mod `MODEL_NAME`/`VLLM_BASE_URL` (`:263`, `:339`), verified by direct diff — same `OHTTP_ENABLED=true`, same `LOG_FORMAT=json`, same token/webhook wiring, same privilege (both inherit `:20-31`). The engine asymmetry has **inverted** relative to two revisions ago: then, qwen35 ran audited bytes while dsv4 ran an unreviewed near-build; the base closed both; now qwen35 runs the audited upstream release while dsv4 runs the unreviewed nightly (Check 1 HIGH). Pin asymmetries: none — both engines revision-pinned with pinned `--trust-remote-code`. No `build:` block exists anywhere in this file (grep-verified) — nothing is resolved from a mutable source at engine build time; the one mutable in-enclave resolve is the downloader's PyPI fetch (Check 3).

## Verdict

**QUALIFIED PASS at this layer — a structurally cleaner file (fewer proxies, fewer ports, fewer listeners, zero dangling references) that spends the lineage's hardest-won property: for the first time since the base closed it, plaintext is again handed to unreviewed engine bytes.** The recipe itself stays clean on delta review: no logging switch handed to any engine or proxy, `--log-requests-level 0` kept on both anchors, OHTTP forced on literally, every checkpoint revision-pinned and pre-fetched at those pins, every telemetry destination literal, all new labels verified truthful against the deployed command and env, the three new env vars pure compute/memory config, no plaintext buffered to disk at any hop, and the operator-interpolable env surface strictly smaller than the base. Qualifications, in descending order of cost: **(1)** the dsv4 engine — the process that reads every DeepSeek user's plaintext — is an unreviewed nightly whose commissioned review was unpublished at review time, making its print discipline, watchdog dump shape, and admin-surface state inherited expectations (Check 1); **(2)** whatever any plaintext service prints leaves the TEE unredacted — twice on the Datadog-bearing host — with the qwen35 watchdog dump verified ARMED on a 5-minute default fuse this file again declines to lengthen, and the dsv4 dump expected on its 30-minute one (Checks 1, 3, 4); **(3)** the unauthenticated `/configure_logging` class remains one POST from full content logging, behind four bare `location /` blocks and allowlist-free proxies, with no API key anywhere in this file (Check 6); **(4)** the qwen35 media fetch remains unguarded with unrestricted egress (Checks 1, 6); **(5)** the `/compose/up` splice can still set `DSV4_RETURN_HIDDEN_STATES` and `WEB_CONTEXT_SEARCH_URL` with no measurement change (Check 2); **(6)** the downloader's unpinned PyPI resolve and dcgm's mutable tag persist (Checks 3, 5). Recommendations: publish the [`5bc4bc0d`](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md) engine review before this identity is treated as covered end-to-end; add `--watchdog-timeout` to the qwen35 command; set `--allowed-media-domains` (or disable multimodal serving) on the qwen35 anchor; and the standing ones — path-allowlist nginx/proxies, pin the downloader.

## Not traced

- **Engine internals of `5bc4bc0dfd36`** — nightly provenance (the claimed c0b6474 base plus near.ai patches), print discipline, `/configure_logging` presence, watchdog dump shape, patch contents, and whether the three new `SGLANG_*` env names are consumed as spelled — commissioned in this same run; its page will be [`/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd36…`](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md) and **did not exist in this repo at review time** (checked). Until it lands, all dsv4 engine-side claims here are inherited from the `ec518148`/upstream lineage.
- Qwen3.5 engine internals — relied on via the published [`984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) page, not re-derived.
- The gpu04 sparse-prefill-off matched arm the comment (`:116`) and `ab_arm` labels reference — a different host and file, outside this page's identity.
- The harness answering for this deploy round, and therefore the Datadog second-leg claim — asserted from the base round's gpu30/[`66007399`](/manifests/measured/sha256-66007399f064f058af68771c552bcae150c9f88622305631b5141da1aa2f7b7e.md) binding; not re-fetched here. The fetch-commit `bde3ddd87955` and `file_sha256` binding were taken from the orchestrator's capture and re-derived locally from the repo clone (both hashes match); the signed action log itself was not re-fetched in this review.
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source pages).
- Proxy internals (path forwarding, web-search fail-closed, stream-idle watchdog behavior) — proxy source pages, relied on above.
- PyPI state on deploy day; dcgm-exporter bytes under its mutable tag; guest swap posture (guest-OS leg, [`os/`](/os/)).
- Live fleet routing: whether the `dsv4-flash` surface also answers with this file this round (this run's capture names the `qwen35-122b` host), and the traffic split across the A/B arms.
