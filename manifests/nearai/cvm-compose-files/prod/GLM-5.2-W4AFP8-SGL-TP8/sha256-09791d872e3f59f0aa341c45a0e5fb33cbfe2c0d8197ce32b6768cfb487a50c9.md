# cvm-compose-files prod/GLM-5.2-W4AFP8-SGL-TP8.yaml @ e141079... (flagship GLM-5.2 pair; delta = proxy bump b183677a -> 98b57ad7 + Datadog OpenMetrics autodiscovery labels removed)

## verdict: QUALIFIED PASS — delta review against the audited base [`d11837e8`](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-d11837e869090731b1420553bb321ac596b47e92c498bf36890d2963b4be85b1.md): the diff is three hunks — the proxy anchor's image digest `b183677a` → `98b57ad7` (`:112`, a build already reviewed QUALIFIED PASS on its own page) and the deletion of the `com.datadoghq.ad.check_names` / `init_configs` / `instances` OpenMetrics autodiscovery labels from the engine (base `:276-279`) and dcgm (base `:315-317`) services — and every load-bearing finding carries over unchanged. Nothing in the engine command, engine env, volumes, nginx config, registrar script or otelcol pipeline moved a byte (block-restricted `diff` below). The label deletion un-arms a would-be Datadog metrics scrape that the measured harness never ran; the retained `com.datadoghq.ad.logs` labels still opt every app container's stdout into Datadog log collection should an agent ever be measured in, and remain the routing key that sends engine stdout to near.ai's gateway today. This layer still configures a clean plaintext path (every engine logging flag literal and minimal, `--log-requests-level 0`, zero `${VAR}` in the engine command, no dump/crash/profile/trace flag, `--trust-remote-code` pinned by revision, `OHTTP_ENABLED=true` literal, no KV or prompt disk cache) while failing to contain the engine's own live paths — the carried residuals: an unauthenticated engine admin surface (`/configure_logging`) reachable through the shared flat network and allowlist-free proxies, unscrubbed stdout shipping off-box, a 256-request/1M-context watchdog dump blast radius, and two splice-reachable content-egress env switches on the E2EE terminator. Deployment nuance: near.ai applied this revision service-scoped to `proxy-glm52` only; the engine container still runs from the `ff35ebb9` revision's declaration (identical engine block; see the action-log table).

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/GLM-5.2-W4AFP8-SGL-TP8.yaml` |
| **file_sha256** | `09791d872e3f59f0aa341c45a0e5fb33cbfe2c0d8197ce32b6768cfb487a50c9` (re-verified locally: `shasum -a 256` of the captured 925-line file matches; `git show e14107901d20:prod/GLM-5.2-W4AFP8-SGL-TP8.yaml \| shasum -a 256` from a full clone of `nearai/cvm-compose-files` also yields `09791d87…`; commit `e14107901d20` "Bump inference proxy to the ee53bec build on every deployed stack (#250)", 2026-09-09T23:33:15Z, tag `v0.0.409`) |
| **serves** | `z-ai/glm-5.2` at `glm-5-2.completions.near.ai` **and** the synthetic long-context tier `z-ai/glm-5.2-long` at `glm-5-2-long.completions.near.ai` — full 1M context. Both hostnames resolve to one CVM (instance `cd9f9c95…`) whose signed action log (179 entries, `actions_hash` `d8e07cc0…`, identical on both host captures) pins this file |
| **topology** | one sglang **TP8** replica across all 8 GPUs behind one `vllm-proxy-rs`, behind nginx (three listeners: `:80`, `:8001` discovery stub, `:443`) |
| **engine** | `lmsysorg/sglang@sha256:1c64fde976bd…` (`:258`) — [audited](/images/docker.io/lmsysorg/sglang/sha256-1c64fde976bdf0d56474a30bccbcfc19667e5b3ab34c826a534c9d6aaca41212.md), verdict *private at the deployed flags* with the two standing live paths (unauthenticated `/configure_logging`, undisableable hang-time dump). Unchanged from base |
| **proxy** | `nearaidev/vllm-proxy-rs@sha256:98b57ad7…` (`:112`) — [audited](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) QUALIFIED PASS (commit `ee53bec3`). **The delta from the base's [`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md)** |
| **runs on** | measured harness [`546161bc…`](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md) (QUALIFIED PASS — **no Datadog agent measured**, fail-closed management filter, defines `dstack_default`), guest OS `9b69bb16…` (dstack-nvidia-0.5.5). Confirmed from both host reports' `info.compose_hash` / `info.os_image_hash` for this capture — not carried |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log at commit `e14107901d20` — **not** hardware-measured; fetched at that commit and hash-checked before use (MATCH). The latest `compose_up` on both hosts is 2026-09-09T23:42:21Z, `services: ['proxy-glm52']` |
| **review** | reviewed 2026-09-10 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review, base = the [`d11837e8` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-d11837e869090731b1420553bb321ac596b47e92c498bf36890d2963b4be85b1.md) (itself a one-line delta over [`ff35ebb9`](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-ff35ebb9cbf87c28145635ab348155274adb2d4be584da9dcde7be58a0493a70.md), over [`db6f4e8f`](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-db6f4e8ffd0324fcc3b2c3ef51ec2a4735b888bd9293057295d9161c9585bf38.md), which reviewed the full file). The diff was computed byte-exactly from a full clone: `git show 33f0f80bf643:…` hashes to `d11837e8…` (base, 931 lines) and `git show e14107901d20:…` to `09791d87…` (this file, 925 lines); `git diff 33f0f80b e1410790 -- prod/GLM-5.2-W4AFP8-SGL-TP8.yaml` returns **three hunks, seven lines: one image line changed, six label lines deleted, nothing added**. The three commits between the two revisions on this path are `c960549` (2026-08-25, "remove Datadog Agent OpenMetrics checks", file hash `49f5d819…` — **never deployed**, absent from the action log), `1755bb7` (2026-09-09T20:44Z, proxy pin `b183677a` → `27265f5a`, file hash `ce019ba9…`, deployed for ~3 h) and `e141079` (proxy `27265f5a` → `98b57ad7`, this file). Every base finding was independently re-located and re-cited at this file's line numbers (unchanged through `:275`; −2 from `:276` to `:311`; −6 from `:312` on). The engine command, engine env, all service blocks other than the two label sets, networks, volumes, and the three `configs:` documents were each `diff`'d as blocks against the base (Check 1) and are byte-identical.

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta-over-delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-09-10).
> Method: [/notes/method.md](/notes/method.md) (§4 Manifest-only checks) · Base: [d11837e8](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-d11837e869090731b1420553bb321ac596b47e92c498bf36890d2963b4be85b1.md) · Harness: [546161bc](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md) · Proxy: [98b57ad7](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md)

### Which revision each running container was declared by (from the signed action log)

near.ai deploys with **service-scoped** `compose up` (`services` field of each action). A scoped `compose_up` recreates only the named service; other containers keep the declaration in effect at the last `compose_up` that included them. From the 38 entries for this path (identical on the `glm-5-2` and `glm-5-2-long` captures), the tail since the last unscoped run:

| timestamp (UTC) | action | tag / commit | file_sha256 | services |
|---|---|---|---|---|
| 2026-08-15T06:19:56 | compose_up | v0.0.340 / `2390cb8c` | `ff35ebb9` | *(all)* |
| **2026-08-15T18:06:41** | **compose_up** | v0.0.340 / `2390cb8c` | **`ff35ebb9`** | ***(all — last unscoped)*** |
| 2026-08-15T21:26:53 | compose_up | v0.0.340 / `2390cb8c` | `ff35ebb9` | `otelcol-contrib` |
| 2026-08-17T02:03:00, 02:20:22 | compose_up | v0.0.340 / `2390cb8c` | `ff35ebb9` | `model-proxy-registrar` |
| 2026-08-20T20:47:44 | compose_up | v0.0.344 / `1a7d6e54` | `d11837e8` (base) | `otelcol-contrib` |
| 2026-09-03T18:49:09 | docker_clean | — | — | — (no file/services recorded) |
| 2026-09-09T20:55:47 | compose_up | v0.0.405 / `1755bb7c` | `ce019ba9` | `proxy-glm52` |
| **2026-09-09T23:42:21** | **compose_up** | **v0.0.409 / `e1410790`** | **`09791d87` (this file)** | **`proxy-glm52`** |

Consequences, stated so the reader does not over-read what "this file is live" means:

- **`proxy-glm52`** is declared by **this file**: image `98b57ad7` (`:112`), the env block `:221-237`. It was on `27265f5a` (`ce019ba9`) for the intervening ~3 h — that build has its own [page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-27265f5a10abcacc3fe80604a00ed2ba32c6e082895115e4b652b40525cb554d.md) (QUALIFIED PASS).
- **`otelcol-contrib`** is declared by the base `d11837e8` (2026-08-20). Its service block (`:328-358`) and config document (`:374-613`) are byte-identical here, so the running collector matches this file.
- **`model-sg-glm52-w4afp8-tp8`** (the engine), **`dcgm-glm52`** and **`nginx`** were last (re)created by the unscoped `ff35ebb9` run of 2026-08-15T18:06Z. The engine's command, env, image and volumes have not changed across `ff35ebb9` → `d11837e8` → this file (Check 1), so the running engine *is* what this file declares — **except its labels**: the running engine and dcgm containers still carry the three `com.datadoghq.ad.*` OpenMetrics labels this file deletes, because no `compose_up` including those services has run since `c960549`. Under the measured harness (no Datadog agent) the difference has no effect; it is recorded here because the next unscoped `compose up` will recreate both containers on the label change alone (an engine restart, not a privacy event).
- **`model-proxy-registrar`** is declared by `ff35ebb9` (2026-08-17); its block and script are byte-identical here.

This page reviews the file as the declared configuration — what the attestation surface presents and what `file_sha256` keys — and notes above where the runtime lags it.

## Check 1 — What changed from the audited base, verified hunk-by-hunk

`git diff 33f0f80bf643 e14107901d20 -- prod/GLM-5.2-W4AFP8-SGL-TP8.yaml` (34 lines with context; `diff -U0` of the two captured files gives the same three hunks):

```
@@ -112 +112 @@   x-vllm-proxy-common anchor
-  image: nearaidev/vllm-proxy-rs@sha256:b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36
+  image: nearaidev/vllm-proxy-rs@sha256:98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284

@@ -276,2 +275,0 @@   model-sg-glm52-w4afp8-tp8 labels
-      com.datadoghq.ad.check_names: '["openmetrics"]'
-      com.datadoghq.ad.init_configs: "[{}]"
@@ -279 +276,0 @@
-      com.datadoghq.ad.instances: '[{"openmetrics_endpoint":"http://model-sg-glm52-w4afp8-tp8:8000/metrics", "histogram_buckets_as_distributions": true, "metrics":["sglang:*"], "service": "sglang", "tags":[...]}]'

@@ -315,3 +311,0 @@   dcgm-glm52 labels
-      com.datadoghq.ad.check_names: '["dcgm"]'
-      com.datadoghq.ad.init_configs: "[{}]"
-      com.datadoghq.ad.instances: '[{"openmetrics_endpoint": "http://%%host%%:9400/metrics", "tags":[...]}]'
```

Orientation confirmed: (1) the anchor `x-vllm-proxy-common` (`:111-122`) image at `:112` is the only changed line; (2) on the engine the `com.datadoghq.ad.logs` label is kept (now `:276`, was `:278`); (3) same removal on dcgm, `ad.logs` kept (now `:312`, was `:318`). Nothing else.

**Block-restricted diffs** (each block cut by line range from both captured files and `diff`'d; ranges differ only by the deletions' shift):

| block | base lines | this file | result |
|---|---|---|---|
| header comments | 1-94 | 1-94 | identical |
| anchors `x-logging-conf`, `x-nvidia`, `x-vllm-proxy-common` | 95-127 | 95-127 | one line: the `:112` image digest |
| engine command anchor `&glm52-cmd` | 128-156 | 128-156 | **identical** |
| engine env anchor `&glm52-model-env` | 158-167 | 158-167 | **identical** |
| `nginx`, `model-proxy-registrar`, `proxy-glm52` services | 168-254 | 168-254 | **identical** (the proxy service inherits the image via `<<: *vllm-proxy-common`, `:220`) |
| `model-sg-glm52-w4afp8-tp8` service | 255-294 | 255-291 | three label lines deleted, nothing else |
| `dcgm-glm52` service | 295-333 | 292-327 | three label lines deleted, nothing else |
| `otelcol-contrib` service | 334-365 | 328-359 | **identical** |
| `networks:` + `volumes:` | 366-377 | 360-371 | **identical** |
| `configs:` — `otelcol_app_config`, `registrar_script`, `nginx_conf` | 378-931 | 372-925 | **identical** |

So the engine command, env, image (`lmsysorg/sglang@1c64fde9`, `:258`), volumes (`:261-263`), nginx conf (`:795-925`), registrar script (`:615-794`) and otelcol config (`:374-613`) are byte-identical to the base.

### INFO (deployed: ON) — proxy image bump b183677a to 98b57ad7 changes the E2EE terminator's build, not this file's plumbing; the new build's page adds no plaintext sink (egress)

`:112` is the only changed line in the anchors block, inherited by `proxy-glm52` at `:220`. The [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) (commit `ee53bec3`, QUALIFIED PASS, reviewed today) traces the whole `c59ea60e` → `ee53bec3` delta to one PR (#228: refuse `/v1/responses` in the catch-all with a fixed-string 404 before the body is read) and re-runs the whole-tree sweeps (194 log macros, every metric label, every filesystem call — no content-bearing sink at any level). **What changes for this recipe versus the base's `b183677a` proxy** ([page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md), verdict PRIVATE with two opt-in egress caveats, reviewed 2026-07-15), read against this file's proxy env (`:221-237`):

- **Backend conversation affinity** (new in the `27265f5a` lineage) — `VLLM_BACKEND_CONVERSATION_AFFINITY` is **not set** in this file (grep-verified: the only `VLLM_*` variable is `VLLM_BACKEND_URLS=http://model-sg-glm52-w4afp8-tp8:8000`, `:233`, a single backend). Per the proxy page's fleet table (row `GLM-5.2-W4AFP8-SGL-TP8 (09791d87…)`, `proxy-glm52 (219)`, backends 1, affinity "inactive (single backend)"), the salted-digest conversation map is never consulted here. Even where it is active the page finds it memory-only with enum/index labels; here it is moot.
- **SSE stream idle watchdog** — `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS` is **not set** (grep-verified; the proxy page records `proxy-dsv4-flash` as the only service in the fleet that sets it). OFF here; its error path is content-free anyway per that page.
- **`/v1/responses` refusal** — a fixed-string 404 before body read; no new log line. Collaterally, no `/v1/responses` body reaches this proxy's memory via the direct host.
- **Carried from `b183677a` and re-rated on the newer page:** web search (the base page's INFO-4 "opt-in, egresses to Brave") is now rated **CRITICAL (deployed: ARMED, client-opt-in)** and fusion (base MEDIUM-2) **HIGH (deployed: ARMED, unknown gate + client-opt-in)** — same mechanisms, stronger rating because the proxy page treats the unattested host-`.env` gates as unknowable. The manifest-layer view of those gates is in Check 4. The `b183677a` page's MEDIUM-1 (transport-error logs carrying the full upstream URL) does not appear on the `27265f5a`/`98b57ad7` pages as a live finding; those pages' whole-tree sweep reports every log argument as ids, counts, durations, enum labels, sanitised URLs and error categories. Not re-derived here.
- **Newly surfaced for this recipe:** the proxy's `info`-level log of the decrypted OHTTP inner request path (LOW on the proxy page) is exported off-box by *this file's* pipeline — Check 3.

### INFO (deployed: OFF, harness-contingent) — Datadog OpenMetrics autodiscovery labels removed from the engine and dcgm services; a would-be third-party metrics scrape is un-armed at the recipe layer (telemetry)

What the six deleted lines would have done: `com.datadoghq.ad.check_names` / `init_configs` / `instances` are Datadog Agent **Autodiscovery** container-label templates. An agent with docker-socket access (the harness the [`db6f4e8f` era](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-db6f4e8ffd0324fcc3b2c3ef51ec2a4735b888bd9293057295d9161c9585bf38.md) measured, gone from [`546161bc`](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md)) reads them off each container and instantiates the named integration: for the engine, the `openmetrics` check polling `http://model-sg-glm52-w4afp8-tp8:8000/metrics` with `metrics: ["sglang:*"]` and `histogram_buckets_as_distributions: true`; for dcgm, the `dcgm` check on `http://%%host%%:9400/metrics` (`%%host%%` is an Autodiscovery template variable resolved to the container IP). Both would have shipped **engine and GPU metric series** — counters, gauges, histogram buckets and their label sets — to Datadog's SaaS, a second, third-party destination alongside this file's own `prometheus/apps` scrape of the same two endpoints (`:461-465` engine, `:478-482` dcgm) to `telemetry.infra.near.ai`. The commit message (`c960549`, "Datadog decommission … the Agent's OpenMetrics scrape was a duplicate of the otelcol-contrib prometheus/apps scrape") matches that reading. Metrics are not message content — the base rated the sglang `/metrics` surface as counters and cache-hit ratios only — so what is un-armed is a **metadata** fork, not a content fork. Rated INFO because on the measured harness these labels were already inert (the base's "one destination, no third-party Datadog fork" INFO rested on that), so nothing changes at the deployed state; the deletion removes the *readiness* for a future Datadog-bearing harness to fork metrics without a recipe change. The harness still permits `DD_API_KEY` in `allowed_envs` (measured page Check 2, "vestigial, drift-ready").

### LOW (deployed: OFF, armed by any Datadog-bearing harness) — the retained com.datadoghq.ad.logs labels still opt every app container's stdout into Datadog log collection, and are the routing key for the near.ai export today (log / telemetry)

The deletion was deliberately scoped to metrics: `com.datadoghq.ad.logs` stays on the proxy (`:239`), engine (`:276`), dcgm (`:312`) and the collector itself (`:358`), and the json-file driver is told to stamp it onto every log record (`labels: "com.datadoghq.ad.logs,com.docker.compose.service"`, `:100`). Two consequences:

1. **Under a Datadog agent it is the log opt-in.** With `DD_LOGS_ENABLED=true` and container-collect-all *off*, the agent collects logs only from containers bearing an `ad.logs` label; with collect-all on it collects everything regardless. Either way this label — `source: sglang, service: sglang`, tags `model:z-ai/glm-5.2, deployment:GLM-5.2-W4AFP8-SGL-TP8, env, host, ip, port` — is exactly what would route the **engine's stdout** (the stream that carries any `/configure_logging` or watchdog dump output) to a third-party SaaS. The recipe keeps that switch armed; the harness decides whether anything is listening. Today nothing is (measured page: no `datadog/agent` service, the only `datadog`/`DD_` strings are `DD_HOSTNAME` and these labels). **A control outside this document — residual, not mitigation.**
2. **It is load-bearing for the near.ai path.** This file's own collector lifts `model` and `deployment` out of this very label (`regex_parser` at `:413-416` and `:417-420`, `parse_from: attributes["attrs"]["com.datadoghq.ad.logs"]`) and `filter/app_logs` **drops** records where both are nil (`:544`). Delete the label from the engine and its stdout would stop leaving the box via `otelcol-contrib`; keep it and it goes. The commit message says so explicitly ("`com.datadoghq.ad.logs` … deliberately retained: the in-CVM OTel collector parses [it] for log service/source").

Rated LOW rather than INFO because, unlike the deleted metrics labels, this one selects the content-carrying stream, and its activation depends on a layer this `file_sha256` does not cover.

## Check 2 — Engine flags: re-verified identical to base, no change

The command is the YAML anchor `&glm52-cmd` (`:128-156`), referenced once at `:260` by the single engine service `model-sg-glm52-w4afp8-tp8` (`:255`). **Zero `${VAR}` interpolation** in the command — no operator env, measured or spliced, can alter an engine flag. Block-diff against the base: identical. Re-verified verbatim at this file's lines:

```
sglang serve
  --model-path PhalaCloud/GLM-5.2-W4AFP8            (:130)
  --revision d975c19a1ad7fa0638e3d9ca9bca0ae74cc2f99e   (:131)
  --served-model-name z-ai/glm-5.2                 (:132)
  --quantization w4afp8
  --disable-shared-experts-fusion
  --disable-custom-all-reduce
  --tp 8
  --kv-cache-dtype fp8_e4m3
  --reasoning-parser glm45
  --tool-call-parser glm47
  --speculative-algorithm EAGLE
  --speculative-num-steps 3
  --speculative-eagle-topk 1
  --speculative-num-draft-tokens 4
  --log-requests-level 0                           (:144)
  --mem-fraction-static 0.75                        (:145)
  --max-running-requests 256                        (:146)
  --max-queued-requests 16
  --cuda-graph-max-bs 128
  --context-length 1048576                          (:149)
  --dist-init-addr 127.0.0.1:29500
  --watchdog-timeout 1800                           (:151)
  --port 8000                                       (:152)
  --host 0.0.0.0                                    (:153)
  --enable-cache-report
  --enable-metrics
  --trust-remote-code                               (:156)
```

- **`--log-requests-level 0` (`:144`)** — the minimum, below the code default of 2 at which prompt/output text first appears. Deliberate defence in depth; not a mitigation for the engine's runtime `/configure_logging` CRITICAL, which overrides the level.
- **`--log-requests` — absent. `--api-key` / `--admin-api-key` — absent everywhere** (grep-verified over the whole file: `api.key`/`api-key`/`admin` match only `WEB_CONTEXT_SEARCH_API_KEY` on the proxy, `:237`).
- **Header comment (`:66`)** reads `# *** CONFIG: EAGLE 3/1/4 + --mem-fraction-static 0.75 + --cuda-graph-max-bs 128 ***` — matches the deployed `0.75` at `:145`. No drift.

### HIGH (deployed: OFF, omission) — no --admin-api-key leaves the engine's admin surface ungated (sink: log/stdout)

Carried unchanged. `--admin-api-key` would install SGLang's auth middleware and gate `/configure_logging`, `/start_profile`, weight-update and every other administrative route behind a bearer. One flag, already implemented upstream, the single highest-value change available to this document. Its absence is what makes Check 4's reachability bite. Sink: the engine's unauthenticated log-enable switch → stdout → the pipeline in Check 3.

### HIGH (deployed: ARMED) — watchdog hang-dump blast radius set to fleet maximum by this file (sink: stdout/telemetry)

Carried unchanged. `--watchdog-timeout 1800` (`:151`) — a deliberate 6x widening of the upstream sglang default (`watchdog_timeout = 300`, `server_args.py`). The engine's hang-time handler dumps the prompt and output **token ids of every in-flight request** to stdout, and there is **no flag that disables it**. This file sets that in-flight population to **`--max-running-requests 256`** (`:146`) at **`--context-length 1048576`** (`:149`) — up to 256 concurrent requests at up to 1M tokens each, the largest dump exposure in the fleet. No `--soft-watchdog-timeout` (grep-verified), so no second, earlier watchdog runs the same dump. Sink: stdout → Check 3 pipeline. Conditional on a hang; the header (`:41-65`) documents hangs/wedges on this hardware as lived history.

**Label-truthfulness check: N/A — no label asserts a watchdog value.** The four `ad.logs` labels (`:239` proxy, `:276` engine, `:312` dcgm, `:358` otelcol) carry only routing metadata (`model`, `deployment`, `env`, `host`, `ip`, `port`). Nothing to falsify.

- **No dump/trace/profile/disk-metrics flags** (grep-verified): no `--crash-dump-folder`, no `--dump-requests-folder` (no CLI flag exists — settable only via `/configure_logging`), no `--enable-trace`, no `--export-metrics-to-file`, no `--debug-tensor-dump-output-folder`, no `--enable-hierarchical-cache`. `--enable-metrics`/`--enable-cache-report` emit counters and cache-hit ratios only.

### Pinning — PASS, model confirmed text-only (carried)

- **`--trust-remote-code` (`:156`) is paired with the pinned `--revision d975c19a…` (`:131`)** — upstream HF-head drift cannot inject code into this plaintext process. The served name `--served-model-name z-ai/glm-5.2` (`:132`) matches the proxy literal `MODEL_NAME=z-ai/glm-5.2` (`:228`).
- **Media-fetch path not reachable — the model is not multimodal.** The `db6f4e8f` page fetched `PhalaCloud/GLM-5.2-W4AFP8/config.json` at `d975c19a…`: `model_type: deepseek_v3`, `architectures: ['GlmMoeDsaForCausalLM']`, no `vision_config`, `auto_map: null`. Revision pin and engine version (v0.5.13.post1) unchanged here; no media/multimodal flag in the command. Carried, not re-fetched this run (no network in this review).

**Engine env** (anchor `&glm52-model-env`, `:158-167`), block-diff identical: `HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` (`:159`, credential, in `allowed_envs`), `HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}` (`:160`, **not** in `allowed_envs` → effectively `0`), literals `NVIDIA_DRIVER_CAPABILITIES`, `OPENBLAS_L2_SIZE`, `NCCL_DEBUG=WARN` (`:163`, quiet), `TORCHINDUCTOR_CACHE_DIR`, `TRITON_CACHE_DIR` (`:166-167`). No `SGLANG_*` debug/verbosity variable.

- **[INFO] No `model-downloader` service.** The engine resolves and fetches `PhalaCloud/GLM-5.2-W4AFP8` itself at start with `HF_HUB_OFFLINE=0` — a live Hugging Face fetch path in the plaintext container. The `--revision` pin makes it drift-safe; reachability noted in Check 4.

## Check 3 — Telemetry pipeline: unscrubbed stdout to one destination (unchanged; label semantics re-examined)

**Result: a fixed, entirely unscrubbed path from engine stdout to one off-box endpoint — byte-identical to the base (`otelcol-contrib` service `:328-358`, config `:374-613`; block-diff identical).**

Trace both ends:

1. **On-disk retention first.** `x-logging-conf` (`:95-100`): `json-file` (`:96`), `max-size 100m` (`:98`), `max-file 10` (`:99`) — up to ~1 GB retained stdout per container before shipment. If `/configure_logging` is triggered, that is ~1 GB of prompt/completion text at rest in the CVM.
2. **`otelcol-contrib` filelog receiver** tails all of it: `include: [/var/lib/docker/containers/*/*-json.log]` (`:385`), mounted `/var/lib/docker/containers:ro` (`:345`), `user: "0:0"` (`:332`).
3. **The receiver's `regex_parser` operators lift routing attributes out of the `ad.logs` label** — `model:(?P<model>…)` (`:415`, block `:413-416`), `deployment:(?P<deployment>…)` (`:419`, block `:417-420`), then `ip`, `port`, `env`, `host` (`:421-436`), all `parse_from: attributes["attrs"]["com.datadoghq.ad.logs"]`; the engine's label carries `model:z-ai/glm-5.2`, `deployment:GLM-5.2-W4AFP8-SGL-TP8` (`:276`). The chain ends with an unconditional `add service.name: cvm-docker` fallback (`:437-439`). `transform/docker_metadata` then overwrites `service.name` / `log.source` from the same label (`:533-537`).
4. **`filter/app_logs` (`:540-549`) KEEPS exactly the records carrying those attributes** and drops management services: drop conditions `attributes["model"] == nil and attributes["deployment"] == nil` (`:544`) plus explicit `compose-manager`, `compose-manager-launcher`, `otelcol-management`, `otelcol-contrib`, `datadog-agent` by name (`:545-549`). So it keeps the app containers — sglang, the proxy, nginx, dcgm, the registrar.
5. **Export:** `otlphttp/gateway` → `endpoint: https://telemetry.infra.near.ai` (`:572-573`), literal, bearer `${MONITORING_INGEST_TOKEN}` (`:575`, env at `:335`), via the logs pipeline `[filelog/docker_containers] → [memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch] → [otlphttp/gateway]` (`:589-592`). Disk-backed `sending_queue`: `num_consumers: 1` (`:577`), `queue_size: 128` (`:578`), `storage: file_storage` (`:579`) on the `otelcol_app_storage` volume (`:346`).

**Findings:**

### HIGH (deployed: ON, systemic) — no scrubbing at any hop; anything the engine prints leaves the CVM (sink: telemetry)

The `logs/app` processor chain (`:591`) is metadata + batching only — `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch`. None redacts, hashes, truncates, or pattern-matches a body. The `com.datadoghq.ad.logs` / `nearai.otel.*` labels tag streams; they never scrub content. Complete chain: **anything the engine prints → json-file (~1 GB retained) → filelog → [no scrubbing] → `https://telemetry.infra.near.ai`.** The engine's `/configure_logging` CRITICAL and the watchdog HIGH both terminate here. Guarantee "no content leaves" is still exactly "no plaintext service ever prints content" — carried by the engine source page.

### LOW (deployed: ON) — the proxy's decrypted OHTTP inner request path is exported through this file's stdout pipeline (sink: log / telemetry)

New cross-layer carry from the [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) (its LOW: after OHTTP decapsulation the proxy logs `inner_path` at `info`, taken verbatim from the decrypted BHTTP control data). The path is not a message body — but a client that puts sensitive data in an inner query string writes it to the proxy's stdout, and this file ships the proxy's stdout: `proxy-glm52` logs under `*logging-conf` (`:122` via the anchor), carries the `ad.logs` label with `model`/`deployment` (`:239`), so `filter/app_logs` keeps its records and `otlphttp/gateway` exports them unscrubbed. Present on the base's `b183677a` too (that page's LOW-3); listed here because the manifest is the layer that turns a stdout line into egress.

### INFO (deployed: OFF, for this deployment) — one destination, no third-party Datadog fork (harness re-confirmed for this capture)

The measured harness for **this** capture is [`546161bc`](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md) (both host reports: `info.compose_hash` `546161bc…`), which **measures no Datadog agent** — so every `com.datadoghq.ad.*` label in this file is inert at the measured layer and **engine stdout leaves the CVM to exactly one destination: near.ai's own gateway, via this file's own `otelcol-contrib`.** The base carried this from an older resolution; it is confirmed for this capture. The recipe-layer changes in Check 1 (metrics autodiscovery labels deleted, `ad.logs` retained) move the *readiness* for a Datadog fork, not the deployed state.

### MEDIUM (deployed: ON) — two open OTLP receivers, no scrubbing, on the shared bridge (sink: telemetry)

`otlp` on `0.0.0.0:4317` (grpc, `:380`) and `0.0.0.0:4318` (http, `:382`), `expose`d only (`:341-343`) not host-published — but on the shared `external: true` bridge (Check 4), so any co-resident container can push in. At this config the `otlp` receiver feeds only the `metrics/otlp` (`:597-600`) and `traces` (`:605-608`) pipelines, **not** the logs pipeline — a span-content risk, not log injection.

- **[INFO] Metrics are counters only** — `prometheus/apps` (`:441-494`) scrapes `/metrics` on the proxy (`:448`), engine (`:465`) and dcgm (`:482`) with static labels. The proxy serves `/metrics` unauthenticated (proxy page); the `98b57ad7` page re-enumerates every proxy metric label as static strs, enums or small integers.
- **[INFO] nginx access logging is metadata** — `json_combined` (`:797-810`) records `$uri` (not `$request`) plus `request_id`, `org_id`, `workspace_id`, status, timings, `upstream_addr`. Per-request, user-attributable; not message content. Applied on all three listeners (`:814`, `:855`, `:910`).

## Check 4 — Network topology and egress (the CRITICAL's reachability)

```
networks:
  default:
    external: true
    name: dstack_default      (:360-363)
```

### CRITICAL (deployed: ON, exposure) — unauthenticated engine logging switch reachable over the shared bridge and allowlist-free listeners, feeding the off-box stdout pipeline (sink: log/telemetry)

Carried unchanged; the mechanism is the engine's, inherited from [`sglang@1c64fde9`](/images/docker.io/lmsysorg/sglang/sha256-1c64fde976bdf0d56474a30bccbcfc19667e5b3ab34c826a534c9d6aaca41212.md) — this document supplies no containment. The engine's unauthenticated `/configure_logging` (turns on full prompt+output logging *and* arbitrary-path request dumping at runtime, no restart, no attestation-visible change) is reachable by two independent routes, and this file declines every available containment:

- **Engine port 8000 is not host-published** (nginx publishes `8000:80`, `8001:8001`, `8444:443` at `:177-180`; dcgm `9400:9400` at `:300-301`). **But `external: true` (`:362`) means the *shared* `dstack_default` bridge** (`:363`), defined by the measured harness, on which compose-manager and the launcher also sit (measured page). Any container on that bridge — including projects outside this file's `file_sha256` — can `curl http://model-sg-glm52-w4afp8-tp8:8000/configure_logging` directly, unauthenticated. No `internal: true`, no segmentation, no egress allowlist.
- **The serving listeners apply no path allowlist.** `:80` is a bare `location /` proxying everything to `proxy-glm52:8000` (`:817-828`, `proxy_pass` `:818`); `:443` likewise (`:913-923`, `:914`), one default server block covering both domains (`server_name glm-5-2.completions.near.ai glm-5-2-long.completions.near.ai`, `:904`).
- **The `:8001` discovery stub is *not* a bypass and is carefully gated** — it serves only `= /v1/models` (`:856`) and `= /healthz` (`:875`), each behind an `auth_request` (`:857`, `:876`) to the internal `= /_engine_health` (`:883-890`, `internal` `:884`, proxying to `proxy-glm52:8000/v1/models` `:885` with `proxy_pass_request_body off` `:886`), and ends in `location / { return 404; }` (`:891-893`). **This is the one place in the file where a path allowlist exists** — proof the containment idiom was within reach for the serving blocks and not taken.
- **The proxy applies no path allowlist — now with one denylist entry.** Per the [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) (MEDIUM, deployed ON): the router's catch-all forwards any traversal-safe path **except `/v1/responses`** to the engine, guarded only by a valid bearer (deployment `TOKEN`, `:230`, **or any valid `sk-` cloud API key**). The refusal is billing-motivated and does not touch `/configure_logging`. The route via nginx → proxy → engine is therefore unchanged in substance from the base's `b183677a`.

**Net:** two routes to the engine's admin surface — (a) any co-resident container on the shared bridge, unauthenticated; (b) any external caller holding an accepted bearer, via `nginx → proxy → engine`. **"OFF at this configuration" — not "cannot happen".** The `:8001` stub shows the pattern that would close route (b); `internal: true` or a project-private network would close route (a).

**Note on the "`dead`/`upstream`" grep artifact:** a naive `grep image:` surfaces the bare words `dead` and `upstream` at `:848` and `:852` — nginx comments in the `:8001` stub describing upstream *health*, not images. Every real `image:` line is digest- or tag-pinned (Check 6).

Every outbound destination configured in this file (unchanged from base):

| destination | service | literal? | carries content? |
|---|---|---|---|
| `https://telemetry.infra.near.ai` (`:573`) | otelcol-contrib | literal | **yes — full app-container log bodies, unscrubbed** |
| `https://cloud-api.near.ai` (`:224`) | proxy (`CLOUD_API_URL`) | literal | usage/auth calls; not message bodies |
| `http://compose-manager:8080` (`:226`) + `extra_hosts` (`:116-117`) | proxy | literal | control plane |
| `https://completions.near.ai` (`:618`) | registrar | literal | endpoint/model registration (`:684`, `:687`); health probe POSTs synthetic `"hi"` (`:697-700`) — not user content |
| Hugging Face (engine, `HF_HUB_OFFLINE=0`, `:160`) | engine | implicit | weight fetch |
| **`${WEB_CONTEXT_SEARCH_URL}`** (`:236`) + `WEB_CONTEXT_SEARCH_API_KEY` (`:237`) | **proxy — E2EE terminator** | **not literal, not defaulted, not in `allowed_envs`** | **yes if populated** — model-generated search query derived from the decrypted conversation |
| **`${FUSION_ENABLED}` / `${FUSION_INTERNAL_BEARER_TOKEN}`** (`:231-232`) | **proxy** | **not literal, not in `allowed_envs`** | **yes if enabled** — the proxy review documents Fusion forwarding the full decrypted conversation to panel/judge endpoints |

### HIGH (deployed: OFF, splice-armed) — two content-egress switches on the E2EE-terminating proxy, outside allowed_envs (sink: egress)

Carried unchanged in mechanism; reconciled against the newer proxy page's rating. `WEB_CONTEXT_SEARCH_*` (`:236-237`) and `FUSION_*` (`:231-232`) are interpolated, undefaulted, and **not** in the harness's 22-entry `allowed_envs` (measured page Check 2, verbatim: `BEARER_TOKEN, CM_MIN_TAG_AGE_HOURS, SLACK_WEBHOOK_URL, AWS_*, CERTBOT_EMAIL, CLOUDFLARE_API_TOKEN, DOCKER_REGISTRY_*, LAUNCHER_CHANNEL, DD_API_KEY, HUGGING_FACE_HUB_TOKEN, PROXY_TOKEN, ENV, HOST_IP, DD_HOSTNAME, HOSTNAME, MONITORING_INGEST_TOKEN, CVM_NAME, CVM_HOST`) — the measured dstack env channel cannot set them. The remaining route is compose-manager's non-measured `POST /compose/up` env overlay, not covered by this `file_sha256`. Both features are additionally per-request opt-in at the proxy and default off; teemoon does not enable them. The [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) rates the same two paths **CRITICAL / HIGH (deployed: ARMED, client-opt-in)** because from inside the image the host-`.env` gate is unknowable; this page can add that the *measured* channel excludes them, which narrows — but does not close — the gate to the overlay. Kept at HIGH (deployed: OFF, splice-armed): this page cannot see the overlay's contents and does not assert they are empty.

- **[INFO] `OHTTP_ENABLED=true` is a literal** (`:229`): the E2EE posture on this node is not operator-degradable through any env channel.
- **[MEDIUM] `LONG_TIER_ONLY` / `LONG_HTTP_PORT` / `HTTP_PORT` / `TLS_PORT`** are interpolated defaults on the registrar (`:203-209`), none in `allowed_envs` — routing controls, not content controls, reachable only through the `/compose/up` overlay.
- **[MEDIUM] nginx spills large request bodies to a temp file** — `client_max_body_size 100m` with `client_body_buffer_size 1m` (`:815-816`, `:911-912`) means bodies over 1 MB write to nginx's client-body temp dir. At 1M-token context that is the normal case. **Under OHTTP the body at nginx is the sealed blob (ciphertext)** — nginx sits in front of the E2EE terminator; on a non-OHTTP path it would be plaintext. `proxy_buffering off; proxy_cache off` (`:825-826`, `:921-922`) — no response body buffered to disk. Image's compiled-in `client_body_temp_path` not verified.

## Check 5 — Privileges, mounts, volumes

### INFO (deployed: ON) — the proxy runs privileged as root with ipc host, sharing an IPC namespace with the engine (structural)

`x-vllm-proxy-common` (`:111-122`, applied `:220`): `image: nearaidev/vllm-proxy-rs@sha256:98b57ad7…` (`:112`), `user: root` (`:113`), `privileged: true` (`:114`), plus `ipc: host` via `x-nvidia` (`:104`, merged at `:115`). Root-equivalent in the guest, sharing an IPC namespace with the engine (which merges the same anchor at `:256`) that holds prompt plaintext. Mounts `/var/run/dstack.sock` (`:119`, attestation/key ops) and `certs:/etc/letsencrypt:ro` (`:120`). Structural exposure inside the box (the E2EE terminator), not a new reader. The proxy page notes the same: the in-memory confinement it claims for its own state is a claim about the binary, not the host.

### Volumes — which persist, and the cross-link

`volumes:` (`:365-371`): `otelcol_app_storage`, `huggingface_cache`, `compile_cache` (all named, persist across container recreation), and `certs: external: true` (`:369-371`). The engine mounts **`compile_cache:/root/.cache`** (`:262`, inductor/triton/deep_gemm) and `huggingface_cache:/root/.cache/huggingface` (`:263`, weights).

- **Engine: no KV or prompt disk cache.** KV is fp8 in GPU memory; at 1M context the pool is resident, not offloaded (no `--enable-hierarchical-cache`, no offload flag). **PASS.**

### MEDIUM (deployed: ARMED, cross-link) — the engine's /root/.cache is a persisted named volume; a dump folder pointed there via /configure_logging survives container recreate and redeploy (sink: disk)

Carried from the base (where it was inline); promoted to a finding heading so it is machine-visible. The engine review notes `dump_requests_folder` — settable only via the unauthenticated `/configure_logging` — accepts an arbitrary path; pointing it under `/root/.cache` writes pickled full request/response pairs into `compile_cache` (`:262`, `:368`), **surviving container recreate and redeploy** (the volume is named and never pruned by this file). Activation requires the CRITICAL's route; once activated the data outlives the process that wrote it. The `docker_clean` action of 2026-09-03 (action-log table above) records no target; whether it touched volumes is not visible from the log.

- **`otelcol-contrib`** mounts `/var/lib/docker/containers:ro` (`:345`, cross-container log visibility), `user: "0:0"` (`:332`).
- **No `pid: host`, no `SYS_PTRACE`** in this file (grep-verified) — those live in the measured harness's compose-manager (measured page HIGH, architectural).

## Check 6 — Image pinning

Digest-pinned: `lmsysorg/sglang@sha256:1c64fde9…` (`:258` — [audited](/images/docker.io/lmsysorg/sglang/sha256-1c64fde976bdf0d56474a30bccbcfc19667e5b3ab34c826a534c9d6aaca41212.md); identity rests on OCI labels + matching upstream CI, **no signed provenance**), `nearaidev/vllm-proxy-rs@sha256:98b57ad7…` (`:112` — [audited](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md), signed provenance to `ee53bec3`), `nginx@sha256:1d13701a…` (`:171`), `curlimages/curl@sha256:d94d07ba…` (`:191`), `otel/opentelemetry-collector-contrib@sha256:85ac41c2…` (`:329` — [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)). **No `dockerfile_inline`, no `build:` — no in-enclave build in this file** (grep-verified).

### MEDIUM (deployed: ON) — dcgm-exporter is tag-pinned (supply chain / telemetry)

`nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:293`) is **tag-only** — still, carried — in a `SYS_ADMIN` container (`:296-297`) with the nvidia runtime over all 8 GPUs (`NVIDIA_VISIBLE_DEVICES=all`, `:299`). The registry can serve different bytes under the same tag with no attestation-visible trace. It cannot see request plaintext by configuration (NVML counters; GPU memory is CC-protected), but it carries the strongest capabilities of any unpinned image here. [Tag-addressed page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md) (telemetry-not-content at audited config; the tag can drift). The label deletion on this service (Check 1) changes what a Datadog agent would scrape from it, not what it can see.

## Cross-check synthesis

- **[X1 / CRITICAL — exposure, not capture]** The engine's unauthenticated `/configure_logging` is reachable, and this document is why: `external: true` shared network (`:360-363`) + bare `location /` on the serving listeners (`:817-828`, `:913-923`) + no `--admin-api-key` + a pipeline that ships stdout verbatim off-box. Nothing here captures content; everything here declines to contain the mechanism that would. The proxy bump adds one billing denylist entry to route (b) and nothing to route (a).
- **[X2 / HIGH]** The watchdog dump has a fixed, unscrubbed exit path, and this file sets its blast radius to the fleet maximum — `--watchdog-timeout 1800` (`:151`), `--max-running-requests 256` (`:146`), `--context-length 1048576` (`:149`), an engine that cannot be told to stop dumping.
- **[X3 / HIGH — systemic]** Global app-stdout shipping is a fixed feature; the manifest proves no content is collected **only if no service prints content**. One destination today (harness re-confirmed for this capture). The retained `ad.logs` label is both the key that routes engine stdout to that destination and the opt-in a future Datadog-bearing harness would honour; the deleted OpenMetrics labels un-arm only a metrics fork.
- **[X4 / HIGH]** Two content-egress switches on the E2EE terminator sit outside `allowed_envs` but within reach of the non-measured `/compose/up` overlay (`WEB_CONTEXT_SEARCH_*`, `FUSION_*`), as does `LONG_TIER_ONLY`. The newer proxy page rates them ARMED from inside the image; the measured layer narrows the gate to the overlay.
- **[X5 / deployment]** The file is live for `proxy-glm52` only; the engine, dcgm, nginx and registrar run from the `ff35ebb9` declaration, which is byte-identical to this file for everything except the two deleted label sets.

## Verdict

**QUALIFIED PASS — the same qualification as the base page; the delta is privacy-neutral at the deployed state, and nothing in this file is made worse for plaintext.**

- **The delta itself.** Two changes: (1) the E2EE terminator moves to `98b57ad7` (`:112`), a build whose own page traces its whole delta to a fixed-string `/v1/responses` refusal and re-sweeps the tree for sinks — no new plaintext copy; neither of the two runtime knobs introduced in that lineage (`VLLM_BACKEND_CONVERSATION_AFFINITY`, `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS`) is set here, and affinity is inactive on a single backend regardless. (2) Six Datadog Autodiscovery metrics labels are deleted from the engine and dcgm; a would-be third-party **metrics** fork is un-armed at the recipe layer, while the **log** opt-in label is deliberately retained and remains the routing key for the near.ai export.
- **What this file configures:** clean. Every logging flag literal and minimal; `--log-requests-level 0` explicit; zero `${VAR}` in the engine command; no dump/crash/profile/trace flag; `--trust-remote-code` pinned by revision; the model text-only; `OHTTP_ENABLED=true` literal; telemetry destination literal; **no KV or prompt disk cache**; the discovery stub properly gated and fail-closed. On the configured path, **no plaintext copy is written to any log, file, cache, or network destination.**
- **What this file fails to contain:** the engine's own live paths. It leaves `/configure_logging` reachable by two routes, declines `--admin-api-key`, sets the watchdog dump's in-flight ceiling to 256 requests at up to 1M tokens, and guarantees anything printed leaves the CVM unscrubbed — including, at LOW, the proxy's decrypted inner request path. **The invariant this layer delivers is "content capture is not configured", not "content capture cannot be enabled".**
- **Recommended, in order:** (1) pass `--admin-api-key` to the engine; (2) apply the `:8001` stub's allowlist idiom to the serving blocks, or move the engine to a project-private network; (3) reconsider `--max-running-requests 256` at 1M context as the dump-exposure ceiling; (4) add a scrubbing/deny processor to `filter/app_logs` for engine-sourced records; (5) digest-pin dcgm-exporter; (6) run an unscoped `compose up` so the running engine/dcgm containers match the declared labels, and remove `DD_API_KEY` from the harness's `allowed_envs` so a Datadog fork cannot be re-armed without a measurement change.

## Not traced

- **The `/compose/up` env overlay's contents** — whether `WEB_CONTEXT_SEARCH_*` / `FUSION_*` are populated on this host. Not visible from this file or the measured layer; the proxy page treats them as ARMED for that reason. Whether empty means fail-closed at the proxy is inherited from the source reviews.
- **What the 2026-09-03 `docker_clean` action targeted** — the log entry carries no file, tag or services. If it pruned volumes, the `compile_cache` cross-link's persistence claim would have been interrupted once; the compose-manager page notes `docker_clean` gained a `containers` target, not what this invocation selected.
- Whether sglang, the proxy, or nginx ever *print* request content at the configured levels — engine/proxy source legs, load-bearing under X3. The `98b57ad7` page's sweep covers the proxy at `ee53bec3`; the engine page covers `1c64fde9`.
- Whether the `b183677a` page's MEDIUM-1 (transport-error log carrying a full upstream URL with caller query string) was fixed between `b183677a` and `c59ea60e` or is subsumed by the later pages' "sanitised URLs" sweep result — not re-derived; a delta review of the proxy across that span would settle it.
- Whether nginx actually spills bodies to `client_body_temp` on this image, and what the guest does with that file afterwards.
- The `PhalaCloud/GLM-5.2-W4AFP8` `config.json` at `d975c19a…` was not re-fetched (no network in this review); the text-only determination is carried from the `db6f4e8f` page.
- dcgm-exporter's effective NVML visibility under CC; the guest OS `9b69bb16…`'s swap and core-dump policy ([`os/`](/os/)).
- The live state of the running engine container's labels was inferred from the action log (no `compose_up` including the engine since `c960549`), not observed.
