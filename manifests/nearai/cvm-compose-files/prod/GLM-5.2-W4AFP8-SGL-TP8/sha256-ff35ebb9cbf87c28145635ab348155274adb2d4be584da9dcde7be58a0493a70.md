# cvm-compose-files prod/GLM-5.2-W4AFP8-SGL-TP8.yaml @ abe26392... (flagship GLM-5.2 pair; delta = one memory-fraction number)

## verdict: QUALIFIED PASS — delta review against the audited base [`db6f4e8f`](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-db6f4e8ffd0324fcc3b2c3ef51ec2a4735b888bd9293057295d9161c9585bf38.md): the diff is a single line — `--mem-fraction-static 0.78 → 0.75` (a VRAM headroom knob, no privacy surface) — and every load-bearing finding carries over unchanged. What *does* change for this deployment is one layer down: the measured harness under it is now [`546161bc`](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md), which measures **no Datadog agent**, so the base page's node-specific fork to `us3.datadoghq.com` does **not** apply here — engine stdout leaves the CVM to exactly one destination, near.ai's own gateway. The carried residuals are the fleet's: an unauthenticated engine admin surface (`/configure_logging`) reachable through the shared flat network and allowlist-free proxies, unredacted stdout shipping, a 256-request/1M-context watchdog blast radius, and two splice-reachable content-egress env switches on the E2EE terminator.

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/GLM-5.2-W4AFP8-SGL-TP8.yaml` |
| **file_sha256** | `ff35ebb9cbf87c28145635ab348155274adb2d4be584da9dcde7be58a0493a70` (re-verified locally: `shasum -a 256` of the captured 930-line file matches; source commit `abe26392a090` in `nearai/cvm-compose-files`, "GLM-5.2: reduce static memory fraction to 0.75", 2026-07-15, re-fetched from GitHub and hash-matched to the pinned artifact) |
| **serves** | `z-ai/glm-5.2` at `glm-5-2.completions.near.ai` **and** the synthetic long-context tier `z-ai/glm-5.2-long` at `glm-5-2-long.completions.near.ai` — full 1M context |
| **topology** | one sglang **TP8** replica across all 8 GPUs behind one `vllm-proxy-rs`, behind nginx (three listeners: `:80`, `:8001` discovery stub, `:443`) |
| **engine** | `lmsysorg/sglang@sha256:1c64fde976bd…` (v0.5.13.post1) — [audited](/images/docker.io/lmsysorg/sglang/sha256-1c64fde976bdf0d56474a30bccbcfc19667e5b3ab34c826a534c9d6aaca41212.md), verdict *private at the deployed flags* |
| **runs on** | measured harness [`546161bc…`](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md) (gpu23, QUALIFIED PASS — **no Datadog agent measured**, fail-closed management filter, defines `dstack_default`) |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log at commit `2390cb8cbb5b` — **not** hardware-measured; fetched at that commit and hash-checked before use |
| **review** | reviewed 2026-08-17 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review, base = the [`db6f4e8f` page](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-db6f4e8ffd0324fcc3b2c3ef51ec2a4735b888bd9293057295d9161c9585bf38.md) (which reviewed the full file). The diff was computed byte-exactly: both revisions were re-fetched from `raw.githubusercontent.com` (base at `ebca09d74a6b` → `db6f4e8f…`, target at `abe26392a090` → `ff35ebb9…`, each hash confirmed against its keyed identity) and `diff -u`'d. The result is **two changed lines, both the same edit**: the engine command's `--mem-fraction-static` value and its documenting header comment. Every base finding was independently re-located and re-cited at this file's line numbers (they are unchanged — both files are 930 lines, all findings land on identical line numbers), the engine command was swept for any logging/dump/api-key change (none), and the deployment's measured harness was newly resolved and read for the one place the base's story changes.

---

# Deployment Manifests & Configuration — Privacy Audit (recipe leg, delta)

> Part of the near.ai inference-path privacy audit (Fable, 2026-08-17).
> Method: [/notes/method.md](/notes/method.md) (§4 Manifest-only checks) · Base: [db6f4e8f](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-TP8/sha256-db6f4e8ffd0324fcc3b2c3ef51ec2a4735b888bd9293057295d9161c9585bf38.md) · Harness: [546161bc](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md)

## Check 1 — What changed from the audited base, verified line-by-line

Relative to `db6f4e8f`, this file changes — and changes only — the engine's static memory fraction:

```
-  --mem-fraction-static 0.78
+  --mem-fraction-static 0.75      (line 145)
```

and the header comment that documents it (`# *** CONFIG: EAGLE 3/1/4 + --mem-fraction-static 0.75 …`, line 66, was `0.78`). Nothing else. `diff -u` over the two 930-line files returns those two hunks and no others.

`--mem-fraction-static` caps the fraction of GPU memory sglang reserves for the static KV pool; lowering it from 0.78 to 0.75 **increases** free VRAM headroom (the commit's rationale — the header's operational notes document repeated VRAM-starved cudagraph-capture OOMs on this hardware). It touches no logging, persistence, telemetry, egress, auth, or network surface. **Privacy-neutral.** Every base finding therefore carries over at its original severity; the rest of this page re-verifies each against the captured file rather than assuming it.

### INFO (deployed: OFF) — the one changed flag has no plaintext surface (context)

`--mem-fraction-static 0.75` (145) is a memory-sizing knob. It is a literal (no `${VAR}`), it does not gate any request-logging, dump, or trace path, and the KV pool it sizes remains fp8 **in GPU memory** (Check 5). No finding.

## Check 2 — Engine flags: re-verified identical to base except the one number

The command is the YAML anchor `&glm52-cmd` (`:128-156`), referenced once at `:260` by the single engine service `model-sg-glm52-w4afp8-tp8`. **Zero `${VAR}` interpolation** in the command — no operator env, measured or spliced, can alter an engine flag. Re-verified verbatim:

```
sglang serve
  --model-path PhalaCloud/GLM-5.2-W4AFP8
  --revision d975c19a1ad7fa0638e3d9ca9bca0ae74cc2f99e
  --served-model-name z-ai/glm-5.2
  --quantization w4afp8
  --disable-shared-experts-fusion
  --disable-custom-all-reduce
  --tp 8
  --kv-cache-dtype fp8_e4m3
  --reasoning-parser glm45
  --tool-call-parser glm47
  --speculative-algorithm EAGLE
  --speculative-num-steps 3
  --speculative-eagle-topk 1
  --speculative-num-draft-tokens 4
  --log-requests-level 0
  --mem-fraction-static 0.75
  --max-running-requests 256
  --max-queued-requests 16
  --cuda-graph-max-bs 128
  --context-length 1048576
  --dist-init-addr 127.0.0.1:29500
  --watchdog-timeout 1800
  --port 8000
  --host 0.0.0.0
  --enable-cache-report
  --enable-metrics
  --trust-remote-code
```

- **`--log-requests-level 0` (`:144`)** — the minimum, below the code default of 2 at which prompt/output text first appears. Deliberate defence in depth; not a mitigation for the engine's runtime `/configure_logging` CRITICAL, which overrides the level.
- **`--log-requests` — absent. `--api-key` / `--admin-api-key` — absent everywhere** (grep-verified over the whole file; the only `api.key`-shaped string is `WEB_CONTEXT_SEARCH_API_KEY` on the proxy, `:237`).

### HIGH (deployed: OFF, omission) — no `--admin-api-key` leaves the engine's admin surface ungated

Carried unchanged from base. `--admin-api-key` would install SGLang's auth middleware and gate `/configure_logging`, `/start_profile`, weight-update and every other administrative route behind a bearer. One flag, already implemented upstream, and the single highest-value change available to this document. Its absence is what makes Check 4's reachability bite. Sink: the engine's unauthenticated log-enable switch → stdout → the pipeline in Check 3.

### HIGH (deployed: ARMED) — watchdog hang-dump blast radius set to fleet maximum by this file

`--watchdog-timeout 1800` (`:151`) — I verified the sglang default upstream this run: `watchdog_timeout … = 300` in `server_args.py:1031-1034`, i.e. **300 s**, so 1800 is a deliberate 6x widening of the hang window. The engine's hang-time handler dumps the prompt and output **token ids of every in-flight request** to stdout, and there is **no flag that disables it**. This file sets that in-flight population to **`--max-running-requests 256`** (`:146`) at **`--context-length 1048576`** (`:149`) — up to 256 concurrent requests at up to 1M tokens each, the largest dump exposure in the fleet (vs 96 at 262k on the 2xTP4 sibling). Nothing in the engine can cap it; only this number can. No `--soft-watchdog-timeout` is set (grep-verified), so there is no second, earlier watchdog running the same dump. Sink: stdout → Check 3 pipeline. Conditional on a hang; the header documents that hangs/wedges on this hardware are lived history.

**Label-truthfulness check (priority item 1): N/A — no label asserts a watchdog value.** No `com.datadoghq.ad.logs` label (or any label) in this file carries a `watchdog_timeout:` tag (grep-verified). The four `ad.logs` labels (`:239` proxy, `:278` engine, `:318` dcgm, `:364` otelcol) carry only routing metadata (`model`, `deployment`, `env`, `host`, `ip`, `port`). Nothing to falsify.

- **No dump/trace/profile/disk-metrics flags** (grep-verified): no `--crash-dump-folder`, no `--dump-requests-folder` (no CLI flag exists — settable only via `/configure_logging`), no `--enable-trace`, no `--export-metrics-to-file`, no `--debug-tensor-dump-output-folder`, no `--enable-hierarchical-cache`. `--enable-metrics`/`--enable-cache-report` emit counters and cache-hit ratios only.

### Pinning — PASS, and the model is confirmed text-only (priority item 2)

- **`--trust-remote-code` (`:156`) is paired with the pinned `--revision d975c19a1ad7fa0638e3d9ca9bca0ae74cc2f99e` (`:131`)** — upstream HF-head drift cannot inject code into this plaintext process. The served name `--served-model-name z-ai/glm-5.2` (`:132`) matches the proxy literal `MODEL_NAME=z-ai/glm-5.2` (`:228`).
- **Media-fetch path: not reachable — the model is not multimodal.** I fetched `PhalaCloud/GLM-5.2-W4AFP8/config.json` **at the pinned revision** `d975c19a…`: `model_type: deepseek_v3`, `architectures: ['GlmMoeDsaForCausalLM']`, **no `vision_config`** and no image/video config key, `auto_map: null`. This is a text-only causal-LM checkpoint. The v0.5.16 upgrade that turned two Qwen3.6 engines into media fetchers elsewhere on this fleet has no analog here: this engine is v0.5.13.post1, the model declares no vision tower, and no media/multimodal flag appears in the command. No request-driven media fetch is configured or reachable at this config.

**Engine env** (anchor `&glm52-model-env`, `:158-167`), unchanged from base: `HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` (credential, in `allowed_envs`), `HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}` (**not** in `allowed_envs` → always effectively `0`), literals `NVIDIA_DRIVER_CAPABILITIES`, `OPENBLAS_L2_SIZE`, `NCCL_DEBUG=WARN` (quiet), `TORCHINDUCTOR_CACHE_DIR`, `TRITON_CACHE_DIR`. No `SGLANG_*` debug/verbosity variable.

- **[INFO] No `model-downloader` service** (the 2xTP4 sibling has one). The engine resolves and fetches `PhalaCloud/GLM-5.2-W4AFP8` itself at start with `HF_HUB_OFFLINE=0` — a live Hugging Face fetch path in the plaintext container. The `--revision` pin makes it drift-safe; reachability noted in Check 4.

## Check 3 — Telemetry pipeline: unscrubbed stdout to one destination now (priority item 3)

**Result: a fixed, entirely unscrubbed path from engine stdout to one off-box endpoint — and, unlike the base page's deployment, *no* second path to a third-party SaaS, because the measured harness under this file carries no Datadog agent.**

Trace both ends of the pipeline (reading `filter/app_logs` alone gives the opposite answer):

1. **On-disk retention first.** `x-logging-conf` (`:95-100`): `json-file`, `max-size 100m`, `max-file 10` — up to ~1 GB retained stdout per container before shipment. If `/configure_logging` is triggered, that is ~1 GB of prompt/completion text at rest in the CVM.
2. **`otelcol-contrib` filelog receiver** tails all of it: `include: [/var/lib/docker/containers/*/*-json.log]` (`:391`), mounted `/var/lib/docker/containers:ro` (`:351`), `user: "0:0"` (`:338`).
3. **The receiver's `regex_parser` operators lift routing attributes out of the label** — `model:(?P<model>…)` (`:427-429`) and `deployment:(?P<deployment>…)` (`:431-433`), parsed from `attributes["attrs"]["com.datadoghq.ad.logs"]`; the engine's label carries `model:z-ai/glm-5.2`, `deployment:GLM-5.2-W4AFP8-SGL-TP8` (`:278`). The operator chain ends with an unconditional `add service.name: cvm-docker` fallback.
4. **`filter/app_logs` (`:546-555`) then KEEPS exactly the records carrying those attributes** and drops management services: its drop conditions are `attributes["model"] == nil and attributes["deployment"] == nil` (`:550`) plus explicit `compose-manager`, `compose-manager-launcher`, `otelcol-management`, `otelcol-contrib`, `datadog-agent` by name (`:551-555`). So it keeps the app containers — sglang, the proxy, nginx, dcgm, the registrar.
5. **Export:** `otlphttp/gateway` → `endpoint: https://telemetry.infra.near.ai` (`:578-579`), literal, via the logs pipeline `[filelog] → [memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch] → [otlphttp/gateway]` (`:595-597`), disk-backed sending queue on the `otelcol_app_storage` volume (`:352`).

**Findings:**

### HIGH (deployed: ON, systemic) — no scrubbing at any hop; anything the engine prints leaves the CVM

The `logs/app` processor chain (`:596`) is metadata + batching only — `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch`. None redacts, hashes, truncates, or pattern-matches a body. The `com.datadoghq.ad.logs` / `nearai.otel.*` labels tag streams; they never scrub content. Complete chain: **anything the engine prints → json-file (~1 GB retained) → filelog → [no scrubbing] → `https://telemetry.infra.near.ai`.** The engine's `/configure_logging` CRITICAL and the watchdog HIGH both terminate exactly here. Guarantee "no content leaves" ≡ "no plaintext service ever prints content" — carried by the engine source page.

### INFO (deployed: OFF, for this deployment) — the base's Datadog fork does not apply here; stdout leaves to one destination

This is the material change from the base page. The base (`db6f4e8f`) ran on measured harness `002b406e`, which measured a `datadog-agent` with `DD_LOGS_CONFIG_CONTAINER_COLLECT_ALL=true` and `DD_SITE=us3.datadoghq.com` — so the `com.datadoghq.ad.logs` labels this file attaches were *live* and every engine log line forked to a third-party US SaaS **in addition to** near.ai. This deployment runs on measured harness [`546161bc`](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md), which **measures no Datadog agent** (that page, Check 1: grep-verified, the agent service and its OTLP-logs receiver are gone). So the `ad.logs` labels are inert at the measured layer here, and **engine stdout leaves the CVM to exactly one destination: near.ai's own gateway, via this file's own `otelcol-contrib`.** The node-lottery HIGH the base page raised is resolved for this host pair — not by this file (its labels and pipeline are unchanged) but by the harness beneath it. Stated as INFO because it is a *reduction* in exposure relative to the base, contingent on the measured harness, and re-derivable only there.

### MEDIUM (deployed: ON) — two open OTLP receivers, no scrubbing, on the shared bridge

`otlp` on `0.0.0.0:4317` (grpc) and `0.0.0.0:4318` (http), `:386`/`:388`, `expose`d only (`:347-349`) not host-published — but on the shared `external: true` bridge (Check 4), so any co-resident container can push in. At this config the `otlp` receiver feeds only the metrics/traces pipelines, **not** the logs pipeline — a span-content risk, not log injection. (The base noted the *measured* Datadog agent additionally exposed an OTLP-logs listener on this node; that surface is gone under `546161bc`.)

- **[INFO] Metrics are counters only** — `prometheus/apps` scrapes `/metrics` on the proxy, engine, and dcgm with static labels.
- **[INFO] nginx access logging is metadata** — `json_combined` records `$uri` (not `$request`) plus `request_id`, `org_id`, `workspace_id`, status, timings, `upstream_addr`. Per-request, user-attributable; not message content. Applied on all three listeners.

## Check 4 — Network topology and egress (priority items 4, and the CRITICAL's reachability)

```
networks:
  default:
    external: true
    name: dstack_default      (:366-369)
```

### CRITICAL (deployed: ON, exposure) — unauthenticated engine logging switch reachable over the shared bridge and allowlist-free listeners, feeding the off-box stdout pipeline

Carried unchanged from base; the mechanism itself is the engine's, inherited from [`sglang@1c64fde9`](/images/docker.io/lmsysorg/sglang/sha256-1c64fde976bdf0d56474a30bccbcfc19667e5b3ab34c826a534c9d6aaca41212.md) — this document supplies no containment. The engine's unauthenticated `/configure_logging` (which turns on full prompt+output logging *and* arbitrary-path request dumping at runtime, no restart, no attestation-visible change) is reachable by two independent routes, and this file declines every available containment:

- **Engine port 8000 is not host-published** (nginx publishes `8000:80`, `8001:8001`, `8444:443` at `:177-180`; dcgm `9400:9400` at `:303-304`). **But `external: true` means the *shared* `dstack_default` bridge** the measured harness creates and every co-resident container joins. Any container on that bridge — including projects outside this file's `file_sha256` — can `curl http://model-sg-glm52-w4afp8-tp8:8000/configure_logging` directly, unauthenticated. No `internal: true`, no segmentation, no egress allowlist. (The measured page, Check 6, confirms it *defines* this network precisely so the recipe can join it.)
- **The serving listeners apply no path allowlist.** `:80` is a bare `location /` proxying everything to `proxy-glm52:8000` (`:822-833`); `:443` likewise (`:918-929`), one default server block covering both domains. No path filtering, no method restriction.
- **The `:8001` discovery stub is *not* a bypass and is carefully gated** — it serves only `= /v1/models` (`:861`) and `= /healthz` (`:880`), each behind an `auth_request` subrequest to the internal `= /_engine_health` (`:888-895`, which proxies to `proxy-glm52:8000/v1/models` with `proxy_pass_request_body off`), and ends in `location / { return 404; }` (`:896-898`). **This is the one place in the file where a path allowlist exists** — proof the containment idiom was within the authors' reach for the serving blocks and not taken.
- **The proxy applies no path allowlist** — inherited context from the [`vllm-proxy-rs` source reviews](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md): the router ends in a catch-all forwarding any traversal-safe path to the engine, guarded only by a valid bearer (deployment `TOKEN` **or any valid `sk-` cloud API key**). I did not re-derive routing from the `b183677a` tree.

**Net:** two routes to the engine's admin surface — (a) any co-resident container on the shared bridge, unauthenticated; (b) any external caller holding an accepted bearer, via `nginx → proxy → engine`. **"OFF at this configuration" — not "cannot happen".** The `:8001` stub shows the pattern that would close route (b); `internal: true` or a project-private network would close route (a).

**Note on the "`dead`/`upstream`" grep artifact (priority note):** a naive `grep image:` surfaces the bare words `dead` and `upstream`, but they are **not images**. Both occur in the `:8001` stub's inlined nginx comments describing upstream *health* — `:853` ("dead upstream still answered 200") and `:857` ("upstream dead -> 500, upstream alive -> 200"). Every real `image:` line is digest- or tag-pinned (Check 6); there is no `image: dead` or `image: upstream`.

Every outbound destination configured in this file (unchanged from base):

| destination | service | literal? | carries content? |
|---|---|---|---|
| `https://telemetry.infra.near.ai` (`:579`) | otelcol-contrib | literal | **yes — full app-container log bodies, unscrubbed** |
| `https://cloud-api.near.ai` (`:224`) | proxy (`CLOUD_API_URL`) | literal | usage/auth calls; not message bodies |
| `http://compose-manager:8080` (`:226`) + `extra_hosts` (`:116-117`) | proxy | literal | control plane |
| `https://completions.near.ai` (`:623`) | registrar | literal | endpoint/model registration; health probe POSTs synthetic `"hi"` — not user content |
| Hugging Face (engine, `HF_HUB_OFFLINE=0`) | engine | implicit | weight fetch |
| **`${WEB_CONTEXT_SEARCH_URL}`** (`:236`) + `WEB_CONTEXT_SEARCH_API_KEY` (`:237`) | **proxy — E2EE terminator** | **not literal, not defaulted, not in `allowed_envs`** | **yes if populated** — model-generated search query |
| **`${FUSION_ENABLED}` / `${FUSION_INTERNAL_BEARER_TOKEN}`** (`:231-232`) | **proxy** | **not literal, not in `allowed_envs`** | **yes if enabled** — the proxy review documents Fusion forwarding the full decrypted conversation to panel/judge endpoints |

### HIGH (deployed: OFF, splice-armed) — two content-egress switches on the E2EE-terminating proxy, outside `allowed_envs`

Carried unchanged. `WEB_CONTEXT_SEARCH_*` (`:236-237`) and `FUSION_*` (`:231-232`) are interpolated, undefaulted, and **not** in the node's `allowed_envs` — the measured dstack env channel cannot set them. The remaining route is compose-manager's non-measured `POST /compose/up` env overlay, not covered by this `file_sha256` and not in the signed action log at the audited compose-manager builds. Both features are additionally per-request opt-in at the proxy and default off; teemoon does not enable them. If empty means fail-closed at the proxy — which the source review indicates — this downgrades.

- **[INFO] `OHTTP_ENABLED=true` is a literal** (`:229`): the E2EE posture on this node is not operator-degradable through any env channel.
- **[MEDIUM] `LONG_TIER_ONLY` / `LONG_HTTP_PORT` / `HTTP_PORT` / `TLS_PORT`** are interpolated defaults, none in `allowed_envs` (`:203-209`) — routing controls, not content controls, reachable only through the `/compose/up` overlay.
- **[MEDIUM] nginx spills large request bodies to a temp file** — `client_max_body_size 100m` with `client_body_buffer_size 1m` (`:820-821`, `:916-917`) means bodies over 1 MB write to nginx's client-body temp dir. At 1M-token context that is the normal case. **Under OHTTP the body at nginx is the sealed blob (ciphertext)** — nginx sits in front of the E2EE terminator; on a non-OHTTP path it would be plaintext. `proxy_buffering off; proxy_cache off` (`:830-831`, `:926-927`) — no response body buffered to disk. Image's compiled-in `client_body_temp_path` not verified.

## Check 5 — Privileges, mounts, volumes (priority items 5, 7)

### INFO (deployed: ON) — the proxy runs privileged as root with `ipc: host`

`x-vllm-proxy-common` (`:111-122`, applied `:220`): `image: nearaidev/vllm-proxy-rs@sha256:b183677a…` (`:112`), `user: root` (`:113`), `privileged: true` (`:114`), plus `ipc: host` via `x-nvidia` (`:102-109`). Root-equivalent in the guest, sharing an IPC namespace with the engine that holds prompt plaintext. Mounts `/var/run/dstack.sock` (attestation/key ops) and `certs:/etc/letsencrypt:ro`. Structural exposure inside the box (the E2EE terminator), not a new reader.

- **[MEDIUM] `ipc: host` on both the engine and the proxy** — one shared IPC namespace across both plaintext-handling containers.

### Volumes — which persist, and the cross-link (priority item 7)

`volumes:` (`:371-377`): `otelcol_app_storage`, `huggingface_cache`, `compile_cache` (all named, persist across container recreation), and `certs: external: true` (`:375-377`). The engine mounts **`compile_cache:/root/.cache`** (`:262`, inductor/triton/deep_gemm) and `huggingface_cache:/root/.cache/huggingface` (`:263`, weights).

- **Engine: no KV or prompt disk cache.** KV is fp8 in GPU memory; at 1M context the pool is resident, not offloaded (no `--enable-hierarchical-cache`, no offload flag). **PASS.**
- **[MEDIUM — cross-link, carried] `/root/.cache` is a persisted named volume.** The engine review notes `dump_requests_folder` — settable only via the unauthenticated `/configure_logging` — accepts an arbitrary path; pointing it under `/root/.cache` writes pickled full request/response pairs into `compile_cache`, **surviving container recreate and redeploy**. The persistence that exists for compile-cache warmth is the persistence a runtime-enabled dump would inherit.
- **`otelcol-contrib`** mounts `/var/lib/docker/containers:ro` (`:351`, cross-container log visibility), `user: "0:0"` (`:338`).
- **No `pid: host`, no `SYS_PTRACE`** in this file (grep-verified) — those live in the measured harness's compose-manager (measured page, Check 5).

## Check 6 — Image pinning

Digest-pinned: `lmsysorg/sglang@sha256:1c64fde9…` (`:258` — [audited](/images/docker.io/lmsysorg/sglang/sha256-1c64fde976bdf0d56474a30bccbcfc19667e5b3ab34c826a534c9d6aaca41212.md); note its identity rests on OCI labels + matching upstream CI, **no signed provenance**), `nearaidev/vllm-proxy-rs@sha256:b183677a…` (`:112` — [audited](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md), signed provenance), `nginx@sha256:1d13701a…` (`:171`), `curlimages/curl@sha256:d94d07ba…` (`:191`), `otel/opentelemetry-collector-contrib@sha256:85ac41c2…` (`:335` — [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)). **No `dockerfile_inline`, no `build:` — no in-enclave build in this file** (grep-verified).

### MEDIUM (deployed: ON) — dcgm-exporter is tag-pinned; two sibling recipes digest-pin the same component

`nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:296`) is **tag-only** — in a `SYS_ADMIN` container (`:299-300`) with the nvidia runtime over all 8 GPUs. The registry can serve different bytes under the same tag with no attestation-visible trace. **The asymmetry is real within this fleet:** the `dsv4-qwen38-glm51` and `dsv4-qwen36-gemma4` recipes **digest-pin** exactly this component (`nvcr.io/nvidia/k8s/dcgm-exporter@sha256:ed594cf5…`), while this recipe and the two GLM-5.2 siblings (2xTP4, GLM-5.1) leave it a mutable tag. It cannot see request plaintext by configuration (NVML counters; GPU memory is CC-protected), but it carries the strongest capabilities of any unpinned image here. [Tag-addressed page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md).

## Cross-check synthesis

- **[X1 / CRITICAL — exposure, not capture]** The engine's unauthenticated `/configure_logging` is reachable, and this document is why: `external: true` shared network (`:366-369`) + bare `location /` on the serving listeners (`:822-833`, `:918-929`) + no `--admin-api-key` + a pipeline that ships stdout verbatim off-box. Nothing here captures content; everything here declines to contain the mechanism that would. The `:8001` stub proves the idiom was available.
- **[X2 / HIGH]** The watchdog dump has a fixed, unscrubbed exit path, and this file sets its blast radius to the fleet maximum — `--watchdog-timeout 1800` (`:151`, 6x the 300 s default), `--max-running-requests 256` (`:146`), `--context-length 1048576` (`:149`), an engine that cannot be told to stop dumping.
- **[X3 / HIGH — systemic]** Global app-stdout shipping is a fixed feature; the manifest proves no content is collected **only if no service prints content**. Unlike the base's deployment, it forks to **one** destination here (near.ai), not two — the measured harness `546161bc` carries no Datadog agent.
- **[X4 / HIGH]** Two content-egress switches on the E2EE terminator sit outside `allowed_envs` but within reach of the non-measured `/compose/up` overlay (`WEB_CONTEXT_SEARCH_*`, `FUSION_*`), as does the routing control `LONG_TIER_ONLY`.

## Verdict

**QUALIFIED PASS — the same qualification as the base page, with one exposure *reduced* by the harness beneath it and nothing in this file made worse.**

- **The delta itself is privacy-neutral.** The only change from the audited base is `--mem-fraction-static 0.78 → 0.75` (`:145`) — a VRAM headroom knob with no logging, persistence, telemetry, egress, auth, or network surface. Every base finding re-verified at its original line and severity.
- **What this file configures:** clean. Every logging flag literal and minimal; `--log-requests-level 0` explicit; zero `${VAR}` in the engine command; no dump/crash/profile/trace flag; `--trust-remote-code` pinned by revision; the model confirmed **text-only** (no `vision_config` at `d975c19a`), so no media-fetch path is reachable; `OHTTP_ENABLED=true` literal; telemetry destinations literal; **no KV or prompt disk cache**; the discovery stub properly gated and fail-closed. On the configured path, **no plaintext copy is written to any log, file, cache, or network destination.**
- **What this file fails to contain:** the engine's own live paths. It leaves `/configure_logging` reachable by two routes, declines `--admin-api-key`, sets the watchdog dump's in-flight ceiling to 256 requests at up to 1M tokens, and guarantees anything printed leaves the CVM unscrubbed. **The invariant this layer delivers is "content capture is not configured", not "content capture cannot be enabled".**
- **The one thing better than the base:** this deployment's measured harness ([`546161bc`](/manifests/measured/sha256-546161bcde5ef901652e7b5b774322e790748e725fbfb158d0f394153271bc86.md)) measures **no Datadog agent**, so engine stdout leaves to exactly one off-box destination (near.ai's gateway) rather than also forking to `us3.datadoghq.com`. That is a property of the host, not of this file.
- **Recommended, in order:** (1) pass `--admin-api-key` to the engine; (2) apply the `:8001` stub's allowlist idiom to the serving blocks, or move the engine to a project-private network; (3) reconsider `--max-running-requests 256` at 1M context as the dump-exposure ceiling; (4) add a scrubbing/deny processor to `filter/app_logs` for engine-sourced records; (5) digest-pin dcgm-exporter as two sibling recipes already do.

## Not traced

- Whether the action log at commit `2390cb8cbb5b` currently pins this `file_sha256` on the live host — asserted by the orchestrator's capture and re-hashed locally (matches); the log itself was not re-fetched in this review.
- Whether the deployed `vllm-proxy-rs@b183677a` tree applies any path filtering, and whether `WEB_CONTEXT_SEARCH_*` / `FUSION_*` empty means fail-closed — inherited from the source reviews, not re-derived here (if fail-closed, the Check 4 HIGH downgrades).
- Whether sglang, the proxy, or nginx ever *print* request content at the configured levels — engine/proxy source legs, load-bearing under X3.
- Whether nginx actually spills bodies to `client_body_temp` on this image, and what the guest does with that file afterwards.
- dcgm-exporter's effective NVML visibility under CC (hardware leg); the guest OS's swap and core-dump policy ([`os/`](/os/)) — the measured harness `546161bc` does not pin `vm.swappiness` (its Not-traced/Check notes), so nothing in the measured layer keeps prompt-derived pages out of swap on this pair.
