# cvm-compose-files prod/dsv4-qwen36-gemma4.yaml @ 795aab85... (four-model inner compose — DeepSeek-V4-Flash-0731 A/B pair + Qwen3.6-27B + Qwen3.6-35B-A3B + Gemma-4-31B)

## verdict: QUALIFIED PASS on logging config — no content-logging switch anywhere, OHTTP forced on all five proxies, watchdog labels are truthful, and the A/B tag machinery carries only deployment metadata; but the sglang upgrade to v0.5.16 turned both Qwen3.6 engines multimodal, so this file now runs two request-driven media fetchers with NO allowlist beside a Gemma-4 that constrains both controls — plus the carried residuals: two Qwen3.6 engines `--trust-remote-code` with no `--revision`, unredacted double stdout shipping, unauthenticated engine admin behind allowlist-free proxies, and the documented `/compose/up` env-splice channel

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/dsv4-qwen36-gemma4.yaml` |
| **file_sha256** | `795aab8598653d4f74e3826d662d35d0040a71bca6e6256e5e3646bd5738d5c8` (re-verified locally: `shasum -a 256` of the captured file matches) |
| **role** | log-pinned inner compose for the four-model combined node: `deepseek-ai/DeepSeek-V4-Flash-0731` (SGLang FP4 TP2, **two A/B arms** — arm2 GPUs 0-1, arm3 GPUs 2-3), `Qwen/Qwen3.6-27B-FP8` (SGLang TP1, GPU 4), `Qwen/Qwen3.6-35B-A3B-FP8` (SGLang TP1, GPU 5), `google/gemma-4-31B-it` (vLLM, 2x TP1 replicas, GPUs 6-7) — behind five `vllm-proxy-rs` instances, nginx, registrar, five dcgm exporters, otelcol-contrib |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. Fetched at compose-manager commit `2390cb8cbb5b` and hash-checked against the log's `file_sha256` before being written to disk; the action-log match was not re-fetched live in this review |
| **review** | reviewed 2026-08-17 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** the full 1777-line compose: four engine definitions (five `x-*` anchors) across six model containers, five proxy configs, the Gemma tile80 startup patch, the model-downloader, nginx routing, the registrar script, five dcgm exporters, and otelcol-contrib end to end. Per-image behavior is deferred to the published image pages, linked per finding: DeepSeek engine [`nearaidev/sglang@ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) (PRIVATE at deployed flags), both Qwen3.6 engines [`lmsysorg/sglang@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (v0.5.16, PRIVATE at deployed flags — audited concurrently in this run), Gemma engine [`vllm/vllm-openai@960ac5b3`](/images/docker.io/vllm/vllm-openai/sha256-960ac5b3fda0547c3ce64d8f69bae9fa242d5ae5382fc74a98cd8ef13950e403.md), and the shared proxy [`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md).

**This is a DELTA review** against the [base page (`cf11b98c`)](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen36-gemma4/sha256-cf11b98ccd81a738e051e8c743a67ded88af181648ab05d44d647409c5127d5e.md) (verdict: qualified PASS — Gemma media fully constrained, OHTTP on all four proxies, two Qwen3.6 `--trust-remote-code` unpinned). Each base finding was re-checked at this file, not assumed. The delta map is below.

---

## Delta summary (base `cf11b98c` → this `795aab85`)

| Base-page finding | Status at `795aab85` |
|---|---|
| Gemma-4 media fetch fully constrained; "no unconstrained multimodal sibling exists here" | **REFUTED as a whole-node statement.** Gemma still sets both controls (`:241`, `:250`). But the two Qwen3.6 engines were upgraded `v0.5.12 (9e02c8e1)` → `v0.5.16 (984699c2)`, and at v0.5.16 both `Qwen/Qwen3.6-27B-FP8` and `Qwen/Qwen3.6-35B-A3B-FP8` are **multimodal** (vision_config + image_token_id; engine page verified), with `--mm-feature-transport cpu` set (`:144`, `:183`). sglang has no media-allowlist mechanism at all. So this node now has two unconstrained multimodal siblings beside the constrained Gemma — **NEW MEDIUM**. |
| Both Qwen3.6 engines `--trust-remote-code` with no `--revision` (base: HIGH) | **CONFIRMED, unchanged config; severity reassessed to LOW (ARMED)** to match the concurrent image review's deployed-state finding (nothing executes at the currently-observed HF `main`; a future push would). Still this file's to state. |
| No content-logging switch; `--log-requests-level 0` on all SGLang commands | **CONFIRMED** (`:106`, `:157`, `:196`). |
| OHTTP forced on all proxies | **CONFIRMED** — now **five** proxy instances, all `OHTTP_ENABLED=true` (`:347`, `:384`, `:544`, `:612`, `:680`). |
| Telemetry: engine stdout leaves the box twice, unredacted (= harness C3) | **CONFIRMED** — recipe-layer otelcol-contrib keeps model-tagged app logs, exports to `telemetry.infra.near.ai`; no redaction stage. |
| `/compose/up` splice sets `DSV4_RETURN_HIDDEN_STATES`, can widen media allowlist | **CONFIRMED**, documented in-file (`:70-79`, `:108`, `:241`). |
| — | **NEW INFO** — richer A/B machinery: two DeepSeek arms (`arm2-chunk4096-sparse-on`, `arm3-chunk4096-sparse-off`) with dense `com.datadoghq.ad.logs` / `nearai.otel.*` tag sets. Checked for content; pure deployment metadata. |
| — | **watchdog-label truthfulness** (requested check): the only service asserting `watchdog_timeout:1800` (DeepSeek) actually passes `--watchdog-timeout 1800` — truthful. See INFO below. |

---

## Env reachability and the splice channel

Every logging knob in this file is a literal, not an operator-set env: `--log-requests-level 0` on all three SGLang commands (`:106`, `:157`, `:196` — the minimum), `VLLM_LOGGING_LEVEL=INFO` on every engine (`:44`, `:115`, `:247`, `:479`), `LOG_FORMAT=json` on all five proxies (`:345`, `:382`, `:542`, `:610`, `:678`), `NCCL_DEBUG=WARN`, otelcol `telemetry.logs.level: info` (`:1503`). **`OHTTP_ENABLED=true` is a literal on all five proxies** (`:347`, `:384`, `:544`, `:612`, `:680`) — E2EE posture is not operator-degradable. **No `--api-key`/`--admin-api-key` anywhere** (grep-verified) — engines are unauthenticated on the flat network.

Interpolated vars reachable only via compose-manager's non-measured `/compose/up` splice (not the measured `allowed_envs` channel; the measured harness for this node is referenced, not re-derived here): `DSV4_RETURN_HIDDEN_STATES` (`:108`), `VLLM_ALLOWED_MEDIA_DOMAINS` (`:241`), `HF_HUB_OFFLINE` (defaulted `0`, `:43`/`:114`/`:246`/`:478`), `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (five proxies), `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (five proxies), `CLOUD_API_USAGE_TOKEN`, `PROXY_TOKEN`, `MODEL_PROXY_TOKEN` (`:322`), `MONITORING_INGEST_TOKEN` (`:925`).

---

### HIGH (deployed: ON) — every plaintext service's stdout leaves the CVM via the recipe's own otelcol-contrib, unredacted, to near.ai's telemetry gateway; on datadog-bearing nodes a measured agent ships it again to a third-party SaaS — sink: telemetry / egress

Traced end to end in this file. Every app service logs via the `json-file` driver (`x-logging-conf`, `:1-6`), which stamps `com.datadoghq.ad.logs` and `com.docker.compose.service` onto each container's log labels. The recipe runs its **own** `otelcol-contrib` (`:918`, digest-pinned `otel/opentelemetry-collector-contrib@sha256:85ac41c2…`), and its `logs/app` pipeline (`:1480-1483`) is:

```
receivers:  [filelog/docker_containers]           # tails /var/lib/docker/containers/*/*-json.log  (:995)
processors: [memory_limiter, resource,
             transform/docker_metadata,           # lifts service.name from the datadog label     (:1421)
             filter/app_logs,                      # KEEP records with model/deployment attrs       (:1428)
             groupbyattrs/docker_service, batch]
exporters:  [otlphttp/gateway]                     # https://telemetry.infra.near.ai                (:1465)
```

The filelog receiver's `regex_parser` operators lift `model:` (`:1025`) and `deployment:` (`:1037`) out of the `com.datadoghq.ad.logs` label into attributes. `filter/app_logs` (`:1428-1436`) then **drops** only records where `model == nil and deployment == nil` (`:1432`) or whose `service.name` is a management service (`compose-manager`, `-launcher`, `otelcol-management`, `otelcol-contrib`, `datadog-agent`), and **keeps** everything else. Every engine, proxy, nginx, registrar, and dcgm container carries a `model:`/`deployment:` tag, so its stdout is kept and exported verbatim to `https://telemetry.infra.near.ai` with `Authorization: Bearer ${MONITORING_INGEST_TOKEN}` (`:1467`). **Reading `filter/app_logs` alone gives the opposite answer** — the keep/drop polarity is only legible with the receiver that populates `model`/`deployment`.

**There is no scrub/hash/truncate processor in this pipeline** (`:1417-1461`). So the guarantee "no content leaves" reduces exactly to "no plaintext service ever prints content" — carried entirely by the per-image source pages. The two known ways a print gets switched on or forced are the engines' unauthenticated `/configure_logging` (CRITICAL on the engine pages) and the watchdog hang-dump (HIGH on the engine pages), multiplied across three engine codebases and five proxies here. On datadog-bearing nodes the measured `datadog-agent` (`DD_LOGS_CONFIG_CONTAINER_COLLECT_ALL=true`, referenced from the measured page, not in this file) additionally carries the same stdout to a third-party US SaaS; on datadog-free nodes the only off-box path is near.ai's own gateway above. Either way, engine stdout leaves the CVM on **every** node.

- **[MEDIUM]** otelcol-contrib's OTLP receivers on `0.0.0.0:4317/4318` (`:990-992`, `expose`d `:931-933`) feed `metrics/otlp` and `traces` pipelines (`:1489-1499`) forwarding **verbatim** to near.ai. Any prompt-bearing span injected on the flat network ships unexamined.
- **[INFO]** All metrics paths are Prometheus scrapes of `/metrics` (`:1069-1391`) — counters/histograms with routing labels (model, model_path, precision, deployment, host, ip, port, instance, and the A/B tag set); no content dimension. The filelog `regex_parser` operators extract the same label taxonomy — metadata, not content.

---

### MEDIUM (deployed: ON) — two Qwen3.6 engines are multimodal at v0.5.16 and fetch request-supplied media URLs with no domain allowlist, while sibling Gemma-4 in the same file constrains both controls — sink: egress (request-driven)

**This is the delta.** The base page found Gemma-4 the only multimodal engine on the node and the three SGLang engines text-only, concluding "no unconstrained multimodal sibling exists here." The Qwen3.6 image was then upgraded `v0.5.12 (9e02c8e1)` → `v0.5.16 (984699c2)` (`:134`, `:173`), and the concurrent [v0.5.16 engine review](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) verified from the HF `config.json` that **both** `Qwen/Qwen3.6-27B-FP8` and `Qwen/Qwen3.6-35B-A3B-FP8` now carry `vision_config` + `image_token_id` and register as multimodal architectures, with multimodal auto-on. This file corroborates: both commands pass `--mm-feature-transport cpu` (`:144`, `:183`), which is only meaningful when multimodal is live.

The sibling asymmetry, in this one file:

| service | media allowlist | redirects |
|---|---|---|
| Gemma-4 (vLLM, `:206-254`) | `--allowed-media-domains ${VLLM_ALLOWED_MEDIA_DOMAINS:-prod-files-secure.s3.us-west-2.amazonaws.com}` (`:241`) | `VLLM_MEDIA_URL_ALLOW_REDIRECTS=0` (`:250`) |
| Qwen3.6-27B (sglang, `:128-166`) | **NONE** (sglang has no allowlist mechanism; engine page MEDIUM) | followed by default |
| Qwen3.6-35B-A3B (sglang, `:167-205`) | **NONE** | followed by default |

Note also that neither Qwen3.6 service pins `--revision`, so the very `config.json` that makes them multimodal is read from a mutable `main` and can change under them — this compounds the unpinned `--trust-remote-code` finding below rather than being independent of it.

The engine page rates the sglang media fetch a MEDIUM (deployed ON): the bytes carried outbound are the requesting client's own URL, so it is not cross-tenant prompt exfiltration, but it is a genuine model-external, request-driven egress from the plaintext process (SSRF / confirmation channel / `file://` local-read primitive), on by default. Two such engines run here. Gemma's constraint remains the model to replicate; sglang cannot replicate it without an upstream change, so on this node the containment is `--disable-multimodal` (at the cost of vision) or a network egress allowlist (there is none — see below). Residual: `VLLM_ALLOWED_MEDIA_DOMAINS` is interpolated and splice-widenable (`:241`); `VLLM_MEDIA_URL_ALLOW_REDIRECTS=0` is a hard literal.

---

### MEDIUM (deployed: OFF) — a documented /compose/up env-splice channel can enable hidden-state passthrough and widen the media allowlist with no attestation-visible change — sink: response / egress

The DeepSeek anchor comment (`:70-79`) states that `DSV4_RETURN_HIDDEN_STATES` is "spliced into /compose/up like GPT_OSS_CACHE_SUBPATH" by the gpu-manager — in-file confirmation of a second env channel that bypasses the measured `allowed_envs` with no measurement change. The `${VAR:+…}` form (`:108`) is a footgun: any non-empty value (`"0"` included) turns the flag ON; only unsetting it is OFF. Default OFF (flag absent when unset). Hidden states are prompt-derived tensors in the API response; end-to-end exposure is deferred to the inference-proxy/cloud-api source. The same channel can set `VLLM_ALLOWED_MEDIA_DOMAINS` (`:241`) to widen Gemma's allowlist. Deployed OFF in this file, but neither var is in the measured env, so neither change would surface in the hardware measurement.

- **[MEDIUM] `WEB_CONTEXT_SEARCH_URL` + `_API_KEY` on all five plaintext-path proxies** (`:355-356`, `:392-393`, `:551-552`, `:619-620`, `:687-688`) — an unpinned external URL handed to the E2EE-terminating services. The deployed proxy build was source-verified fail-closed when unset, opt-in per request, sending only a model-generated query ([`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md), all five proxies run this one digest, `:28`). Residual: operator-suppliable via the splice channel.

---

### MEDIUM (deployed: ON) — the engines' unauthenticated admin surface is reachable through allowlist-free nginx and proxies on the flat network — sink: log / egress

Every SGLang engine here exposes an unauthenticated `/configure_logging` endpoint (CRITICAL on the [v0.5.16 engine page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) and the [DeepSeek engine page](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md)): one POST turns on verbatim prompt+output logging and arbitrary-path request pickling, no restart. This file supplies the reachability. The network is the flat `external: true` bridge `dstack_default` (`:951-953`) — any co-resident container can reach engine port 8000 directly. nginx (digest-pinned `:276`, config `:1662-1777`) publishes plaintext HTTP host ports 8000-8005 (`:286-291`) plus TLS 8444→443 and 8005 (`:292`, `:1743`), and **every one of its 10 server blocks routes with a bare `location /`** (`:1696`, `:1702`, `:1708`, `:1714`, `:1720`, `:1739`, `:1748`, `:1757`, `:1766`, `:1776`) — no path allowlist, so the full HTTP surface of each proxy (and, through the allowlist-free proxy design, each engine) is exposed on every listener, including `/configure_logging`. In-file mitigations: upstreams are always proxies, never raw engines; `proxy_buffering off; proxy_cache off` (`:1687-1688` — no bodies to disk); the access log uses `$uri` not `$request` (`:1664-1679`) — no bodies, though `request_id`/`org_id`/`workspace_id` are per-request user-attributable metadata the telemetry pipeline ships off-box **[INFO]**.

---

### LOW (deployed: ARMED) — both Qwen3.6 engines run --trust-remote-code with no --revision pin, unlike every other model in this file — sink: potential in-enclave code execution

Sibling-comparison finding (audit-surface §8), carried from the base page (which rated it HIGH) and reassessed to LOW (ARMED) to match the concurrent image review's deployed-state analysis. Pinning, service by service:

| service (anchor) | image | `--revision` | `--trust-remote-code` |
|---|---|---|---|
| DeepSeek arm2 + arm3 (`:57-127`) | `nearaidev/sglang@ec518148` | `7872f01b…` (`:83`) | yes — pinned |
| Qwen3.6-27B (`:128-166`) | `lmsysorg/sglang:v0.5.16@984699c2` | **NONE** | **yes — UNPINNED** (`:156`) |
| Qwen3.6-35B-A3B (`:167-205`) | same digest `984699c2` | **NONE** | **yes — UNPINNED** (`:195`) |
| Gemma-4 x2 (`:206-254`) | `vllm/vllm-openai@960ac5b3` | `9a9994e6…` (`:233`) | yes — pinned |

Both Qwen3.6 commands set `--trust-remote-code` (`:156`, `:195`) with no `--revision`, so the model tree resolves to a mutable HF `main` (and `HF_HUB_OFFLINE` defaults `0`, `:114`, so a cache miss reaches the network — the model-downloader (`:256-273`) pre-fetches only DeepSeek at a pinned revision, `:265`; Qwen/Gemma are pulled lazily by the engines). At the currently-observed state there is nothing to execute (the engine page verified `Qwen/Qwen3.6-27B-FP8` `main` has no `auto_map`/`.py`, and `Qwen3_5*` is natively supported), so this is ARMED, not ON: a later push adding an `auto_map` + module would execute attacker-chosen Python inside a plaintext-handling enclave process with **no trace in near.ai's attestation chain** — manifest, measurement, and action log all unchanged. Every other model here is pinned; the inconsistency is these two services. Fix belongs in this file: add `--revision <sha>` to both, mirroring DeepSeek/Gemma.

---

### INFO (deployed: ON) — the A/B experiment tag machinery carries only deployment metadata, and the one service asserting watchdog_timeout:1800 in a label actually deploys that flag

Two questions were asked of the A/B machinery; both answered from the file.

**(a) Do the `com.datadoghq.ad.logs` / `nearai.otel.*` tag sets carry request- or user-derived data?** No. The dense tag sets on the two DeepSeek arms (`:429-461`, `:499-530`) and the Prometheus scrape labels (`:1093-1136`) are deployment metadata only: `config_variant`, `decode_mode`, `chunk_size`, `ab_arm` (`arm2-chunk4096-sparse-on` / `arm3-chunk4096-sparse-off`), `max_prefill_tokens`, `continuous_decode_steps`, `chunked_prefill_admission_reserve`, `watchdog_timeout`, `cuda_graph`, `max_running_requests`, `max_queued_requests`, `schedule_conservativeness`, `sglang_revision`, `engine_image`, `model_revision`. Every value is a static launch parameter or build identifier; none is derived from a request. The two arms differ only in the deployed env `SGLANG_OPT_FLASHMLA_SPARSE_PREFILL=0` on arm3 (`:488`) and its `ab_arm` tag.

**(b) Does `watchdog_timeout:1800` in the label match the deployed command?** Yes, for the only service that asserts it. The `watchdog_timeout:1800` label appears **only** on the two DeepSeek arms (`:429`, `:499`; `nearai.otel.watchdog_timeout: "1800"` at `:454`, `:523`; Prometheus at `:1097`, `:1131`), and the DeepSeek command **does** pass `--watchdog-timeout 1800` (`:97`, inherited by both arms via the `x-dsv4-flash-common` anchor; the DeepSeek engine page confirms 1800 deployed). So the label is truthful. The two Qwen3.6 engines pass **no** `--watchdog-timeout` — at sglang v0.5.16 the default is 300 s, so they run the hang-dump path at 300 s (engine page HIGH) — **but their labels assert no watchdog value at all**, so there is no manifest-truthfulness disagreement to report. Had a Qwen label claimed `1800`, that would have been a finding; it does not. Checked and cleared.

---

### INFO (deployed: ON) — privileges, mounts, volumes, build blocks, and the Gemma patch: structural in-box exposure, no new reader

- **All five proxies:** `privileged: true`, `user: root` (`x-vllm-proxy-common`, `:27-38`), plus `ipc: host` via `x-nvidia` (`:8-15`). Each mounts `dstack.sock` and `certs:ro`. Root-equivalent in the guest; these are the E2EE/OHTTP terminators — structural in-box exposure, not a new reader. **OHTTP is forced on for all five** (`:347`, `:384`, `:544`, `:612`, `:680`), the control that keeps the gateway from seeing plaintext.
- **`ipc: host` node-wide** (`:10`, inherited by every engine and proxy): one shared IPC namespace across four model families' plaintext — cross-model shared-memory visibility, permitted though not configured.
- **`dcgm-exporter` x5** (`x-dcgm-common`, `:49-56`): tag-only `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:50`) with `SYS_ADMIN` (`:53`) + nvidia runtime — mutable-tag bytes can drift under the attestation with no attestation-visible trace; kept in scope for its privilege. At the audited config it exports GPU telemetry counters, not content.
- **Volumes** (`:955-962`): `hugginface_cache` (weights), `kernel_cache:/root/.cache/deep_gemm` and `vllm_cache` / `VLLM_CACHE_ROOT=/root/.cache/vllm/gemma4-31b-qat-vllm` (`:248`, config-hash-keyed compiled kernels), `otelcol_app_storage` (collector send-queue/file_storage, `:936`/`:1473`), `certs` (external, TLS certs). **No KV/prompt/request disk tier; no responses store; no dump/profiler directory.** No request-derived data lands on any volume at deployed flags.
- **No `build:` blocks in this file.** The only in-container code-execution-at-start is the Gemma `gemma4_tile80_patch` (`:220-223`, body `:969-983`): an inline Python script (pinned by this file's hash) that rewrites one vLLM FA4 attention-kernel tile constant in-place. **NON-FATAL by design** (`:978-982`: anchor-not-found → prints a warning, serves unpatched); touches only a `FwdConfig` numeric — no logging, no I/O beyond the one file, no egress, no content path. Privacy-neutral. The `model-downloader` (`:256-273`) runs `uvx --from 'huggingface_hub[hf_xet]' hf download … --revision 7872f01b…` (`:265`) — pinned, weights only.
- **Named outbound destinations, exhaustively:** proxies → `https://cloud-api.near.ai` (literal, `:342` etc.) + `http://compose-manager:8080` (host-gateway, `:33`) + `${WEB_CONTEXT_SEARCH_URL}` (unset default); registrar → `https://completions.near.ai` (`:1509`) registering the four model domains (`:1654-1657`), health-probing with a **synthetic** `"hi"` message (`:1574` — not user content); engines → HF hub (Qwen/Gemma lazily, DeepSeek pre-fetched) and the two Qwen3.6 engines' unguarded media fetch (MEDIUM above); Gemma → the one literal S3 media domain; otelcol-contrib → `https://telemetry.infra.near.ai` (`:1465`). The network has no `internal: true` and no egress allowlist (`:951-953`) — **every engine can open arbitrary outbound connections [MEDIUM]**.

---

## Verdict

**QUALIFIED PASS at this layer.** The file hands no logging switch to any engine or proxy, forces OHTTP on for all five proxies, keeps every telemetry destination literal, buffers no plaintext to disk at any hop, and — checked explicitly — its A/B tag machinery carries only deployment metadata and its one `watchdog_timeout:1800` label matches the flag the deployed DeepSeek command actually sets. The qualifications, in descending order of cost to the end-to-end claim:

1. **The sglang v0.5.16 upgrade turned both Qwen3.6 engines multimodal**, so this file now runs two request-driven media fetchers with no domain allowlist and redirects on, beside a Gemma-4 that constrains both — a per-node containment gap that did not exist at the base page (MEDIUM, deployed ON).
2. **Harness-C3** — everything any of these services prints leaves the TEE via the recipe's own otelcol-contrib to `telemetry.infra.near.ai`, unredacted, on every node, and additionally to Datadog on datadog-bearing nodes; the posture is conditional on per-image print discipline plus no 300 s / 1800 s hang given the watchdog-dump HIGH on the engine pages (HIGH, deployed ON).
3. **Both Qwen3.6 engines run `--trust-remote-code` with no `--revision`** — HF-head code execution inside the enclave with no attestation-visible trace, disarmed only by the currently-observed HF state (LOW, ARMED).
4. The unauthenticated engine `/configure_logging` (CRITICAL on the engine pages) is reachable from the flat network through the allowlist-free `location /` proxies/nginx (MEDIUM, deployed ON).
5. The `/compose/up` splice — documented in this file's own comments — can enable `DSV4_RETURN_HIDDEN_STATES` and widen `VLLM_ALLOWED_MEDIA_DOMAINS` with no measurement change (MEDIUM, deployed OFF).

Recommend, in-file: pin `--revision` on both Qwen3.6 engines; front the engines with a path allowlist; add a redaction/scrub processor to the otelcol `logs/app` pipeline; and either `--disable-multimodal` on the Qwen3.6 engines or add a network egress allowlist, since sglang cannot replicate Gemma's media constraint.

## Not traced (load-bearing)

- Whether this `file_sha256` matches the signed action log's current entry live (fetched at compose-manager `2390cb8cbb5b` and hash-checked to disk; not re-fetched against the live log in this review).
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source).
- Engine print discipline and exact `/configure_logging` / watchdog behavior at the configured levels — deferred to the engine pages ([`984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md), [`ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md)), which are load-bearing under the telemetry finding.
- Proxy no-allowlist forwarding and web-search fail-closed behavior — deferred to the [proxy page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md).
- What `--enable-return-hidden-states` exposes end-to-end when spliced on (inference-proxy + cloud-api source, outside this repo's scope).
- Whether the warm HF cache shields the unpinned Qwen3.6 engines from head drift in practice (runtime state, not config).
- `dcgm-exporter` bytes under its mutable tag; the measured management harness (compose-manager, datadog-agent presence per node) — referenced from the measured layer, not re-derived here.
