# cvm-compose-files prod/dsv4-qwen36-gemma4.yaml @ 52ef39d9... (four-model inner compose — DeepSeek-V4-Flash single TP4 sparse-off arm + Qwen3.6-27B + Qwen3.6-35B-A3B + Gemma-4-31B)

## verdict: QUALIFIED PASS on logging config — the arm consolidation adds no logging/dump/debug switch (the only new env is the kernel-dispatch toggle SGLANG_OPT_FLASHMLA_SPARSE_PREFILL=0) and strictly shrinks the surface: one DS4F TP4 sparse-off arm replaces two TP2 arms, host ports 8004/8005, one proxy, one dcgm and the second TLS listener are gone with zero dangling references, and the new ab_arm/sparse_prefill tags are static deployment metadata; but every base qualification carries: two multimodal Qwen3.6 engines with no media allowlist, unredacted double stdout shipping, both Qwen3.6 engines --trust-remote-code with no --revision, unauthenticated engine admin behind allowlist-free proxies, and the documented /compose/up env-splice channel

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/dsv4-qwen36-gemma4.yaml` |
| **file_sha256** | `52ef39d9746c0c4ac961c881c75efa2893257d2a10cb247f9af364302299a658` (re-verified locally: `shasum -a 256` of the captured file matches; `git show 60cdbdf05d84:prod/dsv4-qwen36-gemma4.yaml` and the merge commit `6aabc6d36b04` both hash to the same value) |
| **role** | log-pinned inner compose for the four-model combined node (gpu04): `deepseek-ai/DeepSeek-V4-Flash-0731` (SGLang FP4, **single TP4 target-only sparse-prefill-off treatment arm**, GPUs 0-3 — the matched sparse-ON control is gpu30's DeepSeek in `prod/qwen35-dsv4-flash.yaml`), `Qwen/Qwen3.6-27B-FP8` (SGLang TP1, GPU 4), `Qwen/Qwen3.6-35B-A3B-FP8` (SGLang TP1, GPU 5), `google/gemma-4-31B-it` (vLLM, 2x TP1 replicas, GPUs 6-7) — behind four `vllm-proxy-rs` instances, nginx, registrar, four dcgm exporters, otelcol-contrib |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. Pinned at commit `6aabc6d36b04` (merge of PR #180 "agent/gpu04-dsv4-tp4-sparse-ab"; file content landed at `60cdbdf05d84`, 2026-08-19), fetched and hash-checked against the log's `file_sha256`; the action-log match was not re-fetched live in this review |
| **review** | reviewed 2026-08-21 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** the full 1546-line compose: four engine anchors across five model containers, four proxy configs, the Gemma tile80 startup patch, the model-downloader, nginx routing, the registrar script, four dcgm exporters, and otelcol-contrib end to end. Per-image behavior is deferred to the published image pages, linked per finding: DeepSeek engine [`nearaidev/sglang@ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md) (PRIVATE at deployed flags), both Qwen3.6 engines [`lmsysorg/sglang@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (v0.5.16, PRIVATE at deployed flags), Gemma engine [`vllm/vllm-openai@960ac5b3`](/images/docker.io/vllm/vllm-openai/sha256-960ac5b3fda0547c3ce64d8f69bae9fa242d5ae5382fc74a98cd8ef13950e403.md), and the shared proxy [`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md).

**This is a DELTA review** against the [base page (`795aab85`)](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen36-gemma4/sha256-795aab8598653d4f74e3826d662d35d0040a71bca6e6256e5e3646bd5738d5c8.md) (verdict: QUALIFIED PASS on logging config — multimodal Qwen3.6 gap, unredacted stdout shipping, unpinned `--trust-remote-code`, unauthenticated admin surface, splice channel). Each base finding was re-checked at this file, not assumed. The old→new diff (commits `33399d201c54` → `60cdbdf05d84`) was independently regenerated with `git diff` and byte-compared against the captured 585-line diff — identical; +56/−287 lines. The delta map is below.

---

## Delta summary (base `795aab85` → this `52ef39d9`)

| Change in the diff | Privacy assessment at `52ef39d9` |
|---|---|
| **DS4F arm restructure**: 2×TP2 A/B arms (`arm2-chunk4096-sparse-on` GPUs 0-1, `arm3-chunk4096-sparse-off` GPUs 2-3) → one TP4 "target-only sparse-OFF" treatment on GPUs 0-3 (`:394`). Command delta: `--tp-size 2`→`4` (`:91`), `--chunked-prefill-size 4096`→`8192` (`:101`). Env delta: `SGLANG_OPT_FLASHMLA_SPARSE_PREFILL=0` promoted from arm3's service env into the shared anchor (`:133`). | **No new flag or env names a logging/dump/debug switch.** The one new env is a FlashMLA kernel-dispatch toggle; the in-file comment (`:131-132`) documents it as a partial bypass (batches with `q.shape[0] > 11673` still enter the sparse kernel) — a performance/correctness matter, not a content sink. `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS=20` now sits on the surviving proxy (`:363` — it was arm3's; the merge kept arm3's env block with the tp4 backend URL at `:357`) — a timeout, not a logging knob. Same audited engine digest `ec518148` (`:76`); `--log-requests-level 0` retained (`:113`). |
| **Service/port removals**: `model-sg-dsv4-flash-fp4-tp2-arm3`, `proxy-dsv4-flash-arm3`, `dcgm-dsv4-flash-arm3` deleted; nginx host ports 8004/8005 deleted (`:294-299` now 8000-8003 + 8444); `TLS_PORT_2` gone from the registrar env (`:326-329`); registrar loops shrink to 8000-8003 (`:1328`, `:1357`, `:1415`); `routing_port_for()` and `check_tls_route()` deleted, `register_endpoint` always dials `TLS_PORT` (`:1425`). | **Strict surface reduction, no dangling references** — grep for `arm3`, `8004`, `8005`, `TLS_PORT_2`, `tp2` finds zero matches in this file. The registrar still carries only health/status: probe payload is the synthetic `"hi"` message (`:1375`), registration payloads are endpoint+routing_port (`:1338`), endpoint (`:1345`), model+domain (`:1352`). No content. |
| **Telemetry label growth**: `ab_arm:tp4-target-only-sparse-off-gpu04` + `sparse_prefill:off` added to the proxy log label (`:365`) and otel labels (`:380-381`), the engine datadog labels (`:399-400`) and otel labels (`:417-421`), the dcgm labels (`:682-683`, `:698-699`), and the Prometheus scrape configs (`:963-967`, `:1069-1070`, `:1139-1140`). | **Deployment metadata only** — every value is a static launch parameter or arm identifier; none is request-derived. Log pipeline scope unchanged from the base page: same filelog receiver (`:867-935`), same `filter/app_logs` keep/drop polarity (`:1237-1246`), same single exporter `https://telemetry.infra.near.ai` (`:1273-1276`). The only pipeline delta is `num_consumers: 1` on the exporter `sending_queue` (`:1278`) — a delivery-ordering setting, no content implication. |
| **`HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}` removed** from the deleted arm3 service env. | **A duplicate removal, not an exposure change**: the shared anchors still set it — `x-sglang-env` (`:47`), the DS4F anchor (`:120`), Gemma (`:255`), model-downloader (`:280`). No new env var, mount, or port anywhere in the diff; the interpolated-var set shrinks by `TLS_PORT_2`. Volumes unchanged (`:828-835`). |
| Base headline caveat: v0.5.16 turned both Qwen3.6 engines multimodal with no allowlist (base MEDIUM, ON) | **CARRIES, unchanged** — same image digest `984699c2` (`:144`, `:183`), `--mm-feature-transport cpu` still passed (`:154`, `:193`), still no allowlist mechanism, beside Gemma's constrained pair (`:251`, `:260`). |
| OHTTP posture | **CARRIES** — now **four** proxy instances, all `OHTTP_ENABLED=true` as literals (`:353`, `:445`, `:513`, `:581`). E2EE posture not operator-degradable. |
| Base findings: stdout telemetry HIGH; splice MEDIUM OFF; admin-surface MEDIUM ON; `--trust-remote-code` LOW ARMED; A/B-tags INFO; privileges INFO | **ALL RE-CHECKED AND CARRIED** below with this file's line numbers; none retired, none escalated. The arm3-specific portions of the base A/B-tag and admin-surface analysis are retired with the removed services. |

---

## Env reachability and the splice channel

Every logging knob in this file is a literal, not an operator-set env: `--log-requests-level 0` on all three SGLang commands (`:113`, `:167`, `:206` — the minimum), `VLLM_LOGGING_LEVEL=INFO` on every engine (`:49`, `:122`, `:257`), `LOG_FORMAT=json` on all four proxies (`:351`, `:443`, `:511`, `:579`), `NCCL_DEBUG=WARN` (`:52`, `:125`, `:263`), otelcol `telemetry.logs.level: info` (`:1313-1314`). **`OHTTP_ENABLED=true` is a literal on all four proxies** (`:353`, `:445`, `:513`, `:581`). **No `--api-key`/`--admin-api-key` anywhere** (grep-verified) — engines are unauthenticated on the flat network.

Interpolated vars reachable only via compose-manager's non-measured `/compose/up` splice (not the measured `allowed_envs` channel; the measured harness for this node is referenced, not re-derived here): `DSV4_RETURN_HIDDEN_STATES` (`:115`), `VLLM_ALLOWED_MEDIA_DOMAINS` (`:251`), `HF_HUB_OFFLINE` (defaulted `0`, `:48`/`:121`/`:256`), `HUGGING_FACE_HUB_TOKEN` (`:47`/`:120`/`:255`/`:280`), `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (four proxies), `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (four proxies), `CLOUD_API_USAGE_TOKEN`, `PROXY_TOKEN`, `MODEL_PROXY_TOKEN` (`:328-329`), `MONITORING_INGEST_TOKEN` (`:798`). `TLS_PORT_2` no longer exists.

---

### HIGH (deployed: ON) — every plaintext service's stdout leaves the CVM via the recipe's own otelcol-contrib, unredacted, to near.ai's telemetry gateway; on datadog-bearing nodes a measured agent ships it again to a third-party SaaS — sink: telemetry / egress

Carried from the base page; re-traced end to end at this file's line numbers, unchanged in structure. Every app service logs via the `json-file` driver (`x-logging-conf`, `:1-6`), stamping `com.datadoghq.ad.logs` and `com.docker.compose.service` onto each container's log labels. The recipe runs its own `otelcol-contrib` (`:791-792`, digest-pinned), whose `logs/app` pipeline (`:1290-1293`) is:

```
receivers:  [filelog/docker_containers]           # tails /var/lib/docker/containers/*/*-json.log  (:867-868)
processors: [memory_limiter, resource,
             transform/docker_metadata,           # lifts service.name from the datadog label     (:1225)
             filter/app_logs,                      # KEEP records with model/deployment attrs      (:1237)
             groupbyattrs/docker_service, batch]
exporters:  [otlphttp/gateway]                     # https://telemetry.infra.near.ai               (:1274)
```

The filelog receiver's `regex_parser` operators lift `model:` (`:898`) and `deployment:` (`:910`) out of the `com.datadoghq.ad.logs` label into attributes. `filter/app_logs` (`:1237-1246`) **drops** only records where `model == nil and deployment == nil` (`:1241`) or whose `service.name` is a management service, and **keeps** everything else. Every engine, proxy, nginx, registrar, and dcgm container carries a `model:`/`deployment:` tag, so its stdout is exported verbatim to `https://telemetry.infra.near.ai` with `Authorization: Bearer ${MONITORING_INGEST_TOKEN}` (`:1274-1276`). **There is no scrub/hash/truncate processor in this pipeline** — the "no content leaves" guarantee still reduces exactly to "no plaintext service ever prints content", carried by the per-image source pages (unauthenticated `/configure_logging` CRITICAL and watchdog hang-dump HIGH there), now multiplied across three engine codebases and **four** proxies. The delta touches this finding only twice, both neutral: one fewer engine/proxy/dcgm feeding the tail, and `num_consumers: 1` on the exporter queue (`:1278`).

- **[MEDIUM]** otelcol-contrib's OTLP receivers on `0.0.0.0:4317/4318` (`:863-865`, `expose`d `:804-806`) feed `metrics/otlp` and `traces` pipelines (`:1298-1301`, `:1306-1309`) forwarding **verbatim** to near.ai. Any prompt-bearing span injected on the flat network ships unexamined.
- **[INFO]** All metrics paths are Prometheus scrapes of `/metrics` (`:940-1200`) — counters/histograms with routing labels only; the new `ab_arm`/`sparse_prefill` dimensions are static deployment metadata, not content.

---

### MEDIUM (deployed: ON) — two Qwen3.6 engines are multimodal at v0.5.16 and fetch request-supplied media URLs with no domain allowlist, while sibling Gemma-4 in the same file constrains both controls — sink: egress (request-driven)

Carried from the base page (its headline caveat), re-checked: **unchanged by this delta**. Both Qwen3.6 services still run the v0.5.16 image at the same digest `984699c2` (`:144`, `:183`) — the [engine page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) verified both models multimodal at that version — and both commands still pass `--mm-feature-transport cpu` (`:154`, `:193`). sglang has no media-allowlist mechanism; Gemma-4 beside them sets `--allowed-media-domains` (`:251`, splice-widenable default) and `VLLM_MEDIA_URL_ALLOW_REDIRECTS=0` (`:260`, hard literal). The bytes carried outbound are the requesting client's own URL — an SSRF / confirmation channel / `file://` local-read primitive from a plaintext process, not cross-tenant prompt exfiltration. Containment options unchanged: `--disable-multimodal` or a network egress allowlist (there is none — the network is the flat `external: true` `dstack_default`, `:823-826`, no `internal: true`). Neither Qwen3.6 service pins `--revision`, so the `config.json` driving multimodality reads from mutable `main` — compounding the ARMED finding below.

---

### MEDIUM (deployed: OFF) — a documented /compose/up env-splice channel can enable hidden-state passthrough and widen the media allowlist with no attestation-visible change — sink: response / egress

Carried, re-confirmed at this file. The DS4F anchor comment (`:77-85`) states `DSV4_RETURN_HIDDEN_STATES` is "spliced into /compose/up like GPT_OSS_CACHE_SUBPATH" — in-file confirmation of a second env channel bypassing the measured `allowed_envs` with no measurement change. The `${VAR:+…}` form (`:115`) still has the documented footgun (any non-empty value, `"0"` included, turns the flag ON — the comment at `:83` now says so explicitly). Default OFF (flag absent when unset). The same channel can set `VLLM_ALLOWED_MEDIA_DOMAINS` (`:251`). Neither var surfaces in the hardware measurement.

- **[MEDIUM] `WEB_CONTEXT_SEARCH_URL` + `_API_KEY` on all four plaintext-path proxies** (`:361-362`, `:452-453`, `:520-521`, `:588-589`) — an unpinned external URL handed to the E2EE-terminating services. The deployed proxy build was source-verified fail-closed when unset, opt-in per request, sending only a model-generated query ([`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md), all four proxies run this one digest, `:33`). Residual: operator-suppliable via the splice channel.

---

### MEDIUM (deployed: ON) — the engines' unauthenticated admin surface is reachable through allowlist-free nginx and proxies on the flat network — sink: log / egress

Carried; the delta **shrinks but does not close** it. Every SGLang engine still exposes the unauthenticated `/configure_logging` endpoint (CRITICAL on the engine pages): one POST turns on verbatim prompt+output logging and arbitrary-path request pickling. This file supplies the reachability: the flat `external: true` bridge `dstack_default` (`:823-826`) lets any co-resident container reach engine port 8000 directly, and nginx (digest-pinned `:286`) publishes plaintext HTTP host ports 8000-8003 plus TLS 8444→443 (`:294-299` — ports 8004/8005 and the second TLS listener are **gone**), with **every one of its now-8 server blocks routing a bare `location /`** (`:1480`, `:1486`, `:1492`, `:1498`, `:1517`, `:1526`, `:1535`, `:1545` — was 10 blocks at the base) — no path allowlist, so each proxy's full HTTP surface (and through the allowlist-free proxy design, each engine's) is exposed on every listener. In-file mitigations unchanged: upstreams are always proxies, never raw engines; `proxy_buffering off; proxy_cache off` (`:1471-1472`); the access log uses `$uri` not `$request` (`:1448-1461`, `:1463`) — no bodies, though `request_id`/`org_id`/`workspace_id` (`:1450-1452`) are per-request user-attributable metadata the telemetry pipeline ships off-box **[INFO]**.

---

### LOW (deployed: ARMED) — both Qwen3.6 engines run --trust-remote-code with no --revision pin, unlike every other model in this file — sink: potential in-enclave code execution

Carried, re-checked, untouched by the diff. Pinning, service by service:

| service (anchor) | image | `--revision` | `--trust-remote-code` |
|---|---|---|---|
| DeepSeek TP4 arm (`:62-136`) | `nearaidev/sglang@ec518148` | `7872f01b…` (`:90`) | yes — pinned (`:88`) |
| Qwen3.6-27B (`:138-175`) | `lmsysorg/sglang:v0.5.16@984699c2` | **NONE** | **yes — UNPINNED** (`:166`) |
| Qwen3.6-35B-A3B (`:177-214`) | same digest `984699c2` | **NONE** | **yes — UNPINNED** (`:205`) |
| Gemma-4 x2 (`:216-263`) | `vllm/vllm-openai@960ac5b3` | `9a9994e6…` (`:243`) | yes — pinned (`:248`) |

`HF_HUB_OFFLINE` defaults `0` (`:48`), so a cache miss reaches the network; the model-downloader (`:266-283`) pre-fetches only DeepSeek at a pinned revision (`:275`). At the currently-observed HF state there is nothing to execute (engine page), so this is ARMED, not ON: a later push to `main` would execute attacker-chosen Python inside a plaintext-handling enclave process with no trace in the attestation chain. Fix belongs in this file: add `--revision <sha>` to both.

---

### INFO (deployed: ON) — the restructured single-arm A/B machinery carries only deployment metadata, and every label value matches the deployed flag it asserts

The two questions the base page asked of the A/B machinery, re-asked of the restructured tags; both answered from the file.

**(a) Do the new `ab_arm:tp4-target-only-sparse-off-gpu04` / `sparse_prefill:off` tag sets carry request- or user-derived data?** No. The tag sets on the proxy (`:365`, `:380-381`), engine (`:399-400`, `:417-421`), dcgm (`:682-683`, `:698-699`) and Prometheus configs (`:963-967`, `:1069-1070`, `:1139-1140`) are static launch parameters and arm/build identifiers only; none is derived from a request.

**(b) Do the labels match the deployed command?** Yes, all four checked: `chunk_size:8192` vs `--chunked-prefill-size 8192` (`:101`); `watchdog_timeout:1800` vs `--watchdog-timeout 1800` (`:104`); `ab_arm:tp4-…` vs `--tp-size 4` (`:91`); `sparse_prefill:off` vs `SGLANG_OPT_FLASHMLA_SPARSE_PREFILL=0` (`:133`) — with the honesty caveat the file itself documents (`:131-132`): the toggle is a **partial** bypass, batches with `q.shape[0] > 11673` still enter the sparse kernel, so "off" is the deployed intent, not an absolute. The two Qwen3.6 engines pass no `--watchdog-timeout` (300 s default at v0.5.16; engine-page HIGH applies) and assert no watchdog label — no truthfulness disagreement. **Residual, not a mitigation:** the matched sparse-ON control now lives in `prod/qwen35-dsv4-flash.yaml` on gpu30 (verified present at commit `6aabc6d36b04`, `ab_arm:tp4-target-only-sparse-on-gpu30`) — a document outside this manifest, reviewed separately in this run.

---

### INFO (deployed: ON) — privileges, mounts, volumes, and the consolidation: structural in-box exposure strictly reduced, no new reader, no dangling reference

- **All four proxies:** `privileged: true`, `user: root` (`x-vllm-proxy-common`, `:32-43`), `ipc: host` via `x-nvidia` (`:13-15`). Each mounts `dstack.sock` and `certs:ro` (`:39-41`). Root-equivalent E2EE/OHTTP terminators — structural, not a new reader. OHTTP forced on all four (`:353`, `:445`, `:513`, `:581`).
- **`ipc: host` node-wide** (`:15`, inherited by every engine and proxy): one shared IPC namespace across four model families' plaintext — permitted though not configured.
- **`dcgm-exporter` x4** (`x-dcgm-common`, `:54-60`): tag-only `4.5.2-4.8.1-distroless` (`:55`) with `SYS_ADMIN` (`:57-58`) + nvidia runtime — mutable-tag bytes can drift under the attestation; kept in scope for privilege. Exports GPU counters, not content; the DS4F instance now spans GPUs 0-3 (`:677`).
- **Volumes** (`:828-835`): `hugginface_cache`, `kernel_cache`, `vllm_cache` (+ `VLLM_CACHE_ROOT`, `:258`), `otelcol_app_storage` (`:809`, collector send-queue), `certs` (external). **No KV/prompt/request disk tier; no dump directory.** Unchanged by the diff; no request-derived data lands on any volume at deployed flags.
- **No `build:` blocks** (grep-verified). The only code-execution-at-start remains the Gemma tile80 patch (`:234-237`, body `:842-856`): rewrites one FA4 tile constant in-place, NON-FATAL (`:853-856`), no I/O beyond the one file, no egress — privacy-neutral. The model-downloader (`:266-283`) fetches weights only, at a pinned revision (`:275`).
- **Named outbound destinations, exhaustively:** proxies → `https://cloud-api.near.ai` (literal, `:348` etc.) + `http://compose-manager:8080` (`:350`, host-gateway `:37-38`) + `${WEB_CONTEXT_SEARCH_URL}` (unset default); registrar → `https://completions.near.ai` (`:1319`) registering the four model domains (`:1438-1441`), health-probing with a **synthetic** `"hi"` message (`:1375` — not user content); engines → HF hub (Qwen/Gemma lazily, DeepSeek pre-fetched) and the two Qwen3.6 engines' unguarded media fetch (MEDIUM above); Gemma → the one literal S3 media domain (`:251`); otelcol-contrib → `https://telemetry.infra.near.ai` (`:1274`). No `internal: true`, no egress allowlist (`:823-826`) — **every engine can open arbitrary outbound connections [MEDIUM]**.
- **Consolidation hygiene:** the removed arm3 trio (engine, proxy, dcgm), ports 8004/8005, `TLS_PORT_2`, `routing_port_for` and `check_tls_route` leave **zero dangling references** — grep for `arm3` / `8004` / `8005` / `TLS_PORT_2` / `tp2` returns nothing. The `HF_TOKEN` line the diff removes was a duplicate of the anchor env (`:120`); secret exposure is unchanged in kind.

---

## Verdict

**QUALIFIED PASS at this layer.** The delta itself is privacy-neutral-to-reducing: the deployed command/env changes are performance-shaped (`--tp-size 4`, `--chunked-prefill-size 8192`, the `SGLANG_OPT_FLASHMLA_SPARSE_PREFILL=0` kernel toggle) with no logging/dump/debug switch anywhere in the diff; the arm consolidation removes an engine, a proxy, a dcgm exporter, two host ports and a TLS listener with no dangling references; the registrar still carries only health/status; and the new A/B tags are deployment metadata whose every asserted value matches the deployed flag. The qualifications are the base page's, re-verified here, in descending order of cost:

1. **Both Qwen3.6 engines remain multimodal with no media allowlist and redirects on**, beside a Gemma-4 that constrains both — the per-node containment gap is untouched by this revision (MEDIUM, deployed ON).
2. **Harness-C3** — everything any of these services prints leaves the TEE via the recipe's own otelcol-contrib to `telemetry.infra.near.ai`, unredacted, on every node (plus Datadog on datadog-bearing nodes); the posture is conditional on per-image print discipline plus no 300 s / 1800 s hang given the watchdog-dump HIGH on the engine pages (HIGH, deployed ON).
3. **Both Qwen3.6 engines run `--trust-remote-code` with no `--revision`** — HF-head code execution inside the enclave with no attestation-visible trace, disarmed only by the currently-observed HF state (LOW, ARMED).
4. The unauthenticated engine `/configure_logging` remains reachable from the flat network through the allowlist-free `location /` proxies/nginx — a smaller surface (8 blocks, 5 host ports) but the same class (MEDIUM, deployed ON).
5. The `/compose/up` splice — documented in this file's own comments — can enable `DSV4_RETURN_HIDDEN_STATES` and widen `VLLM_ALLOWED_MEDIA_DOMAINS` with no measurement change (MEDIUM, deployed OFF).

Recommend, in-file (unchanged from the base page): pin `--revision` on both Qwen3.6 engines; front the engines with a path allowlist; add a redaction/scrub processor to the otelcol `logs/app` pipeline; and either `--disable-multimodal` on the Qwen3.6 engines or a network egress allowlist.

## Not traced (load-bearing)

- Whether this `file_sha256` matches the signed action log's current entry live (identity taken from the orchestrator's capture pinned at commit `6aabc6d36b04` and re-hashed locally against both the captured file and the git content at `60cdbdf05d84`; the action log itself was not re-fetched in this review).
- The gpu30 sparse-ON control file `prod/qwen35-dsv4-flash.yaml` — its presence and arm tags were spot-verified at the pinned commit, but its content is outside this document (own page, reviewed separately in this run).
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source).
- Engine print discipline and exact `/configure_logging` / watchdog behavior at the configured levels — deferred to the engine pages ([`984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md), [`ec518148`](/images/docker.io/nearaidev/sglang/sha256-ec518148762ea02c23aa8615f69ca79b0c18bcd59b3c21c10229db3df323c615.md)), which are load-bearing under the telemetry finding — including whether the `ec518148` engine page's PRIVATE-at-deployed-flags conclusion holds at the new TP4/chunk-8192/sparse-off flag combination (the flags are within the set that page audited, but the combination was not separately re-run).
- Proxy no-allowlist forwarding and web-search fail-closed behavior — deferred to the [proxy page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md).
- What `--enable-return-hidden-states` exposes end-to-end when spliced on (inference-proxy + cloud-api source, outside this repo's scope).
- Whether the warm HF cache shields the unpinned Qwen3.6 engines from head drift in practice (runtime state, not config).
- `dcgm-exporter` bytes under its mutable tag; the measured management harness (compose-manager, datadog-agent presence per node) — referenced from the measured layer, not re-derived here.
