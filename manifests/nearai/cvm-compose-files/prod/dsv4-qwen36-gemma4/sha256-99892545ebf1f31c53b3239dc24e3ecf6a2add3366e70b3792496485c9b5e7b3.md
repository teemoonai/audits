# cvm-compose-files prod/dsv4-qwen36-gemma4.yaml @ 99892545... (four-model inner compose — DeepSeek-V4-Flash TP4 arm flips sparse-prefill ON onto the audited nightly engine + Qwen3.6-27B + Qwen3.6-35B-A3B + Gemma-4-31B)

## verdict: QUALIFIED PASS on logging config — the sparse-prefill flip is a clean four-class delta (engine image swap to the published-PRIVATE nightly 5bc4bc0dfd36, SGLANG_OPT_FLASHMLA_SPARSE_PREFILL 0->1, two new literal kernel/dtype env selectors that the engine review traced to no log/dump/path sink, and datadog/otel label churn whose every asserted value matches the deployed command/env) with no command-flag, mount, volume, port, or interpolation change anywhere in the diff; but every base qualification carries: two multimodal Qwen3.6 engines with no media allowlist, unredacted double stdout shipping, both Qwen3.6 engines --trust-remote-code with no --revision, unauthenticated engine admin behind allowlist-free proxies, and the documented /compose/up env-splice channel

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/dsv4-qwen36-gemma4.yaml` |
| **file_sha256** | `99892545ebf1f31c53b3239dc24e3ecf6a2add3366e70b3792496485c9b5e7b3` (re-verified locally: `shasum -a 256` of the captured 1550-line file matches, and `git show c3140816ffa0:prod/dsv4-qwen36-gemma4.yaml` hashes to the same value; the base bytes `git show 60cdbdf05d84:prod/dsv4-qwen36-gemma4.yaml` re-hash to `52ef39d9…` as their page states) |
| **role** | log-pinned inner compose for the four-model combined node (gpu04): `deepseek-ai/DeepSeek-V4-Flash-0731` (SGLang FP4, single TP4 replica, GPUs 0-3, now **sparse-prefill ON on the near-built nightly `5bc4bc0dfd36…`** — the cross-host sparse-on/off A/B is concluded; gpu04's serving settings now match gpu30's, per the header comment `:8-11` and the pin commit title), `Qwen/Qwen3.6-27B-FP8` (SGLang TP1, GPU 4), `Qwen/Qwen3.6-35B-A3B-FP8` (SGLang TP1, GPU 5), `google/gemma-4-31B-it` (vLLM, 2x TP1 replicas, GPUs 6-7) — behind four `vllm-proxy-rs` instances, nginx, registrar, four dcgm exporters, otelcol-contrib. Serves the `qwen3-6-27b` / `qwen3-6-35b` / `gemma-4-31b` / DS4F endpoints per the fleet capture of 2026-08-21 |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. Pinned at commit `c3140816ffa0` ("DS4F: align gpu04 serving config with gpu30", 2026-08-21), fetched and hash-checked against the log's `file_sha256`; the action-log match was not re-fetched live in this review |
| **review** | reviewed 2026-08-21 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** delta review of the full 1550-line compose against the audited base revision. Per-image behavior is deferred to the published image pages, linked per finding: DeepSeek engine [`nearaidev/sglang@5bc4bc0d`](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md) (**PRIVATE at the deployed flags** — reviewed this same run: upstream nightly `c0b6474b` plus two hash-verified privacy-neutral patches), both Qwen3.6 engines [`lmsysorg/sglang@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (v0.5.16, PRIVATE at deployed flags), Gemma engine [`vllm/vllm-openai@960ac5b3`](/images/docker.io/vllm/vllm-openai/sha256-960ac5b3fda0547c3ce64d8f69bae9fa242d5ae5382fc74a98cd8ef13950e403.md), and the shared proxy [`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (all four proxies run this one digest, `:33`). The identical sparse-on-nightly DS4F arm was independently reviewed in the sibling recipe [`prod/qwen35-dsv4-flash.yaml @ cb190fcb`](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-cb190fcba6b49ca2dd5092ed59e61cf1c26370aac50dc78416f8b50492858695.md) — its "unreviewed nightly engine" HIGH does **not** apply here, because the engine page exists at this page's review time.

**This is a DELTA review** against the [base page (`52ef39d9`)](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen36-gemma4/sha256-52ef39d9746c0c4ac961c881c75efa2893257d2a10cb247f9af364302299a658.md) (verdict: QUALIFIED PASS on logging config). Each base finding was re-checked at this file, not assumed. The old→new diff (commits `60cdbdf05d84` → `c3140816ffa0`) was independently regenerated with `git diff` — +42/−38 lines, file 1546 → 1550 — and both revisions plus the captured file were re-hashed locally. The diff reaches this endpoint through two intermediate committed states ("DS4F: disable overlap on sparse-off arm" `8255de5`, "Fix DS4F containment telemetry labels" `da9f34c`); those intermediate file revisions were **not** individually audited — the net diff covers their combined effect (see Not traced). The delta map is below.

---

## Delta summary (base `52ef39d9` → this `99892545`)

| Change in the diff | Privacy assessment at `99892545` |
|---|---|
| **Engine image swap** (`:76`): DS4F anchor moves from `nearaidev/sglang@ec518148…` (audited, PRIVATE) to `nearaidev/sglang@sha256:5bc4bc0dfd36…`, the near-built **nightly** (anchor comment `:72-75`: upstream overlap-scheduler WAR-fence alignment from sgl-project/sglang#33587; DSPARK disabled). Zero residual references to `ec518148` (grep-verified). | **This arm's plaintext now runs on bytes with a published review**: the [`5bc4bc0d` engine page](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md) verdict is PRIVATE at the deployed flags — upstream `c0b6474b` plus two recovered, hash-verified patches (stream-abort + admission-reserve), neither adding any log/disk/egress of content. Its **residual posture carries into this file's findings below**: unauthenticated `/configure_logging` (that page's CRITICAL, deployed ON — re-verified at the nightly's refactored route/auth code), the undisableable watchdog token-ID dump (HIGH, ARMED behind `--watchdog-timeout 1800`, `:104`), and stdout shipping (this page's HIGH). The command block is untouched by the swap: same `--log-requests-level 0` (`:113`), same `--revision 7872f01b…` pin (`:90`). |
| **Sparse-prefill flip + two new env literals** on the DS4F anchor: `SGLANG_OPT_FLASHMLA_SPARSE_PREFILL` 0 → 1 (`:136`; the base's "partial bypass" caveat comment is deleted with the =0 setting), plus `SGLANG_DSV4_COMPRESS_STATE_DTYPE=bf16` (`:130`, priced by its comment `:129` as +10% KV tokens) and `SGLANG_OPT_USE_TOPK_V2=0` (`:133`, crash containment per comments `:131-132`). | **No new flag or env names a logging/dump/debug switch, and none is path-like.** All three are literals, not operator interpolations. The engine page traced all three to their `environ.py` declarations in the kernel/indexer block (code defaults `float32`/`True`/`True` — so sparse-on matches the engine default) and confirmed **no new disk artifact or sink at the config level**; the compressed state is in-GPU attention/KV state, and no new volume is mounted (`:830-837`, unchanged). Kernel internals remain untraced on that page (carried under Not traced). |
| **Telemetry label churn**: every DS4F A/B tag updated to match — proxy datadog label (`:365`) and otel labels (`:380-381`); engine datadog labels (`:399-400`) and otel labels (`config_variant` `:417`, `sparse_prefill` `:419`, `ab_arm` `:421`, new `compress_state_dtype` `:425`, `sglang_revision` `:431`, `engine_image` `:432`); dcgm labels (`:683-684`, `:699-701`); Prometheus static labels (`:965-980`, `:1072-1074`, `:1143-1144`). | **Deployment metadata only, and truthful** — every value re-checked against the deployed command/env (INFO finding below). No request-derived value appears in any label. Log pipeline scope unchanged: same filelog receiver (`:869-872`), same `filter/app_logs` keep/drop polarity (`:1241-1250`), same single exporter `https://telemetry.infra.near.ai` (`:1277-1280`, `num_consumers: 1` retained `:1282`). |
| **Comment-only changes**: header layout comment (`:8-11` — now "settings match gpu30's service", the A/B framing removed), the anchor rationale (`:72-75`), and the DS4F section comment (`:341`; the old three-line consolidation note deleted). | No config effect. The A/B experiment is concluded — both hosts now run the same sparse-on arm (the sibling gpu30 page documents the identical flags); `ab_arm:…-gpu04` remains a truthful static arm identifier for this node. |
| **Nothing else** — a diff fact: no `command:` line changes anywhere (grep of the diff for flag lines: zero), no mount/volume/port/network change, no new interpolated variable, the Qwen3.6 and Gemma service blocks byte-untouched. | The four base qualifications and every carried finding below were re-located and re-cited at this file's line numbers; none retired, none escalated. |

---

## Env reachability and the splice channel

Every logging knob in this file is a literal, not an operator-set env: `--log-requests-level 0` on all three SGLang commands (`:113`, `:170`, `:209` — the minimum), `VLLM_LOGGING_LEVEL=INFO` on every engine (`:49`, `:122`, `:260`), `LOG_FORMAT=json` on all four proxies (`:351`, `:444`, `:512`, `:580`), `NCCL_DEBUG=WARN` (`:52`, `:125`, `:266`), otelcol `telemetry.logs.level: info` (`:1317-1318`). The three new engine env vars are literals too (`:130`, `:133`, `:136`). **`OHTTP_ENABLED=true` is a literal on all four proxies** (`:353`, `:446`, `:514`, `:582`) — E2EE posture not operator-degradable. **No `--api-key`/`--admin-api-key` anywhere** (grep-verified) — engines are unauthenticated on the flat network.

Interpolated vars reachable only via compose-manager's non-measured `/compose/up` splice (not the measured `allowed_envs` channel; the measured harness for this node is referenced, not re-derived here): `DSV4_RETURN_HIDDEN_STATES` (`:115`), `VLLM_ALLOWED_MEDIA_DOMAINS` (`:254`), `HF_HUB_OFFLINE` (defaulted `0`, `:48`/`:121`/`:259`), `HUGGING_FACE_HUB_TOKEN` (`:47`/`:120`/`:258`/`:283`), `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (four proxies), `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (four proxies), `CLOUD_API_USAGE_TOKEN` (`:349` etc.), `PROXY_TOKEN`, `MODEL_PROXY_TOKEN` (`:331-332`), `MONITORING_INGEST_TOKEN` (`:800`). The interpolated set is unchanged by this delta.

---

### HIGH (deployed: ON) — every plaintext service's stdout leaves the CVM via the recipe's own otelcol-contrib, unredacted, to near.ai's telemetry gateway; on datadog-bearing nodes a measured agent ships it again to a third-party SaaS — sink: telemetry / egress

Carried from the base page; re-traced end to end at this file's line numbers, unchanged in structure. Every app service logs via the `json-file` driver (`x-logging-conf`, `:1-6`), stamping `com.datadoghq.ad.logs` and `com.docker.compose.service` onto each container's log labels. The recipe runs its own `otelcol-contrib` (`:794`, digest-pinned), whose `logs/app` pipeline (`:1294-1297`) is:

```
receivers:  [filelog/docker_containers]           # tails /var/lib/docker/containers/*/*-json.log  (:869-870)
processors: [memory_limiter, resource,
             transform/docker_metadata,           # lifts service.name from the datadog label     (:1234-1235)
             filter/app_logs,                      # KEEP records with model/deployment attrs      (:1241)
             groupbyattrs/docker_service, batch]
exporters:  [otlphttp/gateway]                     # https://telemetry.infra.near.ai               (:1278)
```

The filelog receiver's `regex_parser` operators lift `model:` (`:898-900`) and `deployment:` (`:910-912`) out of the `com.datadoghq.ad.logs` label into attributes. `filter/app_logs` (`:1241-1250`) **drops** only records where `model == nil and deployment == nil` (`:1245`) or whose `service.name` is a management service, and **keeps** everything else. Every engine, proxy, nginx, registrar, and dcgm container carries a `model:`/`deployment:` tag, so its stdout is exported verbatim to `https://telemetry.infra.near.ai` with `Authorization: Bearer ${MONITORING_INGEST_TOKEN}` (`:1278-1280`). **There is no scrub/hash/truncate processor in this pipeline** — the "no content leaves" guarantee still reduces exactly to "no plaintext service ever prints content", carried by the per-image source pages. This delta strengthens that condition's evidence on the DS4F arm: print discipline at the deployed flags is now a **published verification for the actual nightly bytes** ([`5bc4bc0d`](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md)), whose unauthenticated-`/configure_logging` CRITICAL and 1800 s watchdog token-ID-dump HIGH are exactly the events that would put plaintext on this pipeline. The label churn adds only static resource attributes; no pipeline or exporter setting changed.

- **[MEDIUM]** otelcol-contrib's OTLP receivers on `0.0.0.0:4317/4318` (`:865-867`, `expose`d `:806-808`) feed `metrics/otlp` and `traces` pipelines (`:1302-1305`, `:1310-1313`) forwarding **verbatim** to near.ai. Any prompt-bearing span injected on the flat network ships unexamined.
- **[INFO]** All metrics paths are Prometheus scrapes of `/metrics` — counters/histograms with routing labels only; the updated `ab_arm`/`sparse_prefill`/`compress_state_dtype` dimensions (`:965-980`, `:1072-1074`, `:1143-1144`) are static deployment metadata, not content.

---

### MEDIUM (deployed: ON) — two Qwen3.6 engines are multimodal at v0.5.16 and fetch request-supplied media URLs with no domain allowlist, while sibling Gemma-4 in the same file constrains both controls — sink: egress (request-driven)

Carried from the base page (its headline caveat), re-checked: **byte-untouched by this delta** (the diff never enters either Qwen3.6 service block). Both Qwen3.6 services still run the v0.5.16 image at the same digest `984699c2` (`:147`, `:186`) — the [engine page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) verified both models multimodal at that version — and both commands still pass `--mm-feature-transport cpu` (`:157`, `:196`). sglang has no media-allowlist mechanism; Gemma-4 beside them sets `--allowed-media-domains` (`:254`, splice-widenable default) and `VLLM_MEDIA_URL_ALLOW_REDIRECTS=0` (`:263`, hard literal). The bytes carried outbound are the requesting client's own URL — an SSRF / confirmation channel / `file://` local-read primitive from a plaintext process, not cross-tenant prompt exfiltration. Containment options unchanged: `--disable-multimodal` or a network egress allowlist (there is none — the network is the flat `external: true` `dstack_default`, `:825-828`, no `internal: true`). Neither Qwen3.6 service pins `--revision`, so the `config.json` driving multimodality reads from mutable `main` — compounding the ARMED finding below. (The DS4F arm remains text-only: the engine page re-verified `DeepseekV4ForCausalLM` is not multimodal at the nightly.)

---

### MEDIUM (deployed: OFF) — a documented /compose/up env-splice channel can enable hidden-state passthrough and widen the media allowlist with no attestation-visible change — sink: response / egress

Carried, re-confirmed at this file. The DS4F anchor comment (`:77-85`) states `DSV4_RETURN_HIDDEN_STATES` is "spliced into /compose/up like GPT_OSS_CACHE_SUBPATH" — in-file confirmation of a second env channel bypassing the measured `allowed_envs` with no measurement change. The `${VAR:+…}` form (`:115`) still has the documented footgun (any non-empty value, `"0"` included, turns the flag ON — `:83` says so explicitly). Default OFF (flag absent when unset). The same channel can set `VLLM_ALLOWED_MEDIA_DOMAINS` (`:254`). Neither var surfaces in the hardware measurement. Untouched by the diff.

- **[MEDIUM] `WEB_CONTEXT_SEARCH_URL` + `_API_KEY` on all four plaintext-path proxies** (`:361-362`, `:453-454`, `:521-522`, `:589-590`) — an unpinned external URL handed to the E2EE-terminating services. The deployed proxy build was source-verified fail-closed when unset, opt-in per request, sending only a model-generated query ([`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md), all four proxies run this one digest, `:33`). Residual: operator-suppliable via the splice channel.

---

### MEDIUM (deployed: ON) — the engines' unauthenticated admin surface is reachable through allowlist-free nginx and proxies on the flat network — sink: log / egress

Carried, unchanged in shape by this delta — but its DS4F half is now anchored to a published page for the running bytes: the [`5bc4bc0d` engine review](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md) re-verified at the nightly's **refactored** route/auth code that `/configure_logging` remains `ADMIN_OPTIONAL`, the auth middleware is never installed without a key, and one unauthenticated POST enables verbatim prompt+output logging and arbitrary-path request pickling (its CRITICAL, deployed ON). This file supplies the reachability: the flat `external: true` bridge `dstack_default` (`:825-828`) lets any co-resident container reach engine port 8000 directly, and nginx (digest-pinned `:289`) publishes plaintext HTTP host ports 8000-8003 plus TLS 8444→443 (`:297-302`), with **every one of its 8 server blocks routing a bare `location /`** (`:1484`, `:1490`, `:1496`, `:1502`, `:1521`, `:1530`, `:1539`, `:1549`) — no path allowlist, so each proxy's full HTTP surface (and through the allowlist-free proxy design, each engine's) is exposed on every listener. In-file mitigations unchanged: upstreams are always proxies, never raw engines; `proxy_buffering off; proxy_cache off` (`:1475-1476`); the access log uses `$uri` not `$request` (`:1452-1465`, `:1467`) — no bodies, though `request_id`/`org_id`/`workspace_id` (`:1454-1456`) are per-request user-attributable metadata the telemetry pipeline ships off-box **[INFO]**. Fix unchanged: `--admin-api-key` on the engines (none anywhere in this file).

---

### LOW (deployed: ARMED) — both Qwen3.6 engines run --trust-remote-code with no --revision pin, unlike every other model in this file — sink: potential in-enclave code execution

Carried, re-checked, untouched by the diff. Pinning, service by service:

| service (anchor) | image | `--revision` | `--trust-remote-code` |
|---|---|---|---|
| DeepSeek TP4 (`:62-139`) | `nearaidev/sglang@5bc4bc0d` (`:76`) — audited | `7872f01b…` (`:90`) | yes — pinned (`:88`) |
| Qwen3.6-27B | `lmsysorg/sglang:v0.5.16@984699c2` (`:147`) | **NONE** | **yes — UNPINNED** (`:169`) |
| Qwen3.6-35B-A3B | same digest `984699c2` (`:186`) | **NONE** | **yes — UNPINNED** (`:208`) |
| Gemma-4 x2 | `vllm/vllm-openai@960ac5b3` (`:221`) | `9a9994e6…` (`:246`) | yes — pinned (`:251`) |

`HF_HUB_OFFLINE` defaults `0` (`:48`), so a cache miss reaches the network; the model-downloader (`:269-286`) pre-fetches only DeepSeek at a pinned revision (`:278`). At the currently-observed HF state there is nothing to execute (engine page), so this is ARMED, not ON: a later push to `main` would execute attacker-chosen Python inside a plaintext-handling enclave process with no trace in the attestation chain. Fix belongs in this file: add `--revision <sha>` to both.

---

### INFO (deployed: ON) — the updated A/B labels carry only deployment metadata, every asserted value matches the deployed command/env, and the engine-identity tags are now backed by a published page

The two questions the base page asked of the A/B machinery, re-asked of the flipped tag sets; both answered from the file.

**(a) Do the updated `ab_arm:tp4-target-only-sparse-on-gpu04` / `sparse_prefill:on` / `compress_state_dtype:bf16` tag sets carry request- or user-derived data?** No. The tag sets on the proxy (`:365`, `:380-381`), engine (`:399-400`, `:417-432`), dcgm (`:683-684`, `:699-701`) and Prometheus configs (`:965-980`, `:1072-1074`, `:1143-1144`) are static launch parameters and arm/build identifiers only; none is derived from a request.

**(b) Do the labels match the deployed command/env?** Yes, every one checked: `sparse_prefill:on` vs `SGLANG_OPT_FLASHMLA_SPARSE_PREFILL=1` (`:136` — and unlike the base's "off" claim, "on" needs no partial-bypass caveat, since ON is the kernel's default-dispatch state); `compress_state_dtype:bf16` vs `:130`; `config_variant:dsv4-0731-tp4-target-only-sparse-on` and `ab_arm:tp4-…-sparse-on-gpu04` vs `--tp-size 4` (`:91`) + the sparse env; `chunk_size:8192` vs `--chunked-prefill-size 8192` (`:101`); `max_prefill_tokens:16384` vs `:102`; `watchdog_timeout:1800` vs `:104`; `max_running_requests:64` / `max_queued_requests:16` / `schedule_conservativeness:1.3` / `continuous_decode_steps:1` vs `:96-99`; `chunked_prefill_admission_reserve:4096` vs env `:127`; `cuda_graph:1` vs `--cuda-graph-max-bs 64` (`:94`); `model_revision` vs `:90`; `engine_image:5bc4bc0dfd36` vs the image digest (`:76`). `sglang_revision:nightly-c0b6474-near-v1` asserts a source identity this manifest cannot prove — but the [engine page](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md) binds the digest to upstream `c0b6474b` **plus two out-of-tree patches** (hash-verified), and calls this near-internal build string the honest description (a bare `c0b6474b` pin would over-assert). Two caveats carried from the sibling review: the published tuning profile is **incomplete, not false** — `SGLANG_OPT_USE_TOPK_V2=0` (`:133`) appears in **no** label (grep-verified), so the telemetry backend sees the sparse arm but not the TopK containment; and the two Qwen3.6 engines pass no `--watchdog-timeout` (300 s default at v0.5.16; engine-page HIGH applies) and assert no watchdog label — no truthfulness disagreement. **The A/B is concluded, not asymmetric:** with this revision both gpu04 and gpu30 run the identical sparse-on arm (pin commit "DS4F: align gpu04 serving config with gpu30"; the [gpu30 sibling page](/manifests/nearai/cvm-compose-files/prod/qwen35-dsv4-flash/sha256-cb190fcba6b49ca2dd5092ed59e61cf1c26370aac50dc78416f8b50492858695.md) documents the same flags and env — closing that page's untraced cross-host matched-pair claim from this side), so the base page's cross-host-control residual is retired.

---

### INFO (deployed: ON) — privileges, mounts, volumes, and topology: byte-identical to the audited base; the delta adds no reader, sink, or surface

- **All four proxies:** `privileged: true`, `user: root` (`x-vllm-proxy-common`, `:32-43`), `ipc: host` via `x-nvidia` (`:13-15`). Each mounts `dstack.sock` (`:40`) and `certs:ro` (`:41`). Root-equivalent E2EE/OHTTP terminators — structural, not a new reader. OHTTP forced on all four (`:353`, `:446`, `:514`, `:582`).
- **`ipc: host` node-wide** (`:15`, inherited by every engine and proxy): one shared IPC namespace across four model families' plaintext — permitted though not configured.
- **`dcgm-exporter` x4** (`x-dcgm-common`, `:54-60`): tag-only `4.5.2-4.8.1-distroless` (`:55`) with `SYS_ADMIN` (`:57-58`) + nvidia runtime — mutable-tag bytes can drift under the attestation; kept in scope for privilege. Exports GPU counters, not content.
- **Volumes** (`:830-837`): `hugginface_cache`, `kernel_cache`, `vllm_cache` (+ `VLLM_CACHE_ROOT`, `:261`), `otelcol_app_storage` (`:811`, collector send-queue), `certs` (external). **No KV/prompt/request disk tier; no dump directory. Unchanged by the diff** — the engine page confirmed the new sparse/compress flags mount nothing and name no path; no request-derived data lands on any volume at deployed flags.
- **No `build:` blocks** (grep-verified). The only code-execution-at-start remains the Gemma tile80 patch (`:237-240`, body `:844-858`): rewrites one FA4 tile constant in-place, NON-FATAL (`:855-858`), no I/O beyond the one file, no egress — privacy-neutral. The model-downloader (`:269-286`, digest-pinned uv image `:270`) fetches weights only, at a pinned revision (`:278`).
- **Named outbound destinations, exhaustively:** proxies → `https://cloud-api.near.ai` (literal, `:348` etc.) + `http://compose-manager:8080` (`:350`, host-gateway `:38`) + `${WEB_CONTEXT_SEARCH_URL}` (unset default); registrar (`:314`, digest-pinned curl image `:315`) → `https://completions.near.ai` (`:1323`) registering the four model domains (`:1442-1445`), health-probing with a **synthetic** `"hi"` message (`:1379` — not user content); registration payloads are endpoint+routing_port (`:1342`), endpoint (`:1349`), model+domain (`:1356`); port loops span 8000-8003 (`:1332`, `:1361`, `:1419`) and `register_endpoint` always dials `TLS_PORT` (`:1429`). Engines → HF hub (Qwen/Gemma lazily, DeepSeek pre-fetched) and the two Qwen3.6 engines' unguarded media fetch (MEDIUM above); Gemma → the one literal S3 media domain (`:254`); otelcol-contrib → `https://telemetry.infra.near.ai` (`:1278`). No `internal: true`, no egress allowlist (`:825-828`) — **every engine can open arbitrary outbound connections [MEDIUM]**.
- **Delta hygiene:** the engine swap left zero references to `ec518148` and the diff contains no service, port, mount, volume, network, or interpolation change (diff fact) — the structural surface of this file is byte-identical to the audited base outside the DS4F anchor's image/env lines and the label sets.

---

## Verdict

**QUALIFIED PASS at this layer.** The delta is exactly the four change-classes claimed, independently re-derived from the pinned commits: an engine-image swap onto a nightly build **whose source review is published and PRIVATE at these flags**, a sparse-prefill kernel flip, two new literal kernel/dtype env selectors that the engine review traced to no log/dump/path sink, and label churn whose every asserted value matches the deployed command/env — with no command-flag, mount, volume, port, or interpolation change anywhere in the diff. The DS4F arm now matches gpu30's byte-for-byte at the flag/env level, ending the cross-host A/B. The qualifications are the base page's, re-verified here, in descending order of cost:

1. **Both Qwen3.6 engines remain multimodal with no media allowlist and redirects on**, beside a Gemma-4 that constrains both — the per-node containment gap is untouched by this revision (MEDIUM, deployed ON).
2. **Harness-C3** — everything any of these services prints leaves the TEE via the recipe's own otelcol-contrib to `telemetry.infra.near.ai`, unredacted, on every node (plus Datadog on datadog-bearing nodes); the posture is conditional on per-image print discipline plus no 300 s / 1800 s hang given the watchdog-dump HIGHs on the engine pages — now including the published `5bc4bc0d` page for the arm this delta moves (HIGH, deployed ON).
3. **Both Qwen3.6 engines run `--trust-remote-code` with no `--revision`** — HF-head code execution inside the enclave with no attestation-visible trace, disarmed only by the currently-observed HF state (LOW, ARMED).
4. The unauthenticated engine `/configure_logging` — re-verified at the nightly's refactored auth code on the engine page — remains reachable from the flat network through the allowlist-free `location /` proxies/nginx (MEDIUM, deployed ON).
5. The `/compose/up` splice — documented in this file's own comments — can enable `DSV4_RETURN_HIDDEN_STATES` and widen `VLLM_ALLOWED_MEDIA_DOMAINS` with no measurement change (MEDIUM, deployed OFF).

Recommend, in-file (unchanged from the base page): pin `--revision` on both Qwen3.6 engines; front the engines with a path allowlist (or better, `--admin-api-key` on every engine); add a redaction/scrub processor to the otelcol `logs/app` pipeline; and either `--disable-multimodal` on the Qwen3.6 engines or a network egress allowlist.

## Not traced (load-bearing)

- Whether this `file_sha256` matches the signed action log's current entry live (identity taken from the orchestrator's capture pinned at commit `c3140816ffa0` and re-hashed locally against both the captured file and the git content; the action log itself was not re-fetched in this review).
- The two intermediate committed revisions between the audited base and this file (`8255de5` — which briefly put the sparse-OFF arm on the nightly with `--disable-overlap-schedule` — and `da9f34c`): not individually audited; the net diff covers their combined effect, but whether either intermediate state was ever deployed is a question for the action log, not this page.
- Engine print discipline and exact `/configure_logging` / watchdog behavior at the configured levels — deferred to the engine pages ([`5bc4bc0d`](/images/docker.io/nearaidev/sglang/sha256-5bc4bc0dfd3629af7e57168098b5e76b4206dcb02b8ed8a872aebfd153ff240b.md), [`984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md)), which are load-bearing under the telemetry finding. Carried from the `5bc4bc0d` page: the FlashMLA sparse-prefill / compressed-state **kernel internals** were not read there either — traced to env declarations and the absence of any new mounted path; a config-keyed autotune artifact inside the existing `kernel_cache` JIT root cannot be fully excluded, but no request-content sink is possible without a new disk path, and none is mounted.
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source).
- Proxy no-allowlist forwarding and web-search fail-closed behavior — deferred to the [proxy page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md).
- What `--enable-return-hidden-states` exposes end-to-end when spliced on (inference-proxy + cloud-api source, outside this repo's scope).
- Whether the warm HF cache shields the unpinned Qwen3.6 engines from head drift in practice (runtime state, not config).
- `dcgm-exporter` bytes under its mutable tag; the measured management harness (compose-manager, datadog-agent presence per node) — referenced from the measured layer, not re-derived here.
