# cvm-compose-files prod/dsv4-qwen36-gemma4.yaml @ c6ef1271... (four-model inner compose — DeepSeek-V4-Flash-0731 on vLLM DP4+EP + Qwen3.6-27B + Qwen3.6-35B-A3B + Gemma-4-31B)

## verdict: QUALIFIED PASS on logging config — no content-logging switch anywhere, OHTTP forced on all four proxies, the A/B arm machinery and the hidden-state splice lever are gone, and the DeepSeek engine now pins --revision; but the DeepSeek move to vLLM 0.26 adds an ARMED NCCL hang-dump path (flight-recorder trace to container /tmp, desync report to shipped stderr) and a 1M-token context whose request bodies nginx spools to a container-local temp file, plus the carried residuals: two Qwen3.6 multimodal engines with no media allowlist beside a constrained Gemma, two Qwen3.6 engines --trust-remote-code with no --revision, unredacted stdout shipping to telemetry.infra.near.ai with the Datadog label path still wired, unauthenticated sglang admin behind allowlist-free proxies, and a proxy build (e665ebb9) whose behavior is reviewed concurrently

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/dsv4-qwen36-gemma4.yaml` |
| **file_sha256** | `c6ef1271ffdaed7e936b271d371da944644d63f0c277fe84e29582f334119fec` (re-verified locally: `shasum -a 256` of the captured 1526-line file matches; the 1777-line base file re-hashes to `795aab85…` as well) |
| **role** | log-pinned inner compose for the four-model combined node: `deepseek-ai/DeepSeek-V4-Flash-0731` (**vLLM 0.26 DP4+TP1+EP, one service, GPUs 0-3, 1M context** — was SGLang TP2 x 2 A/B arms), `Qwen/Qwen3.6-27B-FP8` (SGLang TP1, GPU 4), `Qwen/Qwen3.6-35B-A3B-FP8` (SGLang TP1, GPU 5), `google/gemma-4-31B-it` (vLLM, 2x TP1 replicas, GPUs 6-7) — behind **four** `vllm-proxy-rs` instances (was five), nginx, registrar, four dcgm exporters, otelcol-contrib |
| **identity** | log-pinned: keyed by `file_sha256` against the signed compose-manager action log — **not** hardware-measured. The log pins `compose_up` of this path at compose-manager commit `83c34077aa7ffe500bc08894a52b44e05ae4af86` (tag `v0.0.356`, applied 2026-08-25T17:32Z, after a `v0.0.359` / `0b56064e` revision was up for ~35 minutes and rolled back); the orchestrator fetched the file at that commit and hash-checked it (match). The measured harness on these hosts is `compose_hash` `9385918d…`, which has its own page under `manifests/measured/`. The action-log match was not re-fetched live in this review. |
| **live** | 4 hosts per the orchestrator's 2026-09-01 capture: gemma-4-31b, qwen3-30b, qwen3-6-27b, qwen3-6-35b (one multi-model node type; placement is the orchestrator's capture, not re-derived here) |
| **review** | reviewed 2026-09-01 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** the full 1526-line compose: four engine anchors across five model containers, four proxy configs, the Gemma tile80 startup patch, the model-downloader, nginx routing, the registrar script, four dcgm exporters, and otelcol-contrib end to end. The complete 685-line base -> this diff was read in three chunks and is enumerated below. Per-image behavior is deferred to the published image pages: Gemma engine [`vllm/vllm-openai@960ac5b3`](/images/docker.io/vllm/vllm-openai/sha256-960ac5b3fda0547c3ce64d8f69bae9fa242d5ae5382fc74a98cd8ef13950e403.md), both Qwen3.6 engines [`lmsysorg/sglang@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md), three proxies on [`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md). **Two images are new in this revision and reviewed concurrently in this same run — their verdicts are not assumed here:** the DeepSeek engine [`vllm/vllm-openai@770fe65b2c73`](/images/docker.io/vllm/vllm-openai/sha256-770fe65b2c73ee74a5c42165cf3433de4048cc2cd9c57a937ca4e35aba5aa87b.md) (identity: `vllm-project/vllm` @ `ffd46bfa`, v0.26.0 line) and the DeepSeek proxy [`vllm-proxy-rs@e665ebb9`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-e665ebb9998fcf333a1611ecb4c49bd209b90aa9e09547ed792bd0436b132f30.md) (identity: signed attestation, `nearai/inference-proxy` @ `101aa49f`).

**This is a DELTA review** against the [base page (`795aab85`)](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen36-gemma4/sha256-795aab8598653d4f74e3826d662d35d0040a71bca6e6256e5e3646bd5738d5c8.md) (verdict: QUALIFIED PASS — two Qwen3.6 multimodal engines without allowlist, unpinned Qwen3.6 `--trust-remote-code`, double stdout shipping, unauthenticated admin behind allowlist-free proxies, `/compose/up` splice). Each base finding was re-read at this file with new line numbers, not carried.

---

## The complete diff, grouped (base `795aab85` -> this `c6ef1271`, +107/-358 over 35 upstream commits)

1. **DeepSeek engine swap** (`x-dsv4-flash-common`, `:62-121`): `nearaidev/sglang@ec518148` `sglang serve --tp-size 2 ...` -> `vllm/vllm-openai@770fe65b2c73` (`:72`) with a bare-positional vLLM command (`:73-95`). Gone from the command: `--watchdog-timeout 1800`, `--log-requests-level 0`, `--enable-cache-report`, `--enable-metrics`, `--json-model-override-args`, and `${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}`. New: `--data-parallel-size 4` / `--tensor-parallel-size 1` / `--enable-expert-parallel` (`:80-82`), `--max-model-len 1048576` (`:89`), `--enable-prefix-caching` (`:93`), `--kv-cache-dtype fp8` (`:78`), `--compilation-config` FULL_DECODE_ONLY (`:88`). `--revision 7872f01b…` (`:75`) and `--trust-remote-code` (`:77`) both kept. Volume mount `kernel_cache:/root/.cache/deep_gemm` -> `kernel_cache:/root/.cache/vllm` (`:98`). Env: four `SGLANG_*` vars dropped; added `VLLM_ENGINE_READY_TIMEOUT_S=1800`, `VLLM_USE_BREAKABLE_CUDAGRAPH=0`, `VLLM_SKIP_P2P_CHECK=1`, `NCCL_CUMEM_ENABLE=1`, `TORCH_NCCL_TRACE_BUFFER_SIZE=500000`, `TORCH_NCCL_DUMP_ON_TIMEOUT=1`, `TORCH_NCCL_DESYNC_DEBUG=1`, `TORCH_NCCL_ENABLE_TIMING=1`, `VLLM_CACHE_ROOT=/root/.cache/vllm/dsv4-0731-vllm026-dp4-fdo`, `OMP_NUM_THREADS=4` (`:108-118`). Header comment rewritten (`:65-71`), file-level layout comment added (`:8-11`).
2. **A/B arm removal**: services `proxy-dsv4-flash-arm3`, `model-sg-dsv4-flash-fp4-tp2-arm3`, `dcgm-dsv4-flash-arm3` deleted; `model-sg-dsv4-flash-fp4-tp2` renamed `model-vllm-dsv4-flash-fp4-dp4` on GPUs 0-3 (`:367-413`, devices `:378`); `dcgm-dsv4-flash` widened to GPUs 0-3 (`:659`). nginx `depends_on` arm3 removed, host ports `8004`/`8005` removed (`:279-284`), the `:8004` plain and `:8005` TLS server blocks removed (nginx conf now `:1426-1526`). Registrar: `TLS_PORT_2` env removed (`:310-314`), port loops now `8000 8001 8002 8003`, `routing_port_for` / `check_tls_route` helpers removed, `register_endpoint` always passes `${TLS_PORT}`. Prometheus scrape jobs for arm3 engine/dcgm/proxy removed.
3. **Tag set rewrite**: the dense sglang tag set (`chunk_size`, `ab_arm:arm2-…`, `watchdog_timeout:1800`, `sglang_revision`, etc.) replaced on the engine's `com.datadoghq.ad.logs`/`ad.instances` (`:383-384`), `nearai.otel.*` (`:385-413`) and the Prometheus job (`:925-957`) by `config_variant:dsv4-0731-vllm026-dp4-ep-fdo`, `parallelism`, `cuda_graph_mode`, `kv_cache_dtype`, `block_size`, `prefix_caching`, `max_model_len`, `max_num_seqs`, `max_num_batched_tokens`, `vllm_revision:0.26.0`, `engine_image:770fe65b2c73`, `ab_arm:vllm026-dp4-ep-fdo-gpu04`. `ab_arm` + `engine:vllm` added to the DeepSeek proxy (`:349`, `:364-365`) and dcgm (`:664-665`, `:680-681`) labels and their scrape jobs.
4. **DeepSeek proxy**: `image:` override to `vllm-proxy-rs@e665ebb9` (`:327`); `VLLM_BASE_URL` -> `http://model-vllm-dsv4-flash-fp4-dp4:8000` (`:340`); new `VLLM_DATA_PARALLEL_SIZE=4` (`:341`); `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS=20` (`:347`, previously only on the deleted arm3 proxy).
5. **otelcol**: one line — `sending_queue.num_consumers: 1` added to `otlphttp/gateway` (`:1258`). Receivers, processors, filter polarity, exporter endpoint, and pipelines are byte-identical to the base.

Nothing else changed: the Qwen3.6 anchors, the Gemma anchor and patch, the three other proxies, nginx logging/buffering directives, and the filelog/filter/pipeline config are untouched (verified by the diff hunks, which contain no other lines).

## Delta summary — every base-page finding re-checked here

| Base-page finding | Status at `c6ef1271` |
|---|---|
| HIGH: every plaintext service's stdout leaves via otelcol-contrib, unredacted; Datadog label path alongside | **CONFIRMED** — pipeline unchanged (`logs/app` `:1270-1273`, exporter `:1253-1260`, no scrub stage `:1181-1250`); `com.datadoghq.ad.logs` still stamped by the log driver (`:6`) on every app service. Now four engine codebases' stdout instead of six containers'. |
| MEDIUM: two Qwen3.6 engines multimodal with no media allowlist beside constrained Gemma | **CONFIRMED** — `--mm-feature-transport cpu` (`:139`, `:178`); Gemma `--allowed-media-domains` (`:236`) + `VLLM_MEDIA_URL_ALLOW_REDIRECTS=0` (`:245`). Unchanged. |
| MEDIUM (OFF): `/compose/up` splice can enable hidden-state passthrough and widen media allowlist | **CHANGED — narrowed.** `${DSV4_RETURN_HIDDEN_STATES:+…}` is **GONE** (grep-verified absent; the vLLM command `:73-95` has no such flag and the comment `:70` says so). `VLLM_ALLOWED_MEDIA_DOMAINS` (`:236`) and `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (four proxies) remain splice-reachable. |
| MEDIUM: unauthenticated engine admin reachable through allowlist-free nginx/proxies | **CHANGED — one fewer sglang engine.** Both Qwen3.6 sglang engines still carry it; the DeepSeek engine is now vLLM (its admin/reconfiguration surface is deferred to the concurrent `770fe65b` page, not assumed absent). nginx still bare `location /` on every block — now **8** blocks (`:1460`, `:1466`, `:1472`, `:1478`, `:1497`, `:1506`, `:1515`, `:1525`), was 10. |
| LOW (ARMED): both Qwen3.6 `--trust-remote-code` with no `--revision` | **CONFIRMED** (`:151`, `:190`; no `--revision` in `:130-153` / `:169-192`). DeepSeek keeps `--revision` on its new engine (`:75`) — the asymmetry is still exactly the two Qwen services. |
| No content-logging switch; `--log-requests-level 0` on every sglang command | **CHANGED** — `:152`, `:191` on the two remaining sglang commands. The DeepSeek sglang command (and its `--log-requests-level 0`) is GONE; the replacement vLLM command carries **no request-logging flag of any kind** (grep for `log-requests`, `enable-log-outputs`, `max-log-len` returns nothing), and `VLLM_LOGGING_LEVEL=INFO` (`:102`). |
| OHTTP forced on all five proxies | **CONFIRMED on four** — `OHTTP_ENABLED=true` literal (`:336`, `:427`, `:495`, `:563`); the fifth (arm3) proxy is GONE. |
| Watchdog: only DeepSeek asserted `watchdog_timeout:1800`, and truthfully; Qwen3.6 set none | **GONE.** No service in this file passes `--watchdog-timeout` and no label asserts one (both grep-verified). Qwen3.6 unchanged (sglang default per the engine page). The DeepSeek hang path is now vLLM's — see the new NCCL finding. |
| INFO: A/B tag machinery carries deployment metadata only | **CHANGED, still clean** — single arm `ab_arm:vllm026-dp4-ep-fdo-gpu04` + `engine:vllm`; every new tag value is a static launch parameter or build id (`:383-384`, `:401-413`). |
| INFO: privileges, mounts, volumes, Gemma patch, named egress | **CONFIRMED** with new lines (below). |
| — | **NEW LOW (ARMED)**: `TORCH_NCCL_DUMP_ON_TIMEOUT=1` + `TORCH_NCCL_DESYNC_DEBUG=1` on the DeepSeek engine. |
| — | **NEW LOW (ON)**: 1M-token context + 100 MB request cap behind nginx `client_body_buffer_size 1m` with no `proxy_request_buffering off`. |
| — | **NEW INFO**: DeepSeek proxy on a different proxy build than its three siblings; documentation drift on the canary comment; shared `kernel_cache` volume now mounted at two different paths by three engines. |

---

## Env reachability and the splice channel

Every logging knob in this file is a literal: `--log-requests-level 0` on both sglang commands (`:152`, `:191`), `VLLM_LOGGING_LEVEL=INFO` on every engine (`:49` via `*sglang-env`, `:102`, `:242`), `LOG_FORMAT=json` on all four proxies (`:334`, `:425`, `:493`, `:561`), `NCCL_DEBUG=WARN` (`:52`, `:105`, `:248`), otelcol `telemetry.logs.level: info` (`:1294`). **`OHTTP_ENABLED=true` is a literal on all four proxies** (`:336`, `:427`, `:495`, `:563`). **No `--api-key` / admin key anywhere** (grep-verified) — engines are unauthenticated on the flat network.

Interpolated vars reachable only via compose-manager's non-measured `/compose/up` splice (the measured `allowed_envs` belongs to the harness page, not re-derived here): `VLLM_ALLOWED_MEDIA_DOMAINS` (`:236`), `HF_HUB_OFFLINE` (defaulted `0`: `:48`, `:101`, `:241`), `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` (`:345-346`, `:434-435`, `:502-503`, `:570-571`), `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (four proxies), `CLOUD_API_USAGE_TOKEN`, `PROXY_TOKEN`, `MODEL_PROXY_TOKEN` (`:313-314`), `HUGGING_FACE_HUB_TOKEN` (`:47`, `:100`, `:240`, `:265`), `MONITORING_INGEST_TOKEN` (`:780`). `DSV4_RETURN_HIDDEN_STATES` is no longer referenced anywhere in the file.

---

### HIGH (deployed: ON) — every plaintext service's stdout leaves the CVM via the recipe's own otelcol-contrib, unredacted, to near.ai's telemetry gateway, with the Datadog label path still wired alongside — sink: telemetry / egress

Re-traced end to end at this file; the pipeline is byte-identical to the base except one queue tuning line. Every app service logs via the `json-file` driver (`x-logging-conf`, `:1-6`), which stamps `com.datadoghq.ad.logs` and `com.docker.compose.service` onto each container's log labels (`:6`). The recipe's own `otelcol-contrib` (`:773-803`, digest-pinned `:774`) mounts `/var/lib/docker/containers:ro` (`:790`) and its `logs/app` pipeline (`:1270-1273`) is:

```
receivers:  [filelog/docker_containers]           # tails /var/lib/docker/containers/*/*-json.log  (:849-850)
processors: [memory_limiter, resource,
             transform/docker_metadata,           # lifts service.name from the datadog label     (:1205-1214)
             filter/app_logs,                      # DROPS only nil-model/nil-deployment + mgmt     (:1217-1226)
             groupbyattrs/docker_service, batch]
exporters:  [otlphttp/gateway]                     # https://telemetry.infra.near.ai                (:1254)
```

The filelog `regex_parser` operators lift `model:` (`:878-880`) and `deployment:` (`:890-892`) out of the `com.datadoghq.ad.logs` label into attributes; `filter/app_logs` drops records where `model == nil and deployment == nil` (`:1221`) or whose `service.name` is one of five management services (`:1222-1226`), and keeps everything else. Every engine, proxy, nginx, registrar, and dcgm container here carries `model:`+`deployment:` tags, so its stdout is exported verbatim with `Authorization: Bearer ${MONITORING_INGEST_TOKEN}` (`:1256`), spooled through `file_storage` at `/var/lib/otelcol-contrib/storage` on the `otelcol_app_storage` volume (`:791`, `:1263-1265`, `:1260`) — so **shipped stdout also persists on a named volume until delivered**. **There is no scrub/hash/truncate processor** (`:1181-1250`). The Datadog label path is not removed by this revision: the `com.datadoghq.ad.logs` / `ad.instances` labels remain on every service (e.g. `:383-384`), so on datadog-bearing nodes the measured `datadog-agent` (harness page, not this file) can still collect the same stdout a second time.

What this means at this revision: the "no content leaves" guarantee reduces to "no plaintext service ever prints content", carried by **four** engine codebases' pages — and now, on the DeepSeek service, additionally by vLLM's behavior on NCCL timeout (`TORCH_NCCL_DESYNC_DEBUG=1`, `:114`, prints its report to the shipped stream — see the ARMED finding below).

- **[MEDIUM]** otelcol-contrib's OTLP receivers on `0.0.0.0:4317/4318` (`:842-847`, `expose`d `:786-788`) feed `metrics/otlp` (`:1278-1281`) and `traces` (`:1286-1289`) pipelines that forward **verbatim** to near.ai with no filter. Any prompt-bearing span injected on the flat network ships unexamined.
- **[INFO]** All metrics paths are Prometheus scrapes of `/metrics` (`prometheus/apps` `:922`, `prometheus/self` `:1173`) — counters/histograms with routing labels only; the new vLLM tag set (`:925-957`) is static launch parameters.

---

### MEDIUM (deployed: ON) — two Qwen3.6 engines are multimodal at sglang v0.5.16 and fetch request-supplied media URLs with no domain allowlist, while sibling Gemma-4 in the same file constrains both controls — sink: egress (request-driven)

Carried, unchanged, re-read. Both Qwen3.6 commands pass `--mm-feature-transport cpu` (`:139`, `:178`) on `lmsysorg/sglang:v0.5.16` (`:129`, `:168`), and the [engine page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) verified both models are multimodal at that version with no allowlist mechanism. Sibling asymmetry in this file:

| service | media allowlist | redirects |
|---|---|---|
| Gemma-4 (vLLM, `:201-249`) | `--allowed-media-domains ${VLLM_ALLOWED_MEDIA_DOMAINS:-prod-files-secure.s3.us-west-2.amazonaws.com}` (`:236`) | `VLLM_MEDIA_URL_ALLOW_REDIRECTS=0` (`:245`) |
| Qwen3.6-27B (sglang, `:123-160`) | **NONE** | followed by default |
| Qwen3.6-35B-A3B (sglang, `:162-199`) | **NONE** | followed by default |
| DeepSeek-V4-Flash (vLLM, `:62-121`) | no `--allowed-media-domains` and no `VLLM_MEDIA_URL_ALLOW_REDIRECTS` set — **but only material if the model is multimodal**, which this document cannot establish; deferred to the concurrent `770fe65b` page | — |

The DeepSeek row is a new observation: the vLLM anchor sets neither of the two controls Gemma sets in the same file. If the model or engine accepts media inputs, the containment gap widens to a third engine; if it is text-only, it is moot. Not asserted either way here. The network still has no `internal: true` and no egress allowlist (`:805-808`).

---

### MEDIUM (deployed: ON) — the sglang engines' unauthenticated admin surface is reachable through allowlist-free nginx and proxies on the flat network — sink: log / egress

Both Qwen3.6 sglang engines expose the unauthenticated `/configure_logging` endpoint (CRITICAL on the engine page — one POST turns on verbatim prompt+output logging and arbitrary-path request pickling). This file supplies the reachability: flat `external: true` bridge `dstack_default` (`:805-808`) — any co-resident container reaches engine port 8000 directly; nginx (digest-pinned `:271`, config `:1426-1526`) publishes plaintext HTTP host ports 8000-8003 plus TLS 8444->443 (`:279-284`), and **every one of its 8 server blocks routes with a bare `location /`** (`:1460`, `:1466`, `:1472`, `:1478`, `:1497`, `:1506`, `:1515`, `:1525`) — no path allowlist. In-file mitigations: upstreams are always proxies, never raw engines; `proxy_buffering off; proxy_cache off` (`:1451-1452`, no response bodies to disk); the access log format uses `$uri` not `$request` (`:1428-1441`, `:1443`) — no bodies, though `request_id`/`org_id`/`workspace_id` are per-request user-attributable metadata the telemetry pipeline ships off-box **[INFO]**. The DeepSeek engine is no longer sglang; whatever runtime-reconfiguration surface vLLM 0.26 exposes is the concurrent `770fe65b` page's question, not assumed here.

---

### MEDIUM (deployed: OFF) — the documented /compose/up env-splice channel can still widen the Gemma media allowlist and supply the web-search URL, with no attestation-visible change; the hidden-state lever is gone — sink: egress

**CHANGED — narrowed.** The base's headline lever, `${DSV4_RETURN_HIDDEN_STATES:+--enable-return-hidden-states}`, is removed with the sglang command; the new header comment states "This candidate does not expose SGLang's optional hidden-state passthrough" (`:70`) and the vLLM command (`:73-95`) contains no equivalent flag — comment and command agree. What remains splice-reachable: `VLLM_ALLOWED_MEDIA_DOMAINS` (`:236`, widens Gemma's allowlist; `VLLM_MEDIA_URL_ALLOW_REDIRECTS=0` at `:245` is a hard literal) and `WEB_CONTEXT_SEARCH_URL` + `_API_KEY` on all four plaintext-path proxies (`:345-346`, `:434-435`, `:502-503`, `:570-571`) — an unpinned external URL handed to the E2EE-terminating services. Three proxies run the source-verified fail-closed build [`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md) (`:33`); the DeepSeek proxy runs `e665ebb9` (`:327`), whose web-search behavior is reviewed concurrently — not carried from the b183677a page. Deployed OFF (unset defaults); neither var is in the measured env, so neither change would surface in the hardware measurement.

---

### LOW (deployed: ARMED) — both Qwen3.6 engines run --trust-remote-code with no --revision pin, unlike every other model in this file — sink: potential in-enclave code execution

Carried, re-read. Pinning, service by service:

| service (anchor) | image | `--revision` | `--trust-remote-code` |
|---|---|---|---|
| DeepSeek-V4-Flash DP4 (`:62-121`) | `vllm/vllm-openai@770fe65b` | `7872f01b…` (`:75`) | yes (`:77`) — pinned |
| Qwen3.6-27B (`:123-160`) | `lmsysorg/sglang:v0.5.16@984699c2` | **NONE** | **yes — UNPINNED** (`:151`) |
| Qwen3.6-35B-A3B (`:162-199`) | same digest `984699c2` | **NONE** | **yes — UNPINNED** (`:190`) |
| Gemma-4 x2 (`:201-249`) | `vllm/vllm-openai@960ac5b3` | `9a9994e6…` (`:228`) | yes (`:233`) — pinned |

`HF_HUB_OFFLINE` defaults `0` (`:48`) so a cache miss reaches the network; the model-downloader (`:251-268`) pre-fetches only DeepSeek at a pinned revision (`:260`); Qwen/Gemma are pulled lazily by the engines. ARMED not ON: the engine page verified nothing executes at the observed HF `main`; a later push would run attacker-chosen Python inside a plaintext-handling enclave with no trace in the attestation chain. Fix belongs in this file: add `--revision <sha>` to both, as DeepSeek and Gemma do.

---

### LOW (deployed: ARMED) — the DeepSeek vLLM engine sets TORCH_NCCL_DUMP_ON_TIMEOUT=1 and TORCH_NCCL_DESYNC_DEBUG=1: an NCCL hang writes a flight-recorder trace to container-local /tmp and a desync report to the shipped stderr — sink: disk / log

**NEW.** The new env block (`:112-115`) sets `TORCH_NCCL_TRACE_BUFFER_SIZE=500000`, `TORCH_NCCL_DUMP_ON_TIMEOUT=1`, `TORCH_NCCL_DESYNC_DEBUG=1`, `TORCH_NCCL_ENABLE_TIMING=1` on the only DP4 engine in the file. What this document establishes:

- **Where a dump would land.** `TORCH_NCCL_DEBUG_INFO_TEMP_FILE` is **not** set (grep-verified), so PyTorch's documented default location (`/tmp/nccl_trace_rank_<rank>`) applies. `/tmp` is not on any declared volume: the engine mounts only `hugginface_cache` (`:97`) and `kernel_cache` at `/root/.cache/vllm` (`:98`). A dump therefore lands on the container's ephemeral overlay filesystem — gone on recreate, but readable by anything with the container's filesystem while it lives, and persisted across `restart: unless-stopped` restarts of the same container.
- **Addendum (2026-09-02, orchestrator verification).** The default location above was taken from PyTorch documentation. Checked at the torch the image pins (`requirements/cuda.txt:7` at `ffd46bfa` -> `torch==2.11.0`): `torch/csrc/distributed/c10d/FlightRecorder.cpp:53-66` at `v2.11.0` derives the default from `XDG_CACHE_HOME`, else `$HOME/.cache`, as `<cache>/torch/comm_lib_trace_rank_<rank>`, overridable by `TORCH_FR_DUMP_TEMP_FILE` or `TORCH_NCCL_DEBUG_INFO_TEMP_FILE`. In this container that is `/root/.cache/torch/comm_lib_trace_rank_<rank>`, which is NOT under either declared mount (`/root/.cache/huggingface` at `:97`, `/root/.cache/vllm` at `:98`), so the conclusion is unchanged: container-local overlay, no volume. Also at `v2.11.0`, `ProcessGroupNCCL.cpp:1716` defaults `TORCH_NCCL_DUMP_ON_TIMEOUT` to true, so the deployed `=1` makes explicit what the library already does. The concurrent engine page (`/images/docker.io/vllm/vllm-openai/sha256-770fe65b2c73ee74a5c42165cf3433de4048cc2cd9c57a937ca4e35aba5aa87b.md`) states the same.
- **What a dump contains.** By PyTorch's documentation the flight recorder records collective metadata (op type, sizes, sequence numbers, stack traces, timing) — not tensor payloads. That is a documentation claim, not a source verification, and vLLM's own behavior when a DP rank times out (what it prints, whether it dumps engine state) is the concurrent `770fe65b` page's question. `TORCH_NCCL_DESYNC_DEBUG=1` prints its desync analysis to the process's stderr, which the HIGH finding above ships to `telemetry.infra.near.ai` unredacted.
- **Deployed state.** ARMED: nothing is written at steady state; a collective timeout (the file's own comment `:106-107` records first-start profiling exceeding a 600 s default) is the trigger. Contrast with the base: the sglang `--watchdog-timeout 1800` hang-dump (HIGH on that engine page) is GONE with the engine; this is its vLLM-side successor and is rated by what the document shows lands where, pending the engine page.

---

### LOW (deployed: ON) — the 1M-token DeepSeek context and 100 MB request cap put request bodies through nginx's client-body spool to a container-local temp file; OHTTP ciphertext if OHTTP is in use — sink: disk

**NEW.** `--max-model-len 1048576` (`:89`) with `VLLM_PROXY_MAX_REQUEST_SIZE=104857600` (`:342`, DeepSeek proxy only) and nginx `client_max_body_size 100m` / `client_body_buffer_size 1m` (`:1454-1455`). nginx's `proxy_request_buffering` is not set (grep-verified) and defaults to on, so any request body over 1 MB — routine at a 1M context — is written to nginx's client-body temp directory on the nginx container's overlay filesystem before being forwarded. No volume is mounted there (nginx mounts only `certs:ro`, `:286`, and the config). The base's `proxy_buffering off; proxy_cache off` (`:1451-1452`) covers responses only. Why LOW not HIGH: nginx sits in front of the OHTTP-terminating proxies (`OHTTP_ENABLED=true`, `:336`), so a body from an OHTTP client is encapsulated ciphertext at this hop. Whether the proxy also accepts non-OHTTP plaintext-over-TLS requests — which would make the spool a plaintext sink — is a proxy-image question (b183677a page for three proxies; concurrent e665ebb9 page for this one) and is listed under Not traced. In-file fix: `proxy_request_buffering off;` in the nginx config (`:1445-1455` block).

---

### INFO (deployed: ON) — DeepSeek moved to vLLM 0.26 DP4+EP: every logging/dump/disk/egress-touching flag and env enumerated; --revision pinned; no request-logging flag; VLLM_CACHE_ROOT on the persistent, now cross-engine-shared kernel_cache volume; prefix cache is GPU-resident at these flags

The full launch surface of `x-dsv4-flash-common` (`:62-121`), classified:

| item | line | class | assessment |
|---|---|---|---|
| `--revision 7872f01b…` | `:75` | identity | **pinned** — matches the downloader (`:260`) and the label `model_revision` (`:383`) |
| `--trust-remote-code` | `:77` | code exec | pinned by `--revision`; same posture as Gemma |
| `--enable-prefix-caching` | `:93` | cache | vLLM prefix cache lives in GPU KV memory; no `--kv-transfer-config`, KV connector, or offload flag is present (grep-verified), so no disk or network KV tier is configured. Cross-request sharing is in-process only. |
| `--max-model-len 1048576` | `:89` | capacity | drives the nginx spool finding above |
| `VLLM_LOGGING_LEVEL=INFO` | `:102` | log | not DEBUG; content at INFO is the engine page's question |
| no `--enable-log-requests` / `--max-log-len` / `--enable-log-outputs` | — | log | **absent** (grep-verified) — vLLM's request-content logging is not switched on by this file |
| `HF_TOKEN=${HUGGING_FACE_HUB_TOKEN}`, `HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}` | `:100-101` | egress | HF hub reachable by default; DeepSeek is pre-fetched pinned, so at steady state this is a cache hit |
| `VLLM_CACHE_ROOT=/root/.cache/vllm/dsv4-0731-vllm026-dp4-fdo` | `:117` | disk | inside `kernel_cache` (`:98`) — persistent compiled-kernel / cudagraph artifacts keyed by config; not request data |
| `TORCH_NCCL_*` x4 | `:112-115` | disk / log | ARMED finding above |
| `NCCL_DEBUG=WARN`, `NCCL_CUMEM_ENABLE=1`, `VLLM_SKIP_P2P_CHECK=1`, `VLLM_USE_BREAKABLE_CUDAGRAPH=0`, `VLLM_ENGINE_READY_TIMEOUT_S=1800`, `OMP_NUM_THREADS=4`, `PYTORCH_CUDA_ALLOC_CONF` | `:105`, `:108-111`, `:116`, `:118` | perf | no logging, disk, or egress semantics |

**Shared volume, new topology:** `kernel_cache` is mounted at `/root/.cache/vllm` by the DeepSeek engine (`:98`) and at `/root/.cache/deep_gemm` by both Qwen3.6 engines (`:156`, `:195`) — one writable volume, three engines, two path conventions. Gemma uses a separate `vllm_cache` (`:27`). Compiled-kernel artifacts only at deployed flags; a cross-model shared writable volume is noted as the one place a future dump path on any of the three would be visible to the other two.

---

### INFO (deployed: ON) — proxy-dsv4-flash runs proxy build e665ebb9 while the other three stay on b183677a; env compared line by line, behavior deferred to the concurrent page

The DeepSeek proxy overrides the anchor image (`:327`) — the only proxy not on `b183677a` (`:33`). Env block comparison, all four proxies (`:329-347`, `:420-435`, `:488-503`, `:556-571`): 14 entries are identical in name and value pattern (`NVIDIA_VISIBLE_DEVICES=all`, `CLOUD_API_URL=https://cloud-api.near.ai`, `CLOUD_API_USAGE_TOKEN`, `COMPOSE_MANAGER_URL=http://compose-manager:8080`, `LOG_FORMAT=json`, `MODEL_NAME`, `OHTTP_ENABLED=true`, `TOKEN`, `FUSION_ENABLED`, `FUSION_INTERNAL_BEARER_TOKEN`, `TLS_CERT_PATH`, `USE_NV_ATTESTATION_SDK=true`, `WEB_CONTEXT_SEARCH_URL`, `WEB_CONTEXT_SEARCH_API_KEY`). Differences: DeepSeek adds `VLLM_DATA_PARALLEL_SIZE=4` (`:341`, new to the file), `VLLM_PROXY_MAX_REQUEST_SIZE=104857600` (`:342`, carried), `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS=20` (`:347`, carried from the deleted arm3 proxy); Gemma uses `VLLM_BACKEND_URLS=<r1>,<r2>` (`:567`) instead of `VLLM_BASE_URL`. None of the three DeepSeek-only vars names a log level, log body, or destination. What `VLLM_DATA_PARALLEL_SIZE` and the stream idle timeout do — in particular whether the proxy buffers or logs anything on idle-timeout — is [reviewed concurrently — see that page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-e665ebb9998fcf333a1611ecb4c49bd209b90aa9e09547ed792bd0436b132f30.md). Structural: all four proxies remain `privileged: true`, `user: root` (`:34-35`), `ipc: host` (`:15`), mounting `dstack.sock` (`:40`) and `certs:ro` (`:41`).

---

### INFO (deployed: ON) — the A/B arm removal: what left, what the single remaining arm tag carries, and that no sglang watchdog is set or claimed anywhere now

Removed with the arms: the second DeepSeek engine, proxy and dcgm (`*-arm3`), host ports 8004/8005 and their nginx blocks, the `TLS_PORT_2` second TLS listener and the registrar's `routing_port_for`/`check_tls_route` logic, three Prometheus jobs, and the whole sglang tag vocabulary (`chunk_size`, `watchdog_timeout`, `schedule_conservativeness`, `sglang_revision`, `ab_arm:arm2/arm3…`). What remains is one `ab_arm:vllm026-dp4-ep-fdo-gpu04` + `engine:vllm` pair on the DeepSeek proxy (`:349`, `:364-365`), engine (`:383-384`, `:411`) and dcgm (`:664-665`, `:680-681`) and their scrape jobs (`:925-957`) — checked value by value: static launch parameters and build identifiers, nothing request-derived. **Watchdog:** no `--watchdog-timeout` on any command and no `watchdog_timeout` label anywhere (both grep-verified) — there is no label claim to test for truthfulness. The two Qwen3.6 sglang engines run at the engine default (the engine page's 300 s hang-dump HIGH applies to them unchanged); the DeepSeek hang path is now the vLLM/NCCL one above. Registrar (`:1296-1424`): registers four model domains (`:1418-1421`) at `https://completions.near.ai` (`:1299`) and health-probes with a **synthetic** `"hi"` message (`:1355`) — not user content.

---

### INFO (deployed: ON) — documentation drift: the anchor comment says GPU04 A/B canary only, the action log has it on four hosts

`:65`: "GPU04 A/B CANARY ONLY: do not expand before the one-host 24-hour gate", and `:68-69`: "the 1M production context cap below still requires an 850K/1M staging qualification". The orchestrator's capture places this exact `file_sha256` on four hosts. Recorded as documentation drift, not a privacy finding: the comment is not a control, and the deployed flags are what was reviewed. It does mean the file's own gating narrative should not be relied on as evidence of the rollout state.

---

### INFO (deployed: ON) — privileges, mounts, volumes, the Gemma patch, and named egress destinations: structural in-box exposure, no new reader

- **`ipc: host` node-wide** (`:15`, inherited by every engine and proxy via `*nvidia`): one shared IPC namespace across four model families' plaintext.
- **`dcgm-exporter` x4** (`:54-60`): tag-only `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (`:55`) with `SYS_ADMIN` (`:58`) — mutable-tag bytes can drift with no attestation-visible trace; GPU counters only at this config. The DeepSeek exporter now scopes NVML to GPUs 0-3 (`:659`).
- **Volumes** (`:810-816`): `hugginface_cache` (weights), `kernel_cache` (shared, above), `vllm_cache` (Gemma), `otelcol_app_storage` (collector send-queue — shipped stdout at rest until delivered), `certs` (external). **No KV/prompt/request disk tier, no responses store, no declared dump/profiler directory** — the two new disk paths (NCCL dump, nginx spool) are container-ephemeral, not volumes.
- **No `build:` blocks.** The only in-container code-at-start is the Gemma `gemma4_tile80_patch` (`:219-222`, body `:824-838`): rewrites one `FwdConfig` tile constant in place, non-fatal, prints only its own status line (`:834`, `:836`, `:838`) — no content path.
- **Named outbound destinations, exhaustively:** proxies -> `https://cloud-api.near.ai` (`:331`, `:422`, `:490`, `:558`) + `http://compose-manager:8080` (`:333` etc., host-gateway `:38`) + `${WEB_CONTEXT_SEARCH_URL}` (unset default); registrar -> `https://completions.near.ai` (`:1299`); engines -> HF hub (DeepSeek pre-fetched pinned `:260`; Qwen/Gemma lazily) and the two Qwen3.6 engines' unguarded media fetch (MEDIUM above); Gemma -> the one literal S3 media domain (`:236`); otelcol-contrib -> `https://telemetry.infra.near.ai` (`:1254`). The network has no `internal: true` and no egress allowlist (`:805-808`) — **every engine can open arbitrary outbound connections [MEDIUM]**.

---

## Verdict

**QUALIFIED PASS at this layer.** The file hands no logging switch to any engine or proxy, forces OHTTP on all four proxies, keeps every telemetry destination literal, drops the A/B arm machinery and the hidden-state splice lever cleanly, and pins `--revision` on the DeepSeek engine it just replaced. The qualifications, in descending order of cost to the end-to-end claim:

1. **Harness-C3** — everything any of these services prints leaves the TEE via the recipe's own otelcol-contrib to `telemetry.infra.near.ai`, unredacted, with the Datadog label path still in place for datadog-bearing nodes; the posture now rests on four engine codebases' print discipline, two of which (`770fe65b`, `e665ebb9`) are reviewed concurrently and not assumed (HIGH, ON).
2. **Two Qwen3.6 engines fetch request-supplied media with no allowlist** beside a Gemma that constrains it; the new DeepSeek vLLM anchor sets neither control either, material only if that model takes media (MEDIUM, ON).
3. **The sglang `/configure_logging` surface** stays reachable through bare `location /` nginx/proxies for the two Qwen3.6 engines (MEDIUM, ON).
4. **The `/compose/up` splice** can still widen the Gemma allowlist and supply the web-search URL; the hidden-state lever is gone (MEDIUM, OFF).
5. **Both Qwen3.6 engines `--trust-remote-code` with no `--revision`** (LOW, ARMED).
6. **New with the vLLM move:** an NCCL-timeout dump to container `/tmp` plus a desync report on shipped stderr (LOW, ARMED), and 1M-context request bodies spooled by nginx to a container-local temp file, ciphertext under OHTTP (LOW, ON).

Recommend, in-file: pin `--revision` on both Qwen3.6 engines; add `proxy_request_buffering off;` to the nginx config; set `TORCH_NCCL_DEBUG_INFO_TEMP_FILE` to a deliberately chosen, non-persistent path or drop `TORCH_NCCL_DUMP_ON_TIMEOUT` outside bring-up; front the engines with a path allowlist; add a redaction processor to `logs/app`; either `--disable-multimodal` on the Qwen3.6 engines or add a network egress allowlist; and fix the canary comment to match the rollout.

## Not traced (load-bearing)

- Whether this `file_sha256` matches the signed action log's current entry live — the orchestrator's fetch-and-hash at `83c34077` is relied on; not re-fetched here.
- **The DeepSeek engine `vllm/vllm-openai@770fe65b` (v0.26.0 line)** — reviewed concurrently; everything this page says about vLLM print discipline at INFO, behavior on NCCL/DP-rank timeout (what is written where), any runtime-reconfiguration endpoint, whether `DeepSeek-V4-Flash-0731` on this engine accepts media inputs, and what `--enable-prefix-caching` shares across tenants is that page's, not this one's.
- **The DeepSeek proxy `vllm-proxy-rs@e665ebb9`** — reviewed concurrently; what `VLLM_DATA_PARALLEL_SIZE=4` and `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS=20` do, whether an idle-timeout logs or buffers request content, and whether web-search remains fail-closed on this build.
- Whether the proxies accept non-OHTTP plaintext-over-TLS requests (which would make the nginx client-body spool a plaintext disk sink rather than a ciphertext one) — proxy pages.
- PyTorch flight-recorder contents and default dump path are taken from PyTorch's documentation, not verified from source in this document review.
- Whether `/compose/up` env splices surface in the signed action log (compose-manager source).
- Engine print discipline and exact `/configure_logging` / hang-dump behavior for the two Qwen3.6 sglang engines — the [`984699c2` page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md), load-bearing under the telemetry finding.
- Whether the warm HF cache shields the unpinned Qwen3.6 engines from head drift in practice (runtime state, not config).
- `dcgm-exporter` bytes under its mutable tag; the measured management harness (`compose_hash` `9385918d…`: compose-manager, datadog-agent presence per node) — referenced from the measured layer, not re-derived here.
