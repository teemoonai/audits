# cvm-compose-files prod/small-models.yaml @ a03816ba... (delta review — gpt-oss-120b out, a temporary Qwen3.8-27B sglang stack with its own nginx and registrar in, proxy bumped on all nine services; every prior finding carries forward)

## verdict: LEAKS — the FLUX diffusion service this file launches still logs every user prompt at INFO and writes every generated image to disk, both unconditionally, and this file's otelcol pipeline still ships that prompt log to telemetry.infra.near.ai with no redaction stage; the delta since cb9040a3 retires gpt-oss-120b cleanly, adds a Qwen3.8-27B sglang engine on the audited febfb971 nightly (minimum request-log level, pinned revision beside --trust-remote-code, no dump/file/trace flag, but no watchdog pin and multimodal left on with no media allowlist — the file now has two allowlist-free media-ingesting engines and three sglang LLM engines at the 300 s watchdog default) behind a second nginx that publishes host ports 8000/8001 with the same bare location / and a second registrar, bumps every proxy to the 98b57ad7 build (one proxy also sets a 100 MB request cap; no proxy sets the new affinity or idle-timeout keys), strips the Datadog OpenMetrics labels fleet-wide while keeping the log labels, and widens the privacy-filter to a 131072-token single pass with four new init-time stderr lines; the otelcol receivers, processors and exporter are byte-identical to the base, so the four new services' stdout joins the same unredacted off-box stream; the action log shows the engine containers were last created from earlier revisions (Qwen3.8 stack from da2ad1fa, privacy-filter from 3bc66577 which predates its 128k change, all other engines from cb9040a3) and only the proxies, both nginx and both registrars run from this exact file

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/small-models.yaml` |
| **file_sha256** | `a03816ba0c737f1ed438bd595bbd6fba6ed1657bff4283c02655e06f1729df60` (re-verified locally: `shasum -a 256` of the captured 2686-line file ✓; `git show e1410790:prod/small-models.yaml` from the compose-repo clone re-hashes to the same value ✓; the captured base file re-hashes to `cb9040a3…` per its own page) |
| **role** | log-pinned inner compose for the small-models node — per the orchestrator's fleet capture of 2026-09-10, **9 live hostnames** (flux2-klein, privacy-filter, qwen3-30b, qwen3-6-35b, qwen3-8-27b, qwen3-embedding, qwen3-reranker, qwen3-vl-30b, whisper-large-v3), all nine returning the **same 160-entry action log** — one CVM (gpu13 cluster) under nine names, measured harness [`d4c89033…`](/manifests/measured/sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md), guest OS `9b69bb16…`: **ten model containers behind nine `vllm-proxy-rs` instances**, 30 services in all (up from 27) |
| **identity** | log-pinned: the signed compose-manager action log pins `compose_up` of this path at cvm-compose-files commit `e14107901d202215ab44eb027eb4792047d5ec83` (tag `v0.0.409`) with this `file_sha256`, in three service-scoped ups — 2026-09-09T23:47:00Z (the nine `proxy-*`), T23:47:54Z (`nginx`, `nginx-qwen38`), T23:48:44Z (`model-proxy-registrar`, `qwen38-registrar`); the orchestrator fetched the file at that commit and hash-checked it (match), and I re-derived the same hash from the clone ✓ — **not** hardware-measured; the measured harness is relied on but not re-captured here (Not traced) |
| **supersedes** | [`cb9040a3…`](/manifests/nearai/cvm-compose-files/prod/small-models/sha256-cb9040a3e32a0e9157baf26b804d95a3ec55b91d9ae87383e0acd4300490078e.md) (commit `4df5d67c`, 2026-08-24) — **this is a delta review** against that page; the full enumeration and original evidence live on [`2443fde4…`](/manifests/nearai/cvm-compose-files/prod/small-models/sha256-2443fde49e26f83d931c443501544fa0c510c658360c45324190c3d243dfefa3.md) |
| **review** | reviewed 2026-09-10 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** `git diff 4df5d67c e1410790 -- prod/small-models.yaml` from the compose-repo clone — **byte-identical to the orchestrator-supplied 1351-line patch** (52 hunks, +609/−262) — plus hashing the file at every commit the action log names. Thirteen upstream commits touch this file between the pins (`b1c47c9`, `ec7d79d`, `c960549`, `4e35ec4`, `5921951`, `33b77d5`, `19a5a2b`, `5046ffd`, `41e524b`, `85b6005`, `0f56625`, `1755bb7`, `e141079`); the aggregate diff was reviewed hunk by hunk, and the four intermediate revisions the action log actually deployed (`3bc66577` @ `33b77d54`, `55c3580b` @ `02254afa`, `da2ad1fa` @ `01356c5d`, `78eeefb4` @ `1755bb7c`) were each diffed against this file to bound what the running containers differ by (section below). Consequence for citations: every base-page line number **at or below 62 holds** (line 30's digest changed in place); the gpt-oss anchor deletion (base 63-107) and the Qwen3.8 anchor insertion (this 112-167) shift everything after by a varying offset, so every citation below was re-read from the captured file, never offset-computed. This is a DOCUMENT review: per-image behavior defers to the published image pages; the Qwen3.8 anchor is compared against its audited origin ([`dsv4-qwen38-glm51@46edfb72`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-46edfb72e0d15618429cedde9fb527bc4d4c4c4073eac08099e854b43c44bc96.md)) by a programmatic diff of the two anchor texts, but re-verified at this file's bytes.

---

## What changed since cb9040a3 (complete delta)

The whole diff, grouped by change (52 hunks, all accounted for):

**A. gpt-oss-120b retired (−), commit `33b77d5`.** The `x-gpt-oss-common` anchor (base 63-107: `vllm-openai@6766ce0c`, `--revision b5c939de…`, runai streamer, the `GPT_OSS_CACHE_SUBPATH` `/compose/up` comment), `proxy-gptoss-120b`, `model-vllm-gptoss-120b-mxfp4-tp1-r3`, its two scrape jobs (`vllm-model-vllm-gptoss-120b-mxfp4-tp1-r3`, `inference-proxy-proxy-gptoss-120b`), `dcgm-gptoss-120b` (renamed, not removed — see D), the nginx `:8001` block and the `gpt-oss-120b.completions.near.ai` TLS block, the registrar's `8001` probe / `register_model "openai/gpt-oss-120b"`, the shared nginx's `8001:8001` port and `proxy-gptoss-120b` dependency. **Sweep:** `grep -ci 'gpt-oss\|gptoss'` = **0** in this file — nothing dangles. The shared registrar now loops `8002…8009` (2177) and probes `:8002` for readiness (2255); the nginx `model:` tag on `nginx`/`model-proxy-registrar` labels changes from `openai/gpt-oss-120b` to `multi` (625, 651). The action log records the matching `compose_down` of the three gpt-oss services at 2026-09-02T22:40Z.

**B. Qwen3.8-27B temporary stack added (+), commits `5046ffd`/`41e524b`/`85b6005`/`0f56625` (2026-09-08).** Comment block 112-124 ("TEMPORARY: … gpu02 CVM maintenance evacuation"; "Remove this block when gpu02 serves Qwen3.8 again", 124); anchor `x-qwen3-8-27b-common` 125-166; comment 745-749 ("Uses its own nginx (host :8000 HTTP, :8001 TLS) and its own registrar (profile register-qwen38) so the shared nginx, the shared registrar and their live connections are never recreated"); services `model-downloader-qwen38` 751-765, `proxy-qwen38-27b` 767-805, `model-sg-qwen38-27b-fp8-tp1-r1` 807-850, `nginx-qwen38` 852-870, `qwen38-registrar` 872-899; volume `qwen38_kernel_cache` 1549; scrape jobs `sglang-model-sg-qwen38-27b-fp8-tp1-r1` 1868-1898 and `inference-proxy-proxy-qwen38-27b` 1900-1917; configs `nginx_qwen38_conf` 2499-2552 and `qwen38_registrar_script` 2554-2686. Detailed in the INFO findings below. The `proxy-qwen36-35b-a3b` / Qwen3.6 engine hunks (patch 362-687) are **pure re-ordering context**: the diff pairs the deleted gpt-oss blocks with the unchanged Qwen3.6 blocks; a line-by-line read confirms the Qwen3.6 proxy env (659-673) and both engine services (691-716, 718-749) are identical to the base modulo the label change in C.

**C. Datadog OpenMetrics labels removed fleet-wide (−), commit `c960549`.** `com.datadoghq.ad.check_names`, `com.datadoghq.ad.init_configs` and `com.datadoghq.ad.instances` are deleted from every model container and every dcgm instance (patch hunks at 687-855: GLM 959-961 removed, FLUX, Qwen3-VL, embedding, reranker, whisper, four dcgm). **Sweep:** `check_names` = 0 hits; `com.datadoghq.ad.logs` = **30** hits, one per service (590 … 1536) plus the anchor's forwarding list (6). What that changes: on any harness that measures a Datadog agent, the agent would no longer autodiscover the engines' and dcgm's `/metrics` for its OpenMetrics integration — **the logs integration is untouched**, so the LOW below (stdout → third-party SaaS where an agent exists) is unchanged in substance, and the metrics leg of that latent egress is gone. Inert on this harness either way (no agent measured).

**D. dcgm re-mapping.** `dcgm-gptoss-120b` → `dcgm-privacy-filter` (1393-1418; GPU 0, 1401; `model:openai/privacy-filter`), scrape job renamed (1782); `dcgm-shared-gpu7` comment drops privacy-filter from the GPU 7 list (1475-1478). Still four instances on the tag-pinned image (286) with `SYS_ADMIN` (288-289), covering 0 / 1,2 / 3-6 / 7 (1401, 1428, 1455, 1487).

**E. privacy-filter widened to the full context (`19a5a2b`, on a dedicated GPU since `33b77d5`).** Inline app: comment rewrite 330-347; `ATTN_IMPL = _env("PRIVACY_ATTN_IMPL", "sdpa", str)` added (351); the single `pipeline(...)` constructor becomes an eager/SDPA branch with fallback (357-445) carrying **four new `sys.stderr.write` calls** (389, 397, 422, 430) — all at model-load time, before the FastAPI app exists (472), formatting `ATTN_IMPL`, `effective_attn_impl`, `MAX_LENGTH` and, at 422/430, the constructor's exception text; `STRIDE` re-clamped (446). Env: `GPU_MEM_LIMIT_GB` 32 → **110** (555), `PRIVACY_BATCH_SIZE` 8 → **1** (560), `PRIVACY_MAX_LENGTH` 4096 → **131072** (561), `PRIVACY_STRIDE=256` unchanged (562), `PRIVACY_ATTN_IMPL=sdpa` new (563); GPU 7 → **0** (572). Unchanged: `FROM pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime` (301), the unpinned `pip install` (302-306), `MODEL_ID` / `MODEL_REVISION = "7ffa9a04…"` (318-319), every `trust_remote_code=True` beside `revision=MODEL_REVISION` (362/366, 373/377, 406/410, 440/444, 452), the `classify()` handler (501-541 — no print, no log, no write; the OOM branch is `empty_cache(); os._exit(1)` at 513-514), the VRAM watchdog's numeric stderr line (461-464), `CMD ["uvicorn", …]` with no log flags (543). See the INFO below for what this does and does not change.

**F. Proxy image bump on every proxy (`1755bb7`, `e141079`).** Anchor `x-vllm-proxy-common` 30: `b183677a` → **`98b57ad7`**; `proxy-qwen38-27b` re-states the same digest inline (769). The intermediate commit `1755bb7` pinned `27265f5a` (deployed 2026-09-09T21:03Z → T23:47Z, see the deployment section); `e141079` moved to `98b57ad7`. The proxy env blocks are otherwise untouched (verified: the only `+`/`-` env lines in the whole patch are the qwen38 proxy's).

**G. nginx keepalive uniformity (`b1c47c9`, `ec7d79d`, `4e35ec4`).** `keepalive_timeout 1h; keepalive_requests 1000000;` now appears in **every** TLS server block of `nginx_conf` (FLUX 2410-2411, Qwen3-VL 2425-2426, embedding 2436-2437, reranker 2447-2448, whisper 2458-2459, privacy-filter 2469-2470, Qwen3.6 2484-2485, GLM 2494-2495 — GLM's was the base's lone instance) and in the new `nginx_qwen38_conf` TLS block (2549-2550), with explanatory comments. **No `access_log`, `log_format`, `proxy_buffering`, `proxy_cache`, `client_body_buffer_size` or body-logging directive changed** — the prelude (2310-2338) is outside every hunk; the only other nginx hunks are the gpt-oss removals in A.

**H. Comment-only hunks.** GLM comment block 204-212 re-worded (origin now `prod/qwen36-qwen38.yaml`, 208; `/compose/up … pull -> build -> up` still documented, 210-212); Qwen3-VL `--async-scheduling` comment shortened (1079-1082); privacy-filter section header removed (patch 771-779); dcgm GPU 7 comment (1475-1478).

**I. Nothing else.** The otelcol config's OTLP receiver (1558-1563), filelog receiver (1565-1636), processors (2047-2116), exporter (2118-2126), extensions (2128-2131), pipelines (2133-2155) and telemetry level (2160) are outside every hunk — **byte-identical to the base**, `num_consumers: 1` (2124) included. The FLUX anchor (168-202), Qwen3.6 anchor (63-110), downloader anchor (214-218), GLM build/cmd/env anchors (223-283), dcgm anchor (285-291), the GLM/Qwen3-VL/embedding/reranker/whisper service bodies, the shared registrar's probe helpers (2219-2232) and the `nginx_conf` prelude are untouched.

**Carry-over map** (every base finding; each re-read at this file's lines):

| base finding | status here |
|---|---|
| FLUX prompt log at INFO shipped off-box [CRITICAL] | **CONFIRMED** (188, 195 → 52-61, 1-6, 1523, 1566, 1594-1597, 1606-1609, 2083-2093, 2120, 2136-2139) |
| FLUX image-to-disk, never deleted [HIGH] | **CONFIRMED** (192-194) |
| FLUX `--trust-remote-code`, no `--revision` [HIGH] | **CONFIRMED** (181-191; 183, 187; still the only unpinned model) |
| app stdout leaves the box unredacted [HIGH, systemic] | **CONFIRMED, widened** — `model-sg-qwen38-27b-fp8-tp1-r1` (821), `model-downloader-qwen38` (765), `nginx-qwen38` (870) and `qwen38-registrar` (899) join the kept set; gpt-oss leaves it |
| three in-enclave `build:` blocks + PyPI-resolved downloader [HIGH, ARMED] | **CONFIRMED, one more downloader** — builds unchanged (171-180, 297-306, 1309-1313; GLM's 223-248 still resolves nothing mutable); a second `uvx … hf download` (758) with read-write on the shared weights volume (761) |
| Qwen3-VL unrestricted media fetch [MEDIUM] | **CONFIRMED, widened** — Qwen3.8-27B is a second multimodal engine with `--mm-attention-backend fa3` (140) and no `--disable-multimodal`; the file still sets no media control on any service |
| unauthenticated sglang admin surface via bare `location /` [MEDIUM] | **CONFIRMED, widened** — four sglang LLM engines now (Qwen3.6 x2, GLM, Qwen3.8) plus diffusion; a second allowlist-free nginx (2534, 2551) |
| `/compose/up` env-splice channel [MEDIUM, ARMED] | **CONFIRMED, documentation changed** — the gpt-oss `env={…}` example is gone with its anchor; `/compose/up` is still documented as build trigger (210-212) and as the `services=[qwen38-registrar]` selector (872) |
| `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` on all nine proxies [MEDIUM, ARMED] | **CONFIRMED** — nine proxies, `proxy-qwen38-27b` included (786-787; 672-673, 918-919, 998-999, 1056-1057, 1141-1142, 1214-1215, 1287-1288, 1364-1365); `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` likewise on all nine (780-781 …) |
| dcgm-exporter tag-pinned + `SYS_ADMIN` x4 [MEDIUM] | **CONFIRMED** (286, 288-289; 1393/1420/1447/1479) |
| `ipc: host` node-wide [MEDIUM] | **CONFIRMED** (12; Qwen3.8 inherits via `<<: *nvidia`, 126) |
| Qwen3.6 replicas at the 300 s watchdog default [LOW, ARMED] | **CONFIRMED, widened** — Qwen3.8 also passes no `--watchdog-timeout` (131-153); GLM alone pins 1800 (268) |
| `com.datadoghq.ad.logs` on all services [LOW, latent] | **CONFIRMED, count 27 → 30**; OpenMetrics labels gone (change C) |
| Qwen3.6 engine audited, flags consumed [INFO] | **CONFIRMED** (70, 97) |
| no metrics-to-file path [INFO, OFF] | **CONFIRMED, one command added** (97/95, 188/191, 260/272, **153/152**) |
| proxies `user: root` + `privileged: true` [INFO] | **CONFIRMED** (31-32); digest changed (30) |
| volumes inventory [INFO] | **CONFIRMED, one added** (1543-1552; `qwen38_kernel_cache` 1549 → `/root/.cache/sglang`, 156) |
| registrar authenticated probes [INFO] | **CONFIRMED, 8001 dropped; a second registrar added** (2167, 2177, 2219-2236, 2244, 2255, 2296-2303; 2560, 2604-2633, 2636-2648) |
| nginx posture (no body logging/buffering) [INFO] | **CONFIRMED on both nginx configs** (2310-2338; 2502-2529) — keepalives now uniform (change G) |
| `OHTTP_ENABLED=true` on all nine proxies | **CONFIRMED** (665, 778, 911, 991, 1049, 1134, 1207, 1280, 1357) |
| GLM-5.1 AWQ TP4 addition [INFO] | **CONFIRMED unchanged** — build 223-248, cmd 249-273, env 275-283, service 937-979, proxy 901-935 all outside every hunk except the label removal (C) |

Nothing is REFUTED. GONE: the gpt-oss row of the sibling table (retired with the service) and the base's `/compose/up` env-splice *example* (the channel itself remains).

---

## Deployment state per service group (from the signed action log)

near.ai deploys with service-scoped `compose up`; the action log names which services were (re)created from which revision. Read from `compose_manager_attestation.actions` (160 entries, identical on all nine hostnames), restricted to `prod/small-models.yaml`, taking each service's **last** `compose_up`:

| service group | last created from | commit / tag | when | differs from this file by |
|---|---|---|---|---|
| all nine `proxy-*` | **`a03816ba`** (this file) | `e1410790` / v0.0.409 | 2026-09-09T23:47:00Z | — |
| `nginx`, `nginx-qwen38` | **`a03816ba`** | `e1410790` / v0.0.409 | T23:47:54Z | — |
| `model-proxy-registrar`, `qwen38-registrar` | **`a03816ba`** | `e1410790` / v0.0.409 | T23:48:44Z | — |
| `model-sg-qwen38-27b-fp8-tp1-r1`, `model-downloader-qwen38`, `otelcol-contrib` | `da2ad1fa` | `01356c5d` / v0.0.403 | 2026-09-08T22:26-22:38Z | **only the two proxy `image:` lines** (`git diff 01356c5d e1410790`: 2 insertions, 2 deletions) — the engine, its anchor, the otelcol config and every scrape job are byte-identical |
| `model-privacy-filter`, `dcgm-privacy-filter` | `3bc66577` | `33b77d54` / v0.0.385 | 2026-09-02T22:42-22:44Z | **the whole of change E** — `33b77d54` predates `19a5a2b` (2026-09-03); the running container was built and started with `GPU_MEM_LIMIT_GB=32`, `PRIVACY_BATCH_SIZE=8`, `PRIVACY_MAX_LENGTH=4096`, no `PRIVACY_ATTN_IMPL`, and the single-constructor app code (on GPU 0 already, per the `33b77d5` comment at 330) |
| Qwen3.6 r1/r2, GLM-5.1 (+ its downloader), FLUX, Qwen3-VL, embedding, reranker, whisper, `dcgm-qwen36-35b-a3b`, `dcgm-glm51`, `dcgm-shared-gpu7` | `cb9040a3` | `7d631eb1` / v0.0.358 | 2026-08-24T20:56Z (the last **unscoped** up, `services: null`) | **labels only** (change C) and comments — image, command, env, volumes, GPU placement unchanged; the running containers still carry the deleted `com.datadoghq.ad.check_names`/`instances` labels (inert without an agent) |

Two further facts from the log: (1) `proxy-qwen3-reranker-0.6b` alone was recreated at `55c3580b` (`02254afa`, 2026-09-08T18:30Z) before the fleet-wide proxy ups; (2) all nine proxies ran the intermediate `27265f5a` build from 2026-09-09T21:03Z (`78eeefb4` @ `1755bb7c`) until the T23:47Z up — a 2 h 44 min window whose proxy digest has its own [image page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-27265f5a10abcacc3fe80604a00ed2ba32c6e082895115e4b652b40525cb554d.md); five bare `docker_restart` entries sit in that window (21:16-21:22Z). **Consequence:** this file is the *declared* configuration the attestation surface presents, and it is what runs for the proxies, both nginx and both registrars; for every engine it is behaviourally equivalent to what runs **except the privacy-filter**, whose running instance is the pre-`19a5a2b` build. Neither state of the privacy-filter logs request content (both are covered by the INFO below); the discrepancy is recorded because a reader keying on this hash would otherwise assume the 131072-token single-pass configuration is live. A hash-keyed page cannot prove a container's provenance; the log is trusted for this table.

---

## Findings

Bodies are condensed for carried items; the [full-enumeration page](/manifests/nearai/cvm-compose-files/prod/small-models/sha256-2443fde49e26f83d931c443501544fa0c510c658360c45324190c3d243dfefa3.md) carries the original analysis. Every line number below was verified against this file.

### CRITICAL (deployed: ON) — FLUX logs every user prompt at INFO to stdout, and this file's otelcol pipeline ships it off-box (log + telemetry egress)

Carried; the delta touches no hop. The FLUX command (181-191) passes `--log-requests-level 0` (188), which the diffusion entrypoint **silently discards** (`parse_known_args`, per the [image page](/images/docker.io/lmsysorg/sglang/sha256-8ece90ad52faa8b56149f0117227d9009db34513213e35990da468aeb6fe0b75.md), verdict LEAKS: `SamplingBatch.log()` interpolates prompt and negative prompt verbatim into a `logger.info` on every generation). Nothing deployed suppresses it: `environment: *sglang-env` (195) → anchor 52-61 sets no diffusion log-level env, and the command sets no `--log-level`. This file completes the egress, every hop re-read: the container logs via `json-file` (1-6, via `x-flux-common` ← `x-vllm-common` `logging: *logging-conf`, 27); otelcol-contrib mounts `/var/lib/docker/containers:ro` (1523) and its `filelog/docker_containers` receiver tails `/var/lib/docker/containers/*/*-json.log` (1566); the receiver's `regex_parser` lifts `model:` (1594-1597) and `deployment:` (1606-1609) from the FLUX container's `com.datadoghq.ad.logs` label (1021) so `filter/app_logs` (2083-2093) — which drops only unlabeled records (2087) and five management services (2088-2092) — keeps it; the `logs/app` pipeline (2136-2139) runs `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch` (2138) — **no redaction stage** — and exports via `otlphttp/gateway` (2139) to the literal `https://telemetry.infra.near.ai` (2120) with the `MONITORING_INGEST_TOKEN` bearer (2122, from 1513). The FLUX container itself runs from `cb9040a3` (deployment table), whose command is line-identical. At the deployed flags nothing in this file can turn this off.

### HIGH (deployed: ON) — FLUX writes every generated image to disk inside the enclave and never deletes it (disk)

Carried unchanged. `save_output=True` is hardcoded in the image (image page); the FLUX service mounts only `hugginface_cache` and `vllm_cache` (192-194), so outputs accumulate in the container's writable layer across restarts. The CRITICAL log line prints the file path including the UUID that guards the image page's unauthenticated read-back endpoint. No flag in this file mitigates.

### HIGH (deployed: ON) — FLUX runs `--trust-remote-code` with no `--revision` pin: unpinned Hugging Face head code executes in the plaintext-prompt process (any sink)

Carried unchanged. `--model-path black-forest-labs/FLUX.2-klein-4B` (183) plus `--trust-remote-code` (187), no `--revision` anywhere in 181-191; `HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}` (55) defaults to live fetch. Still the only unpinned model in the file (sibling table); the new Qwen3.8 engine pins (135) and its downloader pins the same SHA (758).

### HIGH (deployed: ON, systemic) — every app container's stdout leaves the box unredacted via otelcol-contrib, and the Qwen3.8 engine, its downloader, the second nginx and the second registrar join that stream (telemetry egress)

Carried, widened by the delta. All app services log via `json-file` (1-6; the collector itself uses `local`, 1530-1534); otelcol tails every container log (1523, 1566); the drop filter (2083-2093) exempts only `compose-manager`, `compose-manager-launcher`, `otelcol-management`, `otelcol-contrib`, `datadog-agent` (2088-2092) — **it does not name `nginx-qwen38` or `qwen38-registrar`**, and both carry `model:`/`deployment:` tags (870, 899), so their stdout is kept; everything kept ships to `https://telemetry.infra.near.ai` (2120). Processors (2047-2116) contain **no redaction stage**. Four services enter the kept set in this revision: `model-sg-qwen38-27b-fp8-tp1-r1` (821: `model:Qwen/Qwen3.8-27B-FP8` + `deployment:small-models`) — so whatever the Qwen3.8 engine ever prints (the [engine page](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md)'s `/configure_logging` switch-on path and the 300 s watchdog dump included) leaves the box; `model-downloader-qwen38` (765; prints download progress only, 757-759); `nginx-qwen38` (870; its access log is the `json_combined` format at 2502-2515 — `uri`, `request_id`, `org_id`, `workspace_id`, host, method, status, sizes, timings, upstream — **no body, no query string**, same shape as the shared nginx at 2310-2323); and `qwen38-registrar` (899; prints registration status lines — HTTP codes, attempt counts, endpoint and port: 2615-2630, 2644, 2651-2661, 2671-2676 — no content). One service leaves the set (gpt-oss). The guarantee "no content leaves" still equals "no service ever prints content", and FLUX is the live counterexample. The harness facts relied on (no Datadog agent measured; app stdout ships once) come from the [`d4c89033…`](/manifests/measured/sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) page, not re-captured here.

### HIGH (deployed: ARMED) — three in-enclave `build:` blocks and two in-enclave downloaders resolve code from mutable sources at deploy time; `vllm[audio]` can replace whisper's pinned engine (any sink)

Carried; one downloader added, no Dockerfile or base changed. The three mutable builds are byte-identical to the base (all outside every hunk): (1) `sglang-diffusion` (FLUX): `FROM lmsysorg/sglang@sha256:8ece90ad…` (178) then unpinned `pip install -e "python[diffusion]"` (179) and `accelerate einops` (180). (2) `vllm-with-audio` (whisper, 1309-1313): `FROM vllm/vllm-openai@sha256:ccd6a6db…` (1312) then `RUN pip install openai-whisper torchaudio librosa vllm[audio]` (1313) — `vllm[audio]` re-resolves vLLM from PyPI over the pinned digest. (3) `privacy-filter-hf` (297-306): tag-pinned `FROM pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime` (301) plus unpinned `pip install` (302-306); counter-weight unchanged — the inline app is pinned by this file's hash and pins `MODEL_REVISION = "7ffa9a04…"` (319, used at 362/373/406/440/452) beside every `trust_remote_code=True`. The fourth `build:` — GLM's (223-248, digest-pinned `FROM` at 225, inline patch, no fetch) — is still not in this class.

**Downloaders:** `model-downloader` (576-590, `uvx --from 'huggingface_hub[hf_xet]' hf download QuantTrio/GLM-5.1-AWQ --revision 8f60817a…`, 583) and the new `model-downloader-qwen38` (751-765, same form for `Qwen/Qwen3.8-27B-FP8 --revision 017b9c7a…`, 758) both resolve `huggingface_hub` and extras from PyPI at run time, unpinned, inside the enclave, holding `HF_TOKEN` (588, 763) and **read-write on the shared `hugginface_cache` volume** (586, 761) that every engine loads weights from. Neither sees plaintext; the `--revision` pins defend against Hugging Face-side drift but not against a writer on the volume, which the Qwen3.8 engine then reads with `--trust-remote-code` (133). ARMED on any rebuild / downloader run, not per-request; the action-log hash pins the recipe, not the resolved bytes. Both privacy-filter states in the deployment table share the same unpinned `pip install` lines.

### MEDIUM (deployed: ON) — two multimodal engines, Qwen3-VL and the new Qwen3.8-27B, fetch request-supplied media URLs with no domain allowlist and redirects enabled; no service in the file sets either control (egress)

Carried and widened. Qwen3-VL (1083-1093, env `*vllm-env` 1097 → 42-50) still has no `--allowed-media-domains` and no `VLLM_MEDIA_URL_ALLOW_REDIRECTS` — per its [image page](/images/docker.io/vllm/vllm-openai/sha256-ccd6a6dbf4aba4e94c6f7052d1835d6e742082b6a5095276552e9b7a5a47c2e5.md) an unset allowlist means no check runs. **New:** the Qwen3.8-27B command (131-153) passes `--mm-attention-backend fa3` (140) and **no `--disable-multimodal`**; per the [`febfb971` page](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md) (MEDIUM ON) `Qwen/Qwen3.8-27B-FP8` at the pinned revision is multimodal, auto-enabled, and image/audio/video URLs are fetched through a bare `requests.Session` with no host check, redirects on, plus a `file://` local-read primitive. The file sets no media control on any service (grep-verified: `allowed-media` 0, `MEDIA_URL` 0, `disable-multimodal` 0). The sibling recipe this anchor was copied from ([`46edfb72`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-46edfb72e0d15618429cedde9fb527bc4d4c4c4073eac08099e854b43c44bc96.md)) carries the same gap. The proxy in front ([`98b57ad7`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md)) has an image-domain allowlist feature whose env is **unset in this file** (its LOW OFF finding) — a residual, not a mitigation. Request-driven arbitrary egress from two plaintext engines, including SSRF against the admin surface below.

### MEDIUM (deployed: ON) — the engines' unauthenticated sglang admin surface is reachable through two allowlist-free nginx instances and the proxies on the flat external network; four sglang LLM engines now (log / egress)

Carried, widened. The file now runs **four** unauthenticated sglang LLM engines — Qwen3.6 x2 (691, 718; [image page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md): CRITICAL unauthenticated `/configure_logging`), GLM-5.1 (937; [engine page](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md): same CRITICAL) and **Qwen3.8-27B (807; [engine page](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md): same CRITICAL — one POST turns on verbatim prompt+output logging and arbitrary-path request pickling, no restart)** — plus the diffusion one. No `--api-key`/`--admin-api-key` on any command (grep-verified: `api-key` 0 hits). The shared nginx publishes eight plaintext host ports plus 80 and 8444 (605-615) and routes every listener with a bare `location /` (2343, 2349, 2355, 2361, 2367, 2373, 2379, 2385; TLS 2412, 2427, 2438, 2449, 2460, 2471, 2486, 2496); the **new `nginx-qwen38` publishes host `8000` (plain HTTP) and `8001` (TLS)** (858-860) with the same bare `location /` to `proxy-qwen38-27b:8000` (2534, 2551), so the full HTTP surface reaches that proxy too. The proxy page's MEDIUM ON finding is the relevant residual: the authenticated catch-all forwards **any path except `/v1/responses`** to the engine — no engine path allowlist — so `/configure_logging` is reachable from a token-holding client through nginx → proxy → engine. Laterally, every engine and proxy share the flat `dstack_default` network (1538-1541, `external: true`) with no egress allowlist; the media-fetch MEDIUM above gives an unauthenticated caller an SSRF primitive into that network from two engines. The stdout finding above then ships whatever logging is switched on.

### MEDIUM (deployed: ARMED) — the `/compose/up` channel arms env splices and service selection outside the measured `allowed_envs` without a measurement change (config channel arming egress)

Carried; documentation changed. The base's worked example (`pass env={"GPT_OSS_CACHE_SUBPATH":"v3"} to /compose/up`, base 66-70) left with the gpt-oss anchor, so this file no longer documents an env splice — but it documents the same endpoint as the GLM build trigger (`compose-manager's /compose/up already does pull -> build -> up`, 210-212) and as the selector that starts the profile-gated registrar (`compose/up services=[qwen38-registrar]`, 872; `profiles: [register-qwen38]`, 880). The action log confirms the selector is used (every `compose_up` entry since 2026-08-24 is service-scoped). Whether env splices appear in the signed action log remains unverified (compose-manager pages' territory); the log entries carry `services` but no `env` key.

### MEDIUM (deployed: ARMED) — `WEB_CONTEXT_SEARCH_URL` + `_API_KEY` and `FUSION_ENABLED` + `FUSION_INTERNAL_BEARER_TOKEN` on all nine plaintext-terminating proxies, `proxy-qwen38-27b` included: operator-suppliable egress (egress)

Carried in shape; the new proxy carries both pairs (786-787, 780-781) like the other eight (`WEB_CONTEXT_*`: 672-673, 918-919, 998-999, 1056-1057, 1141-1142, 1214-1215, 1287-1288, 1364-1365; `FUSION_*` two lines above each). All values are `${…}` pass-throughs; nothing in this file sets them. The bound is the audited proxy source: the [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) rates web search **CRITICAL ARMED (client-opt-in: model-written queries derived from the decrypted conversation to an external search API)** and fusion **HIGH ARMED (forwards the full plaintext conversation to endpoints from a runtime-fetched listing)**, both fail-closed when the env is unset. Held at MEDIUM ARMED at *this* layer because the file neither sets the values nor can show they are unset; whether the measured `allowed_envs` admits them is the harness page's question (Not traced). Severity follows the proxy page if the orchestrator confirms either value is populated.

### MEDIUM (deployed: ON) — dcgm-exporter x4 pinned by tag, not digest, with `SYS_ADMIN` + nvidia runtime: privileged bytes can drift under the attestation (telemetry, any sink under drift)

Carried unchanged in kind: `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (286) with `cap_add: SYS_ADMIN` (288-289), four instances (`dcgm-privacy-filter` 1393, `dcgm-qwen36-35b-a3b` 1420, `dcgm-glm51` 1447, `dcgm-shared-gpu7` 1479) covering all eight GPUs (1401, 1428, 1455, 1487). The rename from `dcgm-gptoss-120b` changes labels only. See the [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md).

### MEDIUM (deployed: ON) — `ipc: host` on every engine and proxy, including the privacy-filter and the new Qwen3.8 engine (cross-container shared memory)

Carried unchanged (`x-nvidia`, 10-18, `ipc: host` at 12; Qwen3.8 inherits via `<<: *nvidia`, 126; proxies via 33). Qwen3.8 additionally has `cap_add: SYS_NICE` (128-129) — scheduling priority only, no ptrace or cross-process memory read (engine page concurs). No `pid: host`, `SYS_PTRACE`, or docker-socket mount anywhere in this file (grep-verified; the proxies mount `/var/run/dstack.sock`, 37, as before).

### LOW (deployed: ARMED) — three sglang LLM engines run the undisableable watchdog hang-dump at the engine's 300 s default — Qwen3.6 x2 and now Qwen3.8-27B — while GLM alone pins 1800 s (log, feeding the telemetry egress)

Carried, widened. The Qwen3.6 command (71-98) and the **Qwen3.8 command (131-153)** pass no `--watchdog-timeout` (grep: the only `--watchdog-timeout` in the file is GLM's `1800` at 268); per the [`984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) and [`febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md) pages both builds default to **300 s**, and the dump (full prompt and output token IDs of the in-flight batch, at ERROR, undisableable) is rated HIGH ON on each image page. The audited origin recipe ([`46edfb72`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-46edfb72e0d15618429cedde9fb527bc4d4c4c4073eac08099e854b43c44bc96.md)) omits the pin for Qwen3.8 in the same way. Whether the diffusion entrypoint FLUX uses has a watchdog is not on its image page (Not traced). Severity at this layer as before: the manifest omits a pin that would widen the trigger window sixfold; the dump itself is the image page's.

### LOW (deployed: ARMED) — all 30 services carry `com.datadoghq.ad.logs` labels: stdout becomes third-party SaaS egress on any node whose harness measures a Datadog agent; the OpenMetrics labels are gone (egress, latent)

Carried; count 27 → 30. Every service — downloaders 590 and 765, both nginx 625 and 870, both registrars 651 and 899, all nine proxies (675, 789, 921, 1001, 1059, 1144, 1217, 1290, 1367), all ten model containers (702, 729, 821, 962, 1021, 1106, 1181, 1254, 1331, 1387), all four dcgm (1404, 1431, 1458, 1490), and otelcol-contrib itself (1536) — is labeled, and the `json-file` anchor forwards the label (6). Change C removes `check_names`/`init_configs`/`instances` from the ten engines and four dcgm (sweep: 0 remaining), so an agent would no longer autodiscover their `/metrics`; the log-collection leg is unchanged. Inert on the `d4c89033…` harness (no agent measured); pre-enrolled for double log egress on any harness that measures one. The engine containers created from `cb9040a3` still carry the deleted labels (deployment table) — equally inert.

### INFO (deployed: OFF, engine-side residuals ARMED) — the Qwen3.8-27B addition: audited nightly digest, minimum request-log level genuinely consumed, pinned revision beside `--trust-remote-code`, no dump/file/trace flag, no watchdog pin, multimodal left on; anchor identical to the audited origin except two memory-pool sizes (log / disk / egress)

The delta's substantive addition, read in full at this file.

**Anchor (125-166), every line:** `<<: *nvidia` (126: `ipc: host`), `init: true` (127), `cap_add: [SYS_NICE]` (128-129), `image: lmsysorg/sglang@sha256:febfb971…` (130 — digest-pinned; [image page](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md): PRIVATE at the deployed flags, identity self-asserted nightly `c4271c3f`). **Command (131-153):** `sglang serve` (132) `--trust-remote-code` (133) `--model-path Qwen/Qwen3.8-27B-FP8` (134) `--revision 017b9c7af6b5689d5dd426a76e0bc077eb5ca20a` (135 — full SHA; the engine page verifies the revision ships no `.py`/`auto_map`, so the flag has nothing to execute) `--served-model-name Qwen/Qwen3.8-27B` (136) `--tp 1` (137) `--mem-fraction-static 0.60` (138) `--attention-backend flashinfer` (139) `--mm-attention-backend fa3` (140 — multimodal path live) `--chunked-prefill-size 8192` (141) `--max-prefill-tokens 8192` (142) `--enable-mixed-chunk` (143 — scheduler batching: prefill and decode in one batch; names no log or file) `--max-mamba-cache-size 128` (144) `--max-running-requests 64` (145) `--max-queued-requests 128` (146) `--reasoning-parser qwen3` (147) `--tool-call-parser qwen3_coder` (148 — output parsing on the response path, not a sink) `--port 8000` (149) `--host 0.0.0.0` (150) `--enable-cache-report` (151 — cached-token count in `usage`, benign per the engine page) `--enable-metrics` (152 — pull-based `/metrics`, scraped at 1868-1898; label set verified content-free on the engine page) `--log-requests-level 0` (153 — `--log-requests` itself absent, so request logging is OFF; the engine page verifies `sglang serve` genuinely consumes this on the LLM path, unlike the diffusion one). **Absent:** `--log-requests`, `--watchdog-timeout` (LOW above), `--crash-dump-folder`, `--export-metrics-to-file`, `--enable-trace`, `--debug-tensor-dump-output-folder`, `--enable-hierarchical-cache`, `--disable-multimodal`, `--api-key`, `--admin-api-key` (all grep-verified absent from the file). **Volumes (154-156):** `hugginface_cache:/root/.cache/huggingface` (155), `qwen38_kernel_cache:/root/.cache/sglang` (156 — per the engine page, JIT artifacts keyed by shape, not content; no disk-backed KV tier at these flags). **Env (157-163):** `HF_TOKEN` (158), `HF_HUB_OFFLINE: ${HF_HUB_OFFLINE:-0}` (159, live-fetch default as elsewhere), `VLLM_LOGGING_LEVEL=INFO` (160 — a vLLM variable sglang does not read; inert, per the engine page), `NVIDIA_DRIVER_CAPABILITIES` (161), `OPENBLAS_L2_SIZE` (162), `NCCL_DEBUG=WARN` (163). No `SGLANG_*` logging, profiler, dump or storage variable. `restart: unless-stopped` (164), `logging: *logging-conf` (166 — json-file, so stdout enters the otelcol stream).

**Against the audited origin:** a programmatic diff of this anchor against the `x-qwen3-8-27b-common` anchor of `prod/dsv4-qwen38-glm51.yaml` at [`46edfb72`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-46edfb72e0d15618429cedde9fb527bc4d4c4c4073eac08099e854b43c44bc96.md) (the recipe the `febfb971` page reviewed) and against `prod/qwen36-qwen38.yaml` at `e1410790` (the file the comment names as origin, 115-116; no audit page exists for that path) yields **exactly three differing lines in both cases**: `--mem-fraction-static 0.85` → `0.60`, `--max-mamba-cache-size 320` → `128`, and the volume name `huggingface_cache` → `hugginface_cache` (this file's spelling). This matches the file's own comment (117-120). Both changes are GPU-memory sizing; neither names a log, file, or network path. **The engine page's deployed-command analysis therefore applies verbatim**, residuals included.

**Service (807-850):** `<<: *qwen3-8-27b-common` (808), waits on the downloader (810-812), GPU 0 (818 — shared with the privacy-filter, 572), labels with `model:` + `deployment:` (821) and a large static `nearai.otel.*` set (822-850) whose values are flag echoes and identifiers (`config_variant: qwen38-gpu13-gpu0-maintenance`, `sglang_revision: qwen38.27b.g561c8f3`, `engine_image: febfb971c735`) — static strings, not request content; note `sglang_revision` is an operator label and does not correspond to the image's build commit (`c4271c3f` per the engine page), so it is not identity evidence.

**Proxy (767-805):** `image` re-stated inline as `98b57ad7` (769, same as the anchor 30). Environment (772-787) is **line-identical to the eight sibling proxies** modulo `MODEL_NAME` (777), the backend variable — `VLLM_BACKEND_URLS=http://model-sg-qwen38-27b-fp8-tp1-r1:8000` (782, list form with one entry, as Qwen3.6 uses at 669; the other seven use `VLLM_BASE_URL`) — **and one extra key: `VLLM_PROXY_MAX_REQUEST_SIZE=104857600` (783)**, a 100 MB request-body cap present on no other proxy in this file (verified by a programmatic diff of every `environment:` block against this one). **Neither `VLLM_BACKEND_CONVERSATION_AFFINITY` nor `VLLM_PROXY_STREAM_IDLE_TIMEOUT_SECS` appears anywhere in this file** (grep: 0 hits each); per the proxy page, affinity is inactive on a single backend anyway. `OHTTP_ENABLED=true` (778), `TOKEN=${PROXY_TOKEN}` (779), `LOG_FORMAT=json` (776), `USE_NV_ATTESTATION_SDK=true` (785).

**Telemetry wiring:** engine `/metrics` scraped by `sglang-model-sg-qwen38-27b-fp8-tp1-r1` (1868-1899, `metrics_path: /metrics`, static labels only — the same flag-echo set as the container labels), proxy by 1900-1918; metrics pipelines carry counters, not content. No dcgm instance is dedicated to it (GPU 0 is `dcgm-privacy-filter`'s, 1401).

Residuals for this engine are the image page's: unauthenticated `/configure_logging` (MEDIUM above), the 300 s watchdog dump (LOW above), allowlist-free media fetch (MEDIUM above), 400-echo, `/server_info`.

### INFO (deployed: ON) — the second nginx publishes host 8000 (plain HTTP) and 8001 (TLS, `qwen3-8-27b.completions.near.ai`) to the Qwen3.8 proxy only, with the same body-free access log and no buffering; the second registrar probes and registers that one endpoint with the same credentials (structural / local hop)

**`nginx-qwen38` (852-870):** same digest as the shared nginx (853 = 593), depends only on `proxy-qwen38-27b` (855-856), the same 6-hourly reload loop (857), `ports: 8000:8000, 8001:8001` (858-860 — the shared nginx dropped `8001` with gpt-oss and never published `8000`, so there is no host-port collision: 605-615 vs 858-860), `certs:ro` (862), config `nginx_qwen38_conf` (863-866), `logging: *logging-conf` (868). **`nginx_qwen38_conf` (2500-2552):** `log_format json_combined` (2502-2515) and `access_log` (2517) are copies of the shared prelude's (2310-2325) — `$uri` only, no `$request`, `$args`, or body variable; `proxy_buffering off; proxy_cache off` (2525-2526), `client_max_body_size 100m; client_body_buffer_size 1m` (2528-2529); `server { listen 8000; location / { proxy_pass http://proxy-qwen38-27b:8000; } }` (2532-2535 — "registrar probe + health", 2531); `ssl_certificate` / `ssl_protocols TLSv1.2 TLSv1.3` (2537-2539); `server { listen 8001 ssl http2; server_name qwen3-8-27b.completions.near.ai … -stg … ~^qwen3-8-27b-i\d+…; keepalive_timeout 1h; keepalive_requests 1000000; location / { proxy_pass http://proxy-qwen38-27b:8000; } }` (2544-2552). **It exposes the proxy, never the engine directly** — no `server` block names `model-sg-qwen38-27b-fp8-tp1-r1` — but with a bare `location /`, which is the admin-surface MEDIUM above. Two nginx instances now write access logs into the telemetry stream (metadata only).

**`qwen38-registrar` (872-899):** curl image (876 = 628), `profiles: [register-qwen38]` (880 — started only on explicit selection, comment 872-874), env `HOST_IP`, `HTTP_PORT=8000`, `TLS_PORT_QWEN38=8001`, `MODEL_PROXY_TOKEN`, `PROXY_TOKEN` (888-892), script `qwen38_registrar_script` (893-896). **Script (2555-2686):** `PROXY_URL="https://completions.near.ai"` (2558), `TOKEN=MODEL_PROXY_TOKEN` / `INFERENCE_TOKEN=PROXY_TOKEN` (2559-2560), fail-fast (2562-2565), `ENDPOINT=HOST_IP:8000` / `MODEL="Qwen/Qwen3.8-27B"` / `DOMAIN="qwen3-8-27b.completions.near.ai"` (2569-2571), `register_endpoint` with `routing_port` (2576-2581), `unregister_endpoint` (2583-2588), `register_model` (2590-2595), TERM handler withdrawing only this endpoint (2597-2602), `check_chat()` with the synthetic `"hi"` body over plain HTTP to `HOST_IP:8000` (2604-2633, body at 2609), **`check_tls_route()`** — new relative to the shared script — `curl -skS --resolve "$DOMAIN:$TLS_PORT:$HOST_IP" … https://$DOMAIN:8001/v1/models` with the bearer (2636-2648; `-k` skips certificate verification on a loopback-resolved probe), readiness loop on `/v1/models` (2650-2662), registration loop (2665-2686) requiring both probes before `register_endpoint` (2667-2669). Same class as the base INFO: a credential on a CVM-internal hop, no user content. The shared registrar (2162-2307) is unchanged apart from the gpt-oss removals (2177, the `cleanup()` loop at 2203-2209, 2216 "all 8 models", 2244 `8009` → GLM, 2255, 2296-2303).

### INFO (deployed: ON) — the gpt-oss retirement is clean: anchor, engine, proxy, scrape jobs, nginx port and blocks, registrar entries all leave; nothing dangles (structural)

Change A enumerated above; the full-file sweep (`gpt-oss|gptoss` = 0 hits, `8001` appears only in the Qwen3.8 stack: 747, 860, 890, 2541, 2545, 2568) confirms no orphaned route, scrape target, label, or probe. The scrape-job inventory (1641-2042: 4 sglang + 1 sglang-qwen38, 4 vllm, 4 dcgm, 9 inference-proxy, 1 self = 23 jobs) matches the service list one-to-one (the privacy-filter app has no `/metrics` and, as before, no engine scrape job). Removing gpt-oss removes one vLLM stdout source from the telemetry stream and one host port (8001, re-used by the new nginx for TLS). The shared nginx now depends on eight proxies (595-603) — `proxy-qwen38-27b` is deliberately not among them (comment 745-749).

### INFO (deployed: ON) — the otelcol collector config is byte-identical to the base outside the scrape-job swaps: no receiver, processor, exporter, or attribute changed; the drop filter does not name the two new management-style services (telemetry)

Item-by-item against the diff: the only hunks inside `otelcol_app_config` (1555-2160) are the gpt-oss job removals (patch 863-887), the `dcgm-gptoss-120b` → `dcgm-privacy-filter` rename (patch 888-908), and the insertion of the two Qwen3.8 jobs with the `proxy-qwen36-35b-a3b` job as context (patch 909-993) — `job_name`, `targets`, and static labels only; the Qwen3.8 engine job adds static flag-echo labels (`chunk_size` … `engine_image`, 1891-1899) that are string constants in this file. The OTLP receiver (1558-1563, `0.0.0.0:4317/4318`, `expose`d at 1519-1521) still feeds `metrics/otlp` (2144-2147) and `traces` (2152-2155) verbatim; the filelog receiver (1565-1636), `transform/docker_metadata` (2071-2082), `filter/app_logs` (2083-2093), `groupbyattrs` (2094-2112), `batch` (2113-2116), the `otlphttp/gateway` exporter with `num_consumers: 1` / `queue_size: 128` / `file_storage` (2119-2126), and `telemetry.logs.level: info` (2160) are all unchanged. The collector itself was recreated from `da2ad1fa` (deployment table), whose config is byte-identical to this one. No new attribute could carry request content. Note for the systemic HIGH: the filter's management exemptions (2088-2092) are the same five names as the base — `nginx-qwen38` and `qwen38-registrar` are not exempt, and neither is the shared `nginx`/`model-proxy-registrar` (unchanged from the base).

### INFO (deployed: OFF) — the privacy-filter's 128k-context change adds an attention-implementation branch and four init-time stderr lines, none on the request path; the running container predates the change (log)

Change E in full. What the new code can print: 389-393 (`requested=%s … clamping MAX_LENGTH=%d` — the env string `ATTN_IMPL` and an int), 397-401 (`requested=%s effective=%s …` — two implementation names and an int), 422-425 (`non-attention initialization failure (%s)` — **the exception text from `pipeline(...)` construction**, then re-raised), 430-434 (`attn_implementation=%s rejected (%s)` — same), and the pre-existing watchdog line 461-464 (two floats). All four new writes execute inside the model-load block (357-445), **before** `app = FastAPI()` (472) and before any request can exist; the exception text they format comes from `transformers` loading a pinned checkpoint, not from user input. The request path is unchanged: `classify()` (501-541) does not print, log, or write; its only failure branch is `torch.cuda.empty_cache(); os._exit(1)` (513-514); `/v1/models` (480) is static. `PRIVACY_MAX_LENGTH=131072` (561) with `PRIVACY_BATCH_SIZE=1` (560) changes how much text one forward pass holds in GPU memory, not where it goes; `GPU_MEM_LIMIT_GB=110` (555) raises the watchdog's exit threshold. `HF_HUB_OFFLINE` still defaults to live fetch (548) — the revision pin (319) is the defence, as before. uvicorn's own access log (`CMD` 543, no `--no-access-log`) and transformers framework logging remain untraced, as on the base. **Deployment:** per the action log the live `model-privacy-filter` was built and created from `3bc66577` on 2026-09-02, before `19a5a2b`; it runs the base's single-constructor code with `4096`/`8`/`32` and no `ATTN_IMPL`. Both states are content-free by this review; the declared/running mismatch is recorded in the deployment table.

### INFO (deployed: OFF) — no metrics-to-file path exists on any of the four sglang LLM/diffusion commands; the sglang metrics/logging coupling bug is not armed (disk)

Carried, one command added: `--enable-metrics` with `--log-requests-level 0` on Qwen3.6 (95/97), FLUX (191/188), GLM (272/260) and **Qwen3.8 (152/153)**; no `--export-metrics-to-file` or any file/dump/record flag anywhere in the file (grep-verified: `export-metrics` 0, `crash-dump` 0, `enable-trace` 0, `tensor-dump` 0, `hierarchical` 0). Activation would need a command change, visible in the action log — or the runtime `/configure_logging` route (MEDIUM above), which is not.

### INFO (deployed: ON) — the nine `vllm-proxy-rs` E2EE terminators run `user: root` + `privileged: true` on a new digest whose page rates it QUALIFIED PASS; nginx keepalives are now uniform (structural)

Carried (31-32; `ipc: host` via 33 → 12; `OHTTP_ENABLED=true` on all nine). **Digest:** `b183677a` → `98b57ad7` on the anchor (30) and inline on the Qwen3.8 proxy (769). The [`98b57ad7` page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-98b57ad7aa4f9afd8ffff3ac9f4773303997f667e1d221be076c2f064e4a1284.md) was under concurrent review when this page was commissioned; at the time of writing it is on disk with verdict **QUALIFIED PASS** (dated 2026-09-10: the delta refuses `/v1/responses` in the catch-all before the body is read and adds no plaintext sink; every prior result carries) but **not yet gated into `index.json`** — the orchestrator will confirm or add an addendum. That page's deployed-configuration table reads this very file and agrees with the per-proxy facts above (backends, the lone `MAX_REQUEST_SIZE`, no affinity/idle-timeout keys). **nginx:** the base noted GLM's TLS block alone set `keepalive_timeout 1h; keepalive_requests 1000000;`; change G makes every TLS block do so (with comments explaining cloud-api's H2 multiplexing), and the new Qwen3.8 TLS block matches (2549-2550) — connection-lifetime tuning on the client→nginx leg, no logging or buffering change; `proxy_buffering off; proxy_cache off` (2334-2335, 2525-2526) and the `$uri`-only `log_format` still apply in both configs. The sibling asymmetry the base noted is resolved.

### INFO (deployed: OFF, engine-side residuals ARMED) — Qwen3.6's engine digest is audited: deployed logging flags genuinely consumed on the LLM path; the live residuals are the image page's (log)

Carried unchanged (image at 70, `--revision` 74, `--trust-remote-code` 96, `--log-requests-level 0` 97, `--context-length 262144` 87, `kernel_cache:/root/.cache/deep_gemm` 101). The [image page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (PRIVATE at deployed flags) documents the engine-side residuals — the watchdog dump (LOW above), unauthenticated `/configure_logging` (MEDIUM above), unguarded media fetch (text-only model here; the engine page's MEDIUM is for multimodal models on the image). Both replicas run from `cb9040a3`, whose anchor is line-identical.

### INFO (deployed: ON) — volumes: caches hold weights and config-keyed compile/JIT artifacts, not request-derived data; the one request-derived write is FLUX's, off-volume; one volume added (disk)

Named volumes (1543-1552): `otelcol_app_storage` (collector send-queue and checkpoint persistence, 1524 / 2126 / 2129-2131 — the one volume that can hold exported log records, including the FLUX prompt line, while awaiting dispatch), `hugginface_cache` (weights; written by both downloaders, 586 and 761), `vllm_cache`, `kernel_cache` (Qwen3.6 deep_gemm, 101), `compile_cache_glm51` (over all of `/root/.cache` for GLM, 948), **`qwen38_kernel_cache`** (1549, new — `/root/.cache/sglang` for Qwen3.8, 156; JIT artifacts keyed by shape per the engine page), `certs` (external, 1550-1552). Prefix caching remains GPU-only (no disk KV tier anywhere; `hierarchical` 0 hits). The request-derived write that exists is FLUX's image output (HIGH above), in the writable layer.

### INFO (deployed: ON) — registrar probes authenticate with `PROXY_TOKEN` over plaintext HTTP to the host-published ports; the shared registrar now covers eight ports, the Qwen3.8 registrar one (local hop)

Carried: shared script `INFERENCE_TOKEN="$${PROXY_TOKEN}"` (2167), ports `8002…8009` (2177), `check_chat()` (2219-2225) with the synthetic `"hi"` body, `check_models()` (2228-2232), dispatch (`8009` → `"zai-org/GLM-5.1-FP8"` at 2244), readiness on `:8002` (2255), eight `register_model` calls (2296-2303). The Qwen3.8 script mirrors it for `HOST_IP:8000` plus the `-k` TLS route check on `:8001` (INFO above). A credential on a CVM-internal hop, not user content.

---

## Sibling comparison (per-model privacy controls, current state)

| service (lines) | image | `--revision` | `--trust-remote-code` | media controls | request-log flags | watchdog pin |
|---|---|---|---|---|---|---|
| Qwen3.6-35B x2 (691, 718) | [`sglang:v0.5.16@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (70) | `95a723d0…` (74) | yes — pinned (96) | n/a (text) | `--log-requests-level 0` (97) — consumed | **none (300 s default)** |
| **Qwen3.8-27B x1 (807)** — new, temporary | [`sglang@febfb971`](/images/docker.io/lmsysorg/sglang/sha256-febfb971c7352570fc445c466ebd6ffc9d896024958e544a60f2137fd85856b1.md) nightly (130) | `017b9c7a…` (135) | yes — pinned (133) | **multimodal ON (140), NO allowlist, no `--disable-multimodal`** | `--log-requests-level 0` (153) — consumed | **none (300 s default)** |
| GLM-5.1 AWQ TP4 (937) | `glm51-sgl-awq-tp4-patched:local` build FROM [`sglang:dev-cu12@aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md) (225) + hash-pinned patch | `8f60817a…` (252) | yes — pinned (273) | n/a | `--log-requests-level 0` (260) — consumed | `1800` (268) |
| FLUX.2-klein (1017) | `sglang-diffusion` build FROM [`8ece90ad` — **LEAKS**](/images/docker.io/lmsysorg/sglang/sha256-8ece90ad52faa8b56149f0117227d9009db34513213e35990da468aeb6fe0b75.md) (178) + unpinned pip | **NONE** | **yes — UNPINNED** (187) | n/a | `--log-requests-level 0` (188) — **discarded by the engine** | none (untraced) |
| Qwen3-VL-30B (1075) | [`vllm-openai@ccd6a6db`](/images/docker.io/vllm/vllm-openai/sha256-ccd6a6dbf4aba4e94c6f7052d1835d6e742082b6a5095276552e9b7a5a47c2e5.md) (1077) | `d9748a51…` (1085) | no | **NEITHER set** (1083-1093, 42-50) | none (absent) | n/a |
| Qwen3-Embedding (1160) | same digest (1162) | `97b0c614…` (1166) | no | n/a | none | n/a |
| Qwen3-Reranker (1233) | same digest (1235) | `e61197ed…` (1239) | no | n/a | none | n/a |
| whisper-large-v3 (1306) | `vllm-with-audio` build FROM `ccd6a6db` + **`vllm[audio]` from PyPI** (1312-1313) | `06f233fe…` (1317) | no | n/a | none | n/a |
| privacy-filter (1383) | `privacy-filter-hf` build ([`pytorch` tag page](/images/docker.io/pytorch/pytorch/tag-2.5.1-cuda12.4-cudnn9-runtime.md), 301), app inline | `7ffa9a04…` (319, in code) | yes — pinned in code (366, 377, 410, 444, 452) | n/a | no content logging in app code (stderr writes are init-time strings/ints and numeric VRAM stats: 389-434, 461-464) | n/a |

gpt-oss-120b's row retires with the service. The asymmetries that matter now: FLUX remains the **only unpinned `--trust-remote-code`** model and the only one whose log flag is discarded; **two** media-ingesting models (Qwen3-VL, Qwen3.8) set no fetch control and the file contains no counterexample; among the four sglang LLM engines, GLM alone pins the watchdog while Qwen3.6 x2 and Qwen3.8 do not; and `proxy-qwen38-27b` alone caps request size (783).

## Not traced

- **The measured harness paired with this recipe** — not re-captured here. The [`d4c89033…`](/manifests/measured/sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) page (no Datadog agent, management-only collector filter, 22-entry `allowed_envs` with no logging switch, `dstack_default` network) is relied on for the "ships once" claim and the Datadog LOW's inert status; whether `allowed_envs` admits `WEB_CONTEXT_SEARCH_*`, `FUSION_*`, `MODEL_PROXY_TOKEN`, or `HUGGING_FACE_HUB_TOKEN` (now consumed by two downloaders) was not re-derived.
- **Whether the live action log still pins this `file_sha256`** — the orchestrator's capture of 2026-09-10 was hash-verified and re-derived here; the log was not re-fetched after that, and the fleet can redeploy mid-audit.
- **Container provenance beyond the log** — the deployment table trusts the signed action log's `services`/`commit`/`file_sha256` fields; the running containers' actual config was not inspected (no such surface exists for this review).
- **Built-image contents at deploy time** for the three mutable `build:` services and both downloaders' PyPI-resolved `huggingface_hub[hf_xet]` — build-day/run-day state is unrecorded. The privacy-filter's running build is additionally from an earlier revision of its own Dockerfile (deployment table).
- **Whether `/compose/up` env splices appear in the signed action log** — the entries carry `services` but no `env` field; compose-manager source pages' territory.
- **`vllm-proxy-rs` internals** at `98b57ad7` — deferred to its page, which was pending gating at the time of writing; in particular the catch-all's lack of an engine path allowlist is a residual for the admin-surface MEDIUM, not a mitigation.
- **The transient `27265f5a` proxy window** (2026-09-09T21:03Z-T23:47Z) — that digest's page is not re-read here; the deployment table only records it.
- **Whether the sglang diffusion entrypoint FLUX uses has a watchdog** — the `8ece90ad` page does not mention one; FLUX passes no `--watchdog-timeout`.
- **uvicorn/transformers framework logging inside privacy-filter** — untraced, as before; the new SDPA path's `transformers` warnings at load time were not enumerated.
- **Runtime warm-cache state** possibly shielding FLUX from HF-head drift.
- **Access controls on `telemetry.infra.near.ai`** — who reads the shipped logs is outside this file.
- **Intermediate upstream states** between the pins beyond the four the log deployed: the merge-side commits of `5046ffd`/`41e524b`/`85b6005` and the `prod/gpu13-qwen-handover.yaml` / `prod/migration-gpu-preflight.yaml` recipes the log shows running on 2026-09-08 (handover canary, drain, qualify services, since `compose_down`) carry their own paths and hashes and are not covered by this page.
- **`--enable-mixed-chunk` and `--max-mamba-cache-size` semantics on the `febfb971` build** — read as scheduler/memory flags by name; not traced in engine source here (the engine page reviewed the same command with the larger values).
- **`prod/qwen36-qwen38.yaml`** (the origin the comment names) has no audit page; the anchor comparison above used it and the audited `dsv4-qwen38-glm51@46edfb72` anchor, which agree with each other on every line that differs from this file.
