# cvm-compose-files prod/small-models.yaml @ cb9040a3... (delta review — Gemma-4 and two gpt-oss replicas out, a patched-build GLM-5.1 AWQ TP4 engine in; every prior finding carries forward)

## verdict: LEAKS — the FLUX diffusion service this file launches logs every user prompt at INFO and writes every generated image to disk, both unconditionally, and this file's own otelcol pipeline ships that prompt log to telemetry.infra.near.ai unredacted; the delta since the previous revision retires Gemma-4-31B and two gpt-oss replicas (cleanly, no dangling route, scrape target or registrar entry) and adds a GLM-5.1 AWQ TP4 engine built in-enclave from a digest-pinned sglang base plus a visible fail-loud weight-loader patch, launched with the minimum request-log level, a 1800 s watchdog, a pinned revision beside --trust-remote-code and no dump, file or trace flag, behind a proxy whose environment is line-identical to its eight siblings; the otelcol receivers, processors and exporter are byte-identical to the base, so the new engine's stdout joins the same unredacted off-box stream; every finding of the previous review carries forward, the Qwen3-VL media-fetch gap now stands with no in-file counterexample since Gemma-4 left, and the Qwen3.6 replicas remain the only sglang LLM engines in the file without a watchdog pin

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/small-models.yaml` |
| **file_sha256** | `cb9040a3e32a0e9157baf26b804d95a3ec55b91d9ae87383e0acd4300490078e` (re-verified locally: `shasum -a 256` of the captured 2339-line file ✓; the captured base file re-hashes to `ce39ea4c…` ✓) |
| **role** | log-pinned inner compose for the small-models nodes — per the orchestrator's fleet capture of 2026-09-01, **7 live hosts** (flux2-klein, gpt-oss-120b, privacy-filter, qwen3-embedding, qwen3-reranker, qwen3-vl-30b, whisper-large-v3; the base page counted 10 — the gemma-4-31b / qwen3-30b / qwen3-6-35b hostnames now attest a different recipe): **ten model containers behind nine `vllm-proxy-rs` instances**, 27 services in all |
| **identity** | log-pinned: the signed compose-manager action log pins `compose_up` of this path at compose-manager commit `7d631eb1267e3c7f2a63e34fdf5a66b438de38ca` (tag `v0.0.358`, first applied 2026-08-24T20:13Z, then re-applied per-service that evening for `model-sg-glm51-awq-tp4`, `model-proxy-registrar`, `nginx`, `otelcol-contrib`) with this `file_sha256`; the orchestrator fetched the file at that commit and hash-checked it (match) — **not** hardware-measured; the measured harness on these hosts is `compose_hash` [`d4c89033…`](/manifests/measured/sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md), relied on but not re-captured here (Not traced) |
| **supersedes** | [`ce39ea4c…`](/manifests/nearai/cvm-compose-files/prod/small-models/sha256-ce39ea4cfe0ddf4dc2e4fbb96b6c66ab9f72954444d5419b196d06940db5e297.md) (commit `1a7d6e548b19`) — **this is a delta review** against that page; the full enumeration and original evidence live on [`2443fde4…`](/manifests/nearai/cvm-compose-files/prod/small-models/sha256-2443fde49e26f83d931c443501544fa0c510c658360c45324190c3d243dfefa3.md) |
| **review** | reviewed 2026-09-01 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** an independent `diff -u` of the two captured files (base 2436 lines, this 2339; 697-line unified diff, 24 hunks, +171/-269 body lines) — **byte-identical to the orchestrator-supplied diff** — plus hashing both files. Thirty-three upstream commits lie between the pins; per the orchestrator's commit-title list those touching this file are "Trade gemma-4 and two gpt-oss replicas for a GLM-5.2 TP4 engine on gpu13", "Serve GLM-5.1 AWQ TP4 on the freed GPUs instead of GLM-5.2", "Place the GLM-5.1 engine on GPUs 3-6 so the rollout recreates nothing critical", plus docs commits; only the aggregate diff was reviewed, not the intermediate states. Consequence for citations: every base-page line number **at or below 190 holds verbatim**; everything after shifts by a varying offset (the new GLM block inserts, the removed services delete), so every citation below was re-read from the captured file rather than offset-computed. This is a DOCUMENT review: per-image behavior defers to the published image pages, and the GLM-5.1 service is compared against its origin recipe page ([`dsv4-qwen38-glm51@c27ed3b3`](/manifests/nearai/cvm-compose-files/prod/dsv4-qwen38-glm51/sha256-c27ed3b357f44673e4a84ef89f4625e1009de619d83ffdaae27c1265f676f4bb.md)) but re-verified at this file's bytes.

---

## What changed since ce39ea4c (complete delta)

The whole diff, grouped by change (24 hunks, all accounted for):

**A. Gemma-4-31B retired (−).** The `x-gemma-4-31b-common` anchor (base 193-238: image `vllm-openai@960ac5b3`, `--allowed-media-domains`, `VLLM_MEDIA_URL_ALLOW_REDIRECTS=0`, tile80 entrypoint), both engines `model-vllm-gemma4-31b-fp8-tp1-r1/-r2` (GPUs 5, 6), `proxy-gemma4-31b`, `dcgm-gemma4-31b`, the `gemma4_tile80_patch` config block, both `vllm-model-vllm-gemma4-31b-*` scrape jobs, the `dcgm-dcgm-gemma4-31b` and `inference-proxy-proxy-gemma4-31b` scrape jobs, the registrar's `8009` probe and `register_model "google/gemma-4-31B-it"`, the nginx `:8009` comment and the `gemma-4-31b.completions.near.ai` TLS block. **Sweep result:** `grep -ci gemma` = 0, `tile80` = 0 in this file — nothing dangles.

**B. Two gpt-oss replicas retired (−).** `model-vllm-gptoss-120b-mxfp4-tp1-r1` (GPU 2) and `-r2` (GPU 3) with their scrape jobs; `proxy-gptoss-120b`'s backend list collapses to one URL (587: `VLLM_BACKEND_URLS=http://model-vllm-gptoss-120b-mxfp4-tp1-r3:8000`); `dcgm-gptoss-120b` shrinks to GPU 0 (1261); comments updated (571, 609, 2197). `-r3` keeps its legacy name on GPU 0 (609-618). **Sweep:** `gptoss-120b-mxfp4-tp1-r[12]` = 0 hits.

**C. GLM-5.1 AWQ TP4 added (+).** Comment block 193-202 (copied from `prod/dsv4-qwen38-glm51.yaml`, 198-199; build required, 200-202); `x-downloader-common` 204-208 (`ghcr.io/astral-sh/uv` digest-pinned, 205); `x-glm51-awq-build` 213-238 (`dockerfile_inline`, `FROM lmsysorg/sglang:dev-cu12@sha256:aac6b242…` 215, inline Python patch 216-237); `x-glm51-awq-cmd` 239-264; `x-glm51-awq-model-env` 265-273; service `model-downloader` 492-506; `proxy-glm51` 738-772 (replaces `proxy-gemma4-31b` in nginx `depends_on`, 514); service `model-sg-glm51-awq-tp4` 774-818 (GPUs 3-6, 793); `dcgm-glm51` 1313-1346 (GPUs 3-6, 1321); volume `compile_cache_glm51` 1420; scrape jobs `sglang-model-sg-glm51-awq-tp4` 1566-1584, `dcgm-dcgm-glm51` 1705, `inference-proxy-proxy-glm51` 1774; registrar `8009) check_chat … "zai-org/GLM-5.1-FP8"` 2100 and `register_model "zai-org/GLM-5.1-FP8" "glm-5-1.completions.near.ai"` 2160; nginx `:8009` block 2245-2249 and the `glm-5-1.completions.near.ai` TLS block 2331-2339 — the latter carrying two directives no sibling block has, `keepalive_timeout 1h; keepalive_requests 1000000;` (2336-2337).

**D. GPU re-placement.** Qwen3.6 replica 2 moves GPU 4 → 2 (716); `dcgm-qwen36-35b-a3b` covers 1,2 (1291); comment 641. The four dcgm instances still cover all eight GPUs (0 / 1,2 / 3-6 / 7: 1261, 1291, 1321, 1356).

**E. Nothing else.** The otelcol config's receivers (1428-1507), processors (1902-1971), exporter (1973-1981), extensions (1983-1986) and pipelines (1988-2015) are outside every hunk — **byte-identical to the base**, `num_consumers: 1` (1979) included. The FLUX block (157-192), gpt-oss anchor (63-107), Qwen3.6 anchor (108-156), privacy-filter build (283-490), the Qwen3-VL / embedding / reranker / whisper services, nginx `log_format` and proxy settings (2167-2195), and the registrar's probe helpers (2074-2087) are untouched.

**Carry-over map** (every base finding; each re-read at this file's lines):

| base finding | status here |
|---|---|
| FLUX prompt log at INFO shipped off-box [CRITICAL] | **CONFIRMED** (177, 184, 53-61, 1-6, 1395, 1437, 1938-1947, 1974-1975, 1991-1994) |
| FLUX image-to-disk, never deleted [HIGH] | **CONFIRMED** (182-183) |
| FLUX `--trust-remote-code`, no `--revision` [HIGH] | **CONFIRMED** (170-180; 172, 176) |
| C3: app stdout leaves the box unredacted [HIGH] | **CONFIRMED, widened** — the GLM engine (801) and the downloader (506) join the kept set (1-6, 1395, 1437, 1465-1468, 1477-1480, 1938-1947, 1975) |
| three in-enclave `build:` blocks [HIGH, ARMED] | **CONFIRMED, changed shape** — the three mutable builds carry (167-169, 1167-1168, 291-296); a fourth `build:` (GLM, 213-238) resolves nothing mutable; a new PyPI-resolved downloader run (499) with write access to the shared weights volume (502) is added |
| Qwen3-VL unrestricted media fetch [MEDIUM] | **CONFIRMED, changed context** (929-939, 943 → 42-50) — the in-file counterexample (Gemma-4's 222/234) is **gone**; the file now has no service setting either control |
| `/compose/up` env-splice channel [MEDIUM, ARMED] | **CONFIRMED** (66-70; 106); the channel is now also documented as the build trigger for GLM (200-202) |
| `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` on all nine proxies [MEDIUM, ARMED] | **CONFIRMED** — nine proxies, `proxy-glm51` included (755-756; 590-591, 659-660, 838-839, 899-900, 990-991, 1066-1067, 1142-1143, 1224-1225) |
| dcgm-exporter tag-pinned + `SYS_ADMIN` x4 [MEDIUM] | **CONFIRMED** (276, 278-279; 1253/1283/1313/1348) — `dcgm-glm51` now spans four GPUs (1321) |
| `ipc: host` node-wide [MEDIUM] | **CONFIRMED** (12; GLM inherits via `<<: *nvidia`, 775) |
| `com.datadoghq.ad.logs` on all 29 services [LOW, latent] | **CONFIRMED, count changed** — 27 services now, all labeled (506 … 1408) |
| Qwen3.6 engine audited, flags consumed [INFO] | **CONFIRMED** (115, 142) |
| no metrics-to-file path [INFO, OFF] | **CONFIRMED** (140/142, 177/180, **and 250/262 for GLM**; no file/dump/trace flag anywhere) |
| proxies `user: root` + `privileged: true` [INFO] | **CONFIRMED** (31-32) |
| volumes inventory [INFO] | **CONFIRMED, one added** (1415-1423; `compile_cache_glm51` 1420 mounted over all of `/root/.cache`, 785) |
| registrar authenticated probes [INFO] | **CONFIRMED, 8009 target changed** (2022, 2024-2027, 2074-2080, 2078, 2083-2087, 2089-2103, 2100, 2114-2116) |
| nginx posture (no body logging/buffering) [INFO] | **CONFIRMED** (ports 522-533; `log_format` 2167-2180; `proxy_buffering off; proxy_cache off` 2191-2192; bare `location /` 2197-2249 and 2267-2339) — plus the GLM block's keepalive directives (2336-2337), see INFO below |
| `OHTTP_ENABLED=true` on all nine proxies | **CONFIRMED** (583, 652, 748, 831, 892, 983, 1059, 1135, 1217) |
| unauthenticated sglang admin surface via bare `location /` (folded into an INFO on the base page) | **CONFIRMED, broken out** as its own MEDIUM below — this file now runs three unauthenticated sglang LLM engines plus the diffusion one |

Nothing is REFUTED or GONE except the Gemma-4 rows of the sibling table, which retire with the service.

---

## Findings

Bodies are condensed for carried items; the [full-enumeration page](/manifests/nearai/cvm-compose-files/prod/small-models/sha256-2443fde49e26f83d931c443501544fa0c510c658360c45324190c3d243dfefa3.md) carries the original analysis. Every line number below was verified against this file.

### CRITICAL (deployed: ON) — FLUX logs every user prompt at INFO to stdout, and this file's otelcol pipeline ships it off-box (log + telemetry egress)

Carried; the delta does not touch any hop. The FLUX command (170-180) passes `--log-requests-level 0` (177), which the diffusion entrypoint **silently discards** (`parse_known_args`, per the [image page](/images/docker.io/lmsysorg/sglang/sha256-8ece90ad52faa8b56149f0117227d9009db34513213e35990da468aeb6fe0b75.md), verdict LEAKS: `SamplingBatch.log()` interpolates prompt and negative prompt verbatim into a `logger.info` on every generation). Nothing deployed suppresses it: `environment: *sglang-env` (184) → anchor 52-61 sets no diffusion log-level env, and the command sets no `--log-level`. This file completes the egress, every hop re-read here: the container logs via `json-file` (anchor 1-6, via `x-flux-common` ← `x-vllm-common` `logging: *logging-conf`, 27); otelcol-contrib mounts `/var/lib/docker/containers:ro` (1395) and its `filelog/docker_containers` receiver tails `/var/lib/docker/containers/*/*-json.log` (1437); the receiver's `regex_parser` lifts `model:` (1465-1468) and `deployment:` (1477-1480) from the FLUX container's `com.datadoghq.ad.logs` label (863) so `filter/app_logs` (1938-1947) — which drops only unlabeled records (1942) and five management services (1943-1947) — keeps it; the `logs/app` pipeline (1991-1994) runs `memory_limiter, resource, transform/docker_metadata, filter/app_logs, groupbyattrs/docker_service, batch` (1993) — **no redaction stage** — and exports via `otlphttp/gateway` (1994) to the literal `https://telemetry.infra.near.ai` (1975) with the `MONITORING_INGEST_TOKEN` bearer (1977). At the deployed flags nothing in this file can turn this off.

### HIGH (deployed: ON) — FLUX writes every generated image to disk inside the enclave and never deletes it (disk)

Carried unchanged. `save_output=True` is hardcoded in the image (image page); the FLUX service mounts only `hugginface_cache` and `vllm_cache` (182-183), so outputs accumulate in the container's writable layer across restarts. The CRITICAL log line prints the file path including the UUID that guards the image page's unauthenticated read-back endpoint, so the two compose. No flag in this file mitigates.

### HIGH (deployed: ON) — FLUX runs `--trust-remote-code` with no `--revision` pin: unpinned Hugging Face head code executes in the plaintext-prompt process (any sink)

Carried unchanged. `--model-path black-forest-labs/FLUX.2-klein-4B` (172) plus `--trust-remote-code` (176), no `--revision` anywhere in 170-180; `HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}` (55) defaults to live fetch. Still the only unpinned model in the file (sibling table below); the new GLM engine pins (242).

### HIGH (deployed: ON, systemic) — every app container's stdout leaves the box unredacted via otelcol-contrib, and the new GLM engine joins that stream (telemetry egress)

Carried, widened by the delta. All app services log via `json-file` (1-6); otelcol tails every container log (1395, 1437); the drop filter (1938-1947) exempts only management containers; and everything else ships to `https://telemetry.infra.near.ai` (1975). Processors (1902-1971) contain **no redaction stage**. The keep decision depends on the label: the receiver lifts `model:`/`deployment:` into attributes (1465-1468, 1477-1480) before the `model == nil and deployment == nil` drop (1942) runs. Two services enter the kept set in this revision: `model-sg-glm51-awq-tp4` (label 801, `model:QuantTrio/GLM-5.1-AWQ` + `deployment:small-models`) — so whatever the GLM engine ever prints (the [engine page](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md)'s `/configure_logging` switch-on path and the watchdog dump included) leaves the box — and `model-downloader` (506; prints download progress only, 498-500). The guarantee "no content leaves" still equals "no service ever prints content", and FLUX is the live counterexample. The harness facts relied on (no Datadog agent measured; app stdout ships once) come from the [`d4c89033…`](/manifests/measured/sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) page, not re-captured here.

### HIGH (deployed: ARMED) — three in-enclave `build:` blocks and a new in-enclave downloader resolve code from mutable sources at deploy time; `vllm[audio]` can replace whisper's pinned engine (any sink)

Carried, shape changed. The three mutable builds are untouched: (1) `sglang-diffusion` (FLUX): `FROM lmsysorg/sglang@sha256:8ece90ad…` (167) then unpinned `pip install -e "python[diffusion]"` (168) and `accelerate einops` (169). (2) `vllm-with-audio` (whisper, 1164-1168): `FROM vllm/vllm-openai@sha256:ccd6a6db…` (1167) then `RUN pip install openai-whisper torchaudio librosa vllm[audio]` (1168) — `vllm[audio]` re-resolves vLLM from PyPI over the pinned digest. (3) `privacy-filter-hf` (289-297): tag-pinned `FROM pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime` (291) plus unpinned pip installs (292-296); counter-weight unchanged — the inline app is pinned by this file's hash and pins `MODEL_REVISION = "7ffa9a04…"` (309, used 356, 367) beside `trust_remote_code=True` (360, 367).

**The fourth `build:` — GLM's (213-238) — is not in this class**: it resolves a digest-pinned `FROM` (215) and applies an inline patch from this hash-pinned file with no network fetch (see the GLM INFO below). **New in this class instead is `model-downloader`** (492-506): `uvx --from 'huggingface_hub[hf_xet]' hf download QuantTrio/GLM-5.1-AWQ --revision 8f60817a…` (499) resolves `huggingface_hub` and its extras from PyPI at run time, unpinned, inside the enclave, holding `HF_TOKEN` (504) and **read-write on the shared `hugginface_cache` volume** (502) that every engine loads weights from (e.g. 786, 23). It never sees plaintext itself, and the `--revision` pin defends against Hugging Face-side drift — but not against a writer on the volume: a compromised resolver could plant files under the pinned snapshot path that the GLM engine then loads with `--trust-remote-code` (263). Weights sidecars are out of this repo's scope for their function; this one is noted for that write capability. ARMED on any rebuild / any downloader run, not per-request; the action-log hash pins the recipe, not the resolved bytes.

### MEDIUM (deployed: ON) — Qwen3-VL fetches request-supplied media URLs with no domain allowlist and redirects enabled; the file no longer contains the counterexample (egress)

Carried; context changed. The Qwen3-VL command (929-939) has no `--allowed-media-domains` and its env (`*vllm-env`, 943 → 42-50) sets no `VLLM_MEDIA_URL_ALLOW_REDIRECTS`. On the base page the gap was visible by sibling comparison — Gemma-4 in the same file set both. Gemma-4 is gone with this revision (change A), so **no service in this file sets either control**; the asymmetry is now cross-recipe (the `dsv4-qwen36-gemma4` recipe still sets both). Per the [image page](/images/docker.io/vllm/vllm-openai/sha256-ccd6a6dbf4aba4e94c6f7052d1835d6e742082b6a5095276552e9b7a5a47c2e5.md), an unset allowlist means no check runs: request-driven arbitrary egress from a plaintext engine. Qwen3-VL is the only media-ingesting model left in the file.

### MEDIUM (deployed: ON) — the engines' unauthenticated sglang admin surface is reachable through allowlist-free nginx and proxies on the flat external network (log / egress)

Broken out from the base page's structural INFO at the severity the sibling GLM recipe page assigns it, because this file now runs **three** unauthenticated sglang LLM engines — Qwen3.6 x2 (678, 708; image page: CRITICAL unauthenticated `/configure_logging`) and GLM-5.1 (774; [engine page](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md): same CRITICAL) — plus the diffusion one. No `--api-key`/`--admin-api-key` on any command (grep-verified: the only `API_KEY` strings are the proxies' `WEB_CONTEXT_SEARCH_API_KEY`). nginx publishes nine plaintext host ports plus 8444 (522-533) and routes every listener with a bare `location /` (2200, 2206, 2212, 2218, 2224, 2230, 2236, 2242, 2248; TLS 2272 … 2338), so the full HTTP surface reaches the proxies; whether the proxy filters paths is the [proxy page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md)'s territory — a residual for the engines, not a mitigation. Laterally, every engine and proxy share the flat `dstack_default` network (1410-1413, `external: true`) with no egress allowlist. A single POST turns on full prompt+output logging (and disk dumps) with no restart and no attestation-visible change; the stdout finding above then ships it.

### MEDIUM (deployed: ARMED) — the `/compose/up` env-splice channel bypasses the measured `allowed_envs` without a measurement change (config channel arming egress)

Carried unchanged. The gpt-oss comment still documents `pass env={"GPT_OSS_CACHE_SUBPATH":"v3"} to /compose/up` (66-70; consumed at 106), and the GLM comment now documents the same endpoint as the build trigger (`pull -> build -> up`, 200-202). Whether splices appear in the signed action log remains unverified.

### MEDIUM (deployed: ARMED) — `WEB_CONTEXT_SEARCH_URL` + `_API_KEY` on all nine plaintext-terminating proxies, `proxy-glm51` included: an operator-suppliable egress URL (egress)

Carried unchanged in shape; the new proxy carries the pair (755-756) like the other eight (590-591, 659-660, 838-839, 899-900, 990-991, 1066-1067, 1142-1143, 1224-1225). Residual bounded by the audited proxy source ([`b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md)): fail-closed when unset, opt-in per request, model-generated query only.

### MEDIUM (deployed: ON) — dcgm-exporter x4 pinned by tag, not digest, with `SYS_ADMIN` + nvidia runtime: privileged bytes can drift under the attestation (telemetry, any sink under drift)

Carried unchanged in kind: `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (276) with `cap_add: SYS_ADMIN` (278-279), four instances (1253, 1283, 1313, 1348) covering all eight GPUs (1261, 1291, 1321, 1356). `dcgm-glm51` (1313) now spans the four GLM GPUs. See the [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md).

### MEDIUM (deployed: ON) — `ipc: host` on every engine and proxy, including the privacy-filter and the new GLM engine (cross-container shared memory)

Carried unchanged (`x-nvidia`, 12; GLM inherits via `<<: *nvidia`, 775). No `pid: host`, `SYS_PTRACE`, or docker-socket mount anywhere in this file (the proxies mount `/var/run/dstack.sock`, 37, as before).

### LOW (deployed: ARMED) — both Qwen3.6 replicas run the undisableable watchdog hang-dump at the engine's 300 s default while the new GLM engine pins 1800 s (log, feeding the telemetry egress)

Surfaced by the sibling comparison the new command makes explicit; not on the base page as a finding (it was an engine-page residual there). The Qwen3.6 command (116-143) passes no `--watchdog-timeout`; per the [`984699c2` image page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) that build's default dropped to **300 s** and the dump (full prompt and output token IDs of the in-flight batch, at ERROR, undisableable) is rated HIGH ON on the image page. The GLM command sets `--watchdog-timeout 1800` (258), matching what its [engine page](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md) reviewed, so the sglang default for `aac6b242` does not apply to it. Whether the diffusion entrypoint FLUX uses (170-180, no `--watchdog-timeout`) has a watchdog at all is not on its image page (Not traced). Severity at this layer follows the sibling recipe page: the manifest omits a pin that would widen the trigger window sixfold; the dump itself is the image page's.

### LOW (deployed: ARMED) — all 27 services carry `com.datadoghq.ad.logs` labels: stdout becomes third-party SaaS egress on any node whose harness measures a Datadog agent (egress, latent)

Carried; count 29 → 27. Every service — downloader 506, nginx 543, registrar 569, all nine proxies (593 … 1227, `proxy-glm51` 758), all ten model containers (623 … 1247, GLM 801), all four dcgm (1267, 1297, 1327, 1362), and otelcol-contrib itself (1408) — is labeled, and the `json-file` anchor forwards the label (6). Inert on the `d4c89033…` harness (no agent measured); pre-enrolled for double egress on any harness that measures one.

### INFO (deployed: OFF, engine-side residuals ARMED) — the GLM-5.1 AWQ TP4 addition: a digest-pinned base plus a visible weight-loader patch, minimum request-log level, pinned revision beside `--trust-remote-code`, 1800 s watchdog, no dump/file/trace flag, and a proxy line-identical to its siblings (log / disk)

The delta's substantive addition, read in full at this file.

**Build (213-238).** `dockerfile_inline` (214) starts `FROM lmsysorg/sglang:dev-cu12@sha256:aac6b242…` (215 — digest-pinned; [image page](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md), PRIVATE at deployed flags, reviewed as "base + this patch") and runs one `RUN python3 - <<'PYEOF'` step (216-237). **What the patch edits:** exactly one file, `/sgl-workspace/sglang/python/sglang/srt/models/deepseek_common/deepseek_weight_loader.py` (218). The regex (220) anchors on `cat_dim = 1` followed by `fused_weight = torch.cat(` and inserts a conditional that flips `cat_dim` when the `q_a_proj` and `kv_a_proj` weight shapes disagree along the non-cat axis (223-232); `subn(…, count=1)` with `assert n == 1` (234) makes any anchor drift a build failure; the file is rewritten in place (235) and a one-line marker printed at build time (236). **It touches no logging call, no file write beyond the source file it rewrites, no network, no env** — it is on the checkpoint-loading path, not the request path. No `pip install`, `git clone`, `curl`, or tag-resolved input anywhere in the block, so unlike the other three builds nothing mutable is resolved: the running image is the pinned digest plus this hash-pinned text.

**Command (239-264), every flag:** `sglang serve` (240) `--model-path QuantTrio/GLM-5.1-AWQ` (241) `--revision 8f60817aa28023f2607850d1a1e51d21aa34817a` (242) `--served-model-name zai-org/GLM-5.1-FP8` (243) `--tp 4` (244) `--dist-init-addr 127.0.0.1:29500` (245, loopback) `--disable-shared-experts-fusion` (246) `--kv-cache-dtype fp8_e4m3` (247) `--reasoning-parser glm45` (248) `--tool-call-parser glm47` (249) **`--log-requests-level 0`** (250; `--log-requests` itself absent, so request logging is OFF — the LLM serve branch parses strictly per the image page, unlike the diffusion one) `--mem-fraction-static 0.85` (251) `--max-running-requests 128` (252) `--max-queued-requests 16` (253) `--speculative-algorithm EAGLE` / `--speculative-num-steps 3` / `--speculative-eagle-topk 1` / `--speculative-num-draft-tokens 4` (254-257) **`--watchdog-timeout 1800`** (258) `--port 8000 --host 0.0.0.0` (259-260) `--enable-cache-report` (261) **`--enable-metrics`** (262; pull-based `/metrics`, scraped at 1566-1584) **`--trust-remote-code`** (263; pinned by 242, and the downloader pins the same SHA at 499). **Absent:** `--log-requests`, `--crash-dump-folder`, `--export-metrics-to-file`, `--enable-trace`, `--debug-tensor-dump-output-folder`, `--enable-hierarchical-cache`, `--api-key`, `--admin-api-key` (all grep-verified absent from the file). This is the same flag set the origin recipe page reviewed.

**Env (265-273):** `HF_TOKEN` (266), `HF_HUB_OFFLINE: ${HF_HUB_OFFLINE:-0}` (267, live-fetch default as elsewhere), `NVIDIA_DRIVER_CAPABILITIES`, `OPENBLAS_L2_SIZE`, `NCCL_DEBUG: WARN` (270), `SGLANG_ENABLE_SPEC_V2: "1"` (271), `TORCHINDUCTOR_CACHE_DIR` / `TRITON_CACHE_DIR` under `/root/.cache` (272-273). No `SGLANG_*` logging, profiler, dump, or storage variable; no log-level env.

**Service (774-818):** `<<: *nvidia` (775: `ipc: host`), `init: true`, waits on the downloader (777-779), `image: glm51-sgl-awq-tp4-patched:local` (780) / `build: *glm51-awq-build` (781), volumes `compile_cache_glm51:/root/.cache` and `hugginface_cache:/root/.cache/huggingface` (785-786), GPUs 3-6 (793), `logging: *logging-conf` (797 — json-file, so stdout enters the otelcol stream), labels with `model:` + `deployment:` (801). The compile volume mounts the **whole** `/root/.cache` (not just the two named cache dirs), so any future sglang write under that tree persists across recreation; at the deployed flags the engine page finds every disk-persistence feature off and no request-derived write, so it holds compile artifacts only.

**Proxy (738-772):** environment (741-756) is **line-identical to all eight sibling proxies** modulo `MODEL_NAME` (747) and the backend variable — verified by a programmatic diff of each proxy's `environment:` block: `OHTTP_ENABLED=true` (748), `TOKEN=${PROXY_TOKEN}` (749), `FUSION_*` (750-751), `TLS_CERT_PATH`, `USE_NV_ATTESTATION_SDK=true`, `WEB_CONTEXT_SEARCH_*` (755-756), `LOG_FORMAT=json` (746). The backend is a single `VLLM_BASE_URL=http://model-sg-glm51-awq-tp4:8000` (752) — the same single-backend form six other proxies use (835, 896, 987, 1063, 1139, 1221) versus the `VLLM_BACKEND_URLS` list form on gpt-oss and Qwen3.6 (587, 656). Same audited proxy digest via the anchor (30).

**Telemetry wiring:** the engine's `/metrics` is scraped by `sglang-model-sg-glm51-awq-tp4` (1566-1584, `metrics_path: /metrics`, labels only), `dcgm-glm51` by 1705, the proxy by 1774 — metrics pipelines carry counters, not content. The registrar probes `:8009` with the synthetic `"hi"` chat (2100 → 2074-2080) and registers `glm-5-1.completions.near.ai` (2160).

Residuals for this engine are the image page's: unauthenticated `/configure_logging` (MEDIUM above), the watchdog dump at 1800 s, and trust in the pinned `QuantTrio/GLM-5.1-AWQ` revision's Python.

### INFO (deployed: ON) — the retirements are clean: Gemma-4-31B and two gpt-oss replicas leave with their proxies, dcgm, nginx routes, otel scrape jobs and registrar entries, nothing dangles (structural)

Change A and B enumerated above; the full-file sweeps (`gemma` 0, `tile80` 0, `gptoss-120b-mxfp4-tp1-r[12]` 0) confirm no orphaned route, scrape target, label, or probe. The scrape-job inventory (1512-1900: 9 engine jobs, 4 dcgm, 9 proxy, 1 self) matches the service list one-to-one. The `gemma4_tile80_patch` config (a run-time kernel patch of the vLLM FA4 interface) is gone with the service; `configs:` now holds only `otelcol_app_config` (1426), `registrar_script` (2017), `nginx_conf` (2165). Removing two vLLM replicas and two Gemma engines removes four stdout sources from the telemetry stream and the only two services that set the media-fetch controls.

### INFO (deployed: ON) — the otelcol collector config is byte-identical to the base outside the scrape-job swaps: no receiver, processor, exporter, or attribute changed (telemetry)

Item-by-item against the diff: the only hunks inside `otelcol_app_config` (1426-2015) replace the four Gemma/gpt-oss scrape jobs with the three GLM ones (1566-1584, 1705-1722, 1774-1790) — `job_name`, `targets`, and static labels (`model`, `served_model`, `container_name`, `port`, `instance`) only. The OTLP receiver (1429-1434, `0.0.0.0:4317/4318`, `expose`d at 1391-1393) still feeds `metrics/otlp` (1999-2002) and `traces` (2007-2010) verbatim; the filelog receiver (1436-1507), `filter/app_logs` (1938-1947), `groupbyattrs` (1949-1966), `batch` (1968-1971), the `otlphttp/gateway` exporter with `num_consumers: 1` / `queue_size: 128` / `file_storage` (1974-1981), and `telemetry.logs.level: info` (2014-2015) are all unchanged. A new `served_model` label appears in the GLM scrape jobs and `nearai.otel.served_model` container labels (811, 1336) — a static string, not request content. No new attribute could carry request content.

### INFO (deployed: OFF, engine-side residuals ARMED) — Qwen3.6's engine digest is audited: deployed logging flags genuinely consumed on the LLM path; the live residuals are the image page's (log)

Carried unchanged (image at 115, `--log-requests-level 0` at 142, `--trust-remote-code` 141 pinned by 119). The [image page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (PRIVATE at deployed flags) documents the engine-side residuals — the watchdog dump now carried as the LOW above, unauthenticated `/configure_logging` as the MEDIUM above, unguarded media fetch.

### INFO (deployed: OFF) — no metrics-to-file path exists on any of the three sglang LLM/diffusion commands; the sglang metrics/logging coupling bug is not armed (disk)

Carried, one command added: `--enable-metrics` with `--log-requests-level 0` on Qwen3.6 (140/142), FLUX (180/177) and GLM (262/250); no `--export-metrics-to-file` or any file/dump/record flag anywhere in the file. Activation would need a command change, visible in the action log.

### INFO (deployed: ON) — the nine `vllm-proxy-rs` E2EE terminators run `user: root` + `privileged: true`; nginx's GLM TLS block adds long keepalives (structural)

Carried (31-32; `ipc: host` via 12; `OHTTP_ENABLED=true` on all nine, 583 … 1217). New: the `glm-5-1.completions.near.ai` TLS server block alone sets `keepalive_timeout 1h; keepalive_requests 1000000;` (2336-2337) — connection-lifetime tuning for the client→nginx leg, carrying no logging or buffering change; `proxy_buffering off; proxy_cache off` (2191-2192) and the `$uri`-only `log_format` (2167-2180, no body or query-string variable) still apply globally. Noted as a sibling asymmetry only.

### INFO (deployed: ON) — volumes: caches hold weights and config-keyed compile artifacts, not request-derived data; the one request-derived write is FLUX's, off-volume; one volume added (disk)

Named volumes (1415-1423): `otelcol_app_storage` (collector send-queue and checkpoint persistence, 1396 / 1981 / 1984-1985 — the one volume that can hold exported log records, including the FLUX prompt line, while awaiting dispatch), `hugginface_cache` (weights; now also written by the downloader, 502), `vllm_cache`, `kernel_cache` (Qwen3.6 deep_gemm, 146), **`compile_cache_glm51`** (1420, new — mounted over all of `/root/.cache` for GLM, 785, holding the torchinductor/triton dirs named at 272-273), `certs` (external). Prefix caching remains GPU-only (88, 933; no disk KV tier anywhere). The request-derived write that exists is FLUX's image output (HIGH above), in the writable layer.

### INFO (deployed: ON) — registrar probes authenticate with `PROXY_TOKEN` over plaintext HTTP to the host-published ports; the `:8009` probe now targets GLM-5.1 (local hop)

Carried: `INFERENCE_TOKEN="$${PROXY_TOKEN}"` (2022), fail-fast (2024-2027), `check_chat()` (2074-2080) with the synthetic `"hi"` body (2078), `check_models()` (2083-2087), dispatch (2089-2103, `8009` → `"zai-org/GLM-5.1-FP8"` at 2100), readiness header (2114-2116). A credential on a CVM-internal hop, not user content.

---

## Sibling comparison (per-model privacy controls, current state)

| service (lines) | image | `--revision` | `--trust-remote-code` | media controls | request-log flags | watchdog pin |
|---|---|---|---|---|---|---|
| gpt-oss-120b x1 (610) | [`vllm-openai@6766ce0c`](/images/docker.io/vllm/vllm-openai/sha256-6766ce0c459e24b76f3e9ba14ffc0442131ef4248c904efdcbf0d89e38be01fe.md) (65) | `b5c939de…` (85) | no | n/a (text) | none (vLLM opt-in, absent) | n/a |
| Qwen3.6-35B x2 (678, 708) | [`sglang:v0.5.16@984699c2`](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (115) | `95a723d0…` (119) | yes — pinned (141) | n/a | `--log-requests-level 0` (142) — consumed | **none (300 s default)** |
| **GLM-5.1 AWQ TP4 (774)** — new | `glm51-sgl-awq-tp4-patched:local` build FROM [`sglang:dev-cu12@aac6b242`](/images/docker.io/lmsysorg/sglang/sha256-aac6b242680daeb74d2ab1d85f70575357552d7d165d2e5d30eb362797db54a1.md) (215) + hash-pinned patch | `8f60817a…` (242) | yes — pinned (263) | n/a | `--log-requests-level 0` (250) — consumed | `1800` (258) |
| FLUX.2-klein (857) | `sglang-diffusion` build FROM [`8ece90ad` — **LEAKS**](/images/docker.io/lmsysorg/sglang/sha256-8ece90ad52faa8b56149f0117227d9009db34513213e35990da468aeb6fe0b75.md) (167) + unpinned pip | **NONE** | **yes — UNPINNED** (176) | n/a | `--log-requests-level 0` (177) — **discarded by the engine** | none (untraced) |
| Qwen3-VL-30B (918) | [`vllm-openai@ccd6a6db`](/images/docker.io/vllm/vllm-openai/sha256-ccd6a6dbf4aba4e94c6f7052d1835d6e742082b6a5095276552e9b7a5a47c2e5.md) (920) | `d9748a51…` (931) | no | **NEITHER set** (929-939, 42-50) — and no longer any in-file sibling that does | none (absent) | n/a |
| Qwen3-Embedding (1009) | same digest (1011) | `97b0c614…` (1015) | no | n/a | none | n/a |
| Qwen3-Reranker (1085) | same digest (1087) | `e61197ed…` (1091) | no | n/a | none | n/a |
| whisper-large-v3 (1161) | `vllm-with-audio` build FROM `ccd6a6db` + **`vllm[audio]` from PyPI** (1167-1168) | `06f233fe…` (1172) | no | n/a | none | n/a |
| privacy-filter (1243) | `privacy-filter-hf` build ([`pytorch` tag page](/images/docker.io/pytorch/pytorch/tag-2.5.1-cuda12.4-cudnn9-runtime.md), 291), app inline | `7ffa9a04…` (309, in code) | yes — pinned in code (360, 367) | n/a | no content logging in app code (stderr writes are numeric VRAM stats, 376-380) | n/a |

Gemma-4-31B x2 rows retire with the service. The asymmetries that matter now: FLUX remains the **only unpinned `--trust-remote-code`** model and the only one whose log flag is discarded; Qwen3-VL is the **only media-ingesting model** and sets no fetch control, with the in-file counterexample gone; and among the three sglang LLM engines, GLM pins the watchdog while both Qwen3.6 replicas do not.

## Not traced

- **The measured harness paired with this recipe on the 2026-09-01 capture** — not re-captured here. The [`d4c89033…`](/manifests/measured/sha256-d4c89033fb55cdac9db00c775fbbbeff6319fba7249344b2dca99b23ff048479.md) page (no Datadog agent, management-only collector filter, 22-entry `allowed_envs` with no logging switch, `dstack_default` network) is relied on for the "ships once" claim and the Datadog LOW's inert status; whether `allowed_envs` covers the new `HUGGING_FACE_HUB_TOKEN` consumer (the downloader, 504) or anything GLM-specific was not re-derived.
- **Whether the live action log still pins this `file_sha256`** — the orchestrator's fetch at commit `7d631eb1…` was hash-verified; the log was not re-fetched in this review, and the fleet can redeploy mid-audit.
- **Built-image contents at deploy time** for the three mutable `build:` services and the downloader's PyPI-resolved `huggingface_hub[hf_xet]` — build-day/run-day state is unrecorded. The GLM build's output is determined by this file plus the pinned digest, but the built image was not pulled and diffed here.
- **Whether `/compose/up` env splices appear in the signed action log** (compose-manager source pages' territory).
- **`vllm-proxy-rs` internals** — unchanged digest, deferred to its source page; in particular whether it path-filters engine admin routes (`/configure_logging`) — a residual for the MEDIUM above, not a mitigation.
- **Whether the sglang diffusion entrypoint FLUX uses has a watchdog** — the `8ece90ad` page does not mention one; FLUX passes no `--watchdog-timeout`.
- **uvicorn/transformers framework logging inside privacy-filter** — untraced, as before.
- **Runtime warm-cache state** possibly shielding FLUX from HF-head drift.
- **Access controls on `telemetry.infra.near.ai`** — who reads the shipped logs is outside this file.
- **Intermediate upstream states** between the pins (33 commits): only the aggregate diff was reviewed; a transiently deployed GLM-5.2 configuration referenced in the commit titles, if ever applied to a host, would carry its own `file_sha256` and is not covered by this page.
- **`--speculative-algorithm EAGLE` without an explicit draft-model path** on GLM (254-257): assumed to use the checkpoint's own MTP head at the pinned revision; not traced.
