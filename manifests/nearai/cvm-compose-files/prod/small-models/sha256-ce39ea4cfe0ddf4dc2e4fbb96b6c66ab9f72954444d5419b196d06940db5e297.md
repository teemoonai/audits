# cvm-compose-files prod/small-models.yaml @ ce39ea4c... (delta review — a one-line otel ordering change; every finding carries forward)

## verdict: LEAKS — the FLUX diffusion service this file launches logs every user prompt at INFO and writes every generated image to disk, both unconditionally, and this file's own otelcol pipeline ships that prompt log to telemetry.infra.near.ai unredacted; the only change since the previous revision is a one-line export-concurrency setting (num_consumers: 1) inside that same telemetry exporter, which alters delivery ordering and throughput of the leaking log and reduces or scrubs nothing — every finding of the previous review carries forward, the other twelve model services hold their prior qualified pass at deployed flags, and the unpinned-build and Qwen3-VL media-fetch gaps remain open

| | |
|---|---|
| **manifest** | `nearai/cvm-compose-files` — `prod/small-models.yaml` |
| **file_sha256** | `ce39ea4cfe0ddf4dc2e4fbb96b6c66ab9f72954444d5419b196d06940db5e297` (re-verified locally: `shasum -a 256` of both the captured file and `git show 1a7d6e548b19:prod/small-models.yaml` ✓) |
| **role** | log-pinned inner compose for the small-models nodes — per the fleet capture of 2026-08-21 the **whole small-models cluster, 10 of 20 live hosts** (flux2-klein, gemma-4-31b, gpt-oss-120b, privacy-filter, qwen3-30b, qwen3-6-35b, qwen3-embedding, qwen3-reranker, qwen3-vl-30b, whisper-large-v3): thirteen model containers behind nine `vllm-proxy-rs` instances |
| **identity** | log-pinned: fetched at commit `1a7d6e548b19` and hash-checked against the signed compose-manager action log's `file_sha256` — **not** hardware-measured; the measured layer is the node harness (`compose_hash`), whose pairing was not re-captured for this review (see Not traced) |
| **supersedes** | [`2443fde4…`](/manifests/nearai/cvm-compose-files/prod/small-models/sha256-2443fde49e26f83d931c443501544fa0c510c658360c45324190c3d243dfefa3.md) (commit `ed7dac33e50b`) — **this is a delta review** against that page |
| **review** | reviewed 2026-08-21 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage:** an independent `git diff ed7dac33e50b 1a7d6e548b19 -- prod/small-models.yaml` in a local clone, plus hashing both revisions (`ed7dac33e50b` → `2443fde4…` ✓, `1a7d6e548b19` → `ce39ea4c…` ✓) and the captured file (✓). Base file 2435 lines, this file 2436: **byte-identical except one inserted line at 2077**. Exactly one upstream commit touches this file between the pins: `33f0f80bf643` ("fix(otel): preserve model metric request order"), whose version of the file hashes to the same `ce39ea4c…`. Consequence for citations: every base-page line number **below 2077 holds verbatim** in this file, and every number **at/after 2077 shifts by exactly +1**; all load-bearing citations below were additionally re-read directly from the captured file. Per-image behavior defers to the published image pages, as on the base page.

---

## What changed since 2443fde4 (complete delta)

The entire diff between the two pinned revisions, restricted to this file:

```
@@ -2074,6 +2074,7 @@ configs:
           headers:
             Authorization: "Bearer $${env:MONITORING_INGEST_TOKEN}"
           sending_queue:
+            num_consumers: 1
             queue_size: 128
             storage: file_storage
```

One line: `num_consumers: 1` (2077) added to the `sending_queue` (2076-2079) of the otelcol-contrib `otlphttp/gateway` exporter (2072) — the exporter whose endpoint is the literal `https://telemetry.infra.near.ai` (2073) and which serves **every** pipeline: `logs/app` (2089-2092), `metrics/app`, `metrics/otlp`, `metrics/self`, and `traces` (2093-2108).

**What the setting does — and does not do.** `sending_queue.num_consumers` is the OpenTelemetry Collector exporterhelper's count of queue-consumer workers dispatching export requests (default 10). Setting it to 1 serializes dispatch: one batch in flight at a time, batches leave in queue order — matching the commit's stated intent of preserving metric request order. It is a **delivery concurrency/ordering knob only**. No receiver, processor, filter, transform, or redaction stage changed anywhere in the file (the diff above is exhaustive); `queue_size: 128` and the `file_storage` persistence are unchanged. The same records ship, to the same destination — including the FLUX prompt log of the CRITICAL finding below. The change strictly orders (and can only slow, never filter) delivery of the leaking log; it does not reduce or scrub what the pipeline ships, and it retires nothing.

**Sanity sweep:** because the independent diff shows the file byte-identical outside line 2077, no service command, environment, mount, label, build block, or nginx/registrar script changed — there is no new logging, dump, or debug switch anywhere in this revision.

**Carry-over map** (every base finding; line numbers re-verified in this file — all base citations below 2077 are verbatim, those at/after 2077 shift +1):

| base finding | status here |
|---|---|
| FLUX prompt log at INFO shipped off-box [CRITICAL] | **carries over unchanged** (177, 184, 53-61, 1-6, 1478-1479, 2036-2045, 2072-2073, 2089-2092) — the delta sits inside this leak's export path and changes only its delivery order |
| FLUX image-to-disk, never deleted [HIGH] | **carries over unchanged** (182-183) |
| FLUX `--trust-remote-code`, no `--revision` [HIGH] | **carries over unchanged** (170-180) |
| C3: app stdout leaves the box unredacted [HIGH] | **carries over unchanged** (1-6, 1478-1479, 2036-2045, 2073); pipeline block shifted +1 (2089-2092) |
| three in-enclave `build:` blocks [HIGH, ARMED] | **carries over unchanged** (167-169, 1189-1193, 253-261) |
| Qwen3-VL unrestricted media fetch [MEDIUM] | **carries over unchanged** (954-964 vs Gemma's 222/234) |
| `/compose/up` env-splice channel [MEDIUM, ARMED] | **carries over unchanged** (66-70; 106) |
| `WEB_CONTEXT_SEARCH_URL`/`_API_KEY` on all nine proxies [MEDIUM, ARMED] | **carries over unchanged** (e.g. 538-539) |
| dcgm-exporter tag-pinned + `SYS_ADMIN` x4 [MEDIUM] | **carries over unchanged** (240-243; 1286/1316/1346/1380) |
| `ipc: host` node-wide [MEDIUM] | **carries over unchanged** (12) |
| `com.datadoghq.ad.logs` on all 29 services [LOW, latent] | **carries over unchanged** (label anchor line 6; e.g. 491, 517, 541, 1432) |
| Qwen3.6 engine audited, flags consumed [INFO] | **carries over unchanged** (115, 142) |
| no metrics-to-file path [INFO, OFF] | **carries over unchanged** (140/142, 177/180; no file exporter anywhere) |
| proxies `user: root` + `privileged: true` [INFO] | **carries over unchanged** (31-32) |
| volumes inventory [INFO] | **carries over unchanged** (1439-1446) |
| registrar authenticated probes [INFO] | **carries over, shifted +1** (2120, 2122-2125, 2172-2179, 2181-2185, 2189-2199, 2212-2214; probe body still the synthetic `"hi"`, 2176) |
| nginx posture (no body logging/buffering) [INFO] | **carries over, shifted +1** (ports 471-481 unchanged; `log_format` 2266-2279, `proxy_buffering off; proxy_cache off` 2290-2291, `location /` blocks 2299-2347) |
| `OHTTP_ENABLED=true` on all nine proxies | **carries over unchanged** (531, 660, 758, 856, 917, 1008, 1084, 1160, 1242) |

---

## Findings

Bodies are condensed; the [base page](/manifests/nearai/cvm-compose-files/prod/small-models/sha256-2443fde49e26f83d931c443501544fa0c510c658360c45324190c3d243dfefa3.md) carries the full analysis. Every line number below was verified against this file.

### CRITICAL (deployed: ON) — FLUX logs every user prompt at INFO to stdout, and this file's otelcol pipeline ships it off-box (log + telemetry egress)

Carried over unchanged; the delta does not touch it. The FLUX command (170-180) passes `--log-requests-level 0` (177), which the diffusion entrypoint **silently discards** (`parse_known_args` — per the [image page](/images/docker.io/lmsysorg/sglang/sha256-8ece90ad52faa8b56149f0117227d9009db34513213e35990da468aeb6fe0b75.md), verdict LEAKS: `SamplingBatch.log()` interpolates prompt and negative prompt verbatim into a `logger.info` on every generation). Nothing deployed suppresses it: `environment: *sglang-env` (184) → anchor 53-61 sets no diffusion log-level env, and the command sets no `--log-level`. This file completes the egress: the container logs via `json-file` (anchor 1-6), otelcol-contrib tails `/var/lib/docker/containers/*/*-json.log` (1478-1479), `filter/app_logs` (2036-2045) drops only management containers — the FLUX container passes — and the `logs/app` pipeline (2089-2092) exports via `otlphttp/gateway` to `https://telemetry.infra.near.ai` (2072-2073). **No processor redacts content.** The new `num_consumers: 1` (2077) sits inside this exact exporter and changes only how the leaking records are dispatched (serialized, ordered), not whether they ship. At the deployed flags nothing in this file can turn this off.

### HIGH (deployed: ON) — FLUX writes every generated image to disk inside the enclave and never deletes it (disk)

Carried over unchanged. `save_output=True` is hardcoded in the image (image page); the FLUX service mounts only `hugginface_cache` and `vllm_cache` (182-183), so outputs accumulate in the container's writable layer, surviving restarts. The CRITICAL log line above prints the file path including the UUID that guards the image page's unauthenticated read-back endpoint, so the two compose. No flag in this file mitigates.

### HIGH (deployed: ON) — FLUX runs `--trust-remote-code` with no `--revision` pin: unpinned Hugging Face head code executes in the plaintext-prompt process (any sink)

Carried over unchanged. `--model-path black-forest-labs/FLUX.2-klein-4B` (172) plus `--trust-remote-code` (176), no `--revision` anywhere in 170-180; `HF_HUB_OFFLINE=${HF_HUB_OFFLINE:-0}` (55) defaults to live fetch. A push to the HF repo executes attacker-chosen Python inside the container holding plaintext prompts, with no attestation-visible trace. Still the only unpinned model in the file (sibling table below).

### HIGH (deployed: ON, systemic) — every app container's stdout leaves the box unredacted via otelcol-contrib (telemetry egress)

Carried over unchanged. All app services log via `json-file` (1-6); otelcol tails every container log (1478-1479), the drop filter (2036-2045) exempts only management containers, and everything else — thirteen engines, nine proxies, nginx, registrar, dcgm, privacy-filter — ships to `https://telemetry.infra.near.ai` (2073). Processors (2000-2070) contain **no redaction stage**. The keep decision is invisible from the filter alone: the filelog receiver's `regex_parser` operators lift `model:` (1507-1510) and `deployment:` (1519-1522) from the `com.datadoghq.ad.logs` label into attributes before the filter's `model == nil and deployment == nil` drop (2040) runs. The guarantee "no content leaves" equals "no service ever prints content" — the FLUX finding above is the live counterexample. The base page's harness facts (no Datadog agent measured; app stdout ships once) are relied on, not re-verified here (Not traced).

### HIGH (deployed: ARMED) — three in-enclave `build:` blocks resolve code from mutable sources at deploy time; `vllm[audio]` can replace whisper's pinned engine (any sink)

Carried over unchanged. (1) `sglang-diffusion` (FLUX): `FROM lmsysorg/sglang@sha256:8ece90ad…` (167, digest-pinned, audited LEAKS) then unpinned `pip install -e "python[diffusion]"` (168) and `accelerate einops` (169). (2) `vllm-with-audio` (whisper, 1189-1193): `FROM vllm/vllm-openai@sha256:ccd6a6db…` (1192) then `RUN pip install openai-whisper torchaudio librosa vllm[audio]` (1193) — `vllm[audio]` re-resolves vLLM from PyPI over the pinned digest. (3) `privacy-filter-hf` (253-261): tag-pinned `FROM pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime` (255) plus unpinned pip installs (256-260); counter-weight unchanged — the inline app is pinned by this file's hash and pins `revision="7ffa9a04…"` (273). The action-log hash pins the recipe, not the code; ARMED on any rebuild.

### MEDIUM (deployed: ON) — Qwen3-VL fetches request-supplied media URLs with no domain allowlist and redirects enabled (egress)

Carried over unchanged; the gap is still open. The Qwen3-VL command (954-964) has no `--allowed-media-domains` and its env sets no `VLLM_MEDIA_URL_ALLOW_REDIRECTS` — while Gemma-4 in this same file sets both (222, 234). Per the [image page](/images/docker.io/vllm/vllm-openai/sha256-ccd6a6dbf4aba4e94c6f7052d1835d6e742082b6a5095276552e9b7a5a47c2e5.md), an unset allowlist means no check runs: request-driven arbitrary egress from a plaintext engine.

### MEDIUM (deployed: ARMED) — the `/compose/up` env-splice channel bypasses the measured `allowed_envs` without a measurement change (config channel arming egress)

Carried over unchanged. The gpt-oss comment still documents `pass env={"GPT_OSS_CACHE_SUBPATH":"v3"} to /compose/up` (66-70; consumed at 106) — a second env channel outside `allowed_envs`. Whether splices appear in the signed action log remains unverified.

### MEDIUM (deployed: ARMED) — `WEB_CONTEXT_SEARCH_URL` + `_API_KEY` on all nine plaintext-terminating proxies: an operator-suppliable egress URL (egress)

Carried over unchanged (538-539 and the same pair on each of the other eight proxies). Residual bounded by the audited proxy source ([`vllm-proxy-rs@b183677a`](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md)): fail-closed when unset, opt-in per request, model-generated query only.

### MEDIUM (deployed: ON) — dcgm-exporter x4 pinned by tag, not digest, with `SYS_ADMIN` + nvidia runtime: privileged bytes can drift under the attestation (telemetry, any sink under drift)

Carried over unchanged: `nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` (240) with `cap_add: SYS_ADMIN` (242-243), four instances (1286, 1316, 1346, 1380). See the [tag page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md) — covers tag and config, never bytes.

### MEDIUM (deployed: ON) — `ipc: host` on every engine and proxy, including the privacy-filter (cross-container shared memory)

Carried over unchanged (`x-nvidia`, 12, inherited node-wide). No `pid: host`, `SYS_PTRACE`, or docker-socket mount anywhere in this file.

### LOW (deployed: ARMED) — all 29 services carry `com.datadoghq.ad.logs` labels: stdout becomes third-party SaaS egress on any node whose harness measures a Datadog agent (egress, latent)

Carried over unchanged: every service remains labeled for Datadog log autodiscovery (e.g. nginx 491, registrar 517, first proxy 541, otelcol itself 1432), and the `json-file` anchor forwards the label (6). Inert on a harness that measures no agent (base page's pairing); pre-enrolled for double egress on any harness that does.

### INFO (deployed: ON) — the one-line delta: `num_consumers: 1` serializes otel export dispatch; delivery ordering and throughput only — nothing shipped is reduced, filtered, or scrubbed (telemetry)

The only change in this revision (2077, inside `sending_queue` 2076-2079 of `otlphttp/gateway` 2072-2073). One queue-consumer worker instead of the default ten: batches export one at a time, in order — the commit's stated aim ("preserve model metric request order"). Because the exporter is shared, the serialization applies to the `logs/app` pipeline too, so the FLUX prompt log now ships strictly ordered and potentially slower under burst; `queue_size: 128` overflow behavior is unchanged. **Not a mitigation of anything** — it changes how the leaking log is delivered, not that it leaks.

### INFO (deployed: OFF, engine-side residuals ARMED) — Qwen3.6's engine digest is audited: deployed logging flags genuinely consumed on the LLM path; the live residuals are the image page's (log)

Carried over unchanged (image at 115, `--log-requests-level 0` at 142). The [image page](/images/docker.io/lmsysorg/sglang/sha256-984699c298a95b73c469b2191403ddc85fd780506e13c39c4afff3845e27bc6c.md) (PRIVATE at deployed flags) documents the engine-side residuals — unauthenticated `/configure_logging`, hang-time prompt dump, unguarded media fetch — reachable from the flat network this file joins; this file adds no flag that closes or opens them.

### INFO (deployed: OFF) — no metrics-to-file path exists; the sglang metrics/logging coupling bug is not armed (disk)

Carried over unchanged: `--enable-metrics` with `--log-requests-level 0` on both sglang services (140/142; 180/177), and no `--export-metrics-to-file` or any file/dump/record flag anywhere in the file. Activation would need a command change, visible in the action log.

### INFO (deployed: ON) — the nine `vllm-proxy-rs` E2EE terminators run `user: root` + `privileged: true` (structural)

Carried over unchanged (31-32; `ipc: host` via 12; `OHTTP_ENABLED=true`, e.g. 531). Plaintext by design; discipline is the [source page](/images/docker.io/nearaidev/vllm-proxy-rs/sha256-b183677a5d32267539b9b21ec45327a4f3be0a013afeb608c68c4d76e9472e36.md)'s weight. nginx's bare `location /` blocks (2299-2347) forward the full HTTP surface; flat `dstack_default` network, no egress allowlist.

### INFO (deployed: ON) — volumes: caches hold weights and config-keyed compile artifacts, not request-derived data; the one request-derived write is FLUX's, off-volume (disk)

Carried over unchanged (volumes 1439-1446; prefix caching GPU-only, 88 and 958; no disk KV tier). The request-derived write that exists is FLUX's image output (HIGH above), in the writable layer.

### INFO (deployed: ON) — registrar probes authenticate with `PROXY_TOKEN` over plaintext HTTP to the host-published ports (local hop)

Carried over, all citations shifted +1: `INFERENCE_TOKEN="$${PROXY_TOKEN}"` (2120), fail-fast (2122-2125), `check_chat()` (2172-2179), `check_models()` (2181-2185), dispatch (2189-2199), readiness header (2212-2214). Probe body remains the synthetic `"hi"` (2176) — a credential on a CVM-internal hop, not user content.

---

## Sibling comparison (per-model privacy controls, current state)

Unchanged from the base page — every cited line is below 2077 and verbatim in this file: gpt-oss-120b x3 (557-649, `--revision` 85), Qwen3.6-35B x2 (686-747, revision 119, `--log-requests-level 0` 142 — consumed), Gemma-4-31B x2 (784-845, revision 214, both media controls set 222/234), **FLUX.2-klein (882) — no revision, unpinned `--trust-remote-code` (176), log flag discarded (177)**, Qwen3-VL-30B (943, revision 956, **neither media control set**), Qwen3-Embedding (1034, revision 1040), Qwen3-Reranker (1110, revision 1116), whisper-large-v3 (1186, revision 1197, `vllm[audio]` build risk 1192-1193), privacy-filter (1268, revision pinned in code 273). The asymmetries stand: FLUX is the only unpinned `--trust-remote-code` model, and Qwen3-VL the only media-ingesting model without fetch controls while Gemma-4 sets both in the same file.

## Not traced

- **The measured harness paired with this recipe on the 2026-08-21 capture** — not re-captured here. The base page paired with harness `d4c89033…` (no Datadog agent, management-only collector filter, 22-entry `allowed_envs` with no logging switch, `dstack_default` network); the systemic-stdout "ships once" claim and the Datadog LOW finding's inert status rely on that pairing still holding. The harness page is authoritative.
- **Built-image contents at deploy time** for the three `build:` services — build-day PyPI/tag state is unrecorded.
- **Whether `/compose/up` env splices appear in the signed action log** (compose-manager source pages' territory).
- **`vllm-proxy-rs` internals** — unchanged digest, deferred to its source page.
- **uvicorn/transformers framework logging inside privacy-filter** — untraced, as before.
- **Runtime warm-cache state** possibly shielding FLUX from HF-head drift.
- **Access controls on `telemetry.infra.near.ai`** — who reads the shipped logs is outside this file.
- **Exporterhelper semantics of `num_consumers`** were taken from OpenTelemetry Collector documentation knowledge, not re-derived from collector source at the deployed collector digest; the claim relied on is only "consumer concurrency, not content transformation", which the config's own structure (a `sending_queue` key beside `queue_size`) corroborates.
