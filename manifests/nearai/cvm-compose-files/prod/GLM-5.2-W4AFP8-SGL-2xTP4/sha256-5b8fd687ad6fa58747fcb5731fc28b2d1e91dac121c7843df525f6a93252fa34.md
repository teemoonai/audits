# prod/GLM-5.2-W4AFP8-SGL-2xTP4.yaml @ 5b8fd687ad6f...

## verdict: QUALIFIED PASS — carried unchanged from f408de5c: the one-line delta (num_consumers: 1 on the telemetry sending queue) changes export-dispatch concurrency only; every logging flag stays literal and minimal, the two replicas stay byte-identical, and this is still the document that leaves the engine's unauthenticated logging switch reachable, with an unscrubbed stdout pipeline waiting downstream

| | |
|---|---|
| **file** | `prod/GLM-5.2-W4AFP8-SGL-2xTP4.yaml` |
| **file_sha256** | `5b8fd687ad6fa58747fcb5731fc28b2d1e91dac121c7843df525f6a93252fa34` |
| **serves** | `z-ai/glm-5.2` — near.ai's flagship — at `glm-5-2.completions.near.ai`, ctx 262144 |
| **topology** | two co-resident sglang TP4 replicas (GPUs 0-3 / 4-7) behind **one** `vllm-proxy-rs` load-balancing across both; nginx in front |
| **engine** | `lmsysorg/sglang@sha256:5027e95bf6ec…` (v0.5.14) x2 — [audited](/images/docker.io/lmsysorg/sglang/sha256-5027e95bf6ec536856b1b52a91d1f35ff5c564ab83e8a94758a169ff09bb8df3.md) — unchanged from base |
| **source** | `nearai/cvm-compose-files` @ `a141ecdf082e` — verified: the file at that commit hashes to this page's identity |
| **delta base** | [`f408de5c89d7…`](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-2xTP4/sha256-f408de5c89d7e7f8b4715b1f62409f67cadf52e232cfdce663f94482a134bba2.md) @ commit `005dda5f9165` — verified: that revision hashes to that page's identity |
| **runs on** | gpu26 measured harness `5c027cc86ab4…`, per the fleet capture of 2026-08-21 — **supplied by the orchestrating run, not re-derived here** (see Not traced) |
| **review** | reviewed 2026-08-21 (independent AI source review, Fable — method: [/notes/method.md](/notes/method.md)) |

**coverage — an explicitly-scoped delta review.** Verified independently: (1) `shasum -a 256` over the supplied file returns `5b8fd687ad6f…`, this page's identity; (2) `git show a141ecdf082e:prod/GLM-5.2-W4AFP8-SGL-2xTP4.yaml` hashes to the same value, so the pinned commit contains exactly these bytes; (3) `git show 005dda5f9165:…` hashes to `f408de5c89d7…`, the base page's identity; (4) `git diff 005dda5f9165 a141ecdf082e -- prod/GLM-5.2-W4AFP8-SGL-2xTP4.yaml` is **exactly one inserted line** (below), authored by commit `33f0f80b` ("fix(otel): preserve model metric request order", 2026-08-17), the sole intervening commit touching this file, applied fleet-wide as the same 1-line insertion across the recipes. The base page's full-file analysis therefore carries by byte-identity everywhere outside that one line; **every line number cited on this page was nonetheless re-verified directly against this 937-line file**, not computed from the shift. Like the base, this file is log-pinned only, not hardware-measured; whether the signed action log currently pins this `file_sha256` was established by the orchestrating run, not re-derived here.

---

# Delta Review — what changed, and what did not

> Method: [`/notes/method.md`](/notes/method.md) (§4 Manifest-only checks) · Base analysis: [`f408de5c…`](/manifests/nearai/cvm-compose-files/prod/GLM-5.2-W4AFP8-SGL-2xTP4/sha256-f408de5c89d7e7f8b4715b1f62409f67cadf52e232cfdce663f94482a134bba2.md)

## The diff, verbatim and complete

```
--- 005dda5f9165:prod/GLM-5.2-W4AFP8-SGL-2xTP4.yaml   (file_sha256 f408de5c89d7...)
+++ a141ecdf082e:prod/GLM-5.2-W4AFP8-SGL-2xTP4.yaml   (file_sha256 5b8fd687ad6f...)
@@ -678,6 +678,7 @@ configs:
           headers:
             Authorization: "Bearer $${env:MONITORING_INGEST_TOKEN}"
           sending_queue:
+            num_consumers: 1
             queue_size: 128
             storage: file_storage
```

The insertion lands at **line 681** of this file (base: 936 lines, this file: 937). Base-page citations at or above line 680 keep their numbers here; citations from base line 681 down shift **+1**.

**What `num_consumers: 1` is.** It sits inside the `otlphttp/gateway` exporter's `sending_queue` (lines 676-683: exporter at 676, `endpoint: https://telemetry.infra.near.ai` at 677, bearer header at 679, `sending_queue:` at 680, the new `num_consumers: 1` at 681, `queue_size: 128` at 682, `storage: file_storage` at 683). It is the exporter-helper sending-queue consumer count — how many workers drain the queue and dispatch batches to the exporter concurrently. `1` serializes dispatch: one in-flight export at a time, so batches leave in queue order — which is exactly what the authoring commit's title says it is for ("preserve model metric request order").

**What it does not change — verified against this file:**

- **Ingestion**: identical receivers — `filelog/docker_containers` tailing `/var/lib/docker/containers/*/*-json.log` (450, mount at 409, `user: "0:0"` at 396) and the two OTLP listeners (445, 447).
- **Filtering/processing**: identical — `transform/docker_metadata` (628-638), `filter/app_logs` (640-649, still dropping only the management containers and keeping both engines, the proxy, nginx, dcgm, the registrar), the `logs/app` processor chain verbatim at 695 — still **no scrubbing processor anywhere**.
- **Destination and auth**: identical — literal `https://telemetry.infra.near.ai` (677), `MONITORING_INGEST_TOKEN` bearer (679).
- **Pipelines**: identical wiring — `logs/app` 693-696, `metrics/app` 697-700, `metrics/otlp` 701-704, `metrics/self` 705-708, `traces` 709-712, all exporting to `otlphttp/gateway`.
- **Queue capacity and persistence**: identical — `queue_size: 128` (682), disk-backed at `file_storage` -> `/var/lib/otelcol-contrib/storage` (683, 685-688) on the `otelcol_app_storage` volume (410).

So the delta narrows to dispatch concurrency on the already-audited egress path. No new sink, no new destination, no ingestion or filter change, no privilege or network change anywhere in the file.

### INFO (deployed: ON) — the delta: num_consumers: 1 serializes telemetry export dispatch; no new sink, no new destination, no ingestion change (sink: telemetry)

Line 681, the only changed line. Privacy-relevant side effects are marginal and second-order: serialized dispatch slows queue drain, so under load, queued batches — which include full app-container log bodies (base Check 3, unchanged) — sit **marginally longer at rest in the disk-backed sending queue** (683, 687, volume at 410) before leaving; and a slower drain against the unchanged `queue_size: 128` (682) makes queue-full drops under burst somewhat likelier, which is a *reliability* cost, not a privacy one. Nothing new is collected, retained, or reachable. The collector image is unchanged and digest-pinned (`otel/opentelemetry-collector-contrib@sha256:85ac41c2…`, line 393 — [audited](/images/docker.io/otel/opentelemetry-collector-contrib/sha256-85ac41c2db88d0df9bd6145e608a3cb023f5d8443868adbfbbf66efb51087917.md)); the precise semantics of the field in that build were not source-traced here (see Not traced).

---

## Carried findings — re-verified and re-cited at this file's line numbers

The base page's analysis stands in full; each finding below was re-confirmed against this file (not assumed), with this file's line numbers. For the complete reasoning, see the base page — this section carries the findings, their severities, and their deployed states, all unchanged.

### CRITICAL (deployed: OFF, reachable — no containment at this layer) — the engine's unauthenticated /configure_logging switch remains reachable; what it would print feeds the unscrubbed off-box pipeline (sink: log -> telemetry egress)

Unchanged, re-verified: the shared `external: true` network `dstack_default` (424-427: `external: true` at 426, name at 427); nginx still a bare `location /` in both serving blocks (**900** for :80, **925** for :443 — base 899/924, shifted +1) proxying everything to `proxy-glm52:8000` (901, 926); engine port 8000 still unpublished to the host (only nginx `8000:80`/`8444:443` at 190-191 and dcgm `9400:9400` at 362 publish); still no `--admin-api-key`. Any co-resident container on the shared bridge can hit the engine's admin surface unauthenticated; any external caller with an accepted bearer can reach it through nginx -> proxy. The switch is not flipped at this configuration — **"OFF at deployed flags", emphatically not "cannot happen"** — and one unauthenticated POST flips this engine from logging nothing to logging every prompt and completion with no restart, no measurement change, and no action-log entry. The downstream pipeline that would carry it off-box is Check 3's, unchanged.

### HIGH (deployed: OFF, omission) — still no --admin-api-key anywhere: the one-flag gate for the engine's admin surface remains untaken (sink: log)

Re-grepped the whole file: the only `api.key`-shaped string remains `WEB_CONTEXT_SEARCH_API_KEY` on the proxy (247). One upstream-implemented flag would convert `/configure_logging` and the rest of the admin surface from open to bearer-gated; this revision — which did touch the file — did not take it.

### HIGH (deployed: ARMED) — the watchdog token-id dump keeps its fixed, unscrubbed exit path (sink: log -> telemetry egress)

`--watchdog-timeout 1800` at line 140, in the single command anchor (114-145) both replicas consume (`command: *glm52-cmd` at 273 and 318). The dump is undisableable by configuration; up to `--max-running-requests 96` in-flight requests' token ids per replica per fire; the stdout chain below ships whatever it prints. The file's own header still documents the hang-adjacent incident history (105, 109).

### HIGH (deployed: ON) — app-container stdout still ships unscrubbed to telemetry.infra.near.ai (sink: telemetry egress)

The full chain, re-verified: `x-logging-conf` retains up to ~1 GB of stdout per container on the CVM disk (`json-file`, `max-size: "100m"` at 67, `max-file: "10"` at 68); `filelog` tails every container log (450) via the read-only mount (409); `transform/docker_metadata` (628-638) and `filter/app_logs` (640-649) route and select — the filter still *keeps* precisely the plaintext-handling containers; the `logs/app` chain (695) contains **no redaction, hashing, truncation, or pattern-matching of any log body**; export is the literal `https://telemetry.infra.near.ai` (677). The manifest proves no content is collected only if no service prints content — the per-image source reviews remain load-bearing. Note: the base page's observation that the Datadog labels (69, 181, 201, 226, 249, 293, 336, 376, 422) ship nothing was keyed to the **gpu04** harness (`9385918d…`) starting no Datadog agent. This deployment runs on the **gpu26** harness (`5c027cc86ab4…`), and whether those labels stay inert there is that harness page's finding, not this one's — flagged under Not traced.

### HIGH (deployed: OFF, outside allowed_envs but inside the unmeasured overlay) — the two content-egress switches on the E2EE terminator are still interpolated and undefaulted (sink: egress)

Unchanged, re-verified: `FUSION_ENABLED`/`FUSION_INTERNAL_BEARER_TOKEN` (241-242) and `WEB_CONTEXT_SEARCH_URL`/`WEB_CONTEXT_SEARCH_API_KEY` (246-247) on the proxy — interpolated, no defaults, and (per the base analysis) outside the node's `allowed_envs`, leaving compose-manager's unmeasured `/compose/up` env overlay as the remaining route. Both features are additionally per-request opt-in at the proxy and default off. `OHTTP_ENABLED=true` remains a **literal** at 239 — the E2EE posture is still not operator-degradable through any env channel in this file.

### MEDIUM (deployed: ON, span-content latent) — two open, unscrubbed OTLP receivers on the shared bridge (sink: telemetry egress)

`0.0.0.0:4317` (grpc) and `0.0.0.0:4318` (http) at 445/447, `expose`d only (405-407) but on the shared `dstack_default` bridge, so any co-resident container can push telemetry into the pipeline. Still precisely scoped: the `otlp` receiver feeds only `metrics/otlp` (701-704) and `traces` (709-712), not the logs pipeline — a span-content risk, not a log-injection one.

### MEDIUM (deployed: ARMED) — /root/.cache persistence still outlives the container: the write target a runtime-enabled dump would inherit (sink: disk)

`compile_cache_a:/root/.cache` (277) and `compile_cache_b:/root/.cache` (320), named volumes declared at 432-433. The engine's `/configure_logging`-settable `dump_requests_folder` accepts an arbitrary path; anything written under `/root/.cache` survives container recreate and redeploy. Engines still hold **no KV or prompt disk cache** — KV is fp8 in GPU memory.

### MEDIUM (deployed: ON) — nginx still spills >1 MB request bodies to a temp file; ciphertext under OHTTP (sink: disk)

`client_max_body_size 100m` + `client_body_buffer_size 1m` at **898-899** (:80 block) and **923-924** (:443 block). Under OHTTP the body at nginx is the sealed blob; on a non-OHTTP path it would be plaintext. (The compiled-in `client_body_temp_path` remains unverified — a base hand-off, still open.)

### MEDIUM (deployed: ON) — one IPC namespace across all three plaintext-handling containers; the proxy is privileged root (sink: memory)

`ipc: host` in the `x-nvidia` anchor (71-78, the directive at 73), consumed by both engines and the proxy; the proxy anchor (80-91) is still `user: root` (82), `privileged: true` (83), applied at 230, mounting `/var/run/dstack.sock` (88) and `certs` read-only (89). Structural exposure, nothing configures abuse of it — carried at the base severities (the proxy privilege itself was INFO there).

### MEDIUM (deployed: ON, tag-drift latent) — dcgm-exporter remains tag-only pinned, with SYS_ADMIN and the nvidia runtime over all GPUs (sink: egress)

`nvcr.io/nvidia/k8s/dcgm-exporter:4.5.2-4.8.1-distroless` at 354 — still the only non-digest image — with `SYS_ADMIN` (358), `NVIDIA_VISIBLE_DEVICES=all` (360), `count: all` (368), publishing 9400 (362). [Tag-addressed page](/images/nvcr.io/nvidia/k8s/dcgm-exporter/tag-4.5.2-4.8.1-distroless.md).

### Carried INFO results, re-verified

- **No content-logging switch in the manifest**: `--log-requests-level 0` at 127 (below the code default of 2), via the shared anchor, on both replicas; no `--log-requests`; no dump/crash/profile/trace flag anywhere (the only "dump"/"crash" strings in the file are comments at 39 and 109); **zero `${VAR}` interpolation in the engine command** (verified: no match in 114-145).
- **Revisions pinned**: `--revision 8dc3a42a45cf2a6c47d241fd2c226afde73f1316` at **117** in the engine command, and the model-downloader pins the identical revision at 174 — `--trust-remote-code` (145) stays revision-pinned.
- **Replicas byte-identical in every privacy-relevant respect**: same command anchor (273/318), same env anchor (279/322), same image digest (271/316); the only differences remain `device_ids` (285: 0-3 / 328: 4-7), the compile-cache volume name (277/320), and the `instance` telemetry label (293/336).
- **Metrics are counters**: `prometheus/apps` (starting 522; proxy target at 582) scrapes `/metrics` — no content. **nginx access log is metadata**: `json_combined` (880-893) records `"uri":"$$uri"` (887) — not `$request`, so no query string — plus request-id/org/workspace headers, status, sizes, timings.
- **Engine env unchanged** (anchor 147-165): `HF_TOKEN` (148), `HF_HUB_OFFLINE: ${HF_HUB_OFFLINE:-0}` (149), `NCCL_DEBUG: WARN` (152), `SGLANG_ENABLE_SPEC_V2: "1"` (154), inductor/triton cache dirs (164-165). No debug/verbosity knob.

### Base-page citation errata found while re-verifying (file unchanged at these lines; the base page's numbers were off)

Re-verification against **both** revisions showed four small citation errors on the base page — the underlying claims are all correct: `--revision` is at line **117** in both revisions (base page said 118); the engine env anchor spans **147-165** (base said 147-164); the nginx body-size directives sit at **897-898 / 922-923 in the base revision** (base page said 896-897 / 916-917); and the base file is **936** lines, not the "all 937 lines" its coverage note states (937 is this file, after the insertion).

## Verdict

**QUALIFIED PASS, carried — the delta gives no reason to move it in either direction.** The one inserted line tunes how fast the telemetry queue drains, on a path whose ingestion, filtering, destination, and persistence are byte-identical to the audited base. Everything that earned the base its qualification is still true here, at the same severities and deployed states: the configured path writes no plaintext copy anywhere, and the document still declines every available containment — `--admin-api-key` absent, the engines on the shared `external: true` bridge (426-427), a bare `location /` (900, 925), and an unscrubbed stdout-to-off-box pipeline (695, 677). The base page's recommendations stand verbatim; this revision — proof the file is actively maintained — took none of them.

**Not traced (load-bearing):**

- **The signed action log itself.** That the log pins commit `a141ecdf082e` + this `file_sha256` for the gpu26 node was established by the orchestrating run; this review verified the git side (commit -> bytes -> hash) independently but did not re-fetch or re-verify the log.
- **The gpu26 measured harness** (`5c027cc86ab4…`) this recipe runs on — including whether it starts a Datadog agent. The base page's "the OTel exporter is the only measured off-box log destination" held because the **gpu04** harness ran no Datadog agent; on gpu26 that claim must come from the gpu26 harness page. If a Datadog agent is present there, the `com.datadoghq.ad.logs` labels on every service here (69, 181, 201, 226, 249, 293, 336, 376, 422) would additionally route engine stdout to `us3.datadoghq.com`.
- **`num_consumers` semantics in the deployed collector build** — described here from the collector's documented exporter-helper behavior and the authoring commit's stated intent; not source-traced in the (unchanged, digest-pinned, separately audited) image.
- **All base hand-offs, still open**: `vllm-proxy-rs@b183677a` path filtering (inherited from the `b0c5cd07` source review); how `WEB_CONTEXT_SEARCH_*` / `FUSION_*` obtain values and whether empty is fail-closed; nginx's compiled `client_body_temp_path`; whether sglang/proxy/nginx ever print request content at the configured levels; dcgm's effective NVML visibility under CC; guest-OS swap and core-dump policy ([`os/`](/os/)).
