# teemoonai/teemoon-ios @ v1.0.2 (21d534e)

## verdict: QUALIFIED-PASS — messages reach only the send path you chose; no new plaintext sink in the 38-file delta and a live session opened no endpoint the map does not name; keys leave the device only to their own provider, BUT the runtime pass found the near.ai key at rest in cleartext in the shared URL cache on disk (MEDIUM, present since 1.0, found by running the app, not by either source read) and the user-edited system prompt in UserDefaults inside backups (LOW); the on-device runtime's egress is still unobserved