# teemoonai/teemoon-ios @ v1.0.1 (0dfad8b)

## verdict: QUALIFIED-PASS (carried from 1.0) — zero app-code change from the reviewed 1.0 root; the 1.0 read and its findings, including the MEDIUM key-in-URL-cache found by running 1.0.2, apply unchanged

| | |
|---|---|
| **client** | teemoon iPhone app — App Store **1.0.1 (26)** |
| **identity** | tag `v1.0.1` → commit `0dfad8bb4733ea8ca8140c6b33acc2a67e66fa75` |
| **seen in** | App Store 1.0.1, 2026-09-01 |
| **source** | [github.com/teemoonai/teemoon-ios](https://github.com/teemoonai/teemoon-ios) |
| **build attestation** | none — see [`method.md` §5](../method.md#5-reproducing-the-scope-dont-trust-2s-table) |
| **review** | tree comparison 2026-09-10 against [`v1.0.0-2d9e206.md`](v1.0.0-2d9e206.md). Method: [`/client/method.md`](/client/method.md) |

**coverage:** this page is a delta with an empty shipping delta. It makes no
claim the 1.0 page does not make.

---

## Delta map: `2d9e206` → `0dfad8b`

`git diff --name-status 2d9e206 0dfad8b`, in full:

| file | change | bears on the question? |
|---|---|---|
| `teemoon.xcodeproj/project.pbxproj` | `MARKETING_VERSION` 1.0 → 1.0.1; `PRODUCT_BUNDLE_IDENTIFIER` gains a `$(BUNDLE_ID_SUFFIX)` hook so dev installs coexist with the App Store app (empty for the release build) | no — no target, entitlement, or capability change |
| `.github/workflows/test.yml` | CI runner and simulator selection | no — not shipped |
| `ATTESTATION.md`, `README.md`, `SECURITY.md` | docs | no |
| `teemoon/Assets.xcassets/AccentColor.colorset/Contents.json` | accent colour | no |
| `teemoonUITests/ProductE2ETests.swift` | UI test | no — not shipped |

**No Swift source in the app target or any package changed.** `Package.resolved`,
`Info.plist` and the entitlements are byte-identical to `2d9e206`. The
`BUNDLE_ID_SUFFIX` hook resolves to the empty string unless a developer sets it
at build time; the App Store bundle identifier is unchanged.

## Verdict: QUALIFIED-PASS (carried)

The bytes the 1.0 review read are the bytes this release ships. Every finding,
clean check, residual and *not traced* item on the 1.0 page applies verbatim —
including the MEDIUM (key in the URL cache) and LOW (system prompt in
UserDefaults) found by the 1.0.2 runtime pass.
